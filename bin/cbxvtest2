#!/bin/bash
#
# cbxvtest2 — Phase 3 A/B driver N (legacy X11 backend)
# compilation et edition de liens de xvtest2.f
# Created by Phase 03.1 — mirrors bin/cbmail link pattern without mail/lib.

cd $MEFISTO
if !(test -d pp)
then
  mkdir pp
fi

if test -f $MEFISTO/td/m/anglais
then
  LANGAGE=1
  LANGUE=ANGLAISE
  echo
  echo ========================================================================
  echo "MEFISTO-LINUX 64bits: Compilation f+OpenMp + Link Editor of xvtest2.f (X11)"
  echo ========================================================================
else
  LANGAGE=0
  LANGUE=FRANCAISE
  echo
  echo =============================================================================
  echo "MEFISTO-LINUX 64bits: Compilation f+OpenMp + Edition de liens de xvtest2.f (X11)"
  echo =============================================================================
fi

if !(test -f xvue/xvuelc.o)
then
  bin/ccxvue
fi

nompp=pp/ppxvtest2
if test -f $nompp
then
  rm $nompp
fi

echo "gfortran -Wall -mcmodel=large -m64 -O -fopenmp -I. $MEFISTO/prpr/xvtest2.f -o $nompp"
gfortran -Wall -mcmodel=large -m64 -O -fopenmp -I. prpr/xvtest2.f \
     util/lib xvue/lib xvue/xvuelc.o \
    -lgfortran -L/usr/X11R6/lib64 -lX11 -o $nompp

if test -f xvtest2.o
then
  rm xvtest2.o
fi

if test -f $nompp
then
  chmod 755 $nompp
  ls -l $MEFISTO/pp
  echo
  if [ $LANGAGE = 0 ]
  then
    echo "$MEFISTO/$nompp est CORRECT pour executer PHASE 3 A/B TEST 2 (X11)"
  else
    echo "$MEFISTO/$nompp is CORRECT to execute PHASE 3 A/B TEST 2 (X11)"
  fi
else
  if [ $LANGAGE = 0 ]
  then
    echo "$MEFISTO/$nompp est INCORRECT. PHASE 3 A/B TEST 2 (X11) est NON EXECUTABLE"
  else
    echo "$MEFISTO/$nompp is INCORRECT. PHASE 3 A/B TEST 2 (X11) is NOT EXECUTABLE"
  fi
fi
