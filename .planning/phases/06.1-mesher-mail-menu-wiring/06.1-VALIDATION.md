---
phase: 6.1
slug: mesher-mail-menu-wiring
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-20
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Content draft extracted from RESEARCH §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | QTest (Qt 6.10.2 Test module) |
| **Config file** | `xvue/qt/CMakeLists.txt` — `option(XVUE_QT_BUILD_TESTS "..." OFF)` gated via `bin/cbl_tout_qt` |
| **Quick run command** | `cd xvue/qt/build/tests && ./xvue_qt_mail_menu_tests` |
| **Full suite command** | `bin/cbl_tout_qt -DXVUE_QT_BUILD_TESTS=ON && for t in xvue/qt/build/tests/xvue_qt_*_tests; do "$t" || exit 1; done` |
| **Estimated runtime** | ~45 s full suite; mail-menu tests sub-second |

---

## Sampling Rate

- **After every task commit:** Run `cd xvue/qt/build/tests && ./xvue_qt_mail_menu_tests`
- **After every plan wave:** Run the full Qt test suite
- **Before `/gsd-verify-work`:** Full suite green + `bin/cbl_tout` (X11) also passes
- **Max feedback latency:** ~45 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-01-01 | 01 | 1 | UX-05 Success #1 | — | N/A (docs) | manual + schema | `python3 tools/validate_audit_md.py .planning/phases/06.1-*/LEXICON-AUDIT-mail.md` | ❌ W0 | ⬜ pending |
| 06.1-02-01 | 02 | 2 | UX-05 Success #2, #4 | T-6.0-BRIDGE-01 (queue cap) | `kMaxQueueSize=10000` already enforced in `queueLexicon` (6.0) | unit | `./xvue_qt_mail_menu_tests testMeshPointsCreate` | ❌ W0 | ⬜ pending |
| 06.1-02-02 | 02 | 2 | UX-05 Success #4 | — | SVG icon XXE disabled by default (Qt 6.5+) | lint | `sh xvue/qt/cmake/verify_shortcut_modifiers.sh xvue/qt/src && sh xvue/qt/cmake/verify_icon_source.sh xvue/qt/resources/icons` | ✅ | ⬜ pending |
| 06.1-03-01 | 03 | 3 | UX-05 Success #3 | — | No user input in path | integration | `bin/cbl_tout` (X11 build) succeeds after Fortran edit | ❌ W0 (stub first) | ⬜ pending |
| 06.1-03-02 | 03 | 3 | UX-05 Success #5 | — | N/A | unit | `./xvue_qt_mail_menu_tests testFileImport` + `testViewDrawMesh` + `testHelpAbout` | ❌ W0 | ⬜ pending |
| 06.1-03-03 | 03 | 3 | Non-regression, D-09 | T-closeEvent-race | `blockingDepth() > 0` gate | unit | `./xvue_qt_mail_menu_tests testCloseEventPushes99` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Created by the phase plans (not a separate Wave 0 plan):

- [ ] `xvue/qt/src/xvue_qt_mail_actions.cpp` — strong-symbol override of `registerMailActions_stub_` (Plan 02)
- [ ] `xvue/qt/src/xvue_qt_menu_file_parser.{h,cpp}` — bilingual label parser D-12 (Plan 02)
- [ ] `xvue/qt/resources/icons/mail/*.svg` — custom mesh icons (Plan 02)
- [ ] `xvue/qt/resources/xvue_icons.qrc` — append mail SVG entries (Plan 02)
- [ ] `xvue/qt/tests/test_xvue_qt_mail_menu.cpp` — 4 QTest cases per CONTEXT D-13 (Plan 03)
- [ ] `xvue/qt/tests/CMakeLists.txt` — register `xvue_qt_mail_menu_tests` target (Plan 03)
- [ ] `xvue/qt/CMakeLists.txt` — append new sources to `target_sources(xvueqt ...)` (Plan 02)
- [ ] `xvue/xvmodi.f` — Fortran no-op `xvue_module_init_` stub for X11 build (Plan 03, lands BEFORE ppmail.f edit)
- [ ] `prpr/ppmail.f` — add `CALL XVUE_MODULE_INIT('mail', 4)` at line ~172 (Plan 03)
- [ ] `xvue/qt/src/xvue_qt_window.cpp` — rewire `onFileQuit` + `closeEvent` per D-09 (Plan 03)
- [ ] `.planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md` — audit markdown (Plan 01)
- [ ] `tools/validate_audit_md.py` — 30-line schema validator (Plan 01 — optional helper)

Existing from 6.0: `verify_shortcut_modifiers.sh`, `verify_icon_source.sh`, `verify_abi.sh`,
`XvueMenuBridge::queueLexicon`, `XvueEventBridge::waitForEvent` pre-drain,
`registerMailActions_stub_` weak symbol.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Menu text in current language (FR/EN) | UX-05 + UX-08 | Requires observing QMenuBar labels; QTest can assert `QAction::text()` but visual match needs eyes | Launch `pp/ppmail_qt` in `$MEFISTOX/pan2d`; verify `{File, Mesh, View, Help}` render in the language selected by `td/m/anglais` flag |
| Mesh geometry menu leaf clicks reach Fortran prompt | UX-05 Success #5 (A/B) | QTest covers queue state; full Fortran roundtrip needs live xvsouris_ | Click `Mesh → Points → Create`; verify Fortran prompts for point name in console dock |
| `[menu] 1;1;` echo appears in console | D-07 | Visual observation of QPlainTextEdit | After clicking any leaf action, console dock shows `[menu] <lexicon-path>` on a new line |
| Close button pushes `99;` cleanly | D-09 | Involves process exit — can't easily be unit-tested end-to-end | Start `pp/ppmail_qt`, click window-X, verify Fortran runs save flow and process exits 0 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 45 s
- [ ] `nyquist_compliant: true` set in frontmatter after plan review

**Approval:** pending
