---
context: phase
phase: 06.1-mesher-mail-menu-wiring
task: 3
total_tasks: 3
status: phase_complete
last_updated: 2026-04-22T07:12:04.045Z
---

# Phase 6.1 Complete — Paused Between Phases

Phase 6.1 (mesher-mail-menu-wiring) finished on 2026-04-22 with 5/5 ROADMAP
success criteria verified. 16 commits delivered. Next phase 6.2
(elasticity-elas-menu-wiring) has NOT started.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Parallel X11 + Qt builds | `bin/cbl_tout` (X11) and `bin/cbl_tout_qt` (Qt) share `pp/` — running them in parallel corrupts `pp/` artifacts mid-link and produces fake "ppxvtestN INCORRECT" regressions | advisory | Always chain build commands with `&&`, never `&`. Each executor agent enforces this in its `<build_discipline>` block. Memory file `~/.claude/projects/-home-drico-git-mefisto/memory/feedback_parallel_builds_share_pp.md` covers the rule. |
| Anonymous-namespace stub linkage | Stubs placed in anonymous namespace have internal linkage — silently blocks weak-override pattern. Caught in Plan 02 Rule 3 auto-fix (commit `0dbdb57`) | advisory | In 6.2-6.5: `register<Mod>Actions_stub_` must be declared with `extern "C"` + external linkage + `__attribute__((weak))` at file scope (NOT inside `namespace { ... }`). Pattern set in 6.1 is copyable. |
| Archive-search drops strong override | GNU ld's archive-search stops at first symbol match; a strong override TU with no mandatory export gets silently dropped, reverting to the weak stub. Caught in Plan 03 Rule 3 auto-fix (commit `ea7c9df`) | advisory | In 6.2-6.5: each module actions TU must export a `xvue_qt_<mod>_actions_keepalive()` symbol called from `XvueApp::ensure()` so the linker is forced to pull the TU from the archive. Pattern set in 6.1 in `xvue/qt/src/xvue_qt_app.cpp::ensure()`. |

_No `blocking` anti-patterns — resuming agent can proceed directly._

<current_state>
Phase 6.1 complete at commit `61cd932`. All 3 plans executed, all 3 summaries
written, VERIFICATION.md status=human_needed (5/5 hard criteria PASSED, 6
visual items persisted in 06.1-HUMAN-UAT.md for post-phase manual review).
ROADMAP.md checkbox for Phase 6.1 is `[x]` with completion date 2026-04-22.
STATE.md advanced past 6.1.

No in-progress phase. Next phase is 6.2 (elasticity-elas-menu-wiring) — not
yet started, no phase directory, no context, no plans. Phase 6.2 mirrors 6.1
almost exactly except: top-level menu is `{File, Solve, View, Help}` instead
of `{File, Mesh, View, Help}`, and the lexicon audit catalogues elas/ not
mail/.
</current_state>

<completed_work>

Phase 6.1 tasks:
- **06.1-01**: Full recursive LIMTCL tree walk → 211-row 9-column LEXICON-AUDIT-mail.md (FROZEN, auto-approved) + `tools/validate_audit_md.py` schema validator. Commit chain `183e39e..3eb3d2e`.
- **06.1-02**: `xvue_qt_menu_file_parser.{h,cpp}` bilingual parser + `xvue_qt_mail_actions.cpp` strong-symbol body (19 flat QActions File/Mesh/View + 4-of-5 toolbar entries) + 10 mesh SVG icons + qrc append + CMake target_sources. Full Qt build green. Commit chain `2b988be..21d1a42`.
- **06.1-03**: `xvue/xvmodi.f` weak Fortran stub + `prpr/ppmail.f:175 CALL XVUE_MODULE_INIT('mail', 4)` atomic commit + `xvue_qt_window.cpp` D-09 closeEvent/onFileQuit rewrite + 5 QTest cases (4 required per D-13 + 1 D-09 bonus) + auto-approved A/B gate. Commit chain `c00db03..c6c6eac`.

Verification artifacts: `06.1-VERIFICATION.md` + `06.1-HUMAN-UAT.md` committed at `872a87f`. Phase completion committed at `61cd932`.
</completed_work>

<remaining_work>

Phase 6.2 (and beyond):
- **Phase 6.2** elas menu wiring — ~2-3 plans, same shape as 6.1. Reuses patterns established in 6.1. Not started.
- **Phase 6.3** flui menu wiring — depends on 6.2. Not started.
- **Phase 6.4** ther menu wiring — depends on 6.3. Not started.
- **Phase 6.5** nlse menu wiring — depends on 6.4. Not started.
- **Phase 7** image/GIF/postscript export. Not started.
- **Phase 8** A/B validation on testa subset. Not started.
- **Phase 9** retire X11 backend. Not started.

6.1 human-verification items (deferred, non-blocking):
- 6 visual/interactive items in `06.1-HUMAN-UAT.md` — close via `/gsd-verify-work 6.1` on a live desktop session.
- Pre-existing verification debt in phases 00 and 03.1 (3 items each) — review via `/gsd-audit-uat`.
</remaining_work>

<decisions_made>

6.1 decisions (see 06.1-CONTEXT.md D-01..D-14 for full list; these are the ones most likely to matter for 6.2+):

- **Top-level menus**: `{File, Mesh, View, Help}` for ppmail_qt (D-01 — Edit dropped). For 6.2: roadmap specifies `{File, Solve, View, Help}`. For 6.3-6.5 each phase picks its own top-level taxonomy.
- **Flat leaf QActions** (D-05) + **typed lexicon always available** (D-04 invariant from 6.0) — user can click OR type, both reach the same Fortran lexicon dispatch.
- **Top-5 toolbar** (D-06) — the audit's 5 highest-frequency leaves. For 6.1 mail: `{1;1; 2; 3; 10; 99;}`. For 6.2 elas audit will determine its own top-5.
- **Click → `[menu] 1;1;` echo to console dock** (D-07) — reusable across modules.
- **Bilingual label source**: runtime parse of `$MEFISTO/td/m/<menu>` files via `XvueMenuFileParser` (D-12, shared 6.0 infra + 6.1 parser TU). No per-module parser needed in 6.2-6.5.
- **closeEvent silent `99;` dispatch** with `XvueApp::blockingDepth()>0` gate (D-09) — already lives in `xvue_qt_window.cpp`. No per-module rewrite for 6.2-6.5.
- **9-column LEXICON-AUDIT schema** (D-10): `lexicon_path | description_fr | description_en | frequency | qaction | toolbar | icon_source | shortcut | notes`. `tools/validate_audit_md.py` enforces this across modules.
- **Auto-fixes established as reusable patterns** (see Infrastructure State below).
</decisions_made>

<blockers>
None. Phase 6.1 is cleanly complete; all commits pushed; working tree is in a buildable state.

Non-blocking deferrals:
- 6 human-UAT items in `06.1-HUMAN-UAT.md` await manual desktop testing.
- 6 pre-existing human_needed items in phases 00 (3) and 03.1 (3).
</blockers>

## Required Reading (in order)

1. `.planning/phases/06.1-mesher-mail-menu-wiring/06.1-CONTEXT.md` — 14 decisions (D-01..D-14) that shaped 6.1; most carry forward to 6.2-6.5 with small adjustments.
2. `.planning/phases/06.1-mesher-mail-menu-wiring/06.1-RESEARCH.md` — LIMTCL grammar, reachable tree sizing method, QTest pattern (Example 1), Fortran edit pattern (Example 3), deferred-items.md landmines (#1 sendEvent, #2 grab, #3 MMB-abort removal).
3. `.planning/phases/06.1-mesher-mail-menu-wiring/06.1-VERIFICATION.md` — what was verified and how; reference for 6.2 verifier.
4. `.planning/phases/06.1-mesher-mail-menu-wiring/06.1-03-SUMMARY.md` — force-link keepalive pattern + xvmodi.f weak stub + D-09 closeEvent details.
5. `.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-CONTEXT.md` — shared shell decisions (D-02..D-10) that all 6.1-6.5 phases inherit.
6. `.planning/ROADMAP.md §Phase 6.2` — goal + success criteria for the next phase.
7. `CLAUDE.md` — project Fortran norms + X11/Qt parallel-build prohibition.

## Infrastructure State

- **Fortran weak stub**: `xvue/xvmodi.f` defines `SUBROUTINE XVUE_MODULE_INIT` with `!GCC$ ATTRIBUTES WEAK`. Covers ALL modules (mail, elas, flui, ther, nlse) — no new Fortran stub needed for 6.2-6.5.
- **Force-link keepalive**: `xvue/qt/src/xvue_qt_app.cpp::XvueApp::ensure()` currently calls `xvue_qt_mail_actions_keepalive()` + `Q_INIT_RESOURCE(xvue_icons)`. For 6.2-6.5: add a keepalive call for each new module (`xvue_qt_elas_actions_keepalive()`, etc.) so GNU ld pulls the strong override TU from the archive.
- **Bilingual parser**: `xvue/qt/src/xvue_qt_menu_file_parser.{h,cpp}` is module-agnostic. Reused verbatim in 6.2-6.5.
- **LEXICON-AUDIT schema validator**: `tools/validate_audit_md.py` works for any `LEXICON-AUDIT-<mod>.md`. Same 9-column schema + [80, 250] row bound; toolbar=yes count must equal 5.
- **register<Mod>Actions_stub_ weak symbols**: All 5 module stubs (mail/elas/flui/ther/nlse) hoisted to external linkage with `__attribute__((weak))` in Plan 02 Rule 3 auto-fix. For 6.2: ship `xvue/qt/src/xvue_qt_elas_actions.cpp` as strong override — no api.cpp changes needed.
- **verify_abi.sh**: Extended in Plan 02 to filter `register[A-Z][a-zA-Z]*Actions_stub_` out of Fortran ABI count. Keeps count at 58 regardless of how many module stubs land.
- **QTest pattern**: `xvue/qt/tests/test_xvue_qt_mail_menu.cpp` + `xvue/qt/tests/CMakeLists.txt` register `xvue_qt_mail_menu_tests`. For 6.2: mirror with `test_xvue_qt_elas_menu.cpp` + `xvue_qt_elas_menu_tests` target.
- **Working tree**: clean-ish — build artifact dirties on `elas/lib`, `flui/lib`, etc. are normal symlink-timestamp drift; unrelated to 6.1 work.
- **Uncommitted**: `.planning/config.json` may have `workflow._auto_chain_active=true` from the chain; will be cleared by next manual invocation of any GSD command.

## Pre-Execution Critique Required

Not applicable — no design artifact awaiting execution. Phase 6.1 is fully executed and verified.

<context>
The user invoked `/gsd:discuss-phase 6.1` then chose auto-advance at both transition gates (discuss→plan and plan→execute). The full three-leg chain ran with `workflow.auto_advance=true`. Agent hit a usage limit mid-Plan-03 after Task 1's atomic commit landed on main (c00db03); resumed agent completed Tasks 2 and 3. All deferred-items.md landmines from 6.0 were respected — no postEvent/processEvents flakes, no MMB-abort branches. ABI count 58 preserved across all auto-fixes.

The three Rule-3 auto-fixes discovered during 6.1 (anonymous-namespace stub hoist, force-link keepalive, Fortran weak stub) should be treated as the canonical scaffold for 6.2-6.5. Each of 6.2/6.3/6.4/6.5 adds: one new `xvue_qt_<mod>_actions.cpp` TU + one `xvue_qt_<mod>_actions_keepalive()` call in `XvueApp::ensure()` + one LEXICON-AUDIT-<mod>.md + one test file + one `CALL XVUE_MODULE_INIT('<mod>')` in the corresponding `prpr/pp<mod>.f`. The elas-specific novelty in 6.2 is the `{File, Solve, View, Help}` taxonomy and the elas command frequency ranking.

Phase 6.1's 6 human-UAT items should eventually be closed — but they're visual/desktop-environment confirmations not reachable from headless QPA. Worth closing before the milestone v1.0 ships. No urgency between phases.
</context>

<next_action>
Start with: `/gsd-discuss-phase 6.2` — OR, given that 6.2 mirrors 6.1 almost exactly (only the module name + menu taxonomy differ), consider going straight to `/gsd-plan-phase 6.2` and skipping the discuss step. The CONTEXT.md for 6.2 will largely be a find-replace of 6.1's with `mail→elas`, `{File, Mesh, View, Help}→{File, Solve, View, Help}`, and a different lexicon audit. The three auto-fix patterns (weak Fortran stub, force-link keepalive, stub hoist) are already in place — 6.2 just adds one new TU and one new keepalive call.

Alternatively, close 6.1's deferred items first via `/gsd-verify-work 6.1` if you want a fully-signed-off phase before proceeding.
</next_action>
