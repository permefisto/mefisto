---
status: partial
phase: 06.1-mesher-mail-menu-wiring
source: [06.1-VERIFICATION.md, 06.1-03-AB-GATE-log.md]
started: 2026-04-20T20:45:00Z
updated: 2026-04-20T20:45:00Z
---

## Current Test

[awaiting human testing — auto-approved during --auto execution]

## Tests

### 1. Menu bar visually shows {File, Mesh, View, Help} with no Edit entry
expected: Launching `pp/ppmail_qt` in any MEFISTOX project renders the menu bar with exactly 4 top-level menus: File, Mesh, View, Help. No Edit menu appears (per CONTEXT D-01 — deliberate deviation from original roadmap text).
result: [pending]

### 2. Bilingual labels render per `$MEFISTO/td/m/anglais` flag
expected: When `$MEFISTO/td/m/anglais` exists → menu labels are English ("Points", "Lines", "Surfaces", …). When absent → labels are French ("Points", "Lignes", "Surfaces", …). Switch by creating/removing the file and restarting `pp/ppmail_qt`.
result: [pending]

### 3. Mesh → Points → Create reaches DFPOIN prompt
expected: Clicking `Mesh → Points → Create` (or the `1;1;` leaf QAction) causes the Fortran side to print the DFPOIN prompt in the console dock, same as typing `1;1;` at the prompt. Console dock shows `[menu] 1;1;` echo before the Fortran response.
result: [pending]

### 4. Window-close (X button) triggers clean exit via `[menu] 99;`
expected: Clicking the window-X button while Fortran is in a blocking xvsouris_ read causes `[menu] 99;` to appear in the console dock, Fortran's save loop runs (`9900 CALL ARRET(0)`), and the process exits 0. If Fortran is NOT in a blocking read, the window closes via QMainWindow::closeEvent directly.
result: [pending]

### 5. Typed `99;` equivalence with clicked close
expected: Typing `99;<CR>` at any Fortran prompt produces the same clean exit sequence as clicking the window-X or File → Quit. Both paths converge on the same Fortran save/exit flow.
result: [pending]

### 6. X11 regression: `echo "99;" | pp/ppmail` returns exit 0
expected: Running `cd $MEFISTOX/pan2d && echo "99;" | pp/ppmail` (no Qt, pure X11 backend) exits with code 0 after Fortran save. Proves the atomic Task 1 commit's X11 no-op stub linkage is sound.
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps

None yet. Items will be promoted to gaps only if manual testing reveals failures.

## Resolution path

To close these items, run `/gsd-verify-work 6.1` after manually testing each. Alternatively, review items 1-6 the next time you launch `pp/ppmail_qt` and mark them passed in this file.

## Context

Phase 6.1 ran under `/gsd-discuss-phase 6.1` → auto-advance chain → `/gsd-plan-phase 6.1` → `/gsd-execute-phase 6.1 --auto --no-transition`. The `--auto` flag auto-approved two human-verify gates (Plan 01 Task 3 audit FROZEN flip; Plan 03 Task 3 A/B gate). Automated verification covered the 5 ROADMAP success criteria (5/5 passed). These 6 items are the residual visual/interactive checks that could not run under offscreen QPA headless testing.
