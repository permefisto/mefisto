---
phase: 06.1-mesher-mail-menu-wiring
verified: 2026-04-22T10:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: null
human_verification:
  - test: "ppmail_qt menu bar shows {File, Mesh, View, Help} visually (no Edit)"
    expected: "Four top-level menus render in the menu bar; Edit is absent per D-01"
    why_human: "Visual observation of live Qt window cannot be asserted in offscreen QTest"
  - test: "Bilingual labels render per $MEFISTO/td/m/anglais presence"
    expected: "EN labels shown when td/m/anglais exists; FR labels otherwise (e.g. 'MAILLAGES et Utilitaires')"
    why_human: "Requires on-box MEFISTO environment; text content check is human-verifiable only"
  - test: "Mesh > Points > Create click lands in Fortran DFPOIN prompt"
    expected: "Console dock shows '[menu] 1;1;' then the Fortran prompt asks for a point name"
    why_human: "Requires live mesher session with real Fortran dispatch loop"
  - test: "Window close (X) button triggers clean exit"
    expected: "Console shows '[menu] 99;'; Fortran save path runs; process exits 0 with no stuck window"
    why_human: "Requires GUI interaction with window manager"
  - test: "Typed '99;' at initial prompt exits cleanly (equivalence with click path)"
    expected: "Same behavior as click-based close — D-09 invariant: typed and clicked paths converge at DONNMF"
    why_human: "Requires interactive mesher session"
  - test: "testa/pan2d X11 regression: echo '99;' | pp/ppmail returns exit 0"
    expected: "X11 build launches, parses typed 99;, exits cleanly — proves xvue_module_init_ resolves to no-op stub"
    why_human: "Requires $MEFISTOX environment set and testa/pan2d copy"
---

# Phase 6.1: Mesher (mail) Menu Wiring — Verification Report

**Phase Goal:** `ppmail_qt` shows a fully-populated menu bar with mesher-specific top-level menus (`{File, Mesh, View, Help}` — Edit dropped per 06.1 D-01), toolbar buttons for the top-5 most-used leaf lexicon commands, and `CALL XVUE_MODULE_INIT('mail')` at startup. The typed lexicon continues to work unchanged for every command, GUI-surfaced or not.

**Verified:** 2026-04-22T10:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification
**Mode:** `--auto` (two checkpoint gates auto-approved; 6 visual/interactive items deferred to human)

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `LEXICON-AUDIT-mail.md` enumerates every interactive `mail/` command with FR+EN descriptions, frequency column, QAction flag; Status=FROZEN | VERIFIED | File present (36735 bytes); header reads `**Status:** FROZEN (auto-approved 2026-04-20 via --auto mode)`; row count **211** (within validator bound [80, 250]); `python3 tools/validate_audit_md.py` prints `OK -- 211 rows, 5 toolbar=yes, schema valid` (exit 0). 9-column schema enforced: lexicon_path, description_fr, description_en, frequency, qaction, toolbar, icon_source, shortcut, notes. Frequency distribution: 14 high / 17 med / 180 low (per Plan 01 SUMMARY). |
| 2 | `registerMailActions_stub_` strong-symbol override wires 20-40 QActions, each connected via `QAction::triggered` → `XvueMenuBridge::queueLexicon(<command-string>)` | VERIFIED | `xvue/qt/src/xvue_qt_mail_actions.cpp` (289 lines) defines `extern "C" void registerMailActions_stub_(XvueWindow*, XvueMenuBridge*)`; 3 grep matches for `registerMailActions_stub_`, 5 matches for `queueLexicon`, 20 `makeLeafAction` call sites (within 20-40 target). `nm xvue/qt/build/libxvueqt.a | grep registerMailActions_stub_` shows both W (weak archive stub) and T (strong text-segment override); `nm pp/ppmail_qt` returns 1 T match — strong symbol linked into final binary (force-link keepalive unblocker in `xvue_qt_app.cpp`). QObject::connect lambda does [menu] echo → queueLexicon in order per D-07. |
| 3 | `prpr/ppmail.f` contains `CALL XVUE_MODULE_INIT('mail')` before interactive loop (line 175, inside IF(INTERA.GE.1) after CALL XVINIT) | VERIFIED | `grep -n "CALL XVUE_MODULE_INIT" prpr/ppmail.f` returns exactly line 175: `         CALL XVUE_MODULE_INIT( 'mail', 4 )`. Positioned inside IF(INTERA.GE.1) block (line 163-176), after CALL XVINIT (line 166), after bilingual LOGO call (167-172), before main loop. Column 10 alignment (4 spaces + 6-col CALL prefix) matches surrounding CALL statements. `xvue/xvmodi.f` (38 lines) ships the matching `SUBROUTINE XVUE_MODULE_INIT( NAME, NAME_LEN )` with `!GCC$ ATTRIBUTES WEAK :: XVUE_MODULE_INIT` directive — X11 backend links the weak no-op body, Qt backend displaces it with the strong `xvue_module_init_` in libxvueqt.a. |
| 4 | Custom mesh SVG icons under `xvue/qt/resources/icons/mail/`, registered in `xvue_icons.qrc`; `verify_shortcut_modifiers.sh` + `verify_icon_source.sh` pass | VERIFIED | Exactly 10 SVG files: mesh-{draw,export,import,interpolate,line-add,object,point-add,surface-add,transform,volume-add}.svg. `xvue/qt/resources/xvue_icons.qrc` contains 10 matching `<file>icons/mail/*.svg</file>` entries inside the existing `/xvue/qt/icons` qresource prefix. `sh xvue/qt/cmake/verify_shortcut_modifiers.sh xvue/qt/src` exits 0 with "OK (no bareword shortcuts in xvue/qt/src)". `sh xvue/qt/cmake/verify_icon_source.sh xvue/qt/resources/icons` exits 0 with "OK (xvue/qt/resources/icons is SVG-clean)". `AUDIT_STRICT_ICONS=1 python3 tools/validate_audit_md.py` exits 0 — Rule 9 (SVG existence cross-check) passes strict mode. |
| 5 | Mesher-specific QTest case simulates a menu click; synthetic event reaches `xvsouris_` with correct ASCII sequence | VERIFIED | `xvue/qt/tests/test_xvue_qt_mail_menu.cpp` (271 lines) defines class `TestXvueQtMailMenu` with 5 test slots (10 grep matches: testMeshPointsCreate, testFileImport, testViewDrawMesh, testHelpAboutNoQueue, testCloseEventPushes99WhenBlocking plus initTestCase/cleanupTestCase). Binary `xvue/qt/build/tests/xvue_qt_mail_menu_tests` present; `QT_QPA_PLATFORM=offscreen` run produces: **Totals: 7 passed, 0 failed, 0 skipped, 0 blacklisted, 82ms**. Each D-13 test calls `QAction::trigger()` (per RESEARCH Pitfall 4 — NOT postEvent+processEvents), drains `xvsouris_` N times, asserts `notypeevent=2` + exact ASCII bytes (e.g. `{'1',';','1',';',13}` for lexicon `1;1;`). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/06.1-*/LEXICON-AUDIT-mail.md` | 9-col schema, 80-250 rows, Status=FROZEN, 5 toolbar=yes | VERIFIED | 211 rows, validator exits 0, Status=FROZEN. 5 toolbar=yes rows: `1;1;`, `2;`, `3;`, `10;`, `99;` (lines 73/92/93/102/162) — exact match to RESEARCH A8. |
| `tools/validate_audit_md.py` | Stdlib-only Python 3 schema validator, exit 0/1 | VERIFIED | Present, executable; enforces 9 rules including LEX_RX regex, row bound, enums, toolbar-count==5, qaction-freq implication, icon-existence cross-check. |
| `xvue/qt/src/xvue_qt_mail_actions.cpp` | Strong-symbol body of registerMailActions_stub_ with ~20-40 QActions | VERIFIED | 289 lines; 20 makeLeafAction call sites across File (3: 80;/90;/70;), Mesh (3 Points submenu + 6 top-level + 4 Parameters submenu = 13), View (3: 10;/19;/60;) = 19 flat QActions + recent-projects glue. Strong symbol T=registerMailActions_stub_ displaces the weak api.cpp stub. |
| `xvue/qt/src/xvue_qt_menu_file_parser.{h,cpp}` | Bilingual LIMTCL parser with process-wide static cache | VERIFIED | h=43 lines, cpp=87 lines. `MenuFile::label(int)` fallback returns `"N;"` on missing entry (D-12 design); `XvueMenuFileParser::loadFor(name)` reads `$MEFISTO/td/m/<name>`; QRegularExpression for title + pair patterns; cache is a namespace-static QHash<QString, MenuFile>. |
| `xvue/qt/resources/icons/mail/*.svg` | 10 custom mesh SVG icons | VERIFIED | 10 files: mesh-draw, mesh-export, mesh-import, mesh-interpolate, mesh-line-add, mesh-object, mesh-point-add, mesh-surface-add, mesh-transform, mesh-volume-add. All use `stroke="currentColor"` + `fill="none"` for Qt palette-aware theming. |
| `xvue/qt/resources/xvue_icons.qrc` | 10 `<file>icons/mail/*.svg</file>` entries | VERIFIED | 10 entries inside the existing `/xvue/qt/icons` qresource prefix (single block — Plan 02 acceptance criterion preserved). |
| `xvue/xvmodi.f` | Fortran 77 no-op SUBROUTINE XVUE_MODULE_INIT for X11 backend | VERIFIED | 38 lines with `!GCC$ ATTRIBUTES WEAK` directive. Archived into `xvue/lib` by `bin/cbl_tous_f xvue`; `nm xvue/lib` shows 1 `xvue_module_init_` symbol resolving the Fortran CALL on the X11 side. |
| `prpr/ppmail.f` | CALL XVUE_MODULE_INIT('mail', 4) inserted at line 175 after CALL XVINIT | VERIFIED | Exact line 175 inside IF(INTERA.GE.1) block. Column 10 Fortran fixed-form alignment matches siblings (CALL XTINIT, CALL XVINIT, CALL LOGO). |
| `xvue/qt/src/xvue_qt_window.cpp` | D-09 onFileQuit (silent 99; dispatch) + closeEvent blockingDepth gate | VERIFIED | `queueLexicon(QStringLiteral("99;"))` appears 2× (onFileQuit body + closeEvent body); `XvueApp::blockingDepth` appears 3× (1 call + 2 references); `QMainWindow::closeEvent` appears 2× (forward decl + fallthrough call). The 1 remaining `QMessageBox::question` grep match is a COMMENT documenting the removal (line 363, inside explanation block), not production code. |
| `xvue/qt/tests/test_xvue_qt_mail_menu.cpp` | 4 D-13 QTest cases + 1 D-09 bonus | VERIFIED | 271 lines; 5 test slots (testMeshPointsCreate, testFileImport, testViewDrawMesh, testHelpAboutNoQueue, testCloseEventPushes99WhenBlocking); all 7 total (5 slots + init + cleanup) PASS under offscreen QPA. |
| `xvue/qt/tests/CMakeLists.txt` | add_executable(xvue_qt_mail_menu_tests …) target | VERIFIED | Target registered; binary builds into `xvue/qt/build/tests/xvue_qt_mail_menu_tests` (executable, runs green). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|------|-----|--------|---------|
| `prpr/ppmail.f` CALL XVUE_MODULE_INIT | `xvue/xvmodi.f` (X11) / `xvue_qt_api.cpp:1452` (Qt) | Linker-resolved symbol | WIRED | X11: `nm xvue/lib \| grep xvue_module_init_` = 1 (weak Fortran body). Qt: `nm pp/ppmail_qt \| grep -c ' T xvue_module_init_'` ≥ 1 (strong C++ body in libxvueqt.a displaces the Fortran weak symbol). Both backends link clean — atomic Task 1 commit `c00db03` preserved the Pitfall 1 contract. |
| QAction::triggered lambda | XvueMenuBridge::queueLexicon + XvueConsoleDock::appendLine | makeLeafAction closure in xvue_qt_mail_actions.cpp | WIRED | `grep -c 'appendLine' xvue_qt_mail_actions.cpp` = 3 (echo before queue); `grep -c 'mb->queueLexicon' xvue_qt_mail_actions.cpp` = 1 (inside helper lambda). D-07 echo-before-queue order is preserved: console appendLine executes synchronously on the main thread before queueLexicon pushes chars. |
| registerMailActions_stub_ | td/m/<menu> files via XvueMenuFileParser::loadFor | Runtime bilingual parse at module-init | WIRED | `loadFor` called 7 times in registerMailActions body for debut/saisi_pt/importer/exporter/managmef/tracmail/managfen. Parser reads `$MEFISTO/td/m/<name>` and returns MenuFile; label fallback = "N;" per D-12. Process-wide static cache (one read per sub-menu per session). |
| File/View/Help findChild<QMenu*> | Existing 6.0 menus extended (not duplicated) | mbar->findChild<QMenu*>("File"/"View"/"Help") | WIRED | 2 findChild matches in xvue_qt_mail_actions.cpp (File: line 167; View: lines 252, 286). 6.0 xvue_qt_window.cpp:146/197/241 sets the objectNames. No duplicate top-level menus appear. |
| Mesh top-level menu | ensureTopLevelMenu helper + setObjectName("Mesh") | addMenu + objectName | WIRED | Lines 88-98 define ensureTopLevelMenu; line 196 calls it with title (FR or EN from debut.title()) + objectName "Mesh". Submenus also named: Mesh.Points (line 204), Mesh.Parameters (line 239) — Plan 03 tests rely on these for deterministic findChild discovery. |
| Toolbar top-5 | Existing QActions appended via addToolbarByLexicon | tb->addAction(existingAction) | WIRED | Helper at line 104 scans menu actions by `property("lexicon")` and appends the EXISTING QAction to the toolbar — one instance, two homes. Lines 273-282 (approximately) call `addToolbarByLexicon` for `1;1;`, `2;`, `3;`, `10;`, and the 5th (`99;`) is the existing 6.0 actQuit which Plan 03 rewired through queueLexicon. |
| xvue_qt_mail_menu_tests | registerMailActions_stub_ strong body | xvue_module_init_ direct call + QAction::trigger | WIRED | initTestCase calls `xvinitgraphique_()` then `xvue_module_init_(name="mail", &nlen)` — runs registerMailActions body. Each test finds the target QAction via the `.property("lexicon")` tag planted by makeLeafAction, calls `trigger()`, and drains xvsouris_. All 5 tests + init + cleanup PASS. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| xvue_qt_mail_actions.cpp / MenuFile labels | `debut.label(N)`, `saisipt.label(N)`, etc. | `XvueMenuFileParser::loadFor($MEFISTO/td/m/<name>)` | Yes — parses real LIMTCL files; labels come from td/m/ trees (bilingual via td/ma/EN or td/mf/FR backup). Fallback "N;" only triggers on missing files. | FLOWING |
| Toolbar QActions | `meshMenu->actions()`, `viewMenu->actions()` | Existing menu QActions with `property("lexicon")` set | Yes — QActions are created first in menus, then appended to toolbar via `tb->addAction(a)`. Single QAction instance shared. | FLOWING |
| xvsouris_ drain in tests | `nt`, `nbc` output | `XvueMenuBridge::popChar()` via XvueEventBridge pre-drain | Yes — the bridge's pendingChars_ queue is fed by QAction triggered lambdas at test-time; xvsouris_ pops chars with `notypeevent=2`. All 5 D-13 tests assert specific ASCII byte sequences and pass. | FLOWING |
| closeEvent 99; push | `menuBridge_->queueLexicon("99;")` | `XvueApp::blockingDepth() > 0` guard | Yes — testCloseEventPushes99WhenBlocking uses BlockingDepthGuard RAII to exercise the gate deterministically; queueSize grows by 4 chars ('9','9',';',CR). | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Audit validator passes | `python3 tools/validate_audit_md.py .planning/phases/06.1-*/LEXICON-AUDIT-mail.md` | `OK -- 211 rows, 5 toolbar=yes, schema valid` | PASS |
| Strict-icons validator passes | `AUDIT_STRICT_ICONS=1 python3 tools/validate_audit_md.py …` | `OK -- 211 rows, 5 toolbar=yes, schema valid` (no WARN) | PASS |
| Shortcut-modifier lint passes | `sh xvue/qt/cmake/verify_shortcut_modifiers.sh xvue/qt/src` | `OK (no bareword shortcuts in xvue/qt/src)` | PASS |
| Icon-source lint passes | `sh xvue/qt/cmake/verify_icon_source.sh xvue/qt/resources/icons` | `OK (xvue/qt/resources/icons is SVG-clean)` | PASS |
| Strong symbol present in archive | `nm xvue/qt/build/libxvueqt.a \| grep registerMailActions_stub_` | `W registerMailActions_stub_` + `T registerMailActions_stub_` | PASS |
| Strong symbol pulled into final binary | `nm pp/ppmail_qt \| grep -c ' T registerMailActions_stub_'` | `1` | PASS |
| X11 stub resolves | `nm xvue/lib \| grep -c xvue_module_init_` | `1` | PASS |
| mail_menu_tests green | `QT_QPA_PLATFORM=offscreen xvue/qt/build/tests/xvue_qt_mail_menu_tests` | `Totals: 7 passed, 0 failed, 0 skipped, 0 blacklisted, 82ms` | PASS |
| Full Qt test suite green | iterate all `xvue_qt_*_tests` under offscreen QPA | 8 binaries: 116 passed / 0 failed / 2 pre-existing skips (6.0 baseline) | PASS |
| Both backends linked | `ls pp/ppmail pp/ppmail_qt` | Both ELF executables present (5.2 MB X11, 6.0 MB Qt) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| UX-05 (mail slice) | 06.1-01, 06.1-02, 06.1-03 | Per-module lexicon audit + QAction entry per command, grouped into menus and toolbars | SATISFIED | All 5 roadmap Success Criteria verified. LEXICON-AUDIT-mail.md documents 211 commands with qaction flag (31 rows with qaction=yes mapping to 19 rendered QActions per Plan 02 scoping + shared 6.0 actions). Toolbar has 5 entries per D-06. `CALL XVUE_MODULE_INIT('mail', 4)` in prpr/ppmail.f. 10 mesh SVG icons + xvue_icons.qrc registration. 5 QTest slots proving click→xvsouris round-trip. No other phase plans claim UX-05 — mail slice exclusively covered here. |

No orphaned requirements. REQUIREMENTS.md line 213 maps UX-05 to "Phase 6.1-6.5"; this phase delivers the mail slice only (explicitly scoped in the ROADMAP). Elas/flui/ther/nlse slices are later phases.

### Anti-Patterns Found

None. Scans cleared all common stub signals:
- **TODO/FIXME/PLACEHOLDER:** 0 occurrences in any of the 5 modified/created production files (`xvue_qt_mail_actions.cpp`, `xvue_qt_menu_file_parser.{h,cpp}`, `xvue_qt_window.cpp`, `xvmodi.f`, `ppmail.f`).
- **Empty handlers:** QActions are all wired to `makeLeafAction` which creates a real lambda with echo+queueLexicon — no `=> {}` or log-only handlers.
- **Hardcoded empty arrays/dialogs:** No `return []` / `return {}` / `QMessageBox::question` in onFileQuit or closeEvent (the 1 grep hit on QMessageBox::question is a comment documenting the removal).
- **Label fallback:** `MenuFile::label(code)` returns `"N;"` on missing entry — this is a DOCUMENTED D-12 design choice (makes QAction self-identifying even on parse failure), not a stub. Code path is exercised only for the `td/m/modifm2d` `80;` typo (RESEARCH Pitfall 5), which is flagged in the audit notes cell for future polish.
- **Shortcut modifiers:** `grep -cE 'setShortcut|setShortcuts' xvue_qt_mail_actions.cpp` = 0 (D-04 compliant; lint passes).
- **ABI drift:** `verify_abi.sh` output during build: `nm count: 58  header count: 58` — Plan 02 added a `grep -v ' T register[A-Z][a-zA-Z]*Actions_stub_$'` filter to preserve the 58 frozen ABI count despite 5 weak stubs now having external linkage.

### Human Verification Required

This phase ran under `--auto` mode. Two human-verify gates were auto-approved:

1. **Plan 01 Task 3** — LEXICON-AUDIT-mail.md FROZEN flip. Self-reviewed against CONTEXT D-10 schema + RESEARCH A8 top-5 toolbar. Auto-approval committed as `4be363b`. The user may open `.planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md` post-phase and request edits (any edit must preserve: 9-column schema, row count in [80, 250], exactly 5 `toolbar=yes`, `qaction=yes => freq in {high, med}`).
2. **Plan 03 Task 3** — A/B gate. Auto-approval committed as `13db5b6` with 6 [MANUAL-DEFERRED] items in `06.1-03-AB-GATE-log.md`.

The 6 deferred items (visual / interactive, cannot be exercised in headless offscreen sandbox):

#### 1. Menu bar shows {File, Mesh, View, Help} (no Edit)

**Test:** Launch `$MEFISTO/pp/ppmail_qt` in `$MEFISTOX/pan2d`; inspect the menu bar.
**Expected:** Four top-level menus render; Edit is absent per D-01.
**Why human:** Visual observation of live Qt window cannot be asserted in offscreen QTest (the QTest checks objectNames, not visible-menu render).

#### 2. Bilingual labels render per language flag

**Test:** Check `$MEFISTO/td/m/anglais` presence (EN build) vs absence (FR build); launch ppmail_qt.
**Expected:** EN labels ("MESHES and TOOLS" etc.) when anglais exists; FR ("MAILLAGES et Utilitaires" etc.) otherwise.
**Why human:** Requires on-box MEFISTO environment; text-content human-verifiable only.

#### 3. Mesh → Points → Create click reaches Fortran DFPOIN

**Test:** Click `Mesh → Points → Create` in ppmail_qt session.
**Expected:** Console dock shows `[menu] 1;1;` then Fortran prompt asks for a point name; type `99;` to exit cleanly.
**Why human:** Requires live mesher session with real Fortran dispatch loop (offscreen test stops at bridge drain).

#### 4. Window-close X button triggers clean exit

**Test:** Restart ppmail_qt; click window close (X).
**Expected:** Console shows `[menu] 99;`; Fortran save path runs; window closes; process exits 0.
**Why human:** Requires GUI interaction with window manager; offscreen test exercises the closeEvent handler but not the real WM path.

#### 5. Typed 99; at initial prompt exits cleanly (equivalence with click path)

**Test:** Restart ppmail_qt; type `99;` at the initial prompt instead of clicking anything.
**Expected:** Identical clean-exit behavior to #4 — D-09 invariant (click and type paths converge at DONNMF).
**Why human:** Requires interactive session.

#### 6. X11 regression: echo '99;' | pp/ppmail returns exit 0

**Test:** `cd $MEFISTOX/pan2d && echo "99;" | $MEFISTO/pp/ppmail; echo $?`
**Expected:** `exit=0` — proves X11 `CALL XVUE_MODULE_INIT` resolves to the no-op stub without side effects, and VALID-02 preservation holds.
**Why human:** Requires `$MEFISTOX` environment set; sandbox does not have `MEFISTOX` pre-populated.

These checks are **complementary** to the automated verification — all 5 ROADMAP success criteria already VERIFIED via the unit/integration test layer. The human items confirm the end-to-end UX works on the real desktop.

### Gaps Summary

No gaps. All 5 ROADMAP success criteria VERIFIED against live code:

- **Success #1 (audit):** 211-row FROZEN LEXICON-AUDIT-mail.md validator passes (strict-icons mode too).
- **Success #2 (QActions):** strong `registerMailActions_stub_` symbol in `xvue_qt_mail_actions.cpp` wires 19 flat QActions + helper-based 20th slot through `queueLexicon`; force-link keepalive in `xvue_qt_app.cpp` ensures the strong symbol is pulled into `pp/ppmail_qt`.
- **Success #3 (Fortran hook):** `CALL XVUE_MODULE_INIT( 'mail', 4 )` at `prpr/ppmail.f:175`; matching weak Fortran `SUBROUTINE XVUE_MODULE_INIT` in `xvue/xvmodi.f` for X11 backend.
- **Success #4 (icons + lint):** 10 SVG icons registered in qrc; both lint scripts exit 0; strict-icons validator exit 0.
- **Success #5 (QTest):** 4 required D-13 tests + 1 D-09 bonus (all PASS under offscreen QPA; each test drives `QAction::trigger()` + drains `xvsouris_` + asserts ASCII byte sequence).

No regression: all 116 Qt tests green (2 pre-existing skips inherited from Phase 5). ABI count preserved at 58. Both Qt and X11 backends build and link clean.

Two notable auto-fixes during execution (documented in plan SUMMARYs, not gaps):
- **Plan 02:** hoisted `register<Mod>Actions_stub_` out of xvue_qt_api.cpp's anonymous namespace (internal linkage was silently blocking the weak-override pattern). Filter added to `verify_abi.sh` to preserve the 58-count invariant.
- **Plan 03:** force-link keepalive pattern added to unblock GNU ld archive-search (without it, the strong mail-actions TU was silently dropped from pp/ppmail_qt, reverting to the warn-once stub). Also added `Q_INIT_RESOURCE(xvue_icons)` next to the existing fonts init. Both fixes establish reusable idioms for Phases 6.2-6.5.

Status is `human_needed` (not `passed`) solely because 6 visual/interactive UX items are deferred for on-box confirmation per the `--auto` mode policy. The automated verification surface is complete.

---

*Verified: 2026-04-22T10:00:00Z*
*Verifier: Claude (gsd-verifier)*
