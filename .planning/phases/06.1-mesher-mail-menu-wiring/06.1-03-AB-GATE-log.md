# Phase 6.1 Plan 03 Task 3 — Auto-approved A/B gate log

**Mode:** `--auto` (parent orchestrator runs in auto mode; checkpoint
`human-verify` auto-approves after automated verifications pass).
**Approved:** 2026-04-21
**Approver:** executor agent (auto-mode)

## Automated checks (all GREEN)

### 1. Qt build — `bin/cbl_tout_qt`
- Exit: 0
- `libxvueqt.a` links clean.
- verify_abi reports `nm count: 58  header count: 58` (ABI frozen).
- `pp/ppmail_qt`, `pp/ppelas_qt`, `pp/ppflui_qt`, `pp/ppther_qt`,
  `pp/ppnlse_qt`, plus `ppxvtest{0..4}_qt` all produced.
- `nm pp/ppmail_qt | grep ' T registerMailActions_stub_'` returns 1
  (strong override now in the Qt binary — Rule 3 auto-fix confirmed).

### 2. Qt test suite — all 8 binaries under `QT_QPA_PLATFORM=offscreen`
```
xvue_qt_canvas_gestures_tests        12 passed, 0 failed, 0 skipped
xvue_qt_dialogs_console_tests        14 passed, 0 failed, 0 skipped
xvue_qt_event_menu_predrain_tests    10 passed, 0 failed, 0 skipped
xvue_qt_event_tests                  33 passed, 0 failed, 2 skipped
xvue_qt_i18n_menu_prefs_tests        19 passed, 0 failed, 0 skipped
xvue_qt_mail_menu_tests               7 passed, 0 failed, 0 skipped  <- NEW
xvue_qt_menu_tests                    5 passed, 0 failed, 0 skipped
xvue_qt_window_chrome_tests          16 passed, 0 failed, 0 skipped
---------------------------------------------------------------
GRAND TOTAL                         116 passed, 0 failed, 2 skipped
```
The 2 skips are pre-existing in `xvue_qt_event_tests` (inherited from
Phase 5 — not a Plan 03 regression).

### 3. X11 build — `bin/cbl_tout` (serial, after Qt)
- Exit: 0. X11 `pp/ppmail`, `pp/ppelas`, `pp/ppflui`, `pp/ppther`,
  `pp/ppnlse`, `pp/ppinit`, `ppxvtest{0..4}` all produced.
- `nm xvue/lib | grep -c xvue_module_init_` returns 1 (Task 1 stub
  symbol resolves).

### 4. Plan 03 acceptance criteria
- [x] `grep -c "CALL XVUE_MODULE_INIT" prpr/ppmail.f` → 1
- [x] `xvue/qt/build/tests/xvue_qt_mail_menu_tests` exists and exits 0
- [x] All other `xvue/qt/build/tests/xvue_qt_*_tests` still pass
- [x] `bin/cbl_tout_qt` successful link
- [x] `bin/cbl_tout` successful X11 link
- [x] `queueLexicon(QStringLiteral("99;"))` appears 2× in
      `xvue_qt_window.cpp` (onFileQuit + closeEvent)
- [x] No non-comment `QMessageBox::question` in `xvue_qt_window.cpp`
- [x] No non-comment `QCoreApplication::quit` in `xvue_qt_window.cpp`
- [x] `XvueApp::blockingDepth` appears in `xvue_qt_window.cpp` (the
      closeEvent gate)
- [x] `AFTER xvinitgraphique_` comment corrected in `xvue_qt_api.cpp`

## `[MANUAL-DEFERRED]` items (visual / interactive, cannot be automated)

The plan's `<how-to-verify>` enumerates 4 checks (a)–(e) that require a
user-driven mesher session. In `--auto` mode these are deferred and
flagged here for on-box confirmation:

| # | Check                                                                | Why deferred              |
|---|----------------------------------------------------------------------|---------------------------|
| 1 | `pp/ppmail_qt` menu bar shows `{File, Mesh, View, Help}` visually     | Visual observation only   |
| 2 | Bilingual labels render per `$MEFISTO/td/m/anglais` presence          | Visual observation only   |
| 3 | `Mesh → Points → Create` click lands in Fortran's `DFPOIN` prompt     | Requires live mesher run  |
| 4 | Window-close (X) button path: `[menu] 99;` echo + process exits 0     | Requires GUI interaction  |
| 5 | Typed `99;` at initial prompt exits 0 (equivalence with click path)   | Requires GUI interaction  |
| 6 | `$MEFISTOX/pan2d` X11 smoke via `echo "99;" \| $MEFISTO/pp/ppmail`    | MEFISTOX unset in CI env  |

These are UX / end-to-end manual-verify items — they cannot be
exercised inside a headless `QT_QPA_PLATFORM=offscreen` sandbox.
Covered indirectly by the 5 D-13 QTest cases which prove the
click-to-xvsouris round-trip at the unit level, and by the A4
empirical evidence cited in RESEARCH (Phase 6.0 Plan 07 already
shipped `queueLexicon("99;")` → clean exit via testa smoke).

## Sign-off

All automated gates pass. No regressions introduced. Visual checks
flagged for on-box follow-up. Plan 03 proceeds to SUMMARY + phase close.

---
*Task 3 auto-approval — Phase 6.1 Plan 03*
*Timestamp: 2026-04-21*
