---
phase: 00-build-skeleton-abi-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [BUILD-07]
user_setup:
  - service: qt6-base-dev (apt)
    why: "Qt 6 headers + moc + CMake config for find_package(Qt6). Confirmed NOT installed per 00-RESEARCH.md §Environment Availability."
    env_vars: []
    dashboard_config:
      - task: "sudo apt install qt6-base-dev qt6-base-dev-tools"
        location: "Debian trixie host shell"

must_haves:
  truths:
    - "qt6-base-dev and qt6-base-dev-tools are installed and discoverable by find_package(Qt6)"
    - "Legacy X11 build (bin/cbl_tout) produces working pp/ppmail, pp/ppelas, pp/ppflui, pp/ppther, pp/ppnlse on this clone"
    - "A known-good baseline exists so later BUILD-07 regression checks have an anchor"
  artifacts:
    - path: "pp/ppmail"
      provides: "Legacy X11 mesher executable — baseline anchor"
    - path: "pp/ppelas"
      provides: "Legacy X11 elasticity executable — baseline anchor"
    - path: "pp/ppflui"
      provides: "Legacy X11 fluid executable — baseline anchor"
    - path: "pp/ppther"
      provides: "Legacy X11 thermal executable — baseline anchor"
    - path: "pp/ppnlse"
      provides: "Legacy X11 nonlinear executable — baseline anchor"
  key_links:
    - from: "bin/cbl_tout"
      to: "pp/pp*"
      via: "gfortran link of prpr/pp*.f + xvue/xvuelc.o + -lX11"
      pattern: "gfortran.*prpr/ppmail.f"
---

<objective>
Unblock Phase 0 work by (a) installing the qt6-base-dev toolchain that every subsequent plan depends on, and (b) proving the legacy X11 build path currently succeeds on this clone so BUILD-07's "legacy path continues to work unchanged" regression check has a known-good anchor.

Purpose: Per 00-RESEARCH.md §"Legacy Path Preservation": `pp/` is empty on this clone — the legacy build has never been run here. If we scaffold Qt first and legacy is secretly broken, we'll blame Phase 0 for a pre-existing regression. Also per CLAUDE.md: ask before installing system packages, and never break legacy.
Output: Installed Qt 6 toolchain + populated pp/pp* legacy executables + recorded baseline.
</objective>

<execution_context>
@/home/drico/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/drico/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-build-skeleton-abi-stubs/00-CONTEXT.md
@.planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md
@CLAUDE.md
@bin/cbl_tout
@bin/ccxvue
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User installs Qt 6 dev packages via apt</name>
  <read_first>
    - .planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md §"Environment Availability" (confirms qt6-base-dev NOT installed; candidate 6.10.2+dfsg-6 on trixie)
    - CLAUDE.md §"Asking before acting" (ask before installing system packages)
  </read_first>
  <what-built>Nothing built by Claude — this task blocks on the user running a privileged apt install.</what-built>
  <how-to-verify>
    1. On the Debian trixie host, run:
       ```
       sudo apt update
       sudo apt install qt6-base-dev qt6-base-dev-tools
       ```
    2. After install, verify the packages are present:
       ```
       dpkg -l qt6-base-dev qt6-base-dev-tools | grep '^ii'
       ```
       Both packages must show `ii` (installed).
    3. Verify find_package discovery works:
       ```
       pkg-config --modversion Qt6Core Qt6Gui Qt6Widgets Qt6PrintSupport
       ```
       Each must print a 6.x version with no error.
    4. Verify `moc` is on PATH:
       ```
       which moc-qt6 || which /usr/lib/qt6/libexec/moc
       ```
       At least one path must exist.
  </how-to-verify>
  <acceptance_criteria>
    - `dpkg -l qt6-base-dev` prints a line starting `ii`
    - `dpkg -l qt6-base-dev-tools` prints a line starting `ii`
    - `pkg-config --modversion Qt6Core` exits 0 and prints `6.*`
    - `pkg-config --modversion Qt6Widgets` exits 0 and prints `6.*`
    - `pkg-config --modversion Qt6Gui` exits 0 and prints `6.*`
    - `pkg-config --modversion Qt6PrintSupport` exits 0 and prints `6.*`
  </acceptance_criteria>
  <resume-signal>Type "installed" after all four pkg-config modversion commands succeed, or describe the failure.</resume-signal>
  <files></files>
  <action>See <how-to-verify> block above. Task is human-gated per checkpoint type.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification performed interactively per <how-to-verify> above. See <acceptance_criteria>.</automated>
  </verify>
  <done>User responds with resume-signal; acceptance_criteria listed above are all satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Verify legacy X11 build baseline (bin/cbl_tout)</name>
  <read_first>
    - bin/cbl_tout (the canonical legacy build script — 86 lines)
    - bin/cbmail (per-module compile pattern — 71 lines)
    - bin/ccxvue (C compile script for xvuelc.c)
    - CLAUDE.md §"Compilation must never break"
    - .planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md §"Legacy Path Preservation" (explains why this baseline is required before any Qt work)
  </read_first>
  <action>
    Run the legacy X11 build end-to-end and confirm it populates `pp/` with the 5 baseline executables. Concrete steps from repo root `/home/drico/git/mefisto`:

    1. Ensure env is set: `export MEFISTO=/home/drico/git/mefisto`. If `$MEFISTOX` is unset, set `export MEFISTOX=$HOME/mefistox` and `mkdir -p $MEFISTOX`. Add `$MEFISTO/bin` to PATH for this shell: `export PATH=.:$PATH:$MEFISTO/bin`.
    2. From `$MEFISTO`, run: `bin/cbl_tout 2>&1 | tee /tmp/cbl_tout.log`. This will: (a) generate `incl/homdir.inc`, (b) call `bin/ccxvue` to compile `xvue/xvuelc.o`, (c) call `bin/cbl_tous_f` for each module, (d) call `bin/cbinit bin/cbmail bin/cbelas bin/cbther bin/cbflui bin/cbnlse bin/cbpoba` to produce `pp/pp*`.
    3. After it finishes, run `ls -l pp/ | tee /tmp/pp_listing.txt`.
    4. Confirm each of the 5 baseline executables exists and is executable: `test -x pp/ppmail && test -x pp/ppelas && test -x pp/ppflui && test -x pp/ppther && test -x pp/ppnlse && echo LEGACY_BASELINE_OK`.
    5. Confirm xvuelc.o is present (Phase 0 must NOT touch it): `test -f xvue/xvuelc.o && echo XVUELC_O_OK`.
    6. If the legacy build FAILS on this clone: DO NOT proceed to Plan 02. STOP and ask the user — this indicates a pre-existing environment issue (missing libX11-dev, missing gfortran option, etc.) that must be resolved before Phase 0 can proceed. The appropriate error to surface is: "Legacy build broken on this clone — `bin/cbl_tout` failed at step X with error Y. Phase 0 cannot proceed until the legacy baseline is restored."
    7. DO NOT run any `testa/` case yet — that is a manual step deferred to Plan 04 where the user runs them interactively. Here we only verify the binaries link successfully.
    8. DO NOT modify any file in `bin/`, `xvue/`, `prpr/`, `mail/`, `elas/`, `flui/`, `ther/`, `nlse/`, `reso/`, `util/`, or `incl/` (except the auto-generated `incl/homdir.inc` which `cbl_tout` writes itself).

    Per D-02: the only files Phase 0 may create are under `xvue/qt/**`, `bin/cb*_qt`, `bin/cbl_tout_qt`, `xvue/README_COORDS.md`, and `.planning/validation/BASELINE.md`. This task creates nothing — it only runs the legacy script and records the outcome.
  </action>
  <verify>
    <automated>cd /home/drico/git/mefisto && test -x pp/ppmail && test -x pp/ppelas && test -x pp/ppflui && test -x pp/ppther && test -x pp/ppnlse && test -f xvue/xvuelc.o && echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bin/cbl_tout` exits 0 (recorded in /tmp/cbl_tout.log)
    - `test -x pp/ppmail` succeeds
    - `test -x pp/ppelas` succeeds
    - `test -x pp/ppflui` succeeds
    - `test -x pp/ppther` succeeds
    - `test -x pp/ppnlse` succeeds
    - `test -f xvue/xvuelc.o` succeeds
    - No file under `bin/`, `xvue/*.c`, `xvue/*.f`, `prpr/`, `mail/`, `elas/`, `flui/`, `ther/`, `nlse/`, `reso/`, `util/` was modified by this task (verified via `git status --porcelain bin/ xvue/xvuelc.c xvue/*.f prpr/ mail/ elas/ flui/ ther/ nlse/ reso/ util/ | grep -v '^??' | wc -l` returns 0, excluding the expected auto-generated `incl/homdir.inc`)
  </acceptance_criteria>
  <done>
    Legacy X11 build succeeds on this clone, the 5 baseline executables exist in `pp/`, and `xvue/xvuelc.o` is present. Phase 0 now has a known-good anchor for later BUILD-07 regression checks, and Qt scaffolding work can begin in Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apt repository → developer host | Debian trixie `qt6-base-dev` package is pulled from apt mirrors; supply-chain trust rests on Debian's signing infrastructure |
| shell env → cbl_tout | `$MEFISTO` is interpolated into `incl/homdir.inc` as a Fortran string literal; env-var content crosses into source-code-generated output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering (supply chain) | apt install qt6-base-dev | accept | Debian trixie package signing is the project's trust model; no additional verification in this repo. Rationale: consistent with existing `libX11-dev` acceptance. |
| T-00-02 | Injection | `bin/cbl_tout` heredoc writes `$MEFISTO` into `incl/homdir.inc` Fortran string literal | accept | Pre-existing legacy behavior, not introduced by Phase 0. Documented here for traceability. `$MEFISTO` is set by the developer, not from untrusted input. |
| T-00-03 | Denial of service | Legacy build failure during Task 2 | mitigate | Task 2 halts Phase 0 explicitly on legacy failure and surfaces the error, preventing downstream plans from cascading on a broken baseline. |
</threat_model>

<verification>
Manual: User confirms apt install succeeded (Task 1 checkpoint). Automated: shell one-liner in Task 2's `<automated>` block verifies all 5 legacy executables + xvuelc.o are present.
</verification>

<success_criteria>
- Qt 6 dev packages installed and discoverable via pkg-config
- Legacy `bin/cbl_tout` builds `pp/ppmail`, `pp/ppelas`, `pp/ppflui`, `pp/ppther`, `pp/ppnlse` cleanly
- No file under the D-02 read-only set was modified
- BUILD-07 has a known-good anchor for end-of-phase regression check
</success_criteria>

<output>
After completion, create `.planning/phases/00-build-skeleton-abi-stubs/00-01-SUMMARY.md` recording: qt6 package versions installed, cbl_tout success/failure, list of pp/pp* files produced, any warnings observed in the legacy build log.
</output>
