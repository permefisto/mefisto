---
phase: 00-build-skeleton-abi-stubs
plan: 04
type: execute
wave: 4
depends_on: [00-01, 00-02, 00-03]
files_modified:
  - xvue/README_COORDS.md
  - .planning/validation/BASELINE.md
autonomous: false
requirements: [BUILD-09, BUILD-10]

must_haves:
  truths:
    - "xvue/README_COORDS.md documents the audited Y-axis convention: on-screen Y-down top-left origin, PostScript-only Y-flip via ypixels - y"
    - ".planning/validation/BASELINE.md lists exactly 5 canonical testa/ cases (one per module: mesher, elas, flui, ther, nlse) with project dirs and expected qualitative behavior"
    - "End-of-phase regression check confirms bin/cbl_tout (legacy) still builds unchanged pp/pp* from a clean tree"
    - "End-of-phase regression check confirms bin/cbl_tout_qt still produces working pp/pp*_qt"
    - "User has manually run each of the 5 baseline cases through the legacy X11 backend and confirmed each behaves as expected"
  artifacts:
    - path: "xvue/README_COORDS.md"
      provides: "Y-axis convention audit result — read-only reference for Phases 1-7"
      contains: "Y-down"
    - path: ".planning/validation/BASELINE.md"
      provides: "5 canonical testa/ cases as the immutable Phase 1-8 validation baseline"
      contains: "pan2d"
  key_links:
    - from: "xvue/README_COORDS.md"
      to: "Phase 2+ drawing implementations"
      via: "Read-only reference cited from every phase that touches QPainter coordinates"
      pattern: "README_COORDS"
    - from: ".planning/validation/BASELINE.md"
      to: "Phase 1-8 end-of-phase validation runs"
      via: "Every phase runs the 5 cases listed here through both backends"
      pattern: "BASELINE.md"
---

<objective>
Complete Phase 0 by (a) writing the Y-axis convention audit as `xvue/README_COORDS.md` (D-03, BUILD-09), (b) recording the 5 canonical `testa/` validation cases in `.planning/validation/BASELINE.md` (D-14, D-16, BUILD-10), and (c) running the final end-of-phase regression check that both legacy `bin/cbl_tout` AND new `bin/cbl_tout_qt` still succeed from a clean state, plus manually running the 5 baseline `testa/` cases through the legacy X11 backend to confirm the project's starting reference point.

Purpose: Phase 0 ships three deliverables beyond the build plumbing itself: the coordinate-convention doc that every later drawing phase will cite, the validation baseline that every later phase will A/B against, and a clean-tree proof that nothing in Phase 0 broke the legacy path. Per D-16: BASELINE.md's "expected qualitative behavior" field is maintainer knowledge — the user must describe, the planner records. Per D-15: the legacy 5-case run is the Phase 0 validation gate.
Output: Two new documentation files committed, a clean-tree regression log proving both build paths work, and a signed-off baseline.
</objective>

<execution_context>
@/home/drico/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/drico/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-build-skeleton-abi-stubs/00-CONTEXT.md
@.planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md
@.planning/phases/00-build-skeleton-abi-stubs/00-01-SUMMARY.md
@.planning/phases/00-build-skeleton-abi-stubs/00-02-SUMMARY.md
@.planning/phases/00-build-skeleton-abi-stubs/00-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write xvue/README_COORDS.md (Y-axis convention audit) and .planning/validation/BASELINE.md (5 testa/ cases)</name>
  <files>xvue/README_COORDS.md, .planning/validation/BASELINE.md</files>
  <read_first>
    - .planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md §"Y-Axis Convention Audit" (the audit result — copy the "README_COORDS.md content outline" section verbatim)
    - .planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md §"testa/ Baseline Verification" (confirms all 5 cases exist on disk)
    - .planning/phases/00-build-skeleton-abi-stubs/00-CONTEXT.md D-03 (README_COORDS required), D-14/D-16 (BASELINE.md required fields)
    - xvue/xvuelc.c lines 319-332 (origin comment), 1845-1975 (drawing origin comments), 1187-1306 (xvpostscript emitter with Y-flip)
  </read_first>
  <action>
    Create TWO files. Both contain content that, for BASELINE.md, requires reading from xvuelc.c and asking the user for the "expected qualitative behavior" of each testa/ case.

    **File A: `xvue/README_COORDS.md`** — create with the following content verbatim. Per D-03 this file lives at `xvue/README_COORDS.md` (top level of xvue/, NOT inside xvue/qt/ — it documents the convention shared by both backends).

    ```markdown
    # xvue — Y-Axis Coordinate Convention (audited 2026-04-10)

    > This document records the audited Y-axis convention used by `xvue/xvuelc.c`
    > and mirrored byte-for-byte by `xvue/qt/` (the Qt 6 backend). Every phase of
    > the xvue-qt migration from Phase 1 onward MUST read this document before
    > writing any QPainter-based drawing code.

    ## On-screen coordinates (Fortran ↔ C bridge ↔ graphics backend)

    | Property | Value |
    |----------|-------|
    | Origin | Top-left corner of the canvas widget |
    | X axis | Grows rightward |
    | Y axis | Grows **downward** (Y-down) |
    | Unit (X11) | Device pixels |
    | Unit (Qt) | Logical pixels (device-independent, HiDPI-aware) |

    ## Where the convention comes from

    Direct evidence from `xvue/xvuelc.c` (read 2026-04-10):

    - Line 321 (comment inside `xvpxecran_`): "origine coin superieur gauche de l'ecran"
      — "origin = upper-left corner of the screen"
    - Line 1852 (comment inside line-drawing block): "ORIGINE coin superieur gauche"
    - Line 1869 (comment, same context): "ORIGINE coin superieur gauche"
    - Line 1621 (comment inside `xvpxfenetre_`): "la fenetre" (window) — origin implied top-left

    Every `XDrawLine`, `XDrawLines`, `XFillRectangle`, and `XDrawArc` call in
    `xvue/xvuelc.c` passes the Y coordinate **unflipped**. This matches:

    - **Xlib's native convention** — Y-down top-left origin, device pixels
    - **Qt's native `QPainter` convention** — Y-down top-left origin, logical pixels

    Therefore the Qt backend **does not transform Y coordinates** anywhere on the
    on-screen path. Fortran-provided `(x, y)` values are passed directly to
    `QPainter::drawLine(*x, *y, ...)` etc. in Phase 2+.

    ## Where Y IS flipped (PostScript export only — Phase 7 concern)

    PostScript's natural origin is **bottom-left** (Y-up). The hand-rolled PostScript
    emitter in `xvuelc.c` honors this by flipping Y at emit time, not by storing
    flipped coordinates in any state.

    Direct evidence:

    - `xvue/xvuelc.c` lines 1895, 1932, 1953, 1966 — all inside the `xvtrait_`
      PostScript-capture branch — write `ypixels - *y1` (and analogous for the
      second endpoint) into the emitted `.ps` stream, where `ypixels` is the
      canvas height in pixels.
    - Same pattern throughout the `xvpostscript_` function body (line 1187+).

    Formula: `ps_y = ypixels - screen_y`.

    ## Implications for the Qt migration

    | Phase | Action |
    |-------|--------|
    | Phase 1 (Window shell) | No drawing yet — no Y handling needed. |
    | Phase 2 (Drawing primitives) | **DO NOT flip Y anywhere.** Pass Fortran-provided y values directly to `QPainter`. |
    | Phase 3 (Text, fonts, colormap) | Same — text baseline y is top-left-origin, unflipped. |
    | Phase 4 (Pixmap save/restore) | Same — pixmap coordinates are top-left-origin. |
    | Phase 5 (Event bridge) | Mouse y from `QMouseEvent::y()` is already top-left-origin — no transform. |
    | Phase 6 (UX chrome) | Widget coordinates are top-left-origin — no transform. |
    | Phase 7 (Export) | **PRESERVE the `ypixels - y` flip verbatim** when porting the PostScript emitter from `xvuelc.c` to `xvue/xvue_qt_postscript.cpp`. Do NOT try to "clean it up" by storing flipped coordinates — the flip is a PostScript-format concern, not an internal-state concern. |
    | HiDPI (`devicePixelRatioF > 1`) | Orthogonal to the Y-axis audit. Handled separately in Phase 1 (SHELL-06). |

    ## Anti-patterns (common mistakes to avoid)

    - **"Store coordinates with Y-up because PostScript wants that"** — NO. Store
      coordinates as the Fortran side provides them (Y-down). Flip at emit time
      inside `xvpostscript_` only.
    - **"Use `QTransform` to flip Y on the QPainter"** — NO. No y-flip transform
      anywhere in the on-screen path.
    - **"Invert y at the Fortran boundary to 'normalize' to Y-up"** — NO. The
      Fortran `.f` wrappers and the C/C++ stubs are on the same side of the
      boundary, sharing the Y-down convention.

    ## Audit metadata

    - **Audited:** 2026-04-10
    - **Auditor:** Phase 0 planning (direct read of `xvue/xvuelc.c`)
    - **Source lines inspected:** 319-332 (xvpxecran), 1187-1306 (xvpostscript),
      1619-1720 (window query), 1845-1975 (line drawing) — see `.planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md §"Y-Axis Convention Audit"` for full details.
    - **Read-only status:** This document is write-once in Phase 0 and read-only
      for every subsequent phase until Phase 9 retirement.
    ```

    **File B: `.planning/validation/BASELINE.md`** — first ensure the directory exists: `mkdir -p .planning/validation`. Then create the file with the structure below.

    **Crucial:** the "Expected qualitative behavior" field for each case is maintainer knowledge (per D-16 and 00-RESEARCH.md Open Question #2). Write the initial version with placeholders that say `[Maintainer to describe — see 00-04-PLAN Task 2]`, and the interactive Task 2 will update each placeholder with the user's description before committing. File structure (verbatim frame; fill in file paths from `testa/` directory listing confirmed in 00-RESEARCH.md):

    ```markdown
    # Validation Baseline — Phase 0 through Phase 8

    > Immutable list of 5 canonical `testa/` cases (one per solver module) used
    > as the A/B validation baseline for every phase of the xvue-qt migration.
    > Write-once in Phase 0 (2026-04-10). Read-only for every subsequent phase
    > through Phase 8.
    >
    > Per D-14, D-15, D-16 and REQUIREMENTS.md §BUILD-10.

    ## How this baseline is used

    - **Phase 0:** Legacy X11 (`bin/cbl_tout` + `pp/pp*`) runs all 5 cases as the
      known-good starting reference (Task 2 below). Qt backend (`pp/pp*_qt`) is
      link-complete only — no graphics yet.
    - **Phases 1-7:** End of each phase, all 5 cases are run through **both**
      backends. Results logged to `.planning/phases/{padded}-*/VALIDATION.md`.
    - **Phase 8:** All 5 cases also run through `_OMP` variants and on a HiDPI
      display. The checklist in `.planning/phase-8/CHECKLIST.md` is the gate for
      declaring v1 shippable.
    - **Phase 9:** After the one-release-cycle A/B window closes, legacy backend
      is retired; this baseline continues to anchor Qt-only regression testing.

    ## The 5 canonical cases

    ### 1. testa/pan2d — Mesher (ppmail)

    | Field | Value |
    |-------|-------|
    | Project directory | `testa/pan2d/` |
    | Solver module | `mail/` (mesher) |
    | Launcher script | `MAILLER` (spawns `pp/ppmail` under legacy / `pp/ppmail_qt` under Qt) |
    | Phase 0 executable | `pp/ppmail` (legacy, X11) |
    | Later-phase executable | `pp/ppmail_qt` (Qt, Phase 1+) |
    | Expected qualitative behavior | [Maintainer to describe — see Task 2] |
    | Known-flaky touchpoints | [To be discovered — first-run notes below] |
    | First-run notes (Phase 0, legacy) | [Filled in by Task 2 after manual run] |

    ### 2. testa/nafems_le1 — Elasticity (ppelas)

    | Field | Value |
    |-------|-------|
    | Project directory | `testa/nafems_le1/` |
    | Solver module | `elas/` (linear elasticity) |
    | Launcher script | `ELASTICER` |
    | Phase 0 executable | `pp/ppelas` |
    | Later-phase executable | `pp/ppelas_qt` |
    | Expected qualitative behavior | [Maintainer to describe — see Task 2] |
    | Known-flaky touchpoints | [To be discovered] |
    | First-run notes (Phase 0, legacy) | [Filled in by Task 2] |
    | Reference | NAFEMS Linear Elastic benchmark LE1 (standard reference case) |

    ### 3. testa/cavity2d — Fluid (ppflui)

    | Field | Value |
    |-------|-------|
    | Project directory | `testa/cavity2d/` |
    | Solver module | `flui/` (fluid mechanics) |
    | Launcher script | `FLUIDER` |
    | Phase 0 executable | `pp/ppflui` |
    | Later-phase executable | `pp/ppflui_qt` |
    | Expected qualitative behavior | [Maintainer to describe — see Task 2] |
    | Known-flaky touchpoints | [To be discovered] |
    | First-run notes (Phase 0, legacy) | [Filled in by Task 2] |
    | Reference | Classic lid-driven cavity flow |

    ### 4. testa/heat1d — Thermal (ppther)

    | Field | Value |
    |-------|-------|
    | Project directory | `testa/heat1d/` |
    | Solver module | `ther/` (thermal) |
    | Launcher script | `THERMICER` |
    | Phase 0 executable | `pp/ppther` |
    | Later-phase executable | `pp/ppther_qt` |
    | Expected qualitative behavior | [Maintainer to describe — see Task 2] |
    | Known-flaky touchpoints | [To be discovered] |
    | First-run notes (Phase 0, legacy) | [Filled in by Task 2] |
    | Reference | 1D heat conduction (simplest diagnostic for thermal solver) |

    ### 5. testa/nlsecu — Nonlinear (ppnlse)

    | Field | Value |
    |-------|-------|
    | Project directory | `testa/nlsecu/` |
    | Solver module | `nlse/` (nonlinear solver) |
    | Launcher script | `NLSER` |
    | Phase 0 executable | `pp/ppnlse` |
    | Later-phase executable | `pp/ppnlse_qt` |
    | Expected qualitative behavior | [Maintainer to describe — see Task 2] |
    | Known-flaky touchpoints | [To be discovered] |
    | First-run notes (Phase 0, legacy) | [Filled in by Task 2] |
    | Reference | Nonlinear solver canonical case |

    ## Amendment policy

    This file is **write-once in Phase 0**. Amendments are only permitted if:

    1. A case is discovered to be genuinely unrunnable on the legacy backend on a
       clean machine (environment issue, not a Phase 0 bug). The case is then
       replaced with another `testa/` case from the same solver module, and the
       replacement is logged here with the reason.
    2. A new solver module is added to MEFISTO (out of scope for xvue-qt v1).

    Beyond those two cases, the 5 baseline cases are immutable until Phase 9
    retirement.

    ## Audit metadata

    - **Created:** 2026-04-10 (Phase 0, Plan 04, Task 1)
    - **First run through legacy X11:** [date filled in by Task 2]
    - **Source of truth for existence check:** `ls testa/` on 2026-04-10
      confirmed all 5 project directories exist (see `.planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md §"testa/ Baseline Verification"`)
    ```

    Do NOT fill in the `[Maintainer to describe]` placeholders yet — that is Task 2. Do NOT run any `testa/` case yet — Task 2 is the interactive run.

    **Constraint per D-02:** `xvue/README_COORDS.md` is created at the top level of `xvue/`, NOT inside `xvue/qt/`. This is the one file outside `xvue/qt/` that Phase 0 creates (alongside the `bin/cb*_qt` scripts). No file in `xvue/*.f`, `xvue/xvuelc.c`, `xvue/lib`, or elsewhere in `xvue/` is modified.
  </action>
  <verify>
    <automated>cd /home/drico/git/mefisto && test -f xvue/README_COORDS.md && grep -q 'Y-down' xvue/README_COORDS.md && grep -q 'ypixels - screen_y' xvue/README_COORDS.md && grep -q 'xvpostscript' xvue/README_COORDS.md && test -f .planning/validation/BASELINE.md && test $(grep -c '^### ' .planning/validation/BASELINE.md) = "5" && grep -q 'testa/pan2d' .planning/validation/BASELINE.md && grep -q 'testa/nafems_le1' .planning/validation/BASELINE.md && grep -q 'testa/cavity2d' .planning/validation/BASELINE.md && grep -q 'testa/heat1d' .planning/validation/BASELINE.md && grep -q 'testa/nlsecu' .planning/validation/BASELINE.md && git diff --quiet xvue/xvuelc.c xvue/lib 2>/dev/null || true ; test -z "$(git status --porcelain xvue/*.f 2>/dev/null)" && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `xvue/README_COORDS.md` exists
    - `grep -q 'Y-down' xvue/README_COORDS.md` succeeds
    - `grep -q 'top-left' xvue/README_COORDS.md` succeeds
    - `grep -q 'ypixels - screen_y' xvue/README_COORDS.md` succeeds (PostScript flip formula documented)
    - `grep -q 'xvpostscript' xvue/README_COORDS.md` succeeds
    - `grep -q 'PRESERVE the .ypixels - y. flip verbatim' xvue/README_COORDS.md` succeeds (Phase 7 instruction present)
    - `.planning/validation/BASELINE.md` exists
    - `grep -c '^### ' .planning/validation/BASELINE.md` returns exactly `5` (one H3 header per case)
    - `grep -q 'testa/pan2d' .planning/validation/BASELINE.md` succeeds (and same for nafems_le1, cavity2d, heat1d, nlsecu)
    - `grep -q 'ppmail_qt' .planning/validation/BASELINE.md` succeeds (and same for ppelas_qt, ppflui_qt, ppther_qt, ppnlse_qt)
    - `grep -q 'Maintainer to describe' .planning/validation/BASELINE.md` succeeds (placeholders present; Task 2 fills them in)
    - No file under `xvue/*.f`, `xvue/xvuelc.c`, `xvue/lib`, `mail/`, `elas/`, `flui/`, `ther/`, `nlse/`, `reso/`, `util/`, `prpr/`, `incl/*.inc` (except auto-generated `homdir.inc`) was modified
  </acceptance_criteria>
  <done>README_COORDS.md documents Y-down convention with PostScript exception, BASELINE.md lists all 5 testa cases with per-field structure and maintainer-knowledge placeholders. Task 2 will interactively fill in the placeholders and run the 5 cases through the legacy backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final Phase 0 verification — clean-tree rebuild of both backends + manual 5-case baseline run</name>
  <read_first>
    - .planning/validation/BASELINE.md (just created in Task 1 — this is what the user fills in and signs off on)
    - .planning/phases/00-build-skeleton-abi-stubs/00-CONTEXT.md D-15 (validation gate: 5 cases must run on legacy X11)
    - .planning/phases/00-build-skeleton-abi-stubs/00-RESEARCH.md §"Legacy Path Preservation" (clean-rebuild regression check at end of Phase 0)
    - CLAUDE.md §"Tests" ("small tests must continue to pass after every change")
  </read_first>
  <what-built>
    Phase 0 is now functionally complete:
    - CMake scaffold + 57-entry ABI header + warn-once stubs → `xvue/qt/build/libxvueqt.a`
    - bin/cbl_tout_qt + 5 per-module cb*_qt scripts → `pp/pp*_qt` executables
    - Y-axis audit doc at `xvue/README_COORDS.md`
    - Validation baseline skeleton at `.planning/validation/BASELINE.md`

    This final checkpoint asks the user to (a) prove the legacy path still builds cleanly from a clean tree, (b) prove the Qt path still builds cleanly from a clean tree, and (c) manually run the 5 baseline `testa/` cases through the legacy X11 backend and describe the expected qualitative behavior for each in BASELINE.md.
  </what-built>
  <how-to-verify>
    **Step 1 — Clean-tree rebuild of BOTH backends (regression check for BUILD-07 and BUILD-06):**

    From `$MEFISTO`:
    ```
    # Start fresh so no stale artifacts hide any regression
    rm -rf xvue/qt/build
    rm -f  pp/pp*
    rm -f  xvue/xvuelc.o
    rm -f  */*.o  *.o

    # Legacy path
    bin/cbl_tout 2>&1 | tee /tmp/final_cbl_tout.log
    ls -l pp/ | tee /tmp/final_pp_legacy.txt

    # Verify legacy executables
    test -x pp/ppmail && test -x pp/ppelas && test -x pp/ppflui && test -x pp/ppther && test -x pp/ppnlse && echo LEGACY_CLEAN_REBUILD_OK

    # Qt path (incrementally, because legacy already populated pp/)
    bin/cbl_tout_qt 2>&1 | tee /tmp/final_cbl_tout_qt.log
    ls -l pp/ | tee /tmp/final_pp_qt.txt

    # Verify Qt executables
    test -x pp/ppmail_qt && test -x pp/ppelas_qt && test -x pp/ppflui_qt && test -x pp/ppther_qt && test -x pp/ppnlse_qt && echo QT_CLEAN_REBUILD_OK

    # Verify legacy executables ALSO still present (bin/cbl_tout_qt should not have touched them)
    test -x pp/ppmail && test -x pp/ppelas && test -x pp/ppflui && test -x pp/ppther && test -x pp/ppnlse && echo LEGACY_STILL_OK_AFTER_QT_BUILD
    ```

    **Step 2 — Run the 5 baseline testa/ cases through the legacy X11 backend (D-15 gate):**

    For each of the 5 cases below, the user opens a terminal with a working X11 DISPLAY, runs the launcher, interacts with the case, and observes the result. The user then writes a 1-3 sentence "expected qualitative behavior" description into `.planning/validation/BASELINE.md` replacing the `[Maintainer to describe]` placeholder for that case.

    ```
    # Case 1: Mesher / pan2d
    export MEFISTOX=${MEFISTOX:-$HOME/mefistox}
    mkdir -p $MEFISTOX/pan2d
    cd $MEFISTOX/pan2d
    # or copy testa/pan2d inputs into the working dir per the existing project workflow
    MAILLER   # spawns pp/ppmail; user interactively works through the case
    # To save and exit: return to main menu, type "99;" (per CLAUDE.md — never Ctrl-C)

    # Case 2: Elasticity / nafems_le1
    ELASTICER

    # Case 3: Fluid / cavity2d
    FLUIDER

    # Case 4: Thermal / heat1d
    THERMICER

    # Case 5: Nonlinear / nlsecu
    NLSER
    ```

    After each case, edit `.planning/validation/BASELINE.md` and replace the `[Maintainer to describe — see Task 2]` placeholder with the user's description. Also replace `[Filled in by Task 2 after manual run]` with a short note on any surprises, warnings, or flaky behavior observed.

    If a case fails on the legacy X11 backend (crashes, refuses to open, infinite loop), STOP and investigate — this indicates a pre-existing environment issue (missing libX11, wrong DISPLAY, X11 forwarding broken, etc.) that is NOT Phase 0's fault but must be resolved before Phase 0 can be declared complete, because Phases 1-8 all depend on being able to run these 5 cases.

    **Step 3 — Commit the completed BASELINE.md:**

    Once all 5 cases have been run and their behavior descriptions filled in, confirm `.planning/validation/BASELINE.md` no longer contains any `[Maintainer to describe]` or `[Filled in by Task 2]` placeholder, then commit the Phase 0 changes.

    **Step 4 — Decision / resume signal:**

    Respond with one of:
    - `approved` if all 3 steps succeeded (both clean rebuilds pass, all 5 cases ran on legacy X11 and BASELINE.md is fully filled in)
    - `legacy broken: <details>` if Step 1 or Step 2 failed on the legacy path (Phase 0 needs to triage the pre-existing breakage before shipping)
    - `qt broken: <details>` if Step 1 failed on the Qt path (a Phase 0 bug — jump back to Plan 02 or 03 to fix)
    - `baseline pending: <which cases>` if some cases ran but others are pending the user's time/attention
  </how-to-verify>
  <acceptance_criteria>
    - Clean tree: `rm -rf xvue/qt/build pp/pp* xvue/xvuelc.o */*.o` ran without error
    - `bin/cbl_tout` exited 0 from the clean tree
    - `bin/cbl_tout_qt` exited 0 from the clean tree (after cbl_tout)
    - `test -x pp/ppmail && test -x pp/ppelas && test -x pp/ppflui && test -x pp/ppther && test -x pp/ppnlse` (legacy, 5/5)
    - `test -x pp/ppmail_qt && test -x pp/ppelas_qt && test -x pp/ppflui_qt && test -x pp/ppther_qt && test -x pp/ppnlse_qt` (Qt, 5/5)
    - User confirms (interactively) that all 5 `testa/` cases run to their expected behavior on the legacy X11 backend
    - `grep -c 'Maintainer to describe' .planning/validation/BASELINE.md` returns `0` (all placeholders filled in)
    - `grep -c 'Filled in by Task 2' .planning/validation/BASELINE.md` returns `0` (all first-run notes filled in)
    - `grep -q 'First run through legacy X11: 2026' .planning/validation/BASELINE.md` (date stamp updated from [date filled in by Task 2])
    - User responds with `approved` on the checkpoint
  </acceptance_criteria>
  <resume-signal>Type `approved` (and only approved) once the clean-tree rebuild succeeds on BOTH backends AND all 5 baseline testa cases have been run on legacy X11 AND `.planning/validation/BASELINE.md` is fully filled in (no remaining placeholder strings). Otherwise respond with `legacy broken: ...`, `qt broken: ...`, or `baseline pending: ...` so Phase 0 can decide whether to ship, revise, or hand back for triage.</resume-signal>
  <files></files>
  <action>See <how-to-verify> block above. Task is human-gated per checkpoint type.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification performed interactively per <how-to-verify> above. See <acceptance_criteria>.</automated>
  </verify>
  <done>User responds with resume-signal; acceptance_criteria listed above are all satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testa/ input files → solver binaries | Input mesh / config files in `testa/pan2d/`, etc. are read by the legacy solver binaries; these files are trusted project inputs. |
| X11 DISPLAY → legacy binaries | Task 2 manual validation requires a working X11 DISPLAY; the user's display server is trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-11 | Tampering | Clean-tree `rm -rf xvue/qt/build pp/pp* xvue/xvuelc.o */*.o` could remove unrelated files if run from the wrong directory | mitigate | Task 2 explicitly `cd $MEFISTO` first and lists the exact paths to remove. The `*/*.o` glob is inherited verbatim from the legacy `bin/cbl_tout` line 72 — no new risk. |
| T-00-12 | Denial of service | BASELINE.md lockdown — if the 5 cases are described inaccurately, every later phase's A/B validation is against a wrong reference | accept | Mitigated by making this a manual human-verify checkpoint; the user inspects each case and writes the description. No automation can substitute for maintainer knowledge here (per 00-RESEARCH.md Open Question #2). |
| T-00-13 | Information disclosure | BASELINE.md first-run notes may capture working-directory paths or system-specific state | accept | Low-value info; the file is committed to the project's own git repo and is not a published artifact. |
</threat_model>

<verification>
Task 1: automated shell verification of file presence, structure, and placeholders.
Task 2: manual human verification via interactive checkpoint — clean-tree rebuild of both backends + 5-case legacy run + BASELINE.md sign-off.
</verification>

<success_criteria>
- `xvue/README_COORDS.md` exists with Y-down audit, PostScript-flip exception, and Phase 7 preserve-verbatim instruction
- `.planning/validation/BASELINE.md` exists listing all 5 canonical testa cases with fully-filled-in qualitative behavior descriptions and first-run notes
- Clean-tree rebuild: both `bin/cbl_tout` and `bin/cbl_tout_qt` exit 0 from a freshly-cleaned tree, producing all 10 executables (5 legacy + 5 Qt)
- All 5 baseline testa cases run to completion on the legacy X11 backend (D-15 gate)
- User approved the checkpoint
- BUILD-07 verified by clean-rebuild regression check
- BUILD-09 delivered (README_COORDS.md)
- BUILD-10 delivered (BASELINE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/00-build-skeleton-abi-stubs/00-04-SUMMARY.md` recording: clean-tree rebuild wall-clock times for both backends, any warnings surfaced during the rebuild, the user's qualitative behavior descriptions for each of the 5 cases, any flaky touchpoints discovered during first-run, and the final approval status. This SUMMARY closes Phase 0.
</output>
