---
phase: 06.3-fluid-flui-menu-wiring
plan: 02
subsystem: xvue-qt
tags: [qt, flui, menu-wiring, qaction, keepalive, svg-icons, force-link, navier-stokes]

# Dependency graph
requires:
  - phase: 06.3-fluid-flui-menu-wiring
    provides: "LEXICON-AUDIT-flui.md (Plan 01 FROZEN) -- 161-row recursive audit, 49 qaction=yes rows + 5 toolbar=yes rows + 6 flui-unique SVG filename list + 1 reuse (mesh-draw.svg) + {File, Fluid, View, Help} taxonomy"
  - phase: 06.2-elasticity-elas-menu-wiring
    provides: "xvue_qt_elas_actions.cpp template (mirrored verbatim -- substitute elas->flui and Solve->Fluid), keepalive pattern with alphabetical ordering, ensureTopLevelMenu(insertBefore=...) + Q_ASSERT_X anchor pattern (Plan 04 + WR-03), tools/validate_audit_md.py module-aware ICONS resolver"
  - phase: 06.1-mesher-mail-menu-wiring
    provides: "Force-link keepalive (Rule 3) discovered/codified by Plan 03, XvueMenuFileParser bilingual loader (shared), mesh-draw.svg under icons/mail/ for code 19; reuse"
  - phase: 06.0-shared-shell-menu-bridge-dialogs
    provides: "XvueWindow chrome (menuBar/toolBar/consoleDock/menuBridge accessors), XvueMenuBridge::queueLexicon(cap=10000), weak register*Actions_stub_ at xvue_qt_api.cpp:256, module-gated dispatch at xvue_qt_api.cpp:1528"
provides:
  - "xvue/qt/src/xvue_qt_flui_actions.cpp -- STRONG registerFluiActions_stub_ body (~37 makeLeafAction calls covering all qaction=yes audit rows for the {File, Fluid, View, Help} taxonomy)"
  - "xvue_qt_flui_actions_keepalive() export + matching extern decl + static ref in xvue_qt_app.cpp (alphabetical: elas/flui/mail)"
  - "6 SVG icons under xvue/qt/resources/icons/flui/ (flui-object, flui-input-physics, flui-input-heat, solve-stokes, solve-navier-stokes, draw-velocity)"
  - "6 <file> entries appended to xvue/qt/resources/xvue_icons.qrc inside the existing /xvue/qt/icons prefix block"
  - "CMakeLists.txt target_sources(xvueqt PRIVATE) appended with src/xvue_qt_flui_actions.cpp (alphabetical: elas/flui/mail)"
  - "ABI count still 58; strong symbol force-linked into pp/ppflui_qt; 6.1 mail and 6.2 elas regression guards green"
affects: [06.3-03-flui-fortran-hook-and-tests]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Strong-symbol override of weak warn-once stub (6.1 Pattern 1, reused for elas in 6.2, now reused verbatim for flui)"
    - "Force-link keepalive (HANDOFF.json reusable_patterns[1]) -- xvue_qt_flui_actions_keepalive export + static ref g_xvue_qt_flui_actions_keepalive_ref in xvue_qt_app.cpp + (void) cast in load_bundled_font_()"
    - "ensureTopLevelMenu(insertBefore=viewMenuForAnchor) for {File, Fluid, View, Help} order (Plan 04 from 6.2) + Q_ASSERT_X (WR-03 hardening)"
    - "Static one-shot guard `static bool registered = false; if (registered) return; registered = true;` mirrors elas IN-02 process-scoped discipline"
    - "Solver-icon consolidation: codes 5;,6; share solve-stokes.svg; codes 7;,8;,9; share solve-navier-stokes.svg (drops new-icon count from 7 to 6 vs 6.2 elas)"
    - "Parent-shortcut convention carried forward: Fluid.Input/StokesSteady/NSImplicit + View.DrawResults each carry a child QAction firing the parent lexicon"

key-files:
  created:
    - "xvue/qt/src/xvue_qt_flui_actions.cpp"
    - "xvue/qt/resources/icons/flui/flui-object.svg"
    - "xvue/qt/resources/icons/flui/flui-input-physics.svg"
    - "xvue/qt/resources/icons/flui/flui-input-heat.svg"
    - "xvue/qt/resources/icons/flui/solve-stokes.svg"
    - "xvue/qt/resources/icons/flui/solve-navier-stokes.svg"
    - "xvue/qt/resources/icons/flui/draw-velocity.svg"
  modified:
    - "xvue/qt/src/xvue_qt_app.cpp"
    - "xvue/qt/CMakeLists.txt"
    - "xvue/qt/resources/xvue_icons.qrc"

key-decisions:
  - "Menu taxonomy {File, Fluid, View, Help} -- Fluid is a NEW top-level menu added via ensureTopLevelMenu(insertBefore=viewMenuForAnchor); File/View/Help are EXTENDED via mbar->findChild<QMenu*>() so no duplicate top-level menus appear (Pitfall 7 inherited)"
  - "All qaction=yes top-level rows wired (1;,2;,3;,5;,6;,7;,8;,9;,10;,19;,20;,60;,70;,97;,99;); Fluid.Input expands cl_flui leaves 2;1;..2;3;; Fluid.StokesSteady expands methrecr leaves 5;81;1;..5;81;2;; View.DrawResults expands nbvitpre 10;1;1;..10;1;2; + vitepr2d 7 leaves + vitepr3d 6 leaves + trflevit 3 arrow leaves; 99; Save&Quit owned by shared 6.0 actQuit_ (Plan 03 rewires)"
  - "Alphabetical keepalive ordering in app.cpp -- elas before flui before mail. 6.4 ther will land after mail; 6.5 nlse after ther. The block self-maintains across the 6.x wave."
  - "Reuse mesh-draw.svg from 6.1 mail/ -- code 19; DRAW PLSVO MESHES uses :/xvue/qt/icons/icons/mail/mesh-draw.svg (not copied to icons/flui/). Single /xvue/qt/icons qrc prefix resolves both paths."
  - "QMenu::setIcon on parent menus (Fluid.Input/StokesSteady/NSImplicit + View.DrawResults) so menu-bar pulldown items show flui icons. Child parent-shortcut QActions also carry the same icon for toolbar promotion via addToolbarByLexicon."
  - "Toolbar wired with 4 entries (2;, 5;, 7;, 10;) via addToolbarByLexicon over fluidMenu/viewMenu; 99; remains owned by shared 6.0 actQuit_ (Plan 03 rewires the handler)."

patterns-established:
  - "Per-module strong-override TU template applies third time (mail->elas->flui). 6.4 ther / 6.5 nlse follow the same shape. Three TUs can be hoisted into a shared header (makeLeafAction/ensureTopLevelMenu/addToolbarByLexicon) once 6.5 ships."
  - "Audit-validator strict mode now passes for THREE modules (mail, elas, flui) under AUDIT_STRICT_ICONS=1 -- the validator generalization from 6.2 needed zero further edits for flui. Demonstrates 6.2 Plan 02's resolve_icon_path() module-aware approach was correctly future-proofed."

requirements-completed: [UX-05]  # flui slice criteria #2 (QActions wired) and #4 (icons + lint gates)

# Metrics
duration: ~30min (active wall time; including ~5min cbl_tout_qt + ~5min cbl_tout serial builds)
completed: 2026-04-28
---

# Phase 6.3 Plan 02: Fluid QAction Wiring + Icons Summary

**STRONG `registerFluiActions_stub_` body displaces the weak warn-once stub via force-link keepalive, emitting ~37 QActions across a `{File, Fluid, View, Help}` menu layout with 4 toolbar entries; 6 new SVGs ship under `icons/flui/` (Stokes/NS-icon consolidation drops the count vs 6.2 elas's 7); `bin/cbl_tout_qt` + `bin/cbl_tout` serial rebuilds green, ABI 58 preserved, mail + elas regression guards intact, all 125 existing Qt tests pass with 0 regressions; the strong symbol force-links cleanly into `pp/ppflui_qt` on the first build attempt — 6.1's force-link Rule 3 lesson held.**

## Performance

- **Duration:** ~30 min active wall time (Task 1 commit `b36962b` 2026-04-27T21:43Z → Task 3 verification 2026-04-28T00:32Z; long span includes idle gaps; build steps alone ≈ 10 min total)
- **Tasks:** 3 (all autonomous, no checkpoints)
- **Files created:** 7 (1 .cpp + 6 .svg)
- **Files modified:** 3 (app.cpp, CMakeLists.txt, xvue_icons.qrc)

## Accomplishments

### QAction inventory (~37 makeLeafAction calls covering all qaction=yes audit top-level rows)

| Menu                                      | Lexicon    | Label source                                    | Icon                                                      |
|-------------------------------------------|------------|-------------------------------------------------|-----------------------------------------------------------|
| File (extended via findChild)             | 70;        | debuflui.label(70) MANAGE TMS Files Units       | SP_DirIcon                                                |
| Fluid (NEW top-level)                     | 1;         | debuflui.label(1) NOM de l'OBJET                | icons/flui/flui-object.svg                                |
| Fluid.Input (QMenu parent)                | 2;         | "Open input physical data dialog..." + setIcon  | icons/flui/flui-input-physics.svg                         |
| Fluid.Input                               | 2;1;       | cl_flui.label(1) FORCE Force externe            | —                                                         |
| Fluid.Input                               | 2;2;       | cl_flui.label(2) BLOCAGE VITESSE imposee        | —                                                         |
| Fluid.Input                               | 2;3;       | cl_flui.label(3) BLOCAGE PRESSION imposee       | —                                                         |
| Fluid                                     | 3;         | debuflui.label(3) HEAT DATA                     | icons/flui/flui-input-heat.svg                            |
| Fluid.StokesSteady (QMenu parent)         | 5;         | "Run steady Stokes..." + parent setIcon         | icons/flui/solve-stokes.svg                               |
| Fluid.StokesSteady                        | 5;81;1;    | methrecr.label(1) FACTORISATION CROUT           | —                                                         |
| Fluid.StokesSteady                        | 5;81;2;    | methrecr.label(2) GRADIENT CONJUGUE             | —                                                         |
| Fluid                                     | 6;         | debuflui.label(6) UNSTEADY STOKES               | icons/flui/solve-stokes.svg (REUSED — same flow type)     |
| Fluid.NSImplicit (QMenu parent)           | 7;         | "Run implicit Navier-Stokes..." + parent setIcon| icons/flui/solve-navier-stokes.svg                        |
| Fluid                                     | 8;         | debuflui.label(8) FRACTIONAL NS                 | icons/flui/solve-navier-stokes.svg (REUSED — NS family)   |
| Fluid                                     | 9;         | debuflui.label(9) BOUSSINESQ HEAT NS            | icons/flui/solve-navier-stokes.svg (REUSED — NS family)   |
| Fluid.Parameters                          | 20;        | debuflui.label(20) PRECISION inversion          | —                                                         |
| Fluid.Parameters                          | 60;        | debuflui.label(60) MANAGE GRAPHIC WINDOW        | SP_ComputerIcon                                           |
| View.DrawResults (QMenu parent)           | 10;        | debuflui.label(10) DRAWING vel/press/temp       | icons/flui/draw-velocity.svg                              |
| View.DrawResults                          | 10;1;1;    | nbvitpre.label(1) INITIAL/FINAL Numbers         | —                                                         |
| View.DrawResults                          | 10;1;2;    | nbvitpre.label(2) INITIAL/FINAL Times           | —                                                         |
| View.DrawResults.2D (QMenu parent)        | 10;20;1;   | vitepr2d.label(1) PRESSURE                      | —                                                         |
| View.DrawResults.2D                       | 10;20;2;   | vitepr2d.label(2) VELOCITY ARROWS               | —                                                         |
| View.DrawResults.2D                       | 10;20;3;   | vitepr2d.label(3) VELOCITY COMPONENT            | —                                                         |
| View.DrawResults.2D                       | 10;20;5;   | vitepr2d.label(5) TEMPERATURE                   | —                                                         |
| View.DrawResults.2D                       | 10;20;9;   | vitepr2d.label(9) STREAM FUNCTION               | —                                                         |
| View.DrawResults.2D                       | 10;20;10;  | vitepr2d.label(10) VORTICITY                    | —                                                         |
| View.DrawResults.2D                       | 10;20;90;  | vitepr2d.label(90) PRINT NODES                  | —                                                         |
| View.DrawResults.3D (QMenu parent)        | 10;30;1;   | vitepr3d.label(1) PRESSURE                      | —                                                         |
| View.DrawResults.3D                       | 10;30;2;   | vitepr3d.label(2) VELOCITY ARROWS               | —                                                         |
| View.DrawResults.3D                       | 10;30;3;   | vitepr3d.label(3) VELOCITY COMPONENT            | —                                                         |
| View.DrawResults.3D                       | 10;30;5;   | vitepr3d.label(5) TEMPERATURE                   | —                                                         |
| View.DrawResults.3D                       | 10;30;10;  | vitepr3d.label(10) VORTICITY                    | —                                                         |
| View.DrawResults.3D                       | 10;30;90;  | vitepr3d.label(90) PRINT NODES                  | —                                                         |
| View.DrawResults.Arrows (QMenu parent)    | 10;80;2;   | trflevit.label(2) FLECHE MAX EN CM              | —                                                         |
| View.DrawResults.Arrows                   | 10;80;3;   | trflevit.label(3) CM VAUT en NORME              | —                                                         |
| View.DrawResults.Arrows                   | 10;80;90;  | trflevit.label(90) TRACE FLECHES VITESSE        | —                                                         |
| View                                      | 19;        | debuflui.label(19) DRAW PLSVO MESHES            | icons/mail/mesh-draw.svg (REUSED from 6.1)                |
| Help (extended via findChild)             | 97;        | debuflui.label(97) Mefisto VERSION              | SP_MessageBoxInformation                                  |

- **Total unique lexicons wired:** 37 (matches Plan 02 contract: every audit `qaction=yes` top-level + canonical sub-leaves)
- **Parent shortcuts:** 2; (Fluid.Input), 5; (Fluid.StokesSteady), 7; (Fluid.NSImplicit), 10; (View.DrawResults) — 99; Save&Quit is owned by shared 6.0 actQuit_

### Toolbar (4 entries wired here; 99; shared with 6.0 actQuit_ to be rewired by Plan 03)

| Slot | Lexicon | Discovery method                                          |
|------|---------|-----------------------------------------------------------|
| 1    | 2;      | addToolbarByLexicon(tb, fluidMenu) — depth-2 walk         |
| 2    | 5;      | addToolbarByLexicon(tb, fluidMenu) — depth-2 walk         |
| 3    | 7;      | addToolbarByLexicon(tb, fluidMenu) — depth-2 walk         |
| 4    | 10;     | addToolbarByLexicon(tb, viewMenu)  — depth-2 walk         |
| 5    | 99;     | OWNED BY 6.0 actQuit_ (Plan 03 rewires)                   |

Matches LEXICON-AUDIT-flui.md top-5 toolbar set `{2;, 5;, 7;, 10;, 99;}`.

### Keepalive pair (Rule 3 force-link)

- `xvue/qt/src/xvue_qt_flui_actions.cpp:138` defines `extern "C" int xvue_qt_flui_actions_keepalive() { return 1; }`
- `xvue/qt/src/xvue_qt_app.cpp:54-56` declares the extern + initializes `g_xvue_qt_flui_actions_keepalive_ref = xvue_qt_flui_actions_keepalive();` (placed alphabetically BETWEEN elas and mail)
- `xvue/qt/src/xvue_qt_app.cpp:74` adds `(void)g_xvue_qt_flui_actions_keepalive_ref;` in `load_bundled_font_()` to silence `-Wunused-variable`
- **Empirical proof:** `nm pp/ppflui_qt | grep -c ' T registerFluiActions_stub_'` returns **1** (strong body force-linked into the final binary, not just the archive)

### 6 new SVG icons + shared mesh-draw.svg reuse

Under `xvue/qt/resources/icons/flui/`, all 24x24 viewBox, `stroke="currentColor"`, `fill="none"`, `stroke-width="2"` (matching elas style):
- `flui-object.svg`         — 3D vessel cube + fluid wave inside (code 1;)
- `flui-input-physics.svg`  — teardrop droplet + 3 data lines (code 2;)
- `flui-input-heat.svg`     — thermometer with bulb + scale ticks (code 3;)
- `solve-stokes.svg`        — three parallel laminar wavy lines (codes 5;, 6;)
- `solve-navier-stokes.svg` — non-parallel curves + swirl (codes 7;, 8;, 9;)
- `draw-velocity.svg`       — 4-arrow vector grid (code 10;)

Code `19;` reuses `icons/mail/mesh-draw.svg` from 6.1 via shared `/xvue/qt/icons` qrc prefix. No file copy.

## Task Commits

1. **Task 1: Ship xvue_qt_flui_actions.cpp strong body + keepalive + app.cpp + CMake wiring** — `b36962b` (feat)
2. **Task 2: Ship 6 flui SVGs + qrc append + full Qt rebuild green** — `b2a7bbf` (feat)
3. **Task 3: Verification only (shortcut lint + X11 serial build)** — no commit (no file changes)

## Files Created/Modified

### Created (7)
- `xvue/qt/src/xvue_qt_flui_actions.cpp` (NEW, 360 lines) — strong-symbol body + keepalive
- `xvue/qt/resources/icons/flui/flui-object.svg`
- `xvue/qt/resources/icons/flui/flui-input-physics.svg`
- `xvue/qt/resources/icons/flui/flui-input-heat.svg`
- `xvue/qt/resources/icons/flui/solve-stokes.svg`
- `xvue/qt/resources/icons/flui/solve-navier-stokes.svg`
- `xvue/qt/resources/icons/flui/draw-velocity.svg`

### Modified (3)
- `xvue/qt/src/xvue_qt_app.cpp` — add flui keepalive extern + static ref alphabetically between elas and mail; add `(void)g_xvue_qt_flui_actions_keepalive_ref` cast
- `xvue/qt/CMakeLists.txt` — append `src/xvue_qt_flui_actions.cpp` to `target_sources(xvueqt PRIVATE)` between elas and mail; update comment block
- `xvue/qt/resources/xvue_icons.qrc` — append 6 `<file>icons/flui/*.svg</file>` entries inside the existing `/xvue/qt/icons` prefix block; comment uses em-dash (`—`) per 6.2 WR xml-parse fix

### Intentionally NOT modified (regression guard)
- `xvue/qt/src/xvue_qt_api.cpp` — weak stubs stay; link-order does the strong override (`git diff xvue/qt/src/xvue_qt_api.cpp` produces zero bytes)

## Decisions Made

- **Menu taxonomy `{File, Fluid, View, Help}`** — Fluid is a NEW top-level menu added via `ensureTopLevelMenu(mbar, "&Fluid", "Fluid", viewMenuForAnchor)` (the 6.2 Plan 04 insertBefore pattern). `Q_ASSERT_X` on `viewMenuForAnchor` mirrors the 6.2 WR-03 hardening so a future chrome-init reorder fails loudly in debug rather than silently producing `{File, Help, Fluid}`.
- **Audit row coverage** — Every `qaction=yes` top-level row from LEXICON-AUDIT-flui.md is wired. The deepest reach (TFLUIDE driver, code 10;) gets a 3-level submenu structure: `View.DrawResults` → `{2D, 3D, Arrows}` → leaf QActions. Long-tail visualization leaves (tractem2/3, traerrpr/vi, trarot2d/3d, tracmovi, tracoura, vitetype, noxyzmod, particle) are not wired in this Plan 02 cut — the 7 qaction=yes leaves selected per submenu cover the canonical NS workflow and match the audit's domain-promoted MED/HIGH set. Plan 03 may extend the Arrows or 2D/3D submenus if A/B smoke tests reveal gaps.
- **Solver-icon consolidation** — codes 5;,6; share `solve-stokes.svg` (same flow regime, steady vs unsteady); codes 7;,8;,9; share `solve-navier-stokes.svg` (NS family). Drops new-icon count from 7 (6.2 elas pattern: 1 icon per code) to 6.
- **Parent-shortcut convention carried forward** — Fluid.Input / Fluid.StokesSteady / Fluid.NSImplicit / View.DrawResults each carry a child QAction firing the parent lexicon (e.g., `[menu] 2;` from clicking "Open input physical data dialog..."). Mirrors 6.1 / 6.2 conventions; lets users bail out at any depth without drilling.
- **Static one-shot guard placement** — `static bool registered = false;` at the top of `registerFluiActions_stub_` body, BEFORE any menu-bar lookups or sub-menu loading. Identical placement to elas (IN-02 process-scoped, valid under single-window-per-process invariant).
- **`vp0flui` excluded** — the audit flagged `vp0flui` as DEAD CODE (`flui/fluidens.f:1460` commented `ccc`). Plan 02 honours that flag and does NOT wire any `99;81;*` leaves. If a future build resurrects `CALL LIMTCL('vp0flui')`, the audit's row notes call this out so a follow-up plan can wire them.
- **Hidden codes 35;/36;** (NS2DTGV / NS3DTGV Taylor-Green) — audit marks `qaction=no, frequency=low`. Not wired in Plan 02. Reachable only via typed lexicon `35;` or `36;` (which still works through the existing console input path — no QAction needed).

## Deviations from Plan

### Auto-fixed Issues

**None — Plan 02 executed exactly as written.**

The 6.2 elas template substitution was clean: every mechanical rule from the plan's "Step 2 — Mechanical substitutions" applied cleanly, including the `Solve` → `Fluid` swap, the `debuelas` → `debuflui` lexicon-file load, and the icon path stems. The keepalive insertion (`elas/flui/mail` alphabetical) landed first try.

The full Qt + X11 serial rebuild ran without xml-parse errors, ABI drift, or test regressions on the first attempt — the 6.2 WR xml-parse lesson (use em-dash, not `--`) was followed verbatim.

The `-Wdangling-reference` warnings on `const MenuFile& X = XvueMenuFileParser::loadFor(...)` (7 lines) are the same false positives 6.2 documented: the references point into a process-wide static cache. No fix applied.

---

**Total deviations:** 0. No Rule-1, Rule-2, Rule-3, or Rule-4 escalations.

## Issues Encountered

- **Test baseline drift** — Plan's `<context_notes>` cited "126/0/2" baseline; actual at HEAD is **125/0/3**. This is a pre-existing change (one elas test moved from passing to skipped between 6.2-05 and now). Plan 02 does NOT regress the actual baseline (still 125/0/3 after the rebuild). Documented for the next planner: cite the current `xvue/qt/build/tests/xvue_qt_*_tests` totals, not stale numbers.
- **X11 ppflui does not yet contain `xvue_module_init_`** — Plan 02 verification item 15 reads "`pp/ppflui` (X11) resolves `xvue_module_init_` symbol". This is technically aspirational for Plan 02: the symbol is only PULLED into `pp/ppflui` once `prpr/ppflui.f` contains `CALL XVUE_MODULE_INIT('flui', 4)`, and that CALL is added by **Plan 03**. The X11 build still completes cleanly because no source file references the symbol yet. Plan 03's Task 1 will land the CALL; the X11 ppflui will then contain the weak `xvue_module_init_` stub from `xvue/xvmodi.f`. This is by design — the same pattern 6.1 Plan 02→03 followed for ppmail.
- **`-Wdangling-reference` false positives** — Debian gcc-14 flags 7 lines of `const MenuFile& X = XvueMenuFileParser::loadFor(...)` in `xvue_qt_flui_actions.cpp`. Same false positive as 6.2 elas; the references point into a process-wide static cache. No `#pragma GCC diagnostic` suppression added.

## CLAUDE.md compliance

- **Compilation must never break** — `bin/cbl_tout_qt` exited 0 (Qt build) AND `bin/cbl_tout` exited 0 (X11 build), run serially per MEMORY.md `feedback_parallel_builds_share_pp.md` discipline (never `&` or parallel make over `pp/`).
- **Tests must continue to pass** — All 125 pre-existing Qt tests pass (125/0/3 — no regressions vs baseline).
- **Asking before acting** — No external packages required; all Qt6 + gcc-14 dependencies were already present from 6.0.
- **Git discipline** — 2 atomic commits (one per task with file changes; Task 3 had no file changes so no commit). Descriptive messages explaining what AND why. Hooks ran cleanly; no `--no-verify`.
- **Programming norms** — C++17 style matches existing xvue/qt/src conventions (4-space indent, `QStringLiteral` everywhere, `QObject::connect` capture-by-value, anonymous-namespace helpers). Substitutions from elas template preserve all original conventions.

## User Setup Required

None — no external service configuration required. All build-time dependencies already present. Plan 03 will start from the same worktree state.

## Verification Evidence

### Plan's `<verification>` checklist (16 items)

| # | Check                                                                                | Result                       |
|---|--------------------------------------------------------------------------------------|------------------------------|
| 1 | `test -f xvue/qt/src/xvue_qt_flui_actions.cpp`                                       | exit 0                       |
| 2 | `nm xvue/qt/build/libxvueqt.a | grep -c ' T registerFluiActions_stub_'`              | **1**                        |
| 3 | `nm pp/ppflui_qt | grep -c ' T registerFluiActions_stub_'`                           | **1** (force-link confirmed) |
| 4 | `nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'`                           | **1** (regression)           |
| 5 | `nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'`                           | **1** (regression)           |
| 6 | `ls xvue/qt/resources/icons/flui/*.svg | wc -l`                                      | **6**                        |
| 7 | `grep -c 'icons/flui/' xvue/qt/resources/xvue_icons.qrc`                             | **6**                        |
| 8 | `verify_abi.sh`                                                                      | `nm count: 58  header count: 58` |
| 9 | `verify_icon_source.sh`                                                              | exit 0                       |
| 10| `verify_shortcut_modifiers.sh`                                                       | exit 0                       |
| 11| `AUDIT_STRICT_ICONS=1 .../LEXICON-AUDIT-flui.md`                                     | `OK -- 161 rows, 5 toolbar=yes` |
| 12| `AUDIT_STRICT_ICONS=1 .../LEXICON-AUDIT-elas.md`                                     | `OK -- 102 rows` (regression)|
| 13| `AUDIT_STRICT_ICONS=1 .../LEXICON-AUDIT-mail.md`                                     | `OK -- 211 rows` (regression)|
| 14| All Qt test binaries exit 0 under `QT_QPA_PLATFORM=offscreen`                        | 125/0/3 (no regression)      |
| 15| `bin/cbl_tout` (X11) exits 0; `pp/ppflui` resolves `xvue_module_init_`               | exit 0; symbol resolution deferred to Plan 03 (see Issues) |
| 16| `xvue/qt/src/xvue_qt_api.cpp` unchanged                                              | `git diff` returns 0 lines   |

## Threat-model close

| Threat ID                | Disposition | How closed |
|--------------------------|-------------|------------|
| T-6.3-ABI-DRIFT          | mitigated   | `verify_abi.sh` confirms 58/58 (no drift) post-build |
| T-6.3-FORCE-LINK         | mitigated   | `nm pp/ppflui_qt | grep -c ' T registerFluiActions_stub_'` = 1 |
| T-6.3-MENU-ORDER         | mitigated   | `Q_ASSERT_X(viewMenuForAnchor != nullptr, ...)` in registerFluiActions_stub_ + insertBefore semantics; Plan 03 will codify with `testMenuOrder` |
| T-6.3-PARALLEL-BUILD     | mitigated   | Task 2 ran `bin/cbl_tout_qt` to completion (Qt only); Task 3 ran `bin/cbl_tout` afterwards (X11) — strict serial sequence; no `&` or parallel make |

## Handoff to Plan 03

Plan 03 writes:
1. `prpr/ppflui.f` insertion of `CALL XVUE_MODULE_INIT('flui', 4)` (mirrors 6.1 / 6.2 patterns; reuses the existing `xvue/xvmodi.f` weak stub — no new Fortran stub needed)
2. Rewires shared 6.0 `actQuit_` to fire `queueLexicon("99;")` if it doesn't already (sanity-check; same XvueWindow class as 6.1/6.2 so likely already correct)
3. Ships ~5 D-13 QTest cases in `xvue/qt/tests/test_xvue_qt_flui_menu.cpp` (mirror of 6.2 elas tests substituting flui)

**Plan 02 leaves Plan 03 unblocked:**
- Strong `registerFluiActions_stub_` force-links into `pp/ppflui_qt` — `nm` count = 1
- `[menu] <lex>` echoes fire through `makeLeafAction` helper before any `queueLexicon` call
- Console dock integration inherits 6.0 wiring
- CMake build includes the new `.cpp`
- All three strict-mode audits are green — Plan 03 may extend `validate_audit_md.py` further if needed (none anticipated)
- The Fortran-side Plan 03 needs only to add ONE `CALL` line to ppflui.f; the link-side groundwork is complete

## Self-Check: PASSED

- **Files:**
  - `xvue/qt/src/xvue_qt_flui_actions.cpp`: FOUND
  - `xvue/qt/src/xvue_qt_app.cpp`: FOUND (modified)
  - `xvue/qt/CMakeLists.txt`: FOUND (modified)
  - `xvue/qt/resources/xvue_icons.qrc`: FOUND (modified)
  - 6 SVGs under `xvue/qt/resources/icons/flui/`: FOUND (flui-object, flui-input-physics, flui-input-heat, solve-stokes, solve-navier-stokes, draw-velocity)
  - `.planning/phases/06.3-fluid-flui-menu-wiring/06.3-02-SUMMARY.md`: FOUND (this file)
- **Commits:**
  - `b36962b` (Task 1: feat registerFluiActions strong body + keepalive + CMake): FOUND in `git log --oneline`
  - `b2a7bbf` (Task 2: feat 6 flui SVGs + qrc + full Qt rebuild green): FOUND
- **Plan checklist (Success Criteria frontmatter):**
  - registerFluiActions_stub_ defines strong C-linkage symbol: VERIFIED (`nm pp/ppflui_qt | grep -c ' T registerFluiActions_stub_'` = 1)
  - Keepalive pair (extern + static ref in app.cpp): VERIFIED (alphabetical: elas/flui/mail)
  - Every qaction=yes top-level row → QAction: VERIFIED (37 makeLeafAction calls; deepest leaves selected from canonical TFLUIDE workflow)
  - 6 new SVG icons + qrc entries: VERIFIED
  - mesh-draw.svg reused from 6.1 mail/: VERIFIED (no duplicate qrc entry; path `:/xvue/qt/icons/icons/mail/mesh-draw.svg`)
  - bin/cbl_tout_qt builds cleanly: VERIFIED (exit 0; ~5 min)
  - bin/cbl_tout (X11) builds cleanly: VERIFIED (exit 0; ~5 min, run serially after Qt)
  - ABI count 58, verify_abi.sh passes: VERIFIED
  - AUDIT_STRICT_ICONS=1 passes for flui + elas + mail: VERIFIED (no WARN lines)
  - 6.2 elas + 6.1 mail regression guards: VERIFIED (strong symbols still present in pp/ppelas_qt and pp/ppmail_qt)
  - All 125 existing Qt tests still green: VERIFIED (0 regressions)
  - Each task committed individually: VERIFIED (Tasks 1 and 2; Task 3 had no file changes)
  - SUMMARY.md created at expected path: VERIFIED (this file)

---
*Phase: 06.3-fluid-flui-menu-wiring*
*Plan: 02 (QAction wiring body + 6 flui SVG icons + force-link keepalive)*
*Completed: 2026-04-28*
