---
phase: 06.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md
autonomous: false
requirements:
  - UX-05
threat_refs:
  - T-6.3-AUDIT-01
  - T-6.3-AUDIT-02

must_haves:
  truths:
    - "LEXICON-AUDIT-flui.md exists under .planning/phases/06.3-fluid-flui-menu-wiring/ with the exact 9-column schema frozen in 6.1 (lexicon_path / description_fr / description_en / frequency / qaction / toolbar / icon_source / shortcut / notes)"
    - "Audit enumerates every LIMTCL leaf reachable from `td/m/debuflui` (15 top-level codes) via a full recursive tree walk over flui/ + util/ + reso/ + prpr/ppflui.f call sites — expected row count 80-200, validated by tools/validate_audit_md.py"
    - "Every row with frequency in {high, med} has qaction=yes; every row with frequency=low has qaction=no"
    - "Exactly 5 rows have toolbar=yes — top-5 most-used flui leaves derived from testa/*/*.flui evidence + domain review"
    - "tools/validate_audit_md.py exits 0 against the final flui audit (already module-aware after 6.2 Plan 02 — no validator changes needed)"
    - "User has reviewed and approved the frequency bucketing AND the top-5 toolbar set AND the menu taxonomy mapping ({File, Fluid, View, Help}) before the audit is frozen"
  artifacts:
    - path: ".planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md"
      provides: "Per-module flui lexicon audit (UX-05 flui-slice Success Criterion #1 deliverable)"
      contains: "9-column GitHub-markdown table identical schema to LEXICON-AUDIT-elas.md, rooted at debuflui (not debut, not debuelas)"
  key_links:
    - from: ".planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md"
      to: "tools/validate_audit_md.py (module-aware ICONS resolver from 6.2 Plan 02)"
      via: "schema rules enforced at lint time — 9 hard rules apply unchanged"
      pattern: "python3 tools/validate_audit_md.py .planning/phases/06.3-*/LEXICON-AUDIT-flui.md"
    - from: "LEXICON-AUDIT-flui.md rows with qaction=yes"
      to: "Plan 02 registerFluiActions_stub_ QAction set"
      via: "frequency-bucketed rows are the QAction wire-up list"
      pattern: "qaction=yes AND frequency in (high, med)"
    - from: "LEXICON-AUDIT-flui.md rows with toolbar=yes"
      to: "Plan 02 toolbar append set"
      via: "top-5 leaves from the frequency-scored audit"
      pattern: "toolbar=yes (count == 5)"
---

<objective>
Produce `.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md` — the
full recursive catalogue of every interactive command reachable from the
fluider's `td/m/debuflui` LIMTCL tree (15 top-level codes), scored with
high/med/low frequency buckets from `testa/*/*.flui` grep evidence + domain
review, then reviewed and frozen at the user checkpoint before the QAction set
is consumed by Plan 02.

Reuse `tools/validate_audit_md.py` verbatim — the validator was generalized
in 6.2 Plan 02 (module-aware ICONS resolver via `LEXICON-AUDIT-<mod>` filename
stem). No tooling changes needed in this plan.

Purpose: UX-05 flui-slice Success Criterion #1. Every decision downstream
(which QActions exist, which icons are custom-SVG vs Qt built-in, which 5
leaves go on the toolbar) flows from this audit. The validator closes the
loop — it is the only automated verification available for a markdown
deliverable.

Output: One markdown file in the phase directory, plus a user-approved top-5
toolbar set and the {File, Fluid, View, Help} menu taxonomy ratified against
the audit data (differs from 6.1 mail's {File, Mesh, View, Help} and 6.2
elas's {File, Solve, View, Help}; per ROADMAP Phase 6.3 line 201 the elas
"Solve" slot becomes "Fluid" for the fluider).
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md
@.planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md
@.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md
@.planning/phases/06.2-elasticity-elas-menu-wiring/06.2-01-SUMMARY.md
@.planning/phases/06.2-elasticity-elas-menu-wiring/06.2-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key LIMTCL data for the auditor — flui-specific (extracted during planning) -->

### flui top-level menu file (VERIFIED paths)
- FR (runtime active): `$MEFISTO/td/m/debuflui` (821 bytes)
- FR (backup):         `$MEFISTO/td/mf/debuflui` (821 bytes)
- EN (source for EN install): `$MEFISTO/td/ma/debuflui` (688 bytes)
- Parser: `util/limtcl.f:54` (`KNOM = HOMDIR // '/td/m/' // KNM`) — same path as mail/elas

### debuflui top-level (15 codes — VERIFIED against td/m/debuflui)

| Code | FR label                                                  | EN label                                          |
|------|-----------------------------------------------------------|---------------------------------------------------|
| 1    | NOM de l'OBJET a traiter                                  | OBJECT NAME to treat                              |
| 2    | ENTRER les DONNEES PHYSIQUES du FLUIDE                    | GIVE the INPUT PHYSICAL DATA of FLUID             |
| 3    | ENTRER les DONNEES THERMIQUES du FLUIDE                   | GIVE the INPUT HEAT DATA of FLUID                 |
| 5    | RESOUDRE le PROBLEME de STOKES STATIONNAIRE               | INCOMPRESSIBLE STEADY STOKES SOLVER               |
| 6    | RESOUDRE le PROBLEME de STOKES INSTATIONNAIRE             | INCOMPRESSIBLE UNSTEADY STOKES SOLVER             |
| 7    | RESOUDRE NAVIER-STOKES INSTATIONNAIRE IMPLICITE           | IMPLICIT UNSTEADY NAVIER-STOKES SOLVER            |
| 8    | RESOUDRE NAVIER-STOKES INSTATIONNAIRE FRACTIONNAIRE       | FRACTIONAL UNSTEADY NAVIER-STOKES SOLVER          |
| 9    | RESOUDRE NAVIER-STOKES THERMIQUE BOUSSINESQ INSTATIONNAIRE| UNSTEADY BOUSSINESQ HEAT NAVIER-STOKES SOLVER     |
| 10   | DESSINER des VITESSES PRESSIONS TEMPERATURES              | DRAWING of VELOCITIES PRESSURES TEMPERATURES      |
| 19   | TRACER des MAILLAGES des PLSVO                            | DRAWING of PLSVO MESHES                           |
| 20   | PRECISION pour INVERSER A x = b                           | PRECISION to SOLVE A x = b                        |
| 60   | GERER la FENETRE GRAPHIQUE de Mefisto                     | MANAGE the Mefisto GRAPHIC WINDOW                 |
| 70   | GERER les TMS Fichiers Unites de Mefisto                  | MANAGE the Mefisto TMS Files Units                |
| 97   | AFFICHER la DATE de la version de Mefisto                 | Mefisto VERSION NAME                              |
| 99   | SAUVEGARDER les DONNEES et FIN du TRAITEMENT              | SAVE DATA and QUIT                                |

Note: codes 35 (NS2DTGV / TAYLOR-GREEN 2D) and 36 (NS3DTGV / TAYLOR-GREEN 3D)
appear in `prpr/ppflui.f` GOTO table (lines 235-236) but are NOT in the
top-level `td/m/debuflui` menu file. Treat them as "hidden codes" — they fire
through the typed lexicon (e.g., `35;`) but no menu entry is needed unless
the user-review checkpoint upgrades them.

### Flui-reachable sub-menus (42 total — VERIFIED via grep 2026-04-27)

```
$ grep -rEoh "LIMTCL\( *'[^']+'" flui/ util/ prpr/ppflui.f reso/ 2>/dev/null \
  | sed "s/LIMTCL( *'//;s/'$//" | sort -u | wc -l
42
```

Sub-menu set (full list extracted at planning time):

```
affiche, cl_flui, couleur0, couleurs, debuflui, entrsort, fichier,
lecteur, managfen, managmef, methrecr, methvvpr, mode_es, nbvitpre,
noxyzmod, opt_lign, opt_surf, particle, sectvopl, suivi_ms, suivitms,
tracaxes, tracmovi, tracoura, tracpr2d, tracpr3d, tractem2, tractem3,
traerrpr, traerrvi, trarot2d, trarot3d, trflevit, tuer, typ_objt,
typtrait, vitepr2d, vitepr3d, vitetype, vp0flui, vuesplan, zeros
```

Sub-menus UNIQUE to flui (not in 6.1 mail audit AND not in 6.2 elas audit
— so new rows needed and NEW data tables to read):

`cl_flui` (CL fluide — boundary conditions),
`methrecr` (méthode de résolution Crank-Nicolson),
`nbvitpre` (nb itérations vitesse-pression),
`noxyzmod` (n° XYZ modifié),
`particle` (particle tracker),
`tracmovi` (tracé mouvements de vitesses),
`tracoura` (tracé courbes/auras),
`tracpr2d` (tracé pression 2D),
`tracpr3d` (tracé pression 3D),
`tractem2` (tracé température 2D),
`tractem3` (tracé température 3D),
`traerrpr` (tracé erreur pression),
`traerrvi` (tracé erreur vitesse),
`trarot2d` (tracé rotationnel 2D),
`trarot3d` (tracé rotationnel 3D),
`trflevit` (tracé flèches vitesse),
`vitepr2d` (vitesse-pression 2D),
`vitepr3d` (vitesse-pression 3D),
`vitetype` (type de tracé vitesse),
`vp0flui` (vitesse pression initiale flui)

That's 21 flui-unique sub-menus (verified via filesystem `ls td/m/<name>` —
all 21 exist as 100-800 byte LIMTCL files).

Sub-menus SHARED with mail/elas (same td/m/ files — can compress to 1 row
per the 6.2 de-duplication rule):

`affiche, couleur0, couleurs, entrsort, fichier, lecteur, managfen,
managmef, methvvpr, mode_es, opt_lign, opt_surf, sectvopl, suivi_ms,
suivitms, tracaxes, tuer, typ_objt, typtrait, vuesplan, zeros`

That's 21 shared util sub-menus.

Total: 21 flui-unique fully-expanded + 21 shared util compressed + 1
debuflui top = 43 sub-menus catalogued, ~80-180 rows depending on
leaf-density of the flui-unique sub-menus (cl_flui ~5 leaves, particle ~10,
tracpr3d ~15-20 etc.).

### Frequency evidence source

`testa/*/` contains flui projects. The `.flui` files (batch scripts) are
the primary source of typed-command frequency for flui:

```bash
find testa/ -name '*.flui' 2>/dev/null
# expect testa/cavity2d/cavity2d.flui as canonical (per BASELINE.md), plus
# any wave/cavity3d siblings.
```

Run at audit time:
```bash
grep -hE "^ *[0-9]+; *\{" testa/*/*.flui 2>/dev/null \
  | grep -oE "^ *[0-9]+;" | sort | uniq -c | sort -rn > /tmp/flui_freq.txt
```

Bucketing rubric (from 6.1 RESEARCH §Example 6, adapted to the smaller flui
corpus):
- HIGH: count >= 5 across testa/ .flui files (target: 5-8 codes)
- MED:  count 2-4 (target: 6-10 codes)
- LOW:  count 0-1 or zero-count leaves from sub-menus (target: ~80-150)

If the flui testa corpus is sparse, supplement by domain review of the
debuflui codes most commonly expected in a Navier-Stokes workflow:
- `1;` Object name (always set first)
- `2;` Physical data (mandatory before any solve)
- `5;` Steady Stokes (canonical first solver run)
- `7;` or `8;` Implicit/fractional Navier-Stokes (production solvers)
- `10;` Draw velocities/pressures (primary visual)
- `19;` Draw mesh (mesh inspection)
- `99;` Save & quit

These are a priori HIGH based on domain knowledge and the 6.3 UI-SPEC Fluid
menu priorities. Document reasoning in the audit's scope section, mirroring
the 6.2-01-SUMMARY.md "domain-review promotion" pattern.

### Validator (reused from 6.1, generalized in 6.2 Plan 02)

`tools/validate_audit_md.py` — module-aware via `resolve_icon_path()`. Rule
9 deduces the owning module from the audit filename stem
(`LEXICON-AUDIT-flui` → `flui`), looks up `xvue/qt/resources/icons/flui/<file>`,
falls back to scanning all module subdirs for shared reuses (e.g.,
`mesh-draw.svg` from `icons/mail/` for code 19; flui's "Draw PLSVO mesh").

**Plan 01 runs the validator in WARN mode** (default — no `AUDIT_STRICT_ICONS=1`)
because Plan 02 will ship the new flui SVGs after Plan 01 freezes the audit.
Plan 02 flips to strict mode after the SVGs land.

### Top-level menu taxonomy (per ROADMAP + UI-SPEC)

ROADMAP.md Phase 6.3 line 201 specifies `{File, Fluid, View, Help}`. Per
06.0-UI-SPEC §Per-Module Conformance Contract, the flui module-specific
menu title is `Fluid / Fluide` (EN / FR).

Menu-content mapping PROPOSED (user ratifies at Task 2 checkpoint):

- **File menu** extensions (shared 6.0 File + flui additions):
  - `70;` GERER les TMS Fichiers Unites      (File > MS File Tools / TMS Tools)
  - `99;` SAVE DATA and QUIT                  (already bound by 6.0 shared
    Quit action — Plan 02 inherits 6.2 pattern: 99; toolbar slot owned by
    shared actQuit_)

- **Fluid menu** (NEW top-level, replaces "Solve" in taxonomy):
  - `1;`  OBJECT NAME to treat
  - `2;`  GIVE the INPUT PHYSICAL DATA of FLUID    (cl_flui submenu)
  - `3;`  GIVE the INPUT HEAT DATA of FLUID
  - `5;`  INCOMPRESSIBLE STEADY STOKES SOLVER
  - `6;`  INCOMPRESSIBLE UNSTEADY STOKES SOLVER
  - `7;`  IMPLICIT UNSTEADY NAVIER-STOKES SOLVER
  - `8;`  FRACTIONAL UNSTEADY NAVIER-STOKES SOLVER
  - `9;`  UNSTEADY BOUSSINESQ HEAT NAVIER-STOKES SOLVER
  - `Parameters` submenu:
    - `20;` PRECISION to SOLVE A x = b
    - `60;` MANAGE GRAPHIC WINDOW (window dimensions, background)

- **View menu** extensions (shared 6.0 View + flui additions):
  - `10;` DRAWING of VELOCITIES PRESSURES TEMPERATURES (View > Draw Results
    — primary visual; expands to vitepr2d/vitepr3d/tracpr2d/tracpr3d/
    tractem2/tractem3/trarot2d/trarot3d/trflevit submenus per the audit)
  - `19;` DRAWING of PLSVO MESHES                       (View > Draw Mesh
    — REUSE mesh-draw.svg from 6.1 mail/)

- **Help menu** extensions (shared 6.0 Help + flui additions):
  - `97;` MEFISTO VERSION NAME    (Help > Version — or leave to shared
    6.0 About)

### Custom SVG icon set (proposed — 5-7 icons, fewer than 6.2's 7)

Following 6.1's pattern (icons under `xvue/qt/resources/icons/<mod>/`),
6.3 proposes:

- `flui-object.svg`         — Object name (code 1;) — flui-flavoured cube
- `flui-input-physics.svg`  — Physical data (code 2;) — fluid props icon
- `flui-input-heat.svg`     — Heat data (code 3;) — thermometer-on-fluid
- `solve-stokes.svg`        — Steady/Unsteady Stokes (codes 5;,6;) — laminar lines
- `solve-navier-stokes.svg` — NS solver (codes 7;,8;,9;) — turbulent flow
- `draw-velocity.svg`       — Draw velocities (code 10;) — vector arrows

Plus reuse from 6.1:
- `mesh-draw.svg` from `icons/mail/` — code 19; (Draw PLSVO mesh; same as 6.2)

That's 6 new flui-specific SVGs + 1 reuse. May shrink to 5 if the
user-review checkpoint folds Stokes + Navier-Stokes into a single
`solve-flow.svg` icon (since both are flow solvers and carry similar
visual semantics).

### Proposed top-5 toolbar (user ratifies at Task 2)

Candidate set based on flui workflow ergonomics + debuflui frequency:

1. `2;`  GIVE PHYSICAL DATA              (mandatory first step)
2. `5;`  INCOMPRESSIBLE STEADY STOKES    (canonical first solver run)
3. `7;`  IMPLICIT UNSTEADY NAVIER-STOKES (primary production solver)
4. `10;` DRAWING of VELOCITIES PRESSURES (primary result visual)
5. `99;` SAVE DATA and QUIT               (shared convention — close session)

User may swap (e.g. `7;` ↔ `8;` if FRACTIONAL is preferred over IMPLICIT,
or `5;` ↔ `1;` if "Object first" workflow is more common). Any change must
keep the count at exactly 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full recursive LIMTCL tree walk + draft flui audit markdown</name>
  <files>.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md</files>
  <read_first>
    - .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md (the frozen 6.2 template — COPY the structure: same top-matter sections, same Legend, same 9-column table header, same Summary Statistics block at the bottom)
    - .planning/phases/06.2-elasticity-elas-menu-wiring/06.2-01-SUMMARY.md (understand the 6.2 de-duplication rule: module-specific fully expanded; shared util listed once with notes cell citing 6.1 mail audit)
    - .planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md (6.1 audit — useful as the secondary reference for shared-util compressed-row patterns)
    - .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md §Per-Module Conformance Contract (per-module menu-bar contract: `{File, <Module>, View, Help}` left-to-right; flui <Module> title is `Fluid/Fluide`)
    - .planning/ROADMAP.md §"Phase 6.3" (goal line 201 — `{File, Fluid, View, Help}` — confirm taxonomy)
    - td/m/debuflui, td/ma/debuflui, td/mf/debuflui (top-level 15-code menu — FR active, EN source, FR backup; FR labels list above is verbatim from td/m/debuflui)
    - td/m/cl_flui (the flui-unique boundary-conditions sub-menu — likely 4-6 leaves; analogous to elas's cl_elas)
    - td/m/methrecr (Crank-Nicolson solver method)
    - td/m/nbvitpre, td/m/noxyzmod (velocity/pressure iteration parameters)
    - td/m/particle (particle tracker — flui-unique)
    - td/m/tracmovi, td/m/tracoura (motion + curves drawing — flui-unique)
    - td/m/tracpr2d, td/m/tracpr3d (pressure drawing 2D/3D)
    - td/m/tractem2, td/m/tractem3 (temperature drawing 2D/3D — used when code 9; thermal NS runs)
    - td/m/traerrpr, td/m/traerrvi (error drawing for pressure / velocity)
    - td/m/trarot2d, td/m/trarot3d (rotational/vorticity drawing)
    - td/m/trflevit (velocity arrows)
    - td/m/vitepr2d, td/m/vitepr3d (velocity-pressure drawing)
    - td/m/vitetype, td/m/vp0flui (velocity type, initial vel/pressure)
    - util/limtcl.f (runtime grammar reference — same as 6.1/6.2 — confirms td/m/ is the runtime path)
    - util/langue.f (bilingual flag — confirms the install-time FR/EN swap mechanism)
    - prpr/ppflui.f (15 GOTO-table dispatch points — lines 231-241 — and the calls to DFFLUI/DFTHER/STOKESTA/FLUIDENS that drive the cl_flui/methrecr/etc submenu opens)
    - flui/*.f (the source files that call LIMTCL on the 21 flui-unique sub-menus — find via `grep -rl "LIMTCL.*cl_flui" flui/` etc.)
    - testa/cavity2d/ and any other testa/*/ flui fixtures — source for typed-command frequency evidence (`.flui` files)
    - CLAUDE.md (project norms reminder — Fortran fixed-form — not directly touched by this plan, but docs must respect project conventions)
    - tools/validate_audit_md.py (the 6.1+6.2 validator — module-aware as of 6.2 Plan 02; no changes needed; just runs against LEXICON-AUDIT-flui.md transparently)
  </read_first>
  <action>
Produce `.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md` by
walking the LIMTCL tree reachable from `td/m/debuflui` via `flui/`, `util/`,
`prpr/ppflui.f`, and `reso/` call sites, scoring frequency from
`testa/*/*.flui` grep evidence + domain review, and filling the 9-column schema
FROZEN in 6.1 Plan 01 / re-applied in 6.2 Plan 01.

### Step 1 — Re-verify the reachable sub-menu set (drift check)

```bash
cd /home/mefisto/git/mefisto
grep -rEoh "LIMTCL\( *'[^']+'" flui/ util/ prpr/ppflui.f reso/ 2>/dev/null \
  | sed "s/LIMTCL( *'//;s/'$//" | sort -u > /tmp/flui_limtcl_set.txt
wc -l /tmp/flui_limtcl_set.txt   # expect ~42 lines
```

If the count diverges from 42 (the baseline captured during planning) by
more than 2, investigate the delta before proceeding — append a note in
the audit scope section naming the new/removed sub-menus.

### Step 2 — Collect frequency evidence

Flui uses `.flui` batch files as the canonical typed-lexicon log:

```bash
cd /home/mefisto/git/mefisto
grep -hE "^ *[0-9]+; *\{" testa/*/*.flui 2>/dev/null \
  | grep -oE "^ *[0-9]+;" | sort | uniq -c | sort -rn > /tmp/flui_freq.txt
cat /tmp/flui_freq.txt
```

Also scan any `.batch` or composite scripts that chain flui commands:

```bash
grep -hE "^ *[0-9]+; *\{" testa/*/*.batch 2>/dev/null \
  | grep -oE "^ *[0-9]+;" | sort | uniq -c | sort -rn
```

Bucketing rubric (from 6.2-01-SUMMARY.md, adapted to the small flui corpus):
- HIGH: count >= 5 across testa/ .flui files (target: 5-8 codes)
- MED:  count 2-4 (target: 6-10 codes)
- LOW:  count 0-1 or zero-count leaves from sub-menus (target: ~80-150)

If the flui testa corpus is sparse (fewer than 5 HIGH candidates by frequency
alone), supplement via domain review:
- `1;`, `2;` HIGH (mandatory first-step actions per ppflui.f line 245-322)
- `5;` HIGH (canonical Stokes solver — goes directly to `STOKESTA` at line 338)
- `7;` HIGH or `8;` HIGH (production NS solvers)
- `10;` HIGH (primary visual — calls `TFLUIDE` at line 399)
- `19;` MED (mesh inspection — TRMAIL at line 403)
- `99;` HIGH (save&quit)
- `35;`/`36;` MED (Taylor-Green table — domain-review only; not in
  td/m/debuflui but live in ppflui.f GOTO table)

Document reasoning in the audit's Scope section, mirroring 6.2-01-SUMMARY.md
"Domain-review promotion" pattern.

### Step 3 — Apply the tree-walk row strategy (reuse 6.1 rule)

De-duplicate per the 6.2 resolved recommendation:

- **Flui-specific sub-menus fully expand** — every leaf gets its own row
  with the full parent-prefixed `lexicon_path`. Applies to: `cl_flui`,
  `methrecr`, `nbvitpre`, `noxyzmod`, `particle`, `tracmovi`, `tracoura`,
  `tracpr2d`, `tracpr3d`, `tractem2`, `tractem3`, `traerrpr`, `traerrvi`,
  `trarot2d`, `trarot3d`, `trflevit`, `vitepr2d`, `vitepr3d`, `vitetype`,
  `vp0flui` (20 flui-unique sub-menus per the interfaces list — the count
  was 21 above but `cl_flui` is sometimes counted twice if it appears
  under both code 2; and code 3; reach; the audit consolidates to canonical
  parent prefix per the 6.2 disambiguation rule).

  NOTE: `methvvpr` is shared with 6.2 elas (eigenmodes solver method) but
  flui never reaches it (`prpr/ppflui.f` does not call eigenmodes). Keep
  it in the SHARED util compressed list, not the flui-unique expanded
  list.

- **Shared util sub-menus listed ONCE** with a `notes` cell pointing at
  the 6.1/6.2 audits for the leaf-level enumeration, using compression
  form `<parent>;N;` (a representative valid numeric path). Applies to:
  `affiche`, `couleur0`, `couleurs`, `entrsort`, `fichier`, `lecteur`,
  `managfen`, `managmef`, `methvvpr`, `mode_es`, `opt_lign`, `opt_surf`,
  `sectvopl`, `suivi_ms`, `suivitms`, `tracaxes`, `tuer`, `typ_objt`,
  `typtrait`, `vuesplan`, `zeros` (21 shared sub-menus).

- Leaf-level `lexicon_path` format: single-line semicolon-separated, no
  spaces (e.g. `5;1;` not `5 ; 1 ;`). SAME as 6.1/6.2 — validator regex
  `^\d+;(?:\d+;)*$` is unchanged.

Target row count: 80-200 (well within validator bound `[80, 250]`).
Realistic estimate given 20 flui-unique fully expanded + 21 shared util
compressed: expect ~95-160 rows depending on leaf density of the
visualisation sub-menus (the `tracpr*`, `tractem*`, `vitepr*`, `tracmovi`
families can each have 4-10 leaves).

### Step 4 — Write the markdown (mirror 6.2 structure verbatim)

Top matter (copy from 6.2 LEXICON-AUDIT-elas.md, substitute values):

```markdown
# LEXICON-AUDIT-flui.md

**Phase:** 06.3-fluid-flui-menu-wiring
**Generated:** <YYYY-MM-DD>
**Status:** DRAFT (pending user review — Task 2 checkpoint)
**Requirement:** UX-05 (flui slice) Success Criterion #1

## Scope

Full recursive LIMTCL tree walk from `td/m/debuflui` via flui/ + util/ +
reso/ + prpr/ppflui.f call-sites. 42 sub-menus (re-verified
<YYYY-MM-DD>). De-duplication rule from 6.1 Plan 01 / 6.2 Plan 01:

- **Flui-specific sub-menus fully expanded** — cl_flui, methrecr,
  nbvitpre, noxyzmod, particle, tracmovi, tracoura, tracpr2d, tracpr3d,
  tractem2, tractem3, traerrpr, traerrvi, trarot2d, trarot3d, trflevit,
  vitepr2d, vitepr3d, vitetype, vp0flui (20 sub-menus, full leaf
  expansion).
- **Shared util sub-menus listed ONCE** with notes cell citing 6.1
  mail / 6.2 elas audits for leaf-level detail — affiche, couleur0,
  couleurs, entrsort, fichier, lecteur, managfen, managmef, methvvpr,
  mode_es, opt_lign, opt_surf, sectvopl, suivi_ms, suivitms, tracaxes,
  tuer, typ_objt, typtrait, vuesplan, zeros (21 sub-menus, compressed
  to 1 row each).

**Total row count: <N>** — within the validator bound `[80, 250]` (same
as 6.1/6.2).

Frequency bucketing from `testa/*/*.flui` grep evidence (+ domain review
for sparse corpora). Per CONTEXT carryover from 6.1/6.2: rows with
`qaction=yes` have `frequency` in {high, med}; exactly 5 rows have
`toolbar=yes`.

Schema: 9 columns, enforced by `tools/validate_audit_md.py` (shipped in
6.1 Plan 01, generalized to module-aware in 6.2 Plan 02 — reused
verbatim).

## Known data quirks

- `prpr/ppflui.f` GOTO table at line 241 dispatches codes 1..10 plus
  hidden codes 19, 20, 35, 36, 60, 70, 97, 99. Codes 35, 36 (Taylor-Green
  result tables) ARE in the GOTO table but NOT in `td/m/debuflui` —
  treat as hidden lexicons (qaction=no, frequency=low) unless user
  review at Task 2 promotes them.
- Code 4 is absent from both td/m/debuflui AND the GOTO table (the array
  is `(100, 200, 300, 30, 500, 600, 700, 800, 900, 1000)` — slot 4 is
  `30` which is the input-loop reset; effectively a no-op). Skip the
  `4;` row entirely.
- ppflui.f lines 379-394 contain commented-out `ccc` blocks for old
  PISO+Charac and PISO+(V.D)V solvers (codes 7,8 alternate algorithms).
  These are dead code; do NOT add audit rows for them.
- Apply the 6.1 Pitfall 5 log-and-fallback discipline to any sub-menu
  typo encountered during the walk.

## Legend

(Copy verbatim from 6.2 LEXICON-AUDIT-elas.md.)
```

Then a single 9-column table with the exact header row from 6.1/6.2:

```
| lexicon_path | description_fr | description_en | frequency | qaction | toolbar | icon_source | shortcut | notes |
|--------------|----------------|----------------|-----------|---------|---------|-------------|----------|-------|
```

### Step 5 — Populate the 15 debuflui top-level rows first

Fill every debuflui code (1, 2, 3, 5, 6, 7, 8, 9, 10, 19, 20, 60, 70, 97,
99) from the td/m/debuflui and td/ma/debuflui files (verbatim FR/EN
labels), then score each via Step 2's frequency output + domain review.

For the `icon_source` column initial values (flui-specific suggestions —
user ratifies at Task 2):

- `1;`  OBJECT NAME           → `flui-object.svg` (new, Plan 02 ships)
- `2;`  PHYSICAL DATA          → `flui-input-physics.svg` (new)
- `3;`  HEAT DATA              → `flui-input-heat.svg` (new)
- `5;`  STEADY STOKES          → `solve-stokes.svg` (new — laminar lines)
- `6;`  UNSTEADY STOKES        → `solve-stokes.svg` (REUSE — same flow type)
- `7;`  IMPLICIT NAVIER-STOKES → `solve-navier-stokes.svg` (new — turbulent)
- `8;`  FRACTIONAL NAVIER-STOKES → `solve-navier-stokes.svg` (REUSE)
- `9;`  BOUSSINESQ             → `solve-navier-stokes.svg` (REUSE) or new icon
- `10;` DRAW VEL/PRESS/TEMP    → `draw-velocity.svg` (new — vector arrows)
- `19;` DRAW MESH              → `mesh-draw.svg` (REUSE from 6.1 mail/)
- `20;` PRECISION              → `SP_DialogApplyButton` or `—`
- `60;` MANAGE WINDOW          → `SP_ComputerIcon` or `—`
- `70;` MS TMS TOOLS           → `SP_DirIcon` (Qt built-in, not a custom svg)
- `97;` VERSION NAME           → `SP_MessageBoxInformation` or `—`
- `99;` SAVE & QUIT             → `SP_DialogCloseButton` (shared with 6.1/6.2)

For `icon_source` on non-qaction rows: `—`.

For `icon_source` on qaction rows that are flui-specific but too niche
for a custom icon: Qt SP_* enum (as above) or `—` (menu-only text).

Every SVG filename listed MUST be one Plan 02 will actually ship, OR a
filename already shipped by 6.1/6.2 (`mesh-draw.svg` is the canonical
reuse). Custom SVGs MUST live under `xvue/qt/resources/icons/flui/` —
NOT the elas/ or mail/ trees. Plan 02 will append qrc entries
accordingly.

### Step 6 — Expand the 20 flui-specific sub-menus

For each of cl_flui, methrecr, nbvitpre, noxyzmod, particle, tracmovi,
tracoura, tracpr2d, tracpr3d, tractem2, tractem3, traerrpr, traerrvi,
trarot2d, trarot3d, trflevit, vitepr2d, vitepr3d, vitetype, vp0flui:

1. Read the corresponding file under `td/m/<name>` (FR — authoritative
   on this dev tree per `td/m/anglais` flag check) and `td/ma/<name>`
   (EN source) if present. Some flui-unique sub-menus may have no EN
   counterpart — note in `notes` cell and use FR for both columns
   (the bilingual parser fix from 6.2 Plan 05 falls through to td/m/
   when td/ma/ is missing).
2. Extract every `<NUM> : '<label>'` pair.
3. Determine the parent prefix from the call site (grep the sub-menu
   name in flui/*.f, note the LIMTCL context and the surrounding GOTO
   label or IF branch). For shared sub-menus reached from multiple
   parents (e.g., cl_flui from both `2;` and `3;` via DFFLUI/DFTHER):
   pick the canonical parent (`2;` for cl_flui — DFFLUI is the primary
   physical-data caller per ppflui.f:323), document the `3;` reach in
   the `notes` cell. Mirrors 6.2 audit's elas contdepl/tracdepl
   convention.
4. Emit one row per leaf with the parent-prefixed lexicon_path.

For sub-menus reached from sequential prompts under the same parent
(e.g., methrecr / nbvitpre / vp0flui under STOKESTA/FLUIDENS workflows):
use synthetic high-number-offset prefixes per the 6.2 D-04 pattern
(e.g., `5;81;` for methrecr if it sits under STOKESTA after a few
prompts). Document hierarchy in the `notes` cell.

### Step 7 — Compress the 21 shared util sub-menus

For each of affiche, couleur0, couleurs, entrsort, fichier, lecteur,
managfen, managmef, methvvpr, mode_es, opt_lign, opt_surf, sectvopl,
suivi_ms, suivitms, tracaxes, tuer, typ_objt, typtrait, vuesplan, zeros:

Emit ONE row with:
- `lexicon_path` = a valid representative parent-entry numeric path
  (e.g. `60;1;` for managfen reached from code 60; `70;1;` for managmef
  reached from code 70; `10;37;1;` for couleurs deep in TFLUIDE — pick
  whatever is reachable in flui).
- `description_fr/en` = the sub-menu title from td/m/<name> / td/ma/<name>
  followed by `(shared util)`
- `frequency = low`
- `qaction = no`
- `toolbar = no`
- `icon_source = —`
- `shortcut = —`
- `notes = "shared util sub-menu — <N> leaves; see 6.1 LEXICON-AUDIT-mail.md / 6.2 LEXICON-AUDIT-elas.md for full enumeration; reached from flui/<FILE>.f:<LINE>"`

Mirrors the 6.2 Plan 01 auto-fix #2 decision (compressed-row lexicon_path
uses a valid representative parent-entry path, NOT a synthetic marker —
the validator's LEX_RX regex rejects non-numeric paths).

### Step 8 — Draft top-5 toolbar set

Propose the 5 rows with `toolbar=yes` from the 15 debuflui top-level
codes, per the interfaces section default:

1. `2;`  GIVE PHYSICAL DATA
2. `5;`  INCOMPRESSIBLE STEADY STOKES SOLVER
3. `7;`  IMPLICIT UNSTEADY NAVIER-STOKES SOLVER
4. `10;` DRAWING of VELOCITIES PRESSURES TEMPERATURES
5. `99;` SAVE DATA and QUIT

All others `toolbar=no`. The exact top-5 is user-ratifiable at Task 2
checkpoint — common alternatives:
- Swap `5;` for `1;` if Object-name-first is preferred
- Swap `7;` for `8;` if FRACTIONAL is the production solver instead
  of IMPLICIT
- Swap `10;` for `19;` if mesh inspection is more frequent than
  result visualization

Any change must keep the count at exactly 5.

### Step 9 — Status DRAFT (Task 2 flips to FROZEN)

Leave the top-of-file status line as:

```markdown
**Status:** DRAFT (pending user review — Task 2 checkpoint)
```

Task 2's human-verify checkpoint approves or requests edits; on approval
the status flips to `**Status:** FROZEN`.

### Step 10 — Append Summary Statistics block

Mirror 6.2 Plan 01 output: at the end of the file, a bullet list with:

- Total rows
- Rows by frequency bucket (high/med/low counts)
- Rows by qaction (yes/no counts) — yes count should equal high+med
- Top-5 toolbar set (5 bullets with lexicon + label)
- Flui-specific SVG icon filenames needed (for Plan 02 to ship)
- Shared SVG reuses (e.g. `mesh-draw.svg` from 6.1 mail/)
- Menu-taxonomy mapping ratified ({File, Fluid, View, Help} content
  distribution)

Use bullet lists (NOT pipe tables) for the Summary block — pipe tables
collide with the validator's first-pipe-table parser (6.1 Plan 01
auto-fix #3).

### Step 11 — Run the validator (WARN mode for icons)

```bash
cd /home/mefisto/git/mefisto
python3 tools/validate_audit_md.py \
    .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md
```

Expected:
- Exit code 0
- Output includes `schema valid` AND `5 toolbar=yes`
- WARN lines for each new flui SVG filename Plan 02 will ship but hasn't
  yet (validator is module-aware as of 6.2 Plan 02; the resolver will
  look in `icons/flui/` first, fall back scan to `icons/mail/icons/elas/`,
  then return the missing path for the WARN message). In this plan
  these are acceptable WARNs.
- `mesh-draw.svg` resolves into `icons/mail/` cleanly (no WARN — already
  shipped by 6.1).

If the validator exits non-zero, FIX the audit markdown (hard contract —
9 rules are non-negotiable) before committing.

### Specificity notes

- Use the EXACT column header row from 6.1/6.2 — the validator does not
  allow header variations.
- DO NOT escape pipes `|` in description strings as `\|` (6.2 Plan 01
  auto-fix #1: validator's primitive split parser doesn't honour
  backslash escaping). Replace ` | ` with ` & ` (ampersand for "and/or").
- `description_fr` column pulls from `td/m/<menu>` (or `td/mf/<menu>` if
  td/m/ is the EN install — verify with `head -1 td/m/debuflui`; on
  this worktree td/m/ holds the FR variant for debuflui based on the
  bytecount probe).
- `description_en` column pulls from `td/ma/<menu>`.
- Embedded single-quotes in the LIMTCL files (e.g. `'d''un'`) decode to
  literal `d'un` — un-double them when writing cells.
- DO NOT auto-fix flui-side typos in `td/m*/` files (if any surface);
  follow 6.1 Pitfall 5 log-and-fallback (record as a `notes` cell on
  the affected row).
- DO NOT add audit rows for the commented-out `ccc` PISO blocks in
  ppflui.f (lines 379-394) — they are dead code.
  </action>
  <verify>
    <automated>test -f /home/mefisto/git/mefisto/.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md && awk -F'|' '/^\|/ {print NF}' /home/mefisto/git/mefisto/.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md | sort -u && python3 /home/mefisto/git/mefisto/tools/validate_audit_md.py /home/mefisto/git/mefisto/.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md` exists.
    - First markdown table has exactly 11 pipe-separated tokens per row (9 data columns + 2 outer pipes). `awk -F'|' '/^\|/ {print NF}' LEXICON-AUDIT-flui.md | sort -u` emits a single value `11`.
    - `grep -cE '^\| *[0-9]+(;[0-9]+)*; *\|' .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md` returns a number in [80, 250].
    - The 15 debuflui top-level codes are all present as individual rows. Verify with `for c in 1 2 3 5 6 7 8 9 10 19 20 60 70 97 99; do grep -cE "^\| ${c}; \|" LEXICON-AUDIT-flui.md; done` — each iteration returns ≥ 1.
    - Code `4;` does NOT appear as a top-level row (it is absent from debuflui and the ppflui.f GOTO table).
    - `python3 tools/validate_audit_md.py .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md` exits 0.
    - Validator output contains `schema valid` AND `5 toolbar=yes`.
    - Rule 9 WARN lines for new flui SVGs are acceptable (Plan 02 ships the files); mesh-draw.svg row should NOT WARN (resolves to icons/mail/).
    - Top of file carries `**Status:** DRAFT`.
    - At least 5 HIGH rows exist: `awk -F'|' '/^\|/ && $5 ~ /high/' LEXICON-AUDIT-flui.md | wc -l` ≥ 5.
    - File opens cleanly in any markdown renderer (no malformed rows).
    - No reference to `debut` (mail's top-level) AND no reference to `debuelas` (elas's top-level) in audit data — only `debuflui`. (Cross-references in `notes` cells to 6.1/6.2 audits are allowed and expected.)
  </acceptance_criteria>
  <done>
    LEXICON-AUDIT-flui.md exists with 9-column schema identical to 6.1/6.2's,
    all 15 debuflui top-level codes present (code 4; absent), 20 flui-unique
    sub-menus fully expanded, 21 shared util sub-menus compressed to 1 row
    each, frequency bucketing applied from testa/*/*.flui + domain review,
    5 rows marked toolbar=yes, validator exits 0, Status=DRAFT pending
    user review in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review of frequency + top-5 toolbar + {File, Fluid, View, Help} taxonomy — LOCK audit</name>
  <files>.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md</files>
  <read_first>
    - .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md (the Task 1 deliverable)
    - .planning/phases/06.2-elasticity-elas-menu-wiring/06.2-01-SUMMARY.md (auto-mode auto-approves with notes — same convention)
    - .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md §Per-Module Conformance Contract (flui <Module> menu title is Fluid/Fluide)
    - .planning/ROADMAP.md §"Phase 6.3" (canonical menu taxonomy {File, Fluid, View, Help})
  </read_first>
  <what-built>
    Task 1 produced `.planning/phases/06.3-*/LEXICON-AUDIT-flui.md` with the
    full recursive LIMTCL tree walk (~95-160 rows expected), 9-column schema,
    and `frequency` buckets scored from `testa/*/*.flui` grep evidence
    supplemented by domain review of the 15 debuflui top-level codes.
    Validator exits 0.

    Three ratification items (auto-mode auto-approves with notes):

    1. **Frequency bucketing** — HIGH rows should include the canonical
       Navier-Stokes workflow leaves (`2;`, `5;` or `7;`, `10;`, `99;`)
       plus 2-3 others based on .flui evidence.
    2. **Top-5 toolbar** — proposed set (ratify or swap):
        - `2;`  GIVE PHYSICAL DATA
        - `5;`  INCOMPRESSIBLE STEADY STOKES SOLVER
        - `7;`  IMPLICIT UNSTEADY NAVIER-STOKES SOLVER
        - `10;` DRAWING of VELOCITIES PRESSURES TEMPERATURES
        - `99;` SAVE DATA and QUIT
    3. **Menu taxonomy** — `{File, Fluid, View, Help}` per ROADMAP + UI-SPEC.
       Menu-content mapping:
       - File: 70; 99; + shared 6.0 File actions
       - Fluid: 1; 2; 3; 5; 6; 7; 8; 9; + Parameters submenu (20; 60;)
       - View:  10; 19; + shared 6.0 View actions
       - Help:  97; + shared 6.0 Help/About
  </what-built>
  <how-to-verify>
    1. Open the audit markdown:
       `code .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md`
       or via github preview.

    2. Scan the `frequency` column against the Task 1 Step 2 rubric.
       Explicitly check:
       - `2;` (Physical data), `5;` (Steady Stokes), `7;` or `8;` (NS),
         `10;` (Draw vel/press), `99;` (save) marked HIGH.
       - `1;` (Object name), `19;` (Draw mesh) marked HIGH or MED.
       - Niche codes (`3;` heat data is HIGH only when running code 9;
         Boussinesq, otherwise MED; `60;`, `70;`, `97;` LOW unless
         workflow uses them frequently).
       - Expanded cl_flui / vitepr2d / tracpr2d leaves mostly LOW (typed
         once when configuring, not in steady-state use).

    3. Verify the 5 `toolbar=yes` rows match the proposal — or propose
       swaps. Acceptable adjustments:
       - Swap `5;` for `1;` if "Object first" workflow is more common
       - Swap `7;` for `8;` if FRACTIONAL is the production NS solver
       - Swap `10;` for `19;` if mesh inspection dominates
       - Any change must keep the count at exactly 5.
       `grep -c '| yes | yes |' LEXICON-AUDIT-flui.md` should return 5.

    4. Verify rows with `qaction=yes` cover the Fluid menu + File/View
       flui extensions (target ~15-30 rows). Count with:
       `grep -cE '\| yes \|' .planning/phases/06.3-*/LEXICON-AUDIT-flui.md`
       Expect ~15-30 yeses; wildly high (>40) signals over-promotion.

    5. Ratify the **menu taxonomy**:
       - Confirm `{File, Fluid, View, Help}` is the flui top-level bar
         (differs from 6.1's `{File, Mesh, View, Help}` and 6.2's
         `{File, Solve, View, Help}`).
       - Confirm the Fluid menu maps to codes 1;–9; (object/data/solvers)
         plus a `Parameters` submenu (20;, 60;).
       - Confirm code 70; goes into File (TMS file management).
       - Confirm codes 10; 19; go into View (visualization).

    6. Ratify the **flui SVG icon set** (5-7 new icons in Plan 02):
       Default proposal: `flui-object.svg`, `flui-input-physics.svg`,
       `flui-input-heat.svg`, `solve-stokes.svg`, `solve-navier-stokes.svg`,
       `draw-velocity.svg`. Plus the shared `mesh-draw.svg` reuse from
       6.1 mail/ directory for code `19;`. This list is the Plan 02
       `xvue_icons.qrc` append set.

       User may consolidate — e.g., a single `solve-flow.svg` covering
       both Stokes and Navier-Stokes, dropping the count to 5 new SVGs.

    7. If changes are needed, edit the markdown directly then re-run:
       `python3 tools/validate_audit_md.py .planning/phases/06.3-*/LEXICON-AUDIT-flui.md`
       Must exit 0.

    8. Once satisfied, flip `**Status:** DRAFT` to `**Status:** FROZEN`
       at the top of the file. Commit triggers proceed after that.
  </how-to-verify>
  <action>
    This is a checkpoint task (type=checkpoint:human-verify): no automated
    action. The user reviews the LEXICON-AUDIT-flui.md draft using the
    steps in <how-to-verify> above, applies any ratification swaps, and
    either approves by flipping `**Status:** DRAFT` to `**Status:** FROZEN`
    or requests edits. In `--auto` mode the checkpoint auto-approves
    with the DRAFT-proposed values retained verbatim; user can open the
    audit after the phase completes and request polish-pass edits as
    a follow-up.
  </action>
  <verify>
    <automated>grep -q '^\*\*Status:\*\* FROZEN' /home/mefisto/git/mefisto/.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md && python3 /home/mefisto/git/mefisto/tools/validate_audit_md.py /home/mefisto/git/mefisto/.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^\*\*Status:\*\* FROZEN' .planning/phases/06.3-*/LEXICON-AUDIT-flui.md` succeeds (status is FROZEN).
    - `python3 tools/validate_audit_md.py .planning/phases/06.3-*/LEXICON-AUDIT-flui.md` exits 0 after any user-requested edits.
    - Exactly 5 rows have `toolbar=yes` — validator output confirms `5 toolbar=yes`.
    - Menu-taxonomy mapping ({File, Fluid, View, Help}) is ratified in the audit's Scope section (a paragraph-level note near top-of-file).
    - User-requested edits (if any) are applied and re-validated.
  </acceptance_criteria>
  <done>
    User has reviewed the audit; `**Status:** FROZEN` is set; the validator
    still exits 0 after any user-requested edits; the top-5 toolbar set,
    frequency bucketing, AND menu taxonomy are locked as the contract
    Plan 02 will consume.
  </done>
  <resume-signal>
    Type "frozen" if you are happy with the bucketing, top-5 toolbar,
    and the {File, Fluid, View, Help} taxonomy — Plan 02 will consume
    the audit as-is.
    Type "edit" if you want to propose changes — describe them inline
    (e.g. "swap toolbar `5;` for `1;`; demote `60;` to LOW"), then I
    will make the edits, re-run the validator, and return here.
    Type "abort" if the audit fundamentally misses the mark — triggers
    a re-run of Task 1 with adjusted scope.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                  | Description                                             |
|---------------------------|---------------------------------------------------------|
| user-edit → audit.md      | User reviews and possibly edits rows during Task 2      |
| filesystem → validator    | Python reads markdown file; script is read-only         |

Note: Plan 01 adds NO runtime attack surface. It produces a doc deliverable
only; no binary or build-script changes.

## STRIDE Threat Register

| Threat ID      | Category         | Component                       | Disposition | Mitigation Plan |
|----------------|------------------|---------------------------------|-------------|-----------------|
| T-6.3-AUDIT-01 | Tampering        | LEXICON-AUDIT-flui.md schema    | mitigate    | `tools/validate_audit_md.py` enforces the 9 hard rules; exit 1 on any violation. Rule 9 cross-checks SVG filenames via the module-aware `resolve_icon_path` from 6.2 Plan 02 — works for any LEXICON-AUDIT-<mod>.md. |
| T-6.3-AUDIT-02 | Info Disclosure  | audit rows contain FR/EN text   | accept      | Content sourced from `$MEFISTO/td/m*/` which is open-source user data; no secrets. Same disposition as 6.1 T-6.1-AUDIT-02 / 6.2 T-6.2-AUDIT-02. |
</threat_model>

<verification>
Phase-wide checks for Plan 01:
1. `test -f .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md` → exit 0
2. `python3 tools/validate_audit_md.py .planning/phases/06.3-*/LEXICON-AUDIT-flui.md` → exit 0
3. Audit `**Status:**` line reads `FROZEN` after Task 2 sign-off (not `DRAFT`)
4. No changes to any Fortran, C++, CMake, or build-script files (Plan 01 is docs-only)
5. No regression: `bin/cbl_tout_qt` still builds (spot-run before wrap-up); no binary artifacts touched
6. No new files under `xvue/qt/resources/icons/flui/` yet — that is Plan 02's scope
</verification>

<success_criteria>
- UX-05 flui-slice Success Criterion #1 is satisfied: `LEXICON-AUDIT-flui.md`
  exists and enumerates every interactive command reachable from
  `td/m/debuflui` with FR+EN descriptions, frequency column, and QAction
  flag per the 6.1-frozen schema.
- `tools/validate_audit_md.py` exit 0 is the automated gate.
- User has explicitly approved (or auto-approved with notes) the frequency
  bucketing, top-5 toolbar set, AND the `{File, Fluid, View, Help}` menu
  taxonomy — the audit reads `**Status:** FROZEN`.
- Plan 02 can consume the audit without further re-interpretation: the
  rows with `qaction=yes` ARE the QAction set; the rows with `toolbar=yes`
  ARE the top-5 toolbar; the SVG filenames in `icon_source` ARE the
  Plan 02 `xvue_icons.qrc` flui-append set.
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-fluid-flui-menu-wiring/06.3-01-SUMMARY.md`
with:
- Audit row count (total, and split by frequency bucket)
- Final top-5 toolbar set (post-review)
- Flui-unique SVG filenames queued for Plan 02's qrc append set
- Shared SVG reuses from 6.1 (e.g. `mesh-draw.svg`)
- Menu-taxonomy mapping ratified: File / Fluid / View / Help content distribution
- Any user edits applied between DRAFT and FROZEN
- Confirmation that `validate_audit_md.py` exits 0
- Hand-off note to Plan 02: "Consume .planning/phases/06.3-*/LEXICON-AUDIT-flui.md
  rows where qaction=yes as the registerFluiActions_stub_ QAction set;
  rows where toolbar=yes as the toolbar append set; rows where icon_source
  ends in .svg (and the filename is not already in xvue/qt/resources/icons/mail/
  or xvue/qt/resources/icons/elas/) as the xvue_icons.qrc append set for
  icons/flui/."
</output>
</content>
</invoke>