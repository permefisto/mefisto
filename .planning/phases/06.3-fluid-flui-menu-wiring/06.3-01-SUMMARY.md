---
phase: 06.3-fluid-flui-menu-wiring
plan: 01
subsystem: docs
tags: [lexicon-audit, flui, fortran, limtcl, menu-taxonomy, qt-migration]

# Dependency graph
requires:
  - phase: 06.1-mesher-mail-menu-wiring
    provides: "9-column LEXICON-AUDIT schema + tools/validate_audit_md.py validator (reused verbatim) + de-duplication rule (module-specific expanded / shared util compressed to 1 row); shared mesh-draw.svg in xvue/qt/resources/icons/mail/"
  - phase: 06.2-elasticity-elas-menu-wiring
    provides: "Module-aware ICONS resolver in tools/validate_audit_md.py (resolve_icon_path picks icons/<mod>/ based on filename stem); reaffirmed compressed-row representative-numeric-path convention"
  - phase: 06.0-shared-shell-menu-bridge-dialogs
    provides: "Per-Module Conformance Contract — flui module-title Fluid/Fluide; {File, <Module>, View, Help} bar layout"
provides:
  - "LEXICON-AUDIT-flui.md — 161-row recursive catalogue of every LIMTCL leaf reachable from td/m/debuflui"
  - "Ratified top-5 toolbar set: {2;, 5;, 7;, 10;, 99;}"
  - "Ratified menu taxonomy: {File, Fluid, View, Help} with content distribution"
  - "Queue of 6 flui-specific .svg filenames for Plan 02 xvue_icons.qrc append set + 1 reuse (mesh-draw.svg from 6.1 mail/)"
  - "49-row qaction=yes set for Plan 02 registerFluiActions_stub_"
affects: [06.3-02-flui-qactions-wiring, 06.3-03-flui-qt-a-b-smoke]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Module-audit de-duplication: flui-unique sub-menus fully expanded (cl_flui, methrecr, nbvitpre, noxyzmod, particle, tracmovi, tracoura, tracpr2d/3d, tractem2/3, traerrpr/vi, trarot2d/3d, trflevit, vitepr2d/3d, vitetype, vp0flui); shared util sub-menus compressed to 1 row each with notes cell citing 6.1/6.2 audits for leaf-level detail"
    - "Representative numeric lexicon_path for compressed shared-util rows (e.g. 70;1; for mode_es; 10;80;5;1; for couleur0 reached deep in TFLUIDE)"
    - "Domain-review frequency promotion for sparse testa corpora (5;, 7;, 99; domain-promoted HIGH; 1;, 19; promoted MED)"
    - "Synthetic high-number-offset prefixes for sequential-prompt sub-menus under TFLUIDE / STOKESTA / FLUIDENS — preserves validator regex while documenting hierarchy in notes cell"

key-files:
  created:
    - ".planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md"
  modified: []

key-decisions:
  - "Menu taxonomy {File, Fluid, View, Help} (Fluid replaces Solve relative to 6.2 elas; Mesh relative to 6.1 mail)"
  - "Top-5 toolbar = {2;, 5;, 7;, 10;, 99;} — PHYSICAL-DATA / STEADY-STOKES / IMPLICIT-NS / DRAW-VEL+PRESS / SAVE-QUIT"
  - "5; (Steady Stokes), 7; (Implicit NS), 99; (Save) domain-promoted HIGH; 1; (Object) and 19; (Draw mesh) marked MED; 35;/36; (Taylor-Green hidden codes in ppflui.f GOTO but absent from debuflui) recorded as low/qaction=no with notes for future promotion"
  - "Code 4; absent from BOTH debuflui menu file AND ppflui.f dispatch (slot value `30` — input-loop reset no-op); zero rows for code 4; in audit"
  - "20 flui-unique sub-menus fully expanded (139 leaf rows); 22 shared util sub-menus compressed to 1 representative row each (some reached from multiple parent paths inside TFLUIDE — e.g. couleur0 at 10;80;5;1; AND couleurs at 10;80;7;1;)"
  - "mesh-draw.svg reused from 6.1 mail/ directory (not copied to icons/flui/) for debuflui code 19; — qrc prefix resolves both paths"
  - "Stokes (codes 5;,6;) share solve-stokes.svg; Navier-Stokes (codes 7;,8;,9;) share solve-navier-stokes.svg — consolidation drops new-icon count to 6 (not 7+ as 6.2 elas needed)"

patterns-established:
  - "Flui audit mirrors 6.1/6.2 templates verbatim (frontmatter section, Legend, 9-column table header, Summary Statistics block, Top-5 Toolbar section, Cross-References)"
  - "Same validator rules 1-9 apply unchanged; Rule 9 runs in WARN mode for flui pre-Plan-02 (6 new flui SVGs not yet shipped); mesh-draw.svg row resolves cleanly (no WARN — already shipped by 6.1)"
  - "TFLUIDE (visualization driver, code 10;) is the deepest reach in the audit — prompts ~20 distinct sub-menus including all flui-unique drawing variants (vitepr2d/3d, tracpr2d/3d, tractem2/3, traerrpr/vi, trarot2d/3d, trflevit, tracmovi, tracoura, vitetype, noxyzmod, particle) and reaches couleur0/couleurs/typtrait via trflevit"

requirements-completed: [UX-05]

# Metrics
duration: ~12min
completed: 2026-04-27
---

# Phase 6.3 Plan 01: Fluid Lexicon Audit Summary

**Recursive LIMTCL tree walk of 42 flui-reachable sub-menus produced a 161-row LEXICON-AUDIT-flui.md ratified as FROZEN — 10 HIGH + 39 MED rows give Plan 02 its 49-row QAction set and {2;, 5;, 7;, 10;, 99;} toolbar slice under a {File, Fluid, View, Help} menu taxonomy.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-27 (Task 1 commit `10053aa`)
- **Completed:** 2026-04-27 (Task 2 commit `ca0a241`)
- **Tasks:** 2 (Task 1 = audit production; Task 2 = user-ratified freeze)
- **Files created:** 1 (LEXICON-AUDIT-flui.md — 161 rows, 366 lines)
- **Files modified:** 0 (docs-only plan; no Fortran / C / build-script changes)

## Accomplishments

- **LEXICON-AUDIT-flui.md produced** with 9-column schema identical to 6.1's/6.2's, 161 rows within the validator's [80, 250] bound
- **15 debuflui top-level codes** present as individual rows (1;, 2;, 3;, 5;, 6;, 7;, 8;, 9;, 10;, 19;, 20;, 60;, 70;, 97;, 99;); code 4; absent (no GOTO slot)
- **20 flui-unique sub-menus fully expanded** (cl_flui, methrecr, nbvitpre, noxyzmod, particle, tracmovi, tracoura, tracpr2d, tracpr3d, tractem2, tractem3, traerrpr, traerrvi, trarot2d, trarot3d, trflevit, vitepr2d, vitepr3d, vitetype, vp0flui) = 139 leaf rows
- **22 shared util sub-menu compressed-rows** (affiche, couleur0, couleurs, entrsort, fichier, lecteur, managfen, managmef, methvvpr, mode_es, opt_lign, opt_surf, sectvopl, suivi_ms, suivitms, tracaxes, tuer, typ_objt, typtrait, vuesplan, zeros, plus a second couleurs reach at 10;80;7;1;) — each cites 6.1/6.2 audits in notes cell
- **Frequency bucketing**: 10 HIGH / 39 MED / 112 LOW from testa/*/*.flui counts (cavity2d/cavity3d/marche/nstg3d projects) supplemented by domain review for the canonical NS workflow
- **Exactly 5 toolbar=yes rows** — 2;, 5;, 7;, 10;, 99;
- **Menu taxonomy ratified** — {File, Fluid, View, Help} with content distribution:
  - File = {70;, 99;} + shared 6.0 File
  - Fluid = {1;, 2;, 3;, 5;, 6;, 7;, 8;, 9;} + Parameters submenu {20;, 60;}
  - View = {10;, 19;} + shared 6.0 View
  - Help = {97;} + shared 6.0 Help
- **6 new flui-specific SVG icon filenames** queued for Plan 02 qrc set + 1 reuse (mesh-draw.svg from 6.1 mail/)
- **Validator exits 0** — `python3 tools/validate_audit_md.py LEXICON-AUDIT-flui.md` prints `OK -- 161 rows, 5 toolbar=yes, schema valid` with 9 expected Rule-9 WARN lines for the 6 new flui SVGs (some reached from multiple rows — Stokes shared on 5;+6;, Navier-Stokes on 7;+8;+9;)

## Task Commits

Each task committed atomically:

1. **Task 1: Full recursive LIMTCL tree walk + draft flui audit markdown** — `10053aa` (docs)
2. **Task 2: User review — frozen (DRAFT → FROZEN, ratified by user 2026-04-27)** — `ca0a241` (docs)

_Note: Plan 01 is docs-only; no per-task TDD cycle. Two commits for two tasks._

## Files Created/Modified

- **`.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md`** (NEW, 366 lines, 161 rows) — the frozen recursive audit of every interactive flui command reachable from `td/m/debuflui`. Mirrors 6.1/6.2 structure verbatim: frontmatter block, Scope section with drift note and menu-taxonomy mapping, Known data quirks (code 4; absence, code 35;/36; hidden Taylor-Green codes, ppflui.f commented PISO blocks), Legend, 9-column Audit Table, Summary Statistics, Top-5 Toolbar, Flui-unique SVG icon set, Shared SVG reuses, Cross-References to 6.1/6.2 audits.

## Decisions Made

- **Menu taxonomy is `{File, Fluid, View, Help}`** — "Fluid" replaces "Solve" (6.2) and "Mesh" (6.1) per ROADMAP Phase 6.3 line 201 and 06.0-UI-SPEC §Per-Module Conformance Contract (flui `<Module>` title = Fluid/Fluide). Ratified in audit Scope section.
- **Top-5 toolbar = {2;, 5;, 7;, 10;, 99;}** covering: input physical data, steady Stokes solve, implicit NS solve, result visualization, session save-quit. Rationale: `2;` is the mandatory setup step; `5;` is the canonical first solver run (Stokes baseline); `7;` is the production NS solver; `10;` is the primary result visual (TFLUIDE driver); `99;` is the shared 6.1/6.2 close-session convention.
- **Domain-review promotion** — `5;` (Stokes), `7;` (Implicit NS), `99;` (Save) domain-promoted HIGH despite small testa counts (3, 3, indirect) because they are canonical NS workflow checkpoints. `10;` (Draw vel/press/temp) was already HIGH from raw testa count=7.
- **Hidden codes 35; and 36;** (NS2DTGV / NS3DTGV — Taylor-Green vortex) live in `prpr/ppflui.f` GOTO table (lines 235-236) but are NOT in `td/m/debuflui`. Kept as audit rows with `qaction=no, frequency=low` (typed-only access via `35;` / `36;`) per planner's "hidden lexicons" rule. User did not promote at Task 2 freeze.
- **Code 4; absence verified** — not in debuflui, not in ppflui.f GOTO table (slot value is `30`, the input-loop reset). Zero rows for `4;` in audit.
- **Stokes/Navier-Stokes icon consolidation** — codes 5;,6; share `solve-stokes.svg`; codes 7;,8;,9; share `solve-navier-stokes.svg`. Drops new-icon count to 6 (vs 7 needed by 6.2 elas), keeping the icon manifest manageable.
- **TFLUIDE deepest-reach disambiguation** — visualization driver (code 10;) prompts 13 distinct sub-menus sequentially. Synthetic prefixes 10;80;... (e.g. couleur0 at 10;80;5;1; vs couleurs at 10;80;7;1; — both reached via trflevit at TFLUIDE depth) were used to disambiguate two reaches of color sub-menus that share the same util file but differ in their parent context. Validator regex `^\d+;(?:\d+;)*$` accepts.
- **methvvpr classified as shared util** — flui never reaches eigenmodes solver (`prpr/ppflui.f` does not call eigenmodes). methvvpr stays in compressed shared-util list, NOT flui-unique expanded list.

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 3 - Blocking] Worktree path correction (`/home/drico/...` → `/home/mefisto/...`)**

- **Found during:** Resume of Task 2 (after user signaled `frozen`)
- **Issue:** Resume instructions referenced `/home/drico/git/mefisto/.claude/get-shit-done/bin/gsd-tools.cjs` paths from a stale worktree. Actual cwd in this session is `/home/mefisto/git/mefisto` (per env block).
- **Fix:** All commands re-rooted at `/home/mefisto/git/mefisto`. Validator path used was `tools/validate_audit_md.py` (relative; runs from cwd which is correct).
- **Files modified:** None (path-correction only).
- **Verification:** `pwd` confirms `/home/mefisto/git/mefisto`; validator exits 0; commit `ca0a241` lands on `main`.
- **Committed in:** N/A (resume-step adjustment, no commit required)

---

**Total deviations:** 1 auto-fixed (Rule 3 - blocking infrastructure)
**Impact on plan:** Trivial — resume-prompt path skew handled inline. No scope creep, no schema changes, no row-count drift. The 161-row DRAFT was approved by user verbatim with zero edits.

## Issues Encountered

None — Task 1 produced a clean DRAFT that the user approved unchanged at Task 2. No row-by-row edits, no swap of toolbar codes, no menu-taxonomy adjustments. Frequency bucketing (10 HIGH / 39 MED / 112 LOW) ratified as-drafted.

## CLAUDE.md compliance

- **Compilation must never break:** Zero changes to Fortran / C / build scripts. Plan 01 modifies only `.planning/phases/.../LEXICON-AUDIT-flui.md`. No build re-run required.
- **Tests:** Zero impact. `testa/*/*.flui` files are read-only inputs to the frequency pipeline; no tests modified.
- **Asking before acting:** N/A — docs-only plan, no system packages or C includes touched.
- **Git discipline:** Two atomic commits (one per task), descriptive messages explaining what+why. No `--no-verify` used in this resume; commit hooks ran cleanly.
- **Programming norms:** N/A for this plan (no Fortran / C source changes).

## User Setup Required

None — no external service configuration required. Plan 01 is docs-only and does not ship runtime or build artefacts.

## Next Phase Readiness

**Plan 02 (registerFluiActions_stub_ + QAction wiring) is unblocked:**

- Consume `LEXICON-AUDIT-flui.md` rows where `qaction=yes` (49 rows) as the `registerFluiActions_stub_` QAction set
- Consume rows where `toolbar=yes` (5 rows: 2;, 5;, 7;, 10;, 99;) as the toolbar append set
- Consume `icon_source` cells ending in `.svg` whose filename is NOT already under `xvue/qt/resources/icons/mail/` or `xvue/qt/resources/icons/elas/` as the `xvue_icons.qrc` append set for `xvue/qt/resources/icons/flui/`:
  - `flui-object.svg`, `flui-input-physics.svg`, `flui-input-heat.svg`, `solve-stokes.svg`, `solve-navier-stokes.svg`, `draw-velocity.svg`
- `mesh-draw.svg` REUSED from `xvue/qt/resources/icons/mail/` (shared qrc prefix resolves both paths — no file copy)
- Plan 02 may flip Rule 9 to strict mode (`AUDIT_STRICT_ICONS=1`) once the 6 new flui SVGs land, retiring the WARN lines

**Hand-off note for Plan 02:** Consume `.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md` rows where `qaction=yes` as the `registerFluiActions_stub_` QAction set; rows where `toolbar=yes` as the toolbar append set (count == 5); rows where `icon_source` ends in `.svg` AND the filename is not already in `xvue/qt/resources/icons/mail/` or `xvue/qt/resources/icons/elas/` as the `xvue_icons.qrc` append set for `icons/flui/`.

**No blockers.** FROZEN audit is the contract Plan 02 will consume as-is.

## Self-Check: PASSED

- `.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md`: FOUND (366 lines, 161 table rows)
- Commit `10053aa` (Task 1): FOUND in `git log`
- Commit `ca0a241` (Task 2): FOUND in `git log`
- Status line reads `FROZEN`: VERIFIED via `grep -E '^\*\*Status:'` → `**Status:** FROZEN (ratified by user 2026-04-27)`
- Validator exits 0: VERIFIED via `python3 tools/validate_audit_md.py LEXICON-AUDIT-flui.md` → `OK -- 161 rows, 5 toolbar=yes, schema valid`
- All 15 debuflui top-level codes present: VERIFIED (one row each for 1; 2; 3; 5; 6; 7; 8; 9; 10; 19; 20; 60; 70; 97; 99;)
- Code `4;` absent: VERIFIED (`grep -cE "^\| 4; \|"` → 0)
- Toolbar count == 5: VERIFIED (`grep -cE '\| yes \| yes \|'` → 5; toolbar=yes rows are 2; 5; 7; 10; 99;)

---
*Phase: 06.3-fluid-flui-menu-wiring*
*Completed: 2026-04-27*
