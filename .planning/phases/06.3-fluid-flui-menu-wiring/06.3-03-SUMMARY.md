---
phase: 06.3-fluid-flui-menu-wiring
plan: 03
subsystem: ui
tags: [qt6, fortran77, weak-symbol-reuse, force-link-keepalive, qtest, qaction, xvsouris, limtcl, flui, menu-order, bilingual-labels]

# Dependency graph
requires:
  - phase: 06.3-fluid-flui-menu-wiring/01
    provides: LEXICON-AUDIT-flui.md (frozen rows; 5;=Steady Stokes, 7;=Implicit NS, 10;=Draw vel/press, 70;=MS Files, 97;=Mefisto VERSION under Help)
  - phase: 06.3-fluid-flui-menu-wiring/02
    provides: xvue_qt_flui_actions.cpp strong body (registerFluiActions_stub_) + 6 SVG icons + qrc append + xvue_qt_flui_actions_keepalive() wired from XvueApp::ensure() + CMake target_sources
  - phase: 06.2-elasticity-elas-menu-wiring/03
    provides: prpr/ppelas.f CALL XVUE_MODULE_INIT('elas',4) template; xvue_qt_elas_menu_tests test pattern; xvue/xvmodi.f weak Fortran stub (reused verbatim from 6.1 Plan 03)
  - phase: 06.2-elasticity-elas-menu-wiring/04
    provides: testMenuOrder QTest slot pattern (6.3 carries it forward as automated gate)
  - phase: 06.2-elasticity-elas-menu-wiring/05
    provides: XvueMenuFileParser language-aware loadFor() + testBilingualLabelsEnglish slot pattern (6.3 carries it forward as automated gate)
provides:
  - "prpr/ppflui.f CALL XVUE_MODULE_INIT('flui', 4) inside IF(INTERA.GE.1) block after XVINIT + LOGO('Mefisto-FLUIDER')"
  - "xvue/qt/tests/test_xvue_qt_flui_menu.cpp -- 7 D-13 test slots covering 4 D-13 required + closeEvent + testMenuOrder + testBilingualLabelsEnglish (replaces the 6.2 manual A/B checkpoint)"
  - "xvue/qt/tests/CMakeLists.txt -- add_executable(xvue_qt_flui_menu_tests) target with target_compile_definitions(XVUE_QT_TESTING)"
affects: [06.4, 06.5]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single-line Fortran CALL in per-module pp*.f dispatches to strong Qt body or reuses shared X11 weak stub (xvue/xvmodi.f from 6.1 covers all five modules; 6.3 needed zero new Fortran files)"
    - "QTest round-trip pattern verbatim from 6.2 elas: QAction::trigger() + xvsouris_ drain + assert char sequence"
    - "Autonomous-via-test-codification: testMenuOrder + testBilingualLabelsEnglish replace the 6.2 manual A/B human-verify checkpoint -- entire phase machine-verifiable"
    - "Help-lexicon allowlist pattern: testHelpNoQueue accepts a known set of audited Help lexicons (here {97;}) instead of asserting empty -- adapts to per-module audit decisions while still gating unintended payloads"

key-files:
  created:
    - "xvue/qt/tests/test_xvue_qt_flui_menu.cpp"
    - ".planning/phases/06.3-fluid-flui-menu-wiring/06.3-03-SUMMARY.md"
  modified:
    - "prpr/ppflui.f"
    - "xvue/qt/tests/CMakeLists.txt"

key-decisions:
  - "Zero Fortran-side changes to xvue/xvmodi.f -- the 6.1 !GCC$ ATTRIBUTES WEAK stub already covers all five solver modules. Phase 6.3 just adds one CALL site; pattern self-amortizes from 6.2 onward (also confirmed by 6.4/6.5 plan blueprints)."
  - "CALL placement mirrors 6.1 ppmail.f and 6.2 ppelas.f: AFTER CALL XVINIT AND AFTER CALL LOGO('Mefisto-FLUIDER') AND BEFORE ENDIF. Single 3-line insertion (2 comment lines + 1 CALL) inside IF(INTERA.GE.1) so batch/pipe runs never trigger Qt registration."
  - "testHelpNoQueue: tightened from blanket 'no Help lexicon' to 'Help carries only audit-approved {97;}'. Plan 02 wired 97; under Help per LEXICON-AUDIT-flui.md row -- the original blanket assertion (copied from 6.2 elas where Help has zero lexicons) contradicted the shipped Plan-02 wiring. Auto-fix Rule 1: corrected the test contract to match what Plan 02 audited and shipped."
  - "testMenuOrder + testBilingualLabelsEnglish carried forward verbatim from 6.2 Plan 04 + Plan 05 templates. Both PASS on this worktree (anglais flag + td/ma/debuflui present), codifying the gates that 6.2 left as manual UAT items."
  - "Aggregate Qt test totals: 125/0/3 (Plan 02 baseline) -> 135/0/2 (after Plan 03). +9 flui slots = 7 test slots + initTestCase + cleanupTestCase. Slight skip-to-pass shift (3 -> 2) in an unrelated binary unrelated to this plan."

patterns-established:
  - "Pattern proves idempotent across THREE modules now (mail->elas->flui). 6.4 ther / 6.5 nlse are mechanical near-copies: substitute module name + lexicon list + EN-label assertion strings."
  - "Per-module testHelpNoQueue should consult the LEXICON-AUDIT to populate the allowlist of expected Help lexicons. flui has {97;}; 6.4 ther / 6.5 nlse may have different sets."

requirements-completed: [UX-05]

# Metrics
duration: ~25min active wall-time (including ~6min serial Qt+X11 rebuild after Task 1, ~3min Qt rebuild after Task 2 first attempt + testHelpNoQueue auto-fix Qt rebuild)
completed: 2026-04-28
---

# Phase 6.3 Plan 03: flui Fortran hook + 7 D-13 QTest slots Summary

**One-line `CALL XVUE_MODULE_INIT('flui', 4)` in `prpr/ppflui.f` plus 7 D-13 QTest slots in `xvue/qt/tests/test_xvue_qt_flui_menu.cpp` close out Phase 6.3 fully autonomously: `testMenuOrder` and `testBilingualLabelsEnglish` replace the 6.2 manual A/B checkpoint, codifying the {File, Fluid, View, Help} order and the EN-parser gate as machine-verifiable QTest slots; aggregate Qt suite is 135/0/2 (was 125/0/3, +9 flui entries with one skip-to-pass shift in an unrelated binary), ABI 58/58 preserved, all three force-links (elas/flui/mail) intact in their `pp*_qt` binaries, no edits to `xvue/xvmodi.f`, `xvue/qt/src/xvue_qt_api.cpp`, or `xvue/qt/src/xvue_qt_window.cpp`.**

## Performance

- **Duration:** ~25 min active wall-time
- **Started:** 2026-04-28T07:29:04Z (PLAN_START_TIME recorded)
- **Completed:** 2026-04-28T11:07:00Z (wall-clock includes idle gaps; active build/test/edit cycles ≈ 25 min)
- **Rebuild wall-clock:** Task 1 Qt+X11 serial ≈ 6 min; Task 2 first Qt rebuild ≈ 4 min; Task 2 auto-fix Qt rebuild ≈ 4 min
- **Tasks:** 2 (both autonomous, no checkpoints)
- **Files created:** 1 (`xvue/qt/tests/test_xvue_qt_flui_menu.cpp` -- 380 lines)
- **Files modified:** 2 (`prpr/ppflui.f` +3 lines, `xvue/qt/tests/CMakeLists.txt` +30 lines)

## Accomplishments

### Task 1 -- prpr/ppflui.f CALL insertion (commit `fab3f31`)

Three-line insertion (2 comment lines + 1 CALL) inside the `IF( INTERA .GE. 1 )` block, AFTER `CALL XVINIT` AND AFTER `CALL LOGO( 'Mefisto-FLUIDER' )` AND BEFORE `ENDIF`:

```fortran
C        Phase 6.3 D-08: register Qt-side module-specific menu actions.
C        No-op on the X11 backend (xvue/xvmodi.f weak stub from 6.1).
         CALL XVUE_MODULE_INIT( 'flui', 4 )
```

Column-7+ fixed-form discipline respected (`CALL` at column 10 to match the surrounding `CALL XTINIT/XVINIT/LOGO`; `C` in column 1 for comments). Integer `4` is the length of `'flui'`. Insertion lines are 148-150 in the new file (between original line 147 `CALL LOGO( 'Mefisto-FLUIDER' )` and original line 148 `ENDIF`).

### Task 2 -- 7 D-13 QTest slots + new CMake target (commit `5d5f3bc`)

`xvue/qt/tests/test_xvue_qt_flui_menu.cpp` -- 7 slots:

| Slot | Lexicon | Menu | Expected xvsouris drain | Notes |
|------|---------|------|-------------------------|-------|
| `testFluidSolveStatic` | `5;` | Fluid -> StokesSteady (parent shortcut) | `{'5', ';', 13}` | INCOMPRESSIBLE STEADY STOKES SOLVER |
| `testViewDrawResults`  | `10;` | View -> DrawResults (parent shortcut) | `{'1', '0', ';', 13}` | DRAWING of VELOCITIES PRESSURES TEMPERATURES |
| `testFileMSTools`      | `70;` | File | `{'7', '0', ';', 13}` | MANAGE TMS Files Units |
| `testHelpNoQueue`      | n/a (allowlist `{97;}`) | Help | queueSize unchanged | tightened to allow audited 97; |
| `testMenuOrder`        | n/a | (menu bar) | top-4 = `{File, Fluid, View, Help}` | replaces 6.2 manual A/B gap-1 |
| `testBilingualLabelsEnglish` | n/a | (parser) | `label(5)` ~ "STOKES" / "INCOMPRESSIBLE"; `label(99)` ~ "SAVE" / "QUIT" | replaces 6.2 manual A/B gap-2; PASSES on this worktree |
| `testCloseEventPushes99WhenBlocking` | `99;` | (close event) | queueSize += 4 | D-09 shared closeEvent gate inherited from 6.1 |

`xvue/qt/tests/CMakeLists.txt` -- new `add_executable(xvue_qt_flui_menu_tests)` target adjacent to `xvue_qt_elas_menu_tests`, linked against `xvueqt Qt6::Test Qt6::Widgets Qt6::Gui Qt6::Core`, with `target_compile_definitions(... PRIVATE XVUE_QT_TESTING)` enabling `clearCacheForTesting()` + `xvueClearLanguageCacheForTesting()` for the `testBilingualLabelsEnglish` bookend.

### Test results (offscreen QPA)

```
PASS   : TestXvueQtFluiMenu::initTestCase()
PASS   : TestXvueQtFluiMenu::testFluidSolveStatic()
PASS   : TestXvueQtFluiMenu::testViewDrawResults()
PASS   : TestXvueQtFluiMenu::testFileMSTools()
PASS   : TestXvueQtFluiMenu::testHelpNoQueue()
PASS   : TestXvueQtFluiMenu::testMenuOrder()
PASS   : TestXvueQtFluiMenu::testBilingualLabelsEnglish()
PASS   : TestXvueQtFluiMenu::testCloseEventPushes99WhenBlocking()
PASS   : TestXvueQtFluiMenu::cleanupTestCase()
Totals: 9 passed, 0 failed, 0 skipped, 0 blacklisted
```

`testBilingualLabelsEnglish` PASSED (not SKIPPED) because:
- `MEFISTO=/home/mefisto/git/mefisto` env set
- `$MEFISTO/td/m/anglais` flag present
- `$MEFISTO/td/ma/debuflui` readable

The slot QSKIPs cleanly on missing preconditions, so it ports to constrained CI.

### Aggregate Qt test suite (offscreen QPA)

```
xvue_qt_canvas_gestures_tests:      12 passed, 0 failed, 0 skipped
xvue_qt_dialogs_console_tests:      14 passed, 0 failed, 0 skipped
xvue_qt_elas_menu_tests:             9 passed, 0 failed, 0 skipped
xvue_qt_event_menu_predrain_tests:  10 passed, 0 failed, 0 skipped
xvue_qt_event_tests:                33 passed, 0 failed, 2 skipped
xvue_qt_flui_menu_tests:             9 passed, 0 failed, 0 skipped (NEW)
xvue_qt_i18n_menu_prefs_tests:      19 passed, 0 failed, 0 skipped
xvue_qt_mail_menu_tests:             8 passed, 0 failed, 0 skipped
xvue_qt_menu_tests:                  5 passed, 0 failed, 0 skipped
xvue_qt_window_chrome_tests:        16 passed, 0 failed, 0 skipped
====
TOTAL: 135 passed, 0 failed, 2 skipped
```

Plan 02 baseline was `125/0/3`; this plan adds 9 flui entries (`125+9=134`) plus a single skip-to-pass shift in an unrelated binary brings the aggregate to `135/0/2` instead of the predicted `132/0/3` from the planner. The `132/0/3` figure in the user prompt seems to under-count by 3 (likely missing testMenuOrder + testBilingualLabelsEnglish which were not in the elas baseline at the time of the prediction).

## Regression guarantees

| Check | Result |
|-------|--------|
| `git diff xvue/xvmodi.f` | empty (0 bytes) -- 6.1 stub serves all modules |
| `git diff xvue/qt/src/xvue_qt_api.cpp` | empty -- no new ABI entries |
| `git diff xvue/qt/src/xvue_qt_window.cpp` | empty -- D-09 closeEvent inherited unchanged |
| `verify_abi.sh` | `nm count: 58  header count: 58` -- no drift |
| `nm pp/ppflui_qt | grep -c ' T registerFluiActions_stub_'` | **1** -- force-link survives full rebuild |
| `nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'` | **1** -- 6.2 regression guard intact |
| `nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'` | **1** -- 6.1 regression guard intact |
| `nm pp/ppflui | grep -c 'xvue_module_init_'` | **1** -- X11 stub resolves cleanly |

## Decisions Made

- **CALL placement:** mirror 6.1 ppmail.f and 6.2 ppelas.f exactly. Inside `IF( INTERA .GE. 1 ) THEN ... ENDIF` block. After `CALL XVINIT`, after `CALL LOGO( 'Mefisto-FLUIDER' )`, before `ENDIF`. The string `'Mefisto-FLUIDER'` (lowercase 'm' in Mefisto, no bilingual select) differs from elas's `'MEFISTO-ELASTICER'` -- but the placement rule (post-LOGO, pre-ENDIF) is identical.
- **Zero new Fortran files:** The 6.1 `xvue/xvmodi.f` weak stub (`!GCC$ ATTRIBUTES WEAK :: XVUE_MODULE_INIT`) already covers ALL five solver modules. Plan 03 adds one CALL line; no per-module Fortran stub. Confirms the cross-backend pattern is self-amortizing from 6.2 onward.
- **testHelpNoQueue tightened (Auto-fix Rule 1):** the original blanket assertion "no Help action carries any lexicon" copied from 6.2 elas was incorrect for flui because LEXICON-AUDIT-flui.md row 97 explicitly placed `97;` (Mefisto VERSION) under Help, and Plan 02 wired it. Tightened the assertion to use an allowlist `{97;}` so the test still gates UNINTENDED Help lexicons while accepting the audit-approved one. The 6.4/6.5 ports should populate the allowlist from their own audits.
- **Allowlist pattern (`QSet<QString>`):** preferred over `if-else` chain because the audit may grow per-module Help lexicons in future plans without requiring nested branching.
- **testMenuOrder + testBilingualLabelsEnglish carried forward verbatim from 6.2:** these two slots were the explicit autonomous-flag-justifier in this plan's blueprint. Both PASS on this worktree, replacing the 6.2 manual A/B human-verify checkpoint with machine-verifiable QTest gates. Phase 6.3 ships fully autonomous as a result.
- **125 -> 135 test count delta:** +9 flui entries (7 test slots + initTestCase + cleanupTestCase) plus one skip-to-pass migration in a non-flui binary. The prompt's predicted `132/0/3` undercount by 3 was likely written before testMenuOrder and testBilingualLabelsEnglish were folded into the slot list.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] testHelpNoQueue contradicted Plan 02 wiring**

- **Found during:** Task 2 first test run (after initial Qt rebuild)
- **Issue:** The original `testHelpNoQueue` slot (copied from 6.2 elas) asserted that NO Help action carries a lexicon dynamic property. But Plan 02's `xvue_qt_flui_actions.cpp:386-389` explicitly added a Help-menu action with lexicon `"97;"` (Mefisto VERSION NAME), per LEXICON-AUDIT-flui.md row 97 ("Help menu item; dispatches to VRSION"). Test failed with: `Help action "Mefisto VERSION NAME" carries lexicon payload "97;" but should not`.
- **Fix:** Replaced blanket "any lexicon is bad" with allowlist semantics. The slot now skips `lexicon.isEmpty()` actions (shared 6.0 generic Help), then asserts that any non-empty lexicon must be in the audited set `{97;}`. Spirit of the assertion preserved (no UNINTENDED Help payloads).
- **Files modified:** `xvue/qt/tests/test_xvue_qt_flui_menu.cpp` (2 hunks: added `#include <QSet>`; replaced the for-loop body to allowlist-check)
- **Verification:** Re-ran Qt build (incremental), re-ran flui_menu tests -- 9/9 PASS. Full suite still 135/0/2.
- **Committed in:** Task 2 commit `5d5f3bc` (single commit, includes both the original test file and the auto-fix as one cohesive test suite).

---

**Total deviations:** 1 auto-fixed (Rule 1 - blueprint bug: test contracted incorrectly relative to shipped wiring).
**Impact on plan:** Trivial -- the test still gates UNINTENDED Help lexicons; the allowlist is an explicit improvement that 6.4/6.5 will inherit.

## Issues Encountered

- **testHelpNoQueue blueprint inconsistency** (described above as Rule 1 auto-fix). Plan blueprint at line 583-593 specified the blanket assertion, which conflicted with Plan 02's audited Help wiring. The audit (LEXICON-AUDIT-flui.md row 97) had clearly placed `97;` under Help, but the test blueprint inherited the elas template where Help has no lexicons. Auto-fix in 5 minutes; no escalation needed.
- **Aggregate test count differed from prediction:** prompt predicted `132/0/3`; actual is `135/0/2`. The +3 delta vs prediction is likely because the prediction counted only "5 D-13 + 1 closeEvent + 1 testMenuOrder = 7" but the real count includes initTestCase + cleanupTestCase (2 entries) plus testBilingualLabelsEnglish (which the planner may have considered "optional" when SKIP'd). On this worktree all 9 PASS so the count is +9, not +7.

## CLAUDE.md compliance

- **Compilation must never break** -- `bin/cbl_tout_qt` exited 0 (Qt build, both initial and post-auto-fix incremental) AND `bin/cbl_tout` exited 0 (X11 build), run serially per MEMORY.md `feedback_parallel_builds_share_pp.md` discipline (never `&` or parallel make over `pp/`).
- **Tests must continue to pass** -- 135/0/2 green; 6.1 mail (8) + 6.2 elas (9) + 6.3 flui (9 new) all pass; no regressions.
- **Asking before acting** -- N/A: all dependencies (gcc-15, Qt6, libgfortran5 hold) already present.
- **Git discipline** -- 2 atomic commits (one per task). Descriptive messages explaining WHAT and WHY (including the auto-fix rationale). Hooks ran cleanly; no `--no-verify`.
- **Programming norms** -- Fortran 77 fixed-form column 7+ respected in ppflui.f (verified via `cbl_tout`); C++17 style matches existing test conventions (4-space indent, `QStringLiteral`, anonymous namespace helpers, `QSet<QString>` for allowlist).

## User Setup Required

None -- no external service configuration. The plan ships against the existing dev rig with `MEFISTO`, `MEFISTOX`, `td/m/anglais` flag, and `td/ma/debuflui` already in place.

## Verification Evidence

### Plan's `<verification>` checklist (16 items)

| # | Check | Result |
|---|-------|--------|
| 1 | `grep -c "CALL XVUE_MODULE_INIT( 'flui', 4 )" prpr/ppflui.f` | **1** |
| 2 | CALL inside IF(INTERA.GE.1) block | **1** (verified by awk extract) |
| 3 | `xvue/xvmodi.f` unchanged | empty diff |
| 4 | `xvue/qt/src/xvue_qt_api.cpp` unchanged | empty diff |
| 5 | `bin/cbl_tout_qt` exits 0 | exit 0 (Qt) |
| 6 | `bin/cbl_tout` (X11) exits 0 | exit 0 (X11) |
| 7 | ABI 58/58 | nm count: 58, header count: 58 |
| 8 | `nm pp/ppflui_qt | grep -c ' T registerFluiActions_stub_'` | **1** (force-link confirmed) |
| 9 | `nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'` | **1** (regression) |
| 10 | `nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'` | **1** (regression) |
| 11 | `xvue/qt/build/tests/xvue_qt_flui_menu_tests` binary exists | **yes** (1018.9K) |
| 12 | `QT_QPA_PLATFORM=offscreen .../xvue_qt_flui_menu_tests` exits 0 | **9 passed, 0 failed** |
| 13 | `add_executable(xvue_qt_flui_menu_tests` in CMake | **1** |
| 14 | `XVUE_QT_TESTING` count in CMake | **3** (mail + elas + flui) |
| 15 | All pre-existing tests still exit 0 | **9 binaries x rc=0** |
| 16 | `xvue/qt/src/xvue_qt_window.cpp` unchanged | empty diff |

## Threat-model close

| Threat ID | Disposition | How closed |
|-----------|-------------|------------|
| T-6.3-CALL-PLACEMENT | mitigated | CALL placed AFTER XVINIT and AFTER LOGO inside `IF(INTERA.GE.1)`; mirrors 6.1/6.2 placement discipline; verified by `awk` extract showing the CALL inside the block |
| T-6.3-REGRESSION | mitigated | Full Qt suite (135/0/2) ran before commit; 0 regressions |

## Hand-off to Phase 6.4 (ther)

Phase 6.4 follows the same pattern, alphabetically slotted (elas/flui/**ther** keepalive ordering):

1. **Audit:** mint LEXICON-AUDIT-ther.md from `td/m/debuther` (~similar to 6.3 cardinality)
2. **Wire:** ship `xvue/qt/src/xvue_qt_ther_actions.cpp` strong body + 6 SVG icons + qrc append + keepalive between elas/flui and mail (alphabetically: **elas/flui/ther/mail** -- ther goes before mail)
3. **Hook:** add `CALL XVUE_MODULE_INIT('ther', 4)` to `prpr/ppther.f` (3-line insertion, single atomic commit)
4. **Tests:** `xvue/qt/tests/test_xvue_qt_ther_menu.cpp` with 7 slots: testThermalSolve / testViewDrawTemp / testFileMSTools / testHelpNoQueue / testCloseEvent / testMenuOrder / testBilingualLabelsEnglish

Plan 03 leaves Plan 6.4 unblocked:
- Pattern proven idempotent across THREE modules (mail/elas/flui)
- testHelpNoQueue allowlist pattern (lesson learned in 6.3): consult the audit BEFORE writing the test; allowlist any audit-approved Help lexicons
- testMenuOrder + testBilingualLabelsEnglish are now mandatory carry-overs (they replace human-verify checkpoints)
- xvmodi.f and xvue_qt_window.cpp continue to need ZERO edits

## Self-Check: PASSED

- **Files:**
  - `prpr/ppflui.f`: FOUND (modified, +3 lines verified)
  - `xvue/qt/tests/test_xvue_qt_flui_menu.cpp`: FOUND (new, 380 lines)
  - `xvue/qt/tests/CMakeLists.txt`: FOUND (modified, +30 lines for flui target)
  - `.planning/phases/06.3-fluid-flui-menu-wiring/06.3-03-SUMMARY.md`: FOUND (this file)
- **Commits:**
  - `fab3f31` (Task 1: feat ppflui.f CALL XVUE_MODULE_INIT): FOUND in `git log --oneline`
  - `5d5f3bc` (Task 2: test 7 D-13 QTest slots + auto-fix testHelpNoQueue allowlist): FOUND
- **Plan checklist (Success Criteria from user prompt):**
  - prpr/ppflui.f CALL inserted inside IF(INTERA.GE.1) at correct site: VERIFIED (after XVINIT + LOGO, before ENDIF)
  - Task 1 atomic commit (single file: prpr/ppflui.f only): VERIFIED (`fab3f31`, single file in diff)
  - Both bin/cbl_tout_qt + bin/cbl_tout exit 0 serial: VERIFIED
  - Task 2 commit covers test file + CMakeLists.txt: VERIFIED (`5d5f3bc`, both files in diff)
  - verify_abi.sh: nm count 58 / header count 58: VERIFIED
  - All 3 module regression guards: VERIFIED (elas/flui/mail = 1 each)
  - xvue/xvmodi.f UNCHANGED: VERIFIED (empty diff)
  - xvue/qt/src/xvue_qt_api.cpp UNCHANGED: VERIFIED (empty diff)
  - xvue/qt/src/xvue_qt_window.cpp UNCHANGED: VERIFIED (empty diff)
  - xvue_qt_flui_menu_tests exits 0: VERIFIED (9 passed, 0 failed)
  - testMenuOrder PASS: VERIFIED (asserts {File, Fluid, View, Help})
  - testBilingualLabelsEnglish PASS: VERIFIED (worktree has anglais flag + td/ma/debuflui; 5;->STOKES, 99;->SAVE matched)
  - Full Qt test suite green: VERIFIED (135/0/2; expected ~132/0/3 from prompt, actual is +3 delta due to slot count + skip-to-pass migration)
  - 06.3-03-SUMMARY.md created: VERIFIED (this file)

---
*Phase: 06.3-fluid-flui-menu-wiring*
*Plan: 03 (Fortran hook + 7 D-13 QTest slots + auto-fix testHelpNoQueue allowlist)*
*Completed: 2026-04-28*
