---
phase: 03.1
slug: build-xvtest1-4-driver-infrastructure-on-both-backends-qt-le
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-12
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Shell scripts + gfortran compile + headless process smoke |
| **Config file** | none — Phase 03.1 is build-infra; validation is via existing cb* scripts |
| **Quick run command** | `bin/cbxvtest<N>_qt && QT_QPA_PLATFORM=offscreen timeout 5s pp/ppxvtest<N>_qt; echo $?` |
| **Full suite command** | `bin/cbl_tout_qt && bin/cbl_tout && for N in 0 1 2 3 4; do QT_QPA_PLATFORM=offscreen timeout 5s pp/ppxvtest${N}_qt; done` |
| **Estimated runtime** | ~90-120 seconds for full (both backends + 5 smokes) |

---

## Sampling Rate

- **After every task commit:** Run quick command for the affected driver (e.g. rebuild + smoke only the touched `ppxvtestN_qt`)
- **After every plan wave:** Run full suite command — both backends green + all 5 Qt drivers headless-smoke green
- **Before `/gsd-verify-work`:** Full suite must be green and ABI count must be 34 (unchanged from Phase 3)
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command Summary | Status |
|---------|------|------|-------------|-----------|---------------------------|--------|
| 03.1-01-01 | 01 | 0 | BUILD-07, VALID-02 | shell+grep | A6 grep: `EXPECTED/ACTUAL` of `CALL XVOUVRIR` in `prpr/*.f` must equal `xvtest1..4.f` exactly | ⬜ pending |
| 03.1-01-02 | 01 | 0 | BUILD-07, VALID-02 | build+nm+run | `cmake --build xvue/qt/build` green; `nm libxvueqt.a` shows exactly 1 `xtinit_` and 34 total; `xvuelc.c`/`xvouvrir.f`/`xvue_qt_api.h` untouched; `ppxvtest0_qt` headless smoke exit 0 | ⬜ pending |
| 03.1-02-01 | 02 | 1 | BUILD-07 | shell+build | 4 new `bin/cbxvtest{1..4}_qt` exist, reference `prpr/xvtestN.f`, link `-lxvueqt`, produce `pp/ppxvtestN_qt` | ⬜ pending |
| 03.1-02-02 | 02 | 1 | VALID-02 | shell+build | 5 new `bin/cbxvtest{0..4}` (legacy X11) exist, link `xvue/xvuelc.o -lX11`, produce `pp/ppxvtestN` | ⬜ pending |
| 03.1-02-03 | 02 | 1 | BUILD-07, VALID-02 | shell+build+exec | `bin/cbl_tout_qt` invokes all 5 `cbxvtest*_qt`; `bin/cbl_tout` invokes all 5 `cbxvtest*`; full rebuilds green; all 10 drivers + 5 modules land in `pp/` | ⬜ pending |
| 03.1-03-01 | 03 | 2 | BUILD-07, VALID-02, TEXT-01..06 | smoke+nm+grep | All 5 `ppxvtestN_qt` smoke green under `QT_QPA_PLATFORM=offscreen timeout 5s` (exit 0 or 124); grep for xtinit warn-once = 0; ABI still 34; `03.1-NOTES.md` exists with xvtest1+D-04 content | ⬜ pending |
| 03.1-03-02 | 03 | 2 | BUILD-07, VALID-02 | grep | `03-04-PLAN.md` Task 1 patched: contains `timeout --preserve-status 5s pp/ppxvtest`, enriched grep with `xtinit`, `PROBLEME AVEC LA SOURIS` tolerance, `RC -ne 0.*RC -ne 124`, and `03.1-NOTES.md` pointer | ⬜ pending |
| 03.1-03-03 | 03 | 2 | BUILD-07, VALID-02, TEXT-01..06 | doc | `03.1-SUMMARY.md` exists, contains "03-04 is now unblocked"; `STATE.md` contains "Phase 03.1" | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*All 8 tasks have concrete `<verify><automated>` commands in the source plans. The "Automated Command Summary" column is a human-readable condensation — the exact command strings live in the plan files and are run verbatim by the executor.*

---

## Wave 0 Requirements

Wave 0 for Phase 03.1 is the discovery/design decision wave identified by research (Pitfall 1 fix path decision + xvuelc.c:286-305 safety check + prpr/ grep for CALL XVOUVRIR call sites). It does not produce test files — it produces a pinned decision recorded in CONTEXT.md / plan action steps.

- [ ] Pitfall 1 fix path decided (Fortran-side xvouvrir.f vs Qt-side xtinit_ promotion)
- [ ] xvuelc.c:286-305 inspected to verify Fortran-side fix is safe on X11 backend (Assumption A1)
- [ ] `grep -l 'CALL XVOUVRIR' prpr/` run to confirm no other solver drivers are affected (Assumption A6)
- [ ] Plan 03-04 Task 1 smoke grep patch scope decided (own it in 03.1 or defer)

*Existing infrastructure (gfortran, pkg-config, Qt6, libX11) covers all Phase 03.1 requirements — no new framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live xvtest1..4 A/B visual comparison (Qt vs X11) | TEXT-01..06 via 03-04 Task 2 | Phase 03.1 is the enabler — the actual A/B gate is plan 03-04 Tasks 2 and 3 which require a live X11 display | Defer to 03-04 after 03.1 completes |

*All Phase 03.1 internal behaviors (build, headless smoke, ABI check) have automated verification. The only manual work is the downstream 03-04 A/B gate that 03.1 unblocks.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers the Pitfall 1 decision + safety checks
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter (flipped to true in final task when 03.1 closes)

**Approval:** pending
