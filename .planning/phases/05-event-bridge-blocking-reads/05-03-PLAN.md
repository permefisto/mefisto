---
phase: 05-event-bridge-blocking-reads
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - xvue/qt/src/xvue_qt_event.h
  - xvue/qt/src/xvue_qt_event.cpp
  - xvue/qt/tests/test_xvue_qt_event.cpp
autonomous: true
requirements:
  - EVENT-07
must_haves:
  truths:
    - "Mouse motion events are coalesced via QTimer::singleShot(0, loop, QEventLoop::quit)"
    - "waitForEvent returns the LAST (most recent) motion coordinates in a burst, not the first"
    - "Fast-drag integration test shows bounded returned motion events for 100 input moves"
    - "Diagnostic motion counter (stderr) is gated by MEFISTO_XVSOURIS_DEBUG env var"
    - "pending_x/pending_y never hold stale data across separate waitForEvent invocations"
  artifacts:
    - path: "xvue/qt/src/xvue_qt_event.cpp"
      provides: "MouseMove handler with deferred-quit coalescing plus diagnostic counter"
  key_links:
    - from: "XvueEventBridge::eventFilter MouseMove case"
      to: "QEventLoop::quit via QTimer::singleShot(0)"
      via: "deferred quit pattern matching X11 XEventsQueued(QueuedAfterFlush)"
      pattern: "QTimer::singleShot"
---

<objective>
Layer mouse-motion coalescing on top of the Plan 02 bridge. Implement the
QTimer::singleShot(0, loop, QEventLoop::quit) deferred-quit pattern so
waitForEvent() returns the last position in a motion burst with zero added
latency (X11 XEventsQueued(QueuedAfterFlush) parity).

Add a diagnostic fprintf(stderr, ...) motion counter gated by a new env var
MEFISTO_XVSOURIS_DEBUG so Plan 06 can resolve Assumption A2 empirically
(whether AA_CompressHighFrequencyEvents compresses before or after the
filter — RESEARCH §12 Q1/Q2).

Output: A real MouseMove case in eventFilter(), motion-coalescing integration
test, and a debug counter emitting per-waitForEvent motion counts.
</objective>

<execution_context>
@/home/drico/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/drico/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/drico/git/mefisto/.planning/phases/05-event-bridge-blocking-reads/05-CONTEXT.md
@/home/drico/git/mefisto/.planning/phases/05-event-bridge-blocking-reads/05-RESEARCH.md
@/home/drico/git/mefisto/.planning/phases/05-event-bridge-blocking-reads/05-VALIDATION.md

Interfaces from xvue/qt/src/xvue_qt_event.h (after Plan 02):
  class XvueEventBridge has WaitMode enum, Result struct, waitForEvent(),
  private QEventLoop* loop_, Result pending_, bool quit_pending_.

X11 reference semantics from xvuelc.c §2248-2263: only the tail of a motion
burst is emitted; all intermediate MotionNotify events are dropped when
XEventsQueued(QueuedAfterFlush) returns greater than zero.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MouseMove case with deferred-quit coalescing plus diagnostic counter</name>
  <read_first>
    - xvue/qt/src/xvue_qt_event.cpp (from Plan 02 Task 2)
    - xvue/qt/src/xvue_qt_event.h (Plan 02 state)
    - xvue/xvuelc.c lines 2248-2263 (X11 motion coalescing reference)
    - .planning/phases/05-event-bridge-blocking-reads/05-RESEARCH.md §3 (motion coalescing), §11 Pitfall 8 (fresh position per event), Pitfall 9 (reset quit_pending)
    - .planning/phases/05-event-bridge-blocking-reads/05-CONTEXT.md D-04, D-05
  </read_first>
  <behavior>
    - Test 1: Single QTest::mouseMove on the canvas — waitForEvent returns notypeevent=-2, x/y matches the move position.
    - Test 2: 100 rapid QTest::mouseMove calls in a tight loop — waitForEvent returns bounded number of times (≤ 20) with tail position last.
    - Test 3: The motion handler stashes only the most recent position (pending_.x / pending_.y).
    - Test 4: quit_pending_ is reset to false at the start of every waitForEvent call (Pitfall 9).
    - Test 5: When MEFISTO_XVSOURIS_DEBUG=1 is set, stderr contains a "motion_count=N" line per waitForEvent return.
  </behavior>
  <action>
1. Add to xvue/qt/src/xvue_qt_event.h under the private section:
   `int motion_count_ = 0;` plus a static helper
   `static bool debug_logging_enabled();`

2. In xvue/qt/src/xvue_qt_event.cpp add includes for cstdio, cstdlib, QTimer.
   Implement debug_logging_enabled() as a cached env-var check:
   on first call, read getenv("MEFISTO_XVSOURIS_DEBUG"); cache the
   result in a static const bool.

3. In waitForEvent(), immediately after the existing `quit_pending_ = false;`
   line (added in Plan 02), also set `motion_count_ = 0;`. Just before
   the function returns pending_, if debug_logging_enabled() is true,
   fprintf to stderr a single line with fields:
   mode, notypeevent, nbc, x, y, motion_count_, XvueApp::blockingDepth().
   The fprintf format string must include the literal token
   "motion_count=" so the grep in the verify block matches.

4. In eventFilter(), replace the current MouseMove case (which was a
   pass-through `return false;` in Plan 02) with the full coalescing
   implementation:
   - Cast event to QMouseEvent. Set pending_.notypeevent = -2,
     pending_.nbc = 0, pending_.x = me->pos().x(), pending_.y = me->pos().y().
     (Pitfall 8: every branch sets pending_.x / pending_.y fresh; never
     trust stale data.)
   - Increment motion_count_.
   - If quit_pending_ is false, set it to true and call
     QTimer::singleShot(0, loop_, QEventLoop::quit).
   - Return true (eat the event).

5. Update xvue/qt/tests/test_xvue_qt_event.cpp: replace the
   testXvsourisMotion and testMotionCoalescing QSKIP stubs with real
   bodies that drive QTest::mouseMove under a QTimer::singleShot(20,...).
   The testXvsourisMotion case posts a single move to (42,73) and asserts
   waitForEvent returns notypeevent=-2 and x=42, y=73. The
   testMotionCoalescing case posts 100 moves in a tight loop from (10,20)
   through (109,119) and then drains up to 20 waitForEvent calls; the
   assertion is that total returned motion events is bounded by 20 (not
   100). The last returned x must be within the (10..109) range.

6. Add a placeholder `testDebugLoggingEnvVar` that QSKIPs with a note
   that the env-var flag is cached on first call, so this diagnostic is
   validated manually in Plan 06. The actual stderr grep is the
   acceptance_criteria check on `motion_count=` from the test binary's
   stderr output during this task's verify run.

7. Build with `bin/cbl_tout_qt`. Run with MEFISTO_XVSOURIS_DEBUG=1
   through xvfb-run and verify the binary exits 0 and stderr contains
   at least one `motion_count=` line.
  </action>
  <verify>
    <automated>bin/cbl_tout_qt && MEFISTO_XVSOURIS_DEBUG=1 xvfb-run -a xvue/build/tests/xvue_qt_event_tests -v2 2>&amp;1 | tee /tmp/plan03_test.log | grep -E "testXvsourisMotion|testMotionCoalescing|motion_count=" && test $(grep -c "motion_count=" /tmp/plan03_test.log) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "QTimer::singleShot" xvue/qt/src/xvue_qt_event.cpp` returns at least 1
    - `grep -c "motion_count_" xvue/qt/src/xvue_qt_event.cpp` returns at least 3
    - `grep -c "MEFISTO_XVSOURIS_DEBUG" xvue/qt/src/xvue_qt_event.cpp` returns at least 1
    - `grep -c "pending_.notypeevent = -2" xvue/qt/src/xvue_qt_event.cpp` returns at least 1
    - `bin/cbl_tout_qt` exits 0
    - `bin/cbl_tout` exits 0 (X11 untouched)
    - `xvfb-run -a xvue/build/tests/xvue_qt_event_tests -v2` reports PASS for testXvsourisMotion and testMotionCoalescing
    - With MEFISTO_XVSOURIS_DEBUG=1 set, the stderr output contains at least one line matching `motion_count=`
    - `grep -r "QApplication::exec" xvue/qt/src/ xvue/qt/include/` returns 0 matches
  </acceptance_criteria>
  <done>
MouseMove coalescing lands; EVENT-07 unit test PASSes with bounded event
count for 100 input moves. Diagnostic counter emits per-waitForEvent
summary to stderr when MEFISTO_XVSOURIS_DEBUG=1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qt event queue → bridge filter | Motion burst of unbounded size could drive memory/time pressure |
| Diagnostic stderr | Debug env var surfaces internal state; must not leak sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | DoS (event flood) | Fast mouse drag floods filter with unbounded MotionNotify events | mitigate | Deferred-quit pattern consumes motion events via pending_ stash (O(1) memory per event) and coalesces via AA_CompressHighFrequencyEvents at the Qt dispatch layer |
| T-05-03-02 | Info Disclosure | Diagnostic stderr counter reveals internal state | accept | Gated by MEFISTO_XVSOURIS_DEBUG env var; no sensitive data (coordinates and event counts only); default OFF |
| T-05-03-03 | Tampering (stale state) | pending_.x / pending_.y stale across waitForEvent calls | mitigate | Every branch in eventFilter() that touches pending_ sets x/y fresh (Pitfall 8); waitForEvent() resets pending_ to default-constructed Result at entry |
| T-05-03-04 | DoS (deferred-timer leak) | quit_pending_ stuck true across calls leaves timer unarmed | mitigate | waitForEvent() resets quit_pending_ = false at entry (Pitfall 9) |
</threat_model>

<verification>
- `bin/cbl_tout_qt` and `bin/cbl_tout` both exit 0
- testXvsourisMotion and testMotionCoalescing PASS
- With MEFISTO_XVSOURIS_DEBUG=1 set, stderr contains motion_count= lines
- SHELL-03 grep guard still passes
- Closes RESEARCH §11 Pitfalls 8 and 9
- Closes EVENT-07 unit-test portion (human A/B part remains in Plan 06)
- Provides the diagnostic counter Plan 06 needs to resolve Assumption A2
</verification>

<success_criteria>
- MouseMove case in eventFilter uses QTimer::singleShot deferred-quit
- 100-move integration test returns bounded event count (≤ 20)
- Motion counter emits per-waitForEvent summary to stderr when debug env var set
- pending_.x / pending_.y always reflect the most recent event position
</success_criteria>

<output>
After completion, create .planning/phases/05-event-bridge-blocking-reads/05-03-SUMMARY.md
documenting motion coalescing behavior, diagnostic counter usage, and the
recorded bounded-count number from the integration test.
</output>
