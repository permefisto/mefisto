---
phase: 06.4-thermal-ther-menu-wiring
plan: 03
subsystem: ui
tags: [qt6, fortran77, weak-symbol-reuse, force-link-keepalive, qtest, qaction, xvsouris, limtcl, ther, thermicer, menu-order, bilingual-labels, help-allowlist]

# Dependency graph
requires:
  - phase: 06.4-thermal-ther-menu-wiring/01
    provides: frozen LEXICON-AUDIT-ther.md (209 rows; 3;=Steady Heat, 8;=Draw temp+flux, 70;=MS Files, 98;=Mefisto Date Version under Help)
  - phase: 06.4-thermal-ther-menu-wiring/02
    provides: xvue_qt_ther_actions.cpp strong body (registerTherActions_stub_) + 7 SVG icons + qrc append + xvue_qt_ther_actions_keepalive() wired from XvueApp::ensure() + CMake target_sources
  - phase: 06.3-fluid-flui-menu-wiring/03
    provides: prpr/ppflui.f CALL XVUE_MODULE_INIT('flui',4) template; xvue_qt_flui_menu_tests test pattern (7 D-13 slots) reused verbatim with ther substitutions
  - phase: 06.2-elasticity-elas-menu-wiring/03
    provides: ppelas.f bilingual LOGO insertion pattern (THERMICER/HEATER mirrors ELASTICER more closely than flui's single-language LOGO); xvue/xvmodi.f weak Fortran stub (reused verbatim from 6.1 Plan 03)
  - phase: 06.1-mesher-mail-menu-wiring/03
    provides: shared D-09 closeEvent + 99; push-on-blocking-depth in XvueWindow (inherited unchanged by ppther_qt)
provides:
  - "prpr/ppther.f CALL XVUE_MODULE_INIT('ther', 4) inside IF(INTERA.GE.1) block AFTER bilingual LOGO ENDIF (THERMICER/HEATER per LANGAG flag)"
  - "xvue/qt/tests/test_xvue_qt_ther_menu.cpp -- 7 D-13 test slots covering 4 D-13 required + closeEvent + testMenuOrder + testBilingualLabelsEnglish"
  - "xvue/qt/tests/CMakeLists.txt -- add_executable(xvue_qt_ther_menu_tests) target with target_compile_definitions(XVUE_QT_TESTING)"
affects: [06.5]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single-line Fortran CALL inside IF(INTERA.GE.1) -- 4th replication of the 6.1 pattern. ppther.f uses bilingual LOGO (THERMICER/HEATER per LANGAG) -- placement mirrors 6.2 ppelas.f more closely than 6.3 ppflui.f (single-language LOGO)."
    - "QTest round-trip pattern verbatim from 6.3 flui: QAction::trigger() + xvsouris_ drain + assert char sequence (RESEARCH §Pitfall 4 — NOT postEvent+processEvents)"
    - "Per-module Help-allowlist pattern: testHelpNoQueue allowlist {98;} for ther (drawn from LEXICON-AUDIT-ther.md row 98). Distinct from flui's {97;}, mail's {} (empty), and elas's {} (empty). Codifies 6.3 Plan 03 Auto-fix Rule 1 lesson."
    - "Autonomous-via-test-codification: testMenuOrder + testBilingualLabelsEnglish replace the 6.2 manual A/B human-verify checkpoint -- entire phase 6.4 machine-verifiable end-to-end."

key-files:
  created:
    - "xvue/qt/tests/test_xvue_qt_ther_menu.cpp"
    - ".planning/phases/06.4-thermal-ther-menu-wiring/06.4-03-SUMMARY.md"
  modified:
    - "prpr/ppther.f"
    - "xvue/qt/tests/CMakeLists.txt"

key-decisions:
  - "CALL placement after BILINGUAL LOGO ENDIF: prpr/ppther.f line 142 inserts CALL XVUE_MODULE_INIT('ther', 4) AFTER the IF(LANGAG.EQ.0) THEN CALL LOGO('MEFISTO-THERMICER') ELSE CALL LOGO('MEFISTO-HEATER') ENDIF block (line 139), inside the outer IF(INTERA.GE.1) ENDIF (line 143). Mirrors 6.2 ppelas.f bilingual placement; differs from 6.3 ppflui.f's single-language LOGO."
  - "Zero Fortran-side changes to xvue/xvmodi.f -- the 6.1 !GCC$ ATTRIBUTES WEAK stub already covers all five solver modules. Phase 6.4 just adds one CALL site; pattern self-amortizes from 6.2 onward."
  - "testHelpNoQueue allowlist {98;} drawn from LEXICON-AUDIT-ther.md row 98 explicit hand-off section (NOT inherited blindly from 6.3 flui template's {97;}). Codifies 6.3 Plan 03 Auto-fix Rule 1 lesson: read the audit BEFORE writing the test."
  - "testMenuOrder + testBilingualLabelsEnglish carried forward verbatim from 6.3 Plan 03. Both PASS on this worktree (anglais flag + td/ma/debuther present), codifying the gates that 6.2 left as manual UAT items."
  - "Aggregate Qt test totals: 132/1/4 (Plan 02 baseline corrected from stale 6.3 figure of 135/0/2) -> 143/1/2 (after Plan 03). +9 ther entries (7 test slots + initTestCase + cleanupTestCase) plus 2 skip-to-pass shifts in elas/flui menu_tests. The 1 pre-existing testPerModuleGroupIsolation fail is unchanged (deferred-items.md)."

patterns-established:
  - "Pattern proves idempotent across FOUR modules now (mail->elas->flui->ther). 6.5 nlse is a final mechanical near-copy: substitute module name + lexicon list + EN-label assertion strings + Help-allowlist value (drawn from LEXICON-AUDIT-nlse.md when minted)."
  - "Per-module testHelpNoQueue MUST consult the per-module LEXICON-AUDIT BEFORE coding the allowlist. ther = {98;}; flui = {97;}; mail/elas = {}. 6.5 nlse may differ again."
  - "Bilingual ppther.f LOGO insertion: AFTER both branches of the IF(LANGAG.EQ.0)/ELSE/ENDIF block, BEFORE the outer ENDIF of IF(INTERA.GE.1). Same shape as 6.2 ppelas.f."

requirements-completed: [UX-05]

# Metrics
duration: ~12min active wall-time (Task 1 Qt+X11 serial ~5min, Task 2 Qt rebuild ~3min, full test suite verification ~30s)
completed: 2026-04-28
---

# Phase 06.4 Plan 03: ther Fortran hook + 7 D-13 QTest slots Summary

**One-line `CALL XVUE_MODULE_INIT('ther', 4)` in `prpr/ppther.f` (after bilingual `MEFISTO-THERMICER`/`MEFISTO-HEATER` LOGO ENDIF, inside `IF(INTERA.GE.1)` block) plus 7 D-13 QTest slots in `xvue/qt/tests/test_xvue_qt_ther_menu.cpp` close out Phase 6.4 fully autonomously: `testMenuOrder` codifies `{File, Thermal, View, Help}` order; `testBilingualLabelsEnglish` codifies EN-label parser gate against `td/ma/debuther`; `testHelpNoQueue` allowlist `{98;}` (NOT `{97;}` like flui — codifies 6.3 Plan 03 Auto-fix Rule 1 lesson); aggregate Qt suite is 143/1/2 (was 132/1/4 at Plan 02 base, +9 ther entries plus 2 skip-to-pass shifts in elas/flui menu_tests; the 1 fail is the pre-existing `testPerModuleGroupIsolation` from Wave 2's deferred-items.md), ABI 58/58 preserved, all four force-links (mail/elas/flui/ther) intact in their `pp*_qt` binaries, no edits to `xvue/xvmodi.f`, `xvue/qt/src/xvue_qt_api.cpp`, or `xvue/qt/src/xvue_qt_window.cpp`.**

## Performance

- **Duration:** ~12 min active wall-time
- **Started:** worktree base reset to 85ddf3c (Plan 02 tip)
- **Completed:** 2026-04-28
- **Rebuild wall-clock:** Task 1 Qt+X11 serial ~5min; Task 2 Qt rebuild ~3min
- **Tasks:** 2 (both autonomous, no checkpoints; --no-verify per orchestrator parallel-executor directive)
- **Files created:** 2 (`xvue/qt/tests/test_xvue_qt_ther_menu.cpp` -- 433 lines; this SUMMARY.md)
- **Files modified:** 2 (`prpr/ppther.f` +3 lines, `xvue/qt/tests/CMakeLists.txt` +31 lines)

## Accomplishments

### Task 1 -- prpr/ppther.f CALL insertion (commit `3c45508`)

Three-line insertion (2 comment lines + 1 CALL) inside the `IF( INTERA .GE. 1 )` block, AFTER `CALL XVINIT` AND AFTER the bilingual `IF(LANGAG.EQ.0)/ELSE/ENDIF` LOGO block (THERMICER/HEATER) AND BEFORE the outer `ENDIF`:

```fortran
         IF( LANGAG .EQ. 0 ) THEN
            CALL LOGO( 'MEFISTO-THERMICER' )
         ELSE
            CALL LOGO( 'MEFISTO-HEATER' )
         ENDIF
C        Phase 6.4 D-08: register Qt-side module-specific menu actions.
C        No-op on the X11 backend (xvue/xvmodi.f weak stub from 6.1).
         CALL XVUE_MODULE_INIT( 'ther', 4 )
      ENDIF
```

Column-7+ fixed-form discipline respected (`CALL` at column 10 to match the surrounding `CALL XTINIT/XVINIT/LOGO`; `C` in column 1 for comments). Integer `4` is the length of `'ther'`. Insertion lines are 140-142 in the new file (between original line 139 LOGO ENDIF and original line 140 outer ENDIF).

**Bilingual placement detail:** Unlike 6.3 ppflui.f (which has a single `CALL LOGO('Mefisto-FLUIDER')` line and inserts after it directly), ppther.f has the bilingual `IF(LANGAG.EQ.0)/ELSE/ENDIF` LOGO block — the CALL goes AFTER the inner ENDIF of the bilingual block, BEFORE the outer ENDIF of `IF(INTERA.GE.1)`. Same shape as 6.2 ppelas.f.

### Task 2 -- 7 D-13 QTest slots + new CMake target (commit `8d05637`)

`xvue/qt/tests/test_xvue_qt_ther_menu.cpp` -- 7 slots:

| Slot | Lexicon | Menu | Expected xvsouris drain | Notes |
|------|---------|------|-------------------------|-------|
| `testThermalSolveSteady` | `3;` | Thermal -> HeatSteady (parent shortcut) | `{'3', ';', 13}` | STEADY HEAT TRANSFER solver |
| `testViewDrawTemperature`  | `8;` | View -> DrawTemperature (parent shortcut) | `{'8', ';', 13}` | DRAWING of TEMPERATURES + FLUX |
| `testFileMSTools`      | `70;` | File | `{'7', '0', ';', 13}` | MANAGE TMS Files Units |
| `testHelpNoQueue`      | n/a (allowlist `{98;}`) | Help | queueSize unchanged | per ther's audit (NOT `{97;}` like flui) |
| `testMenuOrder`        | n/a | (menu bar) | top-4 = `{File, Thermal, View, Help}` | replaces 6.2 manual A/B gap-1 |
| `testBilingualLabelsEnglish` | n/a | (parser) | `label(3)` ~ "STEADY"/"HEAT"; `label(8)` ~ "TEMPERATURE"/"FLUX"; `label(99)` ~ "SAVE"/"QUIT" | replaces 6.2 manual A/B gap-2; PASSES on this worktree |
| `testCloseEventPushes99WhenBlocking` | `99;` | (close event) | queueSize += 4 | D-09 shared closeEvent gate inherited from 6.1 |

`xvue/qt/tests/CMakeLists.txt` -- new `add_executable(xvue_qt_ther_menu_tests)` target appended AFTER the flui block, linked against `xvueqt Qt6::Test Qt6::Widgets Qt6::Gui Qt6::Core`, with `target_compile_definitions(... PRIVATE XVUE_QT_TESTING)` enabling `clearCacheForTesting()` + `xvueClearLanguageCacheForTesting()` for the `testBilingualLabelsEnglish` bookend. `XVUE_QT_TESTING` count in CMake is now 4 (mail + elas + flui + ther).

### Test results (offscreen QPA)

```
PASS   : TestXvueQtTherMenu::initTestCase()
PASS   : TestXvueQtTherMenu::testThermalSolveSteady()
PASS   : TestXvueQtTherMenu::testViewDrawTemperature()
PASS   : TestXvueQtTherMenu::testFileMSTools()
PASS   : TestXvueQtTherMenu::testHelpNoQueue()
PASS   : TestXvueQtTherMenu::testMenuOrder()
PASS   : TestXvueQtTherMenu::testBilingualLabelsEnglish()
PASS   : TestXvueQtTherMenu::testCloseEventPushes99WhenBlocking()
PASS   : TestXvueQtTherMenu::cleanupTestCase()
Totals: 9 passed, 0 failed, 0 skipped, 0 blacklisted
```

`testBilingualLabelsEnglish` PASSED (not SKIPPED) because:
- `MEFISTO=/home/mefisto/git/mefisto` env set
- `$MEFISTO/td/m/anglais` flag present
- `$MEFISTO/td/ma/debuther` readable

The slot QSKIPs cleanly on missing preconditions, so it ports to constrained CI.

### Aggregate Qt test suite (offscreen QPA)

```
xvue_qt_canvas_gestures_tests:      12 passed, 0 failed, 0 skipped
xvue_qt_dialogs_console_tests:      14 passed, 0 failed, 0 skipped
xvue_qt_elas_menu_tests:             9 passed, 0 failed, 0 skipped
xvue_qt_event_menu_predrain_tests:  10 passed, 0 failed, 0 skipped
xvue_qt_event_tests:                33 passed, 0 failed, 2 skipped
xvue_qt_flui_menu_tests:             9 passed, 0 failed, 0 skipped
xvue_qt_i18n_menu_prefs_tests:      18 passed, 1 failed, 0 skipped (PRE-EXISTING)
xvue_qt_mail_menu_tests:             8 passed, 0 failed, 0 skipped
xvue_qt_menu_tests:                  5 passed, 0 failed, 0 skipped
xvue_qt_ther_menu_tests:             9 passed, 0 failed, 0 skipped (NEW)
xvue_qt_window_chrome_tests:        16 passed, 0 failed, 0 skipped
====
TOTAL: 143 passed, 1 failed, 2 skipped
```

Plan 02 baseline (reported in 06.4-02-SUMMARY) was `132/1/4`; Plan 03 adds 9 ther entries (`132+9=141`) plus a 2-skip-to-pass shift in `xvue_qt_elas_menu_tests` (was 8/0/1, now 9/0/0) and `xvue_qt_flui_menu_tests` (was 8/0/1, now 9/0/0) brings the aggregate to **143/1/2**. The 1 fail is the pre-existing `xvue_qt_i18n_menu_prefs_tests::testPerModuleGroupIsolation` — same fail at base 7d0f892, NOT caused by Plan 03 (deferred-items.md from Wave 2).

## Regression guarantees

| Check | Result |
|-------|--------|
| `git diff xvue/xvmodi.f` | empty (0 bytes) -- 6.1 stub serves all modules |
| `git diff xvue/qt/src/xvue_qt_api.cpp` | empty -- no new ABI entries |
| `git diff xvue/qt/src/xvue_qt_window.cpp` | empty -- D-09 closeEvent inherited unchanged |
| `verify_abi.sh` | `nm count: 58  header count: 58` -- no drift |
| `nm pp/ppther_qt \| grep -c ' T registerTherActions_stub_'` | **1** -- force-link survives full rebuild |
| `nm pp/ppmail_qt \| grep -c ' T registerMailActions_stub_'` | **1** -- 6.1 regression guard intact |
| `nm pp/ppelas_qt \| grep -c ' T registerElasActions_stub_'` | **1** -- 6.2 regression guard intact |
| `nm pp/ppflui_qt \| grep -c ' T registerFluiActions_stub_'` | **1** -- 6.3 regression guard intact |
| `nm pp/ppther \| grep -c 'xvue_module_init_'` | **1** -- X11 stub resolves cleanly |

## Decisions Made

- **CALL placement (bilingual LOGO):** mirror 6.2 ppelas.f exactly. Inside `IF( INTERA .GE. 1 ) THEN ... ENDIF` block. After `CALL XVINIT`, after the BILINGUAL `IF(LANGAG.EQ.0) CALL LOGO('MEFISTO-THERMICER') ELSE CALL LOGO('MEFISTO-HEATER') ENDIF` block, before outer `ENDIF`. The bilingual block presents BOTH `MEFISTO-THERMICER` (FR) and `MEFISTO-HEATER` (EN) translations of the module name -- placement rule (post-LOGO, pre-outer-ENDIF) is identical to all 4 modules.
- **Zero new Fortran files:** The 6.1 `xvue/xvmodi.f` weak stub (`!GCC$ ATTRIBUTES WEAK :: XVUE_MODULE_INIT`) already covers ALL five solver modules. Plan 03 adds one CALL line; no per-module Fortran stub.
- **testHelpNoQueue allowlist {98;} (per LEXICON-AUDIT-ther row 98):** the audit explicitly states `Plan 03's testHelpNoQueue allowlist MUST be {98;} -- explicit per-module variation per 6.3 Plan 03 Auto-fix Rule 1 lesson. Verified against td/m/debuther line 22.` Plan 03's allowlist hard-codes `{ QStringLiteral("98;") }` -- the test gates UNINTENDED Help payloads (e.g., flui's `97;` or any other lexicon) while accepting the audit-approved one.
- **testMenuOrder + testBilingualLabelsEnglish carried forward verbatim from 6.3:** these two slots were the explicit autonomous-flag-justifier in this plan's blueprint. Both PASS on this worktree, replacing the 6.2 manual A/B human-verify checkpoint with machine-verifiable QTest gates. Phase 6.4 ships fully autonomous as a result.
- **132/1/4 -> 143/1/2 test count delta:** +9 ther entries (7 test slots + initTestCase + cleanupTestCase) plus 2 skip-to-pass shifts in elas/flui menu_tests. The 1 pre-existing `testPerModuleGroupIsolation` fail is unchanged. Plan 03 adds zero new failures or skips.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree base on the wrong commit; build-output noise required PATH fixup**

- **Found during:** initial `worktree_branch_check`
- **Issue:** Worktree branch `worktree-agent-a69850c4f41529989` was on commit `ac282f8` (initial commit lineage), not the Wave 2 base `85ddf3c`. Plan 03 depends on Plan 02's `xvue_qt_ther_actions.cpp` + force-link keepalive (commits 6f3e610, d30087e, 85ddf3c).
- **Fix:** `git reset --hard 85ddf3c25c6beae422e43e2aa2599bdb717c85a0` (working tree was clean — safe). Compiler PATH fixup: prepended `/tmp/mefisto-compilers-bin` (existing symlinks `gfortran→gfortran-14`, `gcc→gcc-13`, `g++→g++-14`) to PATH for the build session. Also exported `MEFISTO=/home/mefisto/git/mefisto` for the test runs (Wave 2 used this same workaround per 06.4-02-SUMMARY deviation #3).
- **Files modified:** branch HEAD only; PATH/env are session-local.
- **Verification:** `git log --oneline -1 → 85ddf3c`, `git status → clean`; `gfortran --version → 14.3.0`; both backends build clean.
- **Committed in:** N/A (preparation step before Task 1).

**Total deviations:** 1 (Rule 3 — Blocking, env-only; same as Wave 2 deviation #3 — symlinks live in `/tmp` and don't persist across reboot, so each fresh agent must redo the PATH fixup).

## Issues Encountered

### 1. Pre-existing test failure: testPerModuleGroupIsolation

**Description:** `xvue_qt_i18n_menu_prefs_tests::testPerModuleGroupIsolation()` fails the same way at base `7d0f892` as it does at HEAD post-Plan 03. Documented in Wave 2's `deferred-items.md` and 06.4-02-SUMMARY.md §"Issues Encountered". Out of scope for Plan 03 — same SCOPE BOUNDARY rule applied.

**Resolution:** No change. Plan 03 inherits the deferred-items.md log; verifier should reference it.

## CLAUDE.md compliance

- **Compilation must never break** -- `bin/cbl_tout_qt` exited 0 (Qt build, both initial and post-Task-2 incremental) AND `bin/cbl_tout` exited 0 (X11 build), run serially per MEMORY.md `feedback_parallel_builds_share_pp.md` discipline (never `&` or parallel make over `pp/`).
- **Tests must continue to pass** -- 143/1/2 (the 1 fail is the pre-existing testPerModuleGroupIsolation, unchanged); 6.1 mail (8) + 6.2 elas (9) + 6.3 flui (9) + 6.4 ther (9 new) all pass; no new regressions.
- **Asking before acting** -- N/A: all dependencies (gcc-13, gfortran-14, Qt6, libgfortran5 hold) already present.
- **Git discipline** -- 2 atomic commits (one per task), `--no-verify` per orchestrator parallel-executor directive. Descriptive messages explaining WHAT and WHY (including the bilingual placement rationale and the audit-driven allowlist).
- **Programming norms** -- Fortran 77 fixed-form column 7+ respected in ppther.f (verified via `cbl_tout`); C++17 style matches existing test conventions (4-space indent, `QStringLiteral`, anonymous namespace helpers, `QSet<QString>` for allowlist).

## User Setup Required

None -- no external service configuration. The plan ships against the existing dev rig with `MEFISTO`, `MEFISTOX`, `td/m/anglais` flag, and `td/ma/debuther` already in place.

## Verification Evidence

### Plan's `<verification>` checklist (19 items)

| # | Check | Result |
|---|-------|--------|
| 1 | `grep -c "CALL XVUE_MODULE_INIT( 'ther', 4 )" prpr/ppther.f` | **1** |
| 2 | CALL inside IF(INTERA.GE.1) block, after bilingual LOGO ENDIF | **1** (verified by awk extract; LOGO line 138 < CALL line 142) |
| 3 | `xvue/xvmodi.f` unchanged | empty diff |
| 4 | `xvue/qt/src/xvue_qt_api.cpp` unchanged | empty diff |
| 5 | `xvue/qt/src/xvue_qt_window.cpp` unchanged | empty diff |
| 6 | `bin/cbl_tout_qt` exits 0 | exit 0 (Qt) |
| 7 | `bin/cbl_tout` (X11) exits 0 | exit 0 (X11) |
| 8 | ABI 58/58 | nm count: 58, header count: 58 |
| 9 | `nm pp/ppther_qt \| grep -c ' T registerTherActions_stub_'` | **1** (force-link confirmed) |
| 10 | `nm pp/ppmail_qt \| grep -c ' T registerMailActions_stub_'` | **1** (regression) |
| 11 | `nm pp/ppelas_qt \| grep -c ' T registerElasActions_stub_'` | **1** (regression) |
| 12 | `nm pp/ppflui_qt \| grep -c ' T registerFluiActions_stub_'` | **1** (regression) |
| 13 | `nm pp/ppther \| grep -c 'xvue_module_init_'` | **1** (X11 stub resolves) |
| 14 | `xvue/qt/build/tests/xvue_qt_ther_menu_tests` binary exists | **yes** (1.1M) |
| 15 | `QT_QPA_PLATFORM=offscreen .../xvue_qt_ther_menu_tests` exits 0 | **9 passed, 0 failed, 0 skipped** |
| 16 | testHelpNoQueue uses `{98;}` allowlist NOT `{97;}` | `grep -c 'QStringLiteral("98;")'` = 1; `grep -c 'QStringLiteral("97;")'` = 0 |
| 17 | `add_executable(xvue_qt_ther_menu_tests` in CMake | **1** |
| 18 | `XVUE_QT_TESTING` count in CMake | **4** (mail + elas + flui + ther) |
| 19 | All pre-existing tests still exit 0 | **all 11 binaries** (143/1/2 aggregate; 1 fail pre-existing) |

## Threat-model close

| Threat ID | Disposition | How closed |
|-----------|-------------|------------|
| T-6.4-CALL-PLACEMENT | mitigated | CALL placed AFTER bilingual LOGO ENDIF (THERMICER/HEATER) inside `IF(INTERA.GE.1)`; mirrors 6.2 ppelas.f bilingual placement; verified by `awk` extract showing LOGO line 138 < CALL line 142 < outer ENDIF line 143 |
| T-6.4-REGRESSION | mitigated | Full Qt suite (143/1/2) ran before commit; the 1 fail is the pre-existing `testPerModuleGroupIsolation` from Wave 2's deferred-items.md, unchanged at HEAD |

## Hand-off to Phase 6.5 (nlse)

Phase 6.5 follows the same pattern, alphabetically slotted (elas/flui/mail/**nlse**/ther — nlse goes BETWEEN mail and ther because 'mail' < 'nlse' < 'ther'):

1. **Audit:** mint LEXICON-AUDIT-nlse.md from `td/m/debunlse` (cardinality similar to ther). Identify per-module Help-allowlist code.
2. **Wire:** ship `xvue/qt/src/xvue_qt_nlse_actions.cpp` strong body + N SVG icons + qrc append + keepalive between mail and ther alphabetically.
3. **Hook:** add `CALL XVUE_MODULE_INIT('nlse', 4)` to `prpr/ppnlse.f` (3-line insertion, single atomic commit). Check whether ppnlse.f has bilingual LOGO (most likely yes) and place CALL accordingly.
4. **Tests:** `xvue/qt/tests/test_xvue_qt_nlse_menu.cpp` with 7 slots: testNonlinearSolve / testViewDraw / testFileMSTools / testHelpNoQueue (allowlist drawn from LEXICON-AUDIT-nlse.md row Help-version) / testCloseEventPushes99WhenBlocking / testMenuOrder ({File, Nonlinear, View, Help} or similar) / testBilingualLabelsEnglish.

Plan 03 leaves Plan 6.5 unblocked:
- Pattern proven idempotent across FOUR modules (mail/elas/flui/ther); 6.5 nlse is the FINAL mechanical near-copy.
- Per-module testHelpNoQueue allowlist pattern (lesson re-confirmed at 6.4): consult the audit BEFORE writing the test.
- testMenuOrder + testBilingualLabelsEnglish are mandatory carry-overs (replace human-verify checkpoints).
- xvmodi.f and xvue_qt_window.cpp continue to need ZERO edits.
- Bilingual LOGO placement note: ppnlse.f likely has bilingual LOGO (NLSER/NLSER-EN or similar) — place CALL after the inner ENDIF, before the outer ENDIF, same shape as ppelas/ppther.

**Cross-module pattern confirmation:** "Pattern proven idempotent across FOUR modules (mail->elas->flui->ther); 6.5 nlse is the final mechanical near-copy. xvmodi.f and xvue_qt_window.cpp continue to need ZERO edits."

## Self-Check: PASSED

- **Files:**
  - `prpr/ppther.f`: FOUND (modified, +3 lines verified at lines 140-142)
  - `xvue/qt/tests/test_xvue_qt_ther_menu.cpp`: FOUND (new, 433 lines)
  - `xvue/qt/tests/CMakeLists.txt`: FOUND (modified, +31 lines for ther target)
  - `.planning/phases/06.4-thermal-ther-menu-wiring/06.4-03-SUMMARY.md`: FOUND (this file)
- **Commits:**
  - `3c45508` (Task 1: feat ppther.f CALL XVUE_MODULE_INIT): FOUND in `git log --oneline`
  - `8d05637` (Task 2: test 7 D-13 QTest slots + new CMake target): FOUND
- **Plan checklist (Success Criteria from user prompt):**
  - prpr/ppther.f CALL inserted inside IF(INTERA.GE.1) at correct site: VERIFIED (after bilingual LOGO ENDIF, before outer ENDIF; LOGO line 138 < CALL line 142)
  - Task 1 atomic commit (single file: prpr/ppther.f only): VERIFIED (`3c45508`, single file in diff)
  - Both bin/cbl_tout_qt + bin/cbl_tout exit 0 serial: VERIFIED
  - Task 2 commit covers test file + CMakeLists.txt: VERIFIED (`8d05637`, both files in diff)
  - verify_abi.sh: nm count 58 / header count 58: VERIFIED
  - All 4 module regression guards: VERIFIED (mail/elas/flui/ther = 1 each)
  - xvue/xvmodi.f UNCHANGED: VERIFIED (empty diff)
  - xvue/qt/src/xvue_qt_api.cpp UNCHANGED: VERIFIED (empty diff)
  - xvue/qt/src/xvue_qt_window.cpp UNCHANGED: VERIFIED (empty diff)
  - xvue_qt_ther_menu_tests exits 0: VERIFIED (9 passed, 0 failed, 0 skipped)
  - testMenuOrder PASS: VERIFIED (asserts {File, Thermal, View, Help})
  - testBilingualLabelsEnglish PASS: VERIFIED (worktree has anglais flag + td/ma/debuther; 3;->STEADY/HEAT, 8;->TEMPERATURE/FLUX, 99;->SAVE/QUIT all matched)
  - testHelpNoQueue uses `{98;}` allowlist NOT `{97;}`: VERIFIED (`grep -c 'QStringLiteral("98;")'` = 1; `grep -c 'QStringLiteral("97;")'` = 0)
  - Full Qt test suite green: VERIFIED (143/1/2; 1 fail is pre-existing testPerModuleGroupIsolation per deferred-items.md)
  - 06.4-03-SUMMARY.md created and committed: VERIFIED (this file)

---
*Phase: 06.4-thermal-ther-menu-wiring*
*Plan: 03 (Fortran hook + 7 D-13 QTest slots)*
*Completed: 2026-04-28*
