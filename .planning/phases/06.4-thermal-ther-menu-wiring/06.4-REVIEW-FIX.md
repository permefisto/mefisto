---
phase: 06.4-thermal-ther-menu-wiring
fixed_at: 2026-04-28T00:00:00Z
review_path: .planning/phases/06.4-thermal-ther-menu-wiring/06.4-REVIEW.md
iteration: 1
findings_in_scope: 1
fixed: 1
skipped: 0
status: all_fixed
---

# Phase 06.4: Code Review Fix Report

**Fixed at:** 2026-04-28T00:00:00Z
**Source review:** .planning/phases/06.4-thermal-ther-menu-wiring/06.4-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 1 (Critical + Warning)
- Fixed: 1
- Skipped: 0
- Out-of-scope (Info, not addressed this iteration): 5

## Fixed Issues

### WR-01: `tempgrad` leaves under `View > Draw temperatures + flux` use `7;` prefix instead of `8;`

**Files modified:** `xvue/qt/src/xvue_qt_ther_actions.cpp`
**Commit:** f4cad09
**Applied fix:** Changed the four leaf lexicons in `drawTempMenu` from `7;1;`-`7;4;` to `8;1;`-`8;4;` so that clicks inside the *View > Draw temperatures + flux* submenu dispatch through the temperature TRTHER call (`ppther.f:367`, `NTYPDESS=1`) instead of the eigenvector TRTHER call (`ppther.f:361`, `NTYPDESS=2`). Updated the surrounding comment block to (a) cite the correct entry path, (b) explicitly contrast the two `tempgrad` entry paths (eigenvector vs. temperature, same Fortran routine, different `NTYPDESS` arg), and (c) reference `LEXICON-AUDIT-ther.md` row 215 which picks `7;` as the *canonical synthetic prefix* for tempgrad leaves but does not preclude using `8;` at this specific menu node where the user-facing semantic is temperature drawing.

**Cross-check against LEXICON-AUDIT-ther.md row 215:** The audit note reads "tempgrad reached from ther/trther.f:418 under codes 7; DRAWING EIGEN AND 8; DRAWING TEMP per ther/sdtrth.f:215; canonical parent prefix picked as 7;". The audit explicitly acknowledges both entry paths exist. The canonical-prefix choice is a tabular convenience, not a wiring constraint -- inside the `View > Draw temperatures + flux` submenu the `8;` prefix is the semantically correct dispatch.

**Verification:**
- Tier 1: re-read modified file section, confirmed all four leaves use `8;1;`..`8;4;` and surrounding code intact.
- Tier 2 (rebuild): `bin/cbl_tout_qt` succeeded; `pp/ppther_qt` rebuilt cleanly (no warnings on the patched lines).
- Tier 2 (tests): full Qt test suite preserved baseline at **143 passed / 1 failed / 2 skipped**. The one failure is the pre-existing `testPerModuleGroupIsolation` in `xvue_qt_i18n_menu_prefs_tests`, documented as OK by the orchestrator. The phase-specific binary `xvue_qt_ther_menu_tests` reported **9 passed, 0 failed, 0 skipped** (initTestCase + 7 tests + cleanupTestCase). No regression.

## Skipped Issues

_None._

## Out-of-Scope Findings (Info severity, not addressed this iteration)

The following findings were identified in REVIEW.md but are Info severity and outside this fix iteration's scope (`fix_scope: critical_warning`). They remain open and may be addressed in a follow-up iteration if the orchestrator escalates scope:

- **IN-01:** Stale ABI count comment in `xvue/qt/CMakeLists.txt:95` (says 57, enforcement says 58).
- **IN-02:** Test-file docstring in `xvue/qt/tests/test_xvue_qt_ther_menu.cpp:42-45` references nonexistent `Thermal.HeatSteady` objectName (actual is `Thermal.Steady`).
- **IN-03:** Action-file docstring in `xvue/qt/src/xvue_qt_ther_actions.cpp:18, 339` cites `ppther.f:451` for the dead `THEPOLEVV` call (actual line is `ppther.f:454`).
- **IN-04:** `scheonde.label(2)` not wired in Wave submenu despite menu-file leaf being available.
- **IN-05:** Code `19;` (POLY EIGEN) wired to a runtime no-op without UI affordance (greying-out or "(stub)" suffix).
- **IN-06:** Block comment in `prpr/ppther.f:36` references `ppflui` instead of `ppther` (pre-existing copy-paste, not introduced by Phase 06.4).

---

_Fixed: 2026-04-28T00:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
