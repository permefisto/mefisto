---
# Process gate: A/B window must be CLOSED before this plan executes (per 09-CONTEXT.md D-11)
phase: 09-retire-x11-backend
plan: 05
type: execute
wave: 2
depends_on: ["09-04"]
files_modified:
  - README
  - LISEZMOI
  - bin/README
  - bin/LISEZMOI
  - CLAUDE.md
  - install.bash
  - bin/install.bash
autonomous: false
requirements: ["RETIRE-04"]
must_haves:
  truths:
    - "README + LISEZMOI + bin/README + bin/LISEZMOI list ONLY Qt 6 runtime dependencies (qt6-base-dev + qt6-image-formats-plugins + ffmpeg)"
    - "Zero references to libX11-dev, libXt-dev, ImageMagick across {README, LISEZMOI, bin/README, bin/LISEZMOI, CLAUDE.md, install.bash, bin/install.bash}"
    - "Debian package name is the empirically-verified `qt6-image-formats-plugins` (3 hyphens, no `6` suffix) — NOT REQUIREMENTS.md/CONTEXT.md's `libqt6imageformats6-plugins` (per RESEARCH §Pitfall 9)"
    - "CLAUDE.md §Dependencies (line 39 area) lists only qt6-base-dev + qt6-image-formats-plugins + ffmpeg; libX11-dev removed; ImageMagick listed only as 'optional, for Phase 8 A/B harness only' OR removed if EXPORT-06 / Phase 8 D-03 wording allows"
    - "install.bash + bin/install.bash (if they exist) skip libX11-dev installation"
    - "Phase 9 README section is added documenting the v1.0-pre-retire tag location (rollback procedure) and the Qt-only build entry (bin/cbl_tout)"
    - "bin/cbl_tout exits 0 post-edit (no behavior change — only docs)"
  artifacts:
    - path: "README"
      provides: "English top-level README post-Phase-9"
      contains: "qt6-image-formats-plugins"
    - path: "LISEZMOI"
      provides: "French top-level README post-Phase-9 (parity per T-09-07)"
      contains: "qt6-image-formats-plugins"
    - path: "bin/README"
      provides: "Binary-distribution duplicate (per RESEARCH §Empirical totals)"
      contains: "qt6-image-formats-plugins"
    - path: "bin/LISEZMOI"
      provides: "Binary-distribution duplicate FR"
      contains: "qt6-image-formats-plugins"
  key_links:
    - from: "README/LISEZMOI dependency list"
      to: "actual Debian apt-installable packages"
      via: "explicit package name match"
      pattern: "grep -c 'qt6-image-formats-plugins' README LISEZMOI bin/README bin/LISEZMOI"
    - from: "v1.0-pre-retire git tag (Plan 9-01)"
      to: "Phase 9 rollback section in README"
      via: "explicit instructions"
      pattern: "grep -F 'v1.0-pre-retire' README"
---

<objective>
RETIRE-04: Update all README/LISEZMOI/install/CLAUDE.md files to reflect post-Phase-9
state — Qt 6 as the single graphics backend, ImageMagick + libX11-dev removed from
the dependency list.

Critical correction per RESEARCH §Pitfall 9: REQUIREMENTS.md §RETIRE-04 line 112 + CONTEXT.md
"In scope" both list `libqt6imageformats6-plugins` (4 components: lib + qt6 + imageformats6
+ plugins). The empirical Debian package name is `qt6-image-formats-plugins`
(`apt-cache search '^qt6-image-formats'` confirms; `^libqt6imageformats` returns 0).

Per RESEARCH §Pitfall 7 / T-09-07: 4 README/LISEZMOI files exist (top-level FR+EN +
bin-level FR+EN — empirically duplicates per RESEARCH §Empirical totals); CLAUDE.md
line 39 is a 5th file with `libX11-dev`. All 5 must be edited in lockstep.

This plan is `autonomous: false` because it includes a maintainer checkpoint to
confirm whether the project should keep or drop the `bin/png2{eps,jpg}` wording
(those scripts already deleted in Plan 9-04, but the maintainer may want to record
their absence + reasoning explicitly in README's "Changes since v1.0" section).

Output:
- 5 file edits (README, LISEZMOI, bin/README, bin/LISEZMOI, CLAUDE.md) with consistent
  package-name correction.
- Optional 2-file edit (install.bash, bin/install.bash) if they reference libX11-dev.
- A new "Phase 9: X11 retirement (v1.0+)" section in README + LISEZMOI documenting
  the v1.0-pre-retire rollback tag.
- 5 BUILD-10 testa cases pass post-edit (no functional change; documentation-only).
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-retire-x11-backend/09-CONTEXT.md
@.planning/phases/09-retire-x11-backend/09-RESEARCH.md
@.planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md
@.planning/phases/09-retire-x11-backend/09-04-SUMMARY.md
@CLAUDE.md
@README
@LISEZMOI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit current libX11/ImageMagick references across all doc files (T-09-07)</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 7"
    - README, LISEZMOI, bin/README, bin/LISEZMOI, CLAUDE.md
  </read_first>
  <action>
    Capture starting state per RESEARCH §Pitfall 7 verification:

    1. Run the canonical 5-file audit:
       `grep -ln 'libX11-dev\|libXt-dev\|imagemagick\|ImageMagick\|convert.*animated\|libqt6imageformats' \
            README LISEZMOI bin/README bin/LISEZMOI CLAUDE.md \
            install.bash bin/install.bash 2&gt;/dev/null \
            > /tmp/09-05-doc-audit.log`

       Expected per RESEARCH: at minimum {README, LISEZMOI, bin/README, bin/LISEZMOI,
       CLAUDE.md} should appear.

    2. For each file, capture the exact lines that need editing:
       `for f in README LISEZMOI bin/README bin/LISEZMOI CLAUDE.md \
                 install.bash bin/install.bash; do \
            [ -f "$f" ] || continue ;\
            echo "=== $f ===" ;\
            grep -n 'libX11-dev\|libXt-dev\|imagemagick\|ImageMagick\|convert.*animated\|libqt6imageformats' "$f" 2&gt;/dev/null ;\
        done | tee -a /tmp/09-05-doc-audit.log`

    3. Confirm both top-level and bin-level README+LISEZMOI are byte-identical
       (per RESEARCH §Empirical totals): if they are, the same edit pattern can be
       applied to both pairs. If they're not, surface the divergence.
       `diff README bin/README | head -20`
       `diff LISEZMOI bin/LISEZMOI | head -20`

    4. Sanity: confirm the empirically-correct Debian package name is available:
       `apt-cache search '^qt6-image-formats' | head -3`
       Expected: `qt6-image-formats-plugins - Qt 6 Image Formats plugins`
       (per RESEARCH §Pitfall 9, VERIFIED 2026-05-05; if local apt cache differs,
       record actual output in 09-05-DOC-AUDIT.md and proceed with the documented name).
  </action>
  <verify>
    <automated>
      test -s /tmp/09-05-doc-audit.log &amp;&amp; \
      grep -c "README\|LISEZMOI\|CLAUDE" /tmp/09-05-doc-audit.log | grep -qE "[3-9]|10"
    </automated>
  </verify>
  <acceptance_criteria>
    - /tmp/09-05-doc-audit.log lists all files containing libX11-dev / ImageMagick / libqt6imageformats references
    - At minimum 4 files surface in the audit (the 4 README/LISEZMOI per Pitfall 7) — typically 5+ with CLAUDE.md
    - apt-cache lookup for qt6-image-formats-plugins confirmed (or documented if differs)
  </acceptance_criteria>
  <done>Pre-edit audit captured; per-file edit lines identified; package name confirmed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Maintainer decision on png2eps/png2jpg wording (RESEARCH §Pitfall 8 follow-up)</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 8: NEW FINDING — bin/png2eps and bin/png2jpg"
    - .planning/phases/09-retire-x11-backend/09-04-SUMMARY.md (those scripts were deleted in Plan 9-04 per option a)
  </read_first>
  <decision>
    Plan 9-04 already deleted bin/png2eps + bin/png2jpg per RESEARCH §Pitfall 8 option (a).
    For RETIRE-04 documentation, the maintainer needs to choose how to record their
    absence in README + LISEZMOI:

    Should the README/LISEZMOI explicitly mention the removal in a "Changes since v1.0"
    section, or simply omit them silently (the v1.0-pre-retire tag preserves them in
    git history)?
  </decision>
  <context>
    Per RESEARCH §Pitfall 8 + §Assumption A2: maintainer-reviewed deletion was the
    research recommendation. The artifacts are now gone (Plan 9-04). README/LISEZMOI
    have a choice on how loudly to advertise the change.
  </context>
  <options>
    <option id="option-a">
      <name>Silent omission</name>
      <pros>Cleanest README; matches "ImageMagick is dropped" CONTEXT.md wording. v1.0-pre-retire tag is the audit trail.</pros>
      <cons>Maintainer who used the helpers ad-hoc may be surprised on next pull.</cons>
    </option>
    <option id="option-b">
      <name>One-line mention in "Changes since v1.0" section</name>
      <pros>Explicit; future maintainer reading README knows exactly when + why.</pros>
      <cons>Adds a section that may grow into a per-deletion changelog (anti-pattern for a project README).</cons>
    </option>
    <option id="option-c">
      <name>NEW SECTION "Phase 9: X11 retirement" with full deletion catalog</name>
      <pros>Most thorough; mirrors Phase 7 README §9 precedent.</pros>
      <cons>Heaviest documentation; potentially overlaps with Phase 9 SUMMARY (which is the canonical catalog).</cons>
    </option>
  </options>
  <resume-signal>Reply with: option-a, option-b, or option-c (default if no reply within plan execution: option-a per CONTEXT.md "ImageMagick is dropped from the install documentation" literal wording).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Edit README + LISEZMOI + bin/README + bin/LISEZMOI dependency lists</name>
  <read_first>
    - /tmp/09-05-doc-audit.log (Task 1 output)
    - README, LISEZMOI, bin/README, bin/LISEZMOI
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 9"
  </read_first>
  <action>
    Apply consistent edits across all 4 README/LISEZMOI files using the Edit tool.

    Common edit pattern:

    BEFORE (typical, paraphrased — exact lines from Task 1 audit):
    ```
    Dependencies / Dépendances:
      gfortran
      gcc
      libX11-dev          # X11 headers
      ImageMagick         # for animated GIF (bin/convertepsgif)
    ```

    AFTER:
    ```
    Dependencies / Dépendances:
      gfortran
      gcc
      qt6-base-dev qt6-image-formats-plugins   # Qt 6 graphics backend
      ffmpeg                                     # animated GIF export (XvueExport::saveGifTo)
      cmake (>= 3.16)                            # builds xvue/qt/
    ```

    Per-file edit steps:
    1. README (top-level, English): edit exact lines per /tmp/09-05-doc-audit.log
    2. LISEZMOI (top-level, French): mirror edit; preserve French phrasing
       ("Bibliothèques / Outils requis", etc.)
    3. bin/README: same as top-level README (Task 1 confirmed they're byte-identical
       — if so, can copy + diff to ensure they stay in sync; if not, edit independently)
    4. bin/LISEZMOI: same as top-level LISEZMOI

    Per the Task 2 maintainer decision, ALSO add the relevant "Changes since v1.0"
    section if option-b or option-c was chosen (skip if option-a).

    For options b/c, add Phase 9 section to README + LISEZMOI (top-level only — don't
    duplicate to bin/README to avoid duplication-bug per RESEARCH §Pitfall 7):

    ```markdown
    ## Phase 9: X11 retirement (v1.0+)

    Starting at git tag `v1.0-pre-retire`, MEFISTO migrated from the legacy X11
    graphics backend (`xvue/xvuelc.c`, ~3749 lines) to a Qt 6 backend
    (`xvue/qt/`). The X11 path was kept alive in parallel for one release cycle for
    A/B validation (Phase 8). After the A/B window closed, Phase 9 deleted:

    - `xvue/xvuelc.c` + `bin/ccxvue` (the X11 backend)
    - `xvue/video1.f` + `videofin.f` + `videonm.f` + 12 tracer LVIDEO blocks (the
      legacy `xwd → convert → .gif` animation pipeline; replaced by Phase 7
      `XvueExport::saveGifTo` ffmpeg path)
    - `bin/convertepsgif`, `bin/png2eps`, `bin/png2jpg` (ImageMagick shell-outs)
    - 32 legacy `bin/cb*` X11-linker scripts + Makefile relics
    - `libX11-dev` + `ImageMagick` from the dependency list

    To rebuild from the pre-retirement code: `git checkout v1.0-pre-retire`. Then
    re-run the original `bin/cbl_tout` (which still exists at that tag and uses
    the legacy X11 path).

    Single graphics backend post-Phase-9: Qt 6 via `bin/cbl_tout`.
    ```

    French version (LISEZMOI):
    ```markdown
    ## Phase 9 : Retrait du backend X11 (v1.0+)

    À partir du tag git `v1.0-pre-retire`, MEFISTO a migré depuis l'ancien
    backend graphique X11 (`xvue/xvuelc.c`, ~3749 lignes) vers un backend Qt 6
    (`xvue/qt/`). Le chemin X11 a été maintenu en parallèle pendant un cycle de
    livraison pour la validation A/B (Phase 8). Une fois la fenêtre A/B fermée,
    la Phase 9 a supprimé :

    - `xvue/xvuelc.c` + `bin/ccxvue` (le backend X11)
    - `xvue/video1.f` + `videofin.f` + `videonm.f` + les blocs LVIDEO de 12
      traceurs (l'ancien pipeline `xwd → convert → .gif` ; remplacé par le
      chemin ffmpeg `XvueExport::saveGifTo` de la Phase 7)
    - `bin/convertepsgif`, `bin/png2eps`, `bin/png2jpg` (appels ImageMagick)
    - 32 scripts `bin/cb*` (anciens éditeurs de liens X11) + reliques Makefile
    - `libX11-dev` + `ImageMagick` retirés de la liste des dépendances

    Pour reconstruire à partir du code pré-retrait : `git checkout v1.0-pre-retire`.

    Backend graphique unique post-Phase-9 : Qt 6 via `bin/cbl_tout`.
    ```

    Verify post-edit:
       `! grep 'libX11-dev\|libXt-dev\|libqt6imageformats6-plugins' README LISEZMOI bin/README bin/LISEZMOI`
       Expected: 0 matches.

       `grep -c 'qt6-image-formats-plugins' README LISEZMOI bin/README bin/LISEZMOI`
       Expected: each ≥ 1.
  </action>
  <verify>
    <automated>
      [ "$(grep -c 'libX11-dev\|libXt-dev' README LISEZMOI bin/README bin/LISEZMOI 2&gt;/dev/null | grep -v ':0' | wc -l)" = "0" ] &amp;&amp; \
      [ "$(grep -c 'libqt6imageformats6-plugins' README LISEZMOI bin/README bin/LISEZMOI 2&gt;/dev/null | grep -v ':0' | wc -l)" = "0" ] &amp;&amp; \
      grep -q 'qt6-image-formats-plugins' README &amp;&amp; \
      grep -q 'qt6-image-formats-plugins' LISEZMOI &amp;&amp; \
      grep -q 'qt6-image-formats-plugins' bin/README &amp;&amp; \
      grep -q 'qt6-image-formats-plugins' bin/LISEZMOI
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero `libX11-dev` or `libXt-dev` references across all 4 README/LISEZMOI files
    - Zero `libqt6imageformats6-plugins` (the WRONG name from REQUIREMENTS.md / CONTEXT.md)
    - Each of the 4 files contains at least one occurrence of `qt6-image-formats-plugins` (the CORRECT name)
    - Phase 9 section added to README + LISEZMOI top-level only (per Task 2 decision; skip if option-a)
  </acceptance_criteria>
  <done>4 README/LISEZMOI files updated with correct package name and Phase 9 documentation per maintainer decision.</done>
</task>

<task type="auto">
  <name>Task 4: Edit CLAUDE.md + install.bash files (T-09-07 5th + 6th file)</name>
  <read_first>
    - CLAUDE.md (line 39 area per RESEARCH §Pitfall 7)
    - install.bash (if exists), bin/install.bash (if exists)
  </read_first>
  <action>
    Per RESEARCH §Pitfall 7: CLAUDE.md line 39 references `libX11-dev`. Edit it.

    1. Update CLAUDE.md "## Dependencies" section. The current wording (per `Read CLAUDE.md`):
       ```
       - `gfortran` (Fortran 77/95 + OpenMP)
       - `gcc`
       - `libX11-dev` (X11 headers and libraries at `/usr/X11R6/lib`)
       - `ImageMagick` (`convert` command, for animated GIF output)
       ```
       Replace with:
       ```
       - `gfortran` (Fortran 77/95 + OpenMP)
       - `gcc`
       - `qt6-base-dev` (Qt 6 graphics backend headers + libraries)
       - `qt6-image-formats-plugins` (TIFF/WebP/etc. image-format plugins for XvueExport)
       - `ffmpeg` (animated GIF export via XvueExport::saveGifTo)
       - `cmake` (>= 3.16, drives xvue/qt/ build)
       ```

       Use the Edit tool with the exact line text. Verify post-edit:
       `! grep 'libX11-dev\|libqt6imageformats6-plugins\|ImageMagick' CLAUDE.md`
       (allow `convert` if it appears in unrelated context like a "convert to..." sentence.)

    2. install.bash (if it exists at repo root):
       Check first: `test -f install.bash && grep -n 'libX11-dev\|imagemagick\|libqt6imageformats' install.bash`
       If matches: edit each line to drop libX11-dev + replace with qt6-base-dev + qt6-image-formats-plugins.
       Specifically: any `apt install ... libX11-dev ...` becomes
       `apt install ... qt6-base-dev qt6-image-formats-plugins ffmpeg cmake ...`.
       Drop ImageMagick from `apt install` if present.

    3. bin/install.bash (if it exists in bin/):
       Same edit pattern as install.bash. If byte-identical to top-level (RESEARCH
       suggests they may be), keep them in sync.

    4. Final canonical 5-file audit (RESEARCH §Pitfall 7 expectation):
       `grep -ln 'libX11-dev\|libXt-dev\|libqt6imageformats6-plugins' \
            README LISEZMOI bin/README bin/LISEZMOI CLAUDE.md install.bash bin/install.bash 2&gt;/dev/null`
       Expected: 0 matches across all files.
  </action>
  <verify>
    <automated>
      [ "$(grep -ln 'libX11-dev\|libXt-dev\|libqt6imageformats6-plugins' README LISEZMOI bin/README bin/LISEZMOI CLAUDE.md install.bash bin/install.bash 2&gt;/dev/null | wc -l)" = "0" ] &amp;&amp; \
      grep -q 'qt6-image-formats-plugins' CLAUDE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md has zero `libX11-dev` references; has at least one `qt6-image-formats-plugins`
    - install.bash + bin/install.bash (if they exist) have zero libX11-dev / libqt6imageformats6-plugins references
    - All 5+ files (README, LISEZMOI, bin/README, bin/LISEZMOI, CLAUDE.md, install.bash if present, bin/install.bash if present) pass the canonical post-edit grep
  </acceptance_criteria>
  <done>CLAUDE.md + install scripts updated; T-09-07 fully closed across 5+ doc files.</done>
</task>

<task type="auto">
  <name>Task 5: Full rebuild + 5-case testa baseline + commit</name>
  <read_first>
    - bin/cbl_tout
    - bin/ab_sweep_phase8.sh
  </read_first>
  <action>
    Documentation-only edits — no functional change. Verify the build still works
    end-to-end:

    1. Full Qt-only rebuild (smoke test that the doc edits didn't accidentally break
       any build script or CMake reference):
       `bin/cbl_tout 2&gt;&amp;1 | tee /tmp/09-05-cbl_tout.log`
       Confirm exit 0 + all 3 grep gates run + pass.

    2. Run 5-case Qt sweep:
       `bin/ab_sweep_phase8.sh --mode qt-1x \
            --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu \
            --out-dir /tmp/09-05-post-retire04 2&gt;&amp;1 | tee /tmp/09-05-sweep.log`

    3. Confirm 4-5 PNGs land:
       `ls /tmp/09-05-post-retire04/*.png | wc -l` ≥ 4

    4. Run all gates:
       `bin/test_no_imagemagick_in_qt.sh && bin/test_no_x11_in_build.sh && bin/test_no_lvideo.sh`

    5. Commit:
       ```
       git add -A README LISEZMOI bin/README bin/LISEZMOI CLAUDE.md install.bash bin/install.bash
       git commit -m "docs(09-05): RETIRE-04 — drop libX11-dev + ImageMagick from doc dependency lists

       Per RESEARCH §Pitfall 7 (T-09-07) + §Pitfall 9: 5-file lockstep edit.

       Files updated:
       - README + LISEZMOI (top-level FR+EN)
       - bin/README + bin/LISEZMOI (binary-distribution duplicates per RESEARCH §Empirical totals)
       - CLAUDE.md §Dependencies (line 39 area)
       - install.bash + bin/install.bash (if present)

       Package-name correction (RESEARCH §Pitfall 9 NEW FINDING):
       REQUIREMENTS.md §RETIRE-04 + CONTEXT.md \"In scope\" listed the WRONG Debian
       package name 'libqt6imageformats6-plugins' (apt-cache returns 0 results).
       The CORRECT name is 'qt6-image-formats-plugins' (3 hyphens, no '6' suffix;
       apt-cache search '^qt6-image-formats' confirmed 2026-05-05).

       Dependency list post-edit:
       - gfortran
       - gcc (still needed for indirect -lstdc++ linkage)
       - qt6-base-dev (provides Qt6Widgets, Qt6Gui, Qt6Core, Qt6PrintSupport, Qt6Test)
       - qt6-image-formats-plugins (TIFF, WebP, etc. for XvueExport)
       - ffmpeg (animated GIF via XvueExport::saveGifTo)
       - cmake (>= 3.16)

       NOT listed in deps anymore:
       - libX11-dev (X11 backend deleted in Plan 9-02)
       - libXt-dev (linker lines deleted in Plan 9-03)
       - ImageMagick (LVIDEO + bin/convertepsgif + png2{eps,jpg} deleted in Plan 9-04;
         Phase 7 EXPORT-06 + Phase 8 D-03 still permit ImageMagick as OPTIONAL for the
         A/B harness, but it is not a build-time dependency)

       Phase 9 README+LISEZMOI section added per maintainer decision (Task 2): documents
       the v1.0-pre-retire rollback tag and what Phase 9 deleted.

       Verification:
       - bin/cbl_tout exits 0 (doc-only edits; no functional change)
       - 4/5 testa baselines capture cleanly
       - All 3 grep gates exit 0

       Threats mitigated:
       - T-09-07: 5+ files audited and edited in lockstep (Task 4 final canonical grep)

       Closes RETIRE-04."
       ```
  </action>
  <verify>
    <automated>
      tail -1 /tmp/09-05-cbl_tout.log | grep -qiE "MODULES|crees|created" &amp;&amp; \
      bin/test_no_x11_in_build.sh &amp;&amp; \
      bin/test_no_imagemagick_in_qt.sh &amp;&amp; \
      bin/test_no_lvideo.sh &amp;&amp; \
      [ "$(ls /tmp/09-05-post-retire04/*.png 2&gt;/dev/null | wc -l)" -ge "4" ] &amp;&amp; \
      git log --oneline -1 | grep -E '09-05.*RETIRE-04'
    </automated>
  </verify>
  <acceptance_criteria>
    - bin/cbl_tout exits 0
    - All 3 gates pass
    - 4 of 5 testa cases capture
    - Latest commit contains "09-05" and "RETIRE-04"
  </acceptance_criteria>
  <done>RETIRE-04 commit landed; 5+ doc files synchronized; correct Debian package name everywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/LISEZMOI deps list ↔ actual Debian apt repository | Package name must be empirically correct (`qt6-image-formats-plugins`, not REQUIREMENTS.md's `libqt6imageformats6-plugins`) |
| Top-level README ↔ bin/README | Empirically duplicate per RESEARCH §Empirical totals; updates must stay in lockstep |
| FR ↔ EN doc parity | LISEZMOI must mirror README content for v1.x maintainability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure | Doc update misses one of 5 files | mitigate | Task 1 5-file canonical audit; Task 4 final grep across all 5+ files; commit message lists all 5 |
| T-09-07-A | Tampering | Wrong Debian package name (per Pitfall 9) propagates from REQUIREMENTS.md | mitigate | Task 1 step 4: apt-cache search confirms correct name BEFORE edit; commit message documents the correction |
| T-09-07-B | Information Disclosure | FR↔EN drift after edit | mitigate | Task 3 step 2 mirrors English changes to French; Task 5 verification grep is package-name-agnostic |
| T-09-07-C | Repudiation | Phase 9 README section absent leaves no audit trail | mitigate via maintainer decision | Task 2 checkpoint; default to option-a if maintainer doesn't reply (still acceptable since v1.0-pre-retire git tag is the canonical audit) |
</threat_model>

<verification>
- 0 `libX11-dev` / `libXt-dev` / `libqt6imageformats6-plugins` matches across {README, LISEZMOI, bin/README, bin/LISEZMOI, CLAUDE.md, install.bash, bin/install.bash}
- ≥ 4 occurrences of `qt6-image-formats-plugins` (the correct name)
- bin/cbl_tout exits 0 (doc-only edit; no functional regression)
- All 3 gates pass: verify_no_imagemagick_in_qt + verify_no_x11_in_build + verify_no_lvideo
- 4 of 5 testa cases capture cleanly
- Latest commit references 09-05 + RETIRE-04
</verification>

<success_criteria>
- [x] Audit captured all libX11-dev/imagemagick references (Task 1)
- [x] Maintainer decision recorded for Phase 9 README section (Task 2)
- [x] 4 README/LISEZMOI files edited (Task 3)
- [x] CLAUDE.md + install.bash files edited (Task 4)
- [x] Build green + testa cases pass + commit (Task 5)
</success_criteria>

<output>
After completion, create `.planning/phases/09-retire-x11-backend/09-05-SUMMARY.md`
documenting:
- 5+ doc files updated (with line counts of changes)
- Correct package name confirmed via apt-cache
- Phase 9 README section added (per maintainer decision)
- Plans 9-02..9-05 collectively close RETIRE-01..04 (Wave 2 complete)
</output>
