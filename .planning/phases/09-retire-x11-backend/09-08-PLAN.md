---
# Process gate: A/B window must be CLOSED before this plan executes (per 09-CONTEXT.md D-11)
phase: 09-retire-x11-backend
plan: 08
type: execute
wave: 3
depends_on: ["09-05"]
files_modified:
  - xvue/qt/tests/golden/scene01.eps
  - xvue/qt/tests/golden/wave_legacy.gif
  - xvue/qt/tests/golden/cavity2d_legacy.gif
  - .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md
autonomous: true
requirements: []  # Defect carry-forward from Phase 7 §9 (3 deferred goldens) + Phase 8 override #4
must_haves:
  truths:
    - "3 golden artifacts exist in main worktree under xvue/qt/tests/golden/: scene01.eps + wave_legacy.gif + cavity2d_legacy.gif"
    - "scene01.eps was produced by compiling xvue/qt/tests/golden/scene01_driver.f against the X11 backend (xvuelc.o + libX11) inside the v1.0-pre-retire worktree (per Phase 7 VERIFICATION.md §9.1 procedure)"
    - "wave_legacy.gif was produced by running testa/wave through the X11 backend + bin/convertepsgif inside the v1.0-pre-retire worktree (per Phase 7 VERIFICATION.md §9.2)"
    - "cavity2d_legacy.gif was produced by running testa/cavity2d through the X11 backend + bin/convertepsgif inside the v1.0-pre-retire worktree (per Phase 7 VERIFICATION.md §9.3)"
    - "After commit, ctest -R '^xvue_qt_(postscript|export)_tests$' run under xvfb-run shows 0 SKIP (postscript_tests = 19 PASS 0 SKIP; export_tests = 18 PASS 0 SKIP)"
    - "Phase 7 VALIDATION-LOG.md updated: 3 DEFERRED rows replaced with PASS rows + ISO date + verdict"
    - "/tmp/mefisto-pre-retire worktree CLEANED UP (git worktree list shows no stale entry); main worktree git status clean (no leaked .o files, no leaked pp/* binaries — per RESEARCH §Pitfall 3 / T-09-03)"
    - "bin/cbl_tout in main exits 0 post-commit (Phase 9 invariant)"
  artifacts:
    - path: "xvue/qt/tests/golden/scene01.eps"
      provides: "PostScript byte-parity golden produced from X11+xvuelc baseline"
      min_lines: 100  # EPS file with handleLasops opcodes; expect ~120-200 lines depending on PS verbosity
    - path: "xvue/qt/tests/golden/wave_legacy.gif"
      provides: "Animated GIF baseline from testa/wave under X11+convertepsgif"
    - path: "xvue/qt/tests/golden/cavity2d_legacy.gif"
      provides: "Animated GIF baseline from testa/cavity2d under X11+convertepsgif"
    - path: ".planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md"
      provides: "Phase 7 sign-off ledger updated DEFERRED → PASS for the 3 rows"
      contains: "PASS"
  key_links:
    - from: "v1.0-pre-retire git tag (Plan 9-01)"
      to: "/tmp/mefisto-pre-retire worktree"
      via: "git worktree add /tmp/mefisto-pre-retire v1.0-pre-retire"
      pattern: "git worktree list"
    - from: "Cross-tag worktree X11 build"
      to: "main xvue/qt/tests/golden/"
      via: "cp from /tmp/mefisto-pre-retire artifacts to main repo"
      pattern: "ls xvue/qt/tests/golden/{scene01.eps,wave_legacy.gif,cavity2d_legacy.gif}"
    - from: "ctest QSKIP slots in test_xvue_qt_postscript.cpp + test_xvue_qt_export.cpp"
      to: "PASS-required state"
      via: "presence of golden files + xvfb-run ctest"
      pattern: "ctest --output-on-failure -R '^xvue_qt_(postscript|export)_tests$'"
---

<objective>
Phase 8 carry-forward #3 (Phase 7 hand-off carry-forward): produce 3 deferred goldens
under post-X11-retirement conditions via cross-tag worktree.

Phase 7 VERIFICATION.md §9 documented 3 human-bootstrap items: scene01.eps +
wave_legacy.gif + cavity2d_legacy.gif. Phase 8 Plan 1 Task 2 attempted autonomous
bootstrap and failed because (i) scene01_driver.f link line requires the full
$MEFISTO/xvue/*.o + $MEFISTO/util/*.o + libX11 + libXt chain that Plan 8 Task 1
could not assemble headlessly, and (ii) testa/wave + testa/cavity2d are multi-module
legacy AUTOEXIT chains needing human-issued `99;` saves between MAILLER and the
solver step. Phase 8 override #4 accepted "Phase-7-carry-forward to Phase 9".

X11 backend is GONE from main after RETIRE-01..04 land (Wave 2 plans 9-02..9-05).
The only way to produce these legacy goldens is to check out the v1.0-pre-retire
git tag (created by Plan 9-01) in a SEPARATE worktree. Per RESEARCH §Pattern 3 +
§Pitfall 3 / T-09-03: the worktree MUST live under /tmp/ (NOT inside $MEFISTO),
artifacts get COPIED back to main, then the worktree is REMOVED. No build artifacts
leak into main.

Phase 7 VERIFICATION.md §9.1, §9.2, §9.3 contain the canonical bootstrap procedures.
The link command for scene01_driver is documented at xvue/qt/tests/golden/scene01_driver.f
header lines 36-43. The testa/wave + testa/cavity2d procedure uses Phase 7's
existing bin/convertepsgif (which ships in the pre-retire tree).

Output:
- 3 golden files committed in main: xvue/qt/tests/golden/{scene01.eps,
  wave_legacy.gif, cavity2d_legacy.gif}
- Phase 7 ctest QSKIPs flip from 3 to 0 (postscript_tests = 19 PASS 0 SKIP;
  export_tests = 18 PASS 0 SKIP)
- Phase 7 VALIDATION-LOG.md table: 3 DEFERRED rows → PASS rows
- /tmp/mefisto-pre-retire worktree REMOVED (no stale state; T-09-03 mitigation)
- bin/cbl_tout in main exits 0 (no source modifications outside golden + log)
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-retire-x11-backend/09-CONTEXT.md
@.planning/phases/09-retire-x11-backend/09-RESEARCH.md
@.planning/phases/09-retire-x11-backend/09-01-SUMMARY.md
@.planning/phases/07-image-gif-and-postscript-export/VERIFICATION.md
@.planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md
@xvue/qt/tests/golden/scene01_driver.f
@CLAUDE.md

<interfaces>
<!-- scene01_driver.f BOOTSTRAP NOTE (lines 36-43) — canonical link command:
       $ cd /tmp
       $ cp $MEFISTO/xvue/qt/tests/golden/scene01_driver.f .
       $ gfortran -I$MEFISTO/incl -c scene01_driver.f
       $ gfortran scene01_driver.o $MEFISTO/xvue/xvuelc.o \
             $MEFISTO/xvue/*.o $MEFISTO/util/*.o \
             -L/usr/X11R6/lib -lX11 -lXt -o scene01_x11
       $ ./scene01_x11        # produces TEMPORAIRE.EPS in cwd
       $ cp TEMPORAIRE.EPS $MEFISTO/xvue/qt/tests/golden/scene01.eps

     Phase 7 VERIFICATION.md §9.1 example uses the abbreviated form:
       gfortran scene01_driver.f xvue/xvuelc.o -lX11 -lXt -o scene01_driver
     This abbreviated form is INSUFFICIENT (Phase 8 Plan 1 Task 2 confirmed):
     all of $MEFISTO/xvue/*.o + $MEFISTO/util/*.o are needed transitively.
     Use the canonical scene01_driver.f header form.

     For Plan 9-08, $MEFISTO must be set to /tmp/mefisto-pre-retire (NOT
     /home/mefisto/git/mefisto) for the duration of the worktree session, then
     restored. Per RESEARCH §Pitfall 3 / T-09-03 mitigation steps 1-4.

     Phase 7 VERIFICATION.md §9.2 + §9.3 testa/{wave,cavity2d} procedure (legacy
     AUTOEXIT chain): each case requires running MAILLER first to produce the
     mesh, then the appropriate solver (FLUIDER for wave, FLUIDER for cavity2d
     per existing testa/*/CHEMIN+ files). The legacy convertepsgif chain converts
     the resulting .eps frames into a .gif. Multi-step but mechanically scriptable
     in the pre-retire worktree where MAILLER + solver + convertepsgif still work.

     ctest expectations post-commit:
       postscript_tests: 18 PASS + 1 SKIP → 19 PASS + 0 SKIP
       export_tests:     16 PASS + 2 SKIP → 18 PASS + 0 SKIP
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-tag worktree at /tmp/mefisto-pre-retire + verify pre-retirement state</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-01-SUMMARY.md (v1.0-pre-retire tag SHA)
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pattern 3: Cross-Tag Worktree for Legacy Asset Production"
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 3" (T-09-03 mitigation)
  </read_first>
  <action>
    Per RESEARCH §Pattern 3: create a worktree from the v1.0-pre-retire tag in a
    NON-$MEFISTO directory. /tmp/ is the canonical choice (CLAUDE.md does not
    proscribe it; tmp is auto-cleaned).

    1. Verify the v1.0-pre-retire tag exists (Plan 9-01 prerequisite):
       ```
       git tag --list v1.0-pre-retire | grep -q '^v1.0-pre-retire$' || \
         { echo "ERROR: v1.0-pre-retire tag missing — Plan 9-01 must run first" >&2; exit 1; }
       git rev-parse v1.0-pre-retire | tee /tmp/09-08-pre-retire-sha.txt
       ```

    2. Idempotency: if the worktree already exists from a prior partial run,
       remove it first (DO NOT use --force unless verifying clean state):
       ```
       if git worktree list | grep -q '/tmp/mefisto-pre-retire'; then
           git worktree remove /tmp/mefisto-pre-retire
       fi
       rm -rf /tmp/mefisto-pre-retire   # belt-and-suspenders for orphan dir
       ```

    3. Create the worktree at the tag (detached HEAD is correct here):
       ```
       git worktree add --detach /tmp/mefisto-pre-retire v1.0-pre-retire
       ```

    4. Verify the worktree is at the pre-retirement state (X11 backend present):
       ```
       cd /tmp/mefisto-pre-retire
       test -f xvue/xvuelc.c   || { echo "ERROR: xvuelc.c missing in pre-retire worktree" >&2; exit 1; }
       test -f bin/convertepsgif || { echo "ERROR: convertepsgif missing in pre-retire worktree" >&2; exit 1; }
       grep -q 'lX11' bin/cbxvue 2>/dev/null || grep -lq 'lX11' bin/cb* 2>/dev/null || \
           { echo "ERROR: no -lX11 references in pre-retire bin/cb*" >&2; exit 1; }
       cd -   # back to main worktree
       ```

    5. Build the X11 backend INSIDE the pre-retire worktree (this populates the
       .o files we need for the scene01_driver link line). Set MEFISTO to the
       worktree path for the duration of the build, then RESTORE in the same
       shell session — DO NOT leak the env var:
       ```
       (
           cd /tmp/mefisto-pre-retire
           export MEFISTO=/tmp/mefisto-pre-retire
           export MEFISTOX=/tmp/mefistox-pre-retire   # isolated user-project dir
           mkdir -p $MEFISTOX
           bin/cbl_tout 2&gt;&amp;1 | tee /tmp/09-08-pre-retire-cbl_tout.log | tail -10
           # Confirm xvuelc.o + xvue/*.o + util/*.o exist:
           ls $MEFISTO/xvue/xvuelc.o
           ls $MEFISTO/xvue/*.o | head -5
           ls $MEFISTO/util/*.o | head -5
           ls $MEFISTO/pp/pp{init,mail,flui,elas,ther,nlse}
       )
       ```
       Subshell ensures MEFISTO/MEFISTOX env vars don't leak back to main shell.

    6. Verify main worktree is unaffected (T-09-03 invariant):
       ```
       git status --porcelain | head
       # Should be clean (no new files in main from the pre-retire build)
       ```
  </action>
  <verify>
    <automated>
      git worktree list | grep -q '/tmp/mefisto-pre-retire' &amp;&amp; \
      test -f /tmp/mefisto-pre-retire/xvue/xvuelc.c &amp;&amp; \
      test -f /tmp/mefisto-pre-retire/xvue/xvuelc.o &amp;&amp; \
      test -f /tmp/mefisto-pre-retire/bin/convertepsgif &amp;&amp; \
      ls /tmp/mefisto-pre-retire/pp/pp{init,mail,flui} &amp;&amp; \
      [ -z "$(git -C /home/mefisto/git/mefisto status --porcelain | grep -v '^??')" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Worktree at /tmp/mefisto-pre-retire exists, detached at v1.0-pre-retire tag
    - xvue/xvuelc.c + xvue/xvuelc.o + bin/convertepsgif present (X11 backend functional)
    - pp/pp{init,mail,flui,elas,ther,nlse} executables built (X11 link succeeded)
    - Main worktree git status clean (no leakage)
  </acceptance_criteria>
  <done>Cross-tag worktree built; pre-retire X11 binaries available for golden production.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap scene01.eps via X11 backend in pre-retire worktree (Phase 7 §9.1)</name>
  <read_first>
    - xvue/qt/tests/golden/scene01_driver.f (BOOTSTRAP NOTE lines 33-46 — canonical link command)
    - .planning/phases/07-image-gif-and-postscript-export/VERIFICATION.md §9.1
  </read_first>
  <action>
    Compile + link + run scene01_driver.f against the pre-retire X11 backend
    inside the worktree. Output: TEMPORAIRE.EPS, copied to main.

    Use the FULL link line from scene01_driver.f header lines 38-40 (NOT the
    abbreviated VERIFICATION.md form — Phase 8 Plan 1 Task 2 proved the
    abbreviated form is incomplete).

    1. Inside a subshell to isolate env:
       ```
       (
           set -e
           cd /tmp/mefisto-pre-retire
           export MEFISTO=/tmp/mefisto-pre-retire
           export MEFISTOX=/tmp/mefistox-pre-retire

           # Working dir for the bootstrap (NOT $MEFISTO/xvue/qt/tests/golden — keep .o files out):
           mkdir -p /tmp/09-08-scene01
           cd /tmp/09-08-scene01
           cp $MEFISTO/xvue/qt/tests/golden/scene01_driver.f .

           # Step 1: compile
           gfortran -I$MEFISTO/incl -c scene01_driver.f

           # Step 2: link (canonical line from scene01_driver.f:38-40)
           gfortran scene01_driver.o \
                $MEFISTO/xvue/xvuelc.o \
                $MEFISTO/xvue/*.o \
                $MEFISTO/util/*.o \
                -L/usr/X11R6/lib -lX11 -lXt \
                -o scene01_x11 2&gt;&amp;1 | tee /tmp/09-08-scene01-link.log

           # Step 3: run under Xvfb (per Phase 7 VERIFICATION.md §9.1)
           xvfb-run --auto-servernum ./scene01_x11

           # Step 4: verify TEMPORAIRE.EPS produced
           test -f TEMPORAIRE.EPS &amp;&amp; \
               echo "TEMPORAIRE.EPS produced: $(wc -c &lt; TEMPORAIRE.EPS) bytes"

           # Step 5: copy to main worktree
           cp TEMPORAIRE.EPS /home/mefisto/git/mefisto/xvue/qt/tests/golden/scene01.eps
       )
       ```

       NOTE: If the link line fails (Phase 8 Plan 1 Task 2 noted "scene01_driver.f
       link cannot be solved without invasive Fortran build-infra modification"),
       inspect the linker error and add the missing .o file(s) explicitly. The
       pre-retire worktree HAS the full Fortran build chain — the link MUST
       succeed here, where Phase 8's Qt-only main could not.

       If link still fails, fall back to a less-aggressive globbing of the
       Fortran modules. Try in order:
       - `$MEFISTO/xvue/*.o $MEFISTO/util/*.o` (canonical form per scene01_driver.f)
       - `$MEFISTO/xvue/*.o $MEFISTO/util/*.o $MEFISTO/incl/*.o` (if incl has compiled artifacts)
       - `find $MEFISTO -name '*.o' -not -path '*/xvue/qt/*' | xargs ... ` (broadest)

       Document any deviation in 09-08-DEVIATIONS.md (created at end of Task 4).

    2. Sanity-check the produced EPS:
       ```
       cd /home/mefisto/git/mefisto
       wc -l xvue/qt/tests/golden/scene01.eps   # Expect 100+ lines
       head -3 xvue/qt/tests/golden/scene01.eps  # Expect "%!PS-Adobe-..." header
       grep -E '^(li|ep|el|tr|fa|bo|re)' xvue/qt/tests/golden/scene01.eps | head -10
           # Expect handleLasops opcodes per scene01_driver.f comments (lines 84-117)
       ```

       Expected opcodes (per scene01_driver.f primitive sequence): epais, typet,
       traits (li), traits/facetraits (tr), bord/rectangle (re), arcellipse (el).
  </action>
  <verify>
    <automated>
      test -f xvue/qt/tests/golden/scene01.eps &amp;&amp; \
      [ "$(wc -c &lt; xvue/qt/tests/golden/scene01.eps)" -ge 200 ] &amp;&amp; \
      head -1 xvue/qt/tests/golden/scene01.eps | grep -q 'PS-Adobe' &amp;&amp; \
      [ -z "$(git -C /home/mefisto/git/mefisto status --porcelain xvue/qt/tests/golden/scene01.eps | grep -v '^??\|^A ')" ] -o \
        "$(git -C /home/mefisto/git/mefisto status --porcelain xvue/qt/tests/golden/scene01.eps)" != ""
    </automated>
  </verify>
  <acceptance_criteria>
    - xvue/qt/tests/golden/scene01.eps exists in main worktree
    - File starts with "%!PS-Adobe-..." (valid PostScript header)
    - File contains expected handleLasops opcodes (epais, traits, etc.)
    - Link succeeded inside the pre-retire worktree (no leakage to main)
  </acceptance_criteria>
  <done>scene01.eps materialized from X11 baseline; awaiting commit (Task 4).</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap wave_legacy.gif + cavity2d_legacy.gif via testa cases + bin/convertepsgif</name>
  <read_first>
    - .planning/phases/07-image-gif-and-postscript-export/VERIFICATION.md §9.2 + §9.3
    - .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md §"Procedure to elevate DEFERRED → PASS" steps 3
    - bin/convertepsgif (in pre-retire worktree)
    - testa/wave + testa/cavity2d (CHEMIN+ entrypoints)
  </read_first>
  <action>
    Run testa/wave and testa/cavity2d through the pre-retire X11 backend +
    bin/convertepsgif to produce the two animated GIFs. Phase 7 VERIFICATION.md
    §9.2 / §9.3 documents the procedure.

    Multi-module legacy AUTOEXIT chains: each testa case requires MAILLER
    (mesh generation) followed by FLUIDER (the case is fluid for both wave and
    cavity2d). The launchers in bin/ (MAILLER, FLUIDER) wrap the pp/* binaries
    + handle the AUTOEXIT macros that drive the case. The pre-retire worktree
    has these intact.

    1. Set up the user-project workspace and run testa/wave:
       ```
       (
           set -e
           cd /tmp/mefisto-pre-retire
           export MEFISTO=/tmp/mefisto-pre-retire
           export MEFISTOX=/tmp/mefistox-pre-retire
           export PATH=.:$PATH:$MEFISTO/bin
           export CDPATH=.:$HOME:$MEFISTO:$MEFISTOX
           mkdir -p $MEFISTOX/wave
           cd $MEFISTOX/wave

           # testa/wave is a fluid case — MAILLER then FLUIDER under Xvfb.
           # The legacy AUTOEXIT chain reads the testa/wave/* iexrr files; we
           # batch-pipe them in the canonical order.

           # Step (i): MAILLER (mesh gen)
           xvfb-run --auto-servernum bash -c "
               MEFISTO_BATCH_X11=1 MAILLER &lt; $MEFISTO/testa/wave/wave.iexrr
           " 2&gt;&amp;1 | tee /tmp/09-08-wave-mailler.log | tail -20 || true

           # Step (ii): FLUIDER (solver — produces frames as .eps via xvue X11
           # PostScript pipeline)
           xvfb-run --auto-servernum bash -c "
               MEFISTO_BATCH_X11=1 FLUIDER &lt; $MEFISTO/testa/wave/wave.iexsr
           " 2&gt;&amp;1 | tee /tmp/09-08-wave-fluider.log | tail -20 || true

           # Step (iii): convertepsgif on the .eps frames produced
           ls *.eps 2>/dev/null | head
           bin/convertepsgif wave 2&gt;&amp;1 | tee /tmp/09-08-wave-convert.log | tail -10 || true

           # Step (iv): copy resulting .gif to main as wave_legacy.gif
           ls -la wave.gif animation.gif 2>/dev/null
           if [ -f wave.gif ]; then
               cp wave.gif /home/mefisto/git/mefisto/xvue/qt/tests/golden/wave_legacy.gif
           elif [ -f animation.gif ]; then
               cp animation.gif /home/mefisto/git/mefisto/xvue/qt/tests/golden/wave_legacy.gif
           else
               echo "ERROR: no .gif produced for wave; inspect /tmp/09-08-wave-*.log" >&2
               exit 1
           fi
       )
       ```

       NOTE: AUTOEXIT chain may need a literal `99;\n` line to return-and-save.
       If the case hangs, inspect the .iexrr and .iexsr files for testa/wave
       and append a save-on-exit sequence:
       `echo '99;' >> $MEFISTOX/wave/exit.iex` then re-pipe.

       If MAILLER/FLUIDER do not exist as launchers in pre-retire, fall back to
       direct pp/* invocation:
       `MEFISTO_BATCH_X11=1 pp/ppmail < $MEFISTO/testa/wave/wave.iexrr`
       `MEFISTO_BATCH_X11=1 pp/ppflui < $MEFISTO/testa/wave/wave.iexsr`

    2. Repeat for testa/cavity2d:
       ```
       (
           set -e
           cd /tmp/mefisto-pre-retire
           export MEFISTO=/tmp/mefisto-pre-retire
           export MEFISTOX=/tmp/mefistox-pre-retire
           export PATH=.:$PATH:$MEFISTO/bin
           mkdir -p $MEFISTOX/cavity2d
           cd $MEFISTOX/cavity2d

           xvfb-run --auto-servernum bash -c "
               MEFISTO_BATCH_X11=1 MAILLER &lt; $MEFISTO/testa/cavity2d/cavity2d.iexrr
           " 2&gt;&amp;1 | tee /tmp/09-08-cavity2d-mailler.log | tail -20 || true

           xvfb-run --auto-servernum bash -c "
               MEFISTO_BATCH_X11=1 FLUIDER &lt; $MEFISTO/testa/cavity2d/cavity2d.iexsr
           " 2&gt;&amp;1 | tee /tmp/09-08-cavity2d-fluider.log | tail -20 || true

           ls *.eps 2>/dev/null | head
           bin/convertepsgif cavity2d 2&gt;&amp;1 | tee /tmp/09-08-cavity2d-convert.log | tail -10 || true

           if [ -f cavity2d.gif ]; then
               cp cavity2d.gif /home/mefisto/git/mefisto/xvue/qt/tests/golden/cavity2d_legacy.gif
           elif [ -f animation.gif ]; then
               cp animation.gif /home/mefisto/git/mefisto/xvue/qt/tests/golden/cavity2d_legacy.gif
           else
               echo "ERROR: no .gif produced for cavity2d" >&2
               exit 1
           fi
       )
       ```

    3. Sanity-check the produced GIFs:
       ```
       cd /home/mefisto/git/mefisto
       file xvue/qt/tests/golden/wave_legacy.gif
       file xvue/qt/tests/golden/cavity2d_legacy.gif
       # Expect: "GIF image data, version 89a, ..."
       identify xvue/qt/tests/golden/wave_legacy.gif | head -5
       identify xvue/qt/tests/golden/cavity2d_legacy.gif | head -5
       # Expect: each shows N frames with timing — animated GIFs
       ```

       Expected: each GIF is animated (multi-frame) with frame count > 1.
       Per Phase 7 VERIFICATION.md §9.2: the wave testa case produces
       a multi-frame mesh-evolution sequence.

    4. Verify main worktree state remains clean (T-09-03 mitigation):
       ```
       cd /home/mefisto/git/mefisto
       # Only the 3 golden files should appear as added/untracked:
       git status --porcelain xvue/qt/tests/golden/
       # No .o files, no pp/* binaries leaked:
       git status --porcelain | grep -E '\.o$|^pp/' | head -3
       # Expected empty.
       ```
  </action>
  <verify>
    <automated>
      test -f xvue/qt/tests/golden/wave_legacy.gif &amp;&amp; \
      test -f xvue/qt/tests/golden/cavity2d_legacy.gif &amp;&amp; \
      file xvue/qt/tests/golden/wave_legacy.gif | grep -q 'GIF' &amp;&amp; \
      file xvue/qt/tests/golden/cavity2d_legacy.gif | grep -q 'GIF' &amp;&amp; \
      [ "$(identify -format '%n\n' xvue/qt/tests/golden/wave_legacy.gif | head -1)" -gt 1 ] &amp;&amp; \
      [ "$(identify -format '%n\n' xvue/qt/tests/golden/cavity2d_legacy.gif | head -1)" -gt 1 ] &amp;&amp; \
      [ -z "$(git -C /home/mefisto/git/mefisto status --porcelain | grep -E '\.o$' )" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - wave_legacy.gif + cavity2d_legacy.gif exist in main worktree
    - Both files are valid GIF format with frame count > 1 (animated)
    - No .o files leaked into main worktree (T-09-03 mitigation)
  </acceptance_criteria>
  <done>2 legacy GIFs produced from X11 testa/{wave,cavity2d} pipeline; main clean.</done>
</task>

<task type="auto">
  <name>Task 4: Commit 3 goldens, re-run ctest, flip QSKIPs to PASS, update VALIDATION-LOG, cleanup worktree</name>
  <read_first>
    - .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md
    - xvue/qt/tests/CMakeLists.txt + xvue/qt/tests/test_xvue_qt_postscript.cpp:441 + xvue/qt/tests/test_xvue_qt_export.cpp:517,539 (QSKIP slots)
  </read_first>
  <action>
    1. Re-run Phase 7 ctest under xvfb-run to confirm QSKIP→PASS flip in main:
       ```
       cd /home/mefisto/git/mefisto
       # Defensive: make sure xvue/qt/build is fresh (built against current main, NOT pre-retire):
       bin/cbl_tout 2&gt;&amp;1 | tee /tmp/09-08-main-cbl_tout.log | tail -10
       # Confirm exit 0 (Phase 9 invariant)

       cd xvue/qt/build
       xvfb-run --auto-servernum ctest -R '^xvue_qt_(postscript|export)_tests$' \
           --output-on-failure 2&gt;&amp;1 | tee /tmp/09-08-ctest.log | tail -30
       cd /home/mefisto/git/mefisto
       ```

       Expected output:
         postscript_tests: 19 PASS, 0 SKIP (was 18 PASS + 1 SKIP)
         export_tests:     18 PASS, 0 SKIP (was 16 PASS + 2 SKIP)

       If a QSKIP did NOT flip to PASS, inspect the test source for the SKIP
       condition — it likely checks for the file's existence + a stricter
       byte-parity (or frame-count) constraint. If the constraint is too
       strict for the produced golden, document in
       .planning/phases/09-retire-x11-backend/09-08-DEVIATIONS.md and either
       loosen the test (LAST resort, requires maintainer approval — pause and
       ask) OR re-run Task 2/3 with adjustments to scene/case parameters.

    2. Update Phase 7 VALIDATION-LOG.md DEFERRED→PASS for the 3 rows:

       Edit `.planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md`.
       Replace the 3 DEFERRED rows in the §"Manual A/B sign-off (Plan 06 Task 3)"
       table:
       ```markdown
       | 2026-05-04 | testa/wave A/B | DEFERRED | ... |
       | 2026-05-04 | testa/cavity2d A/B | DEFERRED | ... |
       | 2026-05-04 | scene01.eps byte-parity | DEFERRED | ... |
       ```
       with:
       ```markdown
       | YYYY-MM-DD | testa/wave A/B | PASS | Resolved by Phase 9 Plan 9-08: cross-tag worktree from v1.0-pre-retire produced wave_legacy.gif via testa/wave + bin/convertepsgif. ctest export_tests now 18 PASS 0 SKIP. |
       | YYYY-MM-DD | testa/cavity2d A/B | PASS | Resolved by Phase 9 Plan 9-08: same procedure as testa/wave. cavity2d_legacy.gif committed; ctest slot flipped from QSKIP. |
       | YYYY-MM-DD | scene01.eps byte-parity | PASS | Resolved by Phase 9 Plan 9-08: scene01_driver.f compiled+linked against $MEFISTO/xvue/xvuelc.o + $MEFISTO/xvue/*.o + $MEFISTO/util/*.o + libX11/libXt inside /tmp/mefisto-pre-retire worktree (canonical link line per scene01_driver.f:38-40). TEMPORAIRE.EPS copied to xvue/qt/tests/golden/scene01.eps. ctest postscript_tests now 19 PASS 0 SKIP. |
       ```
       (Use today's ISO date for YYYY-MM-DD.)

    3. Cleanup the pre-retire worktree (T-09-03 final mitigation step):
       ```
       cd /home/mefisto/git/mefisto
       git worktree remove /tmp/mefisto-pre-retire
       rm -rf /tmp/mefistox-pre-retire   # also remove the isolated user-project dir
       git worktree list   # confirm /tmp/mefisto-pre-retire absent
       git worktree prune  # belt-and-suspenders
       ```

    4. Verify main is clean and the 3 grep gates still pass (Phase 9 invariant):
       ```
       git status --porcelain   # should show only the 3 goldens + VALIDATION-LOG.md as A/M
       bin/test_no_imagemagick_in_qt.sh
       bin/test_no_x11_in_build.sh
       bin/test_no_lvideo.sh
       ```

    5. Commit the 3 goldens + VALIDATION-LOG update in a SINGLE atomic commit
       (per RESEARCH §Pattern 3 example):
       ```
       git add xvue/qt/tests/golden/scene01.eps \
               xvue/qt/tests/golden/wave_legacy.gif \
               xvue/qt/tests/golden/cavity2d_legacy.gif \
               .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md
       git commit -m "test(09-08): commit 3 Phase-7-deferred goldens via cross-tag bootstrap

       Closes Phase 8 carry-forward #3 + Phase 7 VERIFICATION.md §9 outstanding
       human-bootstrap items via cross-tag worktree procedure (RESEARCH §Pattern 3).

       Procedure (single deviation from 'Phase 9 only touches Qt-only main' per
       09-CONTEXT.md Claude's Discretion):
       1. git worktree add --detach /tmp/mefisto-pre-retire v1.0-pre-retire
       2. cd /tmp/mefisto-pre-retire ; export MEFISTO=\$PWD MEFISTOX=/tmp/mefistox-pre-retire
       3. bin/cbl_tout (X11 backend builds inside pre-retire worktree)
       4. scene01.eps:  gfortran scene01_driver.o + xvue/xvuelc.o + xvue/*.o +
          util/*.o + -lX11 -lXt → scene01_x11 ; xvfb-run ; cp TEMPORAIRE.EPS to
          main xvue/qt/tests/golden/scene01.eps
       5. wave_legacy.gif: testa/wave under MAILLER + FLUIDER + bin/convertepsgif;
          cp resulting .gif to main
       6. cavity2d_legacy.gif: same procedure as wave on testa/cavity2d
       7. cd /home/mefisto/git/mefisto ; git worktree remove /tmp/mefisto-pre-retire

       ctest -R '^xvue_qt_(postscript|export)_tests\$' under xvfb-run flips 3
       QSKIPs to PASS:
         postscript_tests: 18 PASS + 1 SKIP → 19 PASS + 0 SKIP
         export_tests:     16 PASS + 2 SKIP → 18 PASS + 0 SKIP

       Phase 7 VALIDATION-LOG.md: 3 DEFERRED rows → PASS rows.

       T-09-03 mitigation upheld: /tmp/mefisto-pre-retire worktree REMOVED
       post-commit; git worktree list shows no stale entry; no .o files or
       pp/* binaries leaked to main.

       bin/cbl_tout in main exits 0 (Phase 9 invariant).
       3 grep gates exit 0 (Phase 9 invariant).

       Closes Phase 8 carry-forward #3 (Phase 7 hand-off carry-forward)."
       ```
  </action>
  <verify>
    <automated>
      test -f xvue/qt/tests/golden/scene01.eps &amp;&amp; \
      test -f xvue/qt/tests/golden/wave_legacy.gif &amp;&amp; \
      test -f xvue/qt/tests/golden/cavity2d_legacy.gif &amp;&amp; \
      grep -E '^\| [0-9-]+ \| testa/wave A/B \| PASS' .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md &amp;&amp; \
      grep -E '^\| [0-9-]+ \| scene01\.eps byte-parity \| PASS' .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md &amp;&amp; \
      ! git worktree list | grep -q '/tmp/mefisto-pre-retire' &amp;&amp; \
      ! [ -d /tmp/mefisto-pre-retire ] &amp;&amp; \
      bin/test_no_imagemagick_in_qt.sh &amp;&amp; bin/test_no_x11_in_build.sh &amp;&amp; bin/test_no_lvideo.sh &amp;&amp; \
      grep -E 'Tests passed.*0 SKIP|0 tests skipped' /tmp/09-08-ctest.log || \
        ! grep -E 'SKIP' /tmp/09-08-ctest.log &amp;&amp; \
      git log --oneline -1 | grep -E '09-08.*goldens|09-08.*cross-tag'
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 goldens committed in main: xvue/qt/tests/golden/{scene01.eps, wave_legacy.gif, cavity2d_legacy.gif}
    - Phase 7 VALIDATION-LOG.md: 3 PASS rows replace 3 DEFERRED rows
    - ctest -R xvue_qt_(postscript|export)_tests shows 0 SKIP (or maintainer-noted exception in 09-08-DEVIATIONS.md)
    - /tmp/mefisto-pre-retire worktree REMOVED (git worktree list clean)
    - 3 grep gates pass (Phase 9 invariant)
    - bin/cbl_tout in main exits 0 (Phase 9 invariant)
    - Latest commit references 09-08 + cross-tag
  </acceptance_criteria>
  <done>3 Phase-7 deferred goldens shipped under post-X11 conditions; ctest QSKIPs flipped; worktree cleaned; carry-forward #3 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-retire worktree ↔ main worktree | Build artifacts (.o files, pp/* binaries) must NOT leak from /tmp/mefisto-pre-retire to /home/mefisto/git/mefisto (per RESEARCH §Pitfall 3 / T-09-03) |
| MEFISTO env var ↔ shell session | MEFISTO must point at the worktree DURING build/run, restored after — leakage corrupts main toolchain |
| v1.0-pre-retire tag ↔ Plan 9-08 entry | Plan 9-01 prerequisite must have created the tag; Task 1 verifies, halts if absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Cross-tag worktree leaks artifacts to main on git operation mistake | mitigate | Per RESEARCH §Pitfall 3 mitigation steps 1-4: (1) worktree at /tmp/ NOT $MEFISTO; (2) MEFISTO env var scoped via subshell `( ... )`; (3) `git worktree remove` post-commit; (4) pre-commit `git status` clean check excluding the 3 goldens. Task 1 verify + Task 4 verify both check `git worktree list` |
| T-09-08-A | Tampering | scene01_driver.f link line incomplete (Phase 8 Plan 1 Task 2 failure mode) | mitigate | Use canonical full link line from scene01_driver.f:38-40 ($MEFISTO/xvue/xvuelc.o + $MEFISTO/xvue/*.o + $MEFISTO/util/*.o + libX11 + libXt) — NOT the abbreviated VERIFICATION.md form. Pre-retire worktree's `bin/cbl_tout` (Task 1 step 5) populates the full .o chain. Document deviations in 09-08-DEVIATIONS.md if globbing must change |
| T-09-08-B | Tampering | testa/wave or testa/cavity2d AUTOEXIT chain hangs without `99;` save | mitigate | Use MEFISTO_BATCH_X11=1 + xvfb-run + canonical .iexrr/.iexsr inputs from $MEFISTO/testa/{wave,cavity2d}/. If hang occurs, append `99;\n` save sequence to a temp .iex file and pipe. Document deviation if needed |
| T-09-08-C | DoS | ctest QSKIPs do NOT flip to PASS post-commit (test still skips) | mitigate | Inspect test_xvue_qt_postscript.cpp:441 + test_xvue_qt_export.cpp:517,539 for the QSKIP condition; common cause is path mismatch or missing file checked at fixture-load time. Task 4 step 1 captures actual ctest output; if a QSKIP persists, Task 4 step 1 documents the cause + pauses for maintainer review |
| T-09-08-D | Repudiation | Goldens were produced under cross-tag conditions but commit message claims main-tree provenance | mitigate | Commit message explicitly cites: cross-tag worktree, v1.0-pre-retire SHA (Task 1 captured to /tmp/09-08-pre-retire-sha.txt), and the canonical RESEARCH §Pattern 3 procedure. Future readers can re-verify by re-running the procedure |
</threat_model>

<verification>
- 3 golden files committed in main worktree at xvue/qt/tests/golden/
- ctest -R '^xvue_qt_(postscript|export)_tests$' shows 0 SKIP (3 QSKIPs flipped)
- Phase 7 VALIDATION-LOG.md updated DEFERRED → PASS for the 3 rows
- /tmp/mefisto-pre-retire worktree REMOVED (git worktree list clean)
- bin/cbl_tout in main exits 0
- 3 Phase 9 grep gates pass
- No .o files / pp binaries leaked from worktree to main (T-09-03)
</verification>

<success_criteria>
- [x] Task 1 cross-tag worktree built with X11 backend functional
- [x] Task 2 scene01.eps produced via canonical link line (Phase 8 Plan 1 Task 2 unblocker)
- [x] Task 3 wave_legacy.gif + cavity2d_legacy.gif produced via testa cases + convertepsgif
- [x] Task 4 commit + ctest QSKIP→PASS flip + VALIDATION-LOG update + worktree cleanup
- [x] Phase 7 carry-forward closure (3 rows DEFERRED → PASS)
</success_criteria>

<output>
After completion, create `.planning/phases/09-retire-x11-backend/09-08-SUMMARY.md`
documenting:
- v1.0-pre-retire SHA used for the worktree
- Exact link line used for scene01_driver (full or fallback variant)
- testa/wave + testa/cavity2d AUTOEXIT chain status (clean OR which deviation applied)
- ctest verdict pre/post (18+1 → 19; 16+2 → 18)
- Phase 7 VALIDATION-LOG.md row update timestamps
- Worktree cleanup confirmation (git worktree list output)
- Any 09-08-DEVIATIONS.md notes
</output>
