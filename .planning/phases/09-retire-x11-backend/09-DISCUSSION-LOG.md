# Phase 9: Retire X11 backend - Discussion Log

> **Audit trail only.** Decisions in CONTEXT.md.

**Date:** 2026-05-05
**Phase:** 09-retire-x11-backend
**Areas discussed:** Deletion approach, Carry-forward scope, LVIDEO retirement scope, Rollback safety

---

## Deletion approach

| Option | Description | Selected |
|--------|-------------|----------|
| Incremental per-RETIRE-NN (Recommended) | 4 plans matching RETIRE-01..04. Easier bisect, smaller blast radius. | ✓ |
| Big-bang single commit | One plan, one commit. Clean cut; hardest rollback. | |
| Hybrid (X11 first, ImageMagick second) | 2 plans: X11 + linker, then ImageMagick + LVIDEO + docs. | |

**User's choice:** Incremental per-RETIRE-NN.
**Notes:** D-01, D-02. Plan order = requirement order (RETIRE-01 → RETIRE-04). Each independently buildable + testable.

---

## Carry-forward scope

| Option | Description | Selected |
|--------|-------------|----------|
| All 4 in Phase 9 (Recommended) | Matched-dim + ppnlse_qt + 3 P7 goldens + harness bug all shipped under Phase 9. | ✓ |
| Only ppnlse_qt + harness bug | Critical defects only; goldens and recapture stay deferred. | |
| Phase 9 = retirement only | Strict scope; carry-forwards become Phase 9.1 cleanup. | |

**User's choice:** All 4 in Phase 9.
**Notes:** D-03, D-04. Plans 9-05 (matched-dim) / 9-06 (ppnlse_qt) / 9-07 (P7 goldens) / 9-08 (harness bug + CMake freshness). NOT prerequisites for the 4 RETIRE-NN plans — run after.

---

## LVIDEO retirement scope

| Option | Description | Selected |
|--------|-------------|----------|
| Retire alongside xvuelc.c (Recommended) | RETIRE-03 deletes bin/convertepsgif AND xvue/video*.f AND 18+ tracer subroutines. Single coherent retirement. | ✓ |
| Retire shell-out only | Strip ImageMagick from LVIDEO; keep video*.f + tracers (port to Qt). | |
| Defer LVIDEO entirely | Phase 9 = X11 + ccxvue + libX11 + main convertepsgif only. LVIDEO stays as zombie. | |

**User's choice:** Retire alongside xvuelc.c.
**Notes:** D-05, D-06, D-07. Animation export under Qt is provided by Phase 7 XvueExport `saveGifTo`. LVIDEO removal does NOT remove animation capability.

---

## Rollback safety

| Option | Description | Selected |
|--------|-------------|----------|
| Tag + branch (Recommended) | Pre-Phase-9: git tag v1.0-pre-retire + branch retire-restore-point. Recoverable single-command revert. | ✓ |
| Tag only | git tag v1.0-pre-retire; rollback via checkout from tag. | |
| Trust git history | No explicit checkpoint; git revert if needed. | |

**User's choice:** Tag + branch.
**Notes:** D-08, D-09, D-10. Tag MUST be created before Phase 9 Plan 1. Each plan's commit chain stays bisectable (`bin/cbl_tout` exit 0 after every commit).

---

## Process gate

D-11, D-12 added by orchestrator (not user-asked but locked by Phase 9 ROADMAP semantics):
- Phase 9 starts ONLY when maintainer confirms one-release-cycle A/B window closed.
- A/B window opened 2026-05-05 with Phase 8 sign-off.
- Pre-closure: discuss-phase + plan-phase + research allowed.
- Disallowed pre-closure: execute-phase.

## Claude's Discretion

- Per-plan task breakdown (planner picks the exact split per RETIRE-NN + per carry-forward).
- Research scope: empirical audit of every X11 / ImageMagick / LVIDEO reference before planning.
- Post-retirement testa A/B: re-run all 5 BUILD-10 cases through Qt-only pipeline; commit fresh post-Phase-9 baselines.
- 9-07 cross-tag operation: use v1.0-pre-retire git checkout in a separate worktree to produce X11 baselines for the 3 Phase-7 goldens.

## Deferred Ideas

- Phase-9-finalize summary auto-generated by gsd-verifier (catalogs deleted file count + Fortran subroutine count).
- Qt 7 migration (future milestone; Qt 7 GA expected 2027+).
- Test-suite reorganization post-retirement (testa/ pruning).
- Release tag artifact bundling (tarball, source archive — release-engineering, not Phase 9).
- README badge / version bump (v1.0 → v2.0 to mark Qt-only era — release-engineering decision).
