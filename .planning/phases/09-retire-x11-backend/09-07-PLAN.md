---
# Process gate: A/B window must be CLOSED before this plan executes (per 09-CONTEXT.md D-11)
phase: 09-retire-x11-backend
plan: 07
type: execute
wave: 3
depends_on: ["09-05"]
files_modified:
  - .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md
  # Plus EITHER xvue/qt/src/xvue_qt_api.cpp OR a nlse/*.f file OR
  # bin/ab_sweep_phase8.sh (TIME mitigation note) — chosen by Task 1 root-cause classification.
  # All three files are listed conditionally so wave-overlap detection still works:
  - xvue/qt/src/xvue_qt_api.cpp
  - bin/ab_sweep_phase8.sh
autonomous: true
requirements: []  # Defect carry-forward from Phase 8 override #5; no new REQ-NN
must_haves:
  truths:
    - "evidence/ppnlse-diagnose.md exists with classified root cause = (a) Qt API hang, OR (b) Fortran INTERA/branching skip, OR (c) long TIME=20 loop appearing hung — ONE classification chosen with empirical evidence (gdb backtrace, log excerpt, or strace summary)"
    - "If classification (a): Qt-side fix in xvue/qt/src/xvue_qt_api.cpp (event-pump call inside an EXISTING ABI entry per RESEARCH §Pitfall 6 mitigation 1) — ABI symbol count remains 58"
    - "If classification (b): Fortran-side fix in nlse/ source — ABI unchanged"
    - "If classification (c): documented as expected-long-runtime; bin/ab_sweep_phase8.sh nlsecu line carries explicit TIME-truncation mitigation note (already accepted in Phase 8 override #5); no code change required beyond the documentation"
    - "After fix: Phase-8-equivalent ppnlse_qt offscreen+BATCH_X11 capture (with NO TIME truncation; use canonical workspace) completes within 600s wall-clock OR is documented as long-runtime + waiver retained — verify by capturing a fresh /tmp/09-07-nlsecu-post.png with bin/ab_sweep_phase8.sh --mode qt-1x --cases nlsecu"
    - "ABI symbol count == 58 across every Plan 9-07 commit (T-09-06 mitigation per RESEARCH §Pitfall 6 + plan 09-06 precedent)"
    - "bin/cbl_tout exits 0 after every commit"
  artifacts:
    - path: ".planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md"
      provides: "Empirical diagnose log: root-cause classification + gdb/strace/log-trace evidence + chosen fix path"
      contains: "ROOT CAUSE"
  key_links:
    - from: "evidence/ppnlse-diagnose.md root-cause classification"
      to: "Task 2 fix path"
      via: "explicit (a)/(b)/(c) selector in Task 1 output → Task 2 input"
      pattern: "grep -E 'ROOT CAUSE.*\\((a|b|c)\\)' .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md"
    - from: "Task 2 fix"
      to: "ABI invariant"
      via: "verify_abi.sh exit 0 + nm libxvueqt.a count = 58"
      pattern: "[ \"$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)\" = \"58\" ]"
---

<objective>
Phase 8 carry-forward #2: ppnlse_qt offscreen+MEFISTO_BATCH_X11=1 deadlock fix
(Phase 8 override #5).

Phase 8 Plans 1/3/4 reproduced ppnlse_qt under
`QT_QPA_PLATFORM=offscreen + MEFISTO_BATCH_X11=1` failing to reach the NLSER banner
even at 240s; only ~10 log lines were emitted before the harness timeout (60s).
Phase 8 mitigated via TIME=2 / TIME=0.2 / TIME=0.01 workspace truncations and
accepted Phase-9-deferred-fix as override #5 (08-CHECKLIST.md line 84).

Per RESEARCH §Open Question 3: the symptom "10 log lines, no NLSER banner" is
LIKELY mis-diagnosed. ppnlse.f prints the banner at lines 117/120/121 BEFORE any
solver code; if no banner appears yet 10 log lines are emitted, the banner
sequence is being skipped — which suggests Fortran-side INTERA branching, NOT a
Qt-startup deadlock. RESEARCH explicitly recommends a DIAGNOSE-then-FIX split:
attach gdb, capture backtrace, classify root cause as one of:
  (a) Qt API hang (e.g., QApplication-init race under offscreen+OMP)
  (b) Fortran INTERA/branching skip (banner unreachable on this code path)
  (c) Long-running TIME=20 (~hour-scale) loop appearing hung — NOT a deadlock

Critical constraints (per RESEARCH §Pitfall 6 + plan 09-06 precedent):
- ABI symbol count MUST remain at 58 across every commit. Use the C-only fix
  approach (RESEARCH §Pitfall 6 mitigation 1: add event-pump inside an EXISTING
  ABI entry like `xvvoir_` or `mempxfenetre_`) — do NOT add a new extern "C"
  entry. (Phase 6.0 D-XX locked the count at 58; verify_abi.sh would fail on
  any drift.)
- Diagnose-before-fix: Task 1 PRODUCES the classification; Task 2 implements the
  matching fix path. No speculative implementation prior to Task 1 evidence.

Output:
- evidence/ppnlse-diagnose.md with gdb backtrace + classified root cause
- ONE of: Qt-side fix (xvue_qt_api.cpp, ABI-preserving), Fortran-side fix
  (nlse/), or documentation-only closure (case (c))
- Regression check: ppnlse_qt offscreen+BATCH_X11 capture without TIME
  truncation completes within 600s OR retains waiver with explicit rationale
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-retire-x11-backend/09-CONTEXT.md
@.planning/phases/09-retire-x11-backend/09-RESEARCH.md
@.planning/phases/08-ab-validation-on-testa-subset/08-CHECKLIST.md
@.planning/phases/08-ab-validation-on-testa-subset/08-05-SUMMARY.md
@CLAUDE.md
@xvue/qt/src/xvue_qt_api.cpp
@bin/ab_sweep_phase8.sh

<interfaces>
<!-- Phase 8 override #5 evidence (08-CHECKLIST.md line 84):
       "ppnlse_qt offscreen+BATCH_X11 deadlock at startup (10 log lines emitted,
        no NLSER banner reached even at 240s; reproduced in Plans 1/3/4 with
        TIME=2 / TIME=0.2 truncations); canonical TIME=20 (2000 steps) ~hour-scale
        on this hardware exceeds 60s harness budget."

     ppnlse.f banner lines (per RESEARCH §Open Question 3): 117/120/121 — printed
     BEFORE any solver code. If 10 log lines emit but the banner does not, the
     code path through ppnlse_qt is reaching some 10-print loop and exiting OR
     branching, not blocking on Qt init.

     Likely call sites for fix path (a) [Qt event-pump inside existing ABI]:
       xvue/qt/src/xvue_qt_api.cpp::xvvoir_   — already pumps processEvents
       xvue/qt/src/xvue_qt_api.cpp::mempxfenetre_ — called every iteration
     Phase 9 Plan 9-06 (matched-dim) adds NO new ABI; precedent established.

     Likely fix paths for (b): nlse/ppnlse.f banner-print branching, INTERA
     env detection, or workspace TIME parser.

     Reproducer command (Phase 8 documented):
       QT_QPA_PLATFORM=offscreen MEFISTO_BATCH_X11=1 \
       OMP_NUM_THREADS=1 \
       pp/ppnlse_qt < $MEFISTOX/nlsecu/nlsecu.iexrr 2>&1 | head -50
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose — empirical root-cause classification (gdb backtrace + log trace)</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Open Question 3"
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 6: ppnlse_qt deadlock fix changes the ABI"
    - .planning/phases/08-ab-validation-on-testa-subset/08-CHECKLIST.md (override #5 — line 84 area)
    - .planning/phases/08-ab-validation-on-testa-subset/08-05-SUMMARY.md (Plan 5 OMP-sweep nlsecu evidence)
    - nlse/ppnlse.f (banner-print site at lines 117/120/121 per RESEARCH §OQ3)
    - xvue/qt/src/xvue_qt_api.cpp::xvvoir_ + ::mempxfenetre_ (candidate fix-(a) call sites)
  </read_first>
  <action>
    Per RESEARCH §Open Question 3 + Assumption A3: "HIGH RISK — needs hands-on debugging
    during Plan 9-06 task 1 (a diagnose-then-fix split is recommended)". Plan 9-07
    Task 1 IS that diagnose split.

    **PRECONDITION CHECK (revision iter1 WARNING #8 — gdb ptrace_scope kernel guard):**
    On modern Ubuntu / Debian kernels, `kernel.yama.ptrace_scope` defaults to `1`
    (or higher), which BLOCKS `gdb -p <pid>` attach to non-child processes. Step 2
    of this task uses `gdb -batch -p "$PPNLSE_PID"` against a background process
    spawned by step 1 — that attach will FAIL silently if ptrace_scope > 0.

    Run this check FIRST, before any reproducer:

    ```
    PTRACE_SCOPE=$(cat /proc/sys/kernel/yama/ptrace_scope 2>/dev/null || echo 0)
    if [ "$PTRACE_SCOPE" -gt 0 ]; then
      echo "FATAL: kernel.yama.ptrace_scope=$PTRACE_SCOPE blocks gdb attach to non-child processes."
      echo "Either: (a) sudo sysctl kernel.yama.ptrace_scope=0 (temporary)"
      echo "    OR: (b) Use child-of-gdb pattern: gdb -batch -ex run --args pp/ppnlse_qt < input"
      exit 1
    fi
    ```

    If ptrace_scope is non-zero AND `sudo sysctl` is not available (e.g., locked-down
    CI / sandboxed shell), use **Task 1b alternative — child-of-gdb pattern (fallback (b)):**

    Replace step 1's "background reproducer + step 2's gdb -p attach" with a single
    foreground gdb run that owns the process from birth (gdb's child is always
    attachable regardless of ptrace_scope):

    ```
    gdb -batch         -ex 'set environment QT_QPA_PLATFORM=offscreen'         -ex 'set environment MEFISTO_BATCH_X11=1'         -ex 'set environment OMP_NUM_THREADS=1'         -ex 'handle SIGINT pass'         -ex 'run'         -ex 'thread apply all bt'         --args pp/ppnlse_qt         < $MEFISTOX/nlsecu/nlsecu.iexrr         > /tmp/09-07-gdb-child-run.log 2>&1 &
    GDB_PID=$!
    # Let symptom develop; gdb's `run` command starts the binary inside gdb,
    # so when we SIGINT after 30s the backtrace dump is captured automatically.
    sleep 30
    kill -INT "$GDB_PID" 2>/dev/null || true
    wait "$GDB_PID" 2>/dev/null || true
    head -200 /tmp/09-07-gdb-child-run.log
    ```

    The child-of-gdb fallback (b) yields the same diagnostic value as (a) but
    without requiring ptrace_scope=0. Document in evidence/ppnlse-diagnose.md
    which path was used (sysctl vs child-of-gdb).

    1. Reproduce the symptom in a controlled environment. Run the canonical Phase 8
       reproducer in the BACKGROUND (so step 2 can attach gdb):

       ```
       cd $MEFISTO
       mkdir -p .planning/phases/09-retire-x11-backend/evidence
       # Use the canonical nlsecu workspace (NOT the TIME-truncated variant)
       # so we observe the actual deadlock signature, not a truncated one.
       QT_QPA_PLATFORM=offscreen MEFISTO_BATCH_X11=1 OMP_NUM_THREADS=1 \
         timeout 240 pp/ppnlse_qt < $MEFISTOX/nlsecu/nlsecu.iexrr \
           > /tmp/09-07-ppnlse-stdout.log 2>&1 &
       PPNLSE_PID=$!
       echo "Background PID: $PPNLSE_PID"
       sleep 30   # let the symptom develop (Phase 8 reported 10 log lines by ~30s)
       ```

    2. Capture three independent diagnostic streams while the process is hung
       (or before it exits if classification (c)):

       (2a) gdb backtrace — root-cause discriminator between (a) and (c):
       ```
       gdb -batch -p "$PPNLSE_PID" -ex 'thread apply all bt' \
           > /tmp/09-07-gdb-backtrace.log 2>&1 || true
       head -200 /tmp/09-07-gdb-backtrace.log
       ```

       (2b) stdout log content — root-cause discriminator for (b):
       ```
       wc -l /tmp/09-07-ppnlse-stdout.log
       cat /tmp/09-07-ppnlse-stdout.log
       grep -i 'nlser\|banner\|step\|iter' /tmp/09-07-ppnlse-stdout.log || \
           echo "no NLSER/banner/step/iter strings — confirms banner unreached"
       ```

       (2c) strace summary — orthogonal evidence on what the process is doing:
       ```
       strace -p "$PPNLSE_PID" -c -e trace=poll,select,futex,read,write \
              -o /tmp/09-07-strace.log 2>/dev/null &
       STRACE_PID=$!
       sleep 5
       kill -INT "$STRACE_PID" 2>/dev/null || true
       wait "$STRACE_PID" 2>/dev/null || true
       cat /tmp/09-07-strace.log
       ```

       Then cleanup the reproducer:
       `kill -KILL "$PPNLSE_PID" 2>/dev/null || true ; wait 2>/dev/null || true`

    3. Classify the root cause from the three evidence streams. Decision tree:

       - **gdb backtrace top frame in `QApplication`/`QEventLoop`/`pthread_cond_wait`
         called from a Qt symbol** → classification (a) Qt API hang.
       - **gdb backtrace top frame in Fortran solver code (e.g., `tttsupa2d_`,
         `nlsesp_`) and stdout shows iteration progress (any `iter=` or
         `step=` line)** → classification (c) long-runtime, NOT a deadlock.
       - **gdb backtrace top frame in Fortran but stdout is at exactly the 10-line
         pre-banner state with NO iter/step/banner string** → classification (b)
         Fortran INTERA/branching skip — the code path never reaches the banner
         OR never reaches the solver loop.
       - **strace shows long futex_wait** → corroborates (a). **strace shows
         repeated read() / write() loops** → corroborates (c).

       Write `.planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md`
       with this exact structure:

       ```markdown
       # Plan 9-07 Task 1: ppnlse_qt offscreen+BATCH_X11 root-cause diagnose

       **Date:** YYYY-MM-DD
       **Reproducer:** `QT_QPA_PLATFORM=offscreen MEFISTO_BATCH_X11=1 \
         OMP_NUM_THREADS=1 pp/ppnlse_qt < $MEFISTOX/nlsecu/nlsecu.iexrr`

       ## Stdout state at hang (T+30s)

       Line count: <N>
       Last 10 lines:
       ```text
       <paste tail of /tmp/09-07-ppnlse-stdout.log>
       ```
       NLSER banner reached: yes/no.
       Any iter/step output: yes/no (with line excerpts if yes).

       ## gdb backtrace (top of all threads)

       ```text
       <paste head -200 of /tmp/09-07-gdb-backtrace.log>
       ```

       ## strace summary (5s window)

       ```text
       <paste /tmp/09-07-strace.log>
       ```

       ## ROOT CAUSE: ($a$ | $b$ | $c$)

       Selected: **(a)** / **(b)** / **(c)**.

       Evidence:
       - <bullet pointing at the discriminator line in gdb output>
       - <bullet pointing at the discriminator line in stdout/strace>

       Fix path (forward to Task 2):
       - <one-paragraph description of the targeted fix>
       ```

       Replace the three placeholders ($a$ | $b$ | $c$) with EXACTLY ONE of the
       parenthesized literals so step-2 grep can extract: `(a)`, `(b)`, or `(c)`.

    4. Commit Task 1 evidence:
       ```
       git add .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md
       git commit -m "diagnose(09-07): ppnlse_qt offscreen+BATCH_X11 root-cause classification

       Per RESEARCH §Open Question 3: empirical diagnose-before-fix split (Plan 9-07
       Task 1). gdb backtrace + stdout log + strace summary capture against the
       canonical Phase 8 reproducer (no TIME truncation). Root cause classified as
       (a) Qt API hang | (b) Fortran INTERA/branching skip | (c) long-runtime — see
       evidence/ppnlse-diagnose.md ROOT CAUSE section. Task 2 implements the
       matching fix path."
       ```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md &amp;&amp; \
      grep -E 'ROOT CAUSE.*\((a|b|c)\)' .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md &amp;&amp; \
      grep -q 'gdb backtrace' .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md &amp;&amp; \
      git log --oneline -1 | grep -E '09-07.*diagnose|09-07.*root-cause'
    </automated>
  </verify>
  <acceptance_criteria>
    - evidence/ppnlse-diagnose.md exists with §"Stdout state", §"gdb backtrace", §"strace summary", §"ROOT CAUSE"
    - ROOT CAUSE classification is exactly one of (a), (b), or (c) — extractable via grep
    - Evidence (gdb top frame + stdout discriminator) cited under the chosen classification
    - Latest commit references 09-07 + diagnose
  </acceptance_criteria>
  <done>Diagnose committed; Task 2 has a deterministic fix-path selector.</done>
</task>

<task type="auto">
  <name>Task 2: Fix per Task 1 classification (Qt-side OR Fortran-side OR documentation-only)</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md (Task 1 output)
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 6: ppnlse_qt deadlock fix changes the ABI"
    - xvue/qt/cmake/verify_abi.sh (ABI 58 invariant)
    - For (a): xvue/qt/src/xvue_qt_api.cpp (xvvoir_, mempxfenetre_)
    - For (b): nlse/ppnlse.f + nlse/*.f (the source identified in diagnose §"Fix path")
    - For (c): bin/ab_sweep_phase8.sh nlsecu line + .planning/phases/08-ab-validation-on-testa-subset/08-CHECKLIST.md override #5
  </read_first>
  <action>
    Read the ROOT CAUSE classification from evidence/ppnlse-diagnose.md and execute
    EXACTLY ONE of the three fix paths below. Do NOT execute multiple paths.

    Determine the path:
    `CASE=$(grep -oE 'ROOT CAUSE.*\(([abc])\)' .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md | grep -oE '\([abc]\)' | tr -d '()')`
    Print `echo "Selected fix path: $CASE"` and follow the matching block.

    ---

    **CASE (a) — Qt API hang:**

    Per RESEARCH §Pitfall 6 mitigation 1 (recommended):
    Add an event-pump call (`QCoreApplication::processEvents` or `qApp->processEvents()`)
    inside an EXISTING ABI entry that gets called every iteration. Do NOT add a new
    extern "C" entry — ABI must stay at 58 (T-09-06).

    Recommended call site: `xvvoir_` (already pumps processEvents per its semantics —
    extending the pump frequency does not change ABI). Alternative: `mempxfenetre_`.

    1. Locate xvvoir_ implementation:
       `grep -n 'xvvoir_' xvue/qt/src/xvue_qt_api.cpp`
    2. Inspect — confirm it calls `qApp->processEvents()` or equivalent. If yes:
       extend the pump (e.g., wrap with `processEvents(QEventLoop::AllEvents, 50);`).
       If no: add a single line `qApp->processEvents()` at the function entry,
       guarded by `if (qApp)`.
    3. Build:
       `bin/cbl_tout 2&gt;&amp;1 | tail -10`
       Confirm exit 0.
    4. ABI invariant:
       `[ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ]`
       If this is non-58: STOP, revert the change, fall back to a Fortran-side
       workaround in nlse/ppnlse.f (e.g., insert a periodic `CALL XVVOIR()` in
       the iteration loop — re-uses existing ABI).
    5. Re-run the Task 1 reproducer (60s timeout, no TIME truncation) and capture
       stdout to /tmp/09-07-postfix-stdout.log:
       ```
       QT_QPA_PLATFORM=offscreen MEFISTO_BATCH_X11=1 OMP_NUM_THREADS=1 \
         timeout 60 pp/ppnlse_qt &lt; $MEFISTOX/nlsecu/nlsecu.iexrr \
           &gt; /tmp/09-07-postfix-stdout.log 2&gt;&amp;1 || true
       grep -E 'NLSER|banner|step ' /tmp/09-07-postfix-stdout.log | head -20
       ```
       Expected: NLSER banner now appears in stdout (proves event-loop progresses).

    ---

    **CASE (b) — Fortran INTERA/branching skip:**

    The fix lives in nlse/ source (specifically the file/line cited in
    evidence/ppnlse-diagnose.md §"Fix path"). Typical pattern: an INTERA-detection
    guard reads stdin in a way that consumes the canonical workspace's first
    iexrr line and falls through a printed-but-not-banner branch.

    1. Open the cited file (e.g., nlse/ppnlse.f around lines 117/120/121).
    2. Inspect the branching that decides whether the banner prints. Common bug:
       `IF (LBATCH .NE. 0)` guard incorrectly skipping banner when in
       `MEFISTO_BATCH_X11=1` mode. Fix per the surgical change identified in the
       diagnose document.
    3. Recompile only the affected module:
       `bin/cbnlse 2&gt;&amp;1 | tail -10`  # or full bin/cbl_tout
    4. ABI invariant (Fortran-side fix should not affect Qt ABI but verify):
       `[ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ]`
    5. Re-run reproducer same as CASE (a) step 5; expected NLSER banner appears.

    ---

    **CASE (c) — long-runtime, NOT a deadlock:**

    No code change. The fix is documentation-only: the symptom is canonical
    TIME=20 (2000 steps) running ~hour-scale on the dev hardware, exceeding the
    60s harness timeout. Phase 8 already mitigated via TIME=0.01 / TIME=0.1
    truncations + MAILLER-prereq fallback (08-CHECKLIST.md line 84).

    1. Edit bin/ab_sweep_phase8.sh near the nlsecu case branch (locate via
       `grep -n 'nlsecu' bin/ab_sweep_phase8.sh`). Add an explicit comment:
       ```bash
       # Phase 9 Plan 9-07: nlsecu canonical TIME=20 (2000 steps) is ~hour-scale on
       # this hardware. NOT a deadlock — Plan 9-07 Task 1 diagnose classified as
       # case (c) long-runtime via gdb backtrace + iter/step stdout evidence.
       # Phase 8 mitigation retained: TIME=0.01 (X11 baseline) / TIME=0.1 (OMP)
       # workspace truncations + MAILLER-prereq fallback for Qt 1x/2x.
       # See .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md.
       ```
       (Place above the nlsecu invocation block.)

    2. Open .planning/phases/08-ab-validation-on-testa-subset/08-CHECKLIST.md
       override #5 (line 84 area) and append a sentence to the existing override:
       ```
       Phase 9 Plan 9-07 Task 1 (diagnose) classified as case (c) long-runtime;
       no code defect — see evidence/ppnlse-diagnose.md.
       ```

    3. ABI invariant (no code change but verify the next build still respects it):
       `bin/cbl_tout && [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ]`

    ---

    **All three cases** — capture the post-fix evidence:
    Append to evidence/ppnlse-diagnose.md a §"Post-fix verification" section with:
    - Build exit code (must be 0)
    - ABI count (must be 58)
    - Reproducer stdout (case (a)/(b): NLSER banner reached; case (c): comment landed)

    Commit:
    ```
    git add -A xvue/qt/src/xvue_qt_api.cpp nlse/ppnlse.f \
              bin/ab_sweep_phase8.sh \
              .planning/phases/08-ab-validation-on-testa-subset/08-CHECKLIST.md \
              .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md
    # (git add will silently skip files that weren't touched in your selected case.)
    git commit -m "fix(09-07): carry-forward #2 — ppnlse_qt offscreen+BATCH_X11 (case $CASE)

    Closes Phase 8 override #5 (08-CHECKLIST.md line 84) per RESEARCH §Open Question 3
    diagnose-then-fix split.

    Root cause: case ($CASE) per evidence/ppnlse-diagnose.md.
    - case (a) Qt API hang → event-pump injected into existing ABI entry; ABI 58 preserved
    - case (b) Fortran INTERA/branching skip → surgical nlse/ fix; ABI unaffected
    - case (c) long-runtime, not a deadlock → documentation-only closure; Phase 8
      TIME-truncation mitigation retained per 08-CHECKLIST.md line 84

    ABI invariant: nm libxvueqt.a count = 58 (T-09-06 mitigation per RESEARCH §Pitfall 6).
    bin/cbl_tout exit 0."
    ```
  </action>
  <verify>
    <automated>
      [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ] &amp;&amp; \
      grep -q 'Post-fix verification' .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md &amp;&amp; \
      git log --oneline -1 | grep -E '09-07.*case|09-07.*ppnlse|09-07.*carry-forward'
    </automated>
  </verify>
  <acceptance_criteria>
    - ABI symbol count == 58 post-fix (verify_abi.sh would exit 0)
    - bin/cbl_tout exits 0
    - evidence/ppnlse-diagnose.md has §"Post-fix verification" section
    - Latest commit references 09-07 + matches the chosen case (a/b/c)
    - Files modified match the chosen case (xvue_qt_api.cpp for (a); nlse/ for (b); harness+CHECKLIST.md for (c))
  </acceptance_criteria>
  <done>Fix landed per Task 1 classification; ABI stable; commit references the case.</done>
</task>

<task type="auto">
  <name>Task 3: Regression — re-run Phase-8-equivalent ppnlse_qt capture WITHOUT TIME truncation</name>
  <read_first>
    - bin/ab_sweep_phase8.sh
    - .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md (post-fix section)
    - $MEFISTOX/nlsecu/nlsecu.iexrr (canonical untruncated workspace)
  </read_first>
  <action>
    Verify the fix holds at integration scale: a fresh ppnlse_qt offscreen+BATCH_X11
    capture using the canonical (NOT TIME-truncated) workspace.

    1. Confirm canonical workspace is untruncated:
       `grep -E '^[[:space:]]*TIME' $MEFISTOX/nlsecu/nlsecu.iexrr | head -3`
       Expected: TIME=20 (or whatever the canonical value is). If it shows TIME=0.01
       or TIME=0.1, the workspace is still Phase-8-truncated — STOP and ask
       maintainer to restore canonical workspace OR confirm acceptance.

    2. Run the Phase 8 harness on nlsecu with a generous timeout (case (a)/(b)
       fix should reach NLSER banner quickly; case (c) is expected to time out
       and that's documented):

       ```
       bin/ab_sweep_phase8.sh --mode qt-1x --cases nlsecu \
            --out-dir /tmp/09-07-nlsecu-post 2&gt;&amp;1 | tee /tmp/09-07-final-sweep.log
       ```

       Capture exit timing — note "completed within 60s" vs "timed out at 600s".

    3. Inspect the captured PNG (if any) and the sweep log:
       ```
       ls -la /tmp/09-07-nlsecu-post/nlsecu-qt-1x.png 2>/dev/null || echo "MISSING"
       grep -E 'NLSER|banner|step|TRUNCATED' /tmp/09-07-final-sweep.log | head -10
       ```

    4. Append §"Task 3 regression" to evidence/ppnlse-diagnose.md:
       - Case (a) or (b): expect "NLSER banner reached, capture completed in &lt; 60s,
         PNG > 1KB" → record actual timing + PNG size + identify dim.
       - Case (c): expect "NLSER banner reached eventually OR canonical timeout";
         confirm Phase 8 mitigation (TIME truncation in workspace) remains the
         documented work-around for harness budget.

    5. Build + ABI re-check (defensive — Task 2 commit already verified, but
       Task 3 added a new artifact and we want a clean before-summary state):

       ```
       bin/cbl_tout 2&gt;&amp;1 | tail -3
       [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ]
       ```

    6. Run the 3 grep gates (Phase 9 invariant — same as plan 09-06 Task 4):
       `bin/test_no_imagemagick_in_qt.sh && bin/test_no_x11_in_build.sh && bin/test_no_lvideo.sh`

    7. Commit Task 3 evidence:
       ```
       git add .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md
       git commit -m "verify(09-07): ppnlse_qt regression check — Phase-8-equivalent capture post-fix

       Plan 9-07 Task 3 regression check per quality gate:
       - re-ran bin/ab_sweep_phase8.sh --mode qt-1x --cases nlsecu using canonical
         (untruncated) $MEFISTOX/nlsecu/nlsecu.iexrr workspace
       - case (a)/(b): NLSER banner reached within 60s harness timeout (closes
         override #5 in full)
         OR
       - case (c): canonical timeout reproduced; Phase 8 TIME-truncation mitigation
         retained as documented work-around (override #5 closes via documentation)
       - ABI count = 58 (T-09-06 invariant upheld)
       - 3 grep gates exit 0 (Phase 9 invariant)

       See evidence/ppnlse-diagnose.md §Task 3 regression for actual timing + PNG
       dim + (case-(c)-only) waiver retention rationale."
       ```
  </action>
  <verify>
    <automated>
      test -f /tmp/09-07-final-sweep.log &amp;&amp; \
      grep -q 'Task 3 regression' .planning/phases/09-retire-x11-backend/evidence/ppnlse-diagnose.md &amp;&amp; \
      [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ] &amp;&amp; \
      bin/test_no_imagemagick_in_qt.sh &amp;&amp; bin/test_no_x11_in_build.sh &amp;&amp; bin/test_no_lvideo.sh &amp;&amp; \
      git log --oneline -1 | grep -E '09-07.*regression|09-07.*verify'
    </automated>
  </verify>
  <acceptance_criteria>
    - /tmp/09-07-final-sweep.log exists capturing the regression run
    - evidence/ppnlse-diagnose.md §"Task 3 regression" populated with actual timing + PNG status
    - ABI count = 58
    - 3 grep gates pass
    - Latest commit references 09-07 + regression
  </acceptance_criteria>
  <done>Plan 9-07 commit chain complete; carry-forward #2 closed (fix-applied-and-verified OR case-(c) waiver-retained-with-evidence).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fix path selection (Task 1 → Task 2) | Task 2 must implement EXACTLY the case Task 1 classified — mismatch breaks the diagnose-before-fix invariant |
| ABI symbol count = 58 (T-09-06) | Task 2 implementation MUST NOT add a new extern "C" entry; case (a) uses event-pump inside existing ABI, case (b) is Fortran-only, case (c) is doc-only |
| Canonical vs truncated nlsecu workspace | Task 3 regression must use canonical (TIME=20) workspace; Phase 8 evidence used TIME=0.01/0.1 truncations and is not a regression baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | ABI symbol count drifts from 58 to 59 (RESEARCH §Pitfall 6) | mitigate | Task 2 case (a) injects event-pump inside existing `xvvoir_` or `mempxfenetre_` — no new extern "C" entry; Task 2 verify checks `nm libxvueqt.a | grep ... | wc -l == 58`; Task 3 final verification re-checks. If event-pump-in-existing-ABI is insufficient, fallback to Fortran-side `CALL XVVOIR()` insertion (still no ABI change) |
| T-09-07-D | Tampering | Speculative fix without diagnose evidence | mitigate | Diagnose-before-fix split per RESEARCH §Open Question 3 + Assumption A3 (HIGH RISK). Task 1 commits evidence FIRST; Task 2 reads ROOT CAUSE classification from disk (not from intuition) and applies matching path |
| T-09-07-E | Repudiation | Case (c) closure (no code change) is later mistaken for "fix not done" | mitigate | Append to 08-CHECKLIST.md override #5 the Task 1 diagnose reference (evidence/ppnlse-diagnose.md). Future readers see "case (c) classified — long-runtime, not a defect" with empirical backing |
| T-09-07-F | DoS | Task 1 reproducer hangs gdb / strace and leaves orphan processes | accept | Task 1 uses `timeout 240` on the reproducer + explicit `kill -KILL` cleanup; idempotent across re-runs |
</threat_model>

<verification>
- evidence/ppnlse-diagnose.md exists with Stdout/gdb/strace/ROOT CAUSE/Post-fix/Task 3 regression sections
- ROOT CAUSE classification is exactly one of (a)/(b)/(c) (grep-extractable)
- Task 2 fix matches the case (file scope: xvue_qt_api.cpp for (a); nlse/ for (b); harness+CHECKLIST.md for (c))
- ABI symbol count == 58 across every Plan 9-07 commit
- bin/cbl_tout exits 0
- 3 grep gates pass post-fix
- Task 3 regression captured (canonical workspace, sweep log + PNG status)
</verification>

<success_criteria>
- [x] Task 1 diagnose committed with ROOT CAUSE classification (a/b/c)
- [x] Task 2 fix applied per classified case (no speculative implementation)
- [x] ABI count = 58 across every commit
- [x] bin/cbl_tout exit 0 across every commit
- [x] Task 3 regression evidence captured (canonical workspace)
- [x] All 3 Phase 9 grep gates exit 0
- [x] Phase 8 override #5 closure documented (full fix OR case-(c) waiver-retention)
</success_criteria>

<output>
After completion, create `.planning/phases/09-retire-x11-backend/09-07-SUMMARY.md`
documenting:
- Task 1 ROOT CAUSE classification + key gdb backtrace excerpt
- Task 2 fix scope (file + line numbers if code change; documentation references if (c))
- ABI count pre/post (58/58)
- Task 3 regression timing + PNG status under canonical workspace
- Phase 8 override #5 closure status (resolved-by-fix OR resolved-by-classification)
</output>
