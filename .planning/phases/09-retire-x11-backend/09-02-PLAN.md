---
# Process gate: A/B window must be CLOSED before this plan executes (per 09-CONTEXT.md D-11)
phase: 09-retire-x11-backend
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - xvue/xvuelc.c
  - xvue/xvuelc.o
  - bin/ccxvue
  - bin/cbl_tout
autonomous: true
requirements: ["RETIRE-01"]
must_haves:
  truths:
    - "xvue/xvuelc.c does not exist on disk and is not tracked by git"
    - "xvue/xvuelc.o does not exist on disk"
    - "bin/ccxvue does not exist on disk and is not tracked by git"
    - "No active Fortran INCLUDE / COMMON references the deleted xvuelc.c symbols (every symbol used by Fortran wrappers is provided by libxvueqt.a)"
    - "bin/cbl_tout no longer invokes bin/ccxvue (line removed)"
    - "bin/cbl_tout exits 0 on the post-deletion commit"
    - "5 BUILD-10 testa cases still pass on the Qt-only path (use _qt suffix executables — NOT touched by this plan; that rename happens in Plan 9-03)"
  artifacts:
    - path: "xvue/xvuelc.c"
      provides: "DELETED — must not exist after Plan 9-02"
      anti_artifact: true
    - path: "bin/ccxvue"
      provides: "DELETED — must not exist after Plan 9-02"
      anti_artifact: true
  key_links:
    - from: "Fortran wrappers (xvue/*.f)"
      to: "libxvueqt.a (Qt backend)"
      via: "linker resolution at bin/cb*_qt time"
      pattern: "nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l == 58"
    - from: "bin/cbl_tout"
      to: "bin/ccxvue invocation line (REMOVED)"
      via: "shell-script edit"
      pattern: "! grep -n 'ccxvue' bin/cbl_tout"
---

<objective>
RETIRE-01: Delete `xvue/xvuelc.c` (3749 lines, 156 KB Xlib backend), `xvue/xvuelc.o`
build artifact, and `bin/ccxvue` (the C compile wrapper). Remove the corresponding
invocation from `bin/cbl_tout`. Verify no Fortran INCLUDE/COMMON file references
xvuelc symbols (Phase 9 RESEARCH §Pitfall 1, T-09-01).

Purpose:
- Eliminate the legacy Xlib backend now that the parallel Qt build (Phase 0–7) ships
  identical ABI symbols via `libxvueqt.a`.
- Each Fortran call site must continue to link cleanly through the Qt path
  (`pp/pp*_qt`); the legacy `pp/pp*` (X11) executables become orphaned by Plan 9-03.

Output:
- 3 file deletions (xvuelc.c, xvuelc.o, ccxvue)
- 1 file edit (bin/cbl_tout — drop the ccxvue invocation)
- Post-deletion verification: bin/cbl_tout_qt exit 0 + Qt path testa baseline survives.
- 09-02-SUMMARY.md with delta against 09-01-AUDIT-BASELINE.md.
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-retire-x11-backend/09-CONTEXT.md
@.planning/phases/09-retire-x11-backend/09-RESEARCH.md
@.planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md
@CLAUDE.md
@bin/cbl_tout
@bin/ccxvue

<interfaces>
<!-- ABI invariant: libxvueqt.a (Qt backend) must provide the same symbols
     that xvuelc.o used to provide. Phase 8 verifier already validated 58
     symbols match the header. Plan 9-02 deletion does NOT change the Qt
     side; verify_abi.sh keeps passing because Qt symbols are unchanged. -->

From xvue/qt/cmake/verify_abi.sh:
  Asserts `nm libxvueqt.a | grep ' T ' | grep '_$' | wc -l == 58`
  (the 57 original entries + xvue_module_init_ from Phase 6.0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify ABI completeness BEFORE deletion (Pitfall 1, T-09-01 mitigation)</name>
  <read_first>
    - xvue/qt/cmake/verify_abi.sh (or wherever the ABI count gate lives)
    - .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md §3 (recorded baseline ABI count)
  </read_first>
  <action>
    Pre-deletion sanity: confirm libxvueqt.a still exposes the full 58-symbol ABI.
    If it doesn't, the Qt build is broken and Plan 9-02 must NOT proceed.

    1. Build libxvueqt.a fresh:
       `cmake -S xvue/qt -B xvue/qt/build -DXVUE_QT_BUILD_TESTS=ON 2>&amp;1 | tail -5`
       `cmake --build xvue/qt/build --target xvueqt 2>&amp;1 | tail -5`

    2. Count Fortran-facing symbols (T entries with trailing underscore):
       `COUNT=$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)`
       `echo "ABI count = $COUNT"`
       Must equal 58. If not, STOP — file an issue, do not delete xvuelc.c.

    3. Verify no Fortran source references "xvuelc" symbol literally
       (xvuelc.c is the only file referencing its own name in a comment):
       `grep -rln 'xvuelc' xvue/*.f xvue/*.inc incl/*.inc 2&gt;/dev/null`
       Expected: 0 matches (per RESEARCH §Pitfall 1 verification).
       If non-zero, STOP and surface to maintainer.

    4. Capture pre-deletion artifact list to /tmp/09-02-pre-delete.log:
       `ls -la xvue/xvuelc.c xvue/xvuelc.o bin/ccxvue 2&gt;&amp;1 | tee /tmp/09-02-pre-delete.log`
  </action>
  <verify>
    <automated>
      [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ] &amp;&amp; \
      [ "$(grep -rln 'xvuelc' xvue/*.f xvue/*.inc incl/*.inc 2&gt;/dev/null | wc -l)" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - libxvueqt.a builds cleanly
    - ABI symbol count is exactly 58 (matches 09-01-AUDIT-BASELINE.md §3)
    - Zero Fortran sources reference the literal string "xvuelc" (only the .c file itself does, in comments — and that's about to be deleted)
  </acceptance_criteria>
  <done>Pre-deletion ABI confirmed at 58; zero Fortran ghost references; safe to delete xvuelc.c.</done>
</task>

<task type="auto">
  <name>Task 2: Delete xvuelc.c + xvuelc.o + ccxvue + remove ccxvue from cbl_tout</name>
  <read_first>
    - bin/cbl_tout (locate the ccxvue invocation line)
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pattern 1: Mechanical-Deletion-with-Atomic-Commit"
  </read_first>
  <action>
    Atomic deletion + edit (one logical commit):

    1. Delete the C source + build artifact + wrapper:
       `git rm xvue/xvuelc.c bin/ccxvue`
       `rm -f xvue/xvuelc.o`  (.o is .gitignored — do not git-rm; just rm)

    2. Locate the ccxvue invocation in bin/cbl_tout (typically a line `$MEFISTO/bin/ccxvue`
       near the C-compilation block), and remove it. Use Edit tool with the exact line as
       found via:
       `grep -n 'ccxvue' bin/cbl_tout`
       Edit the line out (or comment-and-delete the whole "Compilation C de xvuelc" block
       if that block is exclusively for ccxvue). Preserve the surrounding shell context.

       Expected: bin/cbl_tout post-edit has zero `ccxvue` references:
       `grep -c 'ccxvue' bin/cbl_tout` returns 0.

    3. (NOTE: bin/cbl_tout itself remains; this plan does NOT rename it. That's Plan 9-03.
       Plan 9-02 simply removes the C-compilation step from bin/cbl_tout so the legacy
       X11 build no longer attempts to compile xvuelc.c. The remaining bin/cb* X11 link
       scripts will fail to link until Plan 9-03 deletes them — that's expected and noted.)

    4. Verify deletions completed:
       `! test -f xvue/xvuelc.c &amp;&amp; \
        ! test -f xvue/xvuelc.o &amp;&amp; \
        ! test -f bin/ccxvue &amp;&amp; \
        ! git ls-files xvue/xvuelc.c bin/ccxvue 2&gt;&amp;1 | grep -q .`

    5. Stage the cbl_tout edit:
       `git add bin/cbl_tout`
  </action>
  <verify>
    <automated>
      ! test -f xvue/xvuelc.c &amp;&amp; \
      ! test -f xvue/xvuelc.o &amp;&amp; \
      ! test -f bin/ccxvue &amp;&amp; \
      [ -z "$(git ls-files xvue/xvuelc.c bin/ccxvue)" ] &amp;&amp; \
      [ "$(grep -c 'ccxvue' bin/cbl_tout)" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - xvue/xvuelc.c does not exist on disk
    - xvue/xvuelc.o does not exist on disk
    - bin/ccxvue does not exist on disk
    - `git ls-files xvue/xvuelc.c bin/ccxvue` returns empty (deletions staged)
    - bin/cbl_tout has zero `ccxvue` references (`grep -c 'ccxvue' bin/cbl_tout` == 0)
  </acceptance_criteria>
  <done>3 files deleted, bin/cbl_tout no longer invokes ccxvue; deletions + edit staged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Qt build green + Qt-path testa baselines unbroken + commit</name>
  <read_first>
    - bin/cbl_tout_qt
    - bin/ab_sweep_phase8.sh
  </read_first>
  <action>
    Confirm the Qt path still builds + runs cleanly. The legacy X11 path may now fail
    to LINK (because cb*-NON-_qt scripts still reference xvuelc.o which no longer
    exists) — that's expected and is fully resolved by Plan 9-03 deleting those scripts.
    For Plan 9-02, the verification target is the Qt-only build:

    1. Build Qt-only path:
       `bin/cbl_tout_qt 2&gt;&amp;1 | tee /tmp/09-02-cbl_tout_qt.log`
       Confirm exit 0.

    2. Run the 5-case Qt-1x sweep:
       `bin/ab_sweep_phase8.sh --mode qt-1x \
            --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu \
            --out-dir /tmp/09-02-post-retire01 2&gt;&amp;1 | tee /tmp/09-02-sweep.log`

    3. Confirm 4-5 PNGs land (nlsecu may still truncate per Phase 8 override #5 — that's
       carried into Plan 9-07):
       `for c in pan2d nafems_le1 cavity2d heat1d; do \
            test -f /tmp/09-02-post-retire01/$c-qt-1x.png || echo "MISSING $c"; done`
       All 4 must be present.

    4. Re-confirm ABI symbol count (regression guard):
       `[ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ]`

    5. Commit:
       ```
       git add -A xvue/xvuelc.c bin/ccxvue bin/cbl_tout
       # (the deletions and edit; nothing else)
       git commit -m "feat(09-02): RETIRE-01 — delete xvuelc.c + ccxvue + remove from cbl_tout

       Drops the 3749-line Xlib backend (xvue/xvuelc.c, 156 KB), the C compile
       wrapper (bin/ccxvue, 44 lines), and the build artifact (xvue/xvuelc.o).
       bin/cbl_tout's ccxvue invocation line removed.

       Pre-deletion sanity:
       - libxvueqt.a ABI symbol count: 58 (matches 09-01 baseline)
       - Zero Fortran sources reference 'xvuelc' literally
         (grep -rln xvuelc xvue/*.f xvue/*.inc incl/*.inc → 0)

       Post-deletion verification:
       - bin/cbl_tout_qt exits 0
       - 4-5 BUILD-10 testa cases capture cleanly via Phase 8 harness
         (nlsecu carries forward to Plan 9-07 per Phase 8 override #5)

       Note: bin/cbl_tout (legacy X11 build entry) now FAILS at the cb{init,mail,
       elas,flui,ther,nlse} link stage because those scripts still reference
       -lX11 + xvuelc.o. That is expected and is fully resolved by Plan 9-03
       (RETIRE-02), which deletes the legacy cb* scripts and renames cb*_qt
       to drop the suffix.

       Closes RETIRE-01."
       ```
  </action>
  <verify>
    <automated>
      tail -1 /tmp/09-02-cbl_tout_qt.log | grep -qiE "MODULES|created|crees|done" &amp;&amp; \
      ls /tmp/09-02-post-retire01/pan2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-02-post-retire01/nafems_le1-qt-1x.png &amp;&amp; \
      ls /tmp/09-02-post-retire01/cavity2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-02-post-retire01/heat1d-qt-1x.png &amp;&amp; \
      [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ] &amp;&amp; \
      git log --oneline -1 | grep -E '09-02.*RETIRE-01'
    </automated>
  </verify>
  <acceptance_criteria>
    - bin/cbl_tout_qt exits 0
    - All 4 fast-running testa cases (pan2d, nafems_le1, cavity2d, heat1d) capture PNGs
    - ABI symbol count remains 58
    - Latest commit message contains "09-02" and "RETIRE-01"
  </acceptance_criteria>
  <done>RETIRE-01 commit landed; Qt path green; 4 testa cases pass; ABI stable at 58.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fortran wrappers ↔ libxvueqt.a (Qt) | All ~60 ABI symbols formerly resolved via xvuelc.o now resolved via libxvueqt.a |
| bin/cbl_tout ↔ bin/ccxvue | The wrapper invocation removed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Hidden xvuelc reference in Fortran INCLUDE/COMMON | mitigate | Task 1 step 3: `grep -rln 'xvuelc' xvue/*.f xvue/*.inc incl/*.inc` MUST return 0; STOP if not (per RESEARCH §Pitfall 1). Plus ABI symbol count gate at 58. |
| T-09-02 | Tampering | Stale xvuelc.o referenced by legacy cb* scripts | accept | Legacy bin/cb{init,mail,elas,...}-NON-_qt scripts WILL fail to link post-deletion. That's expected — Plan 9-03 deletes them. Documented in commit message. |
| T-09-03 | Information Disclosure | xvuelc.c contains decades-old comments / author attribution | accept | Source is being deleted wholesale; git history preserves attribution via the v1.0-pre-retire tag. |
</threat_model>

<verification>
- nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l == 58 (ABI invariant)
- grep -rln xvuelc xvue/*.f xvue/*.inc incl/*.inc → 0 matches
- ! test -f xvue/xvuelc.c && ! test -f xvue/xvuelc.o && ! test -f bin/ccxvue
- grep -c ccxvue bin/cbl_tout == 0
- bin/cbl_tout_qt exits 0
- 4 of 5 testa baselines capture cleanly via bin/ab_sweep_phase8.sh --mode qt-1x
</verification>

<success_criteria>
- [x] Pre-deletion ABI count = 58 confirmed (Task 1)
- [x] Zero Fortran ghost references to "xvuelc" (Task 1)
- [x] xvue/xvuelc.c, xvue/xvuelc.o, bin/ccxvue all deleted (Task 2)
- [x] bin/cbl_tout no longer invokes ccxvue (Task 2)
- [x] bin/cbl_tout_qt exits 0 post-deletion (Task 3)
- [x] 4 of 5 testa baselines capture (Task 3)
- [x] ABI count still 58 post-deletion (Task 3)
- [x] Commit message references RETIRE-01 (Task 3)
</success_criteria>

<output>
After completion, create `.planning/phases/09-retire-x11-backend/09-02-SUMMARY.md`
documenting:
- Files deleted (size totals)
- ABI count pre/post
- testa case capture status
- bin/cbl_tout edit (line removed)
- Note on legacy cb* scripts now broken (resolved by Plan 9-03)
</output>
