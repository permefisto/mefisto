# Plan 9-03 Rename Impact Audit (Task 1)

**Date:** 2026-05-05
**Scope:** All references to `cbl_tout_qt`, `cb{mail,elas,flui,ther,nlse,init,xvtest{0..4}}_qt`, and `pp/pp*_qt` across the active build tree.

Generated by `grep -rln` (excluding `worktree-*`, `worktrees`, `.git`, `pp`); raw output preserved at `/tmp/09-03-cbl_tout_qt-refs.log` (339 hits across all classes; ~58 references when worktree dirs are excluded; 5 active files for `cbl_tout_qt` after .planning filter).

## Concrete pattern confirmation (Task 1 step 3)

| Pattern | File | Line | Body |
|---------|------|------|------|
| `nompp=pp/pp<name>_qt` | `bin/cbmail_qt` | 31 (drift from baseline 4 — confirmed and noted) | `nompp=pp/ppmail_qt` |
| `pp/pp${PREREQ_MODULE}_qt` | `bin/ab_sweep_phase8.sh` | 130 | prereq dispatch |
| `pp/pp${MODULE}_qt` | `bin/ab_sweep_phase8.sh` | 151 | qt-1x branch |
| `pp/pp${MODULE}_qt` | `bin/ab_sweep_phase8.sh` | 161 | qt-2x branch |
| `pp/pp${MODULE}_qt` | `bin/ab_sweep_phase8.sh` | 171 | qt-omp branch |

Drift from plan baseline: `bin/cbmail_qt` `nompp=` line is at line 31, not line 4 (older baseline likely captured it differently). Plan acceptance criteria check the **content** (`grep -q '^nompp=pp/ppmail$' bin/cbmail`), not the line number — so this drift does not affect Task 3 logic.

## Update in lockstep (category a — active build/tooling)

These files MUST be updated as part of this plan to keep the active build/harness chain coherent after the rename:

| File | Edit | Task |
|------|------|------|
| `bin/cbl_tout_qt` | `git mv` → `bin/cbl_tout` (replacing the legacy cbl_tout deleted in step 1) | Task 3 |
| `bin/cbinit_qt` | `git mv` → `bin/cbinit` | Task 3 |
| `bin/cbmail_qt` | `git mv` → `bin/cbmail`; sed `nompp=pp/ppmail_qt` → `nompp=pp/ppmail` | Task 3 |
| `bin/cbelas_qt` | `git mv` → `bin/cbelas`; sed `nompp=pp/ppelas_qt` → `nompp=pp/ppelas` | Task 3 |
| `bin/cbflui_qt` | `git mv` → `bin/cbflui`; sed | Task 3 |
| `bin/cbther_qt` | `git mv` → `bin/cbther`; sed | Task 3 |
| `bin/cbnlse_qt` | `git mv` → `bin/cbnlse`; sed | Task 3 |
| `bin/cbxvtest0_qt` | `git mv` → `bin/cbxvtest0`; sed | Task 3 |
| `bin/cbxvtest1_qt` | `git mv` → `bin/cbxvtest1`; sed | Task 3 |
| `bin/cbxvtest2_qt` | `git mv` → `bin/cbxvtest2`; sed | Task 3 |
| `bin/cbxvtest3_qt` | `git mv` → `bin/cbxvtest3`; sed | Task 3 |
| `bin/cbxvtest4_qt` | `git mv` → `bin/cbxvtest4`; sed | Task 3 |
| `bin/ab_sweep_phase8.sh` | sed lines 130/151/161/171: `pp/pp${...}_qt` → `pp/pp${...}` (BLOCKER #4) | Task 4 |
| `xvue/qt/CMakeLists.txt` | comments at lines 174 + 187 (mention `bin/cbl_tout_qt`); also new `verify_no_x11_in_build` ALL target | Tasks 3 + 5 |
| `xvue/qt/README.md` | lines 9/19/26 reference `bin/cbl_tout_qt` and `pp/pp*_qt` — update to reflect renamed bin/cbl_tout (one explanatory note) | Task 3 step 6 |
| `bin/cbl_tout_qt` body | dispatcher loop invokes `cb<module>_qt` — update to `cb<module>` after move | Task 3 step 4 |

### Pp/pp*_qt references in *active source comments* (NOT updated — Phase 6 historical commentary)

These references are inside **code comments** describing Phase 6 architectural intent (`// pp/pp{elas,flui,nlse,ther}_qt. XvueApp's load_bundled_font_() references this symbol …`). They are historical Phase-6-era documentation embedded in the `.cpp` files; semantic-equivalent to the .planning historical docs. Plan Task 1 step 4 (b) directs us NOT to edit historical Phase-6/7/8 commentary. Leaving as historical:

- `xvue/qt/src/xvue_qt_elas_actions.cpp:144`
- `xvue/qt/src/xvue_qt_flui_actions.cpp:136`
- `xvue/qt/src/xvue_qt_nlse_actions.cpp:172`
- `xvue/qt/src/xvue_qt_ther_actions.cpp:148`
- `xvue/qt/tests/test_xvue_qt_elas_menu.cpp:255,320`
- `xvue/qt/tests/test_xvue_qt_flui_menu.cpp:351`
- `xvue/qt/tests/test_xvue_qt_nlse_menu.cpp:374`
- `xvue/qt/tests/test_xvue_qt_ther_menu.cpp:366`

These comments do NOT affect compilation (pure prose); they describe the historical Phase-6 intent which is exactly the kind of commentary D-04 / Plan §Phase 7/8 Historical preserves.

### Pp/pp*_qt references in *active harness scripts* (also category a — Update)

Beyond `bin/ab_sweep_phase8.sh` already enumerated:

| File | Line | Body | Disposition |
|------|------|------|-------------|
| `bin/phase8_case_batch_map.sh` | 8, 9 | comments referencing `pp/pp${MODULE}_qt` | Update — comments document the active harness contract |
| `bin/qt-capture.sh` | 17 | example invocation `pp/ppmail_qt` | Update — usage example must reflect post-Phase-9 names |
| `bin/xvtest0-pixmap-roundtrip.sh` | 26 | error msg "pp/ppxvtest0_qt not built — run bin/cbxvtest0_qt first" | Update — references both renamed binary + renamed cb script |

Plan §`<files_modified>` includes `bin/ab_sweep_phase8.sh` but not these three. They are clearly part of the same "harness must reference pp/pp${MODULE} no _qt" logical change (BLOCKER #2-#4 spirit). Treating them as Rule 2 (auto-add missing critical functionality — the harness is incoherent if these reference nonexistent binary names): updating as part of Task 4 (harness) for `phase8_case_batch_map.sh` (a sourced helper for ab_sweep_phase8.sh) and Task 3 follow-up for the standalone capture/roundtrip scripts.

## Leave as historical (categories b + c)

Per Plan Task 1 step 4 (b) + (c):

- **All `.planning/` content** (CONTEXT.md / PLAN.md / SUMMARY.md / RESEARCH.md / VALIDATION.md / CHECKLIST.md / STATE.md / REQUIREMENTS.md / PROJECT.md / ROADMAP.md / `.planning/research/*.md` / `.planning/debug/*.md` / `.planning/codebase/*.md`) — they capture state at their respective phase ends.
- All other `.claude/worktrees/agent-*/...` cross-tag artifacts — out of scope.

## Summary counts

- 5 active files reference `cbl_tout_qt` (xvue/qt/CMakeLists.txt, README.md, 3 .cpp comments).
- 4 active files reference `cb*_qt` script invocations (bin/xvtest0-pixmap-roundtrip.sh + 3 phase8 harness helpers).
- 4 lines in `bin/ab_sweep_phase8.sh` reference `pp/pp${MODULE}_qt` / `pp/pp${PREREQ_MODULE}_qt` (lines 130/151/161/171).
- ~12 cb*_qt scripts to be `git mv`-renamed in Task 3.
- ~12 sed body collapses (`nompp=pp/pp<x>_qt` → `nompp=pp/pp<x>`).
- 1 dispatcher edit in `bin/cbl_tout_qt` after rename (cb*_qt → cb*).
- 1 README update (xvue/qt/README.md).

## Acceptance gate

§Update list explicitly enumerates: bin/cbl_tout_qt, all cb*_qt scripts (12), xvue/qt/CMakeLists.txt, bin/ab_sweep_phase8.sh.
