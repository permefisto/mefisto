---
# Process gate: A/B window must be CLOSED before this plan executes (per 09-CONTEXT.md D-11)
phase: 09-retire-x11-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "git tag v1.0-pre-retire exists locally and (recommended) on origin"
    - "git branch retire-restore-point exists pointing at the same commit as v1.0-pre-retire"
    - "Empirical pre-retirement reference counts recorded in 09-01-AUDIT-BASELINE.md (xvuelc.c lines, libX11 ref count, /usr/X11R6 ref count, convert ref count, video*.f line totals, LVIDEO call sites = 12 tracer files, ABI symbol count = 58)"
    - "bin/cbl_tout exits 0 on the v1.0-pre-retire commit"
    - "5 BUILD-10 testa cases captured under the v1.0-pre-retire commit (rerun via Phase 8 harness, no AE diffs vs Phase 8 evidence)"
  artifacts:
    - path: ".planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md"
      provides: "Pre-retirement reference counts + verification log"
      contains: "ABI symbol count"
  key_links:
    - from: "git tag v1.0-pre-retire"
      to: "current main HEAD at Plan 9-01 start"
      via: "git tag -a (annotated) + push origin v1.0-pre-retire"
      pattern: "git tag --list v1.0-pre-retire"
    - from: "Plan 9-07 cross-tag worktree"
      to: "v1.0-pre-retire tag"
      via: "git worktree add /tmp/mefisto-pre-retire v1.0-pre-retire"
      pattern: "git worktree list"
---

<objective>
Establish the rollback safety contract and audit baseline for Phase 9.

Purpose:
- Create `git tag v1.0-pre-retire` and branch `retire-restore-point` (D-08, D-09).
  Both MUST exist BEFORE Wave 2 (Plans 9-02 through 9-05) executes — Plan 9-01 is the
  literal gate.
- Record empirical pre-retirement reference counts in 09-01-AUDIT-BASELINE.md so that
  RETIRE-NN plans can self-validate (e.g., "delete N references, then verify post-grep
  count == 0").
- Verify the v1.0-pre-retire commit itself is healthy: `bin/cbl_tout` exits 0 + 5 testa
  baseline cases capture cleanly via the Phase 8 harness.

Output:
- Annotated git tag `v1.0-pre-retire` (pushed to origin if remote configured).
- Branch `retire-restore-point` from same commit (pushed to origin if remote configured).
- `.planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md` with all reference
  counts + verification log.

This plan is the GATE. Wave 2 plans MUST NOT start until v1.0-pre-retire exists.
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-retire-x11-backend/09-CONTEXT.md
@.planning/phases/09-retire-x11-backend/09-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Maintainer confirms A/B window closure (D-11 process gate)</name>
  <read_first>
    - .planning/STATE.md (look for "Phase 9 entry: A/B window closed YYYY-MM-DD" line in decisions block)
    - .planning/phases/09-retire-x11-backend/09-CONTEXT.md (D-11 closure protocol)
  </read_first>
  <what-built>
    Plans 9-01 through 9-09 are CREATED but NOT EXECUTED. Per D-11/D-12 the executor
    must block here until the maintainer commits an explicit closure line to STATE.md.
  </what-built>
  <how-to-verify>
    1. Read .planning/STATE.md and confirm there is a line of the form
       `Phase 9 entry: A/B window closed YYYY-MM-DD — maintainer initials` in the
       Decisions block.
    2. If absent: STOP. Reply to maintainer: "Phase 9 process gate (D-11) not satisfied —
       commit closure line to STATE.md or reply explicitly that Phase 9 is unblocked."
    3. If present: proceed to Task 2.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -E '^- (\[Phase 9 entry:|Phase 9 entry:).*A/B window closed' .planning/STATE.md`
      returns at least one match, OR maintainer has replied explicitly approving Phase 9 execution
      (record the approval timestamp + initials in 09-01-AUDIT-BASELINE.md §1).
  </acceptance_criteria>
  <resume-signal>Maintainer confirmation captured (STATE.md line OR direct reply).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create v1.0-pre-retire tag + retire-restore-point branch (D-08, D-09)</name>
  <read_first>
    - bin/cbl_tout (verify it exists at the current HEAD)
    - .planning/phases/09-retire-x11-backend/09-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Per D-08: create the rollback artifacts BEFORE any deletion lands.
    Steps (run from repo root):

    1. Verify the tag does not already exist (idempotency guard):
       `git tag --list v1.0-pre-retire`
       If non-empty, STOP and ask maintainer (someone already created it; do not overwrite).

    2. Create the annotated tag at current HEAD:
       `git tag -a v1.0-pre-retire -m "Pre-Phase-9 rollback safety point. Final commit before X11 / ImageMagick / LVIDEO retirement. Single-command revert: git reset --hard v1.0-pre-retire."`

    3. Create the rollback branch at the same commit:
       `git branch retire-restore-point HEAD`

    4. Push to origin if a remote is configured (probe first):
       `if git remote get-url origin >/dev/null 2>&1; then \
            git push origin v1.0-pre-retire && \
            git push origin retire-restore-point ; \
        else echo "No origin remote — skipping push (D-08 'pushed to origin' is best-effort)."; fi`

    5. Record current HEAD SHA + tag verification output to 09-01-AUDIT-BASELINE.md §2 via Task 4.

    NOTE: Do NOT use `--no-verify` or skip hooks. Tag creation is read-only on the working
    tree; if hooks reject it, surface the error and STOP.
  </action>
  <verify>
    <automated>
      git tag --list v1.0-pre-retire | grep -q '^v1.0-pre-retire$' &amp;&amp; \
      git rev-parse v1.0-pre-retire &gt; /dev/null &amp;&amp; \
      git rev-parse retire-restore-point &gt; /dev/null &amp;&amp; \
      [ "$(git rev-parse v1.0-pre-retire)" = "$(git rev-parse retire-restore-point)" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list v1.0-pre-retire` returns exactly `v1.0-pre-retire`
    - `git rev-parse v1.0-pre-retire` and `git rev-parse retire-restore-point` return the
      identical 40-char SHA
    - If origin exists: `git ls-remote origin refs/tags/v1.0-pre-retire` is non-empty
  </acceptance_criteria>
  <done>v1.0-pre-retire tag + retire-restore-point branch exist at the same commit; pushed to origin if remote configured.</done>
</task>

<task type="auto">
  <name>Task 3: Build the v1.0-pre-retire commit + run 5 testa baselines</name>
  <read_first>
    - bin/cbl_tout
    - .planning/phases/08-ab-validation-on-testa-subset/08-VALIDATION.md (5 BUILD-10 cases)
    - bin/ab_sweep_phase8.sh
  </read_first>
  <action>
    Verify the v1.0-pre-retire commit is itself healthy. Plan 9-01 is the gate, so this
    sanity check guards against tagging a broken commit.

    1. Ensure clean tree (no in-flight changes):
       `git status --porcelain` — if non-empty, STOP and ask maintainer.

    2. Full legacy build (X11 + Qt parallel paths still alive at this commit):
       `bin/cbl_tout 2&gt;&amp;1 | tee /tmp/09-01-cbl_tout-pre.log`
       Confirm exit 0 (`echo "exit=$?"`).

    3. Capture all 5 BUILD-10 baseline cases via the Phase 8 Qt-1x sweep harness, mode qt-1x:
       `bin/ab_sweep_phase8.sh --mode qt-1x --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu \
           --out-dir /tmp/09-01-pre-retire-baseline 2&gt;&amp;1 | tee /tmp/09-01-sweep-pre.log`
       (nlsecu may truncate at 600s; that is acceptable — same behavior Phase 8 documented.)

    4. Confirm 5 PNGs (or for nlsecu, a partial PNG &gt;1KB) land:
       `for c in pan2d nafems_le1 cavity2d heat1d nlsecu; do \
            ls -la /tmp/09-01-pre-retire-baseline/$c-qt-1x.png || echo "MISSING: $c"; done`
       At minimum 4/5 must exist; nlsecu may show "partial" or "MISSING" with the timeout
       caveat documented in Phase 8 override #5 (this carry-forward is fixed in Plan 9-07).

    5. Record SHA + build log path + sweep log path + per-case capture status into
       09-01-AUDIT-BASELINE.md (Task 4 writes the file).

    Capture key outputs to /tmp/ for Task 4 to ingest, e.g.:
       `bin/cbl_tout 2&gt;&amp;1 | tail -20 &gt; /tmp/09-01-cbl_tout-tail.log`
  </action>
  <verify>
    <automated>
      tail -1 /tmp/09-01-cbl_tout-pre.log | grep -q "MODULES EXECUTABLES" &amp;&amp; \
      ls /tmp/09-01-pre-retire-baseline/pan2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-01-pre-retire-baseline/nafems_le1-qt-1x.png &amp;&amp; \
      ls /tmp/09-01-pre-retire-baseline/cavity2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-01-pre-retire-baseline/heat1d-qt-1x.png
    </automated>
  </verify>
  <acceptance_criteria>
    - bin/cbl_tout produces "TOUS les MODULES EXECUTABLES sans debogueur de $MEFISTO sous LINUX sont crees" (or English equivalent) at end-of-log AND exits 0.
    - At least 4 of 5 testa baseline PNGs exist under /tmp/09-01-pre-retire-baseline/.
    - nlsecu disposition is documented in 09-01-AUDIT-BASELINE.md (truncated/missing/partial — matches Phase 8 override #5 evidence).
  </acceptance_criteria>
  <done>v1.0-pre-retire commit verified buildable; 4-5 testa baselines captured; nlsecu status logged.</done>
</task>

<task type="auto">
  <name>Task 4: Write 09-01-AUDIT-BASELINE.md with empirical pre-retirement reference counts</name>
  <read_first>
    - xvue/xvuelc.c (line count)
    - xvue/video1.f, xvue/videofin.f, xvue/videonm.f
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md (Empirical totals section)
  </read_first>
  <action>
    Write `.planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md` containing:

    **§1 — Process gate confirmation:**
      Date STATE.md closure line was committed (or maintainer reply timestamp + initials).

    **§2 — Tag + branch creation:**
      `git rev-parse v1.0-pre-retire` SHA.
      `git rev-parse retire-restore-point` SHA (must match).
      Origin push status (yes/no/no-remote).

    **§3 — Pre-retirement reference counts** (running greps; record exact integer counts):
      | Reference | Count | Command |
      |-----------|-------|---------|
      | xvue/xvuelc.c lines | `wc -l xvue/xvuelc.c` (expect ~3749) | `wc -l xvue/xvuelc.c` |
      | -lX11 occurrences across bin/cb* | grep -c | `grep -l 'lX11' bin/cb* \| wc -l` |
      | /usr/X11R6/lib references | grep -c | `grep -rln '/usr/X11R6/lib' bin/cb* \| wc -l` |
      | convert shell-out callers in active source | grep -c | `grep -rln 'CALL SYSTEM.*convert\|^convert ' xvue/*.f flui/ ther/ util/ bin/convertepsgif bin/png2eps bin/png2jpg \| wc -l` |
      | xvue/video*.f line totals | sum of wc -l | `wc -l xvue/video1.f xvue/videofin.f xvue/videonm.f` |
      | LVIDEO call-site tracer files | 12 (per RESEARCH §Empirical totals) | `grep -rln 'CALL VIDEO\|LVIDEO .NE. 0' flui/ ther/ util/ \| sort -u \| wc -l` |
      | ABI symbol count (libxvueqt.a) | 58 | `nm xvue/qt/build/libxvueqt.a 2>/dev/null \| grep ' T ' \| grep '_$' \| wc -l` |
      | bin/cb* scripts referencing X11 | 32 (per RESEARCH §Empirical totals) | `grep -rln 'lX11\|X11R6' bin/cb* bin/Makefile* 2>/dev/null \| wc -l` |
      | README/LISEZMOI files referencing libX11-dev or imagemagick | 4 (per RESEARCH §Pitfall 7) | `grep -ln 'libX11-dev\|imagemagick' README LISEZMOI bin/README bin/LISEZMOI 2>/dev/null \| wc -l` |

    **§4 — Build verification:**
      - bin/cbl_tout exit code: 0
      - tail -3 /tmp/09-01-cbl_tout-pre.log
      - Build start/end timestamps

    **§5 — testa baseline capture log:**
      - For each of {pan2d, nafems_le1, cavity2d, heat1d, nlsecu}: PNG path,
        file size, AE vs phase 8 evidence (if computed via `compare`), or
        "carry-forward to Plan 9-07" annotation for nlsecu.

    **§6 — Carry-forward acknowledgment:**
      - "Plans 9-02..9-05 will validate post-deletion reference counts == 0."
      - "Plans 9-06..9-09 carry forward Phase 8 overrides; reference counts unchanged."

    Format the file as proper Markdown with headings + tables. Commit at end of Task 5.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md &amp;&amp; \
      grep -c "^## " .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md | grep -q "^[6-9]\|^1[0-9]" &amp;&amp; \
      grep -E "v1\.0-pre-retire" .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File 09-01-AUDIT-BASELINE.md exists with at least sections §1..§6
    - SHA of v1.0-pre-retire recorded
    - All 9 reference counts in §3 are populated with explicit integers (not TBD)
    - bin/cbl_tout exit code logged as 0
    - 4-5 testa cases logged with status
  </acceptance_criteria>
  <done>09-01-AUDIT-BASELINE.md written with all reference counts + verification log.</done>
</task>

<task type="auto">
  <name>Task 5: Commit Plan 9-01 artifacts</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md (just written)
  </read_first>
  <action>
    Commit the audit baseline file (the tag + branch are already pushed in Task 2):

    ```
    git add .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md
    git commit -m "docs(09-01): commit pre-retirement audit baseline (v1.0-pre-retire gate)

    Plan 9-01 establishes the rollback safety contract per CONTEXT.md D-08/D-09:
    - git tag v1.0-pre-retire created at this HEAD
    - git branch retire-restore-point created at the same commit
    - 09-01-AUDIT-BASELINE.md records empirical pre-retirement reference counts
      (xvuelc.c lines, libX11 / X11R6 / convert ref counts, video*.f totals,
      LVIDEO tracer count = 12, ABI count = 58, README/LISEZMOI dep file count = 4)
    - bin/cbl_tout exit 0 verified on this commit
    - 5 BUILD-10 testa baselines captured (nlsecu truncated per Phase 8 override #5)

    Wave 2 (Plans 9-02 .. 9-05) is now unblocked.
    "
    ```

    Verify the commit:
      `git log --oneline -1 | grep -q '09-01.*pre-retirement audit'`
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -E '09-01.*audit baseline|09-01.*pre-retire'</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit message contains "09-01" and references the audit baseline
    - 09-01-AUDIT-BASELINE.md is committed (in `git log -1 --name-only`)
  </acceptance_criteria>
  <done>Plan 9-01 audit baseline committed; Wave 2 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer ↔ Phase 9 executor | Process gate D-11 — closure line in STATE.md MUST exist before any deletion lands |
| Working tree ↔ git tag | The v1.0-pre-retire tag is the single rollback artifact for Phase 9 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-A | Tampering | git tag v1.0-pre-retire creation | mitigate | Idempotency check: STOP if tag already exists; do not overwrite. Annotated tag (-a) with explicit message captures intent |
| T-09-00-B | Repudiation | A/B window closure record | mitigate | Require STATE.md commit OR maintainer-reply quote in 09-01-AUDIT-BASELINE.md §1 (timestamp + initials) |
| T-09-00-C | Tampering | Pre-retirement audit counts | mitigate | All counts captured by deterministic grep commands documented in §3; Plans 9-02..9-05 re-run identical greps and assert delta |
| T-09-00-D | DoS | Tag-creation race (parallel executor) | accept | Single-author MEFISTO repo; idempotency check is sufficient |
</threat_model>

<verification>
- bin/cbl_tout exits 0 on the v1.0-pre-retire commit (Task 3)
- 5 BUILD-10 testa cases capture (4-5 PNGs land; nlsecu may truncate)
- 09-01-AUDIT-BASELINE.md has all 9 reference counts populated
- git tag --list v1.0-pre-retire returns the tag
- git rev-parse retire-restore-point matches v1.0-pre-retire SHA
</verification>

<success_criteria>
- [x] Maintainer A/B-closure confirmation recorded (Task 1)
- [x] git tag v1.0-pre-retire created and pushed (Task 2)
- [x] git branch retire-restore-point created and pushed (Task 2)
- [x] bin/cbl_tout exits 0 on this commit (Task 3)
- [x] 5 testa baselines captured to /tmp/09-01-pre-retire-baseline/ (Task 3)
- [x] 09-01-AUDIT-BASELINE.md exists with §1..§6 fully populated (Task 4)
- [x] Audit baseline committed to git (Task 5)
</success_criteria>

<output>
After completion, create `.planning/phases/09-retire-x11-backend/09-01-SUMMARY.md`
documenting:
- v1.0-pre-retire SHA
- Reference counts (will become "expected starting values" for Plans 9-02..9-05)
- Maintainer closure confirmation source
- bin/cbl_tout + testa verification log paths
</output>
