---
# Process gate: A/B window must be CLOSED before this plan executes (per 09-CONTEXT.md D-11)
# DENSE PLAN — monitor context exhaustion. Task 3 alone covers ~40 file edits via sed batch
# (cb*_qt rename + nompp body collapse + bin/ab_sweep_phase8.sh edits). Acceptable per
# revision iter1 WARNING #5 — no split required, but executors should checkpoint each task.
phase: 09-retire-x11-backend
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - bin/cbl_tout
  - bin/cbl_tout_qt
  - bin/cbinit
  - bin/cbmail
  - bin/cbelas
  - bin/cbflui
  - bin/cbther
  - bin/cbnlse
  - bin/cbxvtest0
  - bin/cbxvtest1
  - bin/cbxvtest2
  - bin/cbxvtest3
  - bin/cbxvtest4
  - bin/cbgadap
  - bin/cbgelas
  - bin/cbgflui
  - bin/cbginit
  - bin/cbginit1
  - bin/cbgmail
  - bin/cbgnlse
  - bin/cbgpara
  - bin/cbgparaddd
  - bin/cbgpoba
  - bin/cbgther
  - bin/cbpoba
  - bin/cbpara
  - bin/cbadap
  - bin/cbpppl
  - bin/cbonde
  - bin/cbbrezfort2d
  - bin/cbbrezfort3d
  - bin/Makefile
  - bin/MakefileIBM
  - bin/MakefileMefisto
  - bin/avecMOTIF
  - bin/instal_2disk.hp
  - bin/instal_2disk.src
  - bin/cbmail_qt
  - bin/cbelas_qt
  - bin/cbflui_qt
  - bin/cbther_qt
  - bin/cbnlse_qt
  - bin/cbinit_qt
  - bin/cbxvtest0_qt
  - bin/cbxvtest1_qt
  - bin/cbxvtest2_qt
  - bin/cbxvtest3_qt
  - bin/cbxvtest4_qt
  - bin/test_no_x11_in_build.sh
  - bin/ab_sweep_phase8.sh
  - xvue/qt/CMakeLists.txt
autonomous: true
requirements: ["RETIRE-02"]
must_haves:
  truths:
    - "No bin/cb* script contains -lX11 or /usr/X11R6 references (RETIRE-02 grep gate)"
    - "bin/cbl_tout_qt is renamed to bin/cbl_tout via git mv (history preserved)"
    - "Legacy bin/cb* scripts (init/mail/elas/flui/ther/nlse + xvtest0..4) deleted; their _qt counterparts renamed to drop the _qt suffix"
    - "Legacy parallel/MPI/HP-UX scripts (cbg*, cbpara*, cbpoba, cbonde, cbbrezfort{2d,3d}, Makefile*, avecMOTIF, instal_2disk*, cbadap, cbpppl) deleted (per RESEARCH OQ1)"
    - "All grep audits pass post-rename: grep -rln 'cbl_tout_qt' --include='*.md' --include='*.sh' --include='*.cpp' --include='*.txt' shows ONLY .planning/ historical references (those documenting Phase 7/8 build use); active build path uses bin/cbl_tout"
    - "bin/test_no_x11_in_build.sh exists and exits 0 on the new tree (clones the test_no_imagemagick_in_qt.sh pattern with regex 'lX11|/usr/X11R6')"
    - "xvue/qt/CMakeLists.txt has a verify_no_x11_in_build ALL target wired (mirrors verify_no_imagemagick_in_qt at lines 165-170)"
    - "bin/cbl_tout (post-rename) exits 0 and produces working pp/pp{init,mail,elas,flui,ther,nlse,xvtest0..4} (no _qt suffix)"
    - "pp/* binaries have no `_qt` suffix; harness ab_sweep_phase8.sh references pp/pp${MODULE} (not pp/pp${MODULE}_qt) — POLICY LOCKED per revision iter1: Phase 9 ships pp binaries WITHOUT `_qt` suffix (the suffix existed only for parallel-build A/B and is now obsolete)"
    - "Every renamed cb*_qt script has its `nompp=pp/pp<x>_qt` body line collapsed to `nompp=pp/pp<x>` (concrete pattern verified in pre-revision bin/cbmail_qt:line 4 → `nompp=pp/ppmail`)"
    - "bin/ab_sweep_phase8.sh lines 130/151/161/171 (the four `pp/pp\\${MODULE}_qt` invocations under qt-1x / qt-2x / qt-omp / prereq branches) collapse to `pp/pp\\${MODULE}` — no `_qt` suffix"
    - "5 BUILD-10 testa cases pass on the renamed Qt-only path"
  artifacts:
    - path: "bin/cbl_tout"
      provides: "Qt-only canonical build entry (renamed from bin/cbl_tout_qt)"
      min_lines: 30
    - path: "bin/test_no_x11_in_build.sh"
      provides: "RETIRE-02 grep gate"
      min_lines: 15
    - path: "xvue/qt/CMakeLists.txt"
      provides: "verify_no_x11_in_build ALL target appended after verify_no_imagemagick_in_qt"
      contains: "verify_no_x11_in_build"
    - path: "bin/ab_sweep_phase8.sh"
      provides: "Harness updated to reference pp/pp${MODULE} (no _qt suffix) per locked Phase 9 policy"
      contains: "pp/pp${MODULE}"
  key_links:
    - from: "xvue/qt/CMakeLists.txt verify_no_x11_in_build target"
      to: "bin/test_no_x11_in_build.sh"
      via: "COMMAND sh ... DEPENDS xvueqt"
      pattern: "verify_no_x11_in_build ALL"
    - from: "bin/cbl_tout"
      to: "bin/cbmail (no _qt suffix)"
      via: "shell-script invocation chain"
      pattern: "grep -E '\\$MEFISTO/bin/cb(mail|elas|flui|ther|nlse|init)\\b' bin/cbl_tout"
    - from: "renamed cb* script bodies (e.g., bin/cbmail)"
      to: "pp/pp<module> output (no _qt suffix)"
      via: "sed -i 's|nompp=pp/pp\\([a-z]*\\)_qt|nompp=pp/pp\\1|g' on each renamed script"
      pattern: "grep -E 'nompp=pp/pp[a-z]+(_qt)?' bin/cb{mail,elas,flui,ther,nlse,init,xvtest0,xvtest1,xvtest2,xvtest3,xvtest4}"
    - from: "bin/ab_sweep_phase8.sh qt-1x/qt-2x/qt-omp/prereq dispatch"
      to: "pp/pp${MODULE} invocation (no _qt suffix)"
      via: "sed -i 's|pp/pp\\${MODULE}_qt|pp/pp\\${MODULE}|g' bin/ab_sweep_phase8.sh"
      pattern: "grep -c 'pp/pp\\${MODULE}_qt' bin/ab_sweep_phase8.sh == 0 AND grep -c 'pp/pp\\${MODULE}' bin/ab_sweep_phase8.sh >= 4"
---

<objective>
RETIRE-02: Strip every libX11 / /usr/X11R6 linker reference from the build tree;
collapse the dual-build distinction (legacy X11 vs Qt) into a single Qt-only build;
delete all legacy cb* scripts that were producing X11 binaries; rename the Qt-only
build scripts to drop the `_qt` suffix; collapse pp/pp*_qt binary names to pp/pp*
(no suffix) inside cb script bodies AND inside bin/ab_sweep_phase8.sh harness.

Per RESEARCH §Architectural Map and OQ1/OQ2:
- 32 bin/cb* + bin/Makefile* scripts contain -lX11 or /usr/X11R6/lib references.
- Per OQ2 recommendation: `git rm bin/cbl_tout && git mv bin/cbl_tout_qt bin/cbl_tout`
  (single commit; cleanest git history; semantically identical to a rewrite).
- Per OQ1 recommendation: legacy scripts not in active build chain (cbg*, cbpara*,
  cbpoba, cbonde, cbbrezfort{2d,3d}, Makefile*, avecMOTIF, instal_2disk*) are deleted
  outright.

**REVISION ITER1 POLICY LOCK (BLOCKERs #2-#4):** Phase 9 ships pp binaries WITHOUT
`_qt` suffix (per CONTEXT.md spirit — "Qt 6 as single graphics backend"; the suffix
existed only for the parallel-build A/B window which is now obsolete). Both renamed
cb* script bodies AND bin/ab_sweep_phase8.sh must reference `pp/pp<module>` (no
suffix). The pre-revision baseline file `bin/cbmail_qt` line 4 contains
`nompp=pp/ppmail_qt`; this plan collapses that to `nompp=pp/ppmail`. The pre-revision
baseline file `bin/ab_sweep_phase8.sh` lines 130/151/161/171 all use
`pp/pp${MODULE}_qt`; this plan collapses each to `pp/pp${MODULE}`.

Note on CONTEXT.md D-06 deviation: this plan implements the cbl_tout rename via
`git mv` per researcher recommendation (RESEARCH §Pitfall 4 + OQ2). Legacy cb* are
deleted; _qt counterparts are renamed in lockstep. This is the cleanest git history
approach (no orphan-then-rename two-step).

Output:
- ~32 deletions (legacy bin/cb* scripts + Makefile* + 3 HP-UX/Motif relics)
- ~12 renames via `git mv` (cbl_tout_qt → cbl_tout; cb{init,mail,elas,flui,ther,nlse}_qt → cb{...}; cbxvtest{0..4}_qt → cbxvtest{0..4})
- ~12 in-script body sed edits collapsing `nompp=pp/pp<x>_qt` → `nompp=pp/pp<x>` in each renamed cb* script
- 4 bin/ab_sweep_phase8.sh edits at the documented line numbers (130/151/161/171)
- 1 new file (bin/test_no_x11_in_build.sh, modeled on bin/test_no_imagemagick_in_qt.sh)
- 1 file edit (xvue/qt/CMakeLists.txt: add verify_no_x11_in_build ALL target)
- bin/cbl_tout (post-rename) exits 0; 5 testa cases pass on the renamed binaries
  AT THEIR NEW NAMES (pp/pp{init,mail,elas,flui,ther,nlse,xvtest0..4} — no _qt).
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-retire-x11-backend/09-CONTEXT.md
@.planning/phases/09-retire-x11-backend/09-RESEARCH.md
@.planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md
@.planning/phases/09-retire-x11-backend/09-02-SUMMARY.md
@CLAUDE.md
@bin/test_no_imagemagick_in_qt.sh
@bin/cbl_tout_qt
@bin/cbmail_qt
@bin/ab_sweep_phase8.sh
@xvue/qt/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-rename downstream-tooling audit (Pitfall 4 / T-09-04 mitigation)</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 4"
    - bin/cbl_tout_qt
    - bin/cbl_tout
    - bin/cbmail_qt (line 4 — the pre-revision `nompp=pp/ppmail_qt` pattern)
    - bin/ab_sweep_phase8.sh (lines 130, 151, 161, 171 — the four `pp/pp${MODULE}_qt` invocations)
  </read_first>
  <action>
    Per RESEARCH §Pitfall 4: before renaming `cbl_tout_qt` → `cbl_tout`, audit ALL
    downstream references so they can be updated in lockstep (or knowingly left as
    historical). Per revision iter1 BLOCKERs #2-#4: ALSO audit every `pp/pp*_qt`
    reference because the `_qt` suffix is being collapsed off pp binaries in this plan.

    1. Find every reference to `cbl_tout_qt` in the tree (excluding worktree-* dirs):
       `grep -rln 'cbl_tout_qt' . \
            --include='*.md' --include='*.txt' --include='*.sh' \
            --include='*.cpp' --include='*.cmake' --include='CMakeLists.txt' \
            --exclude-dir='worktree-*' --exclude-dir='.git' \
            > /tmp/09-03-cbl_tout_qt-refs.log`

    2. Same audit for `cb{mail,elas,flui,ther,nlse,init,xvtest{0,1,2,3,4}}_qt`
       script names AND for `pp/pp*_qt` binary references (Plan 9-03 collapses both):
       `grep -rln 'cb\(mail\|elas\|flui\|ther\|nlse\|init\)_qt\|cbxvtest[0-4]_qt' . \
            --include='*.md' --include='*.txt' --include='*.sh' --include='*.cpp' \
            --exclude-dir='worktree-*' --exclude-dir='.git' \
            >> /tmp/09-03-cbl_tout_qt-refs.log`
       `grep -rln 'pp/pp[a-z]*_qt\|pp/pp\${MODULE}_qt' . \
            --include='*.md' --include='*.txt' --include='*.sh' --include='*.cpp' \
            --exclude-dir='worktree-*' --exclude-dir='.git' --exclude-dir='pp' \
            >> /tmp/09-03-cbl_tout_qt-refs.log`

    3. Confirm the concrete patterns the rest of this plan depends on:
       - `grep -n 'nompp=pp/pp' bin/cbmail_qt` should return `4:nompp=pp/ppmail_qt`
         (the canonical pattern; same shape repeats in every cb*_qt script).
       - `grep -nE 'pp/pp\$\{MODULE\}_qt' bin/ab_sweep_phase8.sh` should return
         four lines around 130, 151, 161, 171 (one per qt-* / prereq branch).

    4. Classify each reference from step 1-2:
       (a) Active build/tooling (must be updated): bin/cb*_qt scripts, xvue/qt/README.md,
           xvue/qt/CMakeLists.txt, .planning/codebase/*.md, bin/ab_sweep_phase8.sh
       (b) Historical (Phase 7/8 SUMMARY/CHECKLIST/CONTEXT/RESEARCH/VALIDATION docs):
           DO NOT EDIT — those documents capture state at their respective phase ends.
       (c) STATE.md/PROJECT.md decision logs: DO NOT EDIT for the same reason.

    5. Save the classified list to 09-03-RENAME-IMPACT.md (working scratch document):
       Write `.planning/phases/09-retire-x11-backend/09-03-RENAME-IMPACT.md` with two
       sections: "Update in lockstep" (category a) + "Leave as historical" (categories
       b and c). At minimum the §Update list MUST include:
       - bin/cbl_tout_qt itself
       - bin/cb{mail,elas,flui,ther,nlse,init,xvtest{0..4}}_qt
       - xvue/qt/README.md (if present)
       - xvue/qt/CMakeLists.txt
       - bin/ab_sweep_phase8.sh (lines 130/151/161/171)
       - bin/cbl_tout_qt body (the cb*_qt invocations it dispatches to)
       Do NOT update Phase 7/8 historical docs.
  </action>
  <verify>
    <automated>
      test -f /tmp/09-03-cbl_tout_qt-refs.log &amp;&amp; \
      test -f .planning/phases/09-retire-x11-backend/09-03-RENAME-IMPACT.md &amp;&amp; \
      grep -q 'bin/ab_sweep_phase8.sh' .planning/phases/09-retire-x11-backend/09-03-RENAME-IMPACT.md
    </automated>
  </verify>
  <acceptance_criteria>
    - /tmp/09-03-cbl_tout_qt-refs.log lists every cbl_tout_qt + cb*_qt + pp/pp*_qt reference in the tree
    - 09-03-RENAME-IMPACT.md classifies each as "update" or "historical"
    - §Update explicitly enumerates: bin/cbl_tout_qt, all cb*_qt scripts, xvue/qt/CMakeLists.txt, bin/ab_sweep_phase8.sh
  </acceptance_criteria>
  <done>Rename impact audit complete; downstream consumers identified (including ab_sweep + cb script bodies); historical doc list locked.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy bin/cb* X11 scripts + Makefile* + Motif/HP-UX relics</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Recommended Project Structure"
    - .planning/phases/09-retire-x11-backend/09-01-AUDIT-BASELINE.md §3 (32-script count)
  </read_first>
  <action>
    Delete all legacy X11 build scripts. Each one has -lX11 / /usr/X11R6 references AND
    references xvuelc.o (which Plan 9-02 just deleted). Per RESEARCH §Tools that GO and
    OQ1, the deletion list is:

    A. Legacy active-build cb* (each was producing pp/pp* with -lX11):
       `git rm bin/cbinit bin/cbmail bin/cbelas bin/cbflui bin/cbther bin/cbnlse \
              bin/cbxvtest0 bin/cbxvtest1 bin/cbxvtest2 bin/cbxvtest3 bin/cbxvtest4`

    B. Legacy out-of-active-build cb* (per RESEARCH OQ1):
       `git rm bin/cbgadap bin/cbgelas bin/cbgflui bin/cbginit bin/cbginit1 \
              bin/cbgmail bin/cbgnlse bin/cbgpara bin/cbgparaddd bin/cbgpoba bin/cbgther \
              bin/cbpoba bin/cbpara bin/cbadap bin/cbpppl bin/cbonde \
              bin/cbbrezfort2d bin/cbbrezfort3d`

    C. HP-UX/IBM Makefile relics:
       `git rm bin/Makefile bin/MakefileIBM bin/MakefileMefisto`

    D. Comment-only legacy file + HP-UX dual-disk installer (per RESEARCH §State of the Art):
       `git rm bin/avecMOTIF bin/instal_2disk.hp bin/instal_2disk.src`

    Verify each deletion landed:
       `git status --porcelain | grep -E '^D ' | wc -l`
       Should be ≥ 32 (matches RESEARCH §Empirical totals).

    Verify no `-lX11` / `/usr/X11R6` references remain in surviving bin/cb* scripts:
       `grep -rln 'lX11\|X11R6' bin/cb* bin/Makefile* 2&gt;/dev/null | wc -l`
       Should be 0 (only bin/cb*_qt scripts remain — Task 3 renames them; they don't
       reference X11). If non-zero, list the offenders and STOP for review.

    DO NOT commit yet — Task 3 chains the rename in the same conceptual commit but
    they're separated for clarity.
  </action>
  <verify>
    <automated>
      [ "$(grep -rln 'lX11\|X11R6' bin/cb* bin/Makefile* 2&gt;/dev/null | wc -l)" = "0" ] &amp;&amp; \
      [ "$(git status --porcelain | grep -E '^D  bin/cb' | wc -l)" -ge "20" ] &amp;&amp; \
      ! test -f bin/cbinit &amp;&amp; ! test -f bin/cbmail &amp;&amp; ! test -f bin/cbelas &amp;&amp; \
      ! test -f bin/Makefile &amp;&amp; ! test -f bin/avecMOTIF
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero remaining bin/cb* scripts contain `lX11` or `X11R6` (excluding renamed _qt counterparts)
    - At least 20 deletions staged (`git status --porcelain | grep ^D bin/cb`)
    - Specific files confirmed deleted: cbinit, cbmail, cbelas, cbflui, cbther, cbnlse, cbxvtest0..4, Makefile, avecMOTIF, instal_2disk.*
  </acceptance_criteria>
  <done>~32 legacy scripts staged for deletion; zero -lX11 / X11R6 references in surviving bin/cb*.</done>
</task>

<task type="auto">
  <name>Task 3: Rename cbl_tout_qt → cbl_tout + cb{init,mail,elas,flui,ther,nlse}_qt → cb{...} + cbxvtest{0..4}_qt → cbxvtest{0..4} + collapse pp/pp*_qt → pp/pp* in renamed cb script bodies (BLOCKERs #2-#3)</name>
  <read_first>
    - bin/cbl_tout_qt (the file being renamed)
    - bin/cbmail_qt (line 4: `nompp=pp/ppmail_qt` — concrete pre-revision pattern)
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pitfall 4: Renaming cbl_tout_qt"
  </read_first>
  <action>
    Use `git mv` to preserve history (per RESEARCH §Pitfall 4 anti-patterns), then
    collapse `pp/pp*_qt` → `pp/pp*` inside each renamed script body via batched sed
    (revision iter1 BLOCKER #2 / #3).

    1. Confirm the legacy non-_qt versions are now deleted (Task 2):
       `! test -f bin/cbinit &amp;&amp; ! test -f bin/cbmail &amp;&amp; ! test -f bin/cbl_tout`
       Wait — `bin/cbl_tout` IS still on disk from Plan 9-02 (it just no longer invokes
       ccxvue). It must ALSO be deleted before the rename:
       `git rm bin/cbl_tout`
       (now legacy cbl_tout deleted; cb*_qt scripts remain to be renamed.)

    2. Rename in lockstep:
       ```
       git mv bin/cbl_tout_qt bin/cbl_tout
       git mv bin/cbinit_qt bin/cbinit  # if present
       git mv bin/cbmail_qt bin/cbmail
       git mv bin/cbelas_qt bin/cbelas
       git mv bin/cbflui_qt bin/cbflui
       git mv bin/cbther_qt bin/cbther
       git mv bin/cbnlse_qt bin/cbnlse
       git mv bin/cbxvtest0_qt bin/cbxvtest0
       git mv bin/cbxvtest1_qt bin/cbxvtest1
       git mv bin/cbxvtest2_qt bin/cbxvtest2
       git mv bin/cbxvtest3_qt bin/cbxvtest3
       git mv bin/cbxvtest4_qt bin/cbxvtest4
       ```
       (For each: if source file does not exist, skip silently — some _qt counterparts
       may not exist for every legacy script.)

    3. **Collapse `pp/pp*_qt` → `pp/pp*` inside the body of every renamed cb* script
       (revision iter1 BLOCKER #2):** the pre-revision pattern in bin/cbmail_qt line 4
       is `nompp=pp/ppmail_qt`; after rename the file is bin/cbmail and the body line
       must become `nompp=pp/ppmail`. Use a SINGLE narrow sed pass that targets ONLY
       the `nompp=pp/pp<lowercase-name>_qt` shape (so `xvue/qt/build/` directory
       references — which legitimately keep `_qt` — are preserved):

       ```
       for f in bin/cbl_tout bin/cb{init,mail,elas,flui,ther,nlse} bin/cbxvtest{0,1,2,3,4}; do
           [ -f "$f" ] || continue
           # Collapse `nompp=pp/pp<name>_qt` body line:
           sed -i 's|nompp=pp/pp\([a-z0-9]*\)_qt|nompp=pp/pp\1|g' "$f"
           # Collapse any echo / message lines that name the binary, e.g.
           # `echo "$MEFISTO/$nompp est CORRECT"` references survive unchanged
           # because they reference $nompp; safe.
       done
       ```

       Concrete verification — bin/cbmail line 4 must now read exactly:
       `nompp=pp/ppmail`
       Run:
       `grep -n '^nompp=' bin/cbmail bin/cbelas bin/cbflui bin/cbther bin/cbnlse bin/cbinit bin/cbxvtest0 bin/cbxvtest1 bin/cbxvtest2 bin/cbxvtest3 bin/cbxvtest4 2>/dev/null`
       Each line must end with `pp/pp<name>` (no `_qt`).

    4. Update bin/cbl_tout's per-module `cb*_qt` invocations (the dispatcher loop —
       these reference the SCRIPT names, not pp output names; Task 3 step 2 already
       renamed the scripts, so cbl_tout must invoke them at their new names):
       Likewise, bin/cbl_tout (post-rename) likely contains lines invoking
       `$MEFISTO/bin/cbmail_qt` etc.; update those to the new names. Use a targeted
       sed (NOT `s/_qt//g` — that would corrupt the legitimate `xvue/qt/` directory
       reference at `$QT_LIBS=$(pkg-config --libs Qt6Widgets...)` etc. — only the
       script-name occurrences should change):
       `sed -i 's|\(cb\(mail\|elas\|flui\|ther\|nlse\|init\|xvtest[0-4]\)\)_qt|\1|g' bin/cbl_tout`

    5. Sanity:
       - `test -x bin/cbl_tout` (executable bit preserved by git mv)
       - `! grep -E 'cb(mail|elas|flui|ther|nlse|init|xvtest[0-4])_qt' bin/cbl_tout`
       - `! test -f bin/cbl_tout_qt`
       - `! grep -E 'nompp=pp/pp[a-z0-9]+_qt' bin/cb{l_tout,init,mail,elas,flui,ther,nlse,xvtest0,xvtest1,xvtest2,xvtest3,xvtest4} 2>/dev/null` (zero `_qt` suffix in nompp body lines)

    6. Update xvue/qt/README.md (if it references cbl_tout_qt):
       `grep -n 'cbl_tout_qt' xvue/qt/README.md`
       Replace `cbl_tout_qt` → `cbl_tout` with explanatory note (e.g., "(formerly
       cbl_tout_qt — Phase 9 RETIRE-02 dropped the suffix when retiring the X11 backend)").
  </action>
  <verify>
    <automated>
      test -f bin/cbl_tout &amp;&amp; test -x bin/cbl_tout &amp;&amp; \
      ! test -f bin/cbl_tout_qt &amp;&amp; \
      [ "$(grep -E 'cb(mail|elas|flui|ther|nlse|init|xvtest[0-4])_qt' bin/cbl_tout 2&gt;/dev/null | wc -l)" = "0" ] &amp;&amp; \
      [ "$(grep -hE 'nompp=pp/pp[a-z0-9]+_qt' bin/cbl_tout bin/cbinit bin/cbmail bin/cbelas bin/cbflui bin/cbther bin/cbnlse bin/cbxvtest0 bin/cbxvtest1 bin/cbxvtest2 bin/cbxvtest3 bin/cbxvtest4 2&gt;/dev/null | wc -l)" = "0" ] &amp;&amp; \
      grep -q '^nompp=pp/ppmail$' bin/cbmail
    </automated>
  </verify>
  <acceptance_criteria>
    - bin/cbl_tout exists, is executable, contains the Qt-only build body (formerly cbl_tout_qt)
    - bin/cbl_tout_qt does not exist
    - bin/cb{init,mail,elas,flui,ther,nlse} exist with no `_qt` suffix
    - bin/cbxvtest{0..4} exist with no `_qt` suffix
    - bin/cbl_tout has zero `_qt`-suffixed cb* invocations
    - Every renamed cb* script body has `nompp=pp/pp<name>` (no `_qt`); concrete check: `grep '^nompp=pp/ppmail$' bin/cbmail` returns the line
  </acceptance_criteria>
  <done>Rename complete via git mv; bin/cbl_tout is the canonical Qt-only build entry; pp/pp*_qt body references collapsed to pp/pp*; downstream invocations updated.</done>
</task>

<task type="auto">
  <name>Task 4: Edit bin/ab_sweep_phase8.sh — collapse pp/pp${MODULE}_qt → pp/pp${MODULE} (BLOCKER #4) + pre-Qt-sweep harness assertion</name>
  <read_first>
    - bin/ab_sweep_phase8.sh (lines 130, 151, 161, 171 — the four `pp/pp${MODULE}_qt` invocations under prereq, qt-1x, qt-2x, qt-omp)
  </read_first>
  <action>
    Per revision iter1 BLOCKER #4: bin/ab_sweep_phase8.sh hard-codes
    `pp/pp${MODULE}_qt` at lines 130, 151, 161, 171. Phase 9 ships pp binaries
    WITHOUT `_qt` suffix (policy locked above). Update the four invocations.

    1. Confirm the four lines are still at the documented offsets (the file may have
       drifted since the revision baseline was captured — verify before editing):
       `grep -nE 'pp/pp\$\{MODULE\}_qt' bin/ab_sweep_phase8.sh`
       Expected: four matches, around lines 130/151/161/171, one each in the
       PREREQ_MODULE branch + qt-1x + qt-2x + qt-omp branches.

       If line numbers drift, that's fine — the sed below targets the literal
       pattern, not a line number. Record the actual matched line numbers in the
       Plan summary.

    2. Apply the targeted edit (only the `${MODULE}_qt` literal — no other `_qt`
       occurrences in the harness should be touched):
       `sed -i 's|pp/pp\${MODULE}_qt|pp/pp${MODULE}|g' bin/ab_sweep_phase8.sh`

       (The `${MODULE}` is a bash-variable reference inside the script; the sed
       pattern matches it literally. The escape `\$` in sed is NOT needed here
       because we're matching `$` literally inside single-quotes; double-check
       the resulting file with the grep below.)

       Equivalent explicit Edit-tool form (if sed is undesirable):
       For each of the four lines, replace `"$MEFISTO/pp/pp${MODULE}_qt"` with
       `"$MEFISTO/pp/pp${MODULE}"` and (for the prereq branch) replace
       `"$MEFISTO/pp/pp${PREREQ_MODULE}_qt"` with `"$MEFISTO/pp/pp${PREREQ_MODULE}"`.

    3. Verify the four changes landed:
       `grep -c 'pp/pp\${MODULE}_qt' bin/ab_sweep_phase8.sh` → 0
       `grep -c 'pp/pp\${MODULE}' bin/ab_sweep_phase8.sh` → ≥ 3 (three qt-* branches)
       `grep -c 'pp/pp\${PREREQ_MODULE}_qt' bin/ab_sweep_phase8.sh` → 0
       `grep -c 'pp/pp\${PREREQ_MODULE}' bin/ab_sweep_phase8.sh` → ≥ 1 (prereq branch)

    4. Pre-Qt-sweep assertion (executor-level safety gate per revision iter1
       Task 5 verification add-on): record the exact regex assertion that Task 5
       runs BEFORE invoking the harness. The assertion is now placed in this
       Task 4 to keep the harness-edit + harness-assertion together:
       Before any subsequent Task 5 sweep:
       `grep -q 'pp/pp\${MODULE}' bin/ab_sweep_phase8.sh && ! grep -q 'pp/pp\${MODULE}_qt' bin/ab_sweep_phase8.sh`
       If this fails, STOP — Task 4 is incomplete; do NOT run the qt-1x sweep
       (it would invoke nonexistent binaries).
  </action>
  <verify>
    <automated>
      [ "$(grep -c 'pp/pp\${MODULE}_qt' bin/ab_sweep_phase8.sh 2&gt;/dev/null)" = "0" ] &amp;&amp; \
      [ "$(grep -c 'pp/pp\${PREREQ_MODULE}_qt' bin/ab_sweep_phase8.sh 2&gt;/dev/null)" = "0" ] &amp;&amp; \
      grep -q 'pp/pp\${MODULE}' bin/ab_sweep_phase8.sh &amp;&amp; \
      grep -q 'pp/pp\${PREREQ_MODULE}' bin/ab_sweep_phase8.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero `pp/pp${MODULE}_qt` and zero `pp/pp${PREREQ_MODULE}_qt` references in bin/ab_sweep_phase8.sh
    - At least 3 `pp/pp${MODULE}` references (qt-1x, qt-2x, qt-omp) and ≥1 `pp/pp${PREREQ_MODULE}` reference (prereq branch)
    - Pre-Qt-sweep assertion (the verify line above) succeeds
  </acceptance_criteria>
  <done>bin/ab_sweep_phase8.sh harness aligned with policy lock — references pp/pp${MODULE} (no _qt) at all four invocation sites.</done>
</task>

<task type="auto">
  <name>Task 5: Add bin/test_no_x11_in_build.sh + verify_no_x11_in_build CMake ALL target</name>
  <read_first>
    - bin/test_no_imagemagick_in_qt.sh (the pattern being cloned)
    - xvue/qt/CMakeLists.txt lines 155-170 (the pattern being cloned)
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Wave 0 Gaps"
  </read_first>
  <action>
    Add a build-time grep gate that prevents X11 references from re-entering the tree
    (per RESEARCH §Wave 0 Gaps + §Don't Hand-Roll: clone the verify_no_imagemagick_in_qt
    pattern with the regex 'lX11|/usr/X11R6').

    1. Create `bin/test_no_x11_in_build.sh` (clone bin/test_no_imagemagick_in_qt.sh
       structure; substitute regex):

       ```bash
       #!/bin/bash
       # bin/test_no_x11_in_build.sh — Phase 9 Plan 9-03 (RETIRE-02)
       #
       # CI-style assertion: the post-Phase-9 build must NEVER reference X11.
       # Scope: bin/cb* + bin/Makefile* + xvue/qt/ (the active build path).
       # Out of scope: .planning/ (historical docs may reference X11 contextually),
       # xvue/qt/build/ (CMake-generated, gets clobbered), worktree-*/ (cross-tag
       # work for Plan 9-08 by design contains X11 from v1.0-pre-retire).
       #
       # Exit codes:
       #   0  — gate passes
       #   1  — gate fails (X11 references found)
       #   2  — environment misconfigured

       set -e
       if [ -z "$MEFISTO" ]; then
           SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
           REPO_ROOT="$(dirname "$SCRIPT_DIR")"
           if [ -d "$REPO_ROOT/xvue/qt" ]; then
               MEFISTO="$REPO_ROOT"
           else
               echo "test_no_x11_in_build.sh: \$MEFISTO unset and could not auto-detect from $SCRIPT_DIR" >&2
               exit 2
           fi
       fi
       cd "$MEFISTO"

       # Run the grep on bin/cb* + xvue/qt/. Exclude .planning/ (historical), build/
       # (generated), worktree-*/ (cross-tag artifact dirs).
       MATCHES=$(grep -rln '\-lX11\|\-lXt\|/usr/X11R6\|/usr/X11R5' \
           bin/cb* bin/Makefile* xvue/qt/ 2>/dev/null \
           | grep -v 'xvue/qt/build/' \
           | grep -v '\.planning/' \
           | grep -v 'worktree-' || true)
       if [ -n "$MATCHES" ]; then
           echo "FAIL: post-Phase-9 build path still references X11:" >&2
           echo "$MATCHES" >&2
           exit 1
       fi
       echo "OK: no X11 references in active build path"
       exit 0
       ```

       Make executable: `chmod +x bin/test_no_x11_in_build.sh`.

    2. Append a `verify_no_x11_in_build ALL` target to xvue/qt/CMakeLists.txt right
       after the existing `verify_no_imagemagick_in_qt` target (around line 170):

       ```cmake
       # Phase 9 Plan 9-03 (RETIRE-02): post-retirement gate. Mirrors
       # verify_no_imagemagick_in_qt structure. Fails the build if any X11
       # linker line or path resurfaces under bin/cb* + xvue/qt/.
       add_custom_target(verify_no_x11_in_build ALL
           COMMAND ${CMAKE_COMMAND} -E echo "verify_no_x11_in_build: scanning..."
           COMMAND sh ${CMAKE_CURRENT_SOURCE_DIR}/../../bin/test_no_x11_in_build.sh
           DEPENDS xvueqt
           VERBATIM
       )
       ```

    3. Test the gate works:
       `bin/test_no_x11_in_build.sh`
       Should print "OK: no X11 references in active build path" and exit 0.

       Negative test (intentionally inject one X11 reference and verify gate fails):
       `echo "  -lX11" >> bin/cbmail`
       `bin/test_no_x11_in_build.sh; echo "exit=$?"`
       Should print FAIL and exit 1.
       Revert: `git checkout bin/cbmail`
  </action>
  <verify>
    <automated>
      test -x bin/test_no_x11_in_build.sh &amp;&amp; \
      bin/test_no_x11_in_build.sh &amp;&amp; \
      grep -q "verify_no_x11_in_build" xvue/qt/CMakeLists.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - bin/test_no_x11_in_build.sh exists, is executable, and exits 0 on the current tree
    - xvue/qt/CMakeLists.txt contains a `verify_no_x11_in_build ALL` target with `DEPENDS xvueqt`
    - Negative test: injecting `-lX11` into any bin/cb* script makes the gate fail; revert leaves it green
  </acceptance_criteria>
  <done>RETIRE-02 grep gate active; CMake wired; injection-test confirms it works.</done>
</task>

<task type="auto">
  <name>Task 6: Full Qt-only rebuild + 5-case testa baseline + commit</name>
  <read_first>
    - bin/cbl_tout (post-rename)
    - bin/ab_sweep_phase8.sh (post-edit per Task 4 — pp/pp${MODULE} no _qt)
  </read_first>
  <action>
    Validate the renamed build path end-to-end, with the pre-Qt-sweep assertion
    enforced (revision iter1 add-on).

    1. **PRE-SWEEP ASSERTION (revision iter1 — Task 5 verification add-on, hoisted
       to Task 6 since this is where the Qt sweep actually runs):**
       Before invoking bin/ab_sweep_phase8.sh below, assert the harness references
       pp/pp${MODULE} (no _qt). If this fails, STOP — Task 4 was incomplete:
       ```
       grep -q 'pp/pp\${MODULE}' bin/ab_sweep_phase8.sh && \
         ! grep -q 'pp/pp\${MODULE}_qt' bin/ab_sweep_phase8.sh || \
         { echo "FATAL: bin/ab_sweep_phase8.sh still references pp/pp\${MODULE}_qt — Task 4 incomplete" >&2; exit 1; }
       ```

    2. Clean Qt build:
       `rm -rf xvue/qt/build pp/pp*`
       `bin/cbl_tout 2&gt;&amp;1 | tee /tmp/09-03-cbl_tout.log`
       Confirm exit 0; the verify_no_x11_in_build target must run during this build
       (look for "verify_no_x11_in_build: scanning... OK" in the log).

    3. Confirm the renamed pp/* binaries exist (no _qt suffix — policy lock):
       `for x in ppinit ppmail ppelas ppflui ppther ppnlse ppxvtest0 ppxvtest1 ppxvtest2 ppxvtest3 ppxvtest4; do \
            test -f pp/$x || echo "MISSING pp/$x"; done`
       `! ls pp/pp*_qt 2>/dev/null` (must show no `_qt`-suffixed pp binaries)

    4. Run 5-case Qt sweep (NB: ab_sweep_phase8.sh now references pp/pp${MODULE}
       per Task 4 — so the sweep dispatches to the new pp/pp* names directly):
       `bin/ab_sweep_phase8.sh --mode qt-1x \
            --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu \
            --out-dir /tmp/09-03-post-retire02 2&gt;&amp;1 | tee /tmp/09-03-sweep.log`

    5. Confirm 4-5 PNGs land:
       `for c in pan2d nafems_le1 cavity2d heat1d; do \
            test -f /tmp/09-03-post-retire02/$c-qt-1x.png || echo "MISSING $c"; done`
       4/4 must exist; nlsecu carries to Plan 9-07.

    6. Re-run the gates standalone:
       `bin/test_no_imagemagick_in_qt.sh && bin/test_no_x11_in_build.sh`
       Both exit 0.

    7. Commit. Stage everything from Tasks 2-5:
       ```
       git add -A bin/cb* bin/Makefile* bin/avecMOTIF bin/instal_2disk.* \
                  bin/test_no_x11_in_build.sh \
                  bin/ab_sweep_phase8.sh \
                  xvue/qt/CMakeLists.txt \
                  xvue/qt/README.md
       git status --porcelain | head -50
       git commit -m "feat(09-03): RETIRE-02 — strip libX11 from build + collapse to single Qt-only path + drop _qt suffix from pp/* binaries

       Per RESEARCH §Architectural Map and OQ1/OQ2, plus revision iter1 BLOCKERs
       #2-#4 policy lock (pp binaries WITHOUT _qt suffix; the suffix existed only
       for parallel-build A/B and is now obsolete):

       Deletions (~32):
       - Legacy bin/cb* + bin/Makefile* scripts that linked -lX11 / Xt and referenced
         /usr/X11R6/lib (active: cbinit/mail/elas/flui/ther/nlse/xvtest0..4;
         out-of-active: cbg*/cbpara*/cbpoba/cbonde/cbbrezfort{2d,3d}/cbadap/cbpppl;
         legacy: Makefile/MakefileIBM/MakefileMefisto/avecMOTIF/instal_2disk.{hp,src})

       Renames via git mv (~12):
       - bin/cbl_tout_qt → bin/cbl_tout (single Qt-only build entry; cleanest history per OQ2)
       - bin/cb{init,mail,elas,flui,ther,nlse}_qt → bin/cb{...}
       - bin/cbxvtest{0..4}_qt → bin/cbxvtest{0..4}

       Body collapses (revision iter1 BLOCKER #2):
       - In every renamed cb* script: \`nompp=pp/pp<name>_qt\` → \`nompp=pp/pp<name>\`
         via \`sed -i 's|nompp=pp/pp\\\\([a-z0-9]*\\\\)_qt|nompp=pp/pp\\\\1|g'\`.
         Concrete: bin/cbmail line 4 now reads \`nompp=pp/ppmail\` (was
         \`nompp=pp/ppmail_qt\` per pre-revision baseline).

       Harness collapse (revision iter1 BLOCKER #3-#4):
       - bin/ab_sweep_phase8.sh lines 130/151/161/171: \`pp/pp\\\\\${MODULE}_qt\` →
         \`pp/pp\\\\\${MODULE}\` (qt-1x, qt-2x, qt-omp, prereq branches).

       cbl_tout dispatcher updates:
       - Internal \`cb<module>_qt\` invocations dropped to \`cb<module>\` via targeted
         sed (preserves xvue/qt/ directory references).

       Build infrastructure (Wave 0 Gap):
       - New: bin/test_no_x11_in_build.sh + verify_no_x11_in_build ALL target in
         xvue/qt/CMakeLists.txt — RETIRE-02 grep gate (mirrors verify_no_imagemagick_in_qt)
       - Update xvue/qt/README.md to reflect renamed cbl_tout

       Verification:
       - Pre-sweep assertion: grep -q 'pp/pp\\\\\${MODULE}' bin/ab_sweep_phase8.sh AND
         ! grep -q 'pp/pp\\\\\${MODULE}_qt' bin/ab_sweep_phase8.sh — both pass
       - bin/cbl_tout (post-rename) exits 0
       - 4/5 testa baselines capture (nlsecu carries to Plan 9-07 per Phase 8 override #5)
       - bin/test_no_x11_in_build.sh exits 0; injection-test confirms gate works
       - bin/test_no_imagemagick_in_qt.sh still exits 0 (untouched)
       - pp/pp{init,mail,elas,flui,ther,nlse,xvtest0..4} exist (no _qt suffix)
       - ! ls pp/pp*_qt (no _qt-suffixed pp binaries on disk)

       Threats mitigated:
       - T-09-04: Pre-rename audit captured downstream cbl_tout_qt + pp/pp*_qt
         references in /tmp/09-03-cbl_tout_qt-refs.log; .planning/ historical docs
         not modified

       Closes RETIRE-02 + revision iter1 BLOCKERs #2/#3/#4."
       ```
  </action>
  <verify>
    <automated>
      tail -1 /tmp/09-03-cbl_tout.log | grep -qiE "MODULES|crees|created" &amp;&amp; \
      bin/test_no_x11_in_build.sh &amp;&amp; \
      bin/test_no_imagemagick_in_qt.sh &amp;&amp; \
      grep -q 'pp/pp\${MODULE}' bin/ab_sweep_phase8.sh &amp;&amp; \
      ! grep -q 'pp/pp\${MODULE}_qt' bin/ab_sweep_phase8.sh &amp;&amp; \
      ls /tmp/09-03-post-retire02/pan2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-03-post-retire02/nafems_le1-qt-1x.png &amp;&amp; \
      ls /tmp/09-03-post-retire02/cavity2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-03-post-retire02/heat1d-qt-1x.png &amp;&amp; \
      test -f pp/ppmail &amp;&amp; ! test -f pp/ppmail_qt &amp;&amp; \
      git log --oneline -1 | grep -E '09-03.*RETIRE-02'
    </automated>
  </verify>
  <acceptance_criteria>
    - Pre-sweep assertion succeeds (harness uses pp/pp${MODULE}, no _qt)
    - bin/cbl_tout exits 0 and the build log contains "verify_no_x11_in_build: scanning... OK"
    - 4 of 5 testa cases capture PNGs cleanly
    - Both grep gates exit 0 (test_no_x11_in_build.sh + test_no_imagemagick_in_qt.sh)
    - pp/* binaries exist without _qt suffix (e.g., pp/ppmail) and `pp/pp*_qt` glob is empty
    - Latest commit message contains "09-03" and "RETIRE-02"
  </acceptance_criteria>
  <done>RETIRE-02 commit landed; build collapsed to single Qt-only path; pp binaries shipped without _qt suffix; harness aligned; X11 grep gate live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renamed cbl_tout ↔ downstream tooling | Phase 7/8 historical docs reference cbl_tout_qt; those references must not break (read-only historical), but active tools must use the new name |
| bin/test_no_x11_in_build.sh ↔ CMake ALL build | The gate must detect re-injection of -lX11 / X11R6; mirrors verify_no_imagemagick_in_qt |
| pp/* binary names ↔ harness invocations (BLOCKER #2-#4) | Phase 9 ships pp without `_qt` suffix; cb script bodies and bin/ab_sweep_phase8.sh must reference pp/pp<name> consistently — any drift means harness invokes nonexistent binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | cbl_tout_qt rename breaks downstream tooling | mitigate | Task 1 grep audit captures all cbl_tout_qt references BEFORE rename; classify as "active" (update) vs "historical" (leave); Task 3 step 6 explicitly updates xvue/qt/README.md and the renamed scripts' internal references |
| T-09-04-A | Tampering | Re-injection of -lX11 in future commits | mitigate | bin/test_no_x11_in_build.sh + verify_no_x11_in_build ALL CMake target — same pattern as verify_no_imagemagick_in_qt; Task 5 negative-test confirms it actually fails when X11 reappears |
| T-09-04-B | DoS | Rename collision (bin/cbl_tout already exists) | mitigate | Task 3 step 1 explicitly `git rm bin/cbl_tout` before `git mv bin/cbl_tout_qt bin/cbl_tout`; the old cbl_tout was already gutted by Plan 9-02 (only ccxvue line removed) and is now redundant |
| T-09-04-C | Tampering (revision iter1 BLOCKER #2) | Renamed cb scripts emit pp/pp*_qt outputs that no longer match policy | mitigate | Task 3 step 3 sed batch on `nompp=pp/pp<x>_qt` → `nompp=pp/pp<x>`; Task 6 verify checks pp/* binaries exist without _qt suffix |
| T-09-04-D | Tampering (revision iter1 BLOCKER #4) | bin/ab_sweep_phase8.sh invokes pp/pp${MODULE}_qt which no longer exists post-rename | mitigate | Task 4 sed targets the four `pp/pp${MODULE}_qt` invocations at lines 130/151/161/171; Task 6 hoists the pre-sweep assertion (BLOCKER #4 from revision iter1 add-on) so the harness is verified before the qt-1x sweep runs |
</threat_model>

<verification>
- Pre-sweep assertion (revision iter1 BLOCKER #4 add-on): `grep -q 'pp/pp${MODULE}' bin/ab_sweep_phase8.sh && ! grep -q 'pp/pp${MODULE}_qt' bin/ab_sweep_phase8.sh` succeeds
- bin/cbl_tout exits 0 (post-rename)
- bin/test_no_x11_in_build.sh exits 0 on the new tree
- bin/test_no_imagemagick_in_qt.sh still exits 0 (untouched)
- xvue/qt/CMakeLists.txt has `verify_no_x11_in_build ALL` target
- 4 of 5 testa cases capture PNGs (nlsecu → 9-07)
- grep -rln 'lX11|X11R6' bin/cb* bin/Makefile* → 0 matches
- ! test -f bin/cbl_tout_qt && test -f bin/cbl_tout
- No `nompp=pp/pp[a-z0-9]+_qt` in any renamed cb* script (BLOCKER #2)
- pp/pp{init,mail,elas,flui,ther,nlse,xvtest0..4} exist without `_qt` suffix
- ! ls pp/pp*_qt (zero `_qt`-suffixed pp binaries on disk)
- git log --oneline -1 contains "09-03" and "RETIRE-02"
</verification>

<success_criteria>
- [x] Pre-rename audit complete (Task 1)
- [x] ~32 legacy bin/cb* + Makefile + relics deleted (Task 2)
- [x] cbl_tout_qt + 11 cb*_qt counterparts renamed via git mv (Task 3)
- [x] `nompp=pp/pp<x>_qt` → `nompp=pp/pp<x>` collapsed in every renamed cb* script body (Task 3 BLOCKER #2)
- [x] bin/ab_sweep_phase8.sh lines 130/151/161/171 collapsed pp/pp${MODULE}_qt → pp/pp${MODULE} (Task 4 BLOCKERs #3-#4)
- [x] bin/test_no_x11_in_build.sh + CMake target landed (Task 5)
- [x] Pre-Qt-sweep harness assertion enforced + bin/cbl_tout exits 0 + 4 testa cases pass + commit (Task 6)
</success_criteria>

<output>
After completion, create `.planning/phases/09-retire-x11-backend/09-03-SUMMARY.md`
documenting:
- File deletion count (~32)
- File rename count (~12 via git mv)
- Per-script body sed edit count (~12; one `nompp=` line per renamed cb* script)
- bin/ab_sweep_phase8.sh edit line numbers (the actual matched offsets, since the
  baseline 130/151/161/171 may drift)
- New grep-gate file + CMake target
- Downstream tooling impact (Phase 7/8 historical docs unchanged)
- bin/cbl_tout post-rename invocation flow (cb<module> dispatch + pp/pp<name> output)
- Pre-sweep assertion result (BLOCKER #4 add-on)
- Confirmation that pp/pp*_qt glob is empty post-build (no _qt-suffixed binaries on disk)
</output>
