---
# Process gate: A/B window must be CLOSED before this plan executes (per 09-CONTEXT.md D-11)
phase: 09-retire-x11-backend
plan: 06
type: execute
wave: 3
depends_on: ["09-05"]
files_modified:
  - xvue/qt/src/xvue_qt_api.cpp
  - xvue/qt/README.md
  - bin/ab_sweep_phase8.sh
autonomous: false
requirements: []  # Carry-forward from Phase 8 override #1 — no new REQ-NN; closes the 14 CHECK cells
must_haves:
  truths:
    - "MEFISTO_QT_WINDOW_SIZE=WIDTHxHEIGHT env var is honored by xvinitgraphique_ (or equivalent init point) to size the canvas backing pixmap to the env-specified dim"
    - "When MEFISTO_QT_WINDOW_SIZE is unset, default behavior is unchanged (interactive UX preserved per T-09-05)"
    - "Env var is honored ONLY when MEFISTO_BATCH_X11=1 OR QT_QPA_PLATFORM=offscreen (per RESEARCH §Pitfall 5 mitigation)"
    - "ABI symbol count remains 58 (no new extern \"C\" entry — per T-09-06 + RESEARCH §Anti-Patterns 'Bumping ABI count')"
    - "After re-running 1 Phase-8-style A/B sample (cavity2d or pan2d) with MEFISTO_QT_WINDOW_SIZE=1280x800, the captured PNG is empirically 1280x800 (or within 1px tolerance for chrome accounting; if still differs, iterate per RESEARCH §Open Question 5)"
    - "Re-running the AE comparison (cavity2d-x11.png vs cavity2d-qt-1x.png at matched dim) produces an AE value materially lower than Phase 8's resample-confounded value (ideally <5%; document actual)"
    - "bin/cbl_tout exits 0; 4-5 BUILD-10 testa cases capture cleanly"
  artifacts:
    - path: "xvue/qt/src/xvue_qt_api.cpp"
      provides: "Edited xvinitgraphique_ (or xvinfo_) to honor MEFISTO_QT_WINDOW_SIZE env"
      min_lines: 800  # file already exists; plan adds ~15 lines
    - path: "xvue/qt/README.md"
      provides: "Documents MEFISTO_QT_WINDOW_SIZE env var (headless-only)"
      contains: "MEFISTO_QT_WINDOW_SIZE"
  key_links:
    - from: "xvinitgraphique_ env probe"
      to: "win->canvas()->setMinimumSize/setMaximumSize"
      via: "if (MEFISTO_BATCH_X11=1 OR QT_QPA_PLATFORM=offscreen) AND env set"
      pattern: "grep -n MEFISTO_QT_WINDOW_SIZE xvue/qt/src/xvue_qt_api.cpp"
    - from: "bin/ab_sweep_phase8.sh qt-1x dispatch"
      to: "MEFISTO_QT_WINDOW_SIZE=1280x800 export"
      via: "shell-script env injection in qt-1x branch"
      pattern: "grep -n MEFISTO_QT_WINDOW_SIZE bin/ab_sweep_phase8.sh"
---

<objective>
Phase 8 carry-forward #1: matched-dim Qt recapture.

Phase 8 documented 14 "CHECK" cells in 08-CHECKLIST.md whose AE diffs were dominated
by a resampling artifact (X11 captures land at 1280x800; Qt captures landed at
~760x442 because window chrome consumed pixels). Phase 8 maintainer accepted this
as override #1 with explicit deferral to Phase 9.

Per RESEARCH §Pattern 4 + §Open Question 5: add an env-var hook
`MEFISTO_QT_WINDOW_SIZE=WxH` to `xvinitgraphique_` (or `xvinfo_`) so the harness can
flip Qt captures to match X11's 1280x800 baseline. Then re-run a 1-case A/B
(cavity2d or pan2d) and prove the resample-confound goes away.

Critical constraints:
- ABI count stays at 58 (T-09-06 mitigation; do NOT add a new extern "C" entry — use
  env var instead; per RESEARCH §Anti-Pattern "Bumping ABI count from 58 to 59")
- Interactive UX must be preserved (per RESEARCH §Pitfall 5: gate the env-var
  honoring on MEFISTO_BATCH_X11=1 OR QT_QPA_PLATFORM=offscreen — the harness
  contexts; interactive sessions stay at the existing 1024x768 default)
- Use setMinimumSize + setMaximumSize NOT setFixedSize (so xvfermer_ can clear
  the constraint; per RESEARCH §Pitfall 5 mitigation 2)

Output:
- Edit xvue/qt/src/xvue_qt_api.cpp (~15 lines added in xvinitgraphique_ near
  win->resize call, per RESEARCH Pattern 4 line citations 476-487)
- Edit xvue/qt/README.md to document the new env var (headless-only)
- Edit bin/ab_sweep_phase8.sh qt-1x branch to export MEFISTO_QT_WINDOW_SIZE=1280x800
- Empirical verification: 1 A/B sample at matched-dim shows AE materially lower
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-retire-x11-backend/09-CONTEXT.md
@.planning/phases/09-retire-x11-backend/09-RESEARCH.md
@.planning/phases/09-retire-x11-backend/09-05-SUMMARY.md
@.planning/phases/08-ab-validation-on-testa-subset/08-CHECKLIST.md
@CLAUDE.md
@xvue/qt/src/xvue_qt_api.cpp
@xvue/qt/src/xvue_qt_canvas.cpp
@bin/ab_sweep_phase8.sh

<interfaces>
<!-- xvinitgraphique_ signature (xvue/qt/src/xvue_qt_api.cpp:336-487 per RESEARCH §Sources):
       void xvinitgraphique_(int* ix, int* iy, int* nbcoul, int* nbcoulr,
                             int* nbcouli, ...);
     Fortran callers (per RESEARCH §Pattern 4): pass *ix=LAPXFE=800, *iy=LHPXFE=600
     when EXISTNF != 0 (i.e., when reading existing file dimensions). Window
     chrome (menubar+toolbar+statusbar+console dock) eats into the canvas, so
     captured backing pixmap = 760x442 not 800x600. Override target: 1280x800
     (Phase 8 X11 baseline = Xvfb root-window screenshot at 1280x800).

     Pattern 4 insertion point (RESEARCH lines 476-487): right BEFORE win->resize(*ix, *iy).
     Probe MEFISTO_QT_WINDOW_SIZE env; if set and headless-context, route through
     win->canvas()->setMinimumSize(w,h) + setMaximumSize(w,h) + win->adjustSize().

     XvueCanvas::resizeEvent (xvue/qt/src/xvue_qt_canvas.cpp:126) reallocates
     state_->backing_ from size() * dpr — so setMinimum/Maximum DOES propagate
     to backing-pixmap dim once the window adjusts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose-then-fix — empirically confirm RESEARCH Pattern 4 produces matched-dim PNG</name>
  <read_first>
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Pattern 4: Env-Knob Window Sizing for Matched-Dim Recapture (Plan 9-05)" (referenced as 9-05 in research; this is Plan 9-06)
    - .planning/phases/09-retire-x11-backend/09-RESEARCH.md §"Open Question 5"
    - xvue/qt/src/xvue_qt_api.cpp (lines 336-487 per RESEARCH §Sources, contains xvinitgraphique_)
    - xvue/qt/src/xvue_qt_canvas.cpp (lines 126-209 contain resizeEvent + backing alloc)
  </read_first>
  <action>
    Per RESEARCH §Open Question 5: "does setting setMinimumSize + setMaximumSize
    propagate to the backing-pixmap allocation in XvueCanvas::resizeEvent?" — answer
    "Min/Max constraints DO affect canvas size after win->adjustSize()", but order of
    operations needs empirical verification.

    1. Read xvue/qt/src/xvue_qt_api.cpp around the xvinitgraphique_ implementation
       to find the exact line where win->resize(*ix, *iy) is called (Pattern 4 says
       around line 487 — confirm).

    2. Locate xvinfo_ (RESEARCH §Sources also lists 822-910 as containing it). xvinfo_ is
       called frequently to read screen dimensions; the env probe could go there too.
       Choose one location for the probe. Recommendation per RESEARCH Pattern 4:
       xvinitgraphique_ is the canonical "open the window" entry — put it there.

    3. Insert the env-probe code per RESEARCH Pattern 4 (literal block, modified to
       respect Pitfall 5 gating):

       ```cpp
       // Phase 9 Plan 9-06 (carry-forward #1): matched-dim Qt recapture.
       // Per RESEARCH §Pattern 4 + §Pitfall 5: honor MEFISTO_QT_WINDOW_SIZE=WIDTHxHEIGHT
       // env var, BUT ONLY in headless contexts (MEFISTO_BATCH_X11=1 OR
       // QT_QPA_PLATFORM=offscreen). Interactive sessions retain default sizing.
       // Use setMinimumSize+setMaximumSize (NOT setFixedSize) so xvfermer_ can clear.
       const bool headless = std::getenv("MEFISTO_BATCH_X11") &&
                             std::string(std::getenv("MEFISTO_BATCH_X11")) == "1";
       const char* qpa = std::getenv("QT_QPA_PLATFORM");
       const bool offscreen = qpa && std::string(qpa) == "offscreen";
       const char* qt_window_size = std::getenv("MEFISTO_QT_WINDOW_SIZE");
       if ((headless || offscreen) && qt_window_size && qt_window_size[0]) {
           int w = 0, h = 0;
           if (std::sscanf(qt_window_size, "%dx%d", &w, &h) == 2 &&
               w > 0 && w < 8192 && h > 0 && h < 8192) {  // V5 input-validation bounds
               if (win && win->canvas()) {
                   win->canvas()->setMinimumSize(w, h);
                   win->canvas()->setMaximumSize(w, h);
                   win->adjustSize();
               }
           }
       }
       ```

       (Insertion point: BEFORE the existing `win->resize(*ix, *iy);` line in
       xvinitgraphique_. Use Edit tool with the exact `win->resize(*ix, *iy);` text
       from the file as the anchor.)

    4. Add a corresponding unset/clear in xvfermer_ (per RESEARCH §Pitfall 5
       mitigation 2 "clear constraints on xvfermer_"):
       Find xvfermer_ in xvue_qt_api.cpp; right before window_.reset() or equivalent
       teardown, clear the constraints:
       ```cpp
       if (win && win->canvas()) {
           win->canvas()->setMinimumSize(0, 0);
           win->canvas()->setMaximumSize(QWIDGETSIZE_MAX, QWIDGETSIZE_MAX);
       }
       ```

    5. Build:
       `bin/cbl_tout 2&gt;&amp;1 | tail -10`
       Confirm exit 0; ABI count check (58):
       `[ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ]`

    6. Empirical verify (RESEARCH §Open Question 5 verification step):
       `MEFISTO_QT_WINDOW_SIZE=1280x800 \
        bin/ab_sweep_phase8.sh --mode qt-1x \
            --cases pan2d \
            --out-dir /tmp/09-06-verify-pan2d 2&gt;&amp;1 | tail -5`
       Then:
       `identify -format '%wx%h' /tmp/09-06-verify-pan2d/pan2d-qt-1x.png`
       Expected: `1280x800` (or within 1px chrome-accounting tolerance).

       If NOT 1280x800: iterate per RESEARCH §Open Question 5 (e.g., need additional
       processEvents pump after adjustSize, or add a one-shot QTimer::singleShot to
       force resize). Document the iteration in 09-06-DIAGNOSE.md.

    7. Capture diagnose output to .planning/phases/09-retire-x11-backend/09-06-DIAGNOSE.md
       (working scratch document). Record:
       - Insertion point line numbers (xvinitgraphique_ + xvfermer_)
       - identify-output PNG dim from step 6
       - ABI count (must be 58)
       - Iteration history if step 6 didn't produce 1280x800 on first try
  </action>
  <verify>
    <automated>
      grep -q "MEFISTO_QT_WINDOW_SIZE" xvue/qt/src/xvue_qt_api.cpp &amp;&amp; \
      grep -q "MEFISTO_BATCH_X11" xvue/qt/src/xvue_qt_api.cpp &amp;&amp; \
      grep -q "setMinimumSize" xvue/qt/src/xvue_qt_api.cpp &amp;&amp; \
      grep -q "setMaximumSize" xvue/qt/src/xvue_qt_api.cpp &amp;&amp; \
      [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ] &amp;&amp; \
      test -f /tmp/09-06-verify-pan2d/pan2d-qt-1x.png &amp;&amp; \
      identify -format '%wx%h' /tmp/09-06-verify-pan2d/pan2d-qt-1x.png | grep -E '12[7-9][0-9]x79[0-9]|1280x800|1280x801|1280x799'
    </automated>
  </verify>
  <acceptance_criteria>
    - xvue_qt_api.cpp contains MEFISTO_QT_WINDOW_SIZE + MEFISTO_BATCH_X11 + setMinimumSize + setMaximumSize text
    - bin/cbl_tout exits 0
    - ABI count remains 58 (T-09-06 mitigation)
    - identify reports the resulting PNG at 1280x800 (or within 1px tolerance for chrome accounting)
    - 09-06-DIAGNOSE.md exists capturing the iteration log
  </acceptance_criteria>
  <done>Pattern 4 implemented + empirically verified to produce matched-dim PNG; ABI stable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire harness + re-run 1-case A/B at matched dim + prove AE drops</name>
  <read_first>
    - bin/ab_sweep_phase8.sh
    - .planning/phases/08-ab-validation-on-testa-subset/08-CHECKLIST.md (per-cell AE values)
    - .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11.png (Phase 8 X11 baseline)
  </read_first>
  <action>
    Wire bin/ab_sweep_phase8.sh's qt-1x dispatch to export MEFISTO_QT_WINDOW_SIZE=1280x800
    automatically (so harness invocations don't need to remember the env). Then re-run
    1 A/B comparison on a CHECK cell to prove the AE drop.

    1. Edit bin/ab_sweep_phase8.sh, find the qt-1x mode branch:
       `grep -n 'qt-1x\|MODE.*qt' bin/ab_sweep_phase8.sh | head`
       Add a single env-export line near the top of the qt-1x branch:
       ```bash
       # Phase 9 Plan 9-06: Qt-1x captures use MEFISTO_QT_WINDOW_SIZE=1280x800 by
       # default to match the X11 baseline (Xvfb root-window screenshot dim);
       # eliminates the resample-confound that dominated Phase 8 AE diffs.
       # Override by setting MEFISTO_QT_WINDOW_SIZE explicitly before invocation.
       : "${MEFISTO_QT_WINDOW_SIZE:=1280x800}"
       export MEFISTO_QT_WINDOW_SIZE
       ```
       (Place this after argument parsing but before the per-case capture loop.
       The `: "${VAR:=default}"` form respects user override.)

    2. Re-run a 1-case Phase-8 A/B comparison on cavity2d (canonical CHECK cell per
       08-CHECKLIST.md):
       `bin/ab_sweep_phase8.sh --mode qt-1x --cases cavity2d \
            --out-dir /tmp/09-06-cavity-matched 2&gt;&amp;1 | tee /tmp/09-06-sweep.log`

    3. AE-compare against Phase 8 X11 baseline:
       ```
       compare -metric AE \
         .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11.png \
         /tmp/09-06-cavity-matched/cavity2d-qt-1x.png \
         /dev/null 2>&amp;1 | tee /tmp/09-06-cavity-AE.log
       ```
       Phase 8 reported AE ≈ 425000+ (resample-dominated). With matched dim, AE
       should drop materially. The exact target depends on remaining content
       differences (chrome bars / antialiasing differences); document actual.

       Acceptable: AE materially lower than Phase 8 baseline (e.g., < 100000 or
       ratio < 25%). If still high, capture residual diff via
       `compare -metric AE -compose src cavity2d-x11.png cavity2d-qt-1x.png /tmp/diff.png`
       and inspect /tmp/diff.png for residual structural differences.

    4. Update the qt-1x harness to also tolerate dim-match in dimension-guard logic
       (search for "dimension guard" or related code in bin/ab_sweep_phase8.sh that
       Phase 8 inserted to handle the previous mismatch):
       `grep -n 'dimension\|width\|identify' bin/ab_sweep_phase8.sh | head`
       If the guard exists and is now spurious (Qt now matches X11), either delete
       it or wrap in a comment explaining the post-Plan-9-06 behavior.

    5. Document in xvue/qt/README.md (env-vars section):
       Find the env-var documentation block in xvue/qt/README.md (per RESEARCH:
       "Update xvue/qt/README.md to document the new env var") and append:
       ```markdown
       ### MEFISTO_QT_WINDOW_SIZE (Phase 9 carry-forward — headless only)

       Optional. Format `WIDTHxHEIGHT` (e.g., `1280x800`). When set AND the
       process is headless (`MEFISTO_BATCH_X11=1` OR `QT_QPA_PLATFORM=offscreen`),
       `xvinitgraphique_` calls `setMinimumSize`+`setMaximumSize` on the canvas
       so the captured backing pixmap matches the requested dim. Used by
       `bin/ab_sweep_phase8.sh` qt-1x mode to match the X11 baseline (1280x800
       Xvfb root-window screenshot) and eliminate the resample-confound that
       dominated Phase 8 AE diffs.

       Interactive sessions ignore this var (per RESEARCH §Pitfall 5 mitigation).
       Bounds: 0 < {w,h} < 8192. Malformed values silently ignored.
       ```
  </action>
  <verify>
    <automated>
      grep -q "MEFISTO_QT_WINDOW_SIZE" bin/ab_sweep_phase8.sh &amp;&amp; \
      grep -q "MEFISTO_QT_WINDOW_SIZE" xvue/qt/README.md &amp;&amp; \
      test -f /tmp/09-06-cavity-AE.log
    </automated>
  </verify>
  <acceptance_criteria>
    - bin/ab_sweep_phase8.sh exports MEFISTO_QT_WINDOW_SIZE=1280x800 in qt-1x dispatch
    - xvue/qt/README.md documents the new env var (headless-only, bounds, default)
    - cavity2d AE comparison run + log captured at /tmp/09-06-cavity-AE.log
    - AE value materially lower than Phase 8 baseline of ~425000 (record actual in 09-06-DIAGNOSE.md)
  </acceptance_criteria>
  <done>Harness wired; 1 A/B sample empirically demonstrates the resample-confound is gone or vastly reduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer reviews matched-dim diff + signs off on AE drop</name>
  <read_first>
    - /tmp/09-06-cavity-AE.log
    - /tmp/09-06-cavity-matched/cavity2d-qt-1x.png
    - .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11.png
    - .planning/phases/09-retire-x11-backend/09-06-DIAGNOSE.md
  </read_first>
  <what-built>
    Plan 9-06 wired MEFISTO_QT_WINDOW_SIZE so the harness produces 1280x800 Qt
    captures matching the X11 baseline. The empirical AE on cavity2d should drop
    materially. Maintainer reviews the AE delta + visual diff before commit.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11.png
       and /tmp/09-06-cavity-matched/cavity2d-qt-1x.png side-by-side in your image viewer.
    2. Note the dim of each. The Qt PNG should now be 1280x800 (or within ~1px).
    3. Read /tmp/09-06-cavity-AE.log. Confirm the AE value is materially lower than
       Phase 8's reported value (~425000 for cavity2d per 08-CHECKLIST.md).
    4. Visual sanity: do the two PNGs render the same scene? (Mesh outline,
       streamlines, color bars all present in both.) Residual AE comes from
       chrome-rendering differences (Qt menubar/toolbar vs Xvfb minimal chrome) and
       antialiasing — those are expected, not bugs.
    5. Reply "approved" if AE dropped and visuals match. Reply with specifics if they
       don't (e.g., "AE = X — too high; missing color bar" → executor iterates per
       RESEARCH §Open Question 5).
  </how-to-verify>
  <resume-signal>Maintainer types "approved" or describes residual issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Full rebuild + 5-case testa baseline + commit</name>
  <read_first>
    - bin/cbl_tout
    - bin/ab_sweep_phase8.sh
  </read_first>
  <action>
    1. Full Qt-only rebuild:
       `bin/cbl_tout 2&gt;&amp;1 | tee /tmp/09-06-cbl_tout.log`
       Exit 0; ABI count remains 58.

    2. 5-case Qt sweep (all under matched dim):
       `bin/ab_sweep_phase8.sh --mode qt-1x \
            --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu \
            --out-dir /tmp/09-06-post 2&gt;&amp;1 | tee /tmp/09-06-final-sweep.log`

    3. Confirm 4-5 PNGs land + each is 1280x800:
       `for c in pan2d nafems_le1 cavity2d heat1d; do \
            d=$(identify -format '%wx%h' /tmp/09-06-post/$c-qt-1x.png 2&gt;/dev/null) ;\
            echo "$c: $d" ;\
        done`
       Expected: each line shows "1280x800" (or near-1280x800 within 1-2px).

    4. Run all 3 grep gates:
       `bin/test_no_imagemagick_in_qt.sh && bin/test_no_x11_in_build.sh && bin/test_no_lvideo.sh`

    5. Commit:
       ```
       git add -A xvue/qt/src/xvue_qt_api.cpp xvue/qt/README.md bin/ab_sweep_phase8.sh
       git commit -m "fix(09-06): carry-forward #1 — matched-dim Qt recapture via MEFISTO_QT_WINDOW_SIZE env

       Closes Phase 8 override #1 (the 14 'CHECK' cells in 08-CHECKLIST.md whose
       AE diffs were dominated by a resampling artifact: X11 captures at 1280x800
       vs Qt captures at ~760x442 due to chrome eating into the 800x600 default).

       Implementation per RESEARCH §Pattern 4 + §Pitfall 5 + §Open Question 5:
       - xvinitgraphique_ probes MEFISTO_QT_WINDOW_SIZE=WxH env BEFORE win->resize
         and routes through win->canvas()->setMinimumSize/setMaximumSize +
         win->adjustSize() so the backing pixmap allocates at the env-specified dim.
       - Headless-only gate: env honored ONLY when MEFISTO_BATCH_X11=1 OR
         QT_QPA_PLATFORM=offscreen (interactive UX preserved per Pitfall 5; T-09-05
         mitigation).
       - V5 input-validation bounds: 0 < w,h < 8192; malformed silently ignored.
       - xvfermer_ clears the constraints (setMinimum/Maximum back to 0/QWIDGETSIZE_MAX)
         per Pitfall 5 mitigation 2.
       - bin/ab_sweep_phase8.sh qt-1x branch defaults MEFISTO_QT_WINDOW_SIZE=1280x800.
       - xvue/qt/README.md documents the new env var.

       Empirical verification:
       - cavity2d AE comparison post-fix: see /tmp/09-06-cavity-AE.log and
         09-06-DIAGNOSE.md (maintainer-signed-off in Task 3 checkpoint).
       - All 4 fast testa cases produce 1280x800 PNGs.
       - bin/cbl_tout exit 0; ABI count remains 58 (T-09-06 mitigation upheld).

       Threats mitigated:
       - T-09-05 (interactive UX regression): headless-context gate + setMin/Max-not-Fixed
       - T-09-06 (ABI bump): no new extern \"C\" entry; env var only

       Closes Phase 8 carry-forward #1 (override #1 + 14 CHECK cells)."
       ```
  </action>
  <verify>
    <automated>
      tail -1 /tmp/09-06-cbl_tout.log | grep -qiE "MODULES|crees|created" &amp;&amp; \
      [ "$(nm xvue/qt/build/libxvueqt.a | grep ' T ' | grep '_$' | wc -l)" = "58" ] &amp;&amp; \
      ls /tmp/09-06-post/cavity2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-06-post/heat1d-qt-1x.png &amp;&amp; \
      ls /tmp/09-06-post/pan2d-qt-1x.png &amp;&amp; \
      ls /tmp/09-06-post/nafems_le1-qt-1x.png &amp;&amp; \
      bin/test_no_x11_in_build.sh &amp;&amp; bin/test_no_imagemagick_in_qt.sh &amp;&amp; bin/test_no_lvideo.sh &amp;&amp; \
      git log --oneline -1 | grep -E '09-06.*matched-dim|09-06.*carry-forward'
    </automated>
  </verify>
  <acceptance_criteria>
    - bin/cbl_tout exits 0
    - ABI count remains 58
    - 4 of 5 testa cases capture at 1280x800 (or within 1-2px tolerance)
    - All 3 grep gates pass
    - Latest commit references 09-06 + carry-forward #1
  </acceptance_criteria>
  <done>Plan 9-06 commit landed; matched-dim recapture working; ABI stable; carry-forward #1 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MEFISTO_QT_WINDOW_SIZE env input ↔ setMinimumSize/setMaximumSize call | Untrusted env input must be sscanf'd + bounds-checked before reaching Qt API |
| Headless context detection ↔ interactive session | The gate (MEFISTO_BATCH_X11=1 OR QT_QPA_PLATFORM=offscreen) must distinguish reliably |
| ABI count = 58 invariant | New code must NOT add a new extern "C" entry (per T-09-06 + Phase 6.0 D-XX lock) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | Matched-dim env knob regresses interactive UX | mitigate | Headless gate (MEFISTO_BATCH_X11=1 OR QT_QPA_PLATFORM=offscreen); setMin/Max NOT setFixed; xvfermer_ clears constraints; documented as headless-only in README |
| T-09-06 | Tampering | ABI symbol count drifts from 58 to 59 | mitigate | Implementation uses env var only — no new extern "C" entry; Task 1 verify checks `nm libxvueqt.a | grep ... | wc -l == 58`; Task 4 final verification re-checks |
| T-09-05-A | Input Validation (V5) | MEFISTO_QT_WINDOW_SIZE injection (negative dims, integer overflow) | mitigate | sscanf("%dx%d") + bounds check `0 < w,h < 8192`; malformed silently ignored (no error path that could be exploited) |
| T-09-05-B | DoS | Plan 9-06 fix doesn't actually produce 1280x800 (Open Question 5) | mitigate | Task 1 step 6 empirical verify; iterate per RESEARCH §Open Question 5 if first attempt fails; document in 09-06-DIAGNOSE.md |
</threat_model>

<verification>
- xvinitgraphique_ honors MEFISTO_QT_WINDOW_SIZE only in headless context
- bin/cbl_tout exits 0
- ABI count stays at 58
- identify reports 1280x800 for 4 fast testa cases
- cavity2d AE comparison shows materially lower value than Phase 8 baseline
- All 3 grep gates exit 0
- Maintainer signed off on visual diff (Task 3)
</verification>

<success_criteria>
- [x] Pattern 4 implemented in xvinitgraphique_ + cleared in xvfermer_ (Task 1)
- [x] Empirical 1280x800 PNG verified (Task 1)
- [x] Harness wired + 1-case AE drop demonstrated (Task 2)
- [x] Maintainer signed off (Task 3)
- [x] 5-case testa baseline + commit (Task 4)
</success_criteria>

<output>
After completion, create `.planning/phases/09-retire-x11-backend/09-06-SUMMARY.md`
documenting:
- xvinitgraphique_ insertion line numbers + xvfermer_ clear line numbers
- ABI count pre/post (58/58)
- AE delta on cavity2d (record exact pre and post values)
- Phase 8 override #1 closure note (14 CHECK cells whose disposition can flip from CHECK to PASS at the next harness re-run)
</output>
