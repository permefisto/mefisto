---
phase: 08-ab-validation-on-testa-subset
plan: 01
type: execute
wave: 1
depends_on: []
# NOTE (revision iter1, MAJOR-08): files_modified count is 11 — dense for one plan.
# Monitor context consumption during execution; if any task starts hitting >40%
# context, split Task 3 into 3a (script ship) + 3b (smoke probes) BEFORE proceeding.
files_modified:
  - bin/cbl_tout_qt
  - bin/phase8_case_batch_map.sh
  - xvue/qt/tests/golden/scene01.eps
  - xvue/qt/tests/golden/wave_legacy.gif
  - xvue/qt/tests/golden/cavity2d_legacy.gif
  - bin/ab_compare_pair.sh
  - bin/ab_capture_x11.sh
  - bin/ab_sweep_phase8.sh
  - .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/00-bootstrap-log.md
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/00-case-batch-map.md
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md
autonomous: true
requirements:
  - VALID-01
  - VALID-02
  - VALID-07
must_haves:
  truths:
    - "pp/*_qt binaries are linked against the freshly built libxvueqt.a (mtime check passes for all 5)"
    - "xvue/qt/tests/golden/scene01.eps exists and is byte-comparable to the X11 backend's TEMPORAIRE.EPS output of scene01_driver.f"
    - "xvue/qt/tests/golden/wave_legacy.gif exists and is a valid animated GIF produced by bin/convertepsgif on testa/wave"
    - "xvue/qt/tests/golden/cavity2d_legacy.gif exists and is a valid animated GIF produced by bin/convertepsgif on testa/cavity2d"
    - "ctest -R 'xvue_qt_(postscript|export)_tests' shows 0 SKIP after goldens land (all 3 Phase-7 QSKIPs flip to PASS)"
    - "bin/phase8_case_batch_map.sh emits a per-case BATCH_FILE + WORKSPACE_PREP_CMD pair derived empirically from `ls testa/$CASE/` (NOT from glob assumptions). Map covers the 5 BUILD-10 baseline cases."
    - "bin/ab_compare_pair.sh enforces argc → fuzz validation [1,30] → identify dim guard → compare ORDER (header comment marks the contract)"
    - "bin/ab_capture_x11.sh polls MEFISTO_XVFERMER_READY_FILE and uses 'import -window root' under Xvfb"
    - "bin/ab_sweep_phase8.sh dispatches per (case, backend, mode) and emits a verdict line per cell; supports --smoke-only flag that skips the X11-baseline-compare step (used by Plan 1 Task 3 before any baseline exists)"
    - "bin/ab_sweep_phase8.sh supports --baseline PATH to override the default ${CASE}-x11.png LEFT operand (used by Plan 5 OMP cells comparing against ${CASE}-x11-omp.png)"
    - "Each of the 5 BUILD-10 baseline testa cases completes within 60s under MEFISTO_XVSOURIS_AUTOEXIT=1 on the Qt offscreen backend AND the case-batch map's per-case prerequisite (workspace cp + cwd change) is honored"
  artifacts:
    - path: "bin/phase8_case_batch_map.sh"
      provides: "Empirical per-case batch-file + workspace-prep map sourced by the harness; replaces the fragile *.{mesh,elas,flui,heat,nlse} glob"
      min_lines: 40
    - path: "bin/ab_compare_pair.sh"
      provides: "tolerance-band gate wrapper around ImageMagick compare with strict ORDER: argc → fuzz → identify → compare"
      min_lines: 30
    - path: "bin/ab_capture_x11.sh"
      provides: "Xvfb-display-aware X11 capture wrapper using READY_FILE polling + import -window root"
      min_lines: 30
    - path: "bin/ab_sweep_phase8.sh"
      provides: "top-level harness driving (case, backend, mode) cells; sources phase8_case_batch_map.sh; supports --smoke-only and --baseline; emits verdict line per cell"
      min_lines: 80
    - path: "xvue/qt/tests/golden/scene01.eps"
      provides: "Phase-7 deferred PostScript byte-parity golden"
    - path: "xvue/qt/tests/golden/wave_legacy.gif"
      provides: "Phase-7 deferred GIF baseline for testa/wave"
    - path: "xvue/qt/tests/golden/cavity2d_legacy.gif"
      provides: "Phase-7 deferred GIF baseline for testa/cavity2d"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/00-bootstrap-log.md"
      provides: "Per-step bootstrap evidence log (cbl_tout_qt summary, golden mtimes, ctest result, freshness mtime check)"
      min_lines: 20
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/00-case-batch-map.md"
      provides: "Empirical batch-file + workspace-prep map for the 5 BUILD-10 cases. Records ls output, identified driver chain, and per-case end-to-end smoke verdict"
      min_lines: 30
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md"
      provides: "Per-case AUTOEXIT smoke probe results (exit code + xvfermer_ call count + capture file size) for the 5 cases"
      min_lines: 20
  key_links:
    - from: "bin/cbl_tout_qt"
      to: "pp/pp{mail,elas,flui,ther,nlse}_qt"
      via: "shell script invokes bin/cb{mail,elas,flui,ther,nlse}_qt sequentially after CMake builds libxvueqt.a"
      pattern: "cb(mail|elas|ther|flui|nlse)_qt"
    - from: "bin/phase8_case_batch_map.sh"
      to: "bin/ab_sweep_phase8.sh"
      via: "Harness sources the map via `. bin/phase8_case_batch_map.sh` to look up per-case BATCH_FILE + WORKSPACE_PREP"
      pattern: "phase8_case_batch_map"
    - from: "xvue/qt/tests/test_xvue_qt_postscript.cpp"
      to: "xvue/qt/tests/golden/scene01.eps"
      via: "QSKIP guard at runtime probes file existence"
      pattern: "QSKIP.*scene01\\.eps"
    - from: "xvue/qt/tests/test_xvue_qt_export.cpp"
      to: "xvue/qt/tests/golden/{wave,cavity2d}_legacy.gif"
      via: "QSKIP guard at runtime probes file existence"
      pattern: "QSKIP.*(wave|cavity2d)_legacy\\.gif"
    - from: "bin/ab_compare_pair.sh"
      to: "ImageMagick compare + identify"
      via: "shell exec; argc→fuzz→identify→compare ORDER; refuses on dimension mismatch unless -resample given"
      pattern: "compare -metric AE -fuzz"
---

<objective>
Bootstrap Phase 8: refresh pp/*_qt against the current libxvueqt.a (D-08, Phase 7
Gap-A), materialize the 3 Phase-7-deferred goldens (D-06, D-07), confirm all
3 Phase-7 ctest QSKIPs flip to PASS, **build the empirical per-case batch-file
map (Task 0) that replaces the fragile glob assumption**, run per-case
end-to-end + AUTOEXIT smoke probes against pp/pp{mail,elas,flui,ther,nlse}_qt
under QT_QPA_PLATFORM=offscreen to verify each case can be driven headlessly
without hanging, and ship the three sweep-harness shell scripts
(bin/ab_compare_pair.sh, bin/ab_capture_x11.sh, bin/ab_sweep_phase8.sh) that
Plans 2-6 will consume. This plan is the gate that opens Wave 2 — Plans 2-5
cannot run until pp/*_qt are fresh, the goldens are committed, the case-batch
map is built, and the harness scripts exist.

Purpose: Phase 7 left three goldens DEFERRED and Gap-A surfaced pp/*_qt
staleness. Phase 8 cannot trust an A/B sweep against stale binaries (false
PASS — T-08-01) and cannot complete Phase-7-close without the goldens. **Iter1
checker review additionally surfaced that the 5 testa cases do NOT share a
common batch-file extension** (cavity2d has only `.meshbf`/`.meshth`/`.stoke56cr`;
nlsecu has only `.iexrr*`/`.meshq2`; hexahedron's per-batch chain is named
`ln`/`ob`/`pt`/`sf`/`vl` with no extension at all) — the original `*.{mesh,elas,flui,heat,nlse}`
glob would silently fail for 3 of 5 cases. Task 0 fixes this empirically.

Output:
- 1 case-batch map (bin/phase8_case_batch_map.sh) sourced by the harness
- 5 fresh pp/*_qt binaries (mtime > libxvueqt.a mtime)
- 3 committed goldens under xvue/qt/tests/golden/
- 1 updated VALIDATION-LOG.md row per golden (DEFERRED → PASS)
- 1 ctest run log showing 0 SKIP for xvue_qt_postscript_tests + xvue_qt_export_tests
- 3 sweep-harness shell scripts under bin/ (executable, +x mode) supporting --smoke-only and --baseline flags
- 1 smoke-probe log confirming all 5 BUILD-10 testa cases complete under AUTOEXIT
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/validation/BASELINE.md
@.planning/phases/08-ab-validation-on-testa-subset/08-CONTEXT.md
@.planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md
@.planning/phases/07-image-gif-and-postscript-export/VERIFICATION.md
@.planning/phases/07-image-gif-and-postscript-export/07-UAT.md
@.planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md
@xvue/qt/tests/golden/scene01_driver.f
@bin/cbl_tout_qt
@bin/convertepsgif
@CLAUDE.md

<interfaces>
<!-- Capture/control envvars already implemented in both backends. Phase 8 CONSUMES these — does NOT modify them. -->

xvue/qt/src/xvue_qt_api.cpp xvfermer_ honors:
  - MEFISTO_QT_CAPTURE_PATH       → in-process QPixmap::save() of XvueState::backing_; works under QT_QPA_PLATFORM=offscreen with NO X server.
  - MEFISTO_XVFERMER_READY_FILE   → mirrors X11 sentinel; file gets touched right before window destruction.
  - MEFISTO_XVFERMER_HOLD_MS      → milliseconds to sleep after touching the ready file (deterministic capture window).

xvue/qt/src/xvue_qt_api.cpp xvsouris_ honors:
  - MEFISTO_XVSOURIS_AUTOEXIT=1            → returns synthetic SPACE keypress (notypeevent=2) so blocking event reads exit on first iteration.
  - MEFISTO_XVSOURIS_AUTOEXIT_DELAY_MS=N   → delay before the synthetic event (default 0).

xvue/xvuelc.c xvfermer_ honors:
  - MEFISTO_XVFERMER_READY_FILE   → touches the file then XFlush.
  - MEFISTO_XVFERMER_HOLD_MS      → usleep(N*1000) before display destruction.

xvue/xvuelc.c xvsouris_ honors MEFISTO_XVSOURIS_AUTOEXIT (same contract as Qt side).

bin/cbl_tout_qt sequence (verified 2026-05-05 by ls + Read):
  1. CMake configure + build xvue/qt/build → produces libxvueqt.a
  2. Sequential bin/cb{mail,elas,flui,ther,nlse}_qt → relinks pp/pp{mail,elas,flui,ther,nlse}_qt
  3. Sequential bin/cbxvtest{0..4}_qt → relinks pp/ppxvtest{0..4}_qt

bin/convertepsgif body (one line, verified):
  cd $MEFISTOX/<case> && convert -rotate 90 zfxy0*.eps -extent 980x550 cyl53zfxy.gif

xvue/qt/tests/golden/scene01_driver.f BOOTSTRAP NOTE (verified by Read):
  gfortran -I$MEFISTO/incl -c scene01_driver.f
  gfortran scene01_driver.o $MEFISTO/xvue/xvuelc.o $MEFISTO/xvue/*.o $MEFISTO/util/*.o \
          -L/usr/X11R6/lib -lX11 -lXt -o scene01_x11
  xvfb-run --auto-servernum ./scene01_x11
  cp TEMPORAIRE.EPS $MEFISTO/xvue/qt/tests/golden/scene01.eps

testa case-directory contents (verified empirically 2026-05-05 — refutes naive glob assumption):
  testa/pan2d/        → pan2d.mesh + pan2dcr.t030 + pan2dcr.t3060 + pan2dgc.t030 + pan2dgc.t3060
  testa/nafems_le1/   → nafems_le1.disp + nafems_le1.elas + nafems_le1.mail + nafems_le1.mesh
  testa/cavity2d/     → cavity2d.meshbf + cavity2d.meshth + cavity2d.stoke56cr  (NO `.flui` extension!)
  testa/heat1d/       → heat1d.heat + heat1d.mesh
  testa/nlsecu/       → nlsecu.iexrr + nlsecu.iexrrplus + nlsecu.meshq2  (NO `.nlse` extension!)
  testa/hexahedron/   → ln + ob + pt + sf + vl  (5 batch SCRIPT files; NO recognized extension; this is a multi-step driver chain)

  Verified: `file testa/hexahedron/{ln,ob,pt,sf,vl}` reports "ASCII text" for all 5
  (they are batch script files, NOT subdirectories as one might assume from the bare names).

  Implication: a glob `*.{mesh,elas,flui,heat,nlse}` would only match for pan2d / nafems_le1 / heat1d.
  Task 0 below builds an explicit empirical map.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Build empirical case-batch map (replaces fragile *.{mesh,...} glob; per BLOCKER #1, #3 from iter1 review)</name>
  <files>
    bin/phase8_case_batch_map.sh
    .planning/phases/08-ab-validation-on-testa-subset/evidence/00-case-batch-map.md
  </files>
  <read_first>
    - .planning/validation/BASELINE.md (lines 23-92 — exact 5 BUILD-10 cases + their launcher scripts)
    - testa/pan2d/, testa/nafems_le1/, testa/cavity2d/, testa/heat1d/, testa/nlsecu/ (run `ls -la` on each — DO NOT trust prior assumptions about file extensions; record verbatim ls output in 00-case-batch-map.md)
    - bin/MAILLER, bin/ELASTICER, bin/FLUIDER, bin/THERMICER, bin/NLSER (verify the legacy launcher pattern: `cd $MEFISTOX/$nomproj` BEFORE running pp/* — workspace+cwd discipline must replicate)
    - bin/INITIER (read once — confirm whether/which cases need INITIER to seed a workspace; A4 only proved this for pan2d)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Assumption A4" (read line range — pp/ppmail testa/pan2d/pan2d.mesh worked WITHOUT INITIER; A4 was NOT proven for the other 4 cases)
  </read_first>
  <action>
    The harness in Plan 1 Task 3 must NOT rely on a `testa/$CASE/*.{mesh,elas,flui,heat,nlse}` glob.
    Empirically: cavity2d has no `.flui`, nlsecu has no `.nlse`, hexahedron has no extension at all.
    The legacy launchers (bin/MAILLER, bin/ELASTICER, etc.) cd into `$MEFISTOX/$nomproj` BEFORE
    running pp/* — workspace+cwd discipline must replicate this.

    1. Run `ls -la testa/$CASE/` for each of the 5 BUILD-10 cases (pan2d, nafems_le1, cavity2d,
       heat1d, nlsecu) and record the verbatim output in 00-case-batch-map.md under a
       `## Empirical ls -la for testa/$CASE/` heading per case (5 sections).

    2. For each of the 5 cases, identify the canonical batch file (the one passed as argv[1] to
       pp/pp${MODULE}). Use these rules:
       - pan2d (mail) → `pan2d.mesh`     (verified A4)
       - nafems_le1 (elas) → `nafems_le1.elas`  (canonical elasticity batch — verify by inspecting first lines for solver keywords)
       - cavity2d (flui) → `cavity2d.stoke56cr`  (canonical fluid batch — verify against bin/FLUIDER pattern; the `.meshbf`/`.meshth` are mesh inputs consumed BY this batch)
       - heat1d (ther) → `heat1d.heat`   (canonical thermal batch)
       - nlsecu (nlse) → `nlsecu.iexrr`  (canonical nonlinear batch — `iexrrplus` is a variant; pick `iexrr` per BASELINE.md "canonical case" wording)
       Record the inspection (first 5 lines of each chosen batch file) under `## Canonical batch selection` in 00-case-batch-map.md, with one paragraph per case explaining why that file (vs siblings) is the canonical driver.

    3. For each case, determine the workspace-preparation requirement empirically:
         a. Run a one-shot end-to-end smoke (offscreen + AUTOEXIT) for each case using the
            chosen batch file:
              export MEFISTO=$PWD
              export MEFISTOX=/tmp/mefistox-phase8-task0-${CASE}
              rm -rf $MEFISTOX && mkdir -p $MEFISTOX/${CASE}
              cp -r $MEFISTO/testa/${CASE}/* $MEFISTOX/${CASE}/
              cd $MEFISTOX/${CASE}
              env QT_QPA_PLATFORM=offscreen \
                  MEFISTO_QT_CAPTURE_PATH=/tmp/_task0_${CASE}.png \
                  MEFISTO_XVSOURIS_AUTOEXIT=1 \
                  MEFISTO_XVSOURIS_AUTOEXIT_DELAY_MS=500 \
                  timeout 60 $MEFISTO/pp/pp${MODULE}_qt ${CHOSEN_BATCH} \
                  2>&1 | tee /tmp/_task0_${CASE}.log
              echo "exit=$? size=$(stat -c '%s' /tmp/_task0_${CASE}.png 2>/dev/null || echo 0)"
         b. Record per case: chosen batch file, exit code, capture size (bytes), under
            `## End-to-end smoke verdict per case` in 00-case-batch-map.md.
         c. ALL 5 CASES MUST PRODUCE A NON-EMPTY CAPTURE (size > 0) AND exit ∈ {0, 124-on-timeout-with-capture}.
            Any case where capture_size == 0 OR exit != 0 (without a captured frame) BLOCKS the plan.
            STOP and document the failure mode under `## BLOCKED CASES` and report to user before proceeding.

    4. For cases that need a multi-step driver chain (e.g., heat1d may need MAILLER then THERMICER if
       heat1d.heat doesn't include mesh generation), record the SEQUENCE in the map:
         CASE_HEAT1D_PREREQ_MODULE="mail"
         CASE_HEAT1D_PREREQ_BATCH="heat1d.mesh"
       The harness honors this prereq by running pp/ppmail first, then the main module. Discover
       empirically — if the single-batch smoke in step 3 produces a non-empty capture, no prereq is
       needed for that case.

    5. Write `bin/phase8_case_batch_map.sh` as a sourceable bash file. Required structure:
       ```
       #!/usr/bin/env bash
       # Phase 8 Plan 1 Task 0 — empirical batch-file map (per BLOCKER #1, #3 iter1 revision).
       # Sourced by bin/ab_sweep_phase8.sh: `. "$MEFISTO/bin/phase8_case_batch_map.sh"`
       # Each entry exports per-case:
       #   PHASE8_CASE_${CASE}_MODULE        — pp/pp${MODULE}_qt module suffix
       #   PHASE8_CASE_${CASE}_BATCH         — argv[1] passed to pp/pp${MODULE}_qt
       #   PHASE8_CASE_${CASE}_PREREQ_MODULE — optional; module to run BEFORE main module
       #   PHASE8_CASE_${CASE}_PREREQ_BATCH  — optional; batch file for prereq run
       # WORKSPACE DISCIPLINE: caller MUST cd into $MEFISTOX/$CASE BEFORE running pp/*

       # pan2d (verified A4)
       PHASE8_CASE_pan2d_MODULE=mail
       PHASE8_CASE_pan2d_BATCH=pan2d.mesh

       # nafems_le1 — empirical (Task 0 step 3 result)
       PHASE8_CASE_nafems_le1_MODULE=elas
       PHASE8_CASE_nafems_le1_BATCH=nafems_le1.elas
       # ...etc for cavity2d, heat1d, nlsecu

       # Helper: emit BATCH for a case (used by harness)
       phase8_case_batch() {
         local case=$1
         local var="PHASE8_CASE_${case}_BATCH"
         echo "${!var}"
       }
       phase8_case_module() {
         local case=$1
         local var="PHASE8_CASE_${case}_MODULE"
         echo "${!var}"
       }
       phase8_case_prereq_module() {
         local case=$1
         local var="PHASE8_CASE_${case}_PREREQ_MODULE"
         echo "${!var:-}"
       }
       phase8_case_prereq_batch() {
         local case=$1
         local var="PHASE8_CASE_${case}_PREREQ_BATCH"
         echo "${!var:-}"
       }
       ```
       chmod +x bin/phase8_case_batch_map.sh.

    6. Commit-readiness gate: 5 cases empirically verified end-to-end; map file shippable.
       If hexahedron is later required (Plan 6), it has its OWN out-of-band probe — Task 0 is
       SCOPED to the 5 BUILD-10 cases only.
  </action>
  <verify>
    <automated>cd $MEFISTO && test -x bin/phase8_case_batch_map.sh && bash -c 'set -e; . bin/phase8_case_batch_map.sh; for c in pan2d nafems_le1 cavity2d heat1d nlsecu; do M=$(phase8_case_module $c); B=$(phase8_case_batch $c); [ -n "$M" ] || { echo "MISSING module for $c"; exit 1; }; [ -n "$B" ] || { echo "MISSING batch for $c"; exit 1; }; done' && grep -c '^## End-to-end smoke verdict' .planning/phases/08-ab-validation-on-testa-subset/evidence/00-case-batch-map.md | grep -q '^1$' && grep -v '^#' .planning/phases/08-ab-validation-on-testa-subset/evidence/00-case-batch-map.md | grep -c 'BLOCKED' | grep -q '^0$'</automated>
  </verify>
  <done>bin/phase8_case_batch_map.sh exists + executable + sources cleanly; 5/5 cases have MODULE+BATCH defined; 00-case-batch-map.md records empirical ls + canonical-batch rationale + per-case end-to-end smoke verdict; no BLOCKED cases.</done>
</task>

<task type="auto">
  <name>Task 1: Refresh pp/*_qt via bin/cbl_tout_qt and verify freshness (D-08, Phase 7 Gap-A)</name>
  <files>
    .planning/phases/08-ab-validation-on-testa-subset/evidence/00-bootstrap-log.md
  </files>
  <read_first>
    - bin/cbl_tout_qt (entire file — verify the build sequence + cleanup behavior)
    - .planning/phases/07-image-gif-and-postscript-export/07-UAT.md §"Gap-A" (lines 107-122 — root cause + missing items)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Pattern 4: pp/*_qt freshness (Phase 7 Gap-A)" (lines 368-388 — exact freshness check formula)
  </read_first>
  <action>
    1. Ensure environment is set: export MEFISTO=$PWD; export MEFISTOX=/tmp/mefistox-phase8; mkdir -p $MEFISTOX.
    2. Run `cd $MEFISTO && bin/cbl_tout_qt 2>&1 | tee /tmp/cbl_tout_qt.log`. Verify exit code 0; if non-zero, capture last 50 lines of the log and STOP — do NOT proceed (build must never break per CLAUDE.md §"Working rules"). Report build break to user.
    3. Verify all 5 Qt module binaries were produced and are NEWER than libxvueqt.a:
       LIB_M=$(stat -c '%Y' xvue/qt/build/libxvueqt.a)
       for b in pp/ppmail_qt pp/ppelas_qt pp/ppflui_qt pp/ppther_qt pp/ppnlse_qt; do
         BIN_M=$(stat -c '%Y' "$b")
         [ "$BIN_M" -ge "$LIB_M" ] && echo "OK: $b (mtime $BIN_M >= lib $LIB_M)" || echo "STALE: $b (mtime $BIN_M < lib $LIB_M)"
       done
       Every line MUST start with the EXACT prefix `OK: pp/pp` (no leading space, single space after the colon — the verify gate's grep depends on this exact format per MINOR-10).
    4. Run the existing ABI count gate as a sanity check: `bash xvue/qt/cmake/verify_abi.sh` → must report "nm count: 58 header count: 58" (Phase 7 baseline).
    5. Append a build summary to `.planning/phases/08-ab-validation-on-testa-subset/evidence/00-bootstrap-log.md` with the heading `## Task 1 — pp/*_qt freshness (D-08)` containing:
       - cbl_tout_qt exit code (one line: `cbl_tout_qt_exit=N`)
       - The 5 freshness lines verbatim, each starting with `OK: pp/pp` exactly (5 lines, one per Qt binary; the leading character of each line MUST be 'O', the next 'K', then ': pp/pp' — see verify gate `^OK: pp/pp` regex).
       - The ABI count line verbatim.
       - An ISO date.
  </action>
  <verify>
    <automated>test -f $MEFISTO/pp/ppmail_qt && test -f $MEFISTO/pp/ppelas_qt && test -f $MEFISTO/pp/ppflui_qt && test -f $MEFISTO/pp/ppther_qt && test -f $MEFISTO/pp/ppnlse_qt && LIB_M=$(stat -c '%Y' $MEFISTO/xvue/qt/build/libxvueqt.a); for b in pp/ppmail_qt pp/ppelas_qt pp/ppflui_qt pp/ppther_qt pp/ppnlse_qt; do BIN_M=$(stat -c '%Y' "$MEFISTO/$b"); [ "$BIN_M" -ge "$LIB_M" ] || { echo "STALE: $b"; exit 1; }; done; grep -c '^OK: pp/pp' $MEFISTO/.planning/phases/08-ab-validation-on-testa-subset/evidence/00-bootstrap-log.md | grep -q '^5$'</automated>
  </verify>
  <done>All 5 pp/*_qt binaries exist, each has mtime >= libxvueqt.a mtime, ABI count = 58, and 00-bootstrap-log.md §"Task 1" lists 5 lines starting with the exact prefix `OK: pp/pp`.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap the 3 Phase-7-deferred goldens (D-06) and re-run ctest to flip QSKIPs (D-07)</name>
  <files>
    xvue/qt/tests/golden/scene01.eps
    xvue/qt/tests/golden/wave_legacy.gif
    xvue/qt/tests/golden/cavity2d_legacy.gif
    .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md
    .planning/phases/08-ab-validation-on-testa-subset/evidence/00-bootstrap-log.md
  </files>
  <read_first>
    - xvue/qt/tests/golden/scene01_driver.f (entire file — header BOOTSTRAP NOTE, lines 1-50)
    - bin/convertepsgif (one line — `convert -rotate 90 zfxy0*.eps -extent 980x550 cyl53zfxy.gif`)
    - .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md (entire file — DEFERRED rows to elevate)
    - .planning/phases/07-image-gif-and-postscript-export/VERIFICATION.md §9 (lines 142-200 — exact bootstrap procedure)
    - xvue/qt/tests/test_xvue_qt_postscript.cpp lines 390-450 (QSKIP guard for scene01.eps; understand what PASS demands)
    - xvue/qt/tests/test_xvue_qt_export.cpp lines 500-560 (QSKIP guards for wave_legacy.gif and cavity2d_legacy.gif)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Pattern 3: Plan 1 Bootstrap" (lines 333-366) and §"Assumption A6/A7" (lines 599-606)
  </read_first>
  <action>
    Three independent steps (A, B, C). All three commit goldens to xvue/qt/tests/golden/, then a final ctest run elevates QSKIPs to PASS.

    --- Step A: scene01.eps ---
    1. Build the X11 driver:
       cd /tmp && rm -rf scene01_build && mkdir scene01_build && cd scene01_build
       cp $MEFISTO/xvue/qt/tests/golden/scene01_driver.f .
       gfortran -I$MEFISTO/incl -c scene01_driver.f
       gfortran scene01_driver.o $MEFISTO/xvue/xvuelc.o \
                $MEFISTO/xvue/*.o $MEFISTO/util/*.o \
                -L/usr/X11R6/lib -lX11 -lXt -o scene01_x11 2>&1 | tee link.log
       If link fails on undefined references, fall back to a curated minimal link line per the GOTCHA in 08-RESEARCH.md §Pattern 3: try `gfortran scene01_driver.o $MEFISTO/xvue/xvuelc.o $MEFISTO/xvue/xvinit.o $MEFISTO/xvue/xvouvrir.o $MEFISTO/xvue/xvfermer.o -L/usr/X11R6/lib -lX11 -lXt -o scene01_x11`. The contract is "produces TEMPORAIRE.EPS", not "this exact ld invocation works". Record both attempts in 00-bootstrap-log.md.
    2. Run under Xvfb:
       xvfb-run --auto-servernum ./scene01_x11
       Verify TEMPORAIRE.EPS was produced and is non-empty: test -s TEMPORAIRE.EPS.
    3. Commit golden:
       cp TEMPORAIRE.EPS $MEFISTO/xvue/qt/tests/golden/scene01.eps
       cd $MEFISTO && git add xvue/qt/tests/golden/scene01.eps

    --- Step B: wave_legacy.gif ---
    1. Set up project tree under $MEFISTOX:
       mkdir -p $MEFISTOX/wave && cp -r $MEFISTO/testa/wave/* $MEFISTOX/wave/
    2. Run testa/wave through the X11 backend in batch mode under Xvfb (preserve the exact testa scenario; rely on the case's existing batch script(s)). Record any prerequisite chain (INITIER → MAILLER → FLUIDER) by inspecting testa/wave/ contents. Use MEFISTO_XVSOURIS_AUTOEXIT=1 to short-circuit blocking event reads.
    3. After the case finishes, run `cd $MEFISTOX/wave && bash $MEFISTO/bin/convertepsgif` and verify cyl53zfxy.gif is produced.
    4. Commit golden:
       cp $MEFISTOX/wave/cyl53zfxy.gif $MEFISTO/xvue/qt/tests/golden/wave_legacy.gif
       cd $MEFISTO && git add xvue/qt/tests/golden/wave_legacy.gif

    --- Step C: cavity2d_legacy.gif ---
    Identical to Step B with testa/cavity2d. Final commit path: xvue/qt/tests/golden/cavity2d_legacy.gif.

    --- Final: ctest re-verify (D-07) ---
    cd $MEFISTO/xvue/qt/build && xvfb-run --auto-servernum ctest -R '^xvue_qt_(postscript|export)_tests$' --output-on-failure 2>&1 | tee /tmp/phase8_ctest.log
    Confirm 0 SKIP across both targets:
      grep -c 'SKIP\|Skipping' /tmp/phase8_ctest.log
    The grep MUST return 0 (no SKIP lines). Record the full ctest summary line ("Total tests: N — Passed: N — Skipped: 0") in 00-bootstrap-log.md.

    --- Update Phase-7 VALIDATION-LOG.md ---
    Edit .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md and replace each of the 3 DEFERRED rows in the "Manual A/B sign-off" table with PASS rows dated today. Append a final note: "Elevated DEFERRED→PASS by Phase 8 Plan 1 Task 2 on YYYY-MM-DD; goldens committed under xvue/qt/tests/golden/."

    --- Append bootstrap evidence ---
    Append to 00-bootstrap-log.md a heading `## Task 2 — Phase-7 deferred goldens (D-06, D-07)` containing: scene01.eps SHA-256, wave_legacy.gif SHA-256 + frame count from `identify wave_legacy.gif | wc -l`, cavity2d_legacy.gif SHA-256 + frame count, ctest summary line, ISO date.

    All three steps (A, B, C) MUST commit-or-fail atomically: if any one step fails, do NOT proceed to the ctest re-verify; document the failure in 00-bootstrap-log.md and STOP for human follow-up. Per phase boundary discipline (08-RESEARCH.md §"Phase Boundary Discipline"), do NOT modify xvue/qt/ source to work around bootstrap failures — escalate.
  </action>
  <verify>
    <automated>test -s $MEFISTO/xvue/qt/tests/golden/scene01.eps && test -s $MEFISTO/xvue/qt/tests/golden/wave_legacy.gif && test -s $MEFISTO/xvue/qt/tests/golden/cavity2d_legacy.gif && cd $MEFISTO/xvue/qt/build && xvfb-run --auto-servernum ctest -R '^xvue_qt_(postscript|export)_tests$' --output-on-failure 2>&1 | grep -v '^#' | grep -c -E 'SKIP|Skipping' | grep -q '^0$' && grep -c '^| 2026-' $MEFISTO/.planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md | awk '$1>=3 {exit 0} {exit 1}'</automated>
  </verify>
  <done>3 goldens exist (non-empty), ctest reports 0 SKIP for postscript+export targets, VALIDATION-LOG.md has at least 3 PASS rows for the goldens, bootstrap-log.md §"Task 2" present.</done>
</task>

<task type="auto">
  <name>Task 3: Ship sweep-harness scripts (bin/ab_compare_pair.sh, bin/ab_capture_x11.sh, bin/ab_sweep_phase8.sh) and run per-case AUTOEXIT smoke probes via --smoke-only</name>
  <files>
    bin/ab_compare_pair.sh
    bin/ab_capture_x11.sh
    bin/ab_sweep_phase8.sh
    .planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md
    .planning/phases/08-ab-validation-on-testa-subset/evidence/00-bootstrap-log.md
  </files>
  <read_first>
    - bin/phase8_case_batch_map.sh (Task 0 deliverable — the harness sources this)
    - .planning/phases/08-ab-validation-on-testa-subset/evidence/00-case-batch-map.md (Task 0 evidence — confirm 5/5 cases verified)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Pattern 1: Headless A/B Sweep Loop" (lines 237-294 — canonical Qt-side, X11-side examples)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Pattern 2: AE-fuzz Tolerance Gate (with dimension guard)" (lines 296-331 — the EXACT skeleton this task implements as bin/ab_compare_pair.sh)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Code Examples / tolerance-band gate wrapper" (lines 537-567 — verbatim shell skeleton to match)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Pitfall 1, 2, 3, 4, 5, 8" (lines 415-464 — every guard the harness must encode)
    - bin/cbl_tout_qt (existing harness style — match the same shebang + set -euo pipefail discipline)
    - .planning/codebase/TESTING.md (existing test conventions — preserve them)
  </read_first>
  <action>
    Create three executable shell scripts under bin/. Use Write tool (do NOT use heredocs from Bash). After creation: chmod +x on each.

    --- bin/ab_compare_pair.sh (tolerance-band gate) ---

    **STRICT EXECUTION ORDER** (per BLOCKER #5 from iter1 review). Add the following as a header
    comment at the top of the script BEFORE any other comment:
      # ORDER: argc → fuzz → identify → compare
      # This contract is load-bearing — Plan 1 Task 3 verify gate `bash bin/ab_compare_pair.sh
      # /tmp/_pair_a.png /tmp/_pair_b.png /tmp/_diff.png 50 2>&1 | grep -q 'fuzz must be in'`
      # depends on FUZZ validation happening BEFORE identify is called on (non-existent) PNGs.
      # Re-ordering steps WILL break the verify gate.

    Implementation order in the script body MUST be EXACTLY:
      1. shebang `#!/usr/bin/env bash`
      2. `set -euo pipefail`
      3. argc check: `[ "$#" -ge 3 ] || { echo "usage: $0 A.png B.png DIFF.png [FUZZ%]"; exit 2; }`
      4. Parse positional args: A=$1; B=$2; DIFF=$3; FUZZ=${4:-5}
      5. **FUZZ validation BEFORE any file probe**:
           [[ "$FUZZ" =~ ^[0-9]+$ ]] || { echo "ab_compare_pair: fuzz must be in [1,30] (got: $FUZZ)" >&2; exit 2; }
           [ "$FUZZ" -ge 1 ] && [ "$FUZZ" -le 30 ] || { echo "ab_compare_pair: fuzz must be in [1,30] (got: $FUZZ)" >&2; exit 2; }
      6. identify dim probe on A and B (only AFTER fuzz validation passed):
           DA=$(identify -format "%wx%h" "$A")
           DB=$(identify -format "%wx%h" "$B")
      7. Dimension guard: if DA != DB, resample B to DA via `convert "$B" -filter point -resize "${DA}!" "$B_RESAMPLED"`; emit `RESAMPLED: B (DB) → BR (DA)` to stderr.
      8. compare invocation: `compare -metric AE -fuzz "${FUZZ}%" "$A" "$B" "$DIFF" 2>&1`. Capture exit code.
      9. Final stdout line — single line, machine-parseable, used by ab_sweep_phase8.sh:
           echo "ae=$AE total=$TOTAL_PX pct=$AE_PCT% verdict=$VERDICT diff=$DIFF resampled=${RESAMPLED:-no}"

    Mitigations: T-08-02, T-08-03. The ORDER discipline is the dominant safeguard against
    fixture-order surprises (BLOCKER #5).

    --- bin/ab_capture_x11.sh (Xvfb + import wrapper) ---
    Wrapper around the X11 capture pattern in 08-RESEARCH.md §"Pattern 1 / canonical X11-side":
      Args: --case CASE --module MODULE --batch BATCH_FILE --out OUT.png [--display :99] [--hold-ms 1500]
      a. Default DISPLAY=:99; READY_FILE=/tmp/ab_ready_${CASE}_$$.
      b. Export env: DISPLAY, MEFISTO_XVFERMER_READY_FILE=$READY_FILE, MEFISTO_XVFERMER_HOLD_MS=${HOLD_MS:-1500}, MEFISTO_XVSOURIS_AUTOEXIT=1, MEFISTO_XVSOURIS_AUTOEXIT_DELAY_MS=500.
      c. **Workspace+cwd discipline (per BLOCKER #3)**: caller is responsible for cd'ing into
         $MEFISTOX/$CASE BEFORE invoking this script. Document this contract in the script header.
      d. Launch pp/pp${MODULE} ${BATCH_FILE} in background; capture PID. Implement a 60s overall timeout using `(sleep 60 && kill -TERM $PID 2>/dev/null) &`.
      e. Poll: `while [ ! -f "$READY_FILE" ]; do sleep 0.1; [ ! -d /proc/$PID ] && break; done`. Emit a warning to stderr if poll exited because process died before READY_FILE appeared.
      f. Capture: `DISPLAY=$DISPLAY import -window root "$OUT"`.
      g. wait $PID; capture exit code; remove READY_FILE.
      h. Emit a single machine-parseable status line on stdout: `case=$CASE module=$MODULE backend=x11 out=$OUT exit=$EXIT_CODE size=$(stat -c '%s' "$OUT" 2>/dev/null || echo 0)`.
    Wrap the entire script body inside `xvfb-run --auto-servernum -s "-screen 0 1280x800x24" -- bash -c '...'` ONLY if the caller did not already export DISPLAY. Detect via `[ -z "${DISPLAY:-}" ]`. Otherwise reuse the caller's display. Add a shebang + `set -euo pipefail` + 5-line header comment.

    --- bin/ab_sweep_phase8.sh (top-level harness) ---
    Args: --mode {x11|qt-1x|qt-2x|qt-omp} --cases CSV [--fuzz N] [--out-dir DIR] [--evidence-log PATH] [--smoke-only] [--baseline PATH]
    Defaults: --out-dir=.planning/phases/08-ab-validation-on-testa-subset/evidence; --fuzz=5; --evidence-log=$OUT_DIR/sweep-log-${MODE}.md.

    **NEW FLAGS** (per BLOCKER #2 + MAJOR-7):
      --smoke-only   Skip the X11-baseline-compare step entirely. The harness still captures, but
                     does NOT call ab_compare_pair.sh. Used by Plan 1 Task 3 BEFORE any X11 baseline
                     exists (originally the harness's exit-4 abort would prevent smoke testing).
                     With --smoke-only, the harness records `verdict=SMOKE` per cell (no compare).
      --baseline PATH  Override the default `${OUT_DIR}/${CASE}-x11.png` LEFT operand for compare.
                     Plan 5 Task 2 uses `--baseline ${OUT_DIR}/${CASE}-x11-omp.png` to compare
                     Qt-OMP captures against OMP-context-matched X11-OMP baselines (NOT the
                     deterministic 1x baseline). When --baseline is set, the harness substitutes
                     `${BASELINE_PATH/${CASE}/${CASE}}` (i.e., per-case path resolution) for the
                     LEFT operand of every compare call.

    Behavior:
      a. Parse args; validate MODE is one of the four allowed strings.
      b. **Source the case-batch map** (per BLOCKER #1): `. "$MEFISTO/bin/phase8_case_batch_map.sh"`.
         For each case in --cases CSV, look up MODULE + BATCH via `phase8_case_module $CASE` /
         `phase8_case_batch $CASE`. If empty → exit 3 with "case $CASE not in phase8_case_batch_map.sh".
         If a prereq is defined (`phase8_case_prereq_module $CASE`), run the prereq pp/* invocation
         FIRST (cwd = $MEFISTOX/$CASE), then the main module.
      c. For each case, dispatch to the right capture path (caller cds into $MEFISTOX/$CASE first):
         - x11    → invoke bin/ab_capture_x11.sh (sets up Xvfb + import); record OUT=$OUT_DIR/${CASE}-x11.png
         - qt-1x  → in-process: env QT_QPA_PLATFORM=offscreen MEFISTO_QT_CAPTURE_PATH=$OUT_DIR/${CASE}-qt-1x.png MEFISTO_XVSOURIS_AUTOEXIT=1 MEFISTO_XVSOURIS_AUTOEXIT_DELAY_MS=500 timeout 60 pp/pp${MODULE}_qt $BATCH
         - qt-2x  → same as qt-1x plus QT_SCALE_FACTOR=2; OUT=${CASE}-qt-2x.png
         - qt-omp → same as qt-1x plus OMP_NUM_THREADS=8; OUT=${CASE}-qt-omp.png. SKIP cavity2d here (no FLUIDER_OMP exists per research finding); emit `case=cavity2d mode=qt-omp verdict=N-A reason="no FLUIDER_OMP launcher (D-05)"`.
      d. After capture:
         - If --smoke-only: skip compare entirely; emit `case=$CASE mode=$MODE verdict=SMOKE out=$OUT size=...` and continue.
         - Else if MODE != x11: call bin/ab_compare_pair.sh ${BASELINE_PATH:-${OUT_DIR}/${CASE}-x11.png} OUT_THIS DIFF (where ${CASE} is interpolated into BASELINE_PATH if it contains `${CASE}`). Require the resolved baseline exists or exit 4 with "X11 baseline missing — run --mode x11 first or pass --baseline PATH". Forward the AE/verdict line to evidence log.
      e. Append every cell's machine-parseable status + verdict line to the evidence log under a heading `## Sweep ${MODE} (YYYY-MM-DD HH:MM:SS UTC)`.
      f. Final exit code: 0 if all cells emitted (PASS or CHECK or N-A or SMOKE); non-zero if a capture step crashed or the X11 baseline was missing in non-x11 modes (and --smoke-only was NOT set).
    Add shebang + `set -euo pipefail` + 8-line header comment. ALL three scripts must be `chmod +x`.

    --- Per-case AUTOEXIT smoke probes (Pitfall 8, Open Question 3) ---
    Run each of the 5 cases under qt-1x WITHOUT comparing — purely to verify the case completes without hang. Use the new bin/ab_sweep_phase8.sh **with `--smoke-only`** (per BLOCKER #2):
      cd $MEFISTO && bin/ab_sweep_phase8.sh --mode qt-1x --smoke-only --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu --out-dir /tmp/phase8-smoke 2>&1 | tee /tmp/phase8-smoke.log
    For each case, record:
      - exit code from the pp/*_qt invocation
      - capture file size (>0 means MEFISTO_QT_CAPTURE_PATH fired at least once)
      - count of "MEFISTO_QT_CAPTURE_PATH=" lines in stderr (proxy for # xvfermer_ calls — each call logs one line per xvue_qt_api.cpp:872+883)
    Append findings to .planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md under headings `## pan2d / nafems_le1 / cavity2d / heat1d / nlsecu`. Each section MUST contain: exit_code=N, capture_size_bytes=N, capture_call_count=N, status (OK if exit=0 AND capture_size>0).
    If ANY case has exit != 0 or capture_size == 0, mark its section as STATUS=BLOCKED and document the symptom verbatim. Plan 2 cannot proceed if ANY case is BLOCKED — escalate via 00-smoke-probes.md "## Open Items" section. Cross-reference with Task 0's 00-case-batch-map.md per-case verdict — these MUST agree (both green or both blocked).

    --- Append final summary ---
    Append `## Task 3 — Sweep harness + smoke probes` to 00-bootstrap-log.md with: 4 script paths (including bin/phase8_case_batch_map.sh from Task 0) + their +x mode, smoke probe summary (5 lines, one per case), ISO date.
  </action>
  <verify>
    <automated>test -x $MEFISTO/bin/ab_compare_pair.sh && test -x $MEFISTO/bin/ab_capture_x11.sh && test -x $MEFISTO/bin/ab_sweep_phase8.sh && grep -q '^# ORDER: argc → fuzz → identify → compare' $MEFISTO/bin/ab_compare_pair.sh && bash $MEFISTO/bin/ab_compare_pair.sh /tmp/_pair_a.png /tmp/_pair_b.png /tmp/_diff.png 50 2>&1 | grep -q 'fuzz must be in' && grep -q -E '\-\-smoke-only|smoke_only' $MEFISTO/bin/ab_sweep_phase8.sh && grep -q -E '\-\-baseline|baseline_path' $MEFISTO/bin/ab_sweep_phase8.sh && grep -c '^## ' $MEFISTO/.planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md | awk '$1>=5 {exit 0} {exit 1}' && grep -v '^#' $MEFISTO/.planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md | grep -c 'STATUS=BLOCKED' | grep -q '^0$'</automated>
  </verify>
  <done>3 scripts exist + are executable, ab_compare_pair.sh has the ORDER header comment AND rejects fuzz=50 with the expected error, ab_sweep_phase8.sh supports --smoke-only AND --baseline, all 5 testa cases complete the smoke probe with STATUS=OK, 00-bootstrap-log.md and 00-smoke-probes.md committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build artifact freshness (libxvueqt.a → pp/*_qt) | Stale Qt binaries silently produce a false-PASS A/B sweep |
| testa case-directory ↔ harness glob assumption | A naive `*.{ext}` glob misses 3 of 5 cases (cavity2d, nlsecu, hexahedron); BLOCKER #1 from iter1 review |
| Workspace cwd discipline ↔ pp/* invocation | Legacy launchers cd into $MEFISTOX/$nomproj BEFORE running pp/*; harness must replicate or pp/* writes to the wrong tree |
| Xvfb display ↔ pp/* legacy backend | The X11 capture mechanism (READY_FILE + import -window root) crosses an IPC boundary; missed sentinel = mis-captured frame |
| testa case batch driver ↔ MEFISTO_XVSOURIS_AUTOEXIT | A blocking event read that AUTOEXIT cannot satisfy will hang the case past the 60s timeout |
| Fortran ↔ external link line for scene01_driver.f | The link-line glob (xvue/*.o util/*.o) may pull in OMP-only objects breaking the symbol resolution |
| ab_compare_pair.sh execution order ↔ verify-gate fuzz check | If `identify` runs BEFORE fuzz validation, the gate `... 50 2>&1 \| grep 'fuzz must be in'` triggers an `identify` error first and never reaches fuzz validation; BLOCKER #5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | pp/*_qt (stale build artifact) | mitigate | D-08 + Task 1 freshness guard: stat -c '%Y' check for every pp/*_qt vs libxvueqt.a; abort if any STALE. Verified by Pattern 4 in 08-RESEARCH.md. |
| T-08-29 | Spoofing | Harness glob misses 3 of 5 cases (BLOCKER #1) | mitigate | Task 0 builds bin/phase8_case_batch_map.sh empirically by `ls testa/$CASE/`; harness sources the map (no glob assumption). 00-case-batch-map.md records verbatim ls output as evidence. |
| T-08-30 | Tampering | Workspace cwd not honored — pp/* writes to wrong tree (BLOCKER #3) | mitigate | Task 0 step 3 empirically runs each case under workspace-cp + cwd-change AND records exit code + capture size; bin/ab_capture_x11.sh + bin/ab_sweep_phase8.sh require the caller to cd into $MEFISTOX/$CASE first (header comment). |
| T-08-08 | Spoofing | bin/ab_capture_x11.sh (process death before READY_FILE) | mitigate | Polling loop watches both READY_FILE existence AND /proc/$PID; emits stderr warning if process died first. Capture is taken after sentinel only. |
| T-08-09 | Information Disclosure | scene01_driver.f bootstrap link line | accept | Falls back to a curated minimal link line if the wide glob fails (per 08-RESEARCH.md §Pattern 3 GOTCHA). Failure mode is "build error", visible immediately. |
| T-08-10 | Denial of Service | testa case hangs under AUTOEXIT (Pitfall A1) | mitigate | 60s timeout in ab_capture_x11.sh + qt-1x dispatch in ab_sweep_phase8.sh. Smoke probes in Task 3 (with --smoke-only per BLOCKER #2) surface any hang BEFORE Plan 2 sweep launches. |
| T-08-11 | Tampering | xvue/qt/ source modified during Phase 8 | mitigate | Plan boundary discipline (08-RESEARCH.md §"Phase Boundary Discipline"): files_modified excludes xvue/qt/src/, xvue/xvuelc.c. Verifier grep gate at phase close. |
| T-08-12 | Repudiation | Phase 7 VALIDATION-LOG.md DEFERRED rows persist after goldens land | mitigate | Task 2 explicitly edits the log to PASS rows + adds an elevation note with date and Phase 8 Plan 1 attribution. |
| T-08-31 | Tampering | ab_compare_pair.sh order changed (fuzz validation moved AFTER identify) (BLOCKER #5) | mitigate | Header comment `# ORDER: argc → fuzz → identify → compare` documents the contract; verify gate exercises the fuzz=50 path which only succeeds if FUZZ check fires before identify; later edits trip the gate. |
| T-08-32 | Spoofing | --smoke-only flag missing — Plan 1 Task 3 smoke probe aborts on missing baseline (BLOCKER #2) | mitigate | --smoke-only flag added to ab_sweep_phase8.sh; verify gate greps for the flag in the script body. |
</threat_model>

<verification>
After all 4 tasks (Task 0 + Tasks 1-3):

1. bin/phase8_case_batch_map.sh exists + executable + sources cleanly + defines MODULE+BATCH for all 5 cases.
2. 00-case-batch-map.md records empirical ls output + canonical batch rationale + 5/5 end-to-end smoke verdicts (no BLOCKED).
3. `bin/cbl_tout_qt` rebuild produced 5 fresh pp/*_qt with mtime >= libxvueqt.a; ABI count == 58.
4. xvue/qt/tests/golden/{scene01.eps,wave_legacy.gif,cavity2d_legacy.gif} all exist, are non-empty, and are committed via git add.
5. `cd xvue/qt/build && xvfb-run --auto-servernum ctest -R '^xvue_qt_(postscript|export)_tests$'` reports 0 SKIP across both targets (D-07 gate).
6. .planning/phases/07-image-gif-and-postscript-export/VALIDATION-LOG.md no longer has any DEFERRED rows for goldens — they are PASS as of today.
7. bin/ab_compare_pair.sh has the `# ORDER: argc → fuzz → identify → compare` header comment.
8. bin/ab_compare_pair.sh, bin/ab_capture_x11.sh, bin/ab_sweep_phase8.sh exist, are executable, and pass their respective fuzz-validation / mode-parse smoke checks.
9. bin/ab_sweep_phase8.sh supports both --smoke-only and --baseline flags.
10. .planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md shows STATUS=OK for all 5 testa cases.
11. xvue/xvuelc.c is byte-identical to its pre-Phase-8 state: `git diff -- xvue/xvuelc.c` is empty.
12. xvue/qt/src/ is byte-identical to its pre-Phase-8 state: `git diff -- xvue/qt/src/` is empty.
</verification>

<success_criteria>
- bin/phase8_case_batch_map.sh ships with empirical 5-case map (BLOCKER #1, #3 fix).
- All pp/*_qt binaries are fresh (mtime check OK on all 5).
- 3 Phase-7-deferred goldens committed; ctest postscript+export targets show 0 SKIP.
- 3 sweep-harness scripts ship under bin/ with +x mode; ab_compare_pair.sh has ORDER header (BLOCKER #5); ab_sweep_phase8.sh supports --smoke-only (BLOCKER #2) and --baseline (MAJOR-7).
- 5 testa cases complete a 60s AUTOEXIT smoke probe under offscreen with non-zero capture file size.
- xvue/qt/ and xvue/xvuelc.c remain byte-identical to pre-Phase-8 state.
- VALIDATION-LOG.md elevated DEFERRED → PASS for all 3 goldens.
- 00-bootstrap-log.md + 00-case-batch-map.md + 00-smoke-probes.md committed under evidence/.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ab-validation-on-testa-subset/08-01-SUMMARY.md` summarizing:
- Case-batch map (5 entries: case → module → batch; any prereq notes)
- pp/*_qt freshness check (5 lines)
- 3 golden SHA-256 + frame counts (3 lines)
- ctest result line ("Total: N — Passed: N — Skipped: 0")
- VALIDATION-LOG.md edit confirmation (3 PASS rows replacing DEFERRED)
- 4 harness scripts with line counts (phase8_case_batch_map.sh + ab_compare_pair.sh + ab_capture_x11.sh + ab_sweep_phase8.sh)
- New harness flags shipped: --smoke-only, --baseline
- 5 smoke-probe results (one per case)
- Total wall-clock time
- Hand-off note: "Wave 2 (Plans 2-5) unblocked. ab_sweep_phase8.sh is the canonical entry point. Plans 2-5 source phase8_case_batch_map.sh; no glob assumptions remain."
</output>
