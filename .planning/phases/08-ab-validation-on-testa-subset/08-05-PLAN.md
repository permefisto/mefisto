---
phase: 08-ab-validation-on-testa-subset
plan: 05
type: execute
wave: 2
# PARALLEL SAFETY CONTRACT (per MAJOR-6 from iter1 review + WARNING-2 from iter2 review):
# This plan runs in wave 2 alongside Plans 02, 03, 04 — all parallel-safe.
# - files_modified contain ONLY *-omp.png + sweep-log-omp.md (zero overlap with Plans 02/03/04 outputs)
# - OMP_NUM_THREADS=8 is set per-process via env (not exported globally), so worker processes don't
#   contaminate sibling Qt-1x / Qt-2x / X11-deterministic captures running concurrently.
# - The harness creates per-mode workspace dirs (/tmp/mefistox-phase8-omp) — no shared cwd contention.
# - bin/phase8_case_batch_map.sh is sourced (read-only) — no write contention with other plans.
# - All Xvfb invocations use `xvfb-run --auto-servernum` to avoid display-number collisions across
#   wave-2 parallel plans (02/03/04/05). Each invocation gets an auto-allocated display number.
# - Capture output paths are case-and-mode keyed under evidence/ — no collision with Plans 02/03/04.
# - Pitfall-5 noise-floor pan2d re-run uses `/tmp/_pan2d-x11-omp-noise-*` keyed names so the
#   noise-floor temp files do NOT collide with sibling Plan 02/03/04 temp paths.
depends_on:
  - 08-01
files_modified:
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-x11-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-x11-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-x11-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-x11-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-qt-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-qt-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-qt-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-qt-omp.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-qt-omp-diff.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-qt-omp-diff.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-qt-omp-diff.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-qt-omp-diff.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md
autonomous: true
requirements:
  - VALID-03
must_haves:
  truths:
    - "All 4 OMP-eligible cases (pan2d, nafems_le1, heat1d, nlsecu) produce a non-empty Qt OMP capture under OMP_NUM_THREADS=8 + offscreen + MEFISTO_QT_CAPTURE_PATH"
    - "All 4 OMP-eligible cases also produce an X11 OMP capture under OMP_NUM_THREADS=8 + Xvfb + bin/ab_capture_x11.sh (the OMP context for the LEFT operand of compare per Open Question 2 recommendation)"
    - "cavity2d-omp is recorded as N-A in the OMP sweep log (no bin/FLUIDER_OMP launcher exists per research finding 3)"
    - "Each Qt OMP capture is compared against the matching evidence/${CASE}-x11-omp.png via ab_compare_pair.sh -fuzz 5%"
    - "Per BLOCKER #4 (iter1 review): nafems_le1 ELASTICER_OMP equivalence check runs ONCE — bin/ELASTICER_OMP < <(echo nafems_le1) AE-compared against direct OMP_NUM_THREADS=8 pp/ppelas to prove launcher = direct binary equivalence (VALID-03 literally references the launcher name)"
    - "Qt-side XVUE_QT_ASSERT_MAIN_THREAD() guards held for every public ABI call across all 4 cases (no Q_ASSERT abort)"
    - "Plan 5 uses ab_sweep_phase8.sh's --baseline flag to point compare at ${CASE}-x11-omp.png (no manual rerun + sweep-log pollution per MAJOR-7); the --baseline value is single-quoted so the LITERAL token ${CASE} is passed to the harness, which performs literal-string substitution (per BLOCKER-B iter2)"
    - "xvue/qt/src/ byte-identical to pre-Phase-8 state"
  artifacts:
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-x11-omp.png"
      provides: "X11 OMP baseline (OMP_NUM_THREADS=8) for pan2d (mesher)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-x11-omp.png"
      provides: "X11 OMP baseline for nafems_le1 (elasticity)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-x11-omp.png"
      provides: "X11 OMP baseline for heat1d (thermal)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-x11-omp.png"
      provides: "X11 OMP baseline for nlsecu (nonlinear)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-qt-omp.png"
      provides: "Qt OMP capture for pan2d (mesher)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-qt-omp.png"
      provides: "Qt OMP capture for nafems_le1 (elasticity)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-qt-omp.png"
      provides: "Qt OMP capture for heat1d (thermal)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-qt-omp.png"
      provides: "Qt OMP capture for nlsecu (nonlinear)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md"
      provides: "Per-case OMP_NUM_THREADS=8 verdict + OMP-noise floor measurement + cavity2d N-A annotation"
      min_lines: 40
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md"
      provides: "BLOCKER #4 mitigation: ELASTICER_OMP launcher vs direct OMP_NUM_THREADS=8 pp/ppelas equivalence proof for nafems_le1"
      min_lines: 15
  key_links:
    - from: "OMP_NUM_THREADS=8"
      to: "pp/pp{mail,elas,ther,nlse} + pp/pp{mail,elas,ther,nlse}_qt"
      via: "shared libgomp.so.1 link (verified by 08-RESEARCH.md OMP Sweep section); same binary as serial, env var only changes"
      pattern: "OMP_NUM_THREADS=8"
    - from: "bin/ELASTICER_OMP"
      to: "pp/ppelas"
      via: "launcher script exports OMP_NUM_THREADS=8 then exec's pp/ppelas — equivalent to direct env-set invocation. Plan 5 Task 0 proves this empirically (BLOCKER #4)"
      pattern: "ELASTICER_OMP"
    - from: "Qt-side ABI entry points (all 57)"
      to: "XVUE_QT_ASSERT_MAIN_THREAD()"
      via: "Phase 1 SHELL-07 instrumentation; under QT_DEBUG, off-thread calls trip Q_ASSERT and abort"
      pattern: "XVUE_QT_ASSERT_MAIN_THREAD"
    - from: "evidence/${CASE}-qt-omp.png"
      to: "evidence/${CASE}-x11-omp.png"
      via: "ab_sweep_phase8.sh --baseline flag (MAJOR-7) with literal-token substitution (BLOCKER-B iter2) — no manual rerun"
      pattern: "ab_sweep_phase8.*--baseline"
---

<objective>
Capture the OMP column for the 4 OMP-eligible canonical testa cases under
OMP_NUM_THREADS=8. This plan PRODUCES column 4 of the CHECKLIST.md verdict
matrix (Qt OMP). Per D-05 + research finding 3: cavity2d has NO bin/FLUIDER_OMP
launcher and is therefore recorded as N-A; the remaining 4 cases (pan2d /
nafems_le1 / heat1d / nlsecu) get full OMP captures + comparisons.

Per Open Question 2's recommendation, the OMP cell compares Qt-OMP against
**X11-OMP** (same OMP context, same expected scheduling drift), not against
the deterministic X11-1x baseline from Plan 02. This requires capturing 4
extra X11 baselines under OMP_NUM_THREADS=8, AND uses the harness's new
`--baseline` flag (MAJOR-7) to point ab_compare_pair.sh at the OMP-matched
LEFT operand without a manual rerun + sweep-log pollution.

**BLOCKER #4 (iter1 review)** demands an explicit ELASTICER_OMP-launcher vs
direct-binary equivalence proof, since VALID-03 literally references
"ELASTICER_OMP" by name and D-05 broadens to "_OMP variants where they exist"
— the launcher IS the _OMP variant even though the underlying binary is the
same as pp/ppelas. Task 0 below runs that proof ONCE for nafems_le1
(launcher AE-compare against direct env-set invocation, expected diff = 0)
and documents that the harness uses direct binary invocation for the
remaining 3 modules (mesher/thermal/nlse) since the launcher behavior is
proven equivalent. cavity2d remains N-A (no FLUIDER_OMP exists per research
finding 3).

Purpose: VALID-03 demands "all 5 cases pass through ELASTICER_OMP" — D-05
broadens to all 5 _OMP variants where they exist. The risk this gates is
"Qt layer's main-thread-only invariant under OMP solver parallelism": if a
Fortran solver thread tries to call a graphics ABI from a worker thread,
XVUE_QT_ASSERT_MAIN_THREAD() trips a Q_ASSERT and aborts. The Plan 5 sweep
provides the empirical evidence that this guard holds (or surfaces a real
defect to escalate as a gap-closure plan).

Output: 4 X11-OMP PNGs + 4 Qt-OMP PNGs + 4 diff PNGs + 1 sweep log +
1 ELASTICER_OMP-equivalence check report + 1 N-A annotation for cavity2d.
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ab-validation-on-testa-subset/08-CONTEXT.md
@.planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md
@.planning/phases/08-ab-validation-on-testa-subset/08-01-SUMMARY.md
@.planning/phases/08-ab-validation-on-testa-subset/08-02-SUMMARY.md
@bin/ab_sweep_phase8.sh
@bin/ab_compare_pair.sh
@bin/ab_capture_x11.sh
@bin/phase8_case_batch_map.sh
@bin/ELASTICER_OMP
@bin/MAILLER_OMP
@bin/THERMICER_OMP
@bin/NLSER_OMP
@CLAUDE.md

<interfaces>
<!-- OMP launcher contract — verified by 08-RESEARCH.md §"OMP Sweep — How _OMP Actually Works" + Read tool inspection of the four launchers. -->

OMP launcher pattern (verified):
  bin/ELASTICER_OMP: exports OMP_NUM_THREADS=8 then exec's pp/ppelas (the SAME binary as bin/ELASTICER).
  bin/MAILLER_OMP:   same pattern with pp/ppmail.
  bin/THERMICER_OMP: same with pp/ppther.
  bin/NLSER_OMP:     same with pp/ppnlse.
  bin/FLUIDER_OMP:   does NOT exist on this host (verified by `ls bin/FLUIDER_OMP 2>&1` → "No such file or directory").

Mapping (D-05 + research finding 3):
  pan2d      → mail    → MAILLER_OMP exists    → run pan2d-omp cell
  nafems_le1 → elas    → ELASTICER_OMP exists  → run nafems_le1-omp cell (+ launcher equiv check, BLOCKER #4)
  cavity2d   → flui    → FLUIDER_OMP MISSING   → cavity2d-omp = N-A
  heat1d     → ther    → THERMICER_OMP exists  → run heat1d-omp cell
  nlsecu     → nlse    → NLSER_OMP exists      → run nlsecu-omp cell

Qt-side main-thread guard (verified by 08-RESEARCH.md §"OMP Sweep"):
  - Every public ABI entry in xvue/qt/src/xvue_qt_api.cpp opens with XVUE_QT_ASSERT_MAIN_THREAD() (Phase 1 SHELL-07).
  - Under QT_DEBUG (debug build), off-main-thread calls trip Q_ASSERT and abort the process.
  - Under release build, the macro expands to no-op; off-thread calls return normally but undefined behavior is unsupported.
  - Phase 8 cells should ideally run debug build to surface assertion failures clearly, but release build is acceptable
    (Phase 1's all-57-stubs validation is the primary guarantee; Plan 5 surfaces dynamic-runtime regressions).

Both pp/ppelas (X11) and pp/ppelas_qt (Qt) link libgomp.so.1 (verified by 08-RESEARCH.md §Pitfall 5
+ in-session ldd checks). The OMP code path lives in the Fortran solver; the graphics layer is invoked from
the main thread regardless of OMP scheduling. The only risk is a refactor that accidentally calls a graphics
ABI from a worker thread — that's exactly what Plan 5 tests dynamically.

ab_sweep_phase8.sh new flags (Plan 1 Task 3 deliverable, per BLOCKER #2 + MAJOR-7 + BLOCKER-B iter2):
  --smoke-only      (Plan 1 used this; Plan 5 does NOT)
  --baseline PATH   Override LEFT operand of compare. The PATH may contain the LITERAL token `${CASE}`
                    for per-case interpolation. The harness performs LITERAL-STRING substitution
                    (NOT eval) inside the per-case loop:
                      RESOLVED_BASELINE="${BASELINE_PATH//\$\{CASE\}/$CURRENT_CASE}"
                    Callers MUST single-quote the --baseline value so the shell does not interpolate
                    `${CASE}` at invocation time:
                      --baseline '.planning/phases/08-ab-validation-on-testa-subset/evidence/${CASE}-x11-omp.png'
                    Plan 5 Task 2 uses exactly this pattern. (Per BLOCKER-B iter2 review.)
                    Eliminates the need to invoke ab_compare_pair.sh manually after the sweep.

bin/phase8_case_batch_map.sh (Plan 1 Task 0 deliverable):
  Source it: `. "$MEFISTO/bin/phase8_case_batch_map.sh"`
  Lookups: `phase8_case_module $CASE` and `phase8_case_batch $CASE`
  Plan 5 uses these for the workspace+cwd discipline + correct batch-file naming for cavity2d (irrelevant — N-A) + nlsecu (which has no `.nlse` extension).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: BLOCKER #4 mitigation — prove bin/ELASTICER_OMP equivalence with direct OMP_NUM_THREADS=8 pp/ppelas</name>
  <files>
    .planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md
  </files>
  <read_first>
    - bin/ELASTICER_OMP (entire file — verify it's literally `OMP_NUM_THREADS=8` + `exec pp/ppelas`-style)
    - bin/phase8_case_batch_map.sh (Plan 1 Task 0 — confirm `phase8_case_batch nafems_le1` returns the canonical batch)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"OMP Sweep — How _OMP Actually Works" (lines 728-742)
  </read_first>
  <action>
    VALID-03 literally references "ELASTICER_OMP" by name. The remaining D-05 _OMP launchers
    (MAILLER_OMP, THERMICER_OMP, NLSER_OMP) follow the same pattern. Plan 5's harness uses
    direct `OMP_NUM_THREADS=8 pp/pp${MODULE}_qt` invocation (not the launcher) for ergonomic
    reasons. This task proves the equivalence ONCE for nafems_le1 — once the proof holds,
    direct invocation for the other 3 modules is justified.

    1. Source the case-batch map and locate canonical batch:
       export MEFISTO=$PWD
       . "$MEFISTO/bin/phase8_case_batch_map.sh"
       BATCH=$(phase8_case_batch nafems_le1)   # → nafems_le1.elas (or whatever Task 0 chose)

    2. Set up an isolated workspace:
       export MEFISTOX=/tmp/mefistox-phase8-omp-equiv
       rm -rf $MEFISTOX && mkdir -p $MEFISTOX/nafems_le1
       cp -r $MEFISTO/testa/nafems_le1/* $MEFISTOX/nafems_le1/

    3. RUN A — via launcher bin/ELASTICER_OMP. The launcher reads project name from stdin.
       Prepare a workspace clone for the launcher (do NOT contaminate the direct-invocation copy):
         cp -r $MEFISTOX/nafems_le1 $MEFISTOX/nafems_le1_via_launcher
         cd $MEFISTOX/nafems_le1_via_launcher
       Invoke under Xvfb for X11 capture parity:
         env MEFISTO=$MEFISTO MEFISTOX=$MEFISTOX \
             MEFISTO_XVFERMER_READY_FILE=/tmp/_eq_launcher_ready \
             MEFISTO_XVFERMER_HOLD_MS=1500 \
             MEFISTO_XVSOURIS_AUTOEXIT=1 \
             MEFISTO_XVSOURIS_AUTOEXIT_DELAY_MS=500 \
             xvfb-run --auto-servernum -s "-screen 0 1280x800x24" \
             bash -c 'echo "nafems_le1" | $MEFISTO/bin/ELASTICER_OMP &
                      PID=$!; while [ ! -f /tmp/_eq_launcher_ready ]; do sleep 0.1; [ ! -d /proc/$PID ] && break; done;
                      DISPLAY=$DISPLAY import -window root /tmp/_eq_launcher.png; wait $PID' \
             2>&1 | tee /tmp/_eq_launcher.log
       NOTE: If the launcher's stdin protocol differs (e.g., expects multi-line input), inspect
       bin/ELASTICER_OMP first and adjust the invocation. The contract is "produces a captured
       PNG end-to-end via the launcher", not "this exact stdin pattern works".

    4. RUN B — direct binary with env-set:
         cd $MEFISTOX/nafems_le1
         env OMP_NUM_THREADS=8 \
             MEFISTO_XVFERMER_READY_FILE=/tmp/_eq_direct_ready \
             MEFISTO_XVFERMER_HOLD_MS=1500 \
             MEFISTO_XVSOURIS_AUTOEXIT=1 \
             MEFISTO_XVSOURIS_AUTOEXIT_DELAY_MS=500 \
             xvfb-run --auto-servernum -s "-screen 0 1280x800x24" \
             bash -c "$MEFISTO/pp/ppelas $BATCH &
                      PID=\$!; while [ ! -f /tmp/_eq_direct_ready ]; do sleep 0.1; [ ! -d /proc/\$PID ] && break; done;
                      DISPLAY=\$DISPLAY import -window root /tmp/_eq_direct.png; wait \$PID" \
             2>&1 | tee /tmp/_eq_direct.log

    5. AE-compare the two captures:
       cd $MEFISTO
       bin/ab_compare_pair.sh /tmp/_eq_launcher.png /tmp/_eq_direct.png /tmp/_eq_diff.png 5 \
         | tee /tmp/_eq_compare.log

    6. EXPECTED: ae=0 OR ae very close to OMP noise floor (single-digit pixels). The cells are
       running the SAME binary with the SAME environment — the only difference is the launcher
       wrapper sets the env var via export instead of `env`. Any AE > ~50 pixels indicates the
       launcher does something extra (workspace prep, INITIER call, alternate stdin handling)
       and the direct-invocation shortcut is NOT safe.

    7. Write evidence/elasticer-omp-equiv-check.md:
       - `# ELASTICER_OMP equivalence check (BLOCKER #4 mitigation, VALID-03 literal launcher reference)`
       - `## Setup`: workspace dirs + commands run
       - `## Run A (via bin/ELASTICER_OMP launcher)`: capture path, exit code, capture size
       - `## Run B (direct OMP_NUM_THREADS=8 pp/ppelas)`: same fields
       - `## AE compare`: full bin/ab_compare_pair.sh output line; verdict
       - `## Conclusion`:
         - If AE = 0 (or within OMP noise floor): "ELASTICER_OMP launcher equivalent to direct env-set invocation. Plan 5 harness uses direct invocation for all 4 OMP cases — justified by this empirical proof. Pattern extends by analogy to MAILLER_OMP, THERMICER_OMP, NLSER_OMP (same launcher shape per Read inspection)."
         - If AE > threshold: "ELASTICER_OMP differs from direct invocation. Plan 5 harness MUST use the launcher for VALID-03 compliance — STOP and update ab_sweep_phase8.sh qt-omp dispatch to invoke `bin/${LAUNCHER}_OMP` per case before Task 1 runs."

    8. If conclusion is "differs": this Task 0 BLOCKS Tasks 1+2 until the harness is updated.
       Document the required ab_sweep_phase8.sh edit in the conclusion section AND open a
       follow-up note in 08-bootstrap-log.md (Plan 1's evidence file).
  </action>
  <verify>
    <automated>cd $MEFISTO && test -s .planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md && grep -c -E '^## (Setup|Run A|Run B|AE compare|Conclusion)' .planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md | awk '$1>=5 {exit 0} {exit 1}' && grep -q -E '(equivalent to direct env-set|differs from direct invocation)' .planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md</automated>
  </verify>
  <done>elasticer-omp-equiv-check.md exists with 5 required sections; conclusion explicitly classifies launcher as either "equivalent" (proceeds to Tasks 1+2) or "differs" (BLOCKS until harness updated).</done>
</task>

<task type="auto">
  <name>Task 1: Capture 4 X11-OMP baselines (Open Question 2 recommendation — match OMP context for the LEFT operand of compare)</name>
  <files>
    .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-x11-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-x11-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-x11-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-x11-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md
  </files>
  <read_first>
    - .planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md (Task 0 conclusion — confirm "equivalent" before proceeding)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Pitfall 5" (lines 441-445 — OMP non-determinism risk)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"OMP Sweep — How _OMP Actually Works" (lines 728-742 — confirmed mapping)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Open Question 2" (lines 615-619 — recommendation: capture both X11-1x and X11-OMP)
    - bin/ELASTICER_OMP, bin/MAILLER_OMP, bin/THERMICER_OMP, bin/NLSER_OMP (verify each is just an OMP_NUM_THREADS=8 export + exec of the same pp/* binary)
    - bin/ab_capture_x11.sh (Plan 1 Task 3 deliverable)
    - bin/phase8_case_batch_map.sh (Plan 1 Task 0 — sourced for batch-file lookup)
  </read_first>
  <action>
    1. Pre-flight checks (same as prior Wave 2 plans):
       a. Plan 1 + Plan 2 complete (08-01-SUMMARY.md and 08-02-SUMMARY.md exist; 5 X11-1x baselines exist).
       b. pp/*_qt freshness vs libxvueqt.a holds for all 5.
       c. xvue/qt/src/ + xvue/xvuelc.c byte-identical: git diff --quiet on both.
       d. Confirm bin/FLUIDER_OMP does NOT exist:
            ls bin/FLUIDER_OMP 2>&1 | grep -q "No such file" || { echo "FLUIDER_OMP unexpectedly present — re-evaluate D-05 mapping"; exit 1; }
       e. Task 0 conclusion is "equivalent" (BLOCKER #4 mitigation passed):
            grep -q 'equivalent to direct env-set' .planning/phases/08-ab-validation-on-testa-subset/evidence/elasticer-omp-equiv-check.md \
              || { echo "BLOCKER #4 not yet resolved — Task 0 conclusion is not 'equivalent'"; exit 1; }

    2. Set up OMP-isolated workspaces (sourcing the case-batch map for correct batch lookup):
       export MEFISTO=$PWD
       export MEFISTOX=/tmp/mefistox-phase8-omp
       rm -rf $MEFISTOX && mkdir -p $MEFISTOX
       . "$MEFISTO/bin/phase8_case_batch_map.sh"
       For each case in {pan2d, nafems_le1, heat1d, nlsecu}:
         mkdir -p $MEFISTOX/$case && cp -r $MEFISTO/testa/$case/* $MEFISTOX/$case/

    3. Capture X11 baselines under OMP_NUM_THREADS=8 — invoke ab_capture_x11.sh per case (same harness as Plan 02 but with OMP env var). Per BLOCKER #4 conclusion in Task 0, direct env-set is sound:
       For each (CASE) in {pan2d, nafems_le1, heat1d, nlsecu}:
         MODULE=$(phase8_case_module $CASE)
         BATCH=$(phase8_case_batch $CASE)
         (cd $MEFISTOX/$CASE && \
           OMP_NUM_THREADS=8 $MEFISTO/bin/ab_capture_x11.sh \
               --case $CASE \
               --module $MODULE \
               --batch "$BATCH" \
               --out $MEFISTO/.planning/phases/08-ab-validation-on-testa-subset/evidence/${CASE}-x11-omp.png \
               --hold-ms 1500 \
               2>&1 | tee -a /tmp/phase8-x11-omp.log)

       Each invocation must produce a non-empty PNG at the --out path.

    4. Per-capture sanity checks. For each ${CASE}-x11-omp.png:
       a. test -s
       b. identify confirms valid PNG; dims should be 1280x800 (Xvfb screen)

    5. Optional but recommended (Pitfall 5 noise-floor characterization): repeat ONE case (pan2d) twice
       under identical OMP_NUM_THREADS=8 and compute the AE between the two runs.
       **Per WARNING-2 iter2: parallel-safety**: temp file paths use the keyed prefix
       `/tmp/_pan2d-x11-omp-noise-*` so they cannot collide with sibling Plan 02/03/04 temp files
       running concurrently in wave 2:
         (cd $MEFISTOX/pan2d && OMP_NUM_THREADS=8 $MEFISTO/bin/ab_capture_x11.sh --case pan2d --module mail \
            --batch "$(phase8_case_batch pan2d)" --out /tmp/_pan2d-x11-omp-noise-run2.png --hold-ms 1500)
         $MEFISTO/bin/ab_compare_pair.sh \
             $MEFISTO/.planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-x11-omp.png \
             /tmp/_pan2d-x11-omp-noise-run2.png \
             /tmp/_pan2d-x11-omp-noise-diff.png \
             5
       Record the AE pixel count as the "OMP noise floor" — Plan 7 uses this to set realistic AE expectations
       per case (Pitfall 5 mitigation).

    6. Append to sweep-log-omp.md:
       - `## Plan-5 OMP sweep — X11-OMP baseline column (YYYY-MM-DD HH:MM:SS UTC)` heading
       - Settings: OMP_NUM_THREADS=8; Xvfb 1280x800x24 via xvfb-run --auto-servernum (parallel-safe per WARNING-2 iter2); capture via bin/ab_capture_x11.sh; cavity2d marked N-A.
       - Reference to Task 0 BLOCKER #4 conclusion: "ELASTICER_OMP launcher equivalence proven; direct env-set invocation justified per evidence/elasticer-omp-equiv-check.md".
       - Per-case rows (4 rows): | Case | size | dims | sha256 | OK? |
       - cavity2d row marked: `| cavity2d | N/A | N/A | N/A | N-A (no bin/FLUIDER_OMP launcher per D-05) |`
       - Section `## OMP noise floor (Pitfall 5)` recording the pan2d twice-run AE.
  </action>
  <verify>
    <automated>cd $MEFISTO && for c in pan2d nafems_le1 heat1d nlsecu; do test -s .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-x11-omp.png || { echo "MISSING: ${c}-x11-omp.png"; exit 1; }; D=$(identify -format '%wx%h' .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-x11-omp.png 2>/dev/null); [ "$D" = "1280x800" ] || { echo "BAD DIMS: ${c} = $D"; exit 1; }; done; ! test -e .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11-omp.png && grep -v '^#' .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md | grep -c 'cavity2d.*N-A' | awk '$1>=1 {exit 0} {exit 1}'</automated>
  </verify>
  <done>4 X11-OMP captures (1280x800 each, non-empty), cavity2d marked N-A in the sweep log, OMP noise floor for pan2d recorded with parallel-safe temp paths, Task 0 conclusion referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Capture 4 Qt-OMP cells via ab_sweep_phase8.sh --baseline ${CASE}-x11-omp.png + main-thread guard verification (MAJOR-7 — no manual rerun; BLOCKER-B iter2 — literal-token substitution)</name>
  <files>
    .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-qt-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-qt-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-qt-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-qt-omp.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-qt-omp-diff.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-qt-omp-diff.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-qt-omp-diff.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-qt-omp-diff.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md
  </files>
  <read_first>
    - .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md (Task 1 output — confirm 4 X11-OMP baselines documented + cavity2d N-A)
    - bin/ab_sweep_phase8.sh (--mode qt-omp dispatch logic; SKIP for cavity2d; --baseline flag with literal-${CASE} substitution per BLOCKER-B iter2)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"OMP Sweep — How _OMP Actually Works" §last-bullet (debug build vs release build for Q_ASSERT)
  </read_first>
  <action>
    1. Pre-flight: Task 1 must have produced 4 X11-OMP baselines + cavity2d N-A. Verify:
       for c in pan2d nafems_le1 heat1d nlsecu; do
         test -s .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-x11-omp.png || { echo "Task 1 incomplete: ${c}-x11-omp.png missing"; exit 1; };
       done

    2. Run the Qt OMP sweep with --baseline pointed at the OMP-matched X11 baselines (MAJOR-7 — no manual rerun).
       **Per BLOCKER-B iter2**: the --baseline value MUST be SINGLE-QUOTED so the shell does NOT
       interpolate `${CASE}` at invocation time. The harness performs literal-string substitution
       internally per case:
       ```bash
       # Concrete bash example (matches Plan 1 Task 3 spec):
       # The single quotes around the path preserve `${CASE}` as a literal token.
       # The harness loop reassigns CURRENT_CASE per case and substitutes:
       #   RESOLVED_BASELINE="${BASELINE_PATH//\$\{CASE\}/$CURRENT_CASE}"
       cd $MEFISTO
       OMP_NUM_THREADS=8 bin/ab_sweep_phase8.sh \
           --mode qt-omp \
           --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu \
           --fuzz 5 \
           --baseline '.planning/phases/08-ab-validation-on-testa-subset/evidence/${CASE}-x11-omp.png' \
           --out-dir .planning/phases/08-ab-validation-on-testa-subset/evidence \
           --evidence-log .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md \
           2>&1 | tee /tmp/phase8-qt-omp-sweep.log
       ```

       Note the SINGLE-QUOTED `--baseline` value: the harness substitutes the LITERAL token
       `${CASE}` (5 characters: `$`, `{`, `CASE`, `}`) with the per-case loop variable at compare
       time. Sanity-check after invocation: confirm the harness resolved
       `${CASE}` → `pan2d` etc. by grepping the evidence log:
         grep -E 'baseline=.*pan2d-x11-omp\.png' .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md
       Should find at least 4 lines (one per OMP-eligible case). If this grep returns 0 lines,
       the harness substitution is broken and Task 2 BLOCKS — file a defect against
       bin/ab_sweep_phase8.sh's --baseline implementation.

       cavity2d is SKIPPED inside the harness — emitting a
       `case=cavity2d mode=qt-omp verdict=N-A` line per Plan 1 Task 3 specification.

       The harness now writes the final 4 verdict rows directly against the OMP-matched baseline.
       NO MANUAL RERUN NEEDED. NO sweep-log pollution from a default-baseline pass.

    3. Main-thread guard verification (XVUE_QT_ASSERT_MAIN_THREAD):
       For each of the 4 Qt-OMP runs, scan the captured stderr (/tmp/phase8-qt-omp-sweep.log) for any
       Q_ASSERT abort patterns:
         grep -E 'ASSERT.*main.*thread|Q_ASSERT|aborted' /tmp/phase8-qt-omp-sweep.log
       Expected: empty grep output (zero hits). If ANY hit, this is a real OMP defect — record it under
       `## Main-thread guard violations (CRITICAL)` in sweep-log-omp.md with the full grep output AND mark
       the affected case's verdict as FAIL (not CHECK). Plan 7 cannot ship CHECKLIST.md PASS for any cell with
       a recorded main-thread guard violation; it must escalate as a gap-closure plan or Phase 9 finding.

    4. Per-capture sanity. For each ${CASE}-qt-omp.png + ${CASE}-qt-omp-diff.png:
       test -s on each.

    5. Append to sweep-log-omp.md:
       - `## Plan-5 OMP sweep — Qt-OMP cells + main-thread guard (YYYY-MM-DD HH:MM:SS UTC)` header
       - Settings: --baseline ${CASE}-x11-omp.png (MAJOR-7 — no manual rerun; BLOCKER-B iter2 — literal-token substitution); fuzz=5%; cavity2d N-A.
       - Per-case row table:
         | Case | qt-omp size | dims | x11-omp baseline | resampled | AE | AE% | Verdict | diff path | main-thread guard |
       - cavity2d row: `| cavity2d | N/A | N/A | N/A | N/A | N/A | N/A | N-A | — | — (no FLUIDER_OMP) |`
       - Section `## Main-thread guard verification (XVUE_QT_ASSERT_MAIN_THREAD)`:
         - "grep for Q_ASSERT/main-thread aborts in /tmp/phase8-qt-omp-sweep.log: N hits"
         - For 0 hits: "PASS — Phase 1 SHELL-07 instrumentation held across all 4 OMP-eligible cases."
         - For >0 hits: list verbatim grep output + escalation note ("CRITICAL: open gap-closure plan").
       - Section `## OMP noise context (Pitfall 5)`:
         - Recall pan2d twice-run AE from Task 1.
         - For each case, the Verdict is PASS only if `(qt-omp vs x11-omp AE) <= 2 × (pan2d-x11-omp twice-run AE)` (rough Pitfall 5 heuristic). Cells that exceed this go to CHECK for maintainer review, NOT FAIL — OMP scheduling jitter is not a defect.
       - Section `## Hand-off to Plan 07`:
         - 4 absolute paths to ${CASE}-qt-omp.png
         - 4 absolute paths to ${CASE}-qt-omp-diff.png
         - 4 AE pixel counts + verdicts
         - 1 cavity2d N-A annotation
         - main-thread guard verdict (PASS or CRITICAL)
       - Section `## Outcome`:
         "Plan 05 OMP column complete: 4/4 OMP-eligible captures, 1 N-A (cavity2d), main-thread guard {PASS/FAIL}, M/4 PASS at default fuzz=5%, K/4 CHECK pending Pitfall-5 noise-floor review. ELASTICER_OMP equivalence proven (BLOCKER #4 mitigation, evidence/elasticer-omp-equiv-check.md). --baseline literal-token substitution verified (BLOCKER-B iter2). Hand-off ready for Plan 07."
  </action>
  <verify>
    <automated>cd $MEFISTO && for c in pan2d nafems_le1 heat1d nlsecu; do test -s .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-qt-omp.png || { echo "MISSING: ${c}-qt-omp.png"; exit 1; }; test -s .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-qt-omp-diff.png || { echo "MISSING: ${c}-qt-omp-diff.png"; exit 1; }; done; ! test -e .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-qt-omp.png && grep -v '^#' .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md | grep -c -E '^\| (pan2d|nafems_le1|heat1d|nlsecu) ' | awk '$1>=4 {exit 0} {exit 1}' && grep -c '^## Main-thread guard verification' .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-omp.md | grep -q '^1$' && git diff --quiet -- xvue/qt/src/ && git diff --quiet -- xvue/xvuelc.c</automated>
  </verify>
  <done>4 Qt-OMP captures + 4 diff PNGs exist; cavity2d-qt-omp.png does NOT exist (correctly N-A); sweep log has 4 verdict rows + main-thread guard section + OMP noise context + outcome; uses --baseline flag single-quoted with literal-token substitution per BLOCKER-B iter2 (no manual rerun); xvuelc.c + xvue/qt/src/ byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fortran solver worker thread ↔ Qt-side ABI entry | Off-thread graphics call is the canonical OMP risk; XVUE_QT_ASSERT_MAIN_THREAD is the runtime tripwire |
| OMP_NUM_THREADS env propagation through MAILLER_OMP/ELASTICER_OMP/etc. | The launcher exports the var then exec's pp/*; if the harness's invocation pattern differs from the launcher's, OMP scheduling differs (BLOCKER #4 mitigation: Task 0 proves equivalence) |
| Wave 2 parallel safety | Plan 5 runs concurrently with Plans 02/03/04; per-process env, per-case workspace, per-mode evidence outputs, xvfb-run --auto-servernum, and keyed temp file names ensure no contention (per WARNING-2 iter2) |
| ImageMagick compare ↔ stochastic OMP frame | A non-deterministic frame produces non-zero AE that's not a real defect |
| --baseline `${CASE}` token substitution | Caller MUST single-quote; harness MUST literal-substitute (BLOCKER-B iter2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | OMP scheduling non-determinism produces frame jitter | mitigate | (Pitfall 5) Capture against OMP-matched X11 baseline (not 1x); record OMP noise floor via twice-run on pan2d; CHECK threshold = 2 × noise floor (CHECK→manual review, not auto-FAIL). |
| T-08-19 | Spoofing | Q_ASSERT silently disabled in release build | mitigate | If pp/*_qt is built with -DNDEBUG (release), XVUE_QT_ASSERT_MAIN_THREAD is no-op. Plan 5 documents this explicitly in `## Main-thread guard verification` and recommends a debug-build re-run if any subsequent finding suggests off-thread access. The sweep itself can ship without forcing debug — Phase 1's all-57-stub static instrumentation is the primary guarantee. |
| T-08-20 | Denial of Service | OMP race condition deadlocks pp/*_qt | mitigate | 60s timeout in ab_capture_x11.sh and the ab_sweep_phase8.sh qt-* dispatcher (`timeout 60 pp/...`); Plan 5 hard-fails any case that times out rather than silent-passing. |
| T-08-33 | Spoofing | Direct env-set invocation deviates from ELASTICER_OMP launcher behavior (BLOCKER #4) | mitigate | Task 0 runs an empirical AE-compare between launcher and direct invocation; Tasks 1+2 only proceed if conclusion is "equivalent". |
| T-08-34 | Repudiation | Manual rerun + sweep-log pollution from default-baseline default-vs-OMP-baseline pattern (MAJOR-7) | mitigate | Plan 5 Task 2 uses `--baseline ${CASE}-x11-omp.png` flag; harness substitutes per case; single-pass log; no rerun. |
| T-08-36 | Tampering | --baseline `${CASE}` substitution implemented as no-op or eval (BLOCKER-B iter2) | mitigate | Plan 5 Task 2 passes single-quoted value with literal `${CASE}` token; harness uses `${BASELINE_PATH//\$\{CASE\}/$CURRENT_CASE}` (per Plan 1 Task 3 spec); evidence log grep verifies resolved per-case paths. |
| T-08-38 | Tampering | Wave-2 parallel collisions on Xvfb display, temp files, or workspace dirs (WARNING-2 iter2) | mitigate | Frontmatter PARALLEL SAFETY CONTRACT documents: xvfb-run --auto-servernum across all plans; case-and-mode keyed evidence outputs; keyed temp paths `/tmp/_pan2d-x11-omp-noise-*`; per-mode workspace dirs `/tmp/mefistox-phase8-omp`. No shared writers. |
| T-08-11 | Tampering | xvue/qt/src/ modified during Phase 8 | mitigate | Pre-task `git diff --quiet -- xvue/qt/src/`; verifier rechecks at phase close. |
</threat_model>

<verification>
- bin/ELASTICER_OMP equivalence proven (Task 0 — BLOCKER #4 mitigation).
- 4 X11-OMP baselines + 4 Qt-OMP captures + 4 diff PNGs exist (cavity2d does NOT have OMP captures — correctly N-A).
- sweep-log-omp.md contains: X11-OMP captures section, Qt-OMP cells section with 4 verdict rows + cavity2d N-A row, OMP noise floor measurement, main-thread guard verification, hand-off, outcome, BLOCKER #4 reference, BLOCKER-B iter2 reference, parallel-safety provenance per WARNING-2 iter2.
- ab_sweep_phase8.sh --baseline flag used with single-quoted literal-${CASE} value (BLOCKER-B iter2; MAJOR-7 mitigation).
- xvue/qt/src/ + xvue/xvuelc.c byte-identical.
- pp/*_qt freshness still holds.
</verification>

<success_criteria>
- 4/4 OMP-eligible cells captured at OMP_NUM_THREADS=8 on both backends (8 PNGs + 4 diffs total).
- ELASTICER_OMP launcher equivalence proven (BLOCKER #4).
- cavity2d explicitly N-A annotated in sweep log.
- Main-thread guard runtime verification PASS (no Q_ASSERT aborts) OR CRITICAL escalation documented.
- OMP noise floor measured for pan2d (Pitfall 5 mitigation, parallel-safe temp paths per WARNING-2 iter2).
- ab_sweep_phase8.sh --baseline flag used end-to-end with literal-token substitution (MAJOR-7 + BLOCKER-B iter2).
- xvue/qt/src/ + xvue/xvuelc.c invariants preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ab-validation-on-testa-subset/08-05-SUMMARY.md` summarizing:
- ELASTICER_OMP equivalence verdict (BLOCKER #4 mitigation; one line: "equivalent" or "differs + harness updated")
- 4 X11-OMP captures (case + size + sha256, 4 rows)
- 4 Qt-OMP captures + AE pixel count + verdict (4 rows; OMP-matched baseline via --baseline flag with literal-${CASE} substitution per BLOCKER-B iter2)
- cavity2d N-A annotation (one line)
- Main-thread guard verdict (PASS / CRITICAL escalation)
- OMP noise floor (pan2d twice-run AE; parallel-safe temp paths per WARNING-2 iter2)
- xvue/qt/src/ + xvue/xvuelc.c invariant lines
- Hand-off note: "Qt OMP column complete (4 cells + 1 N-A). Plan 07 will compose CHECKLIST.md row 'Qt _OMP' with the cavity2d N-A annotation per D-05; ELASTICER_OMP equivalence is recorded as a footnote per VALID-03 literal-launcher-name compliance."
</output>
</output>
