---
phase: 08-ab-validation-on-testa-subset
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-x11.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-x11.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-x11.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-x11.png
  - .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md
autonomous: true
requirements:
  - VALID-02
must_haves:
  truths:
    - "All 5 canonical testa cases produce a non-empty X11 capture PNG via bin/ab_capture_x11.sh under Xvfb 1280x800x24"
    - "Each capture's MEFISTO_XVFERMER_READY_FILE was created by xvuelc.c xvfermer_ before the import call (sentinel discipline)"
    - "Each capture has dimensions 1280x800 (matching the Xvfb screen — no DPR involvement on X11 backend)"
    - "Each capture's case ran to natural process termination under MEFISTO_XVSOURIS_AUTOEXIT=1 within 60s"
    - "OMP_NUM_THREADS=1 was set for every X11 capture (deterministic baseline column per Open Question 2 recommendation)"
    - "xvue/xvuelc.c is byte-identical to pre-Phase-8 state (xvuelc.c invariant per CONTEXT.md)"
  artifacts:
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-x11.png"
      provides: "X11 baseline capture for pan2d (mesher)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-x11.png"
      provides: "X11 baseline capture for nafems_le1 (elasticity)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11.png"
      provides: "X11 baseline capture for cavity2d (fluid)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-x11.png"
      provides: "X11 baseline capture for heat1d (thermal)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-x11.png"
      provides: "X11 baseline capture for nlsecu (nonlinear)"
    - path: ".planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md"
      provides: "Per-case capture log: pp/* exit code, ready-file timing, capture size, dimensions"
      min_lines: 30
  key_links:
    - from: "bin/ab_sweep_phase8.sh"
      to: "bin/ab_capture_x11.sh"
      via: "MODE=x11 dispatch invokes capture wrapper"
      pattern: "ab_capture_x11\\.sh"
    - from: "bin/ab_capture_x11.sh"
      to: "pp/pp{mail,elas,flui,ther,nlse}"
      via: "background launch + READY_FILE poll + import -window root"
      pattern: "MEFISTO_XVFERMER_READY_FILE"
---

<objective>
Capture the X11 baseline column for the 5 canonical testa cases. This plan
PRODUCES the reference column (column 1 of the CHECKLIST.md verdict matrix
per D-10) that all subsequent Qt mode plans (03/04/05) will compare against.
No comparison happens here — only capture.

Purpose: VALID-02 demands "the X11 backend continues to pass the same 5 cases
at the end of every phase." Phase 8 establishes the BASELINE column on X11
fresh, then every Qt mode column derives its verdict from comparison against
this exact set of 5 PNGs. Capturing X11 in a dedicated plan also isolates
the X11-side risk surface (xvuelc.c invariant; pre-existing well-validated
code) from the Qt-side risk surface (newer code under active validation).

Output: 5 PNG captures + 1 sweep log under .planning/phases/08-*/evidence/.
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/validation/BASELINE.md
@.planning/phases/08-ab-validation-on-testa-subset/08-CONTEXT.md
@.planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md
@.planning/phases/08-ab-validation-on-testa-subset/08-01-SUMMARY.md
@bin/ab_sweep_phase8.sh
@bin/ab_capture_x11.sh
@bin/phase8_case_batch_map.sh
@CLAUDE.md

<interfaces>
<!-- The X11 capture contract (already implemented in xvuelc.c, verified by Phase 8 Plan 1 Task 3 smoke probes). -->
<!-- DO NOT MODIFY xvuelc.c — it is byte-identical until Phase 9 (RETIRE-01). -->

xvue/xvuelc.c xvfermer_ contract (verified):
  - Reads getenv("MEFISTO_XVFERMER_READY_FILE"); if non-empty, fopen+fprintf("ready\n")+fclose AFTER XFlush.
  - Reads getenv("MEFISTO_XVFERMER_HOLD_MS") (clamped to [0, 60000]); usleep(N*1000) AFTER touching ready file.
  - Final destruction of X11 display happens after the hold.

xvue/xvuelc.c xvsouris_ contract (verified):
  - Reads getenv("MEFISTO_XVSOURIS_AUTOEXIT") = "1" → returns synthetic notypeevent=2 SPACE keypress.
  - Reads getenv("MEFISTO_XVSOURIS_AUTOEXIT_DELAY_MS") = N → delay before synthetic event.

testa case → module → batch resolution (per WARNING-1 iter2 review):

  See `bin/phase8_case_batch_map.sh` for canonical per-case batch resolution.
  Plan 2 invokes the harness which sources this map. The previously-documented
  per-case glob mapping (`pan2d → mail → testa/pan2d/*.mesh`-style) was
  refuted by iter1 review (BLOCKER #1) — cavity2d / nlsecu / hexahedron do
  NOT match a uniform `*.{mesh,elas,flui,heat,nlse}` glob. The harness sources
  bin/phase8_case_batch_map.sh (Plan 1 Task 0 deliverable) and looks up
  MODULE + BATCH per case via `phase8_case_module $CASE` /
  `phase8_case_batch $CASE`. Plan 2 does NOT repeat the glob assumption — it
  delegates entirely to the harness.

  Authoritative source for per-case (MODULE, BATCH) pairs:
    bin/phase8_case_batch_map.sh
  Authoritative evidence record of the empirical resolution:
    .planning/phases/08-ab-validation-on-testa-subset/evidence/00-case-batch-map.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run bin/ab_sweep_phase8.sh --mode x11 over the 5 canonical cases under Xvfb</name>
  <files>
    .planning/phases/08-ab-validation-on-testa-subset/evidence/pan2d-x11.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nafems_le1-x11.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/cavity2d-x11.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/heat1d-x11.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/nlsecu-x11.png
    .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md
  </files>
  <read_first>
    - .planning/phases/08-ab-validation-on-testa-subset/08-01-SUMMARY.md (entire file — confirm Plan 1 ship status, particularly §"Smoke probes" for any case marked BLOCKED)
    - bin/ab_sweep_phase8.sh (entire file — understand --mode x11 dispatch, OUT_DIR default, evidence-log default)
    - bin/ab_capture_x11.sh (entire file — confirm Xvfb setup + READY_FILE polling + import call)
    - bin/phase8_case_batch_map.sh (entire file — the canonical per-case (MODULE, BATCH) source)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Pattern 1 / canonical X11-side" (lines 278-294) — confirm the harness matches the verified pattern
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"Open Question 2" (lines 615-619) — recommendation to set OMP_NUM_THREADS=1 for X11 baseline (deterministic)
    - .planning/validation/BASELINE.md (lines 23-92 — exact 5 cases + their launcher scripts)
  </read_first>
  <action>
    1. Pre-flight check: verify Plan 1 Task 3 smoke probes all show STATUS=OK in
       .planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md.
       grep -v '^#' .planning/phases/08-ab-validation-on-testa-subset/evidence/00-smoke-probes.md | grep -c 'STATUS=OK' MUST equal 5. If not, STOP and escalate.

    2. Pre-flight: verify the X11 binaries (pp/ppmail, pp/ppelas, pp/ppflui, pp/ppther, pp/ppnlse) exist
       and were not modified by Plan 1. Run `ls -la pp/pp{mail,elas,flui,ther,nlse} 2>&1` and confirm
       no errors. xvuelc.c byte-identical check:
         git diff -- xvue/xvuelc.c
       Output MUST be empty.

    3. Set up project workspace:
       export MEFISTO=$PWD
       export MEFISTOX=/tmp/mefistox-phase8-x11
       rm -rf $MEFISTOX && mkdir -p $MEFISTOX
       For each case in {pan2d, nafems_le1, cavity2d, heat1d, nlsecu}:
         mkdir -p $MEFISTOX/$case
         cp -r $MEFISTO/testa/$case/* $MEFISTOX/$case/
       This isolates the sweep so production testa/ trees stay clean.

    4. Run the X11 sweep (deterministic baseline, OMP_NUM_THREADS=1 per Open Question 2):
       cd $MEFISTO
       OMP_NUM_THREADS=1 bin/ab_sweep_phase8.sh \
           --mode x11 \
           --cases pan2d,nafems_le1,cavity2d,heat1d,nlsecu \
           --out-dir .planning/phases/08-ab-validation-on-testa-subset/evidence \
           --evidence-log .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md \
           2>&1 | tee /tmp/phase8-x11-sweep.log

       The harness must complete with exit 0. If exit != 0, capture the failing case from the
       evidence-log, do NOT retry blindly — STOP and document the failure mode in
       sweep-log-x11.md under a `## Failures` section. Do NOT modify pp/* or xvuelc.c
       to make the case work (xvuelc.c invariant; phase boundary discipline).

       The harness internally sources bin/phase8_case_batch_map.sh for per-case (MODULE, BATCH)
       resolution — Plan 2 does NOT re-implement the lookup or glob.

    5. Per-capture sanity checks. For each ${CASE}-x11.png:
       a. test -s evidence/${CASE}-x11.png  (non-empty)
       b. identify evidence/${CASE}-x11.png shows dimensions 1280x800 (matches Xvfb screen)
       c. identify evidence/${CASE}-x11.png is a valid PNG (no parse error)
       Append a check matrix to sweep-log-x11.md under `## Per-case verification`:
       | Case | size_bytes | dims | sha256 | OK? |

    6. Append the sweep log entries:
       - `## Plan-2 X11 baseline sweep (YYYY-MM-DD HH:MM:SS UTC)` heading at top
       - One row per case: case=NAME exit=N capture_size=N dims=WxH ready_file_latency_ms=N (extract from /tmp/phase8-x11-sweep.log; ab_capture_x11.sh emits these lines)
       - Section `## OMP context` recording: "OMP_NUM_THREADS=1 (deterministic baseline per Open Question 2)"
       - Section `## xvuelc.c invariant` recording: "git diff -- xvue/xvuelc.c → empty (byte-identical confirmed at task entry)"
       - Section `## Case-batch resolution` recording: "Per-case MODULE + BATCH sourced from bin/phase8_case_batch_map.sh (Plan 1 Task 0 deliverable per WARNING-1 iter2). No glob assumption used."

    7. Commit-readiness gate: 5 PNG files + 1 evidence log all exist, all PNGs are 1280x800,
       and the evidence log contains at least 5 status lines. Plan 7 (CHECKLIST.md finalize)
       depends on this column being present.
  </action>
  <verify>
    <automated>cd $MEFISTO && for c in pan2d nafems_le1 cavity2d heat1d nlsecu; do test -s .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-x11.png || { echo "MISSING: ${c}-x11.png"; exit 1; }; D=$(identify -format '%wx%h' .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-x11.png 2>/dev/null); [ "$D" = "1280x800" ] || { echo "BAD DIMS: ${c} = $D"; exit 1; }; done; grep -v '^#' .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md | grep -c -E '^\| (pan2d|nafems_le1|cavity2d|heat1d|nlsecu) ' | grep -q '^5$' && git diff --quiet -- xvue/xvuelc.c</automated>
  </verify>
  <done>5 X11 capture PNGs (each 1280x800, non-empty), sweep-log-x11.md contains 5 per-case rows + OMP context line + invariant confirmation + case-batch resolution provenance, xvue/xvuelc.c byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: SHA-256 manifest + per-case timing summary for the X11 baseline</name>
  <files>
    .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md
  </files>
  <read_first>
    - .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md (the file produced by Task 1 — to be appended)
    - .planning/phases/08-ab-validation-on-testa-subset/08-RESEARCH.md §"CHECKLIST.md row schema" (lines 569-580 — the format Plan 7 will consume; this task pre-stages the data)
  </read_first>
  <action>
    1. For each of the 5 captures, compute and record:
       - SHA-256: `sha256sum evidence/${CASE}-x11.png | awk '{print $1}'`
       - File size: `stat -c '%s' evidence/${CASE}-x11.png`
       - Dimensions: `identify -format '%wx%h' evidence/${CASE}-x11.png`
       - PNG bit-depth + color type: `identify -format '%[bit-depth]-bit %[colorspace]' evidence/${CASE}-x11.png`

    2. Append a section `## Manifest` to sweep-log-x11.md as a markdown table:
       | Case | Size (bytes) | Dimensions | Bit depth | SHA-256 |
       |------|--------------|------------|-----------|---------|
       | pan2d | ... | ... | ... | ... |
       (5 rows)

    3. Append a section `## Hand-off to Plans 03/04/05` listing the 5 absolute paths
       (relative to repo root) of the X11 captures. These are the LEFT operand for
       every Qt-mode compare in subsequent plans.

    4. Append a final section `## Outcome` with one line:
       "Plan 02 X11 baseline column complete: 5/5 cases captured at 1280x800 under Xvfb + OMP_NUM_THREADS=1 deterministic baseline. Hand-off ready for Plans 03/04/05."

    5. Validate the manifest is parseable: every SHA-256 must be 64 hex chars; every
       Size must be > 0; every dimension must equal "1280x800".
  </action>
  <verify>
    <automated>cd $MEFISTO && grep -A 10 '^## Manifest' .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md | grep -v '^#' | grep -E '^\| [a-z0-9_]+ \| [0-9]+ \| 1280x800 \| ' | wc -l | grep -q '^5$' && grep -c '^## Hand-off' .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md | grep -q '^1$' && for c in pan2d nafems_le1 cavity2d heat1d nlsecu; do EXPECTED=$(sha256sum .planning/phases/08-ab-validation-on-testa-subset/evidence/${c}-x11.png | awk '{print $1}'); grep -q "$EXPECTED" .planning/phases/08-ab-validation-on-testa-subset/evidence/sweep-log-x11.md || { echo "Missing SHA for $c"; exit 1; }; done</automated>
  </verify>
  <done>Manifest table contains 5 rows with valid SHA-256 + 1280x800 dims; Hand-off section lists 5 paths; Outcome section asserts column completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Xvfb display ↔ pp/* | The capture sentinel + import call cross display-server boundary; Xvfb crashes or display lock would corrupt captures |
| testa/$case → $MEFISTOX/$case | Per-case workspace isolation; reusing the same MEFISTOX dir between runs could leak state across cases |
| OMP_NUM_THREADS env propagation | Setting OMP_NUM_THREADS=1 at the harness level must propagate into the pp/* invocation; sub-shell escapes could lose it |
| Per-case (MODULE, BATCH) resolution ↔ harness | The harness sources bin/phase8_case_batch_map.sh; Plan 2 must NOT bypass it with a glob (WARNING-1 iter2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering | xvue/xvuelc.c byte-identical invariant | mitigate | Pre-task `git diff -- xvue/xvuelc.c` empty check; verifier rechecks at phase close. |
| T-08-14 | Repudiation | OMP scheduling non-determinism contaminates baseline | mitigate | OMP_NUM_THREADS=1 explicitly set at harness invocation; documented in evidence log §"OMP context". (Pitfall 5 mitigation; matches Open Question 2 recommendation.) |
| T-08-15 | Information Disclosure | Capture PNG includes window-decoration chrome | accept | Xvfb root capture under Xvfb 1280x800x24 has no WM by default — no decorations. Verified by Phase 7 UAT precedent. |
| T-08-16 | Denial of Service | A testa case hangs past 60s timeout | mitigate | bin/ab_capture_x11.sh implements 60s overall timeout (Plan 1 Task 3); Plan 2 hard-fails any case that times out rather than silent-passing. |
| T-08-37 | Spoofing | Plan 2 reverts to glob assumption rather than sourcing the case-batch map (WARNING-1 iter2) | mitigate | Plan 2 interfaces block REPLACED the per-case glob mapping table with a pointer to bin/phase8_case_batch_map.sh; harness invocation delegates the lookup; sweep-log records "Per-case MODULE + BATCH sourced from bin/phase8_case_batch_map.sh" provenance. |
</threat_model>

<verification>
- 5 X11 capture PNGs exist under evidence/, each non-empty, each 1280x800, each PNG-valid.
- sweep-log-x11.md contains: per-case status rows, manifest table with SHA-256, hand-off section, outcome line, case-batch resolution provenance line (per WARNING-1 iter2).
- xvue/xvuelc.c is byte-identical: `git diff --quiet -- xvue/xvuelc.c` returns 0.
- pp/pp{mail,elas,flui,ther,nlse} were NOT relinked during this plan (no Qt artifacts touched).
</verification>

<success_criteria>
- 5/5 X11 baseline captures committed.
- Sweep log readable, manifest parseable, hand-off clean for Plans 3/4/5.
- xvuelc.c invariant preserved.
- OMP_NUM_THREADS=1 deterministic baseline documented.
- Case-batch resolution provenance recorded (WARNING-1 iter2).
</success_criteria>

<output>
After completion, create `.planning/phases/08-ab-validation-on-testa-subset/08-02-SUMMARY.md` summarizing:
- 5 X11 captures (case + size + sha256, 5 rows)
- Sweep log path
- xvuelc.c invariant confirmation line
- Case-batch resolution: "Sourced from bin/phase8_case_batch_map.sh per WARNING-1 iter2 (no glob)"
- Hand-off note: "X11 baseline column complete. Plans 03 (Qt 1x), 04 (Qt 2x), 05 (OMP) consume these as the LEFT operand for every compare invocation."
</output>
</output>
