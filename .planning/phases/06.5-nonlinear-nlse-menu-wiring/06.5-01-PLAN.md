---
phase: 06.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md
autonomous: true
requirements:
  - UX-05
threat_refs:
  - T-6.5-AUDIT-01
  - T-6.5-AUDIT-02

must_haves:
  truths:
    - "LEXICON-AUDIT-nlse.md exists under .planning/phases/06.5-nonlinear-nlse-menu-wiring/ with the exact 9-column schema frozen in 6.1 and re-applied in 6.2/6.3/6.4 (lexicon_path / description_fr / description_en / frequency / qaction / toolbar / icon_source / shortcut / notes)"
    - "Audit enumerates every LIMTCL leaf reachable from `td/m/debunlse` (20 top-level codes: 1, 2, 5, 6, 11, 12, 13, 14, 15, 16, 19, 20, 38, 39, 71, 72, 73, 74, 97, 99) via a full recursive tree walk over ther/ + util/ + reso/ + prpr/ppnlse.f call sites — expected row count 80-250, validated by tools/validate_audit_md.py"
    - "Every row with frequency in {high, med} has qaction=yes; every row with frequency=low has qaction=no (Rule 8)"
    - "Exactly 5 rows have toolbar=yes — top-5 most-used nlse leaves derived from domain review of canonical NLSE workflow (no .nlse fixtures exist; gpesigc/nlsecu/nlsesqri test-project assets are MESH inputs, NOT lexicon batch files — frequency evidence comes from domain review of debunlse + ppnlse.f dispatch table)"
    - "tools/validate_audit_md.py exits 0 against the final nlse audit (validator already module-aware as of 6.2 Plan 02 — no validator changes needed)"
    - "Help-allowlist for testHelpNoQueue is documented at the bottom of the audit as `{97;}` (NOT `{98;}` like ther — debunlse line 20 codes 97 = `DATE de version de Mefisto`; this matches flui's value, NOT ther's)"
    - "Top-level menu taxonomy `{File, Nonlinear, View, Help}` ratified per ROADMAP Phase 6.5 line 220 (`Nonlinear` replaces `Thermal`/`Fluid`/`Solve`/`Mesh` from prior slices; FR translation `Non Linéaire` confirmed against td/ma/debunlse line 1 `'Non Linear SCHRODINGER Equation NLSE'`)"
  artifacts:
    - path: ".planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md"
      provides: "Per-module nlse lexicon audit (UX-05 nlse-slice Success Criterion #1 deliverable)"
      contains: "9-column GitHub-markdown table identical schema to LEXICON-AUDIT-ther.md, rooted at debunlse (not debut, not debuelas, not debuflui, not debuther)"
  key_links:
    - from: ".planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md"
      to: "tools/validate_audit_md.py (module-aware ICONS resolver from 6.2 Plan 02)"
      via: "schema rules enforced at lint time — 9 hard rules apply unchanged"
      pattern: "python3 tools/validate_audit_md.py .planning/phases/06.5-*/LEXICON-AUDIT-nlse.md"
    - from: "LEXICON-AUDIT-nlse.md rows with qaction=yes"
      to: "Plan 02 registerNlseActions_stub_ QAction set"
      via: "frequency-bucketed rows are the QAction wire-up list"
      pattern: "qaction=yes AND frequency in (high, med)"
    - from: "LEXICON-AUDIT-nlse.md rows with toolbar=yes"
      to: "Plan 02 toolbar append set"
      via: "top-5 leaves from the frequency-scored audit"
      pattern: "toolbar=yes (count == 5)"
    - from: "LEXICON-AUDIT-nlse.md Help-allowlist note"
      to: "Plan 03 testHelpNoQueue allowlist QSet<QString>"
      via: "audit Summary Statistics block enumerates which Help-menu lexicons are intentionally wired (per 6.3 Plan 03 Auto-fix Rule 1)"
      pattern: "Help-allowlist:.*\\{97;\\}"
---

<objective>
Produce `.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` —
the full recursive catalogue of every interactive command reachable from the
NLSE solver's `td/m/debunlse` LIMTCL tree (20 top-level codes), scored with
high/med/low frequency buckets from **domain review of the canonical NLSE
workflow** (no batch fixtures exist for nlse — testa/nlsecu/, testa/nlsecuvv/,
testa/nlsesqri/, testa/nlsgpe/, testa/gpesigc/ contain only mesh + initial-guess
data files, NOT `.nlse` lexicon scripts), then frozen for Plan 02 QAction wiring.

Reuse `tools/validate_audit_md.py` verbatim — the validator was generalized in
6.2 Plan 02 and re-validated against 6.3 flui + 6.4 ther without changes. No
tooling changes needed in this plan.

Purpose: UX-05 nlse-slice Success Criterion #1. Every decision downstream
(which QActions exist, which icons are custom-SVG vs Qt built-in, which 5
leaves go on the toolbar, which Help lexicons land in Plan 03's allowlist)
flows from this audit. The validator closes the loop — it is the only
automated verification available for a markdown deliverable.

Output: One markdown file in the phase directory, an internally-derived
top-5 toolbar set (auto-frozen — no user checkpoint, mirroring the
fully-autonomous 6.3/6.4 path), the `{File, Nonlinear, View, Help}` menu
taxonomy ratified against the audit data (differs from 6.1 mail's `{File,
Mesh, View, Help}`, 6.2 elas's `{File, Solve, View, Help}`, 6.3 flui's
`{File, Fluid, View, Help}`, and 6.4 ther's `{File, Thermal, View, Help}`;
per ROADMAP Phase 6.5 line 220 the per-module slot is `Nonlinear` for the
NLSE solver), and an explicit Help-allowlist note for Plan 03's
testHelpNoQueue slot (note: nlse's Help code is **`97;`** — same as flui's
debuflui line 21, NOT ther's `98;`; verified against `td/m/debunlse` line
20 and `td/ma/debunlse` line 20 which both list `97 :
'DATE de version de Mefisto'`).
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md
@.planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md
@.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md
@.planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md
@.planning/phases/06.4-thermal-ther-menu-wiring/LEXICON-AUDIT-ther.md
@.planning/phases/06.4-thermal-ther-menu-wiring/06.4-01-SUMMARY.md
@.planning/phases/06.4-thermal-ther-menu-wiring/06.4-VERIFICATION.md
@CLAUDE.md
@td/m/debunlse
@td/ma/debunlse
@prpr/ppnlse.f

<interfaces>
<!-- Key LIMTCL data for the auditor — nlse-specific (extracted during planning 2026-04-29) -->

### nlse top-level menu file (VERIFIED paths and sizes)
- FR (runtime active): `$MEFISTO/td/m/debunlse` (1.1K, 20 codes)
- EN (source for EN install): `$MEFISTO/td/ma/debunlse` (948B, 20 codes)
- FR (variant): `$MEFISTO/td/mf/debunlse` (1.1K, 20 codes — present, mirrors td/m/)
- Parser: `util/limtcl.f:54` (`KNOM = HOMDIR // '/td/m/' // KNM`) — same path as mail/elas/flui/ther
- nlse SHIPS all three variants (m/ + ma/ + mf/) — 6.4 ther shipped only m/ + ma/

### debunlse top-level (20 codes — VERIFIED verbatim against td/m/debunlse + td/ma/debunlse 2026-04-29)

| Code | FR label                                                              | EN label                                              |
|------|-----------------------------------------------------------------------|-------------------------------------------------------|
| 1    | NOM de l'OBJET a TRAITER                                              | OBJECT NAME to treat                                  |
| 2    | ENTREE des DONNEES de l'Equation Non Lineaire de SCHRODINGER          | Non Linear SCHRODINGER Equation INPUT DATA            |
| 5    | IMPLICIT idU(t)/dt -Alfa Laplacien U(t) +N(t,\|U\|)U(t)=F(t,X)         | IMPLICIT i dU(t)/dt -Alfa Laplacian U(t) +N(\|U\|**2)U(t)=F(t,X) |
| 6    | i-TIME methode idU(t)/dt -Alfa Laplacian U(t) +N(\|U\|**2)U(t)=F(t,X)  | i-TIME method idU(t)/dt -Alfa Laplacian U(t) +N(\|U\|**2)U(t)=F(t,X) |
| 11   | DESSIN du MODULE               de l'ONDE NLSE                         | Drawing of \|U(t,X)\| NLSE WAVE MODULE                 |
| 12   | DESSIN de la PARTIE REELLE     de l'ONDE NLSE                         | Drawing of U(t,X) NLSE WAVE REAL      PART            |
| 13   | DESSIN de la PARTIE IMAGINAIRE de l'ONDE NLSE                         | Drawing of U(t,X) NLSE WAVE IMAGINARY PART            |
| 14   | DESSIN des VALEURS du TEST d'ARRET des ITERATIONS                     | Drawing of STOP TEST Values of ITERATIONS             |
| 15   | DESSIN des Max\|U(Noeud)\|(Temps)                                      | Drawing of Max\|U(Node)\|(Time)                        |
| 16   | DESSIN des ERREUR(Temps) = ER(Temps) + i EI(Temps)                    | Drawing of REAL U-ERROR(Time), IMAGINARY U-ERROR(Time)|
| 19   | DESSIN du MAILLAGE des PLSVO                                          | Drawing of PLSVO meshes                               |
| 20   | PRECISION pour INVERSER les systemes lineaires A x = b                | PRECISION to INVERSE the linear systems A x = b       |
| 38   | LARGEUR HAUTEUR PIXELS de la FENETRE de VISUALISATION                 | WINDOW PIXEL WIDTH & HEIGHT numbers                   |
| 39   | COULEUR du FOND de la FENETRE de VISUALISATION                        | BACKGROUND COLOR of the window                        |
| 71   | SUIVI des TMS AFFICHAGE MODIFICATION                                  | TOOLS on TMS                                          |
| 72   | SUIVI des FICHIERS de la MS                                           | TOOLS on FILES of the MS                              |
| 73   | GESTION des UNITES lecture affichage                                  | TOOLS on I/O units                                    |
| 74   | TUER des TMS de PLSVO                                                 | DELETE TMS of PLSVO                                   |
| 97   | DATE de version de Mefisto                                            | MEFISTO VERSION DATE                                  |
| 99   | SAUVEGARDE des donnees FIN TRAITEMENT                                 | SAVE DATA and QUIT                                    |

NOTE 1 (Help code per-module variation): debunlse line 20 reads `97 : 'DATE
de version de Mefisto'`. The Help-allowlist for Plan 03's `testHelpNoQueue`
MUST be **`{97;}`** — same as 6.3 flui (debuflui line 21 also uses `97`),
**NOT `{98;}`** like 6.4 ther. This is the explicit per-module variation
flagged by 6.3 Plan 03's Auto-fix Rule 1 (per-module Help-allowlist drawn
from LEXICON-AUDIT, not inherited blindly from previous-module template).

NOTE 2 (debunlse layout differs from debuther on TMS codes): nlse uses
**four separate top-level codes** `71;`, `72;`, `73;`, `74;` for TMS sub-management
(SUIVI TMS / FICHIERS / UNITES / TUER) where ther uses a single combined
`70;` code that opens managmef. The dispatch in ppnlse.f:467-470 unwraps
the 71-74 range with `NMTCL = NMTCL - 70` then GOTO array slots 1-4 →
SUITMS / SUIFMS / SUIVES / TUER. Audit must show four separate top-level
rows for `71;`/`72;`/`73;`/`74;` (matching 6.1 mail's pattern, since mail
also exposes these as separate top-level codes; UNLIKE 6.2 elas / 6.3 flui /
6.4 ther which use the `70;` combined entry point).

NOTE 3 (debunlse missing codes 0, 3, 4, 7, 8, 9, 10): nlse skips these
codes — `prpr/ppnlse.f:220` GOTO array contains `100, 200, 300, 400, 500,
600, 700, 30, 30, 1000, 1100, 1200, 1300, 1400, 1500, 1600, 30, 30, 1900,
2000` covering codes 1-20 with `30` (loop-back-to-menu) at slots 8, 9, 17,
18 — codes 8;, 9;, 17;, 18; intentionally fall through. Code `4;` slot
dispatches to `400` which fires `NLSEINS` (different from ther's THEINS
target). Audit lists ONLY codes that exist in debunlse — does NOT
fabricate rows for 0/3/4/7/8/9/10.

NOTE 4 (codes 5, 6 → schemas): code 5 dispatches `TESTNL=6, CALL NLSEINS`
(line 330-332); code 6 dispatches `TESTNL=9, CALL NLSEINS` per the
i-TIME variant at line 364-368. Both prompt cl_nlse and coefnlse sub-menus
(see ther/dfnlse.f and ther/coefnlse-handling routines).

NOTE 5 (deleted codes 7-10 + 17-18): ppnlse.f preserves a `ccc`-commented
block at lines 345-354 (deleted `7;` Schema3 SANS MATRICE GLOBALE) and at
lines 361-368 (the live `6;` was originally code `1400` per the comment).
Visualization codes 11-16 dispatch to TRTHER (codes 11/12/13 with NTYPDESS=4,
5, 6 respectively) plus TRNLSETST (14;) / TRNLSEMXU (15;) / TRNLSERR (16;).
This means codes 11; 12; 13; reach the SAME tractem/tracflux/tracgrad
visualization cascade as ther (different NTYPDESS values control the
field — module / real-part / imag-part).

NOTE 6 (mesh-draw shared): code `19;` (DRAWING of PLSVO meshes) dispatches
to `CALL TRMAIL` per ppnlse.f:433. Audit treats `19;` as a shared util
compressed row citing 6.1 LEXICON-AUDIT-mail.md, with icon reuse
`mesh-draw.svg` from `xvue/qt/resources/icons/mail/`.

### nlse-reachable sub-menus (52 total — VERIFIED via grep 2026-04-29)

```
$ grep -rEoh "LIMTCL\( *'[^']+'" ther/ util/ prpr/ppnlse.f reso/ 2>/dev/null \
  | sed "s/LIMTCL( *'//;s/'$//" | sort -u | wc -l
52
```

Sub-menu set (full list extracted at planning time — IDENTICAL to ther's
53 minus debuther itself; the extra one in ther's count was debuther which
is replaced by debunlse for nlse):

```
affiche, cl_nlse, cl_ther, couleur0, couleurs, debunlse, defplan,
dirprofi, eigvvloc, eigvvpol, entrsort, fichier, lecteur, managfen,
managmef, methreso, methvvpr, mode_es, opt_lign, opt_surf, profdroi,
profplan, proj6cub, resoonin, resothin, resothst, scheonde, sectplan,
sectvopl, solsurf3, suivi_ms, suivitms, tempgrad, tracaxes, tracerr2,
tracerr3, tracerrt, tracflux, tracgrad, traconde, tractem1, tractem2,
tractem3, tractemp, trdepond, tuer, typ_objt, typtrait, valisoth,
valzone, valztxy, vuesplan, zeros
```

KEY INSIGHT: The nlse-reachable LIMTCL set is essentially **identical to ther's
53-set**, because ppnlse.f shares the ther/* + util/* + reso/* solver tree
for visualization (TRTHER), eigenvector handling (THEPOLEVV — disabled but
still grep-reachable), and shared utilities (suitms/suifms/etc.). Critical
de-duplication consequence:

**Sub-menus PRIMARY to nlse** (cross-module shared with ther; treat as
PRIMARY here since nlse is the canonical home of NLSE-specific physics):
- `cl_nlse` (NLSE BOUNDARY CONDITIONS — 5 leaves) — was a ther/eigvvpol/
  cross-module ROW in 6.4 (compressed); nlse audit FULLY EXPANDS this
- `coefnlse` (NLSE COEFFICIENT data — `ther/coefnlse.f` reachable from
  ppnlse.f:200 via `CALL DFNLSE` → `td/m/coefnlse` exists, 5-7 leaves)

**Sub-menus PROXY-to-ther (compressed; cite 6.4 LEXICON-AUDIT-ther.md):**
- `cl_ther`, `defplan`, `dirprofi`, `eigvvloc`, `eigvvpol`, `methreso`,
  `profdroi`, `profplan`, `proj6cub`, `resoonin` (DEAD CODE — same flag as 6.4),
  `resothin`, `resothst`, `scheonde`, `sectplan`, `solsurf3`, `tempgrad`,
  `tracerr2`, `tracerr3`, `tracerrt`, `tracflux`, `tracgrad`, `traconde`,
  `tractem1`, `tractem2`, `tractem3`, `tractemp`, `trdepond`, `valisoth`,
  `valzone`, `valztxy` — these 30 sub-menus were FULLY EXPANDED in
  6.4 LEXICON-AUDIT-ther.md; nlse compresses each to a single row with
  notes citing 6.4 audit row range
- `methvvpr` — eigenvector method (was compressed in 6.4 as 6.2-shared);
  nlse stays compressed

**Sub-menus SHARED with mail/elas/flui/ther (compressed; cite 6.1 audit):**
- `affiche, couleur0, couleurs, entrsort, fichier, lecteur, managfen,
  managmef, mode_es, opt_lign, opt_surf, sectvopl, suivi_ms, suivitms,
  tracaxes, tuer, typ_objt, typtrait, vuesplan, zeros` — 20 shared util
  sub-menus (one less than ther's 21 because methvvpr is in the ther-shared
  bucket above)

**Total expected:** 20 top-level debunlse rows + ~10-20 fully-expanded
nlse-primary rows (cl_nlse + coefnlse fully expanded) + ~30 ther-proxy
compressed rows + 20 shared-util compressed rows + ~30-50 NLSE-visualization
leaf rows under codes 11-16 (each routes to a fragment of the
tractem/tracflux/tracgrad cascade with NTYPDESS=4/5/6 prefix variations) =
**~100-150 total rows** — within validator bound `[80, 250]`.

Justification for SMALLER row count than 6.4 ther's 209: ther fully expanded
30 ther-unique sub-menus; nlse legitimately treats those same 30 as
ther-proxy COMPRESSED rows (cite 6.4 audit). The genuinely NLSE-primary
expansion is just cl_nlse + coefnlse. This is the SAME pattern 6.4 used
to compress shared util sub-menus to 21 rows after they were fully
expanded in 6.1 — you don't re-expand shared trees once a peer audit owns
them.

### Frequency evidence source — DOMAIN REVIEW ONLY

**There are NO `.nlse` batch fixture files** on this worktree:

```bash
$ find testa/ testf/ -name "*.nlse" 2>/dev/null
(empty)
```

The five testa/ projects with `nlse` in their names — `nlsecu/`, `nlsecuvv/`,
`nlsecylbreak/` (does not exist on this worktree per `find` — possibly was
deleted), `nlsesqri/`, `nlsgpe/`, plus `gpesigc/` — contain ONLY mesh files
(`.meshq2`, `.meshp1`) + initial-guess files (`.iexri`, `.iexrr`, `.iexrrplus`,
`.evv`, `.imvv`, `.sigc`). NONE of them is a lexicon batch script.

This is a **larger sparsity problem** than 6.4's gourd evidence (which had 1
.ther fixture with 4.3K of multi-line block lexicon). Frequency assignment
is therefore **100% domain review** — no testa-derived counts exist. The
canonical NLSE workflow per ppnlse.f dispatch table + commented physics:

- `1;`  Object name (always set first — HIGH; mandatory per ppnlse.f:243-318)
- `2;`  NLSE INPUT DATA (mandatory before any solve — HIGH; gates DFNLSE +
  cl_nlse/coefnlse cascade)
- `5;`  IMPLICIT-time NLSE solver (canonical first solver run — domain HIGH;
  gourd-equivalent "default" solver; dispatches NLSEINS with TESTNL=6)
- `6;`  i-TIME NLSE solver (Gross-Pitaevskii branch — domain MED; the
  comment block at ppnlse.f:356-368 elevates this to second-tier solver
  for rotation-equation use cases)
- `11;` DRAW NLSE WAVE MODULE (primary visual — HIGH; the |U(t,X)| modulus
  is the universal NLSE result-visualization entry)
- `12;` DRAW NLSE WAVE REAL PART (specialty — MED)
- `13;` DRAW NLSE WAVE IMAG PART (specialty — MED)
- `14;` DRAW STOP-TEST VALUES (post-iteration diagnostic — MED)
- `15;` DRAW Max|U(Node)|(Time) (post-iteration time-series diagnostic — MED)
- `16;` DRAW REAL/IMAG ERROR vs Time (convergence diagnostic — MED)
- `19;` DRAW PLSVO mesh (mesh inspection — MED)
- `20;` PRECISION (parameters tweak — LOW; setter)
- `38;` WINDOW PIXEL DIMS (window-state — LOW)
- `39;` BACKGROUND COLOR (window-state — LOW)
- `71;` SUIVI TMS (TMS administration — LOW)
- `72;` SUIVI FILES MS (file administration — LOW)
- `73;` UNITES I/O (I/O administration — LOW)
- `74;` TUER PLSVO (TMS deletion — LOW; destructive op)
- `97;` MEFISTO VERSION (Help — LOW; Help-allowlist)
- `99;` SAVE & QUIT (HIGH — mandatory close)

**HIGH count target:** 5-7 codes (`1;`, `2;`, `5;`, `11;`, `99;` minimum, plus
optionally `6;` and `12;` if user-review at downstream phase wants Gross-Pitaevskii
parity with implicit-time)
**MED count target:** 6-10 codes
**LOW count target:** ~80-130 (long-tail visualization leaves + ~30 ther-proxy
compressed + 20 shared util compressed + cl_nlse + coefnlse leaves + dead-code
flags)

### Validator (reused from 6.1, generalized in 6.2 Plan 02)

`tools/validate_audit_md.py` — module-aware via `resolve_icon_path()`. Rule
9 deduces the owning module from the audit filename stem
(`LEXICON-AUDIT-nlse` → `nlse`), looks up `xvue/qt/resources/icons/nlse/<file>`,
falls back to scanning all module subdirs for shared reuses (e.g.,
`mesh-draw.svg` from `icons/mail/` for code 19; nlse's "Draw PLSVO mesh";
`solve-eigen.svg` from `icons/elas/` for code 6; if user wants — but eigen
family is NOT in nlse's debunlse so this reuse is unlikely;
`draw-temperature.svg` from `icons/ther/` could be reused for nlse codes
11-13 visualization but a separate `draw-nlse-modulus.svg` is more
informative).

**Plan 01 runs the validator in WARN mode** (default — no `AUDIT_STRICT_ICONS=1`)
because Plan 02 will ship the new nlse SVGs after Plan 01 freezes the audit.
Plan 02 flips to strict mode after the SVGs land.

### Top-level menu taxonomy (per ROADMAP Phase 6.5 + UI-SPEC)

ROADMAP.md Phase 6.5 line 220 specifies `{File, Nonlinear, View, Help}`.
Per 06.0-UI-SPEC §Per-Module Conformance Contract, the nlse module-specific
menu title is `Nonlinear / Non Linéaire` (EN / FR — confirm at audit time
against td/ma/debunlse line 1 which translates "Non Linear SCHRODINGER
Equation NLSE" — the natural FR label for the menu title is `Non Linéaire`).

Menu-content mapping PROPOSED (auto-frozen, mirroring 6.3/6.4 fully-autonomous
path; the auditor commits the proposal as final unless the LEXICON-AUDIT
data forces a deviation):

- **File menu** extensions (shared 6.0 File + nlse additions):
  - `71;` SUIVI TMS                 (File > TMS Tools)
  - `72;` SUIVI FILES MS            (File > MS File Tools)
  - `73;` UNITES I/O                (File > I/O Units)
  - `74;` TUER PLSVO                (File > Delete TMS — destructive; SP_TrashIcon)
  - `99;` SAVE DATA and QUIT (already bound by shared 6.0 actQuit_)

- **Nonlinear menu** (NEW top-level, replaces "Thermal" in taxonomy):
  - `1;`  OBJECT NAME to treat            — nlse-object.svg (new)
  - `2;`  NLSE INPUT DATA                  — nlse-input.svg (new)
  - `5;`  IMPLICIT NLSE solver             — solve-nlse-implicit.svg (new) OR
                                            REUSE elas's solve-dynamic.svg
  - `6;`  i-TIME (Gross-Pitaevskii) solver — solve-nlse-itime.svg (new) OR
                                            REUSE elas's solve-dynamic.svg
  - `Parameters` submenu:
    - `20;` PRECISION                       — SP_DialogApplyButton
    - `38;` WINDOW PIXELS                   — SP_ComputerIcon
    - `39;` BACKGROUND COLOR                — — (no icon)

- **View menu** extensions (shared 6.0 View + nlse additions):
  - `11;` DRAW NLSE WAVE MODULE            — draw-nlse-modulus.svg (new — primary
                                              visual)
  - `12;` DRAW NLSE WAVE REAL PART         — draw-nlse-real.svg (new) OR REUSE
                                              ther's draw-temperature.svg
  - `13;` DRAW NLSE WAVE IMAG PART         — draw-nlse-imag.svg (new) OR REUSE
                                              ther's draw-temperature.svg
  - `14;` DRAW STOP-TEST VALUES            — — (no icon — diagnostic)
  - `15;` DRAW Max|U(Node)|(Time)          — — (no icon — diagnostic)
  - `16;` DRAW REAL/IMAG ERROR vs Time     — — (no icon — diagnostic)
  - `19;` DRAW PLSVO MESHES                — :/xvue/qt/icons/icons/mail/mesh-draw.svg (REUSE)

- **Help menu** extensions (shared 6.0 Help + nlse additions):
  - `97;` MEFISTO VERSION DATE     (Help > Version — note nlse uses `97;`
    same as flui, NOT `98;` like ther. testHelpNoQueue allowlist must be `{97;}`.)

### Custom SVG icon set (proposed — 4-6 new icons + 2-3 reuses from prior phases)

Following 6.4's consolidation pattern (icons reused where semantically
identical), 6.5 proposes:

NEW nlse-specific SVGs (under `xvue/qt/resources/icons/nlse/`):
- `nlse-object.svg`         — Object name (code 1;) — abstract wave glyph
- `nlse-input.svg`          — NLSE input data (code 2;) — wave-equation glyph
- `solve-nlse-implicit.svg` — IMPLICIT solver (code 5;) — wave + integral sign
- `solve-nlse-itime.svg`    — i-TIME solver (code 6;) — wave + rotating arrow
                              (Gross-Pitaevskii rotation hint)
- `draw-nlse-modulus.svg`   — Draw |U(t,X)| (code 11;) — wave packet glyph
- `draw-nlse-real.svg`      — Draw Re U (code 12;) — sinusoid (cosine-like)
- `draw-nlse-imag.svg`      — Draw Im U (code 13;) — sinusoid (sine-like)

That's 7 candidate new SVGs. May consolidate at audit-freeze time:
- Real + Imag could SHARE one `draw-nlse-component.svg` (drops to 6 new)
- Implicit + i-time could SHARE one `solve-nlse.svg` (drops to 5 new)
- Reuse ther's `draw-temperature.svg` for code 11; (drops to 4 new — but
  loses NLSE-specific iconography; not recommended)

Reuses from prior phases (preferred):
- `mesh-draw.svg` from icons/mail/ — code 19; (DRAW PLSVO mesh; canonical reuse,
  same as ther/elas/flui)

Final SVG count target: **4-6 new SVGs** under `icons/nlse/` (after
consolidation) + 1 reuse (mesh-draw.svg).

### Proposed top-5 toolbar (auto-frozen at audit time)

Candidate set based on canonical NLSE workflow ergonomics (no testa
fixtures to anchor the choice, so domain review alone):

1. `2;`  NLSE INPUT DATA            (mandatory first step — gates solver)
2. `5;`  IMPLICIT NLSE solver       (canonical default solver run)
3. `11;` DRAW NLSE WAVE MODULE      (primary result visual — universal NLSE viz)
4. `19;` DRAW PLSVO MESHES          (mesh inspection — borrow from mail toolbar)
5. `99;` SAVE DATA and QUIT          (shared close-session convention)

Alternative: swap `19;` for `1;` if "Object first" workflow is more critical.
Alternative: swap `11;` for `15;` if convergence diagnostic dominates result
visualization. Auditor picks one and freezes — any change must keep the count
at exactly 5.

### Plan 03 Help-allowlist hand-off

The audit MUST contain a top-of-file or Summary-block note explicitly
documenting which Help lexicons Plan 02 will wire and Plan 03 will allow:

```markdown
## Help-allowlist (for Plan 03 testHelpNoQueue)

Per LEXICON-AUDIT-nlse row 97, the Help menu carries the audited lexicon `97;`
(MEFISTO Version Date). Plan 03's testHelpNoQueue slot allowlist:
`QSet<QString>{ QStringLiteral("97;") }` — MATCHES flui's `{97;}` (debuflui
line 21), DIFFERS from ther's `{98;}` (debuther line 22). This explicit
hand-off prevents Plan 03's testHelpNoQueue from inheriting a stale
`{98;}` from the 6.4 ther template.
```

This explicit hand-off prevents Plan 03's testHelpNoQueue from
inheriting a stale `{98;}` from the 6.4 ther template.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor → markdown | The auditor writes a markdown file consumed downstream by Plan 02 (QAction wiring) and Plan 03 (Help-allowlist test). Schema correctness is the trust contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.5-AUDIT-01 | Tampering | LEXICON-AUDIT-nlse.md schema | mitigate | tools/validate_audit_md.py rule set (9 hard rules) gates the file before commit; CI-runnable but enforced manually here. Prevents drift from the 9-column contract. |
| T-6.5-AUDIT-02 | Information Disclosure / Repudiation | Help-allowlist hand-off | mitigate | Audit explicitly documents `{97;}` (NOT `{98;}`) in a dedicated `## Help-allowlist` block; Plan 03 must read from this block, not inherit from a sibling-phase template. Prevents the silent ther-template-inheritance failure that 6.3 Plan 03 had to auto-fix as Rule 1. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate LEXICON-AUDIT-nlse.md via full recursive LIMTCL tree walk + domain-review frequency bucketing</name>
  <files>.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md</files>
  <read_first>
    - td/m/debunlse (FR top-level, 20 codes — the audit root)
    - td/ma/debunlse (EN top-level — bilingual labels source)
    - td/mf/debunlse (FR variant — present, mirrors td/m/; cite if it differs)
    - td/m/cl_nlse + td/ma/cl_nlse (NLSE boundary conditions sub-menu — fully expand)
    - td/m/coefnlse + td/ma/coefnlse (NLSE coefficient sub-menu — fully expand)
    - prpr/ppnlse.f (the dispatch table at lines 215-222: `GOTO( 100, 200, 300, 400, 500, 600, 700, 30, 30, 1000, 1100, 1200, 1300, 1400, 1500, 1600, 30, 30, 1900, 2000), NMTCL`. Note codes 7;, 8;, 9;, 17;, 18; fall through to `30`.)
    - .planning/phases/06.4-thermal-ther-menu-wiring/LEXICON-AUDIT-ther.md (the template — schema, Summary block layout, Help-allowlist section, ther-unique full expansion of cl_ther/methreso/scheonde/etc. that nlse compresses to single rows citing 6.4 audit)
    - .planning/phases/06.3-fluid-flui-menu-wiring/LEXICON-AUDIT-flui.md (helpful for the dead-code treatment pattern + Help-allowlist `{97;}` shape — flui matches nlse on Help code)
    - .planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md (reference for shared-util compressed-row notation + 71-74 separate top-level codes pattern)
    - tools/validate_audit_md.py (the validator — read to confirm Rule 9 module-aware ICONS resolver + the 9-rule contract)
  </read_first>
  <action>
Produce `.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md`
following the LEXICON-AUDIT-ther.md template **verbatim** with the
nlse-specific data substituted in. The auditor MUST NOT improvise the
schema — every section heading, the 9-column table format, and the
Summary Statistics block are byte-identical to the ther template aside
from the substitutions documented below.

### Step 1 — Verify the planning-time interfaces match runtime reality

```bash
cd /home/mefisto/git/mefisto

# 1a. Confirm 20 debunlse top-level codes
# (regex tolerates the leading '(' on the first line: `( 1 : 'NOM...'`)
grep -cE '^[[:space:]]*\(?[[:space:]]*[0-9]+[[:space:]]*:' td/m/debunlse    # expect 20
grep -cE '^[[:space:]]*\(?[[:space:]]*[0-9]+[[:space:]]*:' td/ma/debunlse   # expect 20

# 1b. Confirm 52 reachable LIMTCL sub-menus from ppnlse.f
grep -rEoh "LIMTCL\( *'[^']+'" ther/ util/ prpr/ppnlse.f reso/ 2>/dev/null \
  | sed "s/LIMTCL( *'//;s/'$//" | sort -u | wc -l    # expect 52

# 1c. Confirm cl_nlse + coefnlse menu files exist
test -f td/m/cl_nlse && test -f td/ma/cl_nlse && echo "cl_nlse OK"
test -f td/m/coefnlse && test -f td/ma/coefnlse && echo "coefnlse OK"

# 1d. Confirm Help code is 97; (not 98;)
grep -E '^[[:space:]]*97[[:space:]]*:' td/m/debunlse    # expect 1 line: 97 : 'DATE de version de Mefisto'
grep -E '^[[:space:]]*97[[:space:]]*:' td/ma/debunlse   # expect 1 line: 97 : 'MEFISTO VERSION DATE'
grep -cE '^[[:space:]]*98[[:space:]]*:' td/m/debunlse   # expect 0 (NLSE has no code 98)

# 1e. Confirm no .nlse fixtures exist (for the audit's Frequency Evidence section)
find testa/ testf/ -name "*.nlse" 2>/dev/null    # expect empty output
```

If any check fails, STOP and reconcile against the planning interfaces
table above (`<interfaces>`).

### Step 2 — Walk the LIMTCL tree and build the row set

For each of the 19 debunlse top-level codes:

A. **Codes with NLSE-PRIMARY expansion** (cl_nlse and coefnlse — the two
   sub-menus that 6.4 ther compressed and that 6.5 nlse FULLY expands as
   primary owner of NLSE physics):
   - `2;`  → DFNLSE prompts cl_nlse (5 leaves) + coefnlse (5-7 leaves);
     enumerate every `( N : '...')` row in `td/m/cl_nlse` and
     `td/m/coefnlse` with synthetic prefix `2;X;` where X is the
     LIMTCL choice index.
   - Per the 6.4 D-04 synthetic-high-number-prefix convention, choose
     the dispatch-true prefix where reachable, otherwise pick a
     synthetic high-number that does not collide with any sibling
     leaf.

B. **Codes with PROXY-TO-THER compression** (~30 sub-menus owned by 6.4
   audit):
   - For each of the 30 ther-proxy sub-menus listed in the
     `<interfaces>` block, write ONE row with:
     - `lexicon_path`: synthetic representative root (e.g.
       `11;7;` for tractem* family, `11;6;` for tracflux family,
       reusing the 6.4 audit's same prefixes where applicable —
       cross-check 6.4 LEXICON-AUDIT-ther.md row indices)
     - `description_fr` / `description_en`: from the corresponding
       td/m/ + td/ma/ menu file's `variable Debu... '<title>'` line,
       **suffixed with `(compressed — N leaves)`**
     - `frequency`: `low`
     - `qaction`: `no`
     - `toolbar`: `no`
     - `icon_source`: `—`
     - `shortcut`: `—`
     - `notes`: `<sub-menu> sub-menu — proxy to 6.4
       LEXICON-AUDIT-ther.md (full expansion there); reached from
       <call site>; X leaves total`
   - This is the **same compression** 6.4 ther applied to the 21
     shared util sub-menus. nlse's compression target is just LARGER
     (30 sub-menus instead of 21) because nlse legitimately defers
     the full ther-physics expansion to the 6.4 audit.

C. **Codes with SHARED-UTIL compression** (20 mail/elas/flui/ther
   shared sub-menus):
   - Write ONE row per shared util sub-menu, citing 6.1
     LEXICON-AUDIT-mail.md as the canonical owner (same as 6.4 ther
     did). Use the 6.4 ther rows verbatim minus the methvvpr ther-proxy
     row (methvvpr stays in the ther-proxy bucket above; treat as
     B-proxy not C-shared if you find it duplicated).

D. **Codes with NLSE-VISUALIZATION leaves** (codes 11;, 12;, 13;, 14;,
   15;, 16; visualization cascade):
   - Codes `11;`, `12;`, `13;` dispatch to `CALL TRTHER(KNOMOB, N, IERR)`
     with N in {4, 5, 6} per ppnlse.f:392, 399, 406. TRTHER then prompts
     a tractem* / tracflux / tracgrad / tracerrt cascade — these
     sub-menus are PROXY-TO-THER (compressed via bucket B above).
     The TOP-LEVEL visualizers (codes 11; 12; 13; themselves) get
     ONE ROW EACH at top-level — do NOT write rows for the per-component
     leaves under each, since those leaves are owned by 6.4 audit.
   - Codes `14;`, `15;`, `16;` dispatch to TRNLSETST / TRNLSEMXU /
     TRNLSERR (ppnlse.f:411-428). These are NLSE-PRIMARY routines —
     they reach NO further sub-menus per their grep-set membership
     (none of `tracerrt`, `tractem*`, etc. appear inside them — verify
     by reading `ther/trnlsetst.f`, `ther/trnlsemxu.f`, `ther/trnlserr.f`
     or equivalent located under `ther/`). One ROW each at top-level,
     no further leaves.

E. **Code `99;` SAVE & QUIT**: ONE top-level row with frequency=high,
   qaction=yes, toolbar=yes (mandatory close — convention).

F. **Code `97;` MEFISTO VERSION**: ONE top-level row with frequency=low,
   qaction=yes (Help menu — niche but documented), toolbar=no,
   icon_source=`SP_MessageBoxInformation`. Include the explicit
   Help-allowlist note at audit Summary block.

### Step 3 — Frequency bucketing rubric (DOMAIN ONLY)

NO testa-derived counts. Apply the per-code domain assignments from
the `<interfaces>` "Frequency evidence source — DOMAIN REVIEW ONLY"
table. Final HIGH set: `{1;, 2;, 5;, 11;, 99;}` (5 codes). MED set:
`{6;, 12;, 13;, 14;, 15;, 16;, 19;}` (7 codes). LOW set: everything
else (8 top-level codes + ~100-130 leaf rows; 20 − (5 HIGH + 7 MED) = 8).

Per Rule 8: rows with `qaction=yes` MUST have `frequency` in {high, med};
rows with `qaction=no` MUST have `frequency=low`.

### Step 4 — Top-5 toolbar selection (auto-freeze)

Pick exactly 5 rows from the qaction=yes set:
1. `2;` — NLSE INPUT DATA (mandatory data input gate)
2. `5;` — IMPLICIT NLSE solver (canonical first solver run)
3. `11;` — DRAW NLSE WAVE MODULE (primary result visual)
4. `19;` — DRAW PLSVO MESHES (mesh inspection — reused from 6.1 mail)
5. `99;` — SAVE & QUIT (shared close-session convention)

Mark exactly these 5 with `toolbar=yes`; every other row is `toolbar=no`.
Document the rationale in the audit Summary Statistics block.

### Step 5 — Custom SVG icon manifest

In the audit's "Nlse-unique SVG icon set (Plan 02 ships)" Summary
section, list the planned SVGs. Default = 5 new + 1 reuse:

NEW (under `xvue/qt/resources/icons/nlse/`):
- `nlse-object.svg`        — code 1;
- `nlse-input.svg`         — code 2;
- `solve-nlse-implicit.svg` — code 5;
- `draw-nlse-modulus.svg`  — code 11;
- `draw-nlse-component.svg` — codes 12; (Re U) AND 13; (Im U) — single
                              icon shared between the two leaves to keep
                              new-SVG count modest

REUSED (no new file):
- `mesh-draw.svg` from `xvue/qt/resources/icons/mail/` — code 19;

Plan 02 ships 5 new SVGs. The audit's `icon_source` column for codes
1, 2, 5, 11, 12, 13 cites the corresponding new filename; code 19 cites
the full reuse path `:/xvue/qt/icons/icons/mail/mesh-draw.svg`; code 6
defaults to `—` (no icon — not toolbar; user can add `solve-nlse-itime.svg`
in a polish-pass if Gross-Pitaevskii branch becomes a documented use case).

### Step 6 — Help-allowlist hand-off block

Add this VERBATIM block to the audit at the appropriate position
(between the Top-level table and the Audit Table, mirroring 6.4):

```markdown
## Help-allowlist (for Plan 03 testHelpNoQueue — explicit hand-off)

Per LEXICON-AUDIT-nlse row `97;` (MEFISTO Version Date), the Help menu
carries the audited lexicon **`{97;}`**. Plan 03's `testHelpNoQueue`
slot allowlist:

`QSet<QString>{ QStringLiteral("97;") }` — **MATCHES** flui's `{97;}`
(debuflui line 21 also uses code 97), **DIFFERS** from ther's `{98;}`
(debuther line 22). This explicit hand-off prevents Plan 03 from
inheriting a stale `{98;}` from the 6.4 ther template (Auto-fix Rule 1
lesson from 6.3 Plan 03).
```

### Step 7 — Validate

```bash
python3 tools/validate_audit_md.py \
    .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md
```

Expected output: `OK -- N rows, 5 toolbar=yes, schema valid` where N is in
[80, 250]. **WARN lines about missing nlse SVGs are EXPECTED** at this
stage (no `xvue/qt/resources/icons/nlse/` directory exists yet — Plan 02
creates it). DO NOT set `AUDIT_STRICT_ICONS=1` here. Strict mode is the
Plan 02 acceptance gate (after icons land).

### Step 8 — Commit

Single atomic commit:

```bash
node /home/drico/git/mefisto/.claude/get-shit-done/bin/gsd-tools.cjs commit \
    "docs(06.5): add LEXICON-AUDIT-nlse.md (UX-05 nlse-slice §1)" \
    --files .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md
```

If gsd-tools.cjs is not at that path on this worktree, use:
```bash
git add .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md
git commit -m "docs(06.5): add LEXICON-AUDIT-nlse.md (UX-05 nlse-slice §1)"
```
  </action>
  <verify>
    <automated>cd /home/mefisto/git/mefisto && python3 tools/validate_audit_md.py .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` (exit 0)
    - Validator passes: `python3 tools/validate_audit_md.py .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` exits 0 and prints `OK -- N rows, 5 toolbar=yes, schema valid` with N in [80, 250]
    - Help code is `97;` not `98;`: `grep -c 'QStringLiteral("97;")' .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` returns >= 1; `grep -c 'QStringLiteral("98;")' .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` returns 0
    - Help-allowlist block exists: `grep -c '## Help-allowlist' .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` returns 1
    - Top-level menu taxonomy noted: `grep -cE '\{File,[[:space:]]*Nonlinear,[[:space:]]*View,[[:space:]]*Help\}' .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` returns >= 1
    - 19 top-level debunlse rows exist (lexicon_path matches `^N;` for N in {1,2,5,6,11,12,13,14,15,16,19,20,38,39,71,72,73,74,97,99}): `grep -c '^| 1; \|^| 2; \|^| 5; \|^| 11; \|^| 99; ' .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` returns >= 5
    - Exactly 5 toolbar=yes rows: validator output line `5 toolbar=yes`
    - Frequency bucketing follows Rule 8: `grep -E '\| (high|med) \| no \|' .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` returns no lines (no high/med row may have qaction=no); `grep -E '\| low \| yes \|' .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` returns no lines (no low row may have qaction=yes)
    - File is committed: `git log --oneline -1 .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` shows a docs(06.5) commit
  </acceptance_criteria>
  <done>
    LEXICON-AUDIT-nlse.md is committed, validator-clean, and contains:
    - 20 top-level debunlse rows (codes 1, 2, 5, 6, 11, 12, 13, 14, 15, 16, 19, 20, 38, 39, 71, 72, 73, 74, 97, 99)
    - 5 HIGH rows (`1;`, `2;`, `5;`, `11;`, `99;`) + 7 MED rows + remainder LOW
    - Exactly 5 toolbar=yes rows (`2;`, `5;`, `11;`, `19;`, `99;`)
    - Help-allowlist block stating `{97;}` (NOT `{98;}`)
    - Custom SVG manifest listing 5 new nlse-specific SVGs + 1 reuse (mesh-draw.svg)
    - Top-level menu taxonomy ratified as `{File, Nonlinear, View, Help}`
    Plan 02 can read qaction=yes rows as the QAction wire-up list and the SVG
    manifest as the icon ship-list with zero further audit work.
  </done>
</task>

</tasks>

<verification>
- Validator clean: `python3 tools/validate_audit_md.py .../LEXICON-AUDIT-nlse.md` exits 0
- Row count in `[80, 250]` (forecast: ~100-150 rows; lower than ther's 209
  because nlse legitimately compresses the 30 ther-proxy sub-menus instead
  of expanding them, citing 6.4 audit as canonical owner)
- Exactly 5 toolbar=yes rows
- Help-allowlist `{97;}` documented (NOT `{98;}` like ther — this is the
  explicit per-module variation)
- All 5 HIGH rows are top-level debunlse codes that match ppnlse.f
  dispatch behavior
- Top-level menu taxonomy `{File, Nonlinear, View, Help}` ratified
- Plan 02 can read this file with zero further questions
</verification>

<success_criteria>
The audit is the contract Plan 02 reads to wire QActions. Plan 02 needs:
- A flat list of `qaction=yes` rows with `lexicon_path` + `icon_source`
  + Thermal-equivalent menu placement (top-level, sub-menu, parameters)
- Exactly 5 toolbar rows with `icon_source` columns
- The 5 new SVG filenames it must ship under `xvue/qt/resources/icons/nlse/`
- The Help-allowlist `{97;}` it must hard-code into Plan 03's
  testHelpNoQueue slot

Plan 03 needs:
- The Help-allowlist `{97;}` value verbatim
- The qaction set to size the testHelpNoQueue / testNonlinearSolveImplicit /
  testViewDrawNlseModule / testFileTMSTools test slots

UX-05 nlse-slice Success Criterion #1: ENUMERATES every interactive command
in the nlse module — SATISFIED when 20 top-level debunlse rows are present
and every reachable sub-menu has at least one row (compressed proxy or
expanded primary).
</success_criteria>

<output>
After completion, create `.planning/phases/06.5-nonlinear-nlse-menu-wiring/06.5-01-SUMMARY.md`.
</output>
