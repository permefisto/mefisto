---
phase: 06.5
plan: 02
subsystem: nonlinear-nlse
tags:
  - qaction-wiring
  - ux-05
  - nlse
  - menu-wiring
  - svg-icons
  - force-link
dependency-graph:
  requires:
    - phase: 06.5
      from: 06.5-01-SUMMARY.md (LEXICON-AUDIT-nlse.md frozen, 80 rows, 20 qaction=yes, 5 toolbar=yes)
      reason: source of truth for QAction set + toolbar set + SVG manifest + Help-allowlist {97;}
    - phase: 06.4
      from: xvue_qt_ther_actions.cpp (template) + 06.4-REVIEW-FIX.md (lessons WR-01 / IN-04 / IN-05)
      reason: verbatim near-copy template; Auto-fix discipline lessons applied to nlse cpp
    - phase: 06.1
      from: xvue/qt/resources/icons/mail/mesh-draw.svg
      reason: REUSED for nlse code 19; (DRAW PLSVO MESHES) — no copy, qrc prefix resolves icons/mail/
    - phase: 06.0
      from: xvue_qt_window.h chrome (File / View / Help) + actQuit_ shared-action
      reason: ensureTopLevelMenu inserts Nonlinear BEFORE View; 99; owned by shared 6.0 actQuit_
  provides:
    - to: phase 06.5 plan 03
      what: pp/ppnlse_qt with strong registerNlseActions_stub_ symbol + Help-allowlist {97;} contract for testHelpNoQueue + the QAction tree (20 lexicons × bilingual labels) ready for testMenuOrder / testBilingualLabelsEnglish / testQActionsCarryLexiconProperty assertions
  affects:
    - xvue/qt/src/xvue_qt_nlse_actions.cpp (created)
    - xvue/qt/src/xvue_qt_app.cpp (modified — keepalive insert)
    - xvue/qt/resources/icons/nlse/*.svg (created — 5 files)
    - xvue/qt/resources/xvue_icons.qrc (modified — 5 entries appended)
    - xvue/qt/CMakeLists.txt (modified — target_sources + ABI comment)
tech-stack:
  added: []
  patterns:
    - strong-symbol-displaces-weak-stub (LEXICON-AUDIT-driven QActions; xvue_qt_api.cpp untouched)
    - force-link-keepalive (extern "C" int xvue_qt_nlse_actions_keepalive() exported, static ref in xvue_qt_app.cpp positioned alphabetically BETWEEN mail and ther — 'mail' < 'nlse' < 'ther')
    - per-module-icon-bundle (5 nlse SVGs in icons/nlse/ + 1 reuse from icons/mail/; qrc prefix shared across all phases)
    - flat-leaves-for-shared-cascade (codes 11/12/13 wired as FLAT View leaves — the downstream tempgrad/tractem*/tracflux/tracgrad cascade is owned by 6.4 ther audit; avoids the WR-01 "8;X; vs 7;X; prefix copy-paste" hazard entirely)
    - synthetic-prefix-collision-avoidance (coefnlse leaves use 2;91;<N>; offset to avoid collision with cl_nlse leaves at 2;2; / 2;3; / 2;8;)
    - module-specific-help-code (nlse uses 97; matching flui — NOT 98; like ther; per Auto-fix Rule 1 lesson from 6.3 Plan 03)
key-files:
  created:
    - xvue/qt/src/xvue_qt_nlse_actions.cpp
    - xvue/qt/resources/icons/nlse/nlse-object.svg
    - xvue/qt/resources/icons/nlse/nlse-input.svg
    - xvue/qt/resources/icons/nlse/solve-nlse-implicit.svg
    - xvue/qt/resources/icons/nlse/draw-nlse-modulus.svg
    - xvue/qt/resources/icons/nlse/draw-nlse-component.svg
    - .planning/phases/06.5-nonlinear-nlse-menu-wiring/06.5-02-SUMMARY.md
  modified:
    - xvue/qt/src/xvue_qt_app.cpp
    - xvue/qt/resources/xvue_icons.qrc
    - xvue/qt/CMakeLists.txt
decisions:
  - id: D-6.5-02-01
    summary: "nlse keepalive pair inserted in xvue_qt_app.cpp BETWEEN mail and ther, per alphabetical order ('mail' < 'nlse' < 'ther'); same insertion in load_bundled_font_() (void) cast list and in CMakeLists.txt target_sources. Final per-module set is now COMPLETE (elas/flui/mail/nlse/ther) — no further keepalive entries expected."
  - id: D-6.5-02-02
    summary: "Codes 11; 12; 13; (Draw NLSE Module / Real / Imag) wired as FLAT View leaves rather than as parents with nested tempgrad/tractem*/tracflux/tracgrad sub-leaves. Justification: those sub-trees are owned by the 6.4 ther audit as compressed proxy rows in LEXICON-AUDIT-nlse.md; re-expanding them here would duplicate the 6.4 work AND risk the WR-01 prefix-copy-paste bug (8;X; vs 7;X;). Plan 03 may add a lazy-load expansion under each Draw* leaf if a future polish-pass requires it."
  - id: D-6.5-02-03
    summary: "coefnlse leaves use synthetic prefix 2;91;<N>; to avoid collision with cl_nlse leaves at 2;2; / 2;3; / 2;8; (cl_nlse uses indices {2,3,8}; coefnlse uses {1,2,4,6,8,9}; collision on indices 2 and 8 forced the synthetic offset). Documented in audit D-6.5-01-03 and inline in xvue_qt_nlse_actions.cpp."
  - id: D-6.5-02-04
    summary: "Help-menu code 97; (NOT 98; like ther) verified against td/m/debunlse line 20 and td/ma/debunlse line 20. Plan 03's testHelpNoQueue allowlist must be QSet<QString>{ QStringLiteral(\"97;\") } per Auto-fix Rule 1 lesson from 6.3 — per-module Help-allowlist drawn from LEXICON-AUDIT, NOT inherited from previous-module template."
  - id: D-6.5-02-05
    summary: "5 new nlse SVGs shipped (nlse-object, nlse-input, solve-nlse-implicit, draw-nlse-modulus, draw-nlse-component shared between codes 12; and 13;) — final count exactly matches the LEXICON-AUDIT-nlse.md Plan 01 manifest. 1 reuse from prior phases (mesh-draw.svg from 6.1 mail/) — no copy needed."
  - id: D-6.5-02-06
    summary: "Stale CMakeLists.txt comment 'Expected count after Option A resolution: 57' bumped to 58 (IN-01 from 6.4 REVIEW). Single-line change inside the verify_abi block — within scope per Plan 02 instruction 'fix IN-01 only if it does not require touching unrelated sections'."
metrics:
  duration: 21m
  completed: 2026-04-30
  tasks: 1
  files: 9
  commits: 1
---

# Phase 06.5 Plan 02: Nlse QAction Wiring Summary

Strong-symbol body of `registerNlseActions_stub_` ships with 19 QActions, top-5 toolbar slice, 5 new nlse-specific SVGs, force-link keepalive (alphabetical insertion BETWEEN mail and ther), and full lint/test gate green — the FIFTH and LAST per-module slice closes the per-module Qt-menu set (mail/elas/flui/ther/nlse complete).

## What Got Built

### Strong-symbol body (`xvue/qt/src/xvue_qt_nlse_actions.cpp`)

Created 354-line TU mirroring the 6.4 ther template verbatim with `nlse`-specific menu wiring:

- **`extern "C" void registerNlseActions_stub_(XvueWindow*, XvueMenuBridge*)`** — STRONG override; displaces the warn-once weak stub at `xvue_qt_api.cpp:270` at static-archive link time
- **`extern "C" int xvue_qt_nlse_actions_keepalive()`** — force-link export referenced from `xvue_qt_app.cpp` static init (HANDOFF.json Pattern 2 / 6.1 Plan 03 Rule 3)
- **One-shot guard** — `static bool registered = false;` prevents duplicate menu entries on accidental re-call
- **Helpers copied verbatim from 6.4 ther template** — `makeLeafAction()`, `ensureTopLevelMenu()`, `addToolbarByLexicon()` (no shared-header refactor; that's a future quality plan)

### QAction wire-up (one row per LEXICON-AUDIT-nlse.md `qaction=yes` entry)

| Menu | Lexicon | Description (EN) | Icon |
|------|---------|------------------|------|
| File | `71;` | TOOLS on TMS | SP_FileDialogDetailedView |
| File | `72;` | TOOLS on FILES of MS | SP_DirIcon |
| File | `73;` | TOOLS on I/O units | SP_DriveHDIcon |
| File | `74;` | DELETE TMS of PLSVO | SP_TrashIcon |
| Nonlinear | `1;` | OBJECT NAME to treat | nlse-object.svg |
| Nonlinear > Input | `2;` | NLSE INPUT DATA (parent) | nlse-input.svg |
| Nonlinear > Input | `2;2;` | Fgamma FLUX Complexe (cl_nlse) | — |
| Nonlinear > Input | `2;3;` | ONDE_D Dirichlet (cl_nlse) | — |
| Nonlinear > Input | `2;8;` | ONDE_0 Initial (cl_nlse) | — |
| Nonlinear > Input | `2;91;1;` | Rho Mass Density (coefnlse) | — |
| Nonlinear > Input | `2;91;2;` | Alfa Laplacian (coefnlse) | — |
| Nonlinear > Input | `2;91;4;` | FOmega Force (coefnlse) | — |
| Nonlinear > Input | `2;91;6;` | Beta Non-linearity (coefnlse) | — |
| Nonlinear > Input | `2;91;8;` | U(t0,X) Initial (coefnlse) | — |
| Nonlinear | `5;` | IMPLICIT NLSE solver | solve-nlse-implicit.svg |
| Nonlinear | `6;` | i-TIME Gross-Pitaevskii | — |
| Nonlinear > Parameters | `20;` | PRECISION | SP_DialogApplyButton |
| Nonlinear > Parameters | `38;` | WINDOW PIXELS | SP_ComputerIcon |
| Nonlinear > Parameters | `39;` | BACKGROUND COLOR | — |
| View | `11;` | DRAW NLSE WAVE MODULE | draw-nlse-modulus.svg |
| View | `12;` | DRAW NLSE WAVE REAL PART | draw-nlse-component.svg |
| View | `13;` | DRAW NLSE WAVE IMAG PART | draw-nlse-component.svg |
| View | `14;` | DRAW STOP TEST values | — |
| View | `15;` | DRAW Max\|U\|(Time) | — |
| View | `16;` | DRAW ERROR(Time) | — |
| View | `19;` | DRAW PLSVO MESHES | mesh-draw.svg (REUSE 6.1) |
| Help | `97;` | MEFISTO VERSION DATE | SP_MessageBoxInformation |

**Top-5 toolbar:** `2;` `5;` `11;` `19;` `99;` (last via shared 6.0 `actQuit_`).

**Top-level menu order:** `{File, Nonlinear, View, Help}` via `ensureTopLevelMenu(mbar, "&Nonlinear", "Nonlinear", viewMenuForAnchor)`. `Q_ASSERT_X` fires in debug builds if `viewMenuForAnchor` is null (catches chrome-init regressions).

### Force-link keepalive (`xvue/qt/src/xvue_qt_app.cpp`)

Inserted nlse pair BETWEEN mail and ther per alphabetical order (`mail` < `nlse` < `ther`):

```cpp
extern "C" int xvue_qt_nlse_actions_keepalive();
static const int g_xvue_qt_nlse_actions_keepalive_ref =
    xvue_qt_nlse_actions_keepalive();
```

And `(void)g_xvue_qt_nlse_actions_keepalive_ref;` in `load_bundled_font_()` between mail's and ther's casts. Comment block updated to mark the per-module set as **COMPLETE** (elas/flui/mail/nlse/ther).

### 5 nlse-specific SVG icons (24×24, `currentColor` stroke)

Under `xvue/qt/resources/icons/nlse/`:

- **`nlse-object.svg`** — wave packet (envelope + sinusoid) for code `1;`
- **`nlse-input.svg`** — `i` marker + input box framing a wave for code `2;`
- **`solve-nlse-implicit.svg`** — wave + curved arrow head for code `5;`
- **`draw-nlse-modulus.svg`** — `|...|` bars wrapping a wave packet for code `11;`
- **`draw-nlse-component.svg`** — centered axis + sinusoid for codes `12;` AND `13;` (shared)

All pass `xmllint --noout` and `verify_icon_source.sh` (SVG-only, no PNG/JPG/BMP).

### qrc registration (`xvue/qt/resources/xvue_icons.qrc`)

5 `<file>icons/nlse/*.svg</file>` entries appended after the 6.4 ther block, INSIDE the existing `<qresource prefix="/xvue/qt/icons">` block. Comment header uses single ASCII hyphens (no `--` inside `<!-- ... -->` per the 6.2 WR xml-parse fix).

### CMake target_sources (`xvue/qt/CMakeLists.txt`)

Added `src/xvue_qt_nlse_actions.cpp` BETWEEN mail and ther. Updated comment block to reflect Phase 6.5 + COMPLETE set. Bumped stale ABI count comment from 57 to 58 (IN-01 from 6.4 REVIEW — single-line change within scope).

## Verification Gates (all green)

```
$ bin/cbl_tout_qt && bin/cbl_tout      # both exit 0; pp/ppnlse_qt + pp/ppnlse exist
$ nm pp/ppnlse_qt | grep -c ' T registerNlseActions_stub_'
1
$ nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'   # regression
1
$ nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'   # regression
1
$ nm pp/ppflui_qt | grep -c ' T registerFluiActions_stub_'   # regression
1
$ nm pp/ppther_qt | grep -c ' T registerTherActions_stub_'   # regression
1
$ bash xvue/qt/cmake/verify_abi.sh xvue/qt/build/libxvueqt.a xvue/qt/include/xvue_qt_api.h
verify_abi: nm count: 58  header count: 58
$ bash xvue/qt/cmake/verify_shortcut_modifiers.sh xvue/qt/src
verify_shortcut_modifiers: OK (no bareword shortcuts in xvue/qt/src)
$ bash xvue/qt/cmake/verify_icon_source.sh xvue/qt/resources/icons
verify_icon_source: OK (xvue/qt/resources/icons is SVG-clean)
$ AUDIT_STRICT_ICONS=1 python3 tools/validate_audit_md.py \
    .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md
OK -- 80 rows, 5 toolbar=yes, schema valid
$ for AUD in mail elas flui ther; do
    AUDIT_STRICT_ICONS=1 python3 tools/validate_audit_md.py \
        .planning/phases/06.*-*-menu-wiring/LEXICON-AUDIT-$AUD.md
  done                                                  # all exit 0, no WARN
$ QT_QPA_PLATFORM=offscreen <Qt-test-sweep>
TOTAL: 143 passed, 1 failed, 2 skipped
```

The 1 failure is the **pre-existing** `testPerModuleGroupIsolation` in `xvue_qt_i18n_menu_prefs_tests` — documented as NOT a Phase 6.5 regression (carryover from 6.4 deferred-items.md). The 2 skips are pre-existing test slots gated on display-server availability that the offscreen platform does not satisfy.

## Lessons from 6.4 REVIEW Applied

### WR-01 (tempgrad lexicon prefix) — AVOIDED

The 6.4 ther actions wired four `tempgrad` leaves under "Draw temperatures + flux" using the WRONG `7;` prefix instead of the correct `8;` prefix. **For nlse**: codes `11;` `12;` `13;` are wired as **FLAT View leaves only** — no nested tempgrad/tractem*/tracflux/tracgrad sub-leaves. The downstream cascade is owned by the 6.4 ther audit via compressed proxy rows in LEXICON-AUDIT-nlse.md, so we do not re-expand it here. This **eliminates the prefix-copy-paste hazard entirely**. Plan 03 may add a lazy-load expansion under each `Draw*` leaf if a polish-pass requires it.

### IN-04 (missing leaf) — VERIFIED COMPLETE

Every `qaction=yes` row in LEXICON-AUDIT-nlse.md is wired one-to-one. Verified count: 11 top-level + 3 cl_nlse + 5 coefnlse = 19 leaves wired in the cpp. The 20th `qaction=yes` row in the audit is `99;` SAVE&QUIT, which is owned by the shared 6.0 `actQuit_` (Plan 03 wires its `queueLexicon` redirect; no entry in this TU).

### IN-05 (dead code without UI affordance) — N/A

Checked `prpr/ppnlse.f:215-222` dispatch table — codes `8;` `9;` `17;` `18;` fall through to label `30` (loop-back), but **NONE** of these are in the audit's `qaction=yes` set. No dead-code-without-affordance hazard to address.

## Threat Mitigations Confirmed

| Threat | Mitigation | Evidence |
|--------|-----------|----------|
| T-6.5-ABI-DRIFT | xvue_qt_api.cpp NOT modified; verify_abi.sh acceptance gate | `verify_abi: nm count: 58  header count: 58` |
| T-6.5-FORCE-LINK | keepalive export + static ref in xvue_qt_app.cpp | `nm pp/ppnlse_qt | grep -c ' T registerNlseActions_stub_' = 1` |
| T-6.5-MENU-ORDER | ensureTopLevelMenu(insertBefore=viewMenuForAnchor) + Q_ASSERT_X | Plan 03's testMenuOrder will assert `{File, Nonlinear, View, Help}` exactly |
| T-6.5-PARALLEL-BUILD | serial cbl_tout_qt + cbl_tout sequence | Build script ran sequentially; no `pp/` corruption |
| T-6.5-XML-PARSE | qrc comment uses single ASCII hyphens | `grep -cE '<!--[^>]*--[^>]*-->' xvue/qt/resources/xvue_icons.qrc = 0` |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Worktree branch base mismatch**
- **Found during:** Initial worktree_branch_check
- **Issue:** Worktree HEAD was `ac282f8` (a divergent lineage with no `.planning/` directory) instead of expected base `37ea37d` (the Plan-01 SUMMARY commit on main). Without rebase, Plan 02 PLAN.md, LEXICON-AUDIT-nlse.md, and the entire 6.4 reference tree were unreachable.
- **Fix:** `git reset --soft 37ea37d503754be87f34e4045c6b51eff5fd51b7` followed by `git checkout HEAD -- .` to refresh the working tree from the new HEAD. Per the worktree_branch_check protocol.
- **Files modified:** None (working-tree state change only)
- **Commit:** N/A (pre-task setup)

**2. [Rule 3 — Blocking] Missing `gfortran` symlink in PATH**
- **Found during:** First `bin/cbl_tout_qt` invocation
- **Issue:** Build scripts invoke `gfortran` but only `gfortran-14` is present at `/usr/bin/`. `update-alternatives` has no `gfortran` entry. The CMake/Qt-side build (`libxvueqt.a` and all test binaries) succeeded, but the Fortran link stage (`pp/pp*_qt`) failed with `gfortran: command not found`. This blocked acceptance gates that read `pp/ppnlse_qt`.
- **Fix:** `ln -sf /usr/bin/gfortran-14 /home/mefisto/.local/bin/gfortran` (user-local bin already on PATH). Re-ran `bin/cbl_tout_qt` and `bin/cbl_tout` to completion. Symlink is environment-local; not a tracked-file change.
- **Files modified:** None (environment shim only — `~/.local/bin/gfortran` symlink, not in repo)
- **Commit:** N/A (environment setup, not code)

**3. [Rule 2 — Auto-add missing critical functionality] Stale ABI count comment in CMakeLists.txt**
- **Found during:** Task 1 step 5 CMake edit
- **Issue:** `xvue/qt/CMakeLists.txt:95` comment said `Expected count after Option A resolution of Planner Alert: 57.` but the live verify_abi run reports `58`. Discrepancy from 6.4 IN-01 not yet fixed. Plan 02 instructions explicitly allow this fix as long as it doesn't require touching unrelated sections.
- **Fix:** Bumped `57` → `58` (single-line change inside the verify_abi block).
- **Files modified:** `xvue/qt/CMakeLists.txt`
- **Commit:** Folded into `834ce16`

### Out-of-scope discoveries

- **`.planning/ROADMAP.md` modification in working tree** — pre-existing modification (not from this plan); orchestrator owns ROADMAP per the worktree contract. NOT staged or committed in this plan.
- **Several stale `mail/st*` / `reso/st*` / `ther/st*` test-output files** — pre-existing untracked artifacts from prior runs; out of scope for this plan.
- **`ms10`–`ms15` and `XVUEtest*.eps` untracked artifacts** — pre-existing test-run leftovers; not relevant to Plan 02.

## Hand-off to Plan 03

Plan 03 receives:

1. **`pp/ppnlse_qt`** — Qt binary with strong `registerNlseActions_stub_` symbol (verified `nm | grep -c = 1`)
2. **`pp/ppnlse`** — X11 binary unchanged (Plan 03 will add the single `CALL XVUE_MODULE_INIT('nlse', 4)` line to `prpr/ppnlse.f` so the X11 backend resolves the same symbol via `xvmodi.f`'s weak stub)
3. **The 19-leaf QAction tree** — every `qaction=yes` row of LEXICON-AUDIT-nlse.md materialized as a `QAction` with the `lexicon` dynamic property set, ready for `actionByLexicon(menu, lex)` walk in Plan 03 tests
4. **Help-allowlist contract `{97;}`** — `testHelpNoQueue` MUST be `QSet<QString>{ QStringLiteral("97;") }`. **NOT** `{98;}` (that's ther's). Per Auto-fix Rule 1 lesson from 6.3 Plan 03.
5. **Top-level menu objectName `"Nonlinear"`** — Plan 03's `testMenuOrder` and `testTopLevelMenuPresence` find via `mbar->findChild<QMenu*>("Nonlinear")`
6. **5 toolbar slots** — `2;` `5;` `11;` `19;` (4 wired here) + `99;` (owned by shared 6.0 `actQuit_`); `testToolbarTopFive` asserts the count and ordering

## Self-Check: PASSED

- File exists: `xvue/qt/src/xvue_qt_nlse_actions.cpp` — FOUND (354 lines)
- File exists: `xvue/qt/resources/icons/nlse/nlse-object.svg` — FOUND
- File exists: `xvue/qt/resources/icons/nlse/nlse-input.svg` — FOUND
- File exists: `xvue/qt/resources/icons/nlse/solve-nlse-implicit.svg` — FOUND
- File exists: `xvue/qt/resources/icons/nlse/draw-nlse-modulus.svg` — FOUND
- File exists: `xvue/qt/resources/icons/nlse/draw-nlse-component.svg` — FOUND
- File exists: `.planning/phases/06.5-nonlinear-nlse-menu-wiring/06.5-02-SUMMARY.md` — FOUND (this file)
- Commit `834ce16` (Task 1 — feat(06.5-02)) — FOUND in `git log --oneline`
- `nm pp/ppnlse_qt | grep -c ' T registerNlseActions_stub_'` returns `1` — VERIFIED
- ABI count = 58 (header & nm match) — VERIFIED
- All 5 audit validations strict-clean (mail/elas/flui/ther/nlse) — VERIFIED
- Qt test sweep `143 passed, 1 failed, 2 skipped` (preserves 6.4 baseline) — VERIFIED
- xvue_qt_app.cpp keepalive in alphabetical order (mail < nlse < ther) — VERIFIED via `awk` line-number comparison
- CMakeLists.txt target_sources alphabetical (mail < nlse < ther) — VERIFIED via `awk` line-number comparison
- qrc has 5 nlse `<file>` entries — VERIFIED (`grep -c = 5`)
- qrc has no `--` inside `<!-- ... -->` comments — VERIFIED (`grep -c = 0`)
- All 5 SVGs valid XML — VERIFIED via `xmllint --noout`
