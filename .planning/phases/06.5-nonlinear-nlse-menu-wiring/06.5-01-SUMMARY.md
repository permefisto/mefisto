---
phase: 06.5
plan: 01
subsystem: nonlinear-nlse
tags:
  - lexicon-audit
  - ux-05
  - nlse
  - menu-wiring
  - documentation
dependency-graph:
  requires:
    - phase: 06.4
      from: LEXICON-AUDIT-ther.md
      reason: 9-column schema template (verbatim mirror); ther-proxy compressed-row pattern (31 ther sub-menus deferred to 6.4 audit as canonical owner); domain-review-only frequency bucketing for sparse testa corpora
    - phase: 06.3
      from: LEXICON-AUDIT-flui.md
      reason: Help-allowlist {97;} pattern (matches nlse — NOT ther's {98;})
    - phase: 06.2
      from: tools/validate_audit_md.py module-aware ICONS resolver
      reason: validator works for any LEXICON-AUDIT-<mod>.md; Rule 9 cross-checks SVGs in icons/<mod>/ first then scans all module subdirs
    - phase: 06.1
      from: LEXICON-AUDIT-mail.md
      reason: 71-74 separate-top-level-codes pattern (mirrors mail; UNLIKE 6.2/6.3/6.4 which use combined 70;)
  provides:
    - to: phase 06.5 plan 02
      what: 20 qaction=yes rows (registerNlseActions_stub_ QAction set) + 5 toolbar=yes rows (top-5 toolbar) + 5 nlse-specific SVG filenames (xvue_icons.qrc append set under icons/nlse/) + 1 reuse (mesh-draw.svg from 6.1 mail/)
    - to: phase 06.5 plan 03
      what: Help-allowlist {97;} for testHelpNoQueue QSet<QString> (matches flui; NOT {98;} like ther) + frozen 9-column schema + frequency bucketing for testMenuOrder/testBilingualLabelsEnglish anchors
  affects:
    - .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md (created)
tech-stack:
  added: []
  patterns:
    - module-deferral-to-peer-audit (31 ther-proxy compressed rows cite 6.4 LEXICON-AUDIT-ther.md as canonical owner — same pattern 6.4 used for 21 shared-util rows owned by 6.1 mail; nlse legitimately compresses the entire ther visualization tree under codes 11;/12;/13; since TRTHER cascade leaves are owned by 6.4 audit)
    - synthetic-prefix-collision-avoidance (cl_nlse leaves use 2;<N>; for direct dispatch prefix; coefnlse leaves use synthetic 2;91;<N>; offset to avoid collision since both sub-menus share leaf indices 2; and 8;)
    - domain-review-only frequency bucketing (no .nlse fixtures exist on this worktree — testa/{nlsecu,nlsecuvv,nlsesqri,nlsgpe,gpesigc} contain only mesh + initial-guess data files, NOT lexicon batch scripts; sparser even than 6.4 ther's gourd evidence)
    - module-specific-help-code (nlse uses 97; matching flui — NOT ther's 98;; explicit per-module variation flagged for Plan 03 testHelpNoQueue)
    - menu-taxonomy-per-module ({File, Nonlinear, View, Help} for nlse — replaces 6.1's {File, Mesh, View, Help}, 6.2's {File, Solve, View, Help}, 6.3's {File, Fluid, View, Help}, and 6.4's {File, Thermal, View, Help})
    - separate-top-level-tms-codes (nlse exposes 71;/72;/73;/74; as four separate top-level codes for SUITMS/SUIFMS/SUIVES/TUER — matches 6.1 mail; UNLIKE 6.2 elas / 6.3 flui / 6.4 ther which use combined 70; entry into managmef)
key-files:
  created:
    - .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md
  modified: []
decisions:
  - id: D-6.5-01-01
    summary: "Help-allowlist for nlse is {97;} matching flui — NOT {98;} like ther; explicit per-module variation per 6.3 Plan 03 Auto-fix Rule 1 lesson; verified against td/m/debunlse line 20"
  - id: D-6.5-01-02
    summary: "31 ther-proxy compressed rows (cl_ther/defplan/dirprofi/eigvvloc/eigvvpol/methreso/methvvpr/profdroi/profplan/proj6cub/resoonin DEAD/resothin/resothst/scheonde/sectplan/solsurf3/tempgrad/tracerr2/tracerr3/tracerrt/tracflux/tracgrad/traconde/tractem1/tractem2/tractem3/tractemp/trdepond/valisoth/valzone/valztxy) cite 6.4 LEXICON-AUDIT-ther.md as canonical owner — nlse legitimately defers TRTHER visualization cascade to 6.4 audit since codes 11;/12;/13; share TRTHER dispatch with NTYPDESS variants {4,5,6}"
  - id: D-6.5-01-03
    summary: "coefnlse uses synthetic prefix 2;91;<N>; not 2;<N>; to avoid collision with cl_nlse leaves at 2;2; / 2;3; / 2;8; (cl_nlse uses {2,3,8}; coefnlse uses {1,2,4,6,8,9}; collision on leaf indices 2 and 8 forced the synthetic offset choice for coefnlse)"
  - id: D-6.5-01-04
    summary: "Top-5 toolbar = {2;, 5;, 11;, 19;, 99;} — canonical NLSE workflow ergonomics (data-input gate, IMPLICIT solver, primary modulus visual, mesh inspection borrowed from 6.1 mail, save/quit); 11; primary visual chosen over 15;/16; since abs(U) modulus is the universal NLSE result-visualization entry; 19; mesh inspection preserved as toolbar slot for consistency with 6.1/6.4 mesh-draw.svg reuse pattern"
  - id: D-6.5-01-05
    summary: "5 new nlse-specific SVGs queued for Plan 02 (nlse-object, nlse-input, solve-nlse-implicit, draw-nlse-modulus, draw-nlse-component shared between codes 12;/13;); 1 reuse from prior phases (mesh-draw.svg from 6.1 mail/); user may add solve-nlse-itime.svg for code 6; in a polish-pass if Gross-Pitaevskii branch becomes a documented use case (currently code 6; has icon_source=— since not on toolbar)"
  - id: D-6.5-01-06
    summary: "Menu taxonomy is {File, Nonlinear, View, Help} per ROADMAP Phase 6.5 line 220 — overrides the older 06.0-UI-SPEC table at line 245 which lists `Solve / Calcul` for nlse; conflict documented in audit Menu-taxonomy section; future 06.0-UI-SPEC update can align table to `Nonlinear / Non Linéaire` as polish-pass"
  - id: D-6.5-01-07
    summary: "Frequency 100% domain-review for nlse (zero .nlse fixtures exist on this worktree — testa/{nlsecu,nlsecuvv,nlsesqri,nlsgpe,gpesigc} contain only mesh+initial-guess files, no lexicon scripts); HIGH set = {1;, 2;, 5;, 11;, 99;}; MED set = {6;, 12;, 13;, 14;, 15;, 16;, 19;}; LOW set = remainder + all compressed proxy rows; 9 HIGH total + 11 MED total + 60 LOW total = 80 rows"
metrics:
  duration: 8m08s
  completed: 2026-04-29
  tasks: 1
  files: 1
  commits: 1
---

# Phase 06.5 Plan 01: Nonlinear LEXICON-AUDIT Summary

nlse LIMTCL tree audit (80 rows, 5 toolbar=yes, validator-clean) frozen for Plan 02 QAction wiring + Plan 03 testHelpNoQueue {97;} hand-off — 31 ther-proxy rows compressed citing 6.4 LEXICON-AUDIT-ther.md as canonical owner.

## What Got Built

`.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md`
— the fifth-iteration nlse-slice equivalent of LEXICON-AUDIT-ther.md
from 6.4, mirroring the 9-column schema verbatim. Validator exits 0;
80 rows at the lower edge of bound `[80, 250]`; exactly 5 toolbar=yes;
9 HIGH rows + 11 MED rows + 60 LOW rows; 20 qaction=yes rows.

The 80-row count is much smaller than 6.4 ther's 209 because nlse
legitimately compresses the 31 ther-proxy sub-menus instead of
re-expanding them (the TRTHER visualization cascade reached from
nlse codes 11;/12;/13; is wholly owned by the 6.4 ther audit). The
genuinely NLSE-primary expansion is just `cl_nlse` (3 leaves) +
`coefnlse` (6 leaves) = 9 sub-leaves under code 2;.

## Audit Statistics

- **Total rows:** 80
- **By frequency:**
  - HIGH: 9 rows (`1;`, `2;`, `5;`, `11;`, `99;` top-level + `2;3;` cl_nlse Dirichlet BC + `2;91;1;`/`2;91;2;`/`2;91;6;` coefnlse mandatory material parameters Rho/Alfa/Beta)
  - MED: 11 rows (debunlse codes `6;`, `12;`, `13;`, `14;`, `15;`, `16;`, `19;` top-level + cl_nlse `2;2;`/`2;8;` workflow leaves + coefnlse `2;91;4;`/`2;91;8;` workflow leaves)
  - LOW: 60 rows (8 top-level LOW + 31 ther-proxy compressed + 20 shared-util compressed + 1 zeros compressed under code 20;)
- **By qaction:** yes count == high+med == **20** (Plan 02's `registerNlseActions_stub_` QAction set; smaller than 6.4 ther's 37 because nlse delegates the entire TRTHER visualization cascade to 6.4 via compressed proxy rows)
- **By toolbar:** exactly 5 yes (`2;`, `5;`, `11;`, `19;`, `99;`)

## Final Top-5 Toolbar (auto-frozen 2026-04-29)

1. `2;` — NLSE INPUT DATA / ENTREE des DONNEES de l'Equation Non Lineaire de SCHRODINGER — `nlse-input.svg`
2. `5;` — IMPLICIT NLSE solver / IMPLICIT idU(t)/dt -Alfa Laplacien U(t) +N(t,abs(U))U(t)=F(t,X) — `solve-nlse-implicit.svg`
3. `11;` — DRAW NLSE WAVE MODULE / DESSIN du MODULE de l'ONDE NLSE — `draw-nlse-modulus.svg`
4. `19;` — DRAW PLSVO MESHES / DESSIN du MAILLAGE des PLSVO — `mesh-draw.svg` (REUSE from 6.1 mail/)
5. `99;` — SAVE DATA and QUIT / SAUVEGARDE des donnees FIN TRAITEMENT — `SP_DialogCloseButton`

Rationale: `2;` mandatory data-input gate (every NLSE simulation
prompts DFNLSE → cl_nlse + coefnlse cascade); `5;` canonical first
solver (IMPLICIT — Gross-Pitaevskii i-time `6;` is specialty);
`11;` primary result visual (abs(U(t,X)) modulus is universal NLSE
visualization entry); `19;` mesh inspection (preserves 6.1/6.4
`mesh-draw.svg` reuse pattern); `99;` shared 6.1/6.2/6.3/6.4
close-session convention.

## Nlse-unique SVG Icon Set (Plan 02 ships)

Custom .svg filenames introduced by this audit to be shipped under
`xvue/qt/resources/icons/nlse/`:

- `nlse-object.svg` — OBJECT NAME (code 1;) — abstract wave glyph
- `nlse-input.svg` — NLSE INPUT DATA (code 2;) — wave-equation glyph
- `solve-nlse-implicit.svg` — IMPLICIT NLSE solver (code 5;) — wave + integral sign
- `draw-nlse-modulus.svg` — DRAW abs(U(t,X)) (code 11;) — wave packet glyph
- `draw-nlse-component.svg` — DRAW Re U / Im U (codes 12; AND 13; SHARE)

**Total: 5 new nlse-specific SVGs** (consolidated by sharing
draw-nlse-component.svg between codes 12; and 13;, mirroring 6.4's
solve-wave.svg consolidation between codes 5; and 9;). User may add
`solve-nlse-itime.svg` for code 6; in a polish-pass if
Gross-Pitaevskii branch becomes a documented use case.

## Shared SVG Reuses (no copies needed)

- `mesh-draw.svg` from `xvue/qt/resources/icons/mail/` (6.1) — code `19;`
  (DRAW PLSVO MESHES) — same reuse pattern as 6.2 elas code `10;`,
  6.3 flui code `19;`, and 6.4 ther code `10;`

## Menu-Taxonomy Mapping Ratified

`{File, Nonlinear, View, Help}` per ROADMAP Phase 6.5 line 220
(overrides older 06.0-UI-SPEC table at line 245 which lists
`Solve / Calcul`; conflict documented in audit Menu-taxonomy section):

- **File**: codes `71;` `72;` `73;` `74;` `99;` + shared 6.0 File actions
- **Nonlinear**: codes `1;` `2;` `5;` `6;` + Parameters sub-menu (`20;` `38;` `39;`)
- **View**: codes `11;` `12;` `13;` `14;` `15;` `16;` `19;` + shared 6.0 View actions
- **Help**: code `97;` + shared 6.0 Help/About

(Differs from 6.1 mail's `{File, Mesh, View, Help}`, 6.2 elas's
`{File, Solve, View, Help}`, 6.3 flui's `{File, Fluid, View, Help}`,
and 6.4 ther's `{File, Thermal, View, Help}` — "Nonlinear" replaces
the per-module slot.)

## Help-Allowlist Hand-off (Plan 03)

**`{97;}`** — explicit per-module value for Plan 03's `testHelpNoQueue`
QSet<QString>. **MATCHES flui's `{97;}`** (debuflui line 21 also
uses code 97). **DIFFERS from ther's `{98;}`** (debuther line 22 —
the only sibling that uses 98). Verified against `td/m/debunlse`
line 20 and `td/ma/debunlse` line 20. Documented in the audit's
dedicated `## Help-allowlist` block + Known data quirks + Code 97;
row notes + Summary Statistics + Cross-References.

This addresses the 6.3 Plan 03 Auto-fix Rule 1 lesson up front so
Plan 03 doesn't repeat the testHelpNoQueue mistake (which inherited
a stale `{97;}` from the elas/flui template, and which 6.4 ther had
to flip to `{98;}`, and which 6.5 nlse must flip back to `{97;}`).

## Planning-Time Interface Verification (Step 1 results)

```
$ grep -cE '^[[:space:]]*\(?[[:space:]]*[0-9]+[[:space:]]*:' td/m/debunlse
20
$ grep -cE '^[[:space:]]*\(?[[:space:]]*[0-9]+[[:space:]]*:' td/ma/debunlse
20
$ grep -rEoh "LIMTCL\( *'[^']+'" ther/ util/ prpr/ppnlse.f reso/ 2>/dev/null \
  | sed "s/LIMTCL( *'//;s/'$//" | sort -u | wc -l
53                # 1-drift from planner's 52 (extra is debunlse itself, not compressed)
$ test -f td/m/cl_nlse && test -f td/ma/cl_nlse && echo OK
OK
$ test -f td/m/coefnlse && test -f td/ma/coefnlse && echo OK
OK
$ grep -E '^[[:space:]]*97[[:space:]]*:' td/m/debunlse
 97 : 'DATE de version de Mefisto',
$ grep -E '^[[:space:]]*97[[:space:]]*:' td/ma/debunlse
 97 : 'MEFISTO VERSION DATE',
$ grep -cE '^[[:space:]]*98[[:space:]]*:' td/m/debunlse
0                 # confirmed — nlse has NO code 98
$ find testa/ testf/ -name "*.nlse" 2>/dev/null
                  # empty — confirms 100% domain-review frequency bucketing
```

## Validator Confirmation

```
$ python3 tools/validate_audit_md.py .planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md
WARN: rule 9 (svg exists) failed at row 1: ...icons/nlse/nlse-object.svg
WARN: rule 9 (svg exists) failed at row 2: ...icons/nlse/nlse-input.svg
WARN: rule 9 (svg exists) failed at row 3: ...icons/nlse/solve-nlse-implicit.svg
WARN: rule 9 (svg exists) failed at row 5: ...icons/nlse/draw-nlse-modulus.svg
WARN: rule 9 (svg exists) failed at row 6: ...icons/nlse/draw-nlse-component.svg
WARN: rule 9 (svg exists) failed at row 7: ...icons/nlse/draw-nlse-component.svg
OK -- 80 rows, 5 toolbar=yes, schema valid
```

Exit code: **0**. WARN lines for new nlse SVGs are expected (Plan 02
ships the files). `mesh-draw.svg` (code 19;) resolves cleanly into
`icons/mail/` (no WARN — already shipped by 6.1).

## Hand-off to Plan 02

Consume `.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md`
rows where:

- `qaction=yes` (20 rows) → the `registerNlseActions_stub_` QAction set
- `toolbar=yes` (5 rows) → the toolbar append set
- `icon_source` ends in `.svg` AND filename is not already in
  `xvue/qt/resources/icons/mail/` or `icons/elas/` or `icons/flui/`
  or `icons/ther/` → the `xvue_icons.qrc` append set for
  `xvue/qt/resources/icons/nlse/` (5 new SVGs to ship)

Plan 02 will flip the validator to strict mode (`AUDIT_STRICT_ICONS=1`)
after the SVGs land — same idempotent pattern as 6.3 / 6.4 Plan 02.

## Hand-off to Plan 03

Consume the Help-allowlist note `{97;}` directly to populate
`testHelpNoQueue`'s `QSet<QString>{ QStringLiteral("97;") }`.

This is **NOT** `{98;}` — that's ther's. nlse matches flui's value
of `{97;}`. The 6.3 Plan 03 Auto-fix Rule 1 lesson explicitly warned
per-module Help-allowlist must be drawn from LEXICON-AUDIT, NOT
inherited from previous-module template. nlse's value is `97;` per
`td/m/debunlse` line 20 and `td/ma/debunlse` line 20.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pipe-character in description columns (preventive — would have broken validator if left)**
- **Found during:** Task 1 audit drafting (avoided before validator first ran)
- **Issue:** debunlse code 5; description `IMPLICIT idU(t)/dt -Alfa Laplacien U(t) +N(t,|U|)U(t)=F(t,X)` and code 6; description `i-TIME methode idU(t)/dt -Alfa Laplacian U(t) +N(|U|**2)U(t)=F(t,X)` and codes 11;/15;/16; all contain literal `|U|` pipe-bracketed absolute-value notation; validator's primitive split parser would have interpreted these as column delimiters → row would have 11+ fields instead of 9 (same hazard 6.4 ther hit on eigvvloc/eigvvpol descriptions per 06.4-01-SUMMARY Auto-fixed Issue #1)
- **Fix:** Rewrote pipe-bracketed bars as `abs(...)` (mathematical absolute-value notation) verbatim per the 6.4 fix lesson; applied uniformly to top-level codes 5, 6, 11, 15, 16 + coefnlse leaf 2;91;6; description (Beta `(ur**2+ui**2) ur OU ui` was already pipe-free)
- **Files modified:** `.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md`
- **Commit:** Folded into 11b7298 (initial audit landing — fix applied at draft time, not after validator failure)

**2. [Rule 3 - Blocking] Worktree branch base mismatch (resolved before any audit work)**
- **Found during:** Initial worktree_branch_check
- **Issue:** Worktree was on commit `ac282f8` (a disjoint pre-init lineage with no `.planning/` directory) while the orchestrator expected base `53df19b` (main tip with the planning files). Without the .planning/ directory the plan files were unreachable
- **Fix:** `git reset --hard 53df19bc68ff01d801bfb417eb3441e2079e89fa` per the worktree_branch_check protocol; this discarded the unrelated worktree-agent-a8804be7 working tree but preserved access to the entire .planning/ infrastructure
- **Files modified:** None (working-tree state change only)
- **Commit:** N/A (pre-task setup)

**3. [Rule 2 - Auto-add missing critical functionality] Pre-existing 06.0-UI-SPEC vs ROADMAP conflict on nlse menu title**
- **Found during:** Task 1 menu-taxonomy section drafting
- **Issue:** Plan frontmatter and ROADMAP Phase 6.5 line 220 specify `{File, Nonlinear, View, Help}` taxonomy with module-title `Nonlinear / Non Linéaire`. But `06.0-UI-SPEC` table at line 245 lists nlse module-title as `Solve / Calcul` (a stale entry pre-dating the 6.5 plan freeze). Failure to flag this would either break the audit's downstream contract OR silently inherit the wrong title in Plan 02
- **Fix:** Audit's Menu-taxonomy section explicitly documents the conflict, names ROADMAP as authoritative, and recommends a future 06.0-UI-SPEC alignment as a polish-pass edit (so Plan 02 implementers see the discrepancy and don't accidentally use the stale UI-SPEC value). Decision logged as D-6.5-01-06.
- **Files modified:** `.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` (Menu-taxonomy section)
- **Commit:** Folded into 11b7298

### Out-of-scope discoveries

- **06.0-UI-SPEC line 245 stale entry** — the per-module title for nlse is listed as `Solve / Calcul` but ROADMAP Phase 6.5 line 220 says `Nonlinear / Non Linéaire`. This is a documentation drift that pre-dates this plan. Logged as a polish-pass item in the audit's Menu-taxonomy section; NOT fixed in this plan because the fix lives outside the LEXICON-AUDIT scope (modifying 06.0-UI-SPEC is a 06.0 deferred-items concern, not a 06.5 deliverable).

## Self-Check: PASSED

- File exists: `.planning/phases/06.5-nonlinear-nlse-menu-wiring/LEXICON-AUDIT-nlse.md` — FOUND
- Commit `11b7298` (Task 1 — audit file) — FOUND in `git log --oneline`
- Validator exits 0 against the audit — VERIFIED
- 80 rows in [80, 250] — VERIFIED (at lower edge by design — see Scope explanation)
- 5 toolbar=yes — VERIFIED
- 9 HIGH rows >= 5 — VERIFIED
- {97;} appears in audit (Help-allowlist match) — VERIFIED (1 occurrence of `QStringLiteral("97;")`)
- {98;} does NOT appear (rules out ther-template inheritance) — VERIFIED (0 occurrences of `QStringLiteral("98;")`)
- Help-allowlist heading appears exactly once — VERIFIED
- Top-level menu taxonomy `{File, Nonlinear, View, Help}` documented — VERIFIED (2 occurrences: Menu taxonomy section + Cross-References)
- All 20 debunlse codes present (1, 2, 5, 6, 11, 12, 13, 14, 15, 16, 19, 20, 38, 39, 71, 72, 73, 74, 97, 99) — VERIFIED via inspection of audit table
- Rule 8 invariants: no `high|med` row has `qaction=no`; no `low` row has `qaction=yes` — VERIFIED (0 occurrences each)
- No Fortran / C++ / CMake / build-script changes (docs-only plan) — VERIFIED
- No new files under `xvue/qt/resources/icons/nlse/` (Plan 02 ships them) — VERIFIED
