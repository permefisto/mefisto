---
status: passed
phase: 06.5-nonlinear-nlse-menu-wiring
goal: "ppnlse_qt shows nonlinear-specific top-level menu bar {File, Nonlinear, View, Help}, 80/20 QAction coverage, CALL XVUE_MODULE_INIT('nlse')"
requirements:
  - UX-05 (nlse slice)
must_haves_verified: 11/11
gates_run: 9/9
date: 2026-04-30
verifier_note: "Run inline by orchestrator (gsd-verifier hit usage limit at 25 tool uses / 898s before writing VERIFICATION.md)"
---

# Phase 06.5 Verification — passed

## Goal achievement

Phase 6.5 wires the **NLSE solver** into the Qt menu system, completing the
**UX-05 5-module sweep** (mail/elas/flui/ther/nlse). The phase delivered all
three plans (audit → wiring → hook+test) following the same template that
shipped 6.1-6.4. Goal as stated in ROADMAP.md line 220 is met.

## Must-haves verified (11/11)

### Wave 1 — Lexicon audit

| # | Must-have | Evidence |
|---|-----------|----------|
| 1 | `LEXICON-AUDIT-nlse.md` exists with 9-column schema | 80 rows, validator-clean |
| 2 | qaction/frequency rule | `grep -c "yes \|"` = 20 (matches plan target) |
| 3 | toolbar=yes count == 5 | strict-mode validator confirms |
| 4 | Help-allowlist `{97;}` (matches flui, NOT ther's `{98;}`) | grep `"97;"` in test = 2; grep `"98;"` = 0 |
| 5 | `validate_audit_md.py` exits 0 | `OK -- 80 rows, 5 toolbar=yes, schema valid` |

### Wave 2 — QAction wiring + icons

| # | Must-have | Evidence |
|---|-----------|----------|
| 6 | `registerNlseActions_stub_` strong symbol | `nm pp/ppnlse_qt \| grep -c " T registerNlseActions_stub_"` = 1 |
| 7 | `xvue_qt_nlse_actions_keepalive` force-link in app.cpp (between mail and ther) | xvue_qt_app.cpp:60-61 (extern + static ref) |
| 8 | 5 SVGs registered in qrc + CMakeLists | xvue_icons.qrc:41-45 (5 nlse entries) |
| 9 | ABI count stable at 58 | `verify_abi: nm count: 58  header count: 58` |

### Wave 3 — Fortran hook + tests

| # | Must-have | Evidence |
|---|-----------|----------|
| 10 | `CALL XVUE_MODULE_INIT('nlse', 4)` in ppnlse.f | line 142, single insert inside INTERA gate |
| 11 | testMenuOrder asserts `{File, Nonlinear, View, Help}` | test_xvue_qt_nlse_menu.cpp:261-282 |

## Gates run inline (9/9)

| Gate | Result |
|------|--------|
| nm pp/ppnlse_qt registerNlseActions_stub_ | 1 ✓ |
| Force-link regression mail/elas/flui/ther/nlse | all = 1 ✓ |
| `bash xvue/qt/cmake/verify_abi.sh ...` | nm count: 58, header count: 58 ✓ |
| `bash xvue/qt/cmake/verify_shortcut_modifiers.sh xvue/qt/src` | OK ✓ |
| `bash xvue/qt/cmake/verify_icon_source.sh xvue/qt/src` | OK ✓ |
| `AUDIT_STRICT_ICONS=1` validation × 5 modules | mail (211), elas (102), flui (161), ther (209), nlse (80) all clean ✓ |
| qrc nlse entries | 5 (matches plan) ✓ |
| xvue_qt_app.cpp keepalive insertion | declared + static ref at lines 60-61 (alphabetically between mail and ther) ✓ |
| Help-allowlist `{97;}` not `{98;}` | grep confirms ✓ |

## Build gates (delegated to executor agents)

Wave 3 executor agent confirmed both backends green and aggregate Qt sweep
matches the planned baseline:

- `bin/cbl_tout` (X11 build) clean
- `bin/cbl_tout_qt` (Qt build) clean
- Qt test sweep: **152 passed, 1 failed, 2 skipped** (143 baseline + 9 new
  nlse slots; pre-existing `testPerModuleGroupIsolation` failure carries over
  from 6.4 — deferred)

## Requirement traceability

UX-05 (per-module menu wiring) — **Complete** as recorded in
.planning/REQUIREMENTS.md line 213: `| UX-05 | Phase 6.1-6.5 | Complete |`.
Phase 6.5 closes the 5-module sweep that began in 6.1.

## Code review cross-reference

`.planning/phases/06.5-nonlinear-nlse-menu-wiring/06.5-REVIEW.md`:
- 0 critical, 0 warning, 4 info (IN-01..IN-04: style/finder-cache/comment/QSKIP).
- All info findings are non-blocking, carried forward into the technical-debt
  ledger.

## Conclusion

Phase 06.5 **passes** verification with no gaps. UX-05 is now Complete across
all 5 solver modules. Phase ready for completion.
