---
phase: 06.2-elasticity-elas-menu-wiring
fixed_at: 2026-04-27T12:40:00Z
review_path: .planning/phases/06.2-elasticity-elas-menu-wiring/06.2-REVIEW-GAPS.md
iteration: 1
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 06.2 Gap-Closure: Code Review Fix Report

**Fixed at:** 2026-04-27T12:40:00Z
**Source review:** .planning/phases/06.2-elasticity-elas-menu-wiring/06.2-REVIEW-GAPS.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5
- Fixed: 5
- Skipped: 0

## Fixed Issues

### WR-01: `clearCacheForTesting()` is not compile-gated — ships in production binary

**Files modified:** `xvue/qt/src/xvue_qt_menu_file_parser.h`, `xvue/qt/src/xvue_qt_menu_file_parser.cpp`, `xvue/qt/tests/CMakeLists.txt`, `xvue/qt/CMakeLists.txt`
**Commit:** 87fee2e
**Applied fix:** Wrapped both the declaration (in the header) and the definition (in the `.cpp`) in `#ifdef XVUE_QT_TESTING ... #endif`. Added `target_compile_definitions(xvue_qt_elas_menu_tests PRIVATE XVUE_QT_TESTING)` and `target_compile_definitions(xvue_qt_mail_menu_tests PRIVATE XVUE_QT_TESTING)` in `tests/CMakeLists.txt`. Also added `target_compile_definitions(xvueqt PRIVATE XVUE_QT_TESTING)` in the parent `CMakeLists.txt` inside the `XVUE_QT_BUILD_TESTS` gate (needed so the library itself exports the guarded symbols when tests are built — discovered during WR-02 link failure; folded into IN-02 commit `20ceba4`).

---

### WR-02: Parser cache and `xvueIsEnglish()` static are incoherent in tests

**Files modified:** `xvue/qt/src/xvue_qt_i18n.h`, `xvue/qt/src/xvue_qt_i18n.cpp`, `xvue/qt/tests/test_xvue_qt_elas_menu.cpp`
**Commit:** 0dd08c7
**Applied fix:** Added `xvueClearLanguageCacheForTesting()` declaration to `xvue_qt_i18n.h` under `#ifdef XVUE_QT_TESTING`. Refactored `xvueIsEnglish()` in the `.cpp`: extracted a `probe_english()` helper, added a file-scope `static std::optional<bool> s_englishCache` (under `XVUE_QT_TESTING`), and branched inside `xvueIsEnglish()` — production builds use the original `static const bool` path; test builds use the resettable optional. Added `xvueClearLanguageCacheForTesting()` definition that resets the optional to `std::nullopt`. Updated `test_xvue_qt_elas_menu.cpp` to include `xvue_qt_i18n.h` and call `xvueClearLanguageCacheForTesting()` alongside `clearCacheForTesting()` at both the pre-check and post-check sites in `testBilingualLabelsEnglish`.

---

### WR-03: Silent no-op in `ensureTopLevelMenu` when `insertBefore` anchor is absent

**Files modified:** `xvue/qt/src/xvue_qt_elas_actions.cpp`, `xvue/qt/src/xvue_qt_mail_actions.cpp`
**Commit:** c815701
**Applied fix:** Added `Q_ASSERT_X(viewMenuForAnchor != nullptr, "registerElasActions_stub_", "...")` immediately after the `findChild<QMenu*>("View")` lookup in `xvue_qt_elas_actions.cpp`, and an equivalent `Q_ASSERT_X(..., "registerMailActions_stub_", "...")` in `xvue_qt_mail_actions.cpp`. Both asserts name the containing function and include the expected menu order in the message so the failure is self-documenting in debug builds.

---

### IN-01: `addToolbarByLexicon` searches only two levels — silently skips three-level-deep actions

**Files modified:** `xvue/qt/src/xvue_qt_elas_actions.cpp`, `xvue/qt/src/xvue_qt_mail_actions.cpp`
**Commit:** 827808a
**Applied fix:** Updated the function comment in both files to say "top level + one submenu level" (instead of the ambiguous "one level of its submenus") and appended a `NOTE:` block documenting the two-level depth limit, the count of currently requested lexicons that fit within it, and a pointer to `actionByLexicon` in the test files as the depth-unlimited recursive model for future callers.

---

### IN-02: `static bool registered` guard process-scope inconsistency between elas and mail

**Files modified:** `xvue/qt/src/xvue_qt_mail_actions.cpp`, `xvue/qt/src/xvue_qt_elas_actions.cpp`, `xvue/qt/CMakeLists.txt`
**Commit:** 20ceba4
**Applied fix:** Added the same one-shot `static bool registered` guard to `registerMailActions_stub_` (aligning it with elas). Both guards now carry an `IN-02` comment explaining the single-window-per-process assumption and the recommended per-window-flag migration path for Phase 6.3+. The elas guard comment was also enriched to reference mail and vice versa. The `CMakeLists.txt` change (`target_compile_definitions(xvueqt PRIVATE XVUE_QT_TESTING)` inside `XVUE_QT_BUILD_TESTS`) was folded into this commit because it was required to resolve the link failure discovered while building WR-01/WR-02. Build verified: `bin/cbl_tout_qt` exit 0, ABI = 58, `nm pp/ppxvtest0_qt | grep -c ' T registerElasActions_stub_'` = 1, same for mail.

---

_Fixed: 2026-04-27T12:40:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
