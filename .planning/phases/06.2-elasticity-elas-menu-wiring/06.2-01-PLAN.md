---
phase: 06.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md
autonomous: false
requirements:
  - UX-05
threat_refs:
  - T-6.2-AUDIT-01
  - T-6.2-AUDIT-02

must_haves:
  truths:
    - "LEXICON-AUDIT-elas.md exists under .planning/phases/06.2-*/ with the same 9-column schema frozen in 06.1 (lexicon_path / description_fr / description_en / frequency / qaction / toolbar / icon_source / shortcut / notes)"
    - "Audit enumerates every LIMTCL leaf reachable from `td/m/debuelas` (17 top-level codes) via a full recursive tree walk over elas/ + util/ + prpr/ppelas.f call sites — expected row count 80-250, validated by tools/validate_audit_md.py"
    - "Every row with frequency in {high, med} has qaction=yes; every row with frequency=low has qaction=no"
    - "Exactly 5 rows have toolbar=yes — the top-5 most-used leaves from testa/nafems_le1/*.elas + other elas testa evidence"
    - "tools/validate_audit_md.py exits 0 against the final elas audit markdown (reused verbatim from 6.1 Plan 01 — already generalized)"
    - "User has reviewed and approved the frequency bucketing AND the top-5 toolbar set AND the menu taxonomy mapping ({File, Solve, View, Help}) before the audit is frozen"
  artifacts:
    - path: ".planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md"
      provides: "Per-module elas lexicon audit (UX-05 elas-slice Success Criterion #1 deliverable)"
      contains: "9-column GitHub-markdown table identical schema to LEXICON-AUDIT-mail.md, rooted at debuelas (not debut)"
  key_links:
    - from: ".planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md"
      to: "tools/validate_audit_md.py (6.1 Plan 01 deliverable)"
      via: "schema rules enforced at CI-style lint time — same 9 rules apply"
      pattern: "python3 tools/validate_audit_md.py .planning/phases/06.2-*/LEXICON-AUDIT-elas.md"
    - from: "LEXICON-AUDIT-elas.md rows with qaction=yes"
      to: "Plan 02 registerElasActions_stub_ QAction set"
      via: "frequency-bucketed ~20-35 rows are the QAction wire-up list"
      pattern: "qaction=yes AND frequency in (high, med)"
    - from: "LEXICON-AUDIT-elas.md rows with toolbar=yes"
      to: "Plan 02 toolbar append set"
      via: "top-5 leaves from the frequency-scored audit"
      pattern: "toolbar=yes (count == 5)"
---

<objective>
Produce `.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` — the
full recursive catalogue of every interactive command reachable from the elasticity
solver's `td/m/debuelas` LIMTCL tree (17 top-level codes), scored with high/med/low
frequency buckets from `testa/*/*.elas` grep evidence, then reviewed and frozen at the
user checkpoint before the QAction set is consumed by Plan 02.

Reuse `tools/validate_audit_md.py` verbatim — the 6.1 validator is already generalized
for LEXICON-AUDIT-<mod>.md. No tooling changes needed in this plan.

Purpose: UX-05 elas-slice Success Criterion #1. Every decision downstream (which
QActions exist, which icons are custom-SVG vs Qt built-in, which 5 leaves go on the
toolbar) flows from this audit. The validator closes the loop — it is the only
automated verification available for a markdown deliverable.

Output: One markdown file in the phase directory, plus a user-approved top-5 toolbar
set and the {File, Solve, View, Help} menu taxonomy ratified against the audit data
(differs from 6.1's {File, Mesh, View, Help}).
</objective>

<execution_context>
@/home/mefisto/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/mefisto/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/HANDOFF.json
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md
@.planning/phases/06.1-mesher-mail-menu-wiring/06.1-CONTEXT.md
@.planning/phases/06.1-mesher-mail-menu-wiring/06.1-RESEARCH.md
@.planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md
@.planning/phases/06.1-mesher-mail-menu-wiring/06.1-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key LIMTCL data for the auditor — elas-specific (extracted during planning) -->

### elas top-level menu file (VERIFIED paths)
- FR (runtime active): `$MEFISTO/td/m/debuelas`
- FR (backup):         `$MEFISTO/td/mf/debuelas`
- EN (source for EN install): `$MEFISTO/td/ma/debuelas`
- Parser: `util/limtcl.f:54` (`KNOM = HOMDIR // '/td/m/' // KNM`) — same path as mail

### debuelas top-level (17 codes — VERIFIED against td/mf/debuelas)

| Code | FR label | EN label |
|------|----------|----------|
| 1  | NOM de l'OBJET a traiter | OBJECT NAME to treat |
| 2  | ENTREE DONNEES PROBLEME ELASTICITE | ELASTICITY INPUT DATA |
| 3  | ELASTICITE STATIONNAIRE | STEADY ELASTICITY solver |
| 4  | ELASTICITE INSTATIONNAIRE d2/dt2 | UNSTEADY ELASTICITY solver d2/dt2 |
| 6  | CALCUL des MODES PROPRES | EIGENMODES solver |
| 7  | DESSIN des MODES PROPRES | DRAWING of EIGENMODES |
| 8  | DESSIN des Deformees Contraintes | DRAWING of DISPLACEMENT & STRESS |
| 10 | TRACE du MAILLAGE des PLSVO | DRAWING of PLSVO meshes |
| 20 | PRECISION pour INVERSER A x = b | PRECISION to INVERSE A x = b |
| 38 | LARGEUR HAUTEUR PIXELS de la FENETRE | WINDOW PIXELS WIDTH & HEIGHT |
| 39 | COULEUR du FOND de la FENETRE | BACKGROUND COLOR |
| 71 | SUIVI TMS AFFICHAGE MODIFICATION | TOOLS on TMS |
| 72 | SUIVI FICHIERS de la MS | TOOLS on FILES of the MS |
| 73 | GESTION unites lecture affichage | TOOLS on I/O units |
| 74 | TUER des TMS PLSVO | DELETE TMS of PLSVO |
| 97 | Nom de la version de Mefisto | MEFISTO VERSION NAME |
| 99 | SAUVEGARDE donnees FIN TRAITEMENT | SAVE DATA and QUIT |

### Elas-reachable sub-menus (34 total — VERIFIED via grep 2026-04-22)

```
$ grep -rEoh "LIMTCL\( *'[^']+'" elas/ util/ prpr/ppelas.f reso/ | \
  sed "s/LIMTCL( *'//;s/'$//" | sort -u | wc -l
34
```

Sub-menu set (full list extracted at planning time):

```
affiche, cl_elas, contdepl, couleur0, couleurs, defplan, entrsort,
fichier, lecteur, managfen, managmef, methreso, mode_es, opt_lign,
opt_surf, resoelin, resoelst, schedyna, sectplan, sectvopl,
suivi_ms, suivitms, therelas, tracaxes, traccont, tracdepl, tuer,
typ_objt, typtrait, valzone, vuesplan, zeros, debuelas
```

Sub-menus UNIQUE to elas (not in 6.1 mail audit — so new rows needed):
`cl_elas, contdepl, defplan, methreso, resoelin, resoelst, schedyna,
sectplan, therelas, traccont, tracdepl, valzone`

Sub-menus SHARED with mail (same td/m/ files — can refer to 6.1 row content or
re-list; follow 6.1's de-duplication rule):
`affiche, couleur0, couleurs, entrsort, fichier, lecteur, managfen,
managmef, mode_es, opt_lign, opt_surf, sectvopl, suivi_ms, suivitms,
tracaxes, tuer, typ_objt, typtrait, vuesplan, zeros`

### Frequency evidence source

`testa/*/` contains elas projects. The `.elas` files (batch scripts) are the
primary source of typed-command frequency for elas:

```bash
find testa/ -name '*.elas' | head
# testa/nafems_le1/nafems_le1.elas is the canonical elas baseline
```

Run at audit time:
```bash
grep -hE "^ *[0-9]+; *\{" testa/*/*.elas 2>/dev/null \
  | grep -oE "^ *[0-9]+;" | sort | uniq -c | sort -rn > /tmp/elas_freq.txt
```

Also scan `.mesh` files that chain into elas (less direct but informative):
```bash
grep -hE "^ *[0-9]+; *\{" testa/*/*.elas testa/*/*.batch 2>/dev/null \
  | grep -oE "^ *[0-9]+;" | sort | uniq -c | sort -rn
```

### Validator (reused verbatim from 6.1)

`tools/validate_audit_md.py` — 140 lines, stdlib Python 3. Rules:
1. 9-col header schema exact match
2. Row count in [80, 250]
3. lexicon_path regex `^\d+;(?:\d+;)*$`
4. frequency enum {high, med, low, —}
5. qaction enum {yes, no}
6. toolbar enum {yes, no}
7. exactly 5 toolbar=yes rows
8. qaction=yes implies frequency in {high, med}
9. icon_source ending .svg must have matching file under xvue/qt/resources/icons/mail/
   — **NOTE:** the validator's `ICONS_DIR` constant is hard-wired to the
   `mail/` subdirectory. Plan 01 must either (a) run it under
   `AUDIT_STRICT_ICONS=0` (default WARN-only mode — safe for 6.2 Plan 01
   since Plan 02 ships the icons), or (b) generalize the constant before
   Plan 02 runs under strict mode. **Plan 01 chooses (a)** — Plan 02
   owns the generalization.

### Top-level menu taxonomy (DIFFERS from 6.1)

6.1 (mail) landed `{File, Mesh, View, Help}` — Edit dropped (D-01 in 6.1
CONTEXT).

6.2 (elas) uses `{File, Solve, View, Help}` per ROADMAP.md Phase 6.2 goal
line 188 — "Solve" replaces "Mesh". Per 06.0-UI-SPEC.md §Per-module
menu-bar contract, the elas module-specific menu title is
`Solve / Calcul` (FR / EN).

Menu-content mapping PROPOSED (user ratifies at Task 2 checkpoint):

- **File menu** extensions (shared 6.0 File + elas additions):
  - `72;` SUIVI FICHIERS de la MS    (File > MS File Tools)
  - `73;` GESTION unites lecture     (File > I/O Units)
  - `74;` TUER des TMS PLSVO         (File > Delete TMS)
  - `99;` SAVE DATA and QUIT          (already bound by 6.0 shared Quit
    action — Plan 02 redirects actQuit_ through queueLexicon("99;"))

- **Solve menu** (NEW top-level, replaces Mesh in taxonomy):
  - `1;`  OBJECT NAME to treat
  - `2;`  ELASTICITY INPUT DATA       (expands to cl_elas submenu)
  - `3;`  STEADY ELASTICITY solver    (expands to resoelst/methreso)
  - `4;`  UNSTEADY ELASTICITY solver  (expands to schedyna)
  - `6;`  EIGENMODES solver           (expands to resoelin?)
  - `Parameters` submenu:
    - `20;` PRECISION to INVERSE A x = b
    - `38;` WINDOW PIXELS WIDTH & HEIGHT
    - `39;` BACKGROUND COLOR

- **View menu** extensions (shared 6.0 View + elas additions):
  - `7;`  DRAWING of EIGENMODES       (View > Draw Modes)
  - `8;`  DRAWING of DISPLACEMENT & STRESS  (View > Draw Results — primary visual)
  - `10;` DRAWING of PLSVO meshes     (View > Draw Mesh)
  - `71;` TOOLS on TMS                (View > TMS tools)

- **Help menu** extensions (shared 6.0 Help + elas additions):
  - `97;` MEFISTO VERSION NAME         (Help > Version — or leave as shared About)

### Custom SVG icon set (proposed — 6-8 icons)

Following 6.1's pattern (icons under `xvue/qt/resources/icons/<mod>/`),
6.2 proposes:

- `solve-static.svg`       — Steady-elasticity solver (code 3;)
- `solve-dynamic.svg`      — Unsteady solver d2/dt2 (code 4;)
- `solve-eigen.svg`        — Eigenmodes solver (code 6;)
- `draw-modes.svg`         — Draw eigenmodes (code 7;)
- `draw-stress.svg`        — Draw displacement+stress (code 8;)
- `elas-object.svg`        — Object name (code 1;)
- `elas-input.svg`         — Elasticity input data (code 2;)
- `mesh-draw.svg`          — REUSE from 6.1 mail set for code 10; (DRAW PLSVO meshes)

Planner's Discretion: reusing `mesh-draw.svg` (owned by 6.1) from the
`mail/` directory is allowed — the qrc prefix `/xvue/qt/icons` and file
path `icons/mail/mesh-draw.svg` resolve regardless of module context.
Avoid copying it to `icons/elas/` — share instead.

### Proposed top-5 toolbar (user ratifies at Task 2)

Candidate set based on elas workflow ergonomics + debuelas frequency:
- `3;`  STEADY ELASTICITY solver         (primary solve action)
- `8;`  DRAWING of DISPLACEMENT & STRESS (primary result visual)
- `10;` DRAWING of PLSVO meshes          (mesh inspection)
- `2;`  ELASTICITY INPUT DATA            (setup/edit inputs)
- `99;` SAVE DATA and QUIT                (shared convention — close session)

User may swap `2;` for `1;` (Object name) or `7;` (Draw modes) depending
on typical workflow sequencing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full recursive LIMTCL tree walk + draft elas audit markdown</name>
  <files>.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md</files>
  <read_first>
    - .planning/phases/06.1-mesher-mail-menu-wiring/LEXICON-AUDIT-mail.md (the frozen 6.1 template — COPY the structure: same top-matter sections, same Legend, same 9-column table header, same Summary Statistics block at the bottom)
    - .planning/phases/06.1-mesher-mail-menu-wiring/06.1-01-SUMMARY.md (understand the 6.1 de-duplication rule: mail-specific fully expanded; shared util listed once with notes cell citing reach)
    - .planning/phases/06.1-mesher-mail-menu-wiring/06.1-RESEARCH.md §Pattern 2 (LIMTCL grammar), §Example 5 (sub-menu table shape), §Example 6 (frequency-bucketing rubric), §Pitfall 5 (modifm2d typo — does NOT apply to elas sub-menus, but same log-and-fallback discipline)
    - .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md §Per-Module Conformance Contract (per-module menu-bar contract: `{File, <Module>, View, Help}` left-to-right; elas <Module> title is `Solve/Calcul`)
    - .planning/ROADMAP.md §"Phase 6.2" (goal line 188 — `{File, Solve, View, Help}` — confirm taxonomy)
    - td/m/debuelas, td/ma/debuelas, td/mf/debuelas (top-level 17-code menu — FR active, EN source, FR backup)
    - td/m/cl_elas, td/mf/cl_elas, td/ma/cl_elas (elas-unique sub-menu — the "ELASTICITY CONDITIONS LIMITES" tree)
    - td/m/resoelin, td/m/resoelst, td/m/schedyna, td/m/methreso (solver sub-menus)
    - td/m/therelas, td/m/traccont, td/m/tracdepl, td/m/contdepl, td/m/sectplan, td/m/defplan, td/m/valzone (other elas-unique sub-menus)
    - util/limtcl.f (runtime grammar reference — same as 6.1 — confirms td/m/ is the runtime path; td/mf/ is FR backup; td/ma/ is EN source)
    - util/langue.f (bilingual flag — confirms the install-time FR/EN swap mechanism)
    - testa/nafems_le1/ and any other testa/*/ elas fixtures — source for typed-command frequency evidence (`.elas` files)
    - CLAUDE.md (project norms reminder — Fortran fixed-form — not directly touched by this plan, but docs must respect project conventions)
  </read_first>
  <action>
Produce `.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` by
walking the LIMTCL tree reachable from `td/m/debuelas` via `elas/`, `util/`, `prpr/ppelas.f`,
and `reso/` call sites, scoring frequency from `testa/*/*.elas` grep evidence, and
filling the 9-column schema FROZEN in 6.1 Plan 01.

### Step 1 — Re-verify the reachable sub-menu set (drift check)

```bash
cd /home/mefisto/git/mefisto
grep -rEoh "LIMTCL\( *'[^']+'" elas/ util/ prpr/ppelas.f reso/ 2>/dev/null \
  | sed "s/LIMTCL( *'//;s/'$//" | sort -u > /tmp/elas_limtcl_set.txt
wc -l /tmp/elas_limtcl_set.txt   # expect ~34 lines
```

If the count diverges from 34 (the baseline captured during planning) by
more than 2, investigate the delta before proceeding — append a note in
the audit scope section naming the new/removed sub-menus.

### Step 2 — Collect frequency evidence

Elas uses `.elas` batch files as the canonical typed-lexicon log:

```bash
cd /home/mefisto/git/mefisto
grep -hE "^ *[0-9]+; *\{" testa/*/*.elas 2>/dev/null \
  | grep -oE "^ *[0-9]+;" | sort | uniq -c | sort -rn > /tmp/elas_freq.txt
cat /tmp/elas_freq.txt
```

Also scan any `.batch` or composite batch scripts that chain elas commands:

```bash
grep -hE "^ *[0-9]+; *\{" testa/*/*.batch 2>/dev/null \
  | grep -oE "^ *[0-9]+;" | sort | uniq -c | sort -rn
```

Bucketing rubric (from 6.1 RESEARCH §Example 6, adapted to the smaller
elas corpus):
- HIGH: count >= 5 across testa/ .elas files (target: 6-10 codes)
- MED:  count 2-4 (target: 6-10 codes)
- LOW:  count 0-1 or zero-count leaves from sub-menus (target: ~150-200)

If elas testa corpus is sparse (fewer than 5 HIGH candidates by frequency
alone), supplement by reviewing the `debuelas` codes most commonly expected
in an elasticity workflow: `3;` (Static solve), `8;` (Stress/disp draw),
`10;` (Mesh draw), `2;` (Input data), `1;` (Object name), `99;` (Save-quit)
— these are a priori HIGH based on domain knowledge and the 6.2 UI-SPEC
Solve-menu priorities. Document reasoning in the audit's scope section.

### Step 3 — Apply the tree-walk row strategy (reuse 6.1 rule)

De-duplicate per the 6.1 resolved recommendation:

- **Elas-specific sub-menus fully expand** — every leaf gets its own row
  with the full parent-prefixed `lexicon_path`. Applies to: `cl_elas`,
  `contdepl`, `defplan`, `methreso`, `resoelin`, `resoelst`, `schedyna`,
  `sectplan`, `therelas`, `traccont`, `tracdepl`, `valzone` (12 elas-unique
  sub-menus per the interfaces list).

- **Shared util sub-menus listed ONCE** with a `notes` cell pointing at
  the 6.1 audit for the leaf-level enumeration, using compression form
  `<parent>;N;1;` (a representative valid numeric path). Applies to:
  `affiche`, `couleur0`, `couleurs`, `entrsort`, `fichier`, `lecteur`,
  `managfen`, `managmef`, `mode_es`, `opt_lign`, `opt_surf`, `sectvopl`,
  `suivi_ms`, `suivitms`, `tracaxes`, `tuer`, `typ_objt`, `typtrait`,
  `vuesplan`, `zeros` (20 shared sub-menus).

- Leaf-level `lexicon_path` format: single-line semicolon-separated, no
  spaces (e.g. `3;2;1;` not `3 ; 2 ; 1 ;`). SAME as 6.1 — validator
  regex `^\d+;(?:\d+;)*$` is unchanged.

Target row count: 80-250 (same validator bound as 6.1). Realistic estimate
given 12 elas-unique expanded + 20 util-compressed: expect ~80-150 rows,
lower end of the band because elas has fewer sub-menus than mail.

### Step 4 — Write the markdown (mirror 6.1 structure verbatim)

Top matter (copy from 6.1 LEXICON-AUDIT-mail.md, substitute values):

```markdown
# LEXICON-AUDIT-elas.md

**Phase:** 06.2-elasticity-elas-menu-wiring
**Generated:** <YYYY-MM-DD>
**Status:** DRAFT (pending user review — Task 2 checkpoint)
**Requirement:** UX-05 (elas slice) Success Criterion #1

## Scope

Full recursive LIMTCL tree walk from `td/m/debuelas` via elas/ + util/ +
reso/ + prpr/ppelas.f call-sites. 34 sub-menus (re-verified
<YYYY-MM-DD>). De-duplication rule from 6.1 Plan 01:

- **Elas-specific sub-menus fully expanded** — cl_elas, contdepl,
  defplan, methreso, resoelin, resoelst, schedyna, sectplan, therelas,
  traccont, tracdepl, valzone (12 sub-menus, full leaf expansion).
- **Shared util sub-menus listed ONCE** with notes cell citing the 6.1
  mail audit for leaf-level detail — affiche, couleur0, couleurs,
  entrsort, fichier, lecteur, managfen, managmef, mode_es, opt_lign,
  opt_surf, sectvopl, suivi_ms, suivitms, tracaxes, tuer, typ_objt,
  typtrait, vuesplan, zeros (20 sub-menus, compressed to 1 row each).

**Total row count: <N>** — within the validator bound `[80, 250]` (same
as 6.1 Plan 01).

Frequency bucketing from `testa/*/*.elas` grep evidence (+ domain review
for sparse corpora). Per CONTEXT carryover from 6.1: rows with
`qaction=yes` have `frequency` in {high, med}; exactly 5 rows have
`toolbar=yes`.

Schema: 9 columns, enforced by `tools/validate_audit_md.py` (shipped in
6.1 Plan 01 — reused verbatim).

## Known data quirks

- No typo equivalents to the mail `modifm2d 80;` bug have been found
  in the elas sub-menus at audit time. If any surface during the walk,
  apply the 6.1 Pitfall 5 log-and-fallback discipline (record as a `notes`
  cell on the affected row, keep label fallback to `"N;"`).

## Legend

(Copy verbatim from 6.1 LEXICON-AUDIT-mail.md.)
```

Then a single 9-column table with the exact header row from 6.1:

```
| lexicon_path | description_fr | description_en | frequency | qaction | toolbar | icon_source | shortcut | notes |
|--------------|----------------|----------------|-----------|---------|---------|-------------|----------|-------|
```

### Step 5 — Populate the 17 debuelas top-level rows first

Fill every debuelas code (1, 2, 3, 4, 6, 7, 8, 10, 20, 38, 39, 71, 72,
73, 74, 97, 99) from the td/mf/debuelas and td/ma/debuelas files
(verbatim FR/EN labels), then score each via Step 2's frequency output +
domain review.

For the `icon_source` column initial values (elas-specific suggestions):

- `1;`  OBJECT NAME         → `elas-object.svg` (new, Plan 02 ships)
- `2;`  ELASTICITY INPUT    → `elas-input.svg`  (new, Plan 02 ships)
- `3;`  STEADY SOLVER       → `solve-static.svg` (new, Plan 02 ships)
- `4;`  UNSTEADY SOLVER     → `solve-dynamic.svg` (new, Plan 02 ships)
- `6;`  EIGENMODES SOLVER   → `solve-eigen.svg`  (new, Plan 02 ships)
- `7;`  DRAW MODES          → `draw-modes.svg`   (new, Plan 02 ships)
- `8;`  DRAW DISP+STRESS    → `draw-stress.svg`  (new, Plan 02 ships)
- `10;` DRAW MESHES         → `mesh-draw.svg`    (REUSE from 6.1 mail/)
- `20;` PRECISION           → `SP_DialogApplyButton` or `—`
- `38;` WINDOW SIZE         → `SP_ComputerIcon` or `—`
- `39;` BACKGROUND COLOR    → `SP_DesktopIcon` or `—`
- `71;` TMS TOOLS           → `SP_DirIcon` or `—`
- `72;` MS FILE TOOLS       → `SP_DirIcon`
- `73;` I/O UNITS           → `SP_DirIcon`
- `74;` DELETE TMS          → `SP_TrashIcon`
- `97;` VERSION NAME        → `SP_MessageBoxInformation` or `—`
- `99;` SAVE & QUIT          → `SP_DialogCloseButton` (shared with 6.1)

For `icon_source` on non-qaction rows: `—`.

For `icon_source` on qaction rows that are elas-specific but too niche
for a custom icon: Qt SP_* enum (as above) or `—` (menu-only text).

Every SVG filename listed MUST be one Plan 02 will actually ship, OR a
filename already shipped by 6.1 (`mesh-draw.svg` is the only reuse
candidate — the 10 mesh-*.svg are conceptually mail-specific except for
the view/draw icon).

### Step 6 — Expand the 12 elas-specific sub-menus

For each of cl_elas, contdepl, defplan, methreso, resoelin, resoelst,
schedyna, sectplan, therelas, traccont, tracdepl, valzone:

1. Read the corresponding file under `td/mf/<name>` (FR backup — authoritative)
   and `td/ma/<name>` (EN source).
2. Extract every `<NUM> : '<label>'` pair.
3. Determine the parent prefix from the call site (grep the sub-menu
   name in elas/*.f, note the LIMTCL context and the surrounding
   GOTO label or IF branch).
4. Emit one row per leaf with the parent-prefixed lexicon_path.

### Step 7 — Compress the 20 shared util sub-menus

For each of affiche, couleur0, couleurs, entrsort, fichier, lecteur,
managfen, managmef, mode_es, opt_lign, opt_surf, sectvopl, suivi_ms,
suivitms, tracaxes, tuer, typ_objt, typtrait, vuesplan, zeros:

Emit ONE row with:
- `lexicon_path` = a valid representative parent-entry numeric path
  (e.g. `10;37;1;` if couleurs is reached from a tracmail equivalent in
  elas, OR `7;1;` if it's reached from mode_es at code 1 from code 7, etc.)
- `description_fr/en` = the sub-menu title from td/mf/<name> / td/ma/<name>
  followed by `(shared util)`
- `frequency = low`
- `qaction = no`
- `toolbar = no`
- `icon_source = —`
- `shortcut = —`
- `notes = "shared util sub-menu — <N> leaves; see 6.1 LEXICON-AUDIT-mail.md for full enumeration; reached from elas/<FILE>.f:<LINE>"`

This mirrors the 6.1 Plan 01 auto-fix #2 decision (compressed-row
lexicon_path uses a valid representative parent-entry path, NOT a
synthetic marker — the validator's LEX_RX regex rejects non-numeric
paths).

### Step 8 — Draft top-5 toolbar set

Propose the 5 rows with `toolbar=yes` from the 17 debuelas top-level
codes, per the interfaces section default:

1. `3;`  STEADY ELASTICITY solver
2. `8;`  DRAWING of DISPLACEMENT & STRESS
3. `10;` DRAWING of PLSVO meshes
4. `2;`  ELASTICITY INPUT DATA
5. `99;` SAVE DATA and QUIT

All others `toolbar=no`. The exact top-5 is user-ratifiable at Task 2
checkpoint — the user may swap (e.g. `2;` → `1;` for Object-name-first
workflow) as long as the count stays at exactly 5.

### Step 9 — Status DRAFT (Task 2 flips to FROZEN)

Leave the top-of-file status line as:

```markdown
**Status:** DRAFT (pending user review — Task 2 checkpoint)
```

Task 2's human-verify checkpoint approves or requests edits; on approval
the status flips to `**Status:** FROZEN`.

### Step 10 — Append Summary Statistics block

Mirror 6.1 Plan 01 output: at the end of the file, a bullet list with:

- Total rows
- Rows by frequency bucket (high/med/low counts)
- Rows by qaction (yes/no counts) — yes count should equal high+med
- Top-5 toolbar set (5 bullets with lexicon + label)
- Elas-specific SVG icon filenames needed (for Plan 02 to ship)
- Shared SVG reuses (e.g. `mesh-draw.svg` from 6.1 mail/)

Use bullet lists (NOT pipe tables) for the Summary block — pipe tables
collide with the validator's first-pipe-table parser (6.1 Plan 01
auto-fix #3).

### Step 11 — Run the validator (WARN mode for icons)

```bash
cd /home/mefisto/git/mefisto
python3 tools/validate_audit_md.py \
    .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md
```

Expected:
- Exit code 0
- Output includes `schema valid` AND `5 toolbar=yes`
- WARN lines for each SVG filename Plan 02 will ship but hasn't yet
  (validator's ICONS_DIR is hard-wired to `mail/`; Plan 02 will
  generalize this constant). In this plan these are acceptable WARNs.

If the validator exits non-zero, FIX the audit markdown (hard contract —
9 rules are non-negotiable) before committing.

### Specificity notes

- Use the EXACT column header row from 6.1 — the validator does not
  allow header variations.
- Do NOT attempt to auto-fix elas-side typos in `td/m*/` files (if any
  surface); follow 6.1 Pitfall 5 log-and-fallback.
- `description_fr` column pulls from `td/mf/<menu>` (FR backup); this is
  the robust path against a live EN install where `td/m/` contains EN.
- `description_en` column pulls from `td/ma/<menu>` (EN source).
- Escape pipes `|` in description strings as `\|` for GitHub-Markdown
  safety (6.1 Plan 01 applied this rule).
- Embedded single-quotes in the LIMTCL files (e.g. `'d''un'`) decode to
  literal `d'un` — un-double them when writing cells.
  </action>
  <verify>
    <automated>test -f /home/mefisto/git/mefisto/.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md && awk -F'|' '/^\|/ {print NF}' /home/mefisto/git/mefisto/.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md | sort -u && python3 /home/mefisto/git/mefisto/tools/validate_audit_md.py /home/mefisto/git/mefisto/.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` exists.
    - First markdown table has exactly 11 pipe-separated tokens per row (9 data columns + 2 outer pipes). `awk -F'|' '/^\|/ {print NF}' LEXICON-AUDIT-elas.md | sort -u` emits a single value `11`.
    - `grep -cE '^\| *[0-9]+(;[0-9]+)*; *\|' .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` returns a number in [80, 250].
    - The 17 debuelas top-level codes are all present as individual rows. Verify with `for c in 1 2 3 4 6 7 8 10 20 38 39 71 72 73 74 97 99; do grep -cE "^\| ${c}; \|" LEXICON-AUDIT-elas.md; done` — each iteration returns ≥ 1.
    - `python3 tools/validate_audit_md.py .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` exits 0.
    - Validator output contains `schema valid` AND `5 toolbar=yes`.
    - Rule 9 WARN lines for new SVGs are acceptable (Plan 02 ships the files).
    - Top of file carries `**Status:** DRAFT`.
    - `grep -cE '^\|[^|]*\| *[^|]*\| *[^|]*\| *high *\|' .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` returns at least 5 (at least 5 HIGH rows; may be more).
    - File opens cleanly in any markdown renderer (no malformed rows).
    - No reference to `debut` (mail's top-level); all top-level refs are to `debuelas`.
  </acceptance_criteria>
  <done>
    LEXICON-AUDIT-elas.md exists with 9-column schema identical to 6.1's,
    all 17 debuelas top-level codes present, 12 elas-unique sub-menus
    fully expanded, 20 shared util sub-menus compressed to 1 row each,
    frequency bucketing applied from testa/*/*.elas + domain review,
    5 rows marked toolbar=yes, validator exits 0, Status=DRAFT pending
    user review in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review of frequency + top-5 toolbar + {File, Solve, View, Help} taxonomy — LOCK audit</name>
  <files>.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md</files>
  <read_first>
    - .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md (the Task 1 deliverable)
    - .planning/phases/06.1-mesher-mail-menu-wiring/06.1-CONTEXT.md D-10, D-11, D-14 (user-review-before-freeze convention — auto-mode auto-approves with ratification notes)
    - .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md §Per-Module Conformance Contract (elas <Module> menu title is Solve/Calcul)
    - .planning/ROADMAP.md §"Phase 6.2" (canonical menu taxonomy {File, Solve, View, Help})
  </read_first>
  <what-built>
    Task 1 produced `.planning/phases/06.2-*/LEXICON-AUDIT-elas.md` with the
    full recursive LIMTCL tree walk (~80-150 rows), 9-column schema, and
    `frequency` buckets scored from `testa/*/*.elas` grep evidence supplemented
    by domain review of the 17 debuelas top-level codes. Validator exits 0.

    Three ratification items (auto-mode auto-approves with notes):

    1. **Frequency bucketing** — HIGH rows should include `3;` `8;` `10;` and
       at least 2-3 others based on .elas evidence.
    2. **Top-5 toolbar** — proposed set (ratify or swap):
        - `3;`  STEADY ELASTICITY solver
        - `8;`  DRAWING of DISPLACEMENT & STRESS
        - `10;` DRAWING of PLSVO meshes
        - `2;`  ELASTICITY INPUT DATA
        - `99;` SAVE DATA and QUIT
    3. **Menu taxonomy** — `{File, Solve, View, Help}` per ROADMAP + UI-SPEC.
       Menu-content mapping:
       - File: 72; 73; 74; 99; + shared 6.0 File actions
       - Solve: 1; 2; 3; 4; 6; + Parameters submenu (20; 38; 39;)
       - View:  7; 8; 10; 71; + shared 6.0 View actions
       - Help:  97; + shared 6.0 Help/About
  </what-built>
  <how-to-verify>
    1. Open the audit markdown:
       `code .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md`
       or via github preview.

    2. Scan the `frequency` column against the Task 1 Step 2 rubric.
       Explicitly check:
       - `3;` (Static solve), `8;` (Draw stress), `10;` (Draw mesh)
         marked HIGH (these are the three core elas workflow actions).
       - `2;` (Input data), `1;` (Object name), `99;` (Save-quit)
         marked HIGH or MED — the first-session actions.
       - Niche codes (`38;` window size, `39;` background color,
         `97;` version name) marked LOW.
       - Expanded cl_elas / resoelst / schedyna leaves mostly LOW
         (they're typed once when setting up, not in steady-state use).

    3. Verify the 5 `toolbar=yes` rows match the proposal — or propose
       swaps. Acceptable adjustments:
       - Swap `2;` for `1;` if "Object first" workflow is more common
       - Swap `99;` for `7;` (Draw modes) if eigenmodes is a primary
         visual in your typical elas sessions
       - Any change must keep the count at exactly 5
       `grep -c '| yes | yes |' LEXICON-AUDIT-elas.md` should return 5.

    4. Verify rows with `qaction=yes` cover the Solve menu + File/View
       elas extensions (target 15-25 rows). Count with:
       `grep -cE '\| yes \|' .planning/phases/06.2-*/LEXICON-AUDIT-elas.md`
       Expect ~15-25 yeses; wildly high (>40) is a signal to tighten
       (pushed too many low-frequency items up).

    5. Ratify the **menu taxonomy**:
       - Confirm `{File, Solve, View, Help}` is the elas top-level bar
         (differs from 6.1's `{File, Mesh, View, Help}`)
       - Confirm the Solve menu maps to codes 1;–6; (solver launch) plus
         a `Parameters` submenu (20; 38; 39;)
       - Confirm codes 71; 72; 73; 74; go into File (storage/TMS
         management)
       - Confirm codes 7; 8; 10; go into View (visualization)

    6. Ratify the **elas SVG icon set** (6-8 new icons in Plan 02):
       `solve-static.svg`, `solve-dynamic.svg`, `solve-eigen.svg`,
       `draw-modes.svg`, `draw-stress.svg`, `elas-object.svg`,
       `elas-input.svg`. Plus the shared `mesh-draw.svg` reuse from
       6.1 mail/ directory. This list is the Plan 02 `xvue_icons.qrc`
       append set.

    7. If changes are needed, edit the markdown directly then re-run:
       `python3 tools/validate_audit_md.py .planning/phases/06.2-*/LEXICON-AUDIT-elas.md`
       Must exit 0.

    8. Once satisfied, flip `**Status:** DRAFT` to `**Status:** FROZEN`
       at the top of the file. Commit triggers proceed after that.
  </how-to-verify>
  <action>
    This is a checkpoint task (type=checkpoint:human-verify): no automated
    action. The user reviews the LEXICON-AUDIT-elas.md draft using the
    steps in <how-to-verify> above, applies any ratification swaps, and
    either approves by flipping `**Status:** DRAFT` to `**Status:** FROZEN`
    or requests edits. In `--auto` mode the checkpoint auto-approves
    with the DRAFT-proposed values retained verbatim; user can open the
    audit after the phase completes and request polish-pass edits as
    a follow-up.
  </action>
  <verify>
    <automated>grep -q '^\*\*Status:\*\* FROZEN' /home/mefisto/git/mefisto/.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md && python3 /home/mefisto/git/mefisto/tools/validate_audit_md.py /home/mefisto/git/mefisto/.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^\*\*Status:\*\* FROZEN' .planning/phases/06.2-*/LEXICON-AUDIT-elas.md` succeeds (status is FROZEN).
    - `python3 tools/validate_audit_md.py .planning/phases/06.2-*/LEXICON-AUDIT-elas.md` exits 0 after any user-requested edits.
    - Exactly 5 rows have `toolbar=yes` — `grep -cE '\| yes *\| *[^|]*\| *[^|]*\|[^|]*$' .planning/phases/06.2-*/LEXICON-AUDIT-elas.md` column-5 analysis via validator confirms.
    - Menu-taxonomy mapping ({File, Solve, View, Help}) is ratified in the audit's Scope section (a paragraph-level note near top-of-file).
    - User-requested edits (if any) are applied and re-validated.
  </acceptance_criteria>
  <done>
    User has reviewed the audit; `**Status:** FROZEN` is set; the
    validator still exits 0 after any user-requested edits; the top-5
    toolbar set, frequency bucketing, AND menu taxonomy are locked as
    the contract Plan 02 will consume.
  </done>
  <resume-signal>
    Type "frozen" if you are happy with the bucketing, top-5 toolbar,
    and the {File, Solve, View, Help} taxonomy — Plan 02 will consume
    the audit as-is.
    Type "edit" if you want to propose changes — describe them inline
    (e.g. "swap toolbar `99;` for `7;`; demote `20;` to LOW"), then I
    will make the edits, re-run the validator, and return here.
    Type "abort" if the audit fundamentally misses the mark — triggers
    a re-run of Task 1 with adjusted scope.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                  | Description                                             |
|---------------------------|---------------------------------------------------------|
| user-edit → audit.md      | User reviews and possibly edits rows during Task 2      |
| filesystem → validator    | Python reads markdown file; script is read-only         |

Note: Plan 01 adds NO runtime attack surface. It produces a doc deliverable
only; no binary or build-script changes.

## STRIDE Threat Register

| Threat ID      | Category         | Component                       | Disposition | Mitigation Plan |
|----------------|------------------|---------------------------------|-------------|-----------------|
| T-6.2-AUDIT-01 | Tampering        | LEXICON-AUDIT-elas.md schema    | mitigate    | `tools/validate_audit_md.py` enforces the 9 hard rules; exit 1 on any violation. Rule 9 cross-checks SVG filenames against ICONS_DIR (mail/ by default; Plan 02 generalizes this). |
| T-6.2-AUDIT-02 | Info Disclosure  | audit rows contain FR/EN text   | accept      | Content sourced from `$MEFISTO/td/m*/` which is open-source user data; no secrets. Same disposition as 6.1 T-6.1-AUDIT-02. |
</threat_model>

<verification>
Phase-wide checks for Plan 01:
1. `test -f .planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` → exit 0
2. `python3 tools/validate_audit_md.py .planning/phases/06.2-*/LEXICON-AUDIT-elas.md` → exit 0
3. Audit `**Status:**` line reads `FROZEN` after Task 2 sign-off (not `DRAFT`)
4. No changes to any Fortran, C++, CMake, or build-script files (Plan 01 is docs-only)
5. No regression: `bin/cbl_tout_qt` still builds (spot-run before wrap-up); no binary artifacts touched
6. No new files under `xvue/qt/resources/icons/elas/` yet — that is Plan 02's scope
</verification>

<success_criteria>
- UX-05 elas-slice Success Criterion #1 is satisfied: `LEXICON-AUDIT-elas.md`
  exists and enumerates every interactive command reachable from
  `td/m/debuelas` with FR+EN descriptions, frequency column, and QAction
  flag per the 6.1-frozen schema.
- `tools/validate_audit_md.py` exit 0 is the automated gate.
- User has explicitly approved (or auto-approved with notes) the frequency
  bucketing, top-5 toolbar set, AND the `{File, Solve, View, Help}` menu
  taxonomy — the audit reads `**Status:** FROZEN`.
- Plan 02 can consume the audit without further re-interpretation: the
  rows with `qaction=yes` ARE the QAction set; the rows with
  `toolbar=yes` ARE the top-5 toolbar; the SVG filenames in
  `icon_source` ARE the Plan 02 `xvue_icons.qrc` elas-append set.
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-elasticity-elas-menu-wiring/06.2-01-SUMMARY.md`
with:
- Audit row count (total, and split by frequency bucket)
- Final top-5 toolbar set (post-review)
- Elas-unique SVG filenames queued for Plan 02's qrc append set
- Shared SVG reuses from 6.1 (e.g. `mesh-draw.svg`)
- Menu-taxonomy mapping ratified: File / Solve / View / Help content
  distribution
- Any user edits applied between DRAFT and FROZEN
- Confirmation that `validate_audit_md.py` exits 0
- Hand-off note to Plan 02: "Consume .planning/phases/06.2-*/LEXICON-AUDIT-elas.md
  rows where qaction=yes as the registerElasActions_stub_ QAction set;
  rows where toolbar=yes as the toolbar append set; rows where
  icon_source ends in .svg (and the filename is not already in
  xvue/qt/resources/icons/mail/) as the xvue_icons.qrc append set for
  icons/elas/."
</output>
