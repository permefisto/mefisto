---
phase: 06.2-elasticity-elas-menu-wiring
plan: 03
subsystem: ui
tags: [qt6, fortran77, weak-symbol-reuse, force-link-keepalive, qtest, qaction, xvsouris, limtcl, elas]

# Dependency graph
requires:
  - phase: 06.2-01
    provides: LEXICON-AUDIT-elas.md (frozen rows; 3;=Static solve, 8;=Draw stress, 72;=MS File Tools)
  - phase: 06.2-02
    provides: xvue_qt_elas_actions.cpp strong body (registerElasActions_stub_) + 7 SVG icons + qrc append + xvue_qt_elas_actions_keepalive() wired from XvueApp::ensure() + CMake target_sources
  - phase: 06.1-03
    provides: xvue/xvmodi.f weak Fortran stub (!GCC$ ATTRIBUTES WEAK) — reused verbatim for all remaining modules; XvueWindow D-09 closeEvent behavior — inherited by pp/ppelas_qt
provides:
  - prpr/ppelas.f CALL XVUE_MODULE_INIT('elas', 4) inside IF(INTERA.GE.1) block after XVINIT + LOGO
  - xvue/qt/tests/test_xvue_qt_elas_menu.cpp — 5 D-13 test slots (4 required + 1 bonus closeEvent via BlockingDepthGuard)
  - xvue/qt/tests/CMakeLists.txt — add_executable(xvue_qt_elas_menu_tests) target
affects: [06.3, 06.4, 06.5]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single-line Fortran CALL in per-module pp*.f dispatches to strong Qt body or reuses shared X11 weak stub (no new Fortran source needed — xvmodi.f from 6.1 covers all five modules)"
    - "QTest round-trip: QAction::trigger() + xvsouris_ drain + assert char sequence — reusable verbatim across 6.3/6.4/6.5 by substituting module name + lexicon list"
    - "Shared XvueWindow::closeEvent (6.1 Plan 03) gates on BlockingDepth — click-X and typed 99; converge on same Fortran exit path across ALL pp*_qt binaries"

key-files:
  created:
    - xvue/qt/tests/test_xvue_qt_elas_menu.cpp
  modified:
    - prpr/ppelas.f
    - xvue/qt/tests/CMakeLists.txt

key-decisions:
  - "Zero Fortran changes to xvue/xvmodi.f — the 6.1 !GCC$ ATTRIBUTES WEAK stub already covers ALL five solver modules. Phase 6.2+ just adds one CALL site per module; no per-module Fortran stub needed. Confirms the cross-backend pattern is self-amortizing from 6.2 onward."
  - "Force-link keepalive (6.1 Rule 3 auto-fix) carried over without re-work — Plan 02 added xvue_qt_elas_actions_keepalive() + XvueApp::ensure() reference; Plan 03 rebuild confirms nm pp/ppelas_qt shows 1 strong registerElasActions_stub_ (same as pp/ppmail_qt)."
  - "D-09 closeEvent / onFileQuit semantics inherited for free — XvueWindow is shared across all pp*_qt. Plan 03 MUST NOT re-edit xvue_qt_window.cpp; the bonus test testCloseEventPushes99WhenBlocking exercises the shared implementation against the elas XvueApp instance and confirms the 99; push lands with matching queue delta (=4)."
  - "CALL placement mirrors 6.1 ppmail.f: AFTER CALL XVINIT AND AFTER CALL LOGO('MEFISTO-ELASTICER') AND BEFORE ENDIF. Single 3-line insertion (2 comment lines + 1 CALL). Semantically inside IF(INTERA.GE.1) so batch/pipe runs never trigger Qt registration — X11 xvmodi.f is a no-op anyway but the guard keeps intent clean."
  - "Test count delta +7 (116 -> 123): 5 test slots + 2 built-in initTestCase/cleanupTestCase — matches mail_menu baseline and plan expectation."

patterns-established:
  - "Pattern: single CALL site + zero Fortran stub reuse is the path for remaining 6.3 (flui), 6.4 (ther), 6.5 (nlse) — each adds one line to prpr/pp<mod>.f, one xvue_qt_<mod>_actions.cpp TU, one keepalive pair, one QTest TU. No more Fortran-side work after 6.1 Plan 03's xvmodi.f landed."
  - "Pattern: the 6.1 -> 6.2 symmetry holds verbatim — substitute {mail -> elas, Mesh -> Solve, 10;/1;1;/80; -> 3;/8;/72;} and both the implementation and the test shape are identical. Planner can mint 6.3/6.4/6.5 plans as near-copies."

requirements-completed: [UX-05]

# Metrics
duration: session-adopted (files pre-existed from prior interrupted session; this session ran serial rebuild + tests + atomic commits)
completed: 2026-04-24
---

# Phase 06.2 Plan 03: Close-out — Fortran hook + D-13 elas-menu tests Summary

**One-line Fortran CALL + 5 D-13 QTest cases close Phase 6.2. No new Fortran source, no xvue_qt_api.cpp edits, no xvue_qt_window.cpp edits — pattern is now a copy-paste template for 6.3/6.4/6.5.**

## Performance

- **Duration:** Session-adopted — Tasks 1+2 files were implemented in a prior interrupted session but not committed; this session ran the serial Qt+X11 rebuild, full Qt test suite, X11 smoke, and landed two atomic commits.
- **Rebuild wall-clock:** ~7 min (Qt ~4 min + X11 ~3 min, serial per MEMORY.md feedback_parallel_builds_share_pp.md)
- **Completed:** 2026-04-24
- **Tasks:** 3 / 3 (Task 3 manual A/B items flagged [MANUAL-DEFERRED] — see HUMAN-UAT persistence below)
- **Files created:** 1 (`xvue/qt/tests/test_xvue_qt_elas_menu.cpp` — 281 lines)
- **Files modified:** 2 (`prpr/ppelas.f` +3 lines, `xvue/qt/tests/CMakeLists.txt` +25 lines)

## Accomplishments

### Task 1 — prpr/ppelas.f CALL insertion (commit `5bbbff1`)

Three-line insertion (2 comment lines + 1 CALL) inside the `IF( INTERA .GE. 1 )` block, AFTER `CALL XVINIT` AND AFTER `CALL LOGO( 'MEFISTO-ELASTICER' )` AND BEFORE `ENDIF`:

```fortran
C        Phase 6.2 D-08: register Qt-side module-specific menu actions.
C        No-op on the X11 backend (xvue/xvmodi.f weak stub from 6.1).
         CALL XVUE_MODULE_INIT( 'elas', 4 )
```

Column-7+ fixed-form discipline respected (CALL at column 10 to match the surrounding CALLs; comment `C` in column 1). Integer `4` is the length of `'elas'`.

### Task 2 — D-13 QTest cases (commit `22e410e`)

`xvue/qt/tests/test_xvue_qt_elas_menu.cpp` — 5 slots:

| Slot | Lexicon | Menu | Expected xvsouris drain |
|------|---------|------|-------------------------|
| `testSolveStatic` | `3;` | Solve | `{'3', ';', CR}` |
| `testViewDrawStress` | `8;` | View | `{'8', ';', CR}` |
| `testFileMSTools` | `72;` | File | `{'7', '2', ';', CR}` |
| `testHelpNoQueue` | n/a | Help | queueSize unchanged (no lexicon property on any Help action) |
| `testCloseEventPushes99WhenBlocking` | `99;` | (close event) | queueSize += 4 via BlockingDepthGuard |

Harness pattern verbatim from 6.1's `test_xvue_qt_mail_menu.cpp`: `actionByLexicon` recursive helper walks submenus; `expectChars` drains `xvsouris_` one char at a time asserting `notypeevent == 2`.

`xvue/qt/tests/CMakeLists.txt` — new `add_executable(xvue_qt_elas_menu_tests)` target adjacent to `xvue_qt_mail_menu_tests`, linked against `xvueqt Qt6::Test Qt6::Widgets Qt6::Gui Qt6::Core`.

### Task 3 — manual A/B items flagged [MANUAL-DEFERRED]

Automated portion green:
- `bin/cbl_tout_qt` exit 0 (full rebuild, all pp*_qt regenerated)
- `bin/cbl_tout` exit 0 (X11 — xvue_module_init_ resolves to xvmodi.f weak symbol)
- `verify_abi.sh`: nm count 58 vs header count 58 — ABI preserved
- `nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'` = 1 (Plan 02 keepalive survived full rebuild)
- `nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'` = 1 (6.1 regression guard)
- Full Qt test suite: **123 passed / 0 failed / 2 skipped** across 9 test binaries
  - canvas_gestures 12, dialogs_console 14, elas_menu 7 (new), event_menu_predrain 10, event 33 (2 pre-existing skips), i18n_menu_prefs 19, mail_menu 7, menu 5, window_chrome 16
- `echo "99;" | pp/ppelas` exit 0 (X11 backend resolves CALL without side effects)

Deferred visual items (requires interactive X11 display — persisted in `06.2-HUMAN-UAT.md` for later sweep via `/gsd-verify-work 6.2`):
1. `pp/ppelas_qt` menu bar shows `{File, Solve, View, Help}` with bilingual labels
2. Click `Solve > Static solve` observed to echo `[menu] 3;` in console dock and enter Fortran steady-elasticity solver branch
3. Click window-X observed to echo `[menu] 99;` and exit cleanly
4. Restart + typed `99;` at initial prompt exits identically to click-X (D-09 convergence)
5. Toolbar icons (Static solve / Draw stress / Draw modes / elas-input / elas-object) dispatch equivalently to menu items
6. Canonical `nafems_le1.elas` batch run through `pp/ppelas` (requires INITIER -> MAILLER harness preamble not included in automated smoke)

## Regression guarantees

- `xvue/xvmodi.f` untouched (`git diff` empty) — 6.1 stub serves all modules
- `xvue/qt/src/xvue_qt_api.cpp` untouched — no new ABI entries
- `xvue/qt/src/xvue_qt_window.cpp` untouched — D-09 behaviour inherited
- Mail regression guard: `nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'` still 1

## Phase 6.2 sign-off status

**CODE-COMPLETE** — pending human sign-off on the 6 deferred visual items above.

## Hand-off to Phase 6.3 (fluider)

Pattern is now proven-idempotent across two modules (mail + elas). For 6.3 flui:
1. `/gsd-plan-phase 6.3` — mint 3 plans by substitution: flui audit + xvue_qt_flui_actions.cpp + prpr/ppflui.f CALL + test_xvue_qt_flui_menu.cpp
2. No new Fortran files (xvmodi.f continues to cover flui, ther, nlse)
3. No xvue_qt_api.cpp or xvue_qt_window.cpp edits
4. `tools/validate_audit_md.py` generalizes unchanged (ICONS resolver landed 6.2-02)
5. XvueApp::ensure() keepalive pair list grows from 2 -> 3 (mail + elas + flui)
6. Expected test delta: +7 slots (123 -> 130)
