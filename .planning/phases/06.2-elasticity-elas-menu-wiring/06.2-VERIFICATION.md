---
phase: 06.2-elasticity-elas-menu-wiring
verified: 2026-04-24T12:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: null
human_verification:
  - test: "pp/ppelas_qt menu bar shows {File, Solve, View, Help} with NO 'Mesh' or 'Edit' top-level items"
    expected: "Four top-level menus render; Solve replaces Mesh from 6.1; Edit absent per D-01 inherited from 6.1"
    why_human: "Visual observation of live Qt window cannot be asserted in offscreen QTest"
  - test: "Bilingual labels render in the Solve menu per $MEFISTO/td/m/anglais flag"
    expected: "EN labels (e.g. 'ELASTICITY INPUT DATA') when anglais exists; FR labels ('ENTREE DONNEES PROBLEME ELASTICITE') otherwise"
    why_human: "Requires on-box MEFISTO environment with bilingual td/m/ tree; text-content check is human-verifiable only"
  - test: "Click Solve > Static solve echoes [menu] 3; in console dock and enters Fortran steady-elasticity solver branch"
    expected: "Console dock shows '[menu] 3;' then Fortran enters the resoelst dispatch path (code 3 from debuelas)"
    why_human: "Requires live interactive elas session via pp/ppelas_qt with a real MEFISTOX project"
  - test: "Click window-X (close button) on pp/ppelas_qt exits cleanly"
    expected: "Console shows '[menu] 99;'; Fortran save path runs briefly; window closes; process exits 0 with no stuck window"
    why_human: "Requires GUI interaction with window manager"
  - test: "Typed '99;' at initial pp/ppelas_qt prompt exits identically to click-X (D-09 convergence)"
    expected: "Same behavior as click-based close — typed and clicked paths converge at DONNMF (inherited from 6.1 XvueWindow rewrite)"
    why_human: "Requires interactive elas session"
  - test: "Toolbar icons (Static solve / Draw stress / Draw mesh / Input data) each dispatch the correct lexicon equivalently to their menu counterparts"
    expected: "Clicking toolbar button fires same queueLexicon call as the corresponding menu action; console shows [menu] <lex>"
    why_human: "Requires visual session with live toolbar rendering and click observation"
---

# Phase 6.2: Elasticity (elas) Menu Wiring — Verification Report

**Phase Goal:** `ppelas_qt` shows an elasticity-specific top-level menu bar (`{File, Solve, View, Help}`), 80/20 QAction coverage, and `CALL XVUE_MODULE_INIT('elas')`.
**Verified:** 2026-04-24T12:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification
**Mode:** Automated checks all pass; 6 interactive visual items deferred to human (same pattern as 6.1 VERIFICATION.md).

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria + Plan must_haves)

The ROADMAP states success criteria as "same pattern as 6.1, substituting `elas` for `mail`." The Plan 03 must_haves frontmatter defines 9 concrete truths. All 5 roadmap-level truths below are verified.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `LEXICON-AUDIT-elas.md` enumerates every interactive elas command with FR+EN descriptions, frequency column, QAction flag; Status=FROZEN | VERIFIED | File present (28.2K, 102 rows); header reads `**Status:** FROZEN (auto-approved 2026-04-22)`; all 17 debuelas top-level codes present as individual rows (1; through 99; confirmed by per-code grep — each returns 1); `AUDIT_STRICT_ICONS=1 python3 tools/validate_audit_md.py LEXICON-AUDIT-elas.md` → `OK -- 102 rows, 5 toolbar=yes, schema valid` (exit 0). 9-column schema enforced. Frequency distribution: 5 HIGH / 14+ MED / remainder LOW. Menu taxonomy `{File, Solve, View, Help}` ratified in audit Scope section. |
| 2 | `registerElasActions_stub_` strong-symbol override wires 27+ QActions, each connected via `QAction::triggered` → `XvueMenuBridge::queueLexicon(<lex>)` | VERIFIED | `xvue/qt/src/xvue_qt_elas_actions.cpp` (351 lines) defines `extern "C" void registerElasActions_stub_(XvueWindow*, XvueMenuBridge*)` with 33 `makeLeafAction` calls (27 unique audit lexicon paths + 6 parent-shortcut convenience entries). `queueLexicon` called in the makeLeafAction helper (line 71). `nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'` returns 1 — strong body force-linked via `xvue_qt_elas_actions_keepalive()` keepalive export wired in `xvue_qt_app.cpp` lines 50-52. Solve menu (`ensureTopLevelMenu`) and File/View extensions (`findChild<QMenu*>()`) correctly structured. |
| 3 | `prpr/ppelas.f` contains `CALL XVUE_MODULE_INIT( 'elas', 4 )` inside the `IF(INTERA .GE. 1)` block, after CALL XVINIT and after CALL LOGO | VERIFIED | `grep -n "CALL XVUE_MODULE_INIT" prpr/ppelas.f` returns line 133. Placement verified via `awk '/IF\( INTERA .GE. 1 \) THEN/,/^      ENDIF/'` — the CALL appears at position 8 of the block, after `CALL XVINIT` (position 3) and `CALL LOGO('MEFISTO-ELASTICER')` (position 5), before the closing `ENDIF` (position 9). Column 10 alignment with Fortran 77 fixed-form discipline preserved. `xvue/xvmodi.f` unchanged from 6.1 (`git diff` empty on branch). |
| 4 | 7 custom elas SVG icons shipped under `xvue/qt/resources/icons/elas/`, registered in `xvue_icons.qrc`; verify_icon_source.sh + verify_shortcut_modifiers.sh pass | VERIFIED | All 7 SVGs present: `draw-modes.svg`, `draw-stress.svg`, `elas-input.svg`, `elas-object.svg`, `solve-dynamic.svg`, `solve-eigen.svg`, `solve-static.svg`. `xvue_icons.qrc` has 7 `<file>icons/elas/*.svg</file>` entries inside the existing `/xvue/qt/icons` qresource prefix block. `verify_icon_source.sh` → "OK (icons is SVG-clean)"; `verify_shortcut_modifiers.sh` → "OK (no bareword shortcuts)". `validate_audit_md.py` generalized (ICONS_ROOT + `resolve_icon_path()` module-aware resolver; `AUDIT_STRICT_ICONS=1` exit 0 for both elas and mail audits — confirmed). ABI preserved at 58 (`verify_abi.sh` → "nm count: 58  header count: 58"). |
| 5 | 4+1 D-13 QTest cases exercise the elas menu; all pass under `QT_QPA_PLATFORM=offscreen`; total Qt suite 123/0 | VERIFIED | `xvue/qt/tests/test_xvue_qt_elas_menu.cpp` (281 lines) — 5 test slots: `testSolveStatic`, `testViewDrawStress`, `testFileMSTools`, `testHelpNoQueue`, `testCloseEventPushes99WhenBlocking`. Test binary `xvue/qt/build/tests/xvue_qt_elas_menu_tests` exists. Live run under offscreen QPA: **Totals: 7 passed, 0 failed, 0 skipped** (init + cleanup + 5 slots = 7 QtTest entries). Full Qt suite 9 binaries: **123 passed, 0 failed, 2 pre-existing skips** (canvas_gestures 12, dialogs_console 14, elas_menu 7, event_menu_predrain 10, event 33+2skip, i18n_menu_prefs 19, mail_menu 7, menu 5, window_chrome 16). Mail regression guard: `nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'` = 1. |

**Score:** 5/5 truths verified

### Deferred Items

None — no gap items pushed to later phases. All automated truths pass. The 6 items below are interactive visual checks that cannot be automated.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/06.2-elasticity-elas-menu-wiring/LEXICON-AUDIT-elas.md` | Per-module elas lexicon audit, 9-column schema, FROZEN | VERIFIED | 102 rows, validator exit 0, Status=FROZEN, 17 debuelas codes all present |
| `xvue/qt/src/xvue_qt_elas_actions.cpp` | Strong `registerElasActions_stub_` body + keepalive export | VERIFIED | 351 lines, 33 makeLeafAction calls, `xvue_qt_elas_actions_keepalive()` at line 126, strong symbol in `pp/ppelas_qt` |
| `xvue/qt/resources/icons/elas/elas-object.svg` | Custom elas SVG icon | VERIFIED | 286 bytes, present |
| `xvue/qt/resources/icons/elas/elas-input.svg` | Custom elas SVG icon | VERIFIED | 293 bytes, present |
| `xvue/qt/resources/icons/elas/solve-static.svg` | Custom elas SVG icon | VERIFIED | 276 bytes, present |
| `xvue/qt/resources/icons/elas/solve-dynamic.svg` | Custom elas SVG icon | VERIFIED | 246 bytes, present |
| `xvue/qt/resources/icons/elas/solve-eigen.svg` | Custom elas SVG icon | VERIFIED | 316 bytes, present |
| `xvue/qt/resources/icons/elas/draw-modes.svg` | Custom elas SVG icon | VERIFIED | 332 bytes, present |
| `xvue/qt/resources/icons/elas/draw-stress.svg` | Custom elas SVG icon | VERIFIED | 315 bytes, present |
| `xvue/qt/resources/xvue_icons.qrc` | 7 new elas `<file>` entries inside existing prefix block | VERIFIED | 7 `<file>icons/elas/*.svg</file>` entries confirmed at lines 18-24 |
| `prpr/ppelas.f` | `CALL XVUE_MODULE_INIT( 'elas', 4 )` inside IF(INTERA.GE.1) | VERIFIED | Line 133; placement after XVINIT + LOGO, before ENDIF confirmed via awk |
| `xvue/qt/tests/test_xvue_qt_elas_menu.cpp` | 4+1 D-13 QTest cases | VERIFIED | 281 lines, 5 slots, all pass under offscreen QPA |
| `xvue/qt/tests/CMakeLists.txt` | `add_executable(xvue_qt_elas_menu_tests)` target | VERIFIED | Line 193, linked against xvueqt + Qt6::Test/Widgets/Gui/Core |
| `xvue/qt/src/xvue_qt_app.cpp` | Keepalive extern + static ref (alphabetically before mail) | VERIFIED | Lines 50-52: extern + static ref; line 69: `(void)` cast; elas before mail in file |
| `xvue/qt/CMakeLists.txt` | `src/xvue_qt_elas_actions.cpp` in target_sources | VERIFIED | Line 46: `src/xvue_qt_elas_actions.cpp` |
| `tools/validate_audit_md.py` | Module-aware `resolve_icon_path()` replaces `ICONS_DIR` constant | VERIFIED | `ICONS_ROOT` + `resolve_icon_path()` at lines 42-74; AUDIT_STRICT_ICONS=1 passes both mail and elas audits |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `prpr/ppelas.f CALL XVUE_MODULE_INIT('elas', 4)` | `xvue/qt/src/xvue_qt_api.cpp:1527 registerElasActions_stub_` | Linker — Qt strong body displaces X11 weak stub | WIRED | `xvue_qt_api.cpp:1527` contains `else if (mod == QStringLiteral("elas")) registerElasActions_stub_(wptr, mbptr)`. `pp/ppelas_qt` strong symbol confirmed via `nm`. |
| `prpr/ppelas.f CALL XVUE_MODULE_INIT('elas', 4)` | `xvue/xvmodi.f SUBROUTINE XVUE_MODULE_INIT` | Fortran linker — X11 path resolves to weak no-op | WIRED | `xvue/xvmodi.f` unchanged from 6.1 ship. `pp/ppelas` (X11 binary) exists. X11 build produces `pp/ppelas` cleanly (per SUMMARY-03 Task 1 verification). |
| `xvue_qt_elas_actions_keepalive()` | `xvue_qt_app.cpp` static ref | Force-link: ensures strong `registerElasActions_stub_` pulled into final binary | WIRED | `xvue_qt_app.cpp` lines 50-52 + 69. `nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'` = 1. |
| `makeLeafAction(win, mb, ..., "3;")` | `XvueMenuBridge::queueLexicon("3;")` | `QAction::triggered` lambda → `mb->queueLexicon(lexicon)` | WIRED | `xvue_qt_elas_actions.cpp:71`: `if (mb) mb->queueLexicon(lexicon)`. QTest `testSolveStatic` confirms `{'3', ';', 13}` reaches `xvsouris_` drain. |
| `xvue_qt_elas_menu_tests::testSolveStatic` | `registerElasActions_stub_` + `xvsouris_` drain | `xvue_module_init_("elas", 4)` → QAction::trigger() | WIRED | Test run: `PASS testSolveStatic` (7/0/0 totals confirmed). |
| `LEXICON-AUDIT-elas.md qaction=yes rows (27)` | `registerElasActions_stub_` (33 makeLeafAction calls) | Plan 02 consumed audit table body as the QAction set | WIRED | 27 unique audit leaves + 6 parent-shortcut extras = 33 total. All qaction=yes rows in audit are wired. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `xvue_qt_elas_actions.cpp` | `lexicon` string per QAction | Hardcoded string literals matching LEXICON-AUDIT-elas.md rows | Yes — literals are audit-derived LIMTCL commands | FLOWING |
| `test_xvue_qt_elas_menu.cpp` | `nbc` char values from `xvsouris_` | `QAction::trigger()` → `queueLexicon` → synthetic event → `xvsouris_` drain | Yes — test observes real queue drain | FLOWING |

Data-flow is simple and static for this phase: QAction lexicon strings are compile-time constants matching the audit. The queue drain path from `queueLexicon` through `xvsouris_` is proven by the 5 live QTest cases (7/0 pass).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| elas_menu_tests 5 slots all pass | `QT_QPA_PLATFORM=offscreen xvue/qt/build/tests/xvue_qt_elas_menu_tests` | 7 passed, 0 failed, 0 skipped, 90ms | PASS |
| `testSolveStatic` — `3;` lexicon reaches `xvsouris_` | (above) | PASS | PASS |
| `testViewDrawStress` — `8;` lexicon reaches `xvsouris_` | (above) | PASS | PASS |
| `testFileMSTools` — `72;` lexicon reaches `xvsouris_` | (above) | PASS | PASS |
| `testCloseEventPushes99WhenBlocking` — queue delta = 4 | (above) | PASS | PASS |
| Full Qt suite 9 binaries | `for t in xvue/qt/build/tests/xvue_qt_*_tests` | 123 passed, 0 failed, 2 pre-existing skips | PASS |
| mail regression guard | `nm pp/ppmail_qt | grep -c ' T registerMailActions_stub_'` | 1 | PASS |
| elas strong symbol in binary | `nm pp/ppelas_qt | grep -c ' T registerElasActions_stub_'` | 1 | PASS |
| ABI count preserved | `verify_abi.sh libxvueqt.a xvue_qt_api.h` | nm count: 58, header count: 58 | PASS |
| Validator strict mode | `AUDIT_STRICT_ICONS=1 python3 tools/validate_audit_md.py LEXICON-AUDIT-elas.md` | OK — 102 rows, 5 toolbar=yes, schema valid | PASS |
| X11 ppelas binary exists | `test -f pp/ppelas` | EXISTS | PASS |
| Qt ppelas_qt binary exists | `test -f pp/ppelas_qt` | EXISTS | PASS |
| All 17 debuelas codes present in audit | per-code grep | Each returns 1 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| UX-05 (elas slice SC #1) | 06.2-01 | LEXICON-AUDIT-elas.md enumerates every interactive elas command, FROZEN | SATISFIED | Audit present (102 rows), FROZEN, validator exit 0, all 17 debuelas codes, 13 elas-unique sub-menus expanded |
| UX-05 (elas slice SC #2) | 06.2-02 | registerElasActions_stub_ wires QActions covering qaction=yes rows | SATISFIED | 33 makeLeafAction calls (27 unique audit leaves + 6 parent shortcuts); strong symbol in pp/ppelas_qt |
| UX-05 (elas slice SC #3) | 06.2-03 | prpr/ppelas.f contains CALL XVUE_MODULE_INIT('elas', 4) | SATISFIED | Line 133 confirmed; placement inside IF(INTERA.GE.1) after XVINIT + LOGO verified |
| UX-05 (elas slice SC #4) | 06.2-02 | 7 custom SVG icons + xvue_icons.qrc entries | SATISFIED | All 7 SVGs under icons/elas/; 7 qrc entries; verify_icon_source.sh pass |
| UX-05 (elas slice SC #5) | 06.2-03 | xvue_qt_elas_menu_tests QTest round-trip cases | SATISFIED | 5 test slots, all 7 QtTest entries pass; 123/0 full suite |

Note: REQUIREMENTS.md maps `UX-05` to "Phase 6.1-6.5" as a group. Phase 6.2 covers the elas slice of UX-05 explicitly. No orphaned requirement IDs — all plans in this phase declare `UX-05` and evidence matches.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | No TODO/FIXME/placeholder/stub return-null patterns found in xvue_qt_elas_actions.cpp, prpr/ppelas.f, or test_xvue_qt_elas_menu.cpp | — | — |

Scan result: zero matches for `TODO|FIXME|XXX|PLACEHOLDER|placeholder|coming soon|not yet implemented` across the three key phase files.

### Human Verification Required

The following 6 items cannot be verified by automated tests. They require an interactive Qt session with a real X11 display and a MEFISTOX project directory. These are identical in character to the 6 deferred items in the 06.1-VERIFICATION.md (all 6 from 6.1 were also human_needed).

#### 1. Menu Bar Structure

**Test:** Launch `pp/ppelas_qt` in `$MEFISTOX/nafems_le1`. Observe the top-level menu bar.
**Expected:** Exactly four top-level menus: `File`, `Solve`, `View`, `Help`. `Mesh` absent (elas taxonomy differs from 6.1). `Edit` absent (D-01 inherited from 6.1).
**Why human:** Visual observation of live Qt window; offscreen QPA renders menus but label inspection requires on-display rendering to confirm.

#### 2. Bilingual Labels

**Test:** With `$MEFISTO/td/m/anglais` present, open the `Solve` menu. Without it, open the same menu.
**Expected:** EN: `"ELASTICITY INPUT DATA"` for code 2;; FR: `"ENTREE DONNEES PROBLEME ELASTICITE"`.
**Why human:** Requires MEFISTO environment with bilingual `td/m/` tree installed.

#### 3. Solve > Static solve click-to-Fortran dispatch

**Test:** Click `Solve > Static solve` (lexicon `3;`). Observe the console dock and Fortran prompt.
**Expected:** Console dock shows `[menu] 3;`; Fortran enters the STEADY ELASTICITY solver branch (dispatches to `resoelst` sub-menu equivalent). Type `99;` to exit.
**Why human:** Requires live Fortran dispatch loop in interactive mode; cannot stub in offscreen QTest (the QTest only confirms the queue receives `3;`, not that Fortran runs the solver branch).

#### 4. Window close-button exit (D-09)

**Test:** Restart `pp/ppelas_qt`. Click the window close button (X).
**Expected:** Console shows `[menu] 99;`; Fortran save path runs briefly; window closes; process exits 0 with no stuck window.
**Why human:** Requires GUI interaction with window manager close event.

#### 5. Typed `99;` equivalence with click-X (D-09 convergence)

**Test:** At the initial `pp/ppelas_qt` prompt, type `99;` without clicking.
**Expected:** Identical behavior to window-X close — typed and clicked paths converge at DONNMF. Proves shared `XvueWindow::closeEvent` (6.1 Plan 03) carries to the elas binary.
**Why human:** Requires interactive elas session with Fortran dispatch observable.

#### 6. Toolbar icon dispatch equivalence

**Test:** Click each of the 4 wired toolbar icons (Input data `2;`, Static solve `3;`, Draw stress `8;`, Draw mesh `10;`). Observe console dock.
**Expected:** Each toolbar button emits `[menu] <lex>` identically to the corresponding menu item. `addToolbarByLexicon` maps the audit top-5 set to the QToolBar.
**Why human:** Requires visual session; toolbar icons render only on live display, not offscreen QPA.

### Gaps Summary

No gaps. All 5 automated must-have truths verified. The 6 human verification items are interactive/visual checks that are expected deferred items for this phase (same disposition as 6.1). They do not block the automated contract.

---

_Verified: 2026-04-24T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
