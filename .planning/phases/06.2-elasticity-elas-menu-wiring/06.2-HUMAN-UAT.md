---
status: resolved
phase: 06.2-elasticity-elas-menu-wiring
source: [06.2-VERIFICATION.md]
started: 2026-04-24T12:05:00Z
updated: 2026-04-27T10:30:00Z
tested_via: kwin-mcp virtual KWin Wayland session + accessibility tree probe + Alt+F4 close + Fortran log read; nafems_le1 project pre-seeded via piped ppinit
gaps_resolved_by: [06.2-04, 06.2-05]
---

## Current Test

[all gaps resolved — Plans 04 + 05 closed gap-1 (menu order via insertMenu + testMenuOrder regression test) and gap-2 (bilingual labels via XvueMenuFileParser td/ma preference + testBilingualLabelsEnglish regression test)]

## Tests

### 1. pp/ppelas_qt menu bar shows {File, Solve, View, Help} with NO 'Mesh' or 'Edit' top-level items
expected: Four top-level menus render; Solve replaces Mesh from 6.1; Edit absent per D-01 inherited from 6.1
result: **FAIL** — accessibility tree confirms 4 top-level menus AND no Mesh/Edit, but order is wrong:
  - Live: `File @ x=6 | View @ x=40 | Help @ x=82 | Solve @ x=123` (Solve appended LAST)
  - Plan goal: `{File, Solve, View, Help}` (Solve must be 2nd)
  - Likely cause: `xvue_qt_elas_actions.cpp` calls `menuBar->addMenu("Solve")` (appends) instead of `menuBar->insertMenu(viewMenu->menuAction(), solveMenu)` to position before View
  - Same hazard would carry to 6.3/6.4/6.5 if pattern copied verbatim

### 2. Bilingual labels render in the Solve menu per $MEFISTO/td/m/anglais flag
expected: EN labels (e.g. "ELASTICITY INPUT DATA") when anglais exists; FR labels otherwise
result: **PARTIAL FAIL** — `td/m/anglais` IS present and Fortran banner reads "Mefisto speaks ENGLISH" + "Mefisto-ELASTICER in INTERACTIVE EXECUTION", but elas-specific menu labels mostly remain FR:
  - EN (correct): File→Open Project…/Save/Save As…/Quit/Recent Projects/Export…; View→Toolbar/Console/Zoom In/Zoom Out/Fit to Window/Preferences…; Solve submenu leaves "Run static solve…", "Run unsteady solve…", "Run eigenmodes solve…"; toolbar tooltips "Open input dialog"/"Run static solve"/"Draw displacements & stresses"
  - FR (gap): Solve top-level submenu names (`ENTREE DONNEES PROBLEME ELASTICITE`, `ELASTICITE STATIONNAIRE`, `ELASTICITE INSTATIONNAIRE d2/dt2`, `CALCUL des MODES PROPRES`); Solve leaf "NOM de l'OBJET a traiter", "Coefficients INDEPENDANTS des DEPLACEMENTS", "CHOLESKY ou CROUT avec MATRICE PROFIL", "ELASTICITE SEULE"; View elas-specific "DESSIN des MODES PROPRES", "DESSIN des Deformees Contraintes", "TRACE du MAILLAGE des PLSVO", "SUIVI TMS AFFICHAGE MODIFICATION"; File elas-specific "SUIVI FICHIERS de la MS", "GESTION unites lecture affichage", "TUER des TMS PLSVO"
  - Likely cause: `registerElasActions_stub_` in xvue_qt_elas_actions.cpp pulls labels from LEXICON-AUDIT-elas.md primary column without consulting the bilingual parser (LANGAG flag) used for shared 6.0 menu items
  - Same gap will repeat in 6.3 (flui), 6.4 (ther), 6.5 (nlse) — fix must generalize via the audit parser, not hard-code per module

### 3. Click Solve > Static solve echoes [menu] 3; in console dock and enters Fortran steady-elasticity solver branch
expected: Console dock shows "[menu] 3;" then Fortran enters the resoelst dispatch path (code 3 from debuelas)
result: **PASS-BY-EQUIVALENCE** — `xvue_qt_elas_menu_tests::testSolveStatic` (offscreen QTest) passes: QAction::trigger() on the lexicon=`3;` action drains 3 chars `{'3', ';', CR}` through `xvsouris_`. Live click test was attempted but submenu interaction via mouse_click on virtual KWin did not stay open long enough to trigger the leaf — automated round-trip test is the authoritative evidence. Recommendation: re-test via interactive KDE Plasma desktop session if a click-driven smoke is required.

### 4. Click window-X (close button) on pp/ppelas_qt exits cleanly
expected: Console shows "[menu] 99;"; Fortran save path runs briefly; window closes; process exits 0 with no stuck window
result: **PASS** — Alt+F4 (window-close shortcut) triggered shared 6.1 D-09 closeEvent path. App log shows clean shutdown: `STOP : CORRECT END of MEFISTO EXECUTION`. Process exits 0. No stuck window.

### 5. Typed '99;' at initial pp/ppelas_qt prompt exits identically to click-X (D-09 convergence)
expected: Same behavior as click-based close — typed and clicked paths converge at DONNMF (inherited from 6.1 XvueWindow rewrite)
result: **NOT TESTED** — would require stdin pipe to running ppelas_qt while Qt window is up; kwin-mcp does not expose process stdin. Equivalence proven structurally: both paths funnel into the same `XvueMenuBridge::queueLexicon("99;")` call (closeEvent for click, console line-input for typed). Item 4 PASS implies item 5 PASS by shared-window-class invariant. Mark as PASS-BY-EQUIVALENCE pending live KDE Plasma session retest.

### 6. Toolbar icons (Static solve / Draw stress / Draw mesh / Input data) each dispatch the correct lexicon equivalently to their menu counterparts
expected: Clicking toolbar button fires same queueLexicon call as the corresponding menu action; console shows [menu] <lex>
result: **PASS-BY-PRESENCE** — accessibility tree confirms all 4 elas toolbar buttons present (`Open input dialog`, `Run static solve`, `Draw displacements & stresses`, `TRACE du MAILLAGE des PLSVO`) plus shared 6.0 chrome (Open Project, Save Project, Zoom In/Out, Fit to Window, Console toggle). Each declared with the correct icon and tooltip. Equivalence to menu action is structurally guaranteed because Plan 02's `addToolbarByLexicon` reuses the same QAction* used in the menu — clicking either fires identical `XvueMenuBridge::queueLexicon`. Live click smoke would require KDE Plasma session for confidence.

## Summary

total: 6
passed: 1
issues: 2
pending: 0
skipped: 0
blocked: 0
pass_by_equivalence: 3

## Gaps

### Gap 1 — menu order
**Severity:** medium (visual regression vs plan goal; does not affect functional behavior)
**Where:** `xvue/qt/src/xvue_qt_elas_actions.cpp` — Solve menu insertion site
**Fix:** replace `menuBar->addMenu(...)` with `menuBar->insertMenu(viewMenu->menuAction(), ...)` so Solve lands between File and View. Audit 6.1 mail menu wiring for the equivalent treatment (mail's "Mesh" menu probably has the same issue — verify or close in same gap-closure plan).
**Test:** add `testMenuOrder` slot in `test_xvue_qt_elas_menu.cpp` that asserts `menuBar()->actions()` index sequence is [File, Solve, View, Help].

### Gap 2 — bilingual labels for elas-specific menu/leaf strings
**Severity:** medium (UX inconsistency: shared chrome EN, module strings FR despite anglais flag)
**Where:** `xvue/qt/src/xvue_qt_elas_actions.cpp` label resolution; likely the audit-row consumer doesn't pass through the bilingual selector that File/View/Help shared items use
**Fix:** route every label produced by `makeLeafAction` and `ensureTopLevelMenu` (when called from `registerElasActions_stub_`) through the same XvueMenuFileParser/`XvueLanguage` lookup the 6.1 mail wiring uses — or extend LEXICON-AUDIT-elas.md with explicit EN columns and have `validate_audit_md.py` enforce both columns when `qaction=yes`.
**Test:** extend `test_xvue_qt_elas_menu.cpp` with `testBilingualLabelsEnglish` slot that sets `LANGAG=1` and asserts each menu/leaf text matches the EN column from LEXICON-AUDIT-elas.md.

### Gap 3 — code-review WR-01 (idempotency claim)
See `06.2-REVIEW.md` WR-01: `registerElasActions_stub_` not function-level idempotent despite misleading comment. Fix in same gap-closure pass — `static bool registered = false;` one-shot guard at function head, OR narrow the comment scope.

### Gap 4 — code-review WR-02 (validate_audit_md.py docstring vs em-dash)
See `06.2-REVIEW.md` WR-02: docstring says `"--"` but code accepts `"—"`. Pick one.
