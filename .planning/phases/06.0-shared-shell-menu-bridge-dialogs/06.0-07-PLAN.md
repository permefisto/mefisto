---
phase: 06.0
plan: 07
type: execute
wave: 5
depends_on: [06.0-06]
files_modified:
  - .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md
  - xvue/qt/README.md
autonomous: false
requirements: [UX-01, UX-04, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12, UX-13]
tags: [qt6, validation, manual-ab, phase-closeout, dark-mode-human-gate, wave-5]

must_haves:
  truths:
    - "Clean rebuild via bin/cbl_tout_qt exits 0 with 58 ABI symbols."
    - "All 6 QTest binaries (xvue_qt_event_tests, xvue_qt_menu_tests, xvue_qt_i18n_menu_prefs_tests, xvue_qt_event_menu_predrain_tests, xvue_qt_dialogs_console_tests, xvue_qt_canvas_gestures_tests, xvue_qt_window_chrome_tests) PASS under xvfb-run."
    - "pp/ppmail_qt launches and renders the Phase 6.0 shell: menu bar with File/View/Help, toolbar with 7 actions, status bar with coord label, bottom console dock."
    - "Human visual verdict: empty-state canvas text 'No project open / Choose File → Open Project' appears when no project loaded."
    - "Human verdict: File→Open QFileDialog opens with MEFISTOX as start dir (if set); recent projects submenu populates after opening one."
    - "Human verdict: dark-mode toggle via Preferences dialog applies QPalette to chrome; canvas backing pixmap unchanged."
    - "Human verdict: Console dock captures at least one line of Fortran stdout during a pan2d interactive session (via the pipe-redirect + QSocketNotifier path)."
    - "Human verdict: *** ERREUR pattern — NOT testable without a real solver error; documented as a Plan 07 caveat; will be retested in 6.1 mesher wiring."
    - "Human verdict: QSettings round-trip — close pp/ppmail_qt with the window at a specific position; re-launch; window restores position."
    - "Human verdict: F9 toggles console dock visibility; Ctrl+0 resets canvas view; Ctrl+Q prompts quit-confirm dialog."
    - "xvue/qt/README.md extended with Phase 6.0 architecture section + 'Phase 6.1..6.5 integration contract' reference."
    - "06.0-VALIDATION.md: nyquist_compliant: true; wave_0_complete: true; all Per-Task Verification Map rows green; Approval: approved by drico."
  artifacts:
    - path: ".planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md"
      provides: "Closed validation document with all QTest rows green + manual A/B log + Checker Dimensions 1-6 sign-off"
    - path: "xvue/qt/README.md"
      provides: "Phase 6.0 architecture section describing menu bridge, i18n, console dock, preferences, dark-mode, canvas gestures, module init hook, QSettings namespace"
  key_links:
    - from: "pp/ppmail_qt launch"
      to: "menu bridge + console dock + status bar"
      via: "xvue_module_init_('mail') → XvuePrefs/colorScheme/installStdoutRedirect → registerMailActions_stub → shell fully rendered"
      pattern: "xvue_module_init_"
    - from: "human visual verdict"
      to: "UI-SPEC Dimensions 1-6"
      via: "manual sweep per UI-SPEC §Conformance Summary"
      pattern: "Conformance Summary"
---

<objective>
Close Phase 6.0. Run the complete QTest regression suite (7 test binaries), perform a manual A/B sweep on `pp/ppmail_qt` verifying UI-SPEC Dimensions 1-6, sign off `06.0-VALIDATION.md`, and extend `xvue/qt/README.md` with the Phase 6.0 architecture section + the 6.1..6.5 integration contract.

Purpose: Phase 6.0 closes when:
1. All automated tests green (nyquist_compliant gate).
2. Human visual verdict approved for the shared shell (menu/toolbar/status/dock render correctly + dark-mode palette + QSettings round-trip + console captures stdout + empty-state text).
3. Documentation updated so 6.1..6.5 planners know the override points.

This plan has a `checkpoint:human-verify` task that pauses execution for the user to drive `pp/ppmail_qt` and confirm the behaviors listed above. Autonomous tasks handle the clean rebuild and the validation-document closure.

Output:
- `.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-07-SUMMARY.md` — phase-close summary
- Updated `06.0-VALIDATION.md` with nyquist_compliant:true + approval date
- Extended `xvue/qt/README.md` with Phase 6.0 section
</objective>

<execution_context>
@/home/drico/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/drico/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-CONTEXT.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-RESEARCH.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-01-SUMMARY.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-02-SUMMARY.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-03-SUMMARY.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-04-SUMMARY.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-05-SUMMARY.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-06-SUMMARY.md
@xvue/qt/README.md
@.planning/phases/05-event-bridge-blocking-reads/05-VALIDATION.md

<interfaces>
Phase close pattern mirrors Phase 5 Plan 06:
- Automated evidence first (clean rebuild + QTest suite).
- Human visual verdict second.
- Documentation + VALIDATION.md sign-off third.

README.md structure to extend:
- Add section "## Phase 6.0 — Shared shell, menu bridge, dialogs" AFTER the existing "## Phase 5 — Event bridge" section.
- Cover: XvueMenuBridge architecture + queue-drain pattern, i18n compile-time table, XvuePrefs QSettings namespace, XvueConsoleDock stdout pipe, dialogs (About/Preferences/RecentProjects), dark-mode via QPalette, canvas gestures (wheel/middle-drag/context-menu), xvue_module_init_ ABI hook + 6.1..6.5 override contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean rebuild + run all 7 QTest binaries + capture results</name>
  <files>
    .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-07-test-log.txt (new, temp artifact)
  </files>
  <read_first>
    bin/cbl_tout_qt (top-level Qt build script),
    xvue/qt/tests/CMakeLists.txt (all 6 test target names declared across Plans 01-06 + Phase 5's 7th)
  </read_first>
  <action>
1. Clean rebuild:
   ```sh
   cd /home/drico/git/mefisto
   rm -rf xvue/qt/build
   bin/cbl_tout_qt 2>&1 | tee .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-07-build-log.txt
   ```
2. Verify clean-build exit code 0 + ABI count 58:
   ```sh
   nm xvue/qt/build/libxvueqt.a | grep ' T [a-zA-Z_][a-zA-Z0-9_]*_$' | grep -vc ' T _Z'
   # Expected: 58
   ```
3. Run each QTest binary under xvfb-run and capture pass/fail counts:
   ```sh
   TEST_LOG=.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-07-test-log.txt
   : > "$TEST_LOG"
   for bin in xvue_qt_event_tests \
              xvue_qt_menu_tests \
              xvue_qt_i18n_menu_prefs_tests \
              xvue_qt_event_menu_predrain_tests \
              xvue_qt_dialogs_console_tests \
              xvue_qt_canvas_gestures_tests \
              xvue_qt_window_chrome_tests; do
       echo "=== $bin ===" | tee -a "$TEST_LOG"
       xvfb-run -a ./xvue/qt/build/tests/$bin 2>&1 | tee -a "$TEST_LOG" | tail -3
   done
   ```
4. Confirm every binary's final line says "Totals: N passed, 0 failed, 0 skipped" (or QSKIPs are only the pre-existing Phase 5 ones).
5. Sanity check legacy X11 build still works:
   ```sh
   bin/cbl_tout 2>&1 | tail -5
   ```
6. Record the summary into 06.0-07-test-log.txt (pass count per binary + build time + ABI count).
  </action>
  <verify>
    <automated>cd /home/drico/git/mefisto && test -f xvue/qt/build/libxvueqt.a && test "$(nm xvue/qt/build/libxvueqt.a | grep ' T [a-zA-Z_][a-zA-Z0-9_]*_$' | grep -vc ' T _Z')" = "58" && grep -c '0 failed' .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-07-test-log.txt</automated>
  </verify>
  <acceptance_criteria>
    1. `bin/cbl_tout_qt` clean rebuild exits 0.
    2. ABI count = 58 (Plan 01 bump still holds).
    3. All 7 QTest binaries report "0 failed" in their summary lines.
    4. No test failure logs exist in `06.0-07-test-log.txt`.
    5. `bin/cbl_tout` (legacy X11) still exits 0 (BUILD-07 invariant preserved across Phase 6.0).
  </acceptance_criteria>
  <done>Full automated regression suite green. 58-symbol ABI. Legacy X11 build preserved. Ready for human visual verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual A/B sweep on pp/ppmail_qt (10-criteria manual verdict)</name>
  <what-built>
Phase 6.0 shared shell: `pp/ppmail_qt` launches with:
- `QMainWindow` with menu bar (File / View / Help — 13 QActions), toolbar (7 actions), status bar (coord label at right), console dock (bottom).
- Empty-state text "No project open / Choose File → Open Project (Ctrl+O) to get started." centered on the canvas until `xvue_module_init_` is called.
- Dark-mode Preferences switch: Preferences dialog (Ctrl+,) has a color-scheme combo (System/Light/Dark).
- File menu: Open Project (Ctrl+O, opens QFileDialog starting at $MEFISTOX), Save Project (Ctrl+S, v1 placeholder), Save As (Ctrl+Shift+S), Recent Projects submenu (populates after Open), Export (disabled), Quit (Ctrl+Q).
- View menu: Toolbar toggle, Console toggle (F9), Zoom In/Out (Ctrl+±), Fit to Window (Ctrl+0), Preferences (Ctrl+,).
- Help menu: Documentation (F1 → $MEFISTO/doc/), About MEFISTO (QMessageBox::about with Perronnet credits).
- Canvas gestures: wheel zoom, middle-drag pan, right-click context menu (empty in 6.0 — 6.1+ populates).
- Modal guard: pressing Ctrl+O during a Fortran blocking call shows "Finish current operation first (type 99;)" in the status bar for 3 seconds.
- QSettings persistence: window geometry + state + dock visibility + color scheme survive process restart.
- Console dock: captures Fortran stdout via in-process pipe + QSocketNotifier; *** ERREUR lines would trigger QMessageBox::warning (not testable without a real solver error — documented caveat).
- Bilingual FR/EN via `$MEFISTO/td/m/anglais` flag file presence.
  </what-built>
  <how-to-verify>
    Open a terminal, set env vars per CLAUDE.md, and drive `pp/ppmail_qt`:

    1. **Launch + empty-state:**
       ```sh
       export MEFISTO=/home/drico/git/mefisto
       export MEFISTOX=$HOME/mefistox        # create $HOME/mefistox if absent
       cd $MEFISTO
       pp/ppmail_qt &
       ```
       Verify:
       - A 1024x768 window titled "MEFISTO" opens.
       - Menu bar shows: File / View / Help (three menus left-aligned).
       - Toolbar under menu bar has ≥7 icon buttons.
       - Status bar at bottom shows "X: 0  Y: 0" at the right.
       - Console dock is visible at the bottom.
       - Canvas shows "No project open" centered, and below it "Choose File → Open Project (Ctrl+O) to get started."

    2. **Canvas empty-state flips off:**
       After the mesher starts its interactive loop (calls xvue_module_init_('mail')), the empty-state text should disappear on next paint.

    3. **Dark-mode toggle:**
       - View → Preferences (Ctrl+,) → Appearance combo → Dark → OK.
       - Verify: menu bar, toolbar, status bar, dock, dialog chrome all darken.
       - Verify: canvas pixels UNCHANGED (if any are drawn — open a project first if you want to see a colored colormap; otherwise the backing is still black).
       - Open Preferences again → Appearance → System → OK. Chrome returns to system theme.

    4. **QSettings round-trip:**
       - Drag the window to the top-left corner, resize to ~1400x900.
       - Close the window (File → Quit → OK, or click X).
       - Relaunch `pp/ppmail_qt`.
       - Verify: window reopens at ~1400x900 at top-left.
       - Also: if you hid the console dock before quitting, it should stay hidden after relaunch.

    5. **File → Open Project:**
       - File → Open Project (Ctrl+O). QFileDialog appears, default dir is `$MEFISTOX` (or `$HOME` with a "MEFISTOX env var unset" status-bar warning if MEFISTOX is empty).
       - Cancel the dialog. No crash. No entry added to Recent Projects.
       - Open it again, select a valid directory, click Open.
       - Verify: File → Recent Projects submenu now has the selected directory as its first entry.

    6. **Recent Projects → Clear Recent:**
       - File → Recent Projects → Clear Recent Projects → see the "Clear Recent Projects?" confirm dialog with OK+Cancel.
       - Click Cancel. The recent list is unchanged.
       - Click Clear Recent Projects again → OK. The list clears (submenu shows "(none)" or similar).

    7. **View → Fit to Window:**
       - Scroll the mouse wheel over the canvas to zoom in (if any content).
       - Ctrl+0 or View → Fit to Window. View resets.

    8. **View → Console toggle:**
       - F9 hides the console dock. F9 again shows it.
       - Alternatively: View → Console ✓ → uncheck → dock hides.

    9. **Console dock captures stdout:**
       - In the typed lexicon (after mesher starts), type `5;` ENTER. Some Fortran output should flow into the console dock.
       - If no Fortran output is triggered by the commands available in the bare shell, this verification is deferred to the 6.1 mesher plan's manual A/B.

    10. **Help → About:**
        - Help → About MEFISTO. QMessageBox::about shows. Verify body contains "Alain Perronnet", "LJLL", "UPMC Paris", and a version string + Qt version + build date.
        - Close the box.

    11. **Help → Documentation:**
        - F1 or Help → Documentation. The system's default viewer should open a file-URL pointing to `$MEFISTO/doc/`. On this machine, `doc/` is a symlink to `doca/` or `docf/`. Verify a viewer opens (contents are out of scope — may just be a directory listing).

    12. **Ctrl+Q quit confirm:**
        - Ctrl+Q → dialog "Quit MEFISTO? Any unsaved work will be lost." with OK+Cancel.
        - Click Cancel. Window stays open.
        - Click Ctrl+Q again → OK. Window closes cleanly (no crash).

    13. **Typed lexicon preservation (critical D-04 + UX-02/03 check):**
        - Launch pp/ppmail_qt again and enter its mesher loop.
        - Type `99;` in the canvas area. Verify the mesher responds as it would on the legacy X11 backend (should exit the module).
        - This confirms D-04: plain chars still reach Fortran via XvueEventBridge, not stolen by QShortcut.

    14. **Menu → Fortran via XvueMenuBridge (critical UX-03 check):**
        - 6.0 ships no module-specific menu items that queue lexicon strings. So this wiring is NOT end-to-end verifiable in 6.0 — it becomes verifiable in 6.1 when registerMailActions wires a QAction to queueLexicon("5;90;"). Documented as a Plan 07 caveat.

    **Verdict criteria** (all must be YES for approval):
    - [ ] Menu/toolbar/status/dock all render correctly.
    - [ ] Empty-state canvas text appears at launch, disappears after module loads.
    - [ ] Dark-mode Preferences switch re-tints chrome; canvas backing unchanged.
    - [ ] QSettings geometry round-trip works across restart.
    - [ ] File→Open opens QFileDialog in MEFISTOX dir; recent projects populates.
    - [ ] Clear Recent shows destructive-confirm dialog.
    - [ ] Help→About shows Alain Perronnet credits.
    - [ ] Ctrl+Q prompts quit-confirm.
    - [ ] F9 toggles console dock.
    - [ ] Typed `99;` still reaches Fortran (typed-lexicon preserved).

    **Deferred to 6.1..6.5 manual verification** (NOT blocking for 6.0 approval):
    - *** ERREUR QMessageBox surfacing (requires a real solver error path).
    - QAction → XvueMenuBridge → Fortran end-to-end (requires registerMailActions body).
    - Module-specific menu items (per-module audit + wiring).
    - Middle-drag pan during blocking read vs Phase 5 MMB abort (A6 audit finding in Plan 03 informs this — document which was chosen).
  </how-to-verify>
  <resume-signal>Type `approved` if all criteria above are YES. Type `blocked: <reason>` if any is NO — the orchestrator will reopen Phase 6.0 with a gap-closure plan for the blocker.</resume-signal>
  <files>(human-interactive — no files modified during this checkpoint)</files>
  <action>Human tester drives pp/ppmail_qt per the how-to-verify steps above. This is a `checkpoint:human-verify` task; Claude does not execute code here. Claude reads the tester's resume-signal and logs the verdict into 06.0-VALIDATION.md Manual A/B Log (handled by Task 3).</action>
  <verify>Human verdict captured via resume-signal `approved` or `blocked: <reason>`.</verify>
  <done>Tester has exercised all 10 criteria in the how-to-verify block and signaled approval. Claude updates Manual A/B Log in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Close 06.0-VALIDATION.md + extend xvue/qt/README.md with Phase 6.0 architecture section</name>
  <files>
    .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md,
    xvue/qt/README.md
  </files>
  <read_first>
    .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md (current draft; needs Wave 0 gaps, Per-Task Verification Map rows, sign-off),
    .planning/phases/05-event-bridge-blocking-reads/05-VALIDATION.md (Phase 5 closed template to mirror),
    xvue/qt/README.md (existing Phase 5 section — extend with Phase 6.0 section after it),
    .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-RESEARCH.md §Validation Architecture + §Dependency Order
  </read_first>
  <action>
**3a. Edit 06.0-VALIDATION.md** — flip to closed/approved:

```yaml
# Frontmatter:
# ---
phase: 06.0
slug: shared-shell-menu-bridge-dialogs
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-19
approved_by: drico
approved_date: <today's date>
# ---
```

Body:
- Test Infrastructure block: framework=QTest; quick command = `./xvue/qt/build/tests/xvue_qt_menu_tests`; full suite command = concatenation of all 7 test binaries under xvfb-run; estimated runtime ≈ 60s.
- Sampling Rate block: per-task (single binary <5s), per-wave (all 7 <60s), phase gate (full suite green + human verdict).
- Per-Task Verification Map: one row per task across Plans 01-06, all status ✅. Pull requirement/threat IDs from each plan's frontmatter. Total ~16 task rows.
- Wave 0 Requirements: all ticked (Plan 01 delivered).
- Manual-Only Verifications: 3 rows — dark-mode palette visual verdict (approved Plan 07); QSettings round-trip (approved); *** ERREUR MessageBox (DEFERRED to 6.1 since no solver errors surface in bare shell).
- Validation Sign-Off: all boxes checked; Approval: approved <date> (drico).

**3b. Extend xvue/qt/README.md** — append a "## Phase 6.0 — Shared shell, menu bridge, dialogs, persistence" section AFTER the existing "## Phase 5 — Event bridge" section. Content should cover:

1. Scope: QMenuBar/QToolBar/QStatusBar/QDockWidget chrome + XvueMenuBridge queue pattern + QSettings persistence + dark-mode palette + bilingual i18n + 6.1..6.5 override contract.
2. Files added (9 new pairs: xvue_qt_menu_bridge.{h,cpp}, xvue_qt_i18n, xvue_qt_prefs, xvue_qt_console_dock, xvue_qt_error_batcher, xvue_qt_about_dialog, xvue_qt_preferences, xvue_qt_recent_projects, xvue_qt_shortcut_audit).
3. ABI bump: 57 → 58 (xvue_module_init_).
4. `XvueMenuBridge` queue-drain architecture (mermaid or ASCII diagram):
   ```
   QAction::triggered
       -> XvueMenuBridge::queueLexicon("5;90;")
       -> pendingChars_ enqueues {'5', ';', '9', '0', ';', 13}
       -> (next xvsouris_ call)
       -> XvueEventBridge::waitForEvent pre-drain
       -> popChar() returns '5'
       -> Result{notypeevent=2, nbc='5'} -> Fortran SACLAV
       -> (next call) -> ';' ... until CR=13 flushes KLG line to DONNMF
   ```
5. `XvueConsoleDock` stdout pipe: `freopen`-based dup2 + setvbuf(_IOLBF) + F_SETPIPE_SZ(1MB) + QSocketNotifier. Line-buffered; *** ERREUR batched 500ms.
6. `XvuePrefs` QSettings: organization=LJLL, application=mefisto, per-module keygroup, shared `ui/color-scheme`. File: `$HOME/.config/LJLL/mefisto.conf`.
7. i18n compile-time table: `MsgId` enum + `kTable` array + `xvueIsEnglish()` reading `$MEFISTO/td/m/anglais` once per process.
8. Dark-mode: `QPalette` via `XvueApp::applyColorSchemePreference`; `QStyleHints::colorSchemeChanged` auto re-apply on system toggle.
9. Canvas gestures: wheel 1.15^notches clamped [0.1,10.0], middle-drag pan (Qt-side transform, NOT dispatched to Fortran), right-click context menu suppressed during blocking reads.
10. **6.1..6.5 integration contract:**
    - Each per-module phase adds `void registerMailActions(XvueWindow*, XvueMenuBridge*)` (or elas/flui/ther/nlse analog) as a stronger symbol overriding the 6.0 stub.
    - Each phase adds `CALL XVUE_MODULE_INIT('mail')` to `prpr/ppXxx.f` BEFORE any graphics call.
    - Each phase populates `.planning/phases/06.X-.../LEXICON-AUDIT-<module>.md` per the 6.0 template schema.
    - Each phase may append SVG icons to `xvue/qt/resources/xvue_icons.qrc` (enforced SVG-only by `verify_icon_source.sh`).
    - Shortcuts must have a modifier or be F-keys (enforced by `verify_shortcut_modifiers.sh`).
11. Acknowledge the deferred items (from Plan 07 human verify):
    - *** ERREUR QMessageBox surfacing — to be verified in 6.1 with a real mesher error path.
    - QAction → queueLexicon end-to-end — 6.1 mesher phase adds the first real menu-queued lexicon action.
    - MMB pan vs Phase 5 MMB abort — behavior in v1 per Plan 03 A6 audit verdict (cite the AUDIT-A6.md line).

**3c. Create the Plan 07 summary at 06.0-07-SUMMARY.md:**

```markdown
# ---
phase: 06.0-shared-shell-menu-bridge-dialogs
plan: 07
subsystem: ui
tags: [qt6, validation, phase-closeout, human-verify]
requires:
  - phase: 06.0-01
    provides: scaffold (9 stub pairs + ABI 58 + lint scripts + test target)
  - phase: 06.0-02
    provides: i18n + menu-bridge + prefs bodies
  - phase: 06.0-03
    provides: event-bridge pre-drain + A6 audit
  - phase: 06.0-04
    provides: console dock + error batcher + about/preferences/recent dialogs
  - phase: 06.0-05
    provides: canvas gestures + view_transform + empty-state + mouseCoords signal
  - phase: 06.0-06
    provides: XvueApp accessors + XvueWindow chrome composition + xvue_module_init_ dispatch
provides:
  - Phase 6.0 closure: manual A/B approved by drico
  - xvue/qt/README.md extended with Phase 6.0 architecture + 6.1..6.5 contract
  - 06.0-VALIDATION.md nyquist_compliant: true, approved
duration: <actual>
completed: <today's date>
# ---

# Phase 6.0 Plan 07: Phase closure

Manual A/B sweep approved. All 7 QTest binaries green. ABI 58. Both backends (Qt + X11)
still build. Documentation extended. 6.1..6.5 integration contract published.
```
  </action>
  <verify>
    <automated>cd /home/drico/git/mefisto && grep -q 'nyquist_compliant: true' .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md && grep -q 'approved_by:' .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md && grep -q 'Phase 6.0' xvue/qt/README.md && test -f .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-07-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    1. `06.0-VALIDATION.md` frontmatter has `nyquist_compliant: true`, `wave_0_complete: true`, `approved_by:`, `approved_date:`.
    2. `06.0-VALIDATION.md` Per-Task Verification Map has no ⬜ pending or ❌ red rows.
    3. `xvue/qt/README.md` has a new section titled "## Phase 6.0 — Shared shell, menu bridge, dialogs, persistence".
    4. That section references all 9 new source files + the menu-queue drain diagram + 6.1..6.5 integration contract.
    5. `06.0-07-SUMMARY.md` exists with frontmatter + body summarizing the phase close.
  </acceptance_criteria>
  <done>Phase 6.0 closes. Automated tests green; human verdict approved; documentation extended; 6.1..6.5 planners have a clear integration contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester (drico) | User-driven interactive session; trusted operator. |
| Manual A/B Log rows | Subjective verdicts — documented, not auditable by code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.0-07-01 | Tampering | Stale test binary from previous build | mitigate | Clean rebuild (`rm -rf xvue/qt/build`) in Task 1 before running test suite. |
| T-06.0-07-02 | DoS | xvfb-run hangs on a test | accept | Phase 5 validated xvfb-run pattern; 6.0 tests follow same structure; any hang would be a regression surfaced by Task 1. |
| T-06.0-07-03 | Spoofing | Human verdict approved without actual testing | accept | Plan 07 is an honor-system checkpoint; CLAUDE.md working rules require the human to actually drive the app. The ASK is auditable via the 06.0-07-test-log.txt + VALIDATION.md log. |
</threat_model>

<verification>
- Task 1 automated: clean build + all 7 test binaries + legacy X11 build all exit 0.
- Task 2 human: 10 criteria checklist approved or blocked.
- Task 3 automated: VALIDATION.md + README.md + SUMMARY.md all land with expected content.
</verification>

<success_criteria>
- [ ] `bin/cbl_tout_qt` clean rebuild exits 0 + ABI count 58
- [ ] All 7 QTest binaries (Phase 5's event_tests + 6 new ones) pass under xvfb-run
- [ ] `bin/cbl_tout` (legacy X11) still exits 0
- [ ] Human verdict on 10-item manual sweep: approved by drico
- [ ] `06.0-VALIDATION.md` frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, `approved_by: drico`, `approved_date` set
- [ ] Per-Task Verification Map: all rows green (✅)
- [ ] `xvue/qt/README.md` extended with Phase 6.0 section + 6.1..6.5 contract
- [ ] `06.0-07-SUMMARY.md` exists with frontmatter + body
- [ ] Deferred items documented: *** ERREUR MessageBox + QAction-to-Fortran end-to-end + MMB pan vs abort clarified
</success_criteria>

<output>
After completion, `.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-07-SUMMARY.md` exists. Update `.planning/STATE.md` to mark Phase 6.0 complete and point the next-phase focus at Phase 6.1 (mesher menu wiring).
</output>
