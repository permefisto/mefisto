---
phase: 06.0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xvue/qt/CMakeLists.txt
  - xvue/qt/cmake/verify_abi.sh
  - xvue/qt/cmake/verify_shortcut_modifiers.sh
  - xvue/qt/cmake/verify_icon_source.sh
  - xvue/qt/resources/xvue_icons.qrc
  - xvue/qt/include/xvue_qt_api.h
  - xvue/qt/src/xvue_qt_api.cpp
  - xvue/qt/src/xvue_qt_menu_bridge.h
  - xvue/qt/src/xvue_qt_menu_bridge.cpp
  - xvue/qt/src/xvue_qt_i18n.h
  - xvue/qt/src/xvue_qt_i18n.cpp
  - xvue/qt/src/xvue_qt_prefs.h
  - xvue/qt/src/xvue_qt_prefs.cpp
  - xvue/qt/src/xvue_qt_console_dock.h
  - xvue/qt/src/xvue_qt_console_dock.cpp
  - xvue/qt/src/xvue_qt_error_batcher.h
  - xvue/qt/src/xvue_qt_error_batcher.cpp
  - xvue/qt/src/xvue_qt_about_dialog.h
  - xvue/qt/src/xvue_qt_about_dialog.cpp
  - xvue/qt/src/xvue_qt_preferences.h
  - xvue/qt/src/xvue_qt_preferences.cpp
  - xvue/qt/src/xvue_qt_recent_projects.h
  - xvue/qt/src/xvue_qt_recent_projects.cpp
  - xvue/qt/src/xvue_qt_shortcut_audit.h
  - xvue/qt/src/xvue_qt_shortcut_audit.cpp
  - xvue/qt/tests/CMakeLists.txt
  - xvue/qt/tests/test_xvue_qt_menu_scaffold.cpp
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-06, UX-07, UX-08, UX-09, UX-10, UX-11, UX-12, UX-13]
tags: [qt6, scaffold, abi, wave-0, test-infrastructure]

must_haves:
  truths:
    - "Fresh bin/cbl_tout_qt clean build succeeds with 58 Fortran-facing ABI entries (was 57)."
    - "xvue_module_init_ extern \"C\" entry point links with a warn-once stub body (no dispatch yet)."
    - "Nine new .cpp source files compile into libxvueqt.a with empty/stub bodies (i18n, menu-bridge, prefs, console-dock, error-batcher, about-dialog, preferences, recent-projects, shortcut-audit)."
    - "New QTest target xvue_qt_menu_tests builds, links, and runs one sanity test under xvfb-run in <5s."
    - "Two new lint scripts verify_shortcut_modifiers.sh and verify_icon_source.sh exist, are executable, and exit 0 on the current tree."
    - "verify_abi.sh expected-count bumped to 58; build fails if ABI drifts."
    - "Empty xvue/qt/resources/xvue_icons.qrc registers via qt_add_resources with zero files (ready for 6.1..6.5 to append)."
  artifacts:
    - path: "xvue/qt/src/xvue_qt_menu_bridge.h"
      provides: "XvueMenuBridge class decl (QObject), QQueue<char> pendingChars_, queueLexicon(QString), popChar()->std::optional<char>, markModuleRegistered, hasRegisteredModule, populateContextMenu, setContextMenuPopulator"
    - path: "xvue/qt/src/xvue_qt_i18n.h"
      provides: "enum class MsgId (38 entries per UI-SPEC §Copywriting), xvueIsEnglish(), tr(MsgId), xvueT(MsgId)"
    - path: "xvue/qt/src/xvue_qt_prefs.h"
      provides: "XvuePrefs static API — initialize(module), window geometry/state, consoleVisible, recentProjects list, colorScheme"
    - path: "xvue/qt/src/xvue_qt_console_dock.h"
      provides: "XvueConsoleDock : QDockWidget with installStdoutRedirect, appendLine, onPipeReadable slot"
    - path: "xvue/qt/src/xvue_qt_error_batcher.h"
      provides: "XvueErrorBatcher : QObject with enqueue(QString) + 500ms timer + D-08 deferral"
    - path: "xvue/qt/src/xvue_qt_about_dialog.h"
      provides: "XvueAboutDialog::show(QWidget* parent) static launcher — wraps QMessageBox::about"
    - path: "xvue/qt/src/xvue_qt_preferences.h"
      provides: "XvuePreferencesDialog : QDialog"
    - path: "xvue/qt/src/xvue_qt_recent_projects.h"
      provides: "XvueRecentProjectsMenu : QMenu populator + refresh()"
    - path: "xvue/qt/src/xvue_qt_shortcut_audit.h"
      provides: "XvueShortcutAudit::validateNoCollisions(QWidget*) static"
    - path: "xvue/qt/cmake/verify_shortcut_modifiers.sh"
      provides: "build-time lint — fails if QKeySequence(\"[a-zA-Z0-9]\") plain-char shortcut found"
    - path: "xvue/qt/cmake/verify_icon_source.sh"
      provides: "build-time lint — fails if PNG/BMP/JPG present under xvue/qt/resources/icons/"
    - path: "xvue/qt/resources/xvue_icons.qrc"
      provides: "empty Qt resource file with prefix=/xvue/qt/icons — 6.1..6.5 append entries"
    - path: "xvue/qt/tests/test_xvue_qt_menu_scaffold.cpp"
      provides: "QTest skeleton + scaffold sanity test for xvue_qt_menu_tests target"
  key_links:
    - from: "xvue/qt/CMakeLists.txt"
      to: "9 new src/xvue_qt_*.cpp files"
      via: "add_library(xvueqt STATIC ...) target_sources addition"
      pattern: "xvue_qt_(menu_bridge|i18n|prefs|console_dock|error_batcher|about_dialog|preferences|recent_projects|shortcut_audit)"
    - from: "xvue/qt/CMakeLists.txt"
      to: "verify_shortcut_modifiers.sh + verify_icon_source.sh"
      via: "add_custom_target(verify_shortcut_modifiers ALL) + add_custom_target(verify_icon_source ALL)"
      pattern: "add_custom_target.*verify_(shortcut_modifiers|icon_source)"
    - from: "xvue/qt/include/xvue_qt_api.h"
      to: "xvue_qt_api.cpp xvue_module_init_ stub"
      via: "extern \"C\" void proc(xvue_module_init)(const char*, int*)"
      pattern: "xvue_module_init_"
    - from: "xvue/qt/cmake/verify_abi.sh"
      to: "libxvueqt.a"
      via: "nm grep | wc -l count check (expected 58)"
      pattern: "NM_COUNT.*58"
---

<objective>
Wave 0 scaffold for Phase 6.0: create every new file, header, CMake target, and test target that downstream plans 02/03/04/05/06 will extend. This plan writes STUBS and SKELETONS only — it declares all interfaces so that Wave 2 plans 02/04/05 can execute in parallel without exploring the codebase, and Wave 3 plan 03 can edit event.cpp against a stable menu-bridge API.

Purpose: Eliminate the "scavenger hunt" anti-pattern. Every class body in Phase 6.0 is declared here; every downstream plan fills in behavior. Also bumps the ABI count from 57 → 58 by adding the `xvue_module_init_` extern "C" entry point (warn-once stub body; real dispatch lands in Plan 06).

Output:
- 9 new xvue/qt/src/ source+header pairs (stubs)
- 1 new extern "C" ABI entry (xvue_module_init_) in xvue_qt_api.h + xvue_qt_api.cpp
- 1 new test target xvue_qt_menu_tests with one sanity test
- 2 new lint scripts (verify_shortcut_modifiers.sh, verify_icon_source.sh)
- 1 new empty Qt resource file (xvue_icons.qrc)
- CMakeLists.txt extensions for all above
- verify_abi.sh expected count 57→58
</objective>

<execution_context>
@/home/drico/git/mefisto/.claude/get-shit-done/workflows/execute-plan.md
@/home/drico/git/mefisto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-CONTEXT.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-RESEARCH.md
@.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-VALIDATION.md
@.planning/phases/05-event-bridge-blocking-reads/05-06-SUMMARY.md
@xvue/qt/README.md
@xvue/qt/CMakeLists.txt
@xvue/qt/cmake/verify_abi.sh
@xvue/qt/tests/CMakeLists.txt
@xvue/qt/tests/test_xvue_qt_event.cpp
@xvue/qt/tests/test_helpers.h
@xvue/qt/include/xvue_qt_api.h
@xvue/qt/src/xvue_qt_app.h
@xvue/qt/src/xvue_qt_window.h
@xvue/qt/src/xvue_qt_event.h

<interfaces>
<!-- Reference types/APIs the scaffold must match so downstream plans compile cleanly. -->

From xvue/qt/src/xvue_qt_app.h (existing — DO NOT MODIFY):
```cpp
class XvueApp {
public:
    static void ensure();
    static QApplication* qapp();
    static std::unique_ptr<XvueWindow>& window_slot();
    static int blockingDepth();
    // Phase 6 will add: static XvueMenuBridge* menuBridge();
    // Phase 6 will add: static void applyColorSchemePreference();
    // Those additions happen in Plan 06, NOT this scaffold plan.
};
```

From xvue/qt/src/xvue_qt_event.h (existing — DO NOT MODIFY):
```cpp
struct BlockingDepthGuard { ... };  // RAII on XvueApp::blockingDepth_
class XvueEventBridge : public QObject { ... Result waitForEvent(WaitMode, int*, int*); ... };
```

From UI-SPEC §Copywriting Contract (authoritative source for MsgId enum):
- 38 entries: AppName, FileMenuTitle, FileOpen, FileOpenTip, FileSave, FileSaveTip,
  FileSaveAs, FileRecentSubmenu, FileRecentClear, FileExport, FileQuit, FileQuitTip,
  ViewMenuTitle, ViewToolbar, ViewConsole, ViewZoomIn, ViewZoomOut, ViewFit,
  ViewPreferences, HelpMenuTitle, HelpDocumentation, HelpAbout, ConsoleTitle,
  StatusCoordFormat, ModalRefuse, ErrorMsgBoxTitle, QuitConfirmTitle, QuitConfirmBody,
  AboutTitle, AboutBody, OpenProjectDialogTitle, OpenProjectFilter,
  PreferencesTitle, PrefRecentCountLabel, PrefConsoleDefaultLabel, PrefColorSchemeLabel,
  PrefColorSchemeSystem, PrefColorSchemeLight, PrefColorSchemeDark,
  ButtonOk, ButtonCancel, ButtonApply, ButtonClose, ButtonQuit,
  DestructiveConfirmBodyGeneric, FileRecentClearConfirmTitle,
  EmptyStateHeading, EmptyStateBody,
  _Count_ (sentinel)
(actual count is 47 + sentinel; planner-declared here so the enum width is locked.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 9 class stub headers+sources, xvue_icons.qrc, ABI bump (57→58), lint scripts, test target skeleton</name>
  <files>
    xvue/qt/src/xvue_qt_menu_bridge.h, xvue/qt/src/xvue_qt_menu_bridge.cpp,
    xvue/qt/src/xvue_qt_i18n.h, xvue/qt/src/xvue_qt_i18n.cpp,
    xvue/qt/src/xvue_qt_prefs.h, xvue/qt/src/xvue_qt_prefs.cpp,
    xvue/qt/src/xvue_qt_console_dock.h, xvue/qt/src/xvue_qt_console_dock.cpp,
    xvue/qt/src/xvue_qt_error_batcher.h, xvue/qt/src/xvue_qt_error_batcher.cpp,
    xvue/qt/src/xvue_qt_about_dialog.h, xvue/qt/src/xvue_qt_about_dialog.cpp,
    xvue/qt/src/xvue_qt_preferences.h, xvue/qt/src/xvue_qt_preferences.cpp,
    xvue/qt/src/xvue_qt_recent_projects.h, xvue/qt/src/xvue_qt_recent_projects.cpp,
    xvue/qt/src/xvue_qt_shortcut_audit.h, xvue/qt/src/xvue_qt_shortcut_audit.cpp,
    xvue/qt/resources/xvue_icons.qrc,
    xvue/qt/include/xvue_qt_api.h,
    xvue/qt/src/xvue_qt_api.cpp,
    xvue/qt/cmake/verify_shortcut_modifiers.sh,
    xvue/qt/cmake/verify_icon_source.sh,
    xvue/qt/cmake/verify_abi.sh
  </files>
  <read_first>
    xvue/qt/include/xvue_qt_api.h (lines 1-60 for the proc() macro + MefistoPoint pattern),
    xvue/qt/src/xvue_qt_api.cpp (skim — 57 existing bodies; xvsouris_ body near line 953 for XVUE_QT_ASSERT_MAIN_THREAD pattern),
    xvue/qt/src/xvue_qt_event.h (BlockingDepthGuard, XvueEventBridge::Result layout),
    xvue/qt/cmake/verify_abi.sh (existing pattern; only the expected-count 57→58 and the warning-line message change),
    xvue/qt/cmake/verify_no_exec.sh (pattern for a CMake-integrated source-tree lint),
    .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-UI-SPEC.md §Copywriting (authoritative MsgId list),
    .planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-RESEARCH.md §1 (XvueMenuBridge spec), §2 (i18n skeleton), §4 (XvuePrefs API), §11 (build/CMake changes)
  </read_first>
  <behavior>
    - Test (test_xvue_qt_menu_scaffold.cpp::testScaffoldIsLinkable): instantiates one of each new class (XvueMenuBridge, XvueConsoleDock, XvueErrorBatcher, XvuePreferencesDialog, XvueRecentProjectsMenu), verifies each is alive; calls XvuePrefs::initialize("mail") without crashing; calls xvueT(MsgId::AppName) and asserts the return QString is non-empty (scaffold may return "MEFISTO" or "" — test accepts both since Plan 02 fills strings).
    - Test (test_xvue_qt_menu_scaffold.cpp::testAbiContainsModuleInit): calls xvue_module_init_ with name="mail" + len=4 — must not crash (warn-once stub OK). No further behavioral assertion (Plan 06 fills the dispatch).
    - Test (test_xvue_qt_menu_scaffold.cpp::testMenuBridgeQueueBasicOps): creates an XvueMenuBridge, queueLexicon("") — popChar returns nullopt. queueLexicon("a") (minimal string via Plan 01 stub behavior), popChar's existence is verified by call-site compile. Real behavior lands in Plan 02 — this test ASSERTS only that the function addresses resolve + no segfault.
    - No test for lint scripts here — the scripts themselves ARE the verification (exit 0 means pass).
  </behavior>
  <action>
**1. Create 9 new header/source pairs under xvue/qt/src/. Each .cpp includes `<QDebug>` and a single warn-once stub if a body would be wrong to leave empty; every class is a minimal QObject/QDialog/QDockWidget/QMenu subclass with the API surface declared but bodies that do not crash. All headers have `#pragma once` + include-guard-free style matching xvue_qt_app.h. All classes declared in headers (not .cpp) per BUILD-03.**

**1a. xvue/qt/src/xvue_qt_menu_bridge.h** — declare:
```cpp
#pragma once
#include <QObject>
#include <QQueue>
#include <QString>
#include <functional>
#include <optional>
class QMenu;
class XvueMenuBridge : public QObject {
    Q_OBJECT
public:
    explicit XvueMenuBridge(QObject* parent = nullptr);
    ~XvueMenuBridge() override;
    // Plan 02 fills body: push each char of cmd + trailing CR (13).
    void queueLexicon(const QString& cmd);
    // Plan 02 fills body: dequeue one ASCII code; nullopt if empty.
    std::optional<char> popChar();
    // Hooks populated by 6.1..6.5 per UI-SPEC §Canvas right-click.
    using ContextPopulator = std::function<void(QMenu*)>;
    void setContextMenuPopulator(ContextPopulator fn);
    void populateContextMenu(QMenu* m);
    // §10 layer 2 — registration sentinel.
    bool hasRegisteredModule() const { return moduleRegistered_; }
    void markModuleRegistered()      { moduleRegistered_ = true; }
    // T-06.0-BRIDGE-01 bound.
    static constexpr int kMaxQueueSize = 10000;
    int queueSize() const { return pendingChars_.size(); }
private:
    QQueue<char>     pendingChars_;
    ContextPopulator contextPopulator_;
    bool             moduleRegistered_ = false;
};
```

**1a body** (xvue_qt_menu_bridge.cpp) — scaffold bodies that do not crash; Plan 02 replaces queueLexicon/popChar with real implementations:
- ctor/dtor default.
- queueLexicon: no-op stub (Plan 02 fills).
- popChar: return std::nullopt.
- setContextMenuPopulator: store in field.
- populateContextMenu: if contextPopulator_ set, call it; else no-op.

**1b. xvue/qt/src/xvue_qt_i18n.h** — declare:
```cpp
#pragma once
#include <QString>
enum class MsgId : int {
    AppName,
    FileMenuTitle, FileOpen, FileOpenTip, FileSave, FileSaveTip,
    FileSaveAs, FileRecentSubmenu, FileRecentClear, FileRecentClearConfirmTitle,
    FileExport, FileQuit, FileQuitTip,
    ViewMenuTitle, ViewToolbar, ViewConsole, ViewZoomIn, ViewZoomOut, ViewFit, ViewPreferences,
    HelpMenuTitle, HelpDocumentation, HelpAbout,
    ConsoleTitle, StatusCoordFormat, ModalRefuse, ErrorMsgBoxTitle,
    QuitConfirmTitle, QuitConfirmBody,
    AboutTitle, AboutBody,
    OpenProjectDialogTitle, OpenProjectFilter,
    PreferencesTitle, PrefRecentCountLabel, PrefConsoleDefaultLabel,
    PrefColorSchemeLabel, PrefColorSchemeSystem, PrefColorSchemeLight, PrefColorSchemeDark,
    ButtonOk, ButtonCancel, ButtonApply, ButtonClose, ButtonQuit,
    DestructiveConfirmBodyGeneric,
    EmptyStateHeading, EmptyStateBody,
    _Count_
};
bool xvueIsEnglish();
const char* tr(MsgId id);
QString xvueT(MsgId id);
```

**1b body** (xvue_qt_i18n.cpp) — scaffold:
- `xvueIsEnglish()` returns false (FR default; Plan 02 wires actual $MEFISTO/td/m/anglais check).
- `tr(MsgId id)` returns `""` (Plan 02 fills the 48-row std::array<Entry> lookup table).
- `xvueT(MsgId id)` returns QString::fromUtf8(tr(id)).
- Include a `static_assert` that `static_cast<int>(MsgId::_Count_) > 40` to catch enum truncation. Plan 02 adds the exact-size static_assert once the table is filled.

**1c. xvue/qt/src/xvue_qt_prefs.h** — declare XvuePrefs static-method wrapper per RESEARCH §4:
```cpp
#pragma once
#include <QByteArray>
#include <QString>
#include <QStringList>
class XvuePrefs {
public:
    static void initialize(const char* moduleName);
    static QByteArray windowGeometry();
    static void       saveWindowGeometry(const QByteArray&);
    static QByteArray windowState();
    static void       saveWindowState(const QByteArray&);
    static bool consoleVisible(bool fallback = true);
    static void saveConsoleVisible(bool);
    static QStringList recentProjects();
    static void        pushRecentProject(const QString& absPath);
    static void        clearRecentProjects();
    static QString colorScheme();                    // "system"|"light"|"dark"
    static void    saveColorScheme(const QString&);
private:
    static QString moduleGroup_;
};
```

**1c body** (xvue_qt_prefs.cpp) — scaffold:
- initialize: store moduleName into moduleGroup_. Do NOT call QCoreApplication::setOrganizationName in the scaffold — Plan 02 adds that.
- windowGeometry/State: return empty QByteArray.
- saveWindowGeometry/State: no-op.
- consoleVisible(fallback): return fallback.
- saveConsoleVisible: no-op.
- recentProjects: return empty QStringList.
- pushRecentProject/clearRecentProjects: no-op.
- colorScheme: return "system".
- saveColorScheme: no-op.
All bodies are 1–2 lines; Plan 02 fills the real QSettings logic.

**1d. xvue/qt/src/xvue_qt_console_dock.h** — declare:
```cpp
#pragma once
#include <QDockWidget>
class QPlainTextEdit;
class QSocketNotifier;
class XvueErrorBatcher;
class XvueConsoleDock : public QDockWidget {
    Q_OBJECT
public:
    explicit XvueConsoleDock(QWidget* parent = nullptr);
    ~XvueConsoleDock() override;
    // Plan 04 fills: pipe() + dup2 + setvbuf + F_SETPIPE_SZ(1MB) + QSocketNotifier.
    void installStdoutRedirect();
    // Public for test access; appends a line + triggers auto-scroll.
    void appendLine(const QString& line);
private slots:
    void onPipeReadable();
private:
    QPlainTextEdit*   textEdit_    = nullptr;
    QSocketNotifier*  notifier_    = nullptr;
    XvueErrorBatcher* errorBatcher_= nullptr;
    QByteArray        partialLine_;
    int               readFd_      = -1;
};
```

**1d body** (xvue_qt_console_dock.cpp) — scaffold:
- ctor: sets windowTitle (empty string — Plan 04 sets xvueT(MsgId::ConsoleTitle)).
  Creates a QPlainTextEdit with readOnly(true), setMaximumBlockCount(10000), monospace font via QFontDatabase::systemFont(QFontDatabase::FixedFont).
  setWidget(textEdit_).
- appendLine: textEdit_->appendPlainText(line). Auto-scroll via moveCursor(End).
- installStdoutRedirect: no-op in scaffold (Plan 04 adds pipe+dup2+notifier body).
- onPipeReadable: no-op (Plan 04 fills).
- dtor default.

**1e. xvue/qt/src/xvue_qt_error_batcher.h** — declare:
```cpp
#pragma once
#include <QObject>
#include <QStringList>
class QTimer;
class XvueErrorBatcher : public QObject {
    Q_OBJECT
public:
    explicit XvueErrorBatcher(QObject* parent = nullptr);
    ~XvueErrorBatcher() override;
    void enqueue(const QString& errorLine);
    static constexpr int kBatchWindowMs = 500;  // UI-SPEC §Console dock
private slots:
    void flushErrorBatch();
private:
    QStringList pendingErrorLines_;
    QTimer*     timer_ = nullptr;
};
```
**1e body** — scaffold: ctor creates timer with setSingleShot(true); enqueue is no-op; flushErrorBatch is no-op. Plan 04 fills.

**1f. xvue/qt/src/xvue_qt_about_dialog.h** — declare:
```cpp
#pragma once
class QWidget;
class XvueAboutDialog {
public:
    // D-09 + UX-09 — uses QMessageBox::about() wrapper; no custom QDialog.
    static void show(QWidget* parent);
};
```
**1f body** — scaffold: XvueAboutDialog::show does nothing. Plan 04 fills.

**1g. xvue/qt/src/xvue_qt_preferences.h** — declare:
```cpp
#pragma once
#include <QDialog>
class QComboBox;
class QSpinBox;
class QCheckBox;
class XvuePreferencesDialog : public QDialog {
    Q_OBJECT
public:
    explicit XvuePreferencesDialog(QWidget* parent = nullptr);
    ~XvuePreferencesDialog() override;
private slots:
    void onAccept();
private:
    QComboBox* colorSchemeCombo_     = nullptr;
    QCheckBox* consoleVisibleCheck_  = nullptr;
    QSpinBox*  recentCountSpin_      = nullptr;
};
```
**1g body** — scaffold: ctor sets windowTitle to empty; creates empty layout; onAccept accepts. Plan 04 fills the single-panel form (recent count, console default, color scheme).

**1h. xvue/qt/src/xvue_qt_recent_projects.h** — declare:
```cpp
#pragma once
#include <QMenu>
class XvueMenuBridge;
class XvueRecentProjectsMenu : public QMenu {
    Q_OBJECT
public:
    explicit XvueRecentProjectsMenu(XvueMenuBridge* bridge, QWidget* parent = nullptr);
    ~XvueRecentProjectsMenu() override;
    void refresh();   // Rebuild actions from XvuePrefs::recentProjects
signals:
    void openProjectRequested(const QString& absPath);
private slots:
    void onClearRequested();
private:
    XvueMenuBridge* bridge_ = nullptr;
};
```
**1h body** — scaffold: ctor stores bridge; refresh is no-op; onClearRequested is no-op. Plan 04 fills (build QAction per path + "Clear Recent" + D-06 startup prune).

**1i. xvue/qt/src/xvue_qt_shortcut_audit.h** — declare:
```cpp
#pragma once
class QWidget;
class XvueShortcutAudit {
public:
    // Walks win->findChildren<QAction*>() and Q_ASSERTs no collisions.
    // Called from module-specific registerXxxActions at the end.
    static void validateNoCollisions(QWidget* win);
};
```
**1i body** — scaffold: validateNoCollisions iterates children and logs count via qInfo(). Plan 02/04 extend if needed.

**2. ABI bump 57 → 58** — add `xvue_module_init_` extern "C" entry:

**2a. xvue/qt/include/xvue_qt_api.h** — append in the same style as existing entries, at the end of the entry-declaration block before `#endif`:
```c
// -------------------------------------------------------------------
// Phase 6.0 (UX-05 foundation) — module identity hook.
// Called once per pp*_qt process by each module's prpr/pp*.f (in Phase 6.1..6.5)
// BEFORE xvinitgraphique_. Takes the module name as a Fortran CHARACTER*(*)
// so callers can pass a literal: CALL XVUE_MODULE_INIT('mail')
// Plan 06 fills the dispatch body; Plan 01 ships a warn-once stub.
// -------------------------------------------------------------------
void proc(xvue_module_init)(char* name, int* name_len);
```
This takes the ABI count from 57 to 58.

**2b. xvue/qt/src/xvue_qt_api.cpp** — add a warn-once stub body near the end of the file (in the same style as existing warn-once stubs). Include the standard `XVUE_QT_ASSERT_MAIN_THREAD` at entry. Body:
```cpp
// ---- 58. xvue_module_init_ ----
// Phase 6.0 Plan 01 scaffold: warn-once stub. Plan 06 fills the dispatch body
// that reads the module name and invokes registerMailActions/registerElasActions/etc.
void proc(xvue_module_init)(char* name, int* name_len) {
    XvueApp::ensure();
    XVUE_QT_ASSERT_MAIN_THREAD();
    static bool warned = false;
    if (!warned) {
        warned = true;
        int n = (name_len ? *name_len : 0);
        if (n < 0) n = 0;
        if (n > 32) n = 32;
        std::fprintf(stderr,
            "xvue-qt: xvue_module_init_('%.*s') called — scaffold stub; "
            "module dispatch lands in Plan 06.\n",
            n, (name ? name : ""));
    }
}
```

**3. xvue/qt/resources/xvue_icons.qrc** — create an empty-body QRC file:
```xml
<!DOCTYPE RCC>
<RCC version="1.0">
  <qresource prefix="/xvue/qt/icons">
    <!-- 6.1..6.5 append SVG icon entries here. No PNG/JPG/BMP allowed — -->
    <!-- verify_icon_source.sh enforces SVG-only. -->
  </qresource>
</RCC>
```

**4. Create xvue/qt/cmake/verify_shortcut_modifiers.sh** — mirror verify_no_exec.sh pattern:
```sh
#!/bin/sh
# verify_shortcut_modifiers.sh — fails if any plain-character QShortcut or
# QKeySequence is found in *_actions.cpp or plan 06 code. D-04 modifier rule.
# Pattern forbids: setShortcut(QKeySequence("a")), setShortcut(QKeySequence("5")), etc.
set -eu
SRC_DIR="$1"
# Grep for single-char QKeySequence / QShortcut construction without modifier.
# Accept: "Ctrl+O", "Ctrl+Shift+S", "F1", "F9", "Ctrl++", "Ctrl+-", "Ctrl+0", "Ctrl+,".
# Reject: "a", "5", ";", "o" (bare char).
HITS=$(grep -rn 'QKeySequence\("[a-zA-Z0-9;]"' "$SRC_DIR" 2>/dev/null \
    | grep -v 'Ctrl' | grep -v 'Alt' | grep -v 'Shift' || true)
if [ -n "$HITS" ]; then
    echo "ERROR: D-04 violation — bare-char QKeySequence found:" >&2
    echo "$HITS" >&2
    exit 1
fi
exit 0
```
Make executable via `chmod +x`.

**5. Create xvue/qt/cmake/verify_icon_source.sh**:
```sh
#!/bin/sh
# verify_icon_source.sh — UI-SPEC §Iconography HiDPI: SVG only under icons/.
set -eu
ICONS_DIR="$1"
if [ ! -d "$ICONS_DIR" ]; then exit 0; fi   # empty before 6.1
HITS=$(find "$ICONS_DIR" -type f \( -iname '*.png' -o -iname '*.jpg' \
         -o -iname '*.jpeg' -o -iname '*.bmp' -o -iname '*.gif' \) 2>/dev/null || true)
if [ -n "$HITS" ]; then
    echo "ERROR: only SVG icons allowed under $ICONS_DIR:" >&2
    echo "$HITS" >&2
    exit 1
fi
exit 0
```
Make executable.

**6. Edit xvue/qt/cmake/verify_abi.sh** — change the two occurrences of "57" to "58":
- line ~32: `if [ "$NM_COUNT" != "57" ]` → `if [ "$NM_COUNT" != "58" ]`
- line ~33: the warning message "expected 57 Fortran-facing entries (Planner Alert Option A)" → "expected 58 Fortran-facing entries (Phase 6.0 adds xvue_module_init_)"

**7. Edit xvue/qt/CMakeLists.txt** — extend `add_library(xvueqt STATIC ...)` target_sources with the 9 new .cpp files; add xvue_icons qt_add_resources block; add verify_shortcut_modifiers + verify_icon_source custom targets; add target_compile_options `-finput-charset=UTF-8`; wire test subdir to include menu_tests:
```cmake
add_library(xvueqt STATIC
    src/xvue_qt_api.cpp
    src/xvue_qt_app.cpp
    src/xvue_qt_canvas.cpp
    src/xvue_qt_event.cpp
    src/xvue_qt_state.cpp
    src/xvue_qt_window.cpp
    # Phase 6.0 additions (scaffold: stubs; bodies fill in Plans 02-06):
    src/xvue_qt_menu_bridge.cpp
    src/xvue_qt_i18n.cpp
    src/xvue_qt_prefs.cpp
    src/xvue_qt_console_dock.cpp
    src/xvue_qt_error_batcher.cpp
    src/xvue_qt_about_dialog.cpp
    src/xvue_qt_preferences.cpp
    src/xvue_qt_recent_projects.cpp
    src/xvue_qt_shortcut_audit.cpp
)

# Phase 6.0: empty icon resource — 6.1..6.5 append entries.
qt_add_resources(xvueqt xvue_icons
    PREFIX "/xvue/qt/icons"
    FILES   # (none — 6.1..6.5 add mesh/solve SVGs)
)

# Phase 6.0 Pitfall 2.4: UTF-8 source encoding for accented FR i18n strings.
target_compile_options(xvueqt PRIVATE -finput-charset=UTF-8)

# Phase 6.0 §10 layer 1: D-04 modifier-rule lint.
add_custom_target(verify_shortcut_modifiers ALL
    COMMAND ${CMAKE_COMMAND} -E echo "verify_shortcut_modifiers: scanning..."
    COMMAND sh ${CMAKE_CURRENT_SOURCE_DIR}/cmake/verify_shortcut_modifiers.sh
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    DEPENDS xvueqt
    VERBATIM
)

# Phase 6.0: SVG-only enforcement for custom icons (D-10 + UI-SPEC §Iconography).
add_custom_target(verify_icon_source ALL
    COMMAND ${CMAKE_COMMAND} -E echo "verify_icon_source: scanning..."
    COMMAND sh ${CMAKE_CURRENT_SOURCE_DIR}/cmake/verify_icon_source.sh
        ${CMAKE_CURRENT_SOURCE_DIR}/resources/icons
    DEPENDS xvueqt
    VERBATIM
)
```
Note: qt_add_resources with an empty FILES list is a CMake-language error on some Qt versions. To handle this, create a minimal .qrc at xvue/qt/resources/xvue_icons.qrc and reference it via `qt_add_resources(xvueqt xvue_icons PREFIX "/xvue/qt/icons" FILES resources/xvue_icons.qrc)` — OR use `qt_add_resources(xvueqt RESOURCES_FILES xvue_icons_qrc FILES resources/xvue_icons.qrc)`. Simpler: use the `.qrc`-file-based form:
```cmake
set(XVUE_ICONS_QRC ${CMAKE_CURRENT_SOURCE_DIR}/resources/xvue_icons.qrc)
target_sources(xvueqt PRIVATE ${XVUE_ICONS_QRC})
# CMake's AUTORCC picks up .qrc via target_sources; confirm AUTORCC is ON.
set_target_properties(xvueqt PROPERTIES AUTORCC ON)
```
This is Qt-idiomatic and avoids the empty-FILES issue.

**8. Edit xvue/qt/tests/CMakeLists.txt** — add xvue_qt_menu_tests target:
```cmake
add_executable(xvue_qt_menu_tests
    test_xvue_qt_menu_scaffold.cpp
    test_helpers.cpp
)
target_link_libraries(xvue_qt_menu_tests
    PRIVATE xvueqt Qt6::Test Qt6::Widgets Qt6::Gui Qt6::Core
)
target_include_directories(xvue_qt_menu_tests PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/..
    ${CMAKE_CURRENT_SOURCE_DIR}/../src
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
)
```

**9. Create xvue/qt/tests/test_xvue_qt_menu_scaffold.cpp** — mirror test_xvue_qt_event.cpp skeleton (QApplication created in main, QTEST_APPLESS_MAIN NOT used because we need QApplication for QDockWidget construction). Use an `int main` that constructs QApplication and runs `QTest::qExec(&suite, argc, argv)`. Tests:
- `testScaffoldIsLinkable`: instantiate XvueMenuBridge, XvueConsoleDock, XvueErrorBatcher, XvuePreferencesDialog; XvuePrefs::initialize("mail"); verify nothing crashed.
- `testAbiContainsModuleInit`: call `xvue_module_init_` via extern "C" declaration with a 4-char "mail" buffer; assert no crash.
- `testMenuBridgeQueueBasicOps`: XvueMenuBridge mb; QCOMPARE(mb.queueSize(), 0); QVERIFY(!mb.popChar().has_value()); mb.queueLexicon("x"); QCOMPARE(mb.queueSize(), 0); /* scaffold stub doesn't push; Plan 02 flips this assertion to QCOMPARE(size,2) for "x" + CR */ QVERIFY(!mb.popChar().has_value());
- Use `extern "C" void xvue_module_init_(char*, int*)` forward-decl in the test file.
  </action>
  <verify>
    <automated>cd /home/drico/git/mefisto && bin/cbl_tout_qt 2>&1 | tail -30 && ls -la xvue/qt/build/tests/xvue_qt_menu_tests && xvfb-run -a ./xvue/qt/build/tests/xvue_qt_menu_tests -maxwarnings 50 2>&1 | tail -20 && nm xvue/qt/build/libxvueqt.a | grep ' T xvue_module_init_$'</automated>
  </verify>
  <acceptance_criteria>
    1. `bin/cbl_tout_qt` exits 0. No warnings from gcc about missing Q_OBJECT symbols.
    2. `nm xvue/qt/build/libxvueqt.a | grep ' T [a-zA-Z_][a-zA-Z0-9_]*_$' | grep -vc ' T _Z'` returns **58** (was 57).
    3. `grep -c 'T xvue_module_init_$' <(nm xvue/qt/build/libxvueqt.a)` returns 1 (symbol present).
    4. `./xvue/qt/build/tests/xvue_qt_menu_tests` under xvfb-run exits 0; all 3 scaffold tests PASS.
    5. `./xvue/qt/build/tests/xvue_qt_event_tests` (Phase 5 suite) still exits 0 — no regression.
    6. `bash xvue/qt/cmake/verify_shortcut_modifiers.sh xvue/qt/src` exits 0.
    7. `bash xvue/qt/cmake/verify_icon_source.sh xvue/qt/resources/icons` exits 0 (dir may not exist; script handles empty).
    8. `grep -c '"58"' xvue/qt/cmake/verify_abi.sh` >= 1 (expected count updated).
    9. `bin/cbl_tout` (legacy X11) exits 0 — no damage to X11 backend (BUILD-07 preservation).
  </acceptance_criteria>
  <done>
    Scaffold plan complete. Nine new classes stub out; ABI count is 58; xvue_qt_menu_tests target builds and passes 3 sanity tests; lint scripts exist and exit 0; verify_abi.sh counts 58 (not 57); both Qt and legacy X11 builds succeed. Plans 02/03/04/05 can now execute in parallel without needing to declare headers or add CMake plumbing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fortran→C (`xvue_module_init_`) | Fortran passes a char* name + int* name_len. Length attacker-controlled if a future module supplies a malformed CHARACTER*(*); Plan 01 stub clamps to 32 bytes before printing. |
| Resource system (xvue_icons.qrc) | Compile-time `.qrc` only; no runtime filesystem load. T-06.0-RESOURCE-01 inapplicable in Plan 01 (no SVGs yet). |
| Build-time lint scripts | Shell scripts run at `cmake --build` time; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.0-01-01 | Tampering | xvue_module_init_ stub | mitigate | Clamp printed name length to max 32 chars + treat `name_len <= 0` as zero. No further processing in Plan 01 (Plan 06 adds full validation). |
| T-06.0-01-02 | DoS | Empty qrc file | accept | Empty `.qrc` files are Qt-idiomatic per `qt_add_resources` docs; no runtime cost. 6.1..6.5 add real entries. |
| T-06.0-01-03 | Tampering | Lint scripts (verify_shortcut_modifiers.sh) | accept | Scripts operate on the in-tree source only. Build-time only. No runtime surface. |
</threat_model>

<verification>
- **Scaffold complete:** 9 new src/*.{h,cpp} pairs compile, ABI=58, xvue_qt_menu_tests target builds and passes 3 sanity tests.
- **No regressions:** xvue_qt_event_tests (Phase 5) still green; `bin/cbl_tout` legacy build still green.
- **Lint scripts:** Both new scripts (verify_shortcut_modifiers, verify_icon_source) exist, chmod +x, exit 0 on current tree.
- **Downstream unblock:** Plans 02, 03, 04, 05 can import headers from 9 new files and find all declared API surfaces — no class or method resolution error in their compile step.
- **Commit discipline:** one commit per logical step (scaffold files, ABI bump, lint scripts, CMakeLists updates, test target) per CLAUDE.md git discipline.
</verification>

<success_criteria>
- [ ] 9 new `xvue/qt/src/xvue_qt_{menu_bridge,i18n,prefs,console_dock,error_batcher,about_dialog,preferences,recent_projects,shortcut_audit}.{h,cpp}` exist with stub bodies
- [ ] `xvue_qt_api.h` declares `proc(xvue_module_init)(char*, int*)`
- [ ] `xvue_qt_api.cpp` implements a warn-once stub for `xvue_module_init_`
- [ ] `nm xvue/qt/build/libxvueqt.a | grep '_$' | grep -v ' _Z' | wc -l` → 58
- [ ] `verify_abi.sh` expected count = 58; `verify_abi` custom target green
- [ ] `xvue/qt/resources/xvue_icons.qrc` exists with empty body
- [ ] `xvue/qt/cmake/verify_shortcut_modifiers.sh` exists, executable, exits 0
- [ ] `xvue/qt/cmake/verify_icon_source.sh` exists, executable, exits 0
- [ ] `xvue/qt/CMakeLists.txt` extended with 9 new sources + AUTORCC for xvue_icons + 2 new custom_target lint entries + `-finput-charset=UTF-8` compile option
- [ ] `xvue/qt/tests/CMakeLists.txt` extended with `xvue_qt_menu_tests` target
- [ ] `xvue/qt/tests/test_xvue_qt_menu_scaffold.cpp` exists with 3 sanity tests
- [ ] `bin/cbl_tout_qt` clean rebuild exits 0
- [ ] `xvfb-run ./xvue/qt/build/tests/xvue_qt_menu_tests` exits 0
- [ ] `xvfb-run ./xvue/qt/build/tests/xvue_qt_event_tests` exits 0 (Phase 5 unchanged)
- [ ] `bin/cbl_tout` (legacy X11) exits 0 (BUILD-07 preservation)
</success_criteria>

<output>
After completion, create `.planning/phases/06.0-shared-shell-menu-bridge-dialogs/06.0-01-SUMMARY.md` documenting:
- Number of new files created; final ABI count (58); xvue_qt_menu_tests sanity test pass count
- Any scaffold-body drift from what's declared here (e.g., a method whose body ended up different from the "no-op stub" spec)
- Confirmation both builds (Qt + legacy X11) still green
</output>
